{
  "name": "stellata",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "author": "Alex Marshall",
  "scripts": {
    "build:catalog": "tsx scripts/build-catalog.ts",
    "build:lut": "tsx scripts/blackbody-lut.ts",
    "build:binaries": "python3 scripts/build-binaries.py",
    "build:clouds": "python3 scripts/build-clouds.py",
    "refresh:gaia-hip": "python3 scripts/refresh-gaia-hip-xmatch.py",
    "refresh:gaia-tyc": "python3 scripts/refresh-gaia-tyc-xmatch.py",
    "refresh:gaia-nss": "python3 scripts/refresh-gaia-nss.py",
    "refresh:bailer-jones": "python3 scripts/refresh-bailer-jones.py",
    "refresh:gaia-apsis": "python3 scripts/refresh-gaia-apsis.py",
    "refresh:gaia-astrometry": "python3 scripts/refresh-gaia-astrometry.py",
    "refresh:hip2": "python3 scripts/refresh-hipparcos2.py",
    "refresh:simbad": "python3 scripts/refresh-simbad-sample.py",
    "build:local-group": "tsx scripts/build-local-group.ts",
    "build:dust-sync": "tsx scripts/sync-dust.ts",
    "build:client": "vite build",
    "build": "npm run build:catalog && npm run build:clouds && npm run build:local-group && npm run build:dust-sync && npm run build:client",
    "dev": "npm run build:catalog && npm run build:clouds && npm run build:local-group && npm run build:dust-sync && vite",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "fuse.js": "^7.0.0",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/node": "^20.10.0",
    "@types/three": "^0.160.0",
    "@vitest/coverage-v8": "^4.1.5",
    "csv-parse": "^5.5.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vite": "^8.0.10",
    "vitest": "^4.1.5",
    "wrangler": "^4.84.1"
  }
}
