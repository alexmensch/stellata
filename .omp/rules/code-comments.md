---
description: Forbidden code-comment patterns, refused as they are written
scope: "tool:edit(src/**/*.ts), tool:write(src/**/*.ts), tool:edit(src/**/*.js), tool:write(src/**/*.js), tool:edit(src/**/*.py), tool:write(src/**/*.py), tool:edit(scripts/**/*.ts), tool:write(scripts/**/*.ts), tool:edit(scripts/**/*.js), tool:write(scripts/**/*.js), tool:edit(scripts/**/*.py), tool:write(scripts/**/*.py)"
interruptMode: tool-only
condition:
  - "\\bstellata-(?![0-9]{3,}\\b)[a-z0-9]{3,5}(?:\\.\\d+)*\\b"
  - "(?<![\\w\\\\.-])(?![0-9]{3,}\\b)[a-z0-9]{3,5}(?:\\.\\d+)+\\b"
  - "(?<![\\w\\\\-])(?:pre|post|since)-(?![0-9]{3,}\\b)[a-z0-9]{3,5}\\.\\d"
  - "\\[\\[[a-z][a-z0-9-]+\\]\\]"
  - "(?i)\\b(?:see PR\\s*#\\s*\\d|extracted in PR\\b)"
---

<!-- AUTO-GENERATED by pnpm run build:omp-rules -->

This edit writes a comment pattern AGENTS.md § Code comments forbids.

Substitutions:

- Credit a bead — the commit subject, never the code. `git blame` recovers it.
- Reference a memory — nothing in code. A wikilink is invisible to any reader
  without bd.
- Cite a pull request — drop it; the history is in git.
- Decomposition history ("moved from X") — the diff already shows it.

Rewrite the comment without the reference, or delete it: the default action on
a comment that does not clearly earn its keep is delete, not reword.

Patterns, by name:

- bead-ID with stellata- prefix
- bare bead-ID (<epic>.NN[.MM…])
- bead-relative time (pre-/post-/since-<epic>.NN)
- memory-key wikilink [[name]]
- PR reference (see PR # / extracted in PR)
