# Module-docstring length allowlist — files grandfathered above the
# 1-3-line cap. New files MUST stay under the cap; the test in
# tests/code-comment-rules.test.ts fails on any file not on this list
# whose module docstring exceeds the cap.
#
# Goal: list shrinks over time. When you trim a module docstring,
# remove the entry. Adding entries is conspicuous in diff — let
# review push back.
#
# Format: one repo-relative path per line; blank lines and lines
# starting with `#` are comments.

tests/artifact-freshness.test.ts
tests/astronomy-constants-sync.test.ts
tests/cadence-layer-declarations.test.ts
tests/sid-ledger-guard.test.ts
tests/site-claims.test.ts
tests/site-css-rules.test.ts
tests/tsl-frag-depth.test.ts
tests/tsl-loop-control.test.ts
tests/webgpu-import-boundary.test.ts
