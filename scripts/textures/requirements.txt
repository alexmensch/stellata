# Dependencies for the texture build (manual-run) and the measurement
# scripts beside it. Install into the project venv:
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r scripts/textures/requirements.txt
#   .venv/bin/python scripts/textures/build-textures.py
#
# Lower bounds reflect versions verified at pin time; upper bounds are
# intentionally absent so security fixes flow through.
#
# Pillow's zlib build decides the compressed byte stream, so a different one
# rewrites the ring strips to the same pixels under a different tail — pixel
# identity, not byte identity, is what a rebuild preserves across versions.

numpy>=1.26
Pillow>=10.0
