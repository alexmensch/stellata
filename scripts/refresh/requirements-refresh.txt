# Dependencies for the Phase 1 catalogue-refresh scripts (manual-run).
# Requires Python >= 3.10 (PEP 604 `X | None` annotations throughout
# the scripts; astropy >= 6.0 also requires it).
# Install into the project venv:
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r scripts/refresh/requirements-refresh.txt
#
# Lower bounds reflect versions verified at pin time; upper bounds are
# intentionally absent so security fixes flow through.

numpy>=1.26
astropy>=6.0
pyvo>=1.5
requests>=2.31
