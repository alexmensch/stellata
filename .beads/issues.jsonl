{"_type":"issue","id":"stellata-9mm.197","title":"build-binaries: parse_gaia_{hip,tyc}_xmatch treats malformed angular_distance as best match","description":"`scripts/build-binaries.py:499` and `:516` parse the Gaia HIP/Tycho xmatch TSVs and pick the nearest match per HIP/Tyc by `angular_distance`. Both lines use:\n\n    ang = safe_float(r.get(\"angular_distance\") or \"\") or 0.0\n\nA row with a missing or malformed `angular_distance` is coerced to `0.0` — which is the most-preferred value. The row wins the tie-break and displaces any real match.\n\nThese are the foundational HIP→Gaia and Tyc→Gaia cross-walks the entire resolution chain (Stages 2-7 of stellata-dch) depends on. A swap here corrupts every downstream lookup keyed by that classical identifier.\n\n## Acceptance\n\n1. Replace `or 0.0` with `or float(\"inf\")` (corrupted = worst-possible) OR skip the row entirely: `if ang is None: continue`. Pick whichever is clearer at the call site.\n2. Unit test: feed a fixture with two rows for the same HIP — one with `angular_distance=\"\"` (malformed), one with `angular_distance=\"0.5\"`; assert the 0.5 row wins.\n\n## Smoke\n\nRe-run `build-binaries.py` on committed inputs; cardinality of `hip_to_gaia` / `tyc_to_gaia` unchanged (no malformed rows in current TSVs, so this is a future-proofing fix).","status":"closed","priority":1,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:04:32Z","created_by":"Alex Marshall","updated_at":"2026-05-19T14:58:00Z","closed_at":"2026-05-19T14:58:00Z","close_reason":"Fixed in PR #100 (commit f85effb) — parse_gaia_{hip,tyc}_xmatch now coerces malformed angular_distance to +inf so corrupted rows always lose the tie-break. Regression tests added.","labels":["pr-97"],"dependencies":[{"issue_id":"stellata-9mm.197","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-19T11:04:31Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.196","title":"refresh_lib: atomic TSV write + auto-include lib mtime in is_up_to_date","description":"Two coupled defects in `scripts/refresh_lib.py` surfaced in the retrospective on PRs 90-97. Both expose every refresh script's output to silent corruption / staleness.\n\n**H1 — `write_tsv` is not atomic.** `write_tsv` (refresh_lib.py:362) opens the output directly. Any mid-write failure (disk full, KeyboardInterrupt, an unexpected `float(None)` on a malformed cell, OOM on a large batch) leaves a partially-written TSV whose mtime is newer than the script source. The next run sees it as up-to-date and downstream silently consumes the broken file. Affects every refresh script + `build-binaries.py` (which depends on these TSVs).\n\n**H2 — `is_up_to_date` does not track `refresh_lib.py` itself.** Every refresh script passes `[Path(__file__)]` (plus inputs) as sources, but not `Path(refresh_lib.__file__)`. If we fix a bug in `coerce_masked`, change rounding in `write_tsv`, or extend `_dtype_matches`, none of the existing outputs are invalidated. Currently requires `--force` everywhere, with no enforcement.\n\n## Acceptance\n\n1. `write_tsv` writes to `output.with_suffix(output.suffix + \".tmp\")` then `os.replace()` to swap in atomically (POSIX guarantee). Test: kill mid-write fixture (write 3 rows then raise from the row generator); assert the original file is either absent or fully intact, never half-written.\n2. `is_up_to_date` auto-includes `refresh_lib`'s own `__file__` in the sources iterable (or a `make_is_up_to_date(extra)` wrapper does so). Test: touch `refresh_lib`'s source forward, assert `is_up_to_date` returns False without callers having to list it explicitly.\n3. All 7 refresh scripts re-run `--force` end-to-end; resulting TSVs byte-identical to before.\n\n## Smoke\n\nForce-rebuild one TSV (e.g. `gaia_dr3_hip_xmatch.tsv`) via `--force`; diff against the previous committed version → expected: byte-identical.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:04:25Z","created_by":"Alex Marshall","updated_at":"2026-05-19T16:12:01Z","started_at":"2026-05-19T15:07:25Z","closed_at":"2026-05-19T16:12:01Z","close_reason":"Closed","labels":["pr-88","pr-90","pr-91","pr-92","pr-95"],"dependencies":[{"issue_id":"stellata-9mm.196","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-19T11:04:25Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.47","title":"Phase 3 — build-catalog.ts: Bailer-Jones distance override for stars with Gaia DR3 source_id","description":"# Why\n\nWire the B-J distance posteriors (from dch.46) into `scripts/build-catalog.ts` so every AT-HYG star with a Gaia DR3 source_id picks up its B-J distance in place of the naive 1/π value. See dch.46 for the underlying motivation (parallax inversion fails catastrophically for low-S/N parallaxes; a handful of luminous distant stars currently render outside the galactic-disc wireframe).\n\nThe integration is principled and self-targeting: B-J's photogeometric posterior dominates the prior at high parallax S/N (so well-measured nearby stars don't move), and collapses onto the Galactic-density prior at low S/N (so 14-kpc parallax-inversion artifacts are pulled back to plausible distances). Per [[stellata-data-fidelity-principle]] — no per-star carve-outs, no famous-star tier; uniform across the catalog.\n\n# Scope (one commit/PR)\n\nIn `scripts/build-catalog.ts`:\n\n1. Load `data/bailer-jones-dr3.tsv` alongside existing AT-HYG load. Build `Map\u003cgaia_source_id, distance_pc\u003e` keyed on source_id with `r_med_photogeo` (preferred) || `r_med_geo` (fallback when photogeo missing for a row).\n2. For each AT-HYG row with non-empty `gaia` AND a B-J entry:\n   - Override `dist` with the B-J value.\n   - Recompute `x0`, `y0`, `z0` from the original `ra`/`dec` and the new `dist` (standard ICRS spherical → Cartesian, same convention AT-HYG uses).\n   - Recompute `absmag` from the original `mag` and the new `dist` (`mag − 5·log10(dist/10)`) so disc/glow sizing in `star-physics.ts` stays consistent with placement. **This is the one footnote that's easy to miss** — without it, stars get placed at the new distance but lit for the old one.\n   - Set `dist_src = \"BJ\"` (or extend existing enum; pick a value that distinguishes from `G_R3`).\n3. Stars with no `gaia` column OR no B-J entry retain AT-HYG's values unchanged. No NaN risk.\n4. Log override count + percentage at build time (per [[stellata-test-coverage-discipline]] tier-2 statistics output).\n5. Update `build-catalog-expected.json` (created in earlier phases per the dch architecture) with the new statistical bounds.\n6. Pure helpers (ICRS spherical→Cartesian, absmag formula) lift to module scope or a `*-pure.ts` sibling so they are testable, per [[stellata-test-coverage-discipline]].\n\n**No binary format change.** `dist`, `x0/y0/z0`, `absmag` are existing fields. The v6 binary format from dch.33 is unaffected; both beads can land independently within Phase 3.\n\n# Acceptance criteria\n\n- Build runs to completion; `public/catalog.bin` regenerates.\n- Log line shows ≥ 90% override coverage (close to B-J's coverage of AT-HYG.gaia).\n- **Three** of the four catastrophic outliers see `dist` reduce by ≥ 25% relative to current AT-HYG value:\n  - HIP 22365 (9963 → 6245 pc, 37% drop)\n  - HIP 25733 (14327 → 5466 pc, 62% drop)\n  - HIP 38430 (12658 → 6215 pc, 51% drop)\n  - HIP 46144 (9190 → 7515 pc, **18% drop only** — flag=10022 vs the others' 10033, lower posterior S/N).\n  Pin each numerically with `toBe(...)` on the B-J map value in tier-A vitest. HIP 46144 gets its own assertion at the 15-20% band rather than the 25% threshold.\n- HIP 23785 (well-measured F-dwarf at 93 pc): `|Δdist| / dist \u003c 5%` — no degradation for good measurements.\n- Internal consistency post-override: `sqrt(x0² + y0² + z0²) ≈ dist` to float32 precision; `mag − 5·log10(dist/10) ≈ absmag`.\n- Pure helpers tested as `*-pure.ts` siblings; constants imported, not redefined.\n\n# Smoke\n\nAfter build:\n- `npx tsx scripts/verify-catalog.ts` — confirm row count unchanged, header layout unchanged.\n- Run dev server, focus or warp to HIP 25733 — confirm it pulls back to a plausible disc-residing position.\n- Confirm HIP 23785 visually unchanged.\n\n# Blocked by\n\ndch.46 — refresh-bailer-jones.py producing `data/bailer-jones-dr3.tsv`.\n\n# Connects\n\n[[stellata-data-fidelity-principle]] — Bayesian distances for every catalog star with a Gaia ID, uniformly.\n[[stellata-rename-and-stale-prose-sweep]] — `dist_src` enum gains a new value; sweep SCIENCE.md + docs at commit time.\n[[stellata-test-coverage-discipline]] — numeric headlines (override count, the four named outliers' new distances) pinned with `toBe(...)`.\nSibling Phase 3 beads dch.34 (GCVS cross-match) and dch.35 (CCDM cross-match) are independent — same phase but separate `build-catalog.ts` change points.","acceptance_criteria":"- Build completes; public/catalog.bin regenerates without errors.\n- Override coverage ≥ 90% logged at build time.\n- HIP 22365 / 25733 / 38430 / 46144 distances drop ≥ 25% (tier-A vitest with toBe).\n- HIP 23785 distance unchanged within 5% (regression guard for well-measured stars).\n- x0²+y0²+z0² ≈ dist² and absmag = mag − 5·log10(dist/10) hold post-override.\n- Pure helpers extracted to *-pure.ts; tests import constants, never redefine.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-17T16:19:23Z","created_by":"Alex Marshall","updated_at":"2026-05-19T08:11:57Z","started_at":"2026-05-18T21:29:22Z","closed_at":"2026-05-19T08:11:57Z","close_reason":"Merged as PR #98 (commit cac044b). B-J override fires for 99.5% of Gaia-bearing rows; +15 stars rescued from prior \u003e50kpc filter. Documented transient LMC regression — dch.8l0 is the lockstep partner queued next.","labels":["catalog","data-pipeline","distance"],"dependencies":[{"issue_id":"stellata-dch.47","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-17T17:19:22Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.47","depends_on_id":"stellata-dch.46","type":"blocks","created_at":"2026-05-17T17:19:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.46","title":"Phase 1 — scripts/refresh-bailer-jones.py + commit data/bailer-jones-dr3.tsv","description":"# Why\n\nAT-HYG's `dist` is computed by naive 1/parallax (no Bayesian prior, no Lindegren zero-point correction). This catastrophically inverts low-S/N small parallaxes: ~20% of Gaia DR3 stars have negative measured parallaxes (no defined 1/π), and stars with parallax comparable to its own error get distances biased systematically long. Result in Stellata: a small population of distant luminous stars (OB supergiants etc.) renders outside the galactic-disc wireframe at 10-15 kpc when their true distances are more like 3-6 kpc. Concrete examples: HIP 25733 (cat: 14.3 kpc, gal R 22 kpc), HIP 22365, HIP 38430, HIP 46144 all sit visibly outside the 15 kpc midplane ring.\n\nBailer-Jones et al. (2021, AJ 161, 147; VizieR I/352) publishes Bayesian distance posteriors for every Gaia DR3 source by combining the Gaussian parallax likelihood with a direction-dependent Galactic-density prior. Two flavours per star:\n\n- `r_med_geo` (geometric) — prior is direction-dependent Galactic 3D density model.\n- `r_med_photogeo` (photogeometric) — adds BP/RP + apparent G via a CMD prior. Most informative.\n\nThe method handles negative parallaxes naturally (posterior collapses onto prior), self-targets the noisy-parallax regime (high-S/N stars are near-no-op since the likelihood dominates), and applies the Lindegren parallax zero-point correction upstream. This is the principled fix for the \"outside the disc\" outlier class — see [[stellata-data-fidelity-principle]] for the bar.\n\n# Scope (one commit/PR)\n\n1. New script `scripts/refresh-bailer-jones.py`. Reuses the shared TAP client from dch.21 (`scripts/refresh_lib.py`), configured CDS-only (see \"Backend\" below). Reads AT-HYG.gaia source_ids from `data/athyg_33_classic_ids.csv` and queries VizieR TAP (`I/352/gedr3dis`) with a batched `WHERE \"Source\" IN (...)` clause. Empirical batch sweet spot on CDS: 5000 source_ids per batch (~80 s per batch, superlinear above that).\n2. Output schema (TSV): `source_id`, `r_med_geo`, `r_lo_geo`, `r_hi_geo`, `r_med_photogeo`, `r_lo_photogeo`, `r_hi_photogeo`, `flag`. The script renames VizieR's case-sensitive columns (`Source`, `rgeo`/`b_rgeo`/`B_rgeo`, `rpgeo`/`b_rpgeo`/`B_rpgeo`, `Flag`) to the paper's names on write — output is self-documenting against the Bailer-Jones 2021 reference.\n3. Commit `data/bailer-jones-dr3.tsv` (LFS — expected ~15-20 MB for ~300k rows × 8 cols).\n4. Per [[frozen-external-data]]: script is manual-run, NOT in `npm run build`. Idempotency via `refresh_lib.is_up_to_date`. Total refresh runtime ~85 minutes against CDS.\n5. Document in the same `scripts/README.md` (or equivalent) used by sibling Phase 1 refresh scripts (dch.22-25, dch.29).\n\n# Backend (not ESA→CDS)\n\nEarlier draft of this bead said \"Script supports the same TAP-fallback (ESA → CDS) behaviour as the dch.21 shared client.\" That is WRONG — I/352/gedr3dis is a VizieR-only table; the ESA Gaia archive returns a 500 with no response context and refresh_lib does not classify it as transient, so the default ESA→CDS fallback never triggers. The script passes `backends=[cds_backend]` to `TapClient` explicitly. The refresh_lib API supports this directly (custom `backends=` arg) — no library change needed.\n\n# Coverage expectation\n\n≥ 90% of AT-HYG.gaia source_ids should resolve to a B-J row. The ~10% gap is mostly very bright HIP stars Gaia couldn't measure (same coverage cliff Gaia's own astrometry hits in dch.29). The build-catalog integration (next bead) falls back to AT-HYG's value for these — no NaN risk. Empirical probe on the first 5000 AT-HYG source_ids returned 4935 rows (98.7%) — well above the ≥90% bar.\n\n# Blocks\n\nbuild-catalog.ts B-J distance override (next bead, depends on this output file).\n\n# Connects\n\n[[stellata-data-fidelity-principle]] — Bayesian-distance fix is exactly the \"systematic data engineering, not famous-star carve-outs\" approach the principle calls for.\n[[frozen-external-data]] — committed-file + manual-refresh pattern.\n[[stellata-bd-operations]] — refresh-script naming + manual cadence matches Phase 1 sibling beads.","acceptance_criteria":"- Script runs end-to-end against VizieR TAP; produces non-empty TSV with the documented schema.\n- ≥ 90% of AT-HYG.gaia source_ids resolve to a B-J row.\n- Spot-check HIP 25733 (Gaia source_id present in AT-HYG): r_med_photogeo \u003c AT-HYG dist (14327 pc) by ≥ 25%.\n- Spot-check HIP 23785 (well-measured nearby F-dwarf): |r_med_photogeo − AT-HYG dist| / dist \u003c 5%.\n- Output file is committed and LFS-tracked.\n- Script supports the same TAP-fallback (ESA → CDS) behaviour as the dch.21 shared client.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-17T16:18:44Z","created_by":"Alex Marshall","updated_at":"2026-05-18T17:22:24Z","started_at":"2026-05-18T10:56:22Z","closed_at":"2026-05-18T17:22:24Z","close_reason":"Closed","labels":["catalog","data-pipeline","distance"],"dependencies":[{"issue_id":"stellata-dch.46","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-17T17:18:43Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.46","depends_on_id":"stellata-dch.21","type":"blocks","created_at":"2026-05-17T18:04:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-8l0","title":"Kinematic distance refinement for LMC supergiants in AT-HYG","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic stellata-dch — this bead is Phase 3 Bead AA: LMC kinematic distance refinement layer).\n\n# Goal\n\nReplace Gaia DR3 parallax-inversion distances with the LMC eclipsing-binary distance (49.59 kpc, Pietrzyński 2019) for AT-HYG stars that pass a kinematic + sky-cone filter identifying them as LMC members. Affects ~60 stars currently misrendered as a \"line of stars between MW and LMC in the intergalactic void\".\n\n# Why\n\nDiagnosed during stellata-38m PR (LG wireframes) review. In the LMC sky direction (RA 4-6 h, Dec −65 to −75°) AT-HYG carries:\n\n- 2551 stars at \u003c 5 kpc — bonafide MW foreground (correct).\n- **31 stars at 5-30 kpc** — appears as \"intergalactic stars\".\n- 18 stars at 30-60 kpc — LMC main body.\n- 11 stars at \u003e 60 kpc, some pushed to 196 kpc.\n\nAll 60 stars beyond 5 kpc show:\n- Proper motion ~(+1.9, +0.3) mas/yr — matches LMC bulk motion (van der Marel 2002) to within scatter.\n- HDE numbers in 268xxx-271xxx range — the Henry Draper Extension's LMC concentration.\n- B-supergiant spectral types where present (e.g. HD 268749 \"B 7 IAB\" at apparent 13 kpc).\n\nThey're all really at the LMC distance. Gaia DR3 parallax at d=50 kpc is 0.02 mas with per-source uncertainty 0.02-0.05 mas — SNR ≈ 1, so parallax-inversion smears distances over an order of magnitude. Real-world LMC publications use eclipsing binaries / Cepheids; AT-HYG's distance-inversion path is the wrong tool at this scale.\n\nSpecific to LMC because SMC and Sgr supergiants are too faint to enter AT-HYG's brightness cut (G ~10-12.5). LMC's brightest B-supergiants happen to land just inside.\n\n# Ordering with Phase 3 Bead Z (Bailer-Jones distance override)\n\n**This bead runs AFTER stellata-dch.47 (Bead Z, Bailer-Jones distance override).** B-J first fixes catastrophic 1/π inversion across the bulk catalog; then this bead overrides LMC-cone stars (~60 entries) to Pietrzyński 2019's 49.59 kpc. Order matters: if B-J ran second, it would clobber the kinematic value because LMC stars have Gaia source_ids and would be in the B-J data file.\n\n**Why B-J alone doesn't fix this**: B-J's prior is a smooth Galactic-density model; the LMC isn't in the prior. B-J posteriors for LMC stars peak somewhere intermediate (5-20 kpc), not at 49.59 kpc. The kinematic sky-cone + PM filter is the principled \"solve the class\" fix per stellata-data-fidelity-principle. Soft awareness link to dch.47 is wired via `bd dep add stellata-8l0 stellata-dch.47 --type=related`.\n\nWithout this bead, Bead Z (dch.47) alone introduces a transient LMC regression vs today's behaviour — the two beads ship in lockstep. Priority bumped from P2 → P1 to reflect this coupling.\n\n# Approach\n\nIn `scripts/build-catalog.ts`, after the B-J distance override (Bead Z) and before the `MAX_DIST_PC = 50_000` cutoff (`scripts/build-catalog.ts:47`):\n\n1. Test sky position: is the star inside an LMC cone (centre RA 78.76°, Dec −69.19°, half-angle ~15°)?\n2. Test proper motion: is `(pmra, pmdec)` within `±0.5 mas/yr` of `(+1.85, +0.20)`?\n3. If both pass, override the distance to the LMC's eclipsing-binary distance (49.59 kpc, Pietrzyński 2019). Recompute x0/y0/z0 from (ra, dec, distance_override). Recompute absmag from (mag, distance_override).\n4. Tag the row's `dist_src` as `LMC_KIN`, joining the enum `{HIP, GJ, T, N, G_R3, BJ, LMC_KIN}` so downstream consumers (e.g. Bead AB / dch.45's CCDM optical-double filter) can identify kinematically-refined entries.\n\nParameters (cone angle, PM tolerance, target distance) live as named constants in `catalog-pure.ts` so they're testable + tunable + extensible to future populations (SMC and Sgr will graduate into this regime when DR4 lands or AT-HYG goes deeper).\n\n# Cutoff role (clarified)\n\nThe `MAX_DIST_PC = 50_000` cutoff (`scripts/build-catalog.ts:47`) stays in place AFTER this bead's override is applied. Its role becomes a **bounded-scope safety gate**, NOT a primary include/exclude filter:\n\n- Stars refined to 49.59 kpc by this bead survive the cutoff (just inside).\n- Stars still \u003e 50 kpc post-refinement (no kinematic match in any modelled population) are dropped as unmodelled extragalactic — out of scope for v1.\n- The cutoff value is tuned exactly to the modelled scope. When SMC/Sgr/M31 refinement layers land, bump the cutoff accordingly.\n\nDocumentation update lives in dch.42 (SCIENCE.md rewrite) — the SCIENCE.md:98-99 cutoff prose gets reframed from \"safety net for catalog noise\" to \"bounded-scope final gate after refinement layers.\"\n\n# Acceptance\n\n- `npm run build:catalog` runs cleanly.\n- A new test in `catalog-pure.test.ts` exercises the kinematic filter:\n  - LMC-direction + LMC-PM star at parallax-inversion distance 13 kpc → distance overridden to 49590 pc.\n  - LMC-direction + NON-LMC-PM star (e.g. high-velocity halo star) → distance unchanged.\n  - NON-LMC-direction + LMC-like PM (e.g. Solar Neighbourhood star with similar PM) → distance unchanged.\n  - SMC-direction + SMC-PM star (if any) → distance unchanged (no SMC handler yet).\n- Ordering test: a synthetic LMC-cone + LMC-PM star with a B-J entry confirms LMC_KIN wins over BJ (this bead runs after Bead Z).\n- `dist_src = \"LMC_KIN\"` and absmag recomputed (`mag − 5·log10(dist/10)`) on every overridden row.\n- Verify post-build: query the catalog for stars in the LMC cone at non-LMC distance → expect 0 (or only halo K-giants that pass the PM cut should fail it; ~0 expected in this region).\n- Visual smoke (stellata-38m PR's LMC wireframe should look cleaner): no \"smear\" of stars stretched between 5-200 kpc in the LMC direction; instead a concentrated 50 kpc shell.\n- Tier-A vitest under Bead R covers 2-3 LMC B-supergiants from the HDE 268xxx range (added to corpus in Bead Q / dch.37).\n\n# Why this approach (vs alternatives)\n\n- **Filter (drop the rows)**: throws information away. We KNOW these are LMC stars; deletion is worse than refinement.\n- **Accept (do nothing)**: faithful to Gaia DR3 but the visual is wrong and stellata-data-fidelity-principle prefers fixed-at-build to user-confusing-at-runtime.\n- **Hand-curated overrides per star**: solves the visible exemplars, not the class — fails when AT-HYG / DR4 grows. Wrong per stellata-data-fidelity-principle.\n- **Rely on B-J alone**: B-J's Galactic-density prior doesn't cover the LMC; posteriors land somewhere intermediate (5-20 kpc), not at 49.59 kpc. B-J handles the bulk-catalog catastrophic-inversion problem; LMC kinematic refinement is the LMC-specific second layer.\n\nKinematic + sky-cone filter is the \"solve the class\" approach: scales to future catalogs, applies to SMC / Sgr / M31 when their populations enter the brightness regime, and uses well-established LMC kinematics from the literature.\n\n# References\n\n- van der Marel \u0026 Kallivayalil 2014, ApJ 781, 121 — LMC bulk PM\n- Pietrzyński et al. 2019, Nature 567, 200 — LMC distance (eclipsing binaries; 49.594 ± 0.55 kpc)\n- Gaia Collaboration et al. 2021 — DR3 parallax uncertainty floor\n- Bailer-Jones et al. 2021, AJ 161, 147 — Bayesian distance posteriors (the layer this bead runs on top of)\n\n# Out of scope\n\n- SMC / Sgr / other Magellanic-system or Local Group populations — they're not in AT-HYG depth yet. Same approach extends when they appear (file a sibling kinematic-override bead following this bead's pattern; bump the cutoff as each new modelled population is added).\n- Magellanic Stream stars — too faint for AT-HYG.\n- Hypervelocity stars and runaways genuinely at intermediate distances — assume they fail the LMC-PM test (different velocities). The kinematic filter must be tight enough to leave them alone.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T11:40:25Z","created_by":"Alex Marshall","updated_at":"2026-05-19T09:48:31Z","started_at":"2026-05-19T08:16:02Z","closed_at":"2026-05-19T09:48:31Z","close_reason":"LMC kinematic distance refinement landed in PR #99 (squash commit d3af8f5, v2.1.0). 54 / 4727 LMC-cone stars snapped to Pietrzyński 2019's 49.594 kpc. Also folded in the dch.47 dist_src gate fix (HIP / GJ / N / OTHER rows no longer over-eagerly B-J-overridden). Six follow-up beads filed and wired: dch.54 (Dec PM widen), dch.55 (SMC sibling), dch.56 (LMC structural-disc fallback), dch.57 (SIMBAD + self-consistency QA), dch.58 (OGLE Cepheid cross-match), epic mju (Magellanic stellar-population overlay).","labels":["build-pipeline","catalog","gaia","local-group"],"dependencies":[{"issue_id":"stellata-8l0","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-17T18:04:43Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8l0","depends_on_id":"stellata-dch.47","type":"related","created_at":"2026-05-17T18:04:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2br.5","title":"Mid-Fly recentre + pin-guard relax for navigate-mode warp arrivals","description":"Smoke-discovered regression on PR #66. The cubic-Hermite log-distance Fly profile exposes a latent Float32 precision fault in navigate-mode warp arrivals: while the camera is in the source-star's local frame and gets within ~ULP(|B|) of the destination, lookAt(B) per frame jitters as B − camera.position loses precision. For a 200pc warp ending at 1 AU, ULP(|B|) ≈ 2.4e-5 pc ≈ 5 AU, and the cubic-Hermite log-d profile spends ~last 19% of Fly inside that chaos zone (vs ~0.0001s under the old piecewise-quadratic, which the slam hid).\n\nTwo visible symptoms, single root cause:\n1. **Sol off-screen on arrival, jumps in at finishWarp.** lookAt jitter occasionally lands the destination outside the NDC frustum; finishWarp's setFocus → recenterOrigin → target.snap(0,0,0) drops the destination at local (0,0,0), the pin engages, snap to centre.\n2. **Discrete-stage jitter on approach.** The lookAt quaternion stepping across Float32 representations of B.\n\nSame class as stellata-fqw (observe→observe Phase 3), already fixed there by pulling recenterFocusToStar forward into Phase 3 start. This bead extends the same convention to navigate-mode Fly.\n\n# Fix\n\n1. Add `shiftArrivalWaypoints(state, dx, dy, dz)` to camera-motion.ts. Shifts ArrivalState's pStart / pEnd / target.center in place. d0 / dEnd / dir are translation-invariant, no recompute. Sibling to shiftWarpWaypoints in warp-pure.ts.\n\n2. Add `recenteredToDest: boolean` to WarpState (init false in startWarp).\n\n3. updateWarp Fly branch: after tickArrival, if `!state.recenteredToDest \u0026\u0026 state.destKind === 'star'` and the camera is past the trajectory midpoint, call recenterFocusToStar(destIdx), shiftWarpWaypoints + shiftArrivalWaypoints with the returned delta, set recenteredToDest = true. lookAt(B) from then on becomes lookAt(0,0,0) — numerically clean.\n\n4. Relax isPinEngaged: `!warpState` → `!warpState || warpState.recenteredToDest`. After mid-Fly recentre, focusedStar = destination, camera is doing lookAt(B) per frame (= lookAt(local origin)), and controls.target.lengthSq() \u003c threshold (target was snapped by setFocus's recentre). All three pin preconditions are restored. The shader-pin then bypasses any residual Float32 projection-chain noise via uPinFocusToCenter = projectionMatrix * vec4(0, 0, -dPc, 1).\n\n5. finishWarp navigate-arrival branch: if state.recenteredToDest, focusedStar already === destIdx so setFocus(destIdx) would short-circuit (line 1436-1438) and never emit 'focus' / 'state'. Detect early-recentre and emit them explicitly.\n\n# Scope decisions\n\n- Trigger: \"camera past trajectory midpoint\" — projection of (camera − pStart) onto (pEnd − pStart) \u003e 0.5 · |pEnd − pStart|. Matches alex's stated convention (\"source star behind camera\"). Fires roughly at u_eased = 0.5, comfortably before the chaos zone (which starts at u_eased ≈ 0.66 for the 200pc → 1 AU case).\n- Star arrivals only. Cloud arrivals don't recentre origin (setFocusedCloud doesn't call recenterOrigin), so the same fault exists for cloud warps but needs its own infrastructure. File as follow-up.\n\n# Acceptance\n\n- Manual smoke (alex): Betelgeuse focus from 200 pc — no off-screen / no discrete-stage jitter. Warp Sol → Betelgeuse — same. Cloud focus — unchanged (out of scope).\n- Unit: shiftArrivalWaypoints translation test + invariance of d0/dEnd/dir.\n\n# Closes on merge\n\nstellata-2br PR #66 (umbrella).","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T08:59:47Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:01:29Z","started_at":"2026-05-16T09:00:13Z","closed_at":"2026-05-16T21:01:29Z","close_reason":"shipped in PR #66 (commit 25e4b8b)","labels":["camera","pr-66","ux"],"dependencies":[{"issue_id":"stellata-2br.5","depends_on_id":"stellata-2br","type":"parent-child","created_at":"2026-05-16T09:59:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.44","title":"Phase 6 — CLAUDE.md module roster + Local Commands + docs/build-and-data.md + RELEASING.md sweep","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nFinal documentation update tying everything together. Depends on stellata-dch.42 (SCIENCE.md) + stellata-dch.43 (docs/cross-match.md).\n\n# Per memory defer-doc-updates AND stellata-rename-and-stale-prose-sweep\n\nThis is the place to catch every stale reference to the OLD pipeline (build-binaries.py shape, refresh-gaia-binaries.py, gaia_dr3_binaries.tsv, position-based filtering language) and update to the new state.\n\n# Deliverables\n\n- CLAUDE.md updates:\n  - Module roster — add: scripts/refresh_lib.py, scripts/refresh-gaia-hip-xmatch.py, scripts/refresh-gaia-tyc-xmatch.py, scripts/refresh-gaia-astrometry.py, scripts/refresh-gaia-nss.py, scripts/refresh-hipparcos2.py, scripts/refresh-simbad-sample.py, **scripts/refresh-bailer-jones.py**, **scripts/refresh-gaia-apsis.py**, scripts/validate-simbad-sample.py, scripts/known-stars.tsv, scripts/requirements-refresh.txt.\n  - Module roster — remove or update: scripts/refresh-gaia-binaries.py (deleted), data/gaia_dr3_binaries.tsv (deleted), data/multiples-overrides.tsv (kept but shrunken).\n  - Local Commands — add: npm run refresh:gaia-hip, refresh:gaia-tyc, refresh:gaia-astrometry, refresh:gaia-nss, refresh:hip2, refresh:simbad, **refresh:bailer-jones**, **refresh:gaia-apsis**, validate:simbad.\n  - Documentation index — add docs/cross-match.md row with reading-time + scope.\n  - Data layout — update data/ tree to list new committed files (gaia_dr3_hip_xmatch.tsv, gaia_dr3_tyc_xmatch.tsv, gaia_dr3_astrometry.tsv, gaia_dr3_nss_two_body.tsv, hip2_van_leeuwen.tsv, simbad_sample.tsv, **bailer-jones-dr3.tsv**, **gaia_dr3_apsis.tsv**).\n\n- docs/build-and-data.md updates:\n  - Add Layer 1 (committed reference data) sub-section with table of every external file (including data/bailer-jones-dr3.tsv and data/gaia_dr3_apsis.tsv).\n  - Add Layer 2 (refresh scripts) sub-section with the manual-run protocol per frozen-external-data.\n  - Add venv setup steps: python3 -m venv .venv \u0026\u0026 .venv/bin/pip install -r scripts/requirements-refresh.txt.\n  - v6/v7 binary format documented (record byte layout, gaia_source_id field plus Apsis fields if dch.49 lands inline or as a v7 bump).\n  - Build pipeline diagram updated to show multi-layer distance refinement (AT-HYG 1/π → B-J → LMC kinematic → MAX_DIST_PC gate).\n\n- RELEASING.md updates:\n  - Note that catalogue refresh is a MANUAL pre-release step (not in npm run build).\n  - Document the cadence (no rule; refresh on DR4 / AT-HYG / Bailer-Jones releases — typically in lockstep when Gaia drops a new DR).\n  - Document the version-bump policy for catalogue refreshes (likely minor if numbers change visibly, patch if just data refresh).\n\n- Cross-references swept: ensure no remaining references to refresh-gaia-binaries.py, gaia_dr3_binaries.tsv, the dch.18 manual-upload procedure, the old position-based cascade.\n\n# Acceptance\n\n- grep -rn 'refresh-gaia-binaries\\|gaia_dr3_binaries\\.tsv\\|dch.18 manual' CLAUDE.md SCIENCE.md docs/ RELEASING.md returns no hits (old refs swept).\n- New Local Commands all listed (incl. refresh:bailer-jones).\n- Module roster matches current scripts/ + data/ tree (verify with ls + diff). Both refresh-bailer-jones.py + bailer-jones-dr3.tsv listed; refresh-gaia-apsis.py + gaia_dr3_apsis.tsv listed.\n- v6/v7 binary format documented (current as-built).\n- Multi-layer distance-refinement pipeline shown in the build-pipeline diagram in docs/build-and-data.md.\n\n# Smoke\n\n- npm run typecheck (no .md changes affect TS, but defensively verify).\n- Read CLAUDE.md cold — verify the project overview still makes sense + reflects current state.\n\n# Files to touch\n\n- CLAUDE.md\n- docs/build-and-data.md\n- RELEASING.md\n\n# Commit\n\nOne commit. PR labelled skip-version-bump (docs-only).\n\n# After this lands\n\nThis is the FINAL Phase 6 bead. After merge, stellata-dch.10 (BinaryOrbitField runtime, existing) is unblocked and runs Phase 7. The epic stellata-dch can be marked complete once dch.10 ships.","status":"open","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:23:28Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:30:09Z","labels":["docs","phase-6"],"dependencies":[{"issue_id":"stellata-dch.44","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:23:28Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.44","depends_on_id":"stellata-dch.42","type":"blocks","created_at":"2026-05-15T11:25:13Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.44","depends_on_id":"stellata-dch.43","type":"blocks","created_at":"2026-05-15T11:25:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.43","title":"Phase 6 — docs/cross-match.md: developer walk-through of source-ID resolution","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nAuthoring of a new developer-facing doc that walks through the catalogue cross-match logic in detail. Complements SCIENCE.md (which is for the astronomer audience): this doc is for the engineer who needs to extend the pipeline, refresh data when DR4 lands, or debug a resolution failure for a specific star. Depends on Phase 2 + Phase 3 beads landed.\n\n# Per memory defer-doc-updates\n\nAuthored AFTER the code lands. Reading the final implementation, then documenting it.\n\n# Deliverables\n\n- docs/cross-match.md (new, ~10-minute read) — sections:\n\n  ## When to read this doc\n  - You're refreshing external catalogs (e.g. Gaia DR4 landed); see also: docs/build-and-data.md.\n  - You're adding a known-star to the validation corpus.\n  - You're debugging why a specific star doesn't render correctly.\n\n  ## The 5-tier resolution chain\n  - Each tier explained: what it does, when it's used, what failure mode it handles.\n  - Worked examples: Sirius A (orb6_hip), Proxima (athyg_gaia_native), a faint Tycho-only star (position_pm), an unmatchable star (unresolved).\n\n  ## Astrometry routing\n  - gaia_5p / gaia_nss_systemic / hip2_long_baseline: when each fires, why each is correct in its regime.\n  - The bright-binary-contamination problem and how the cascade handles it.\n\n  ## Orbital element selection\n  - NSS \u003e ORB6 \u003e ORB6_spectroscopic \u003e none priority.\n  - Conversion from NSS / ORB6 published units to canonical v5 schema.\n\n  ## Multi-layer distance-refinement stack (NEW section, drives Beads Y + Z + AA)\n  - **Layer 1 — AT-HYG 1/π baseline**: today's behaviour. Catastrophically wrong for low-S/N parallaxes.\n  - **Layer 2 — Bailer-Jones (2021) Bayesian override** (Bead Z / dch.47): `r_med_photogeo` ‖ `r_med_geo`. Direction-dependent Galactic prior + CMD prior. Self-targets noisy regime; near-no-op for high-S/N stars. Applies Lindegren et al. (2021) zero-point upstream. **Known limitation**: B-J's Galactic-density prior doesn't cover the LMC; LMC supergiants get pulled to intermediate-wrong distances.\n  - **Layer 3 — LMC kinematic + sky-cone override** (Bead AA / stellata-8l0): sky-cone (LMC centre, half-angle) + PM gate identifies LMC members; distance overridden to Pietrzyński 2019's 49.59 kpc. `dist_src = \"LMC_KIN\"`.\n  - **Ordering constraint** (CRITICAL): B-J runs first (Bead Z), LMC kinematic runs second (Bead AA). If reversed, B-J clobbers the kinematic override because LMC stars have Gaia source_ids. Encoded as a `--type=related` link in bd.\n  - **Layer 4 — `MAX_DIST_PC = 50_000` bounded-scope safety gate** (`scripts/build-catalog.ts:47`): applied LAST. Drops anything still beyond LMC as unmodelled extragalactic. Cutoff is a function of what populations are modelled — NOT a primary include/exclude filter.\n  - **Extending to SMC/Sgr/M31**: when those populations enter AT-HYG depth (DR4 or AT-HYG deepening), file a sibling kinematic-override bead following Bead AA's pattern. Bump the cutoff as each new modelled population is added.\n  - **Stars not covered**: rows without a Gaia source_id retain AT-HYG values (1/π); flagged with the original `dist_src` (`HIP`, `GJ`, etc.).\n\n  ## Astrophysical parameters (Apsis) per star (NEW section, drives Beads BB + CC — `dch.48` + `dch.49`)\n  - **What Apsis is**: Gaia DR3 publishes two parallel astrophysical-parameter pipelines per source_id — `gspphot` (photometric fit to BP/RP + parallax) and `gspspec` (spectroscopic fit to RVS spectra). Each emits Teff / logg / [M/H]; gspphot additionally emits A0 line-of-sight extinction at 547.7 nm.\n  - **Coverage**: empirical probe (`stellata-zsr.1`, 1000-star sample, seed 20260517) — gspphot ∪ gspspec covers 84.8% of AT-HYG.gaia for (Teff, logg). Status-quo spectral-class parsing covers 29.3%. Net uplift: 2.87×.\n  - **Routing in build-catalog.ts** (lives in `stellata-dch.49`): prefer `teff_gspphot` (broader coverage, ~80%) → fall back to `teff_gspspec` (~5% incremental) → fall back to spectral-class T_TABLE (~15% gap, faint stars without high-S/N BP/RP). Per-row `teff_src` field records the chosen source.\n  - **Downstream consumers** (each filed as its own follow-on bead):\n    - Tier 1 colour LUT (`stellata-zsr.3`) — re-keys Teff source from Ballesteros(B-V) → Apsis-direct where available.\n    - Phase 5 Bead T (`stellata-dch.40`) — direct logg improves giant / subgiant mass-ratio estimates.\n    - Phase 5 Bead U (`stellata-dch.41`) — direct Teff feeds limb-darkening coefficient selection.\n    - Edenhofer dust-map cross-check — uses `azero_gspphot` as an Apsis-side prior on line-of-sight extinction; filed when desired.\n  - **DR4 refresh path**: `scripts/refresh-gaia-apsis.py` re-keys onto DR4 source_ids when the next data release lands. Cross-walk follows the same pattern as Bailer-Jones / astrometry (DR3↔DR4 source-id mapping during the transition window).\n  - **Stars not covered**: the ~15% gap (faint Tycho-only stars, hot O/B without reliable GSP-Phot fit) falls back to spectral-class T_TABLE. Coverage degradation by magnitude is logged at build time for visibility.\n\n  ## Optical-pair filter\n  - 4-tier cascade explained.\n  - The asymmetric-Gaia gate worked example (Sirius A-C).\n\n  ## Adding to the known-stars corpus\n  - Step-by-step: pick a star, cite expected values, add the row, run tests.\n\n  ## Refreshing data when DR4 / new AT-HYG lands\n  - Step-by-step: which refresh scripts to run, in which order, expected runtimes, smoke-checks per refresh.\n  - **Bailer-Jones DR4 cadence**: B-J publishes a DR4-keyed catalog after each Gaia release. Refresh `data/bailer-jones-dr3.tsv` → `data/bailer-jones-dr4.tsv` in lockstep with the AT-HYG DR4 swap; Gaia provides DR3↔DR4 source-id cross-walks for the transition window.\n\n  ## Debug recipes\n  - 'Star X renders at wrong distance' — grep multiples.tsv for the system, check resolve_via, check astrometry_via, check the source files. For distance specifically: check `dist_src` value (`G_R3` raw / `BJ` refined / `LMC_KIN` LMC-overridden / `HIP` or `GJ` AT-HYG fallback / `N` dropped).\n  - 'New WDS pair appears as spurious optical' — check WDS Notes flag, both components' Gaia coverage, asymm-gate eligibility.\n\n# Acceptance\n\n- A fresh contributor reads docs/cross-match.md cold and successfully picks up a Phase-2-equivalent debug task (e.g. 'figure out why 70 Oph is rendering at distance X instead of Y').\n- A fresh contributor can also pick up a Phase-3 distance-refinement debug task (e.g. 'figure out why this LMC supergiant is at 13 kpc instead of 49.59 kpc') using only this doc.\n- A fresh contributor can pick up an Apsis-routing debug task (e.g. 'figure out why this star uses spectral-class T_TABLE instead of teff_gspphot') using the Apsis section's routing prose + the per-row `teff_src` field.\n- All code references in the doc (file:line) are accurate at commit time.\n- Links to memories (stellata-data-fidelity-principle, frozen-external-data) embedded.\n\n# Smoke\n\n- Read it cold; verify it makes sense.\n- A second reviewer reads it cold; ditto.\n\n# Files to touch\n\n- docs/cross-match.md (new)\n- CLAUDE.md docs index — add a row for docs/cross-match.md in the next bead (stellata-dch.44)\n\n# Commit\n\nOne commit. PR labelled skip-version-bump.","status":"open","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:23:05Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:29:37Z","labels":["docs","phase-6"],"dependencies":[{"issue_id":"stellata-dch.43","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:23:04Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.43","depends_on_id":"stellata-dch.32","type":"blocks","created_at":"2026-05-15T11:25:13Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.42","title":"Phase 6 — SCIENCE.md catalogue-pipeline section rewrite with citations","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nRewrite the 'Multiple-star pipeline' and 'Stellar catalog ingestion' sections of SCIENCE.md to reflect the new source-ID-anchored architecture, with full citations for every cross-walk table and every science decision. Per the data-fidelity principle: 'an astronomer asking about Stellata's catalogue pipeline at any level should hear an answer we're proud of.' This is the public-facing science doc that delivers on that bar. Depends on all Phase 2-5 beads landed (the actual implementation; we document what was built).\n\n# Per memory defer-doc-updates\n\nDoc rewrite happens AFTER the code lands, not before. Reading the actual final diff for each phase, then writing the corresponding science prose, captures the as-built state and avoids the doc-drift trap.\n\n# Deliverables\n\n- SCIENCE.md — sections 'Multiple-star pipeline' and 'Stellar catalog ingestion' rewritten top-to-bottom. Content shape:\n\n  ## Stellar catalog ingestion (rewrite)\n  - AT-HYG v3.3 as base; what each *_src column means; per-row provenance.\n  - Layer 1 reference data table (every committed external file, source, refresh cadence).\n  - Layer 2 refresh scripts; manual-run rationale per frozen-external-data principle.\n  - v6 binary format; gaia_source_id field; per-record provenance fields.\n  - Cross-match passes: GCVS (gaia_source_id primary key), CCDM (HIP-anchored).\n  - **Multi-layer distance-refinement pipeline** (NEW subsection, drives Beads Y + Z + AA):\n    - The catastrophic-1/π-inversion problem (low-S/N parallaxes produce divergent posterior mean; biased high; ~20% of DR3 stars have negative parallaxes; Lutz-Kelker selection bias; Lindegren et al. (2021) zero-point offset).\n    - **Bailer-Jones (2021) Bayesian posterior** (Bead Z / dch.47) — `r_med_photogeo` ‖ `r_med_geo` from VizieR I/352. Direction-dependent Galactic prior + CMD prior (BP/RP photometry). Handles negative parallaxes via prior dominance; self-targets noisy regime (near-no-op for high-S/N); zero-point correction applied upstream. Worked example: HIP 25733 (cat 14.3 kpc → B-J ~5-7 kpc).\n    - **LMC kinematic + sky-cone override** (Bead AA / stellata-8l0) — Pietrzyński et al. (2019) eclipsing-binary distance 49.59 kpc anchored on sky-cone + proper-motion gate identifying LMC members. Why B-J alone doesn't fix this: smooth Galactic-density prior doesn't cover the LMC; posteriors land somewhere intermediate (5-20 kpc). The kinematic filter is the principled \"solve the class\" fix per stellata-data-fidelity-principle; extends to SMC/Sgr/M31 when those populations enter AT-HYG depth.\n    - **`MAX_DIST_PC = 50_000` bounded-scope safety gate** — applied LAST, after refinement layers. Reframe SCIENCE.md:98-99 prose: cutoff is NOT a primary include/exclude filter; it's a modelled-scope statement tuned to the populations the catalog represents (Sol → LMC for v1). Future satellite refinements bump it.\n    - **`dist_src` enum extension `{HIP, GJ, T, N, G_R3, BJ, LMC_KIN, ...}`** documented with layer-per-value semantics.\n  - **Astrophysical parameters (Apsis) per star** (NEW subsection, drives Beads BB + CC — `dch.48` + `dch.49`):\n    - What Apsis is: Gaia DR3's two astrophysical-parameter pipelines — `gspphot` (photometric fit to BP/RP + parallax) and `gspspec` (spectroscopic fit to RVS spectra). Each emits Teff / logg / [M/H], and gspphot additionally emits A0 (line-of-sight monochromatic extinction at 547.7 nm).\n    - Coverage uplift: empirical probe in `stellata-zsr.1` (1000-star random sample of AT-HYG.gaia, seed 20260517) showed (Teff, logg) goes from 29.3% via spectral-class parsing to 84.8% via gspphot ∪ gspspec — a 2.87× improvement.\n    - Routing in build-catalog.ts: prefer gspphot Teff (broader coverage; ~80%), fall back to gspspec where gspphot null (~5% incremental); fall back to spectral-class T_TABLE for the ~15% gap (faint stars without high-S/N BP/RP photometry).\n    - Downstream consumers: Tier 1 colour LUT re-keys onto Apsis-direct Teff (`stellata-zsr.3`); Phase 5 mass-ratio gains direct logg for giants / subgiants (`stellata-dch.40` Bead T); Phase 5 eclipse photometry gains direct Teff for limb-darkening (`stellata-dch.41` Bead U); azero_gspphot cross-checks the Edenhofer dust map.\n    - DR4 refresh: `refresh-gaia-apsis.py` re-keys onto DR4 source_ids when the next data release lands; cross-walk follows the same pattern as Bailer-Jones / astrometry.\n\n  ## Multiple-star pipeline (rewrite)\n  - Why this exists (current section, kept).\n  - Three regimes (kept; updated to reference new astrometry sources).\n  - Stage 1-7 catalogue builder architecture with citations.\n  - Resolution chain: 5 tiers, each tier's data source + citation.\n  - Astrometry routing: gaia_5p / gaia_nss_systemic / hip2_long_baseline; citation for each.\n  - Orbital element selection: NSS \u003e ORB6 with rationale + citation (Gaia DR3 NSS reference paper Pourbaix et al. 2022; ORB6 reference).\n  - Optical-pair filter cascade: 4 tiers; asymmetric-Gaia-gate rationale + the Sirius A-C example as worked case.\n  - Mass-ratio q tables: WD / giant / subgiant / main-sequence with Cox 2000 / Allen 2000 citations.\n\n# Citations to include (with full bibliographic info)\n\n- Gaia DR3 main reference: Gaia Collaboration et al. 2023, A\u0026A 674, A1\n- Gaia DR3 NSS reference: Pourbaix et al. 2022, A\u0026A 674, A34\n- Hipparcos van Leeuwen 2007 reduction: van Leeuwen 2007, A\u0026A 474, 653\n- Tycho-2: Hog et al. 2000, A\u0026A 355, L27\n- WDS: Mason et al. 2001, AJ 122, 3466\n- ORB6: Hartkopf et al. (Sixth Catalog of Orbits of Visual Binary Stars; USNO)\n- GCVS: Samus et al. 2017, ARep 61, 80\n- SIMBAD: Wenger et al. 2000, A\u0026AS 143, 9\n- Allen 2000 / Cox 2000 (Allen's Astrophysical Quantities, 4th ed.)\n- **Bailer-Jones et al. 2021, AJ 161, 147** (geometric + photogeometric distance posteriors)\n- **Lindegren et al. 2021, A\u0026A 649, A4** (Gaia DR3 parallax zero-point correction)\n- **Andrae et al. 2023, A\u0026A 674, A27** (Gaia DR3 GSP-Phot — photometric astrophysical parameters)\n- **Recio-Blanco et al. 2023, A\u0026A 674, A29** (Gaia DR3 GSP-Spec — spectroscopic astrophysical parameters)\n- **Pietrzyński et al. 2019, Nature 567, 200** (LMC distance from eclipsing binaries — 49.594 ± 0.55 kpc)\n- **van der Marel \u0026 Kallivayalil 2014, ApJ 781, 121** (LMC bulk proper motion)\n\n# Acceptance\n\n- Section flow: an external astronomer reading SCIENCE.md cold can follow the pipeline end-to-end and verify Stellata's choices against literature.\n- Every science-driven decision has a citation.\n- Worked examples (Sirius A-B, α Cen A-B+Proxima, Algol, HIP 25733 B-J refinement, an LMC supergiant) demonstrate the cascade behavior.\n- The new \"Multi-layer distance-refinement pipeline\" subsection clearly explains why B-J alone doesn't fix LMC stars and why the cutoff is a bounded-scope statement rather than a filter.\n- The new \"Astrophysical parameters (Apsis) per star\" subsection clearly explains gspphot vs gspspec, the 84.8% coverage number with its empirical-probe provenance, and the spectral-class fallback path for the ~15% gap.\n- No v1.0 / v1-era prose (Phase 0 sweep already complete).\n- grep 'TODO\\|FIXME\\|XXX' SCIENCE.md returns no hits in the rewritten sections.\n\n# Smoke\n\n- Read SCIENCE.md cold (or have a reviewer read it).\n- Verify two arbitrary stars from the catalogue (one nearby high-S/N, one distant low-S/N) can be traced through the pipeline by reading only this doc.\n- Verify the cutoff reframing reads as a modelled-scope statement, not a hardcoded filter.\n\n# Files to touch\n\n- SCIENCE.md (rewrite of two named sections, keep others intact; update :98-99 cutoff prose)\n\n# Commit\n\nOne commit. PR labelled skip-version-bump (docs-only).","status":"open","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:22:40Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:28:57Z","labels":["docs","phase-6","science"],"dependencies":[{"issue_id":"stellata-dch.42","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:22:40Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.42","depends_on_id":"stellata-dch.32","type":"blocks","created_at":"2026-05-15T11:25:11Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.42","depends_on_id":"stellata-dch.39","type":"blocks","created_at":"2026-05-15T11:25:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.38","title":"Phase 4 — scripts/known-stars.test.ts: tier-A vitest assertions against built catalog","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nTier-A validation: vitest asserts every row in scripts/known-stars.tsv matches the built public/catalog.bin within documented tolerances. This is the regression-prevention surface for the entire pipeline at known-star granularity. Depends on stellata-dch.33 (v6 binary format), stellata-dch.37 (corpus), and benefits from stellata-dch.32 (multiples.tsv fully landed).\n\n# Deliverables\n\n- scripts/known-stars.test.ts (new) — vitest file. Loads scripts/known-stars.tsv via csv parser, loads public/catalog.bin via a reusable lookup library (see refactor below), for each corpus row asserts:\n  - Primary lookup by HIP and by gaia_source_id both succeed\n  - Primary distance within ±0.01 pc OR 1-sigma whichever is larger (toBeCloseTo with calibrated precision)\n  - Primary absmag within ±0.05 (toBeCloseTo)\n  - Primary spectral class index + subclass exact match (toBe)\n  - For each companion: comp_letter present in multiples.tsv, comp's hip + gaia_source_id match, comp's absmag within ±0.05\n  - For systems with orbital_period_days: orbit_via != 'none', period within ±5%\n- scripts/verify-catalog.ts — REFACTOR (per memory stellata-named-constants-and-dry) to expose loadCatalog(path), lookupByHip(catalog, hip), lookupByGaiaSourceId(catalog, source_id) as library functions, so known-stars.test.ts can import them instead of duplicating parse logic.\n\n# Test assertions per memory stellata-test-coverage-discipline\n\n- Numeric headline claims pinned with toBe(N) where possible, NOT toBeLessThanOrEqual.\n- Each test failure shows actionable diagnostic: 'Sirius A: expected distance 2.64 pc, got 2.71 pc (diff 0.07 pc \u003e tolerance 0.01 pc)'.\n- Per-tier breakdown: separate test describe blocks for single stars, visual binaries, NSS-orbited systems.\n\n# Acceptance\n\n- All 50+ corpus rows pass.\n- Any failure shows a clear diagnostic naming the corpus row + the field + the discrepancy.\n- npm test green.\n- Test takes \u003c 5 seconds (loads catalog once, iterates corpus).\n\n# Smoke\n\n- npm test (catalog.bin must exist)\n- Deliberately mutate one corpus row's expected_distance and verify the test FAILS with a clear message; revert.\n\n# Files to touch\n\n- scripts/known-stars.test.ts (new)\n- scripts/verify-catalog.ts (refactor for reusable lookup)\n\n# Commit\n\nOne commit. PR labelled skip-version-bump (validation infra, not user-visible).","status":"open","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:20:53Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:20:53Z","labels":["phase-4","tests","validation"],"dependencies":[{"issue_id":"stellata-dch.38","depends_on_id":"stellata-9mm.199","type":"blocks","created_at":"2026-05-19T12:27:29Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.38","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:20:52Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.38","depends_on_id":"stellata-dch.32","type":"blocks","created_at":"2026-05-15T11:24:58Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.38","depends_on_id":"stellata-dch.33","type":"blocks","created_at":"2026-05-15T11:24:57Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.38","depends_on_id":"stellata-dch.37","type":"blocks","created_at":"2026-05-15T11:24:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":4,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.37","title":"Phase 4 — scripts/known-stars.tsv: 50+ hand-verified system corpus (single stars + binaries)","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nCurate the validation corpus: a TSV of 50-80 hand-verified stars/systems with expected values for use by Phase 4 Bead R (tier-A vitest assertions). MUST include single stars AND binaries — single-star coverage is part of the explicit requirement per parent epic 'Coverage across the full catalogue' section. No external dependencies; can run in parallel with all other phases. The LARGEST manual effort in the rewrite (~25-40 hours hand-verification work).\n\n# Deliverables\n\n- scripts/known-stars.tsv (new) — TSV with columns:\n  - wds_id (nullable for single stars)\n  - system_name (e.g. 'Sirius', 'α Cen', 'Vega', '61 Cygni')\n  - primary_hip (nullable for HIP-less)\n  - primary_gaia_source_id (int64, the canonical anchor)\n  - primary_distance_pc (float)\n  - primary_distance_pc_err (float, 1-sigma)\n  - primary_absmag (float)\n  - primary_spectral (string)\n  - companions (semicolon-separated 'comp_letter:hip:gaia_id:absmag' tuples, EMPTY string for single stars)\n  - orbital_period_days (nullable; only set for systems with well-determined orbits)\n  - notes_source (string — citation: 'SIMBAD \u003cpermalink\u003e' / 'ORB6 entry P=NN' / 'Gaia DR3 ADQL spot-check 2026-MM-DD' / 'van Leeuwen 2007 HIP2' / etc.)\n\n# Coverage requirement (explicit)\n\nSuggested mix of 50-80 entries:\n- ~15 single stars: Sol (HIP 0 / synthetic), Vega (HIP 91262), Polaris (HIP 11767), Altair (HIP 97649), Procyon-as-single (HIP 37279 ignoring Procyon B), faint Tycho-population samples chosen for magnitude diversity (~5-10 stars at V \u003e 11).\n- ~25 famous visual binaries: Sirius AB (HIP 32349 + DA2 companion), α Centauri AB (HIP 71683 + HIP 71681), Castor system (HIP 36850 + HIP 36760, multi-component), 61 Cygni AB (HIP 104214 + HIP 104217), Procyon AB (HIP 37279 + WD), Algol (HIP 14576, eclipsing), β Cygni / Albireo (HIP 95947), γ Andromedae / Almach (HIP 9640), ζ Reticuli AB, etc.\n- ~15 systems where current Stellata is known to mis-render (TBD via spot-checks during Phase 2 execution) OR where Gaia DR3 NSS provides orbital elements (so Phase 4 Bead R can validate NSS-vs-ORB6 selection).\n\n# Distance-refinement regression cases (required)\n\nCoupled to Phase 3 Beads Z (B-J distance override) and AA (LMC kinematic refinement). Each catches a different failure mode of naive 1/π distance:\n\n- **B-J override regression cases** (currently rendered outside the galactic-disc wireframe by 1/π catastrophic inversion):\n  - HIP 22365 (cat: ~9.96 kpc, gal R 17.9 kpc — outside the rim)\n  - HIP 25733 (cat: ~14.33 kpc, gal R 22.4 kpc — outside the rim)\n  - HIP 38430 (cat: ~12.66 kpc, gal R 17.9 kpc — outside the rim)\n  - HIP 46144 (cat: ~9.19 kpc, gal z +6 kpc — above the ±400 pc thickness rings)\n  \n  Each row asserts post-B-J distance ≥ 25% smaller than current AT-HYG value (Tier-A vitest pins with `toBe(...)`).\n\n- **B-J no-degradation regression guard** (well-measured nearby F-dwarf):\n  - HIP 23785 (93 pc, normal high-S/N parallax)\n  \n  Row asserts post-B-J distance within ±5% of current AT-HYG value — B-J must not degrade good measurements.\n\n- **LMC kinematic refinement regression cases** (currently mis-placed by 1/π inversion in the LMC direction):\n  - 2-3 LMC B-supergiants from the HDE 268xxx range (e.g. HD 268749 \"B 7 IAB\", others TBD during corpus curation by grepping AT-HYG for HDE 268xxx-271xxx).\n  \n  Each row asserts post-LMC-kinematic distance = 49590 pc (Pietrzyński 2019), `dist_src = \"LMC_KIN\"`.\n\n# Authoring discipline\n\n- Every row has a notes_source citation (no row authored without traceability).\n- Expected values verified against AT LEAST TWO independent sources where possible (SIMBAD + Gaia DR3 ADQL; ORB6 + Hipparcos van Leeuwen; etc.).\n- Mass-ratio q expected values for binaries computed from spectral classes via current rules (so Bead T's mass-ratio refinement can produce expected updates).\n- Tolerances documented in a header comment block of the TSV: distance ±0.01 pc OR 1-sigma whichever is larger; absmag ±0.05; spectral class index + subclass exact match; orbital period ±5%.\n\n# Acceptance\n\n- 50+ rows minimum, 80 maximum.\n- Single-star coverage: at least 12 rows have empty companions field.\n- Binary coverage: at least 20 rows have non-empty companions.\n- Distance-refinement regression cases: 4 B-J override cases + 1 B-J no-degradation guard + 2-3 LMC kinematic cases (~7-8 rows total).\n- Every row has a notes_source.\n- Random 10-row spot-check via SIMBAD live (manual) reveals zero discrepancies.\n\n# Smoke\n\n- wc -l scripts/known-stars.tsv shows 50+ rows\n- python3 -c 'import csv; rows = list(csv.DictReader(open(\"scripts/known-stars.tsv\"), delimiter=\"\\t\")); print(sum(1 for r in rows if not r[\"companions\"])); print(sum(1 for r in rows if r[\"companions\"]))'\n- Random sample 10 rows; cross-check against SIMBAD; expect zero discrepancies\n\n# Files to touch\n\n- scripts/known-stars.tsv (new)\n- A small README block at top of the TSV documenting columns + tolerances + provenance discipline.\n\n# Commit\n\nOne commit. PR labelled skip-version-bump (data-only, no user-visible behavior change). Standalone — independent of all other beads, can start immediately.","status":"open","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:20:33Z","created_by":"Alex Marshall","updated_at":"2026-05-17T17:06:01Z","labels":["corpus","phase-4","validation"],"dependencies":[{"issue_id":"stellata-dch.37","depends_on_id":"stellata-9mm.199","type":"blocks","created_at":"2026-05-19T12:27:29Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.37","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:20:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.34","title":"Phase 3 — build-catalog.ts GCVS cross-match: prefer gaia_source_id over hip/hd","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nRe-anchor the GCVS variable-star cross-match to use Gaia DR3 source_id as the primary key, with HIP/HD as fallback. This benefits the ENTIRE catalog (every star, not just binaries) and is expected to increase the GCVS match rate for stars without HIP/HD identifiers. Depends on stellata-dch.33 (v6 binary surfaces gaia_source_id).\n\n# Current state\n\nbuild-catalog.ts:569-587 (applyVariability function) cross-matches GCVS rows to AT-HYG via byHip / byHd maps only. AT-HYG rows that have a gaia field but no hip/hd are NOT cross-matched even if GCVS has a Gaia source_id reference.\n\n# Deliverables\n\n- scripts/build-catalog.ts — extend applyVariability() to build a third index byGaia (gaia_source_id -\u003e at_hyg_row) and check it first. Cross-match priority: byGaia (when both sides have gaia source_id) -\u003e byHip -\u003e byHd. Log match-source stats: 'GCVS matches: via gaia=X, via hip=Y, via hd=Z, unmatched=W'.\n- GCVS cross-id parsing: scripts/catalog-pure.ts has GCVS parsers. Verify whether GCVS publishes Gaia DR3 source_ids in its cross-id catalog (crossid.txt). If yes, parse them. If no, fall back to existing logic.\n- scripts/catalog-pure.test.ts — add tests for the new byGaia cross-match path.\n\n# Acceptance\n\n- Match-rate stats log shows non-zero via gaia counts.\n- For a known variable star without HIP but with Gaia DR3 (TBD during execution by sampling), verify it's now matched (was missed in current pipeline).\n- Overall GCVS match rate strictly higher than current baseline. Pin the new rate in scripts/build-counts-expected.json.\n\n# Smoke\n\n- npm run build:catalog\n- Inspect GCVS match-stats log\n- npx tsx scripts/verify-catalog.ts: variable star count higher than before\n\n# Files to touch\n\n- scripts/build-catalog.ts (applyVariability + cross-id parsing)\n- scripts/catalog-pure.ts (if GCVS cross-id parsing needs extending)\n- scripts/catalog-pure.test.ts\n\n# Commit\n\nOne commit. Likely minor version bump (more variable stars rendering = user-visible change).","status":"open","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:19:29Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:19:29Z","labels":["cross-match","gcvs","phase-3"],"dependencies":[{"issue_id":"stellata-dch.34","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:19:29Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.34","depends_on_id":"stellata-dch.33","type":"blocks","created_at":"2026-05-15T11:24:55Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.33","title":"Phase 3 — Binary format v6: surface gaia_source_id per record (8 bytes)","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nBump catalog binary format from v5 to v6, surfacing Gaia DR3 source_id (int64, 8 bytes) per record. This benefits the ENTIRE 313k-star catalog (not just binaries) — downstream code (search index, GCVS lookup, future POI features) can use the source_id for identifier-based operations. Depends on stellata-dch.27 (which extends AthygRow schema to carry the gaia column from AT-HYG).\n\n# Format change\n\n- Header magic: HYG5 -\u003e HYG6\n- Record size: 48 bytes -\u003e 56 bytes (one 8-byte gaia_source_id field appended)\n- Endianness, field offsets for existing fields: UNCHANGED so existing field reads still work after offset adjustment\n- gaia_source_id stored as int64 little-endian. Value 0 = no Gaia source_id (sentinel). Sentinel only valid when AT-HYG row's gaia field is missing/null.\n\n# Open design question (resolve here)\n\nWhether to keep gaia_source_id inline in the record (8 bytes per row * 313k = ~2.5 MB increase) or use a side table indexed by record offset. Recommendation per parent epic: inline. AT-HYG carries gaia for ~98% of rows so side-table sparsity doesn't pay off. Inline is simpler + O(1) lookup. Confirm during implementation that the size budget is acceptable (current catalog.bin is ~13 MB; v6 will be ~16 MB).\n\n# Deliverables\n\n- scripts/catalog-pure.ts — update packers / unpackers / record-layout constants for v6 schema. New field gaia_source_id at offset 48 (after existing 48-byte v5 record), 8 bytes int64 LE.\n- scripts/build-catalog.ts — extract gaia from AthygRow + emit it in the v6 record. Update HEADER_MAGIC constant.\n- scripts/verify-catalog.ts — recognize v6 magic; dump gaia_source_id in record-inspection output.\n- src/client/catalog-loader.ts — decode v6 records; expose gaia_source_id alongside other fields. Update Catalog struct.\n- scripts/catalog-pure.test.ts — bump format-regression tests to assert v6 layout. Add field-offset test for gaia_source_id at byte 48; assert record stride = 56.\n- Migration strategy: NONE (full rebuild). Old catalog.bin (v5) gets replaced when npm run build:catalog runs.\n\n# Acceptance\n\n- typecheck + vitest green\n- npx tsx scripts/verify-catalog.ts reports 'format v6, record size 56' and dumps sample records with gaia_source_id values\n- Spot-check: Sirius (HIP 32349) in built catalog has gaia_source_id=2947050466531873024 surfaced\n- npm run build:catalog completes without error; public/catalog.bin grows to ~16 MB\n\n# Smoke\n\n- npm run build:catalog\n- npx tsx scripts/verify-catalog.ts\n- npm test green\n\n# Files to touch\n\n- scripts/catalog-pure.ts (record layout)\n- scripts/build-catalog.ts (emit)\n- scripts/verify-catalog.ts (decode)\n- scripts/catalog-pure.test.ts (regression tests)\n- src/client/catalog-loader.ts (client decode)\n\n# Commit\n\nOne commit. Major version bump (v5 -\u003e v6) is user-visible (no auto-upgrade) so this needs at least a minor package.json version bump per RELEASING.md.","status":"open","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:19:11Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:47:33Z","external_ref":"archive/dch-pipeline-foundation","labels":["binary-format","phase-3"],"dependencies":[{"issue_id":"stellata-dch.33","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:19:11Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.33","depends_on_id":"stellata-dch.27","type":"blocks","created_at":"2026-05-15T11:24:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":5,"comment_count":0}
{"_type":"issue","id":"stellata-dch.32","title":"Phase 2 — build-binaries.py Stages 5+6+7: optical-pair filter cascade, multiples.tsv schema, stats","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nThe final Phase 2 bead. Three things land together:\n\n(Stage 5) — rewrite the optical-pair filter cascade to be ID-anchored, with a new asymmetric-Gaia gate that catches Sirius-style spurious pairs directly. (Stage 6) — extend multiples.tsv schema with provenance columns. (Stage 7) — emit build-time statistics into scripts/build-counts-expected.json.\n\nDepends on stellata-dch.28, .30, .31.\n\n# Stage 5 — Optical-pair filter (4-tier cascade, ID-anchored)\n\nFor each WDS pair (component A, component B):\n\n1. WDS Notes flag chars — S/U/X/Y reject (optical); T/V/Z keep (physical). Unchanged from current implementation.\n2. Both-components-Gaia gate — when BOTH A and B have resolved Gaia astrometry (resolve_via in tiers 1-3, OR tier 4 successfully). Test: |plx_A - plx_B| \u003c 3 * sqrt(e_plx_A^2 + e_plx_B^2) AND |pmra_A - pmra_B| \u003c 5 mas/yr AND |pmdec_A - pmdec_B| \u003c 5 mas/yr. Pass = physical; fail = optical. Expect this gate to engage for \u003e95% of pairs vs ~73% in the dch.18 position-based implementation.\n3. Asymmetric-Gaia gate (NEW) — when ONLY B has Gaia astrometry AND A has a known distance from AT-HYG/HIP2. Test: |plx_B - plx_A_known| \u003c 3 * sqrt(e_plx_B^2 + e_plx_A_known^2). Pass = physical (companion at primary's distance); fail = optical (companion is a background star). CATCHES the Sirius A-C/D/E/F case directly: A's distance is 2.64 pc (known), C/D/E/F's Gaia parallax says 1+ kpc -\u003e reject.\n4. Magnitude-gap sanity heuristic — current tier-3 backstop unchanged. Used when both components miss Gaia (rare with source-ID anchoring, but possible for faint Tycho-only stars without HIP).\n\n# Stage 6 — multiples.tsv schema\n\nAdd columns: resolve_via, astrometry_via, orbit_via, gaia_source_id. Preserve existing columns (system_id, comp, hip, x_pc, y_pc, z_pc, absmag, ci, spect, name, source, regime, orbital fields from dch.8).\n\n# Stage 7 — Build-time statistics\n\nEmit per-strategy + per-tier counts at end of build:\n- Resolution: orb6_hip / wds_notes_hip / athyg_gaia_native / position_pm / position_nopm / unresolved\n- Astrometry: gaia_5p / gaia_nss_systemic / hip2_long_baseline\n- Orbits: gaia_nss / orb6 / orb6_spectroscopic / none\n- Optical filter: wds_notes_kept / wds_notes_rejected / gaia_kept / gaia_rejected / asymm_kept / asymm_rejected / mag_heuristic_kept / mag_heuristic_rejected\n- Position-fallback residual histogram (for position_pm matches)\n\nWrite to scripts/build-counts-expected.json (extend existing structure per memory stellata-test-coverage-discipline tier-2). Phase 4 Bead S consumes these for population statistical gates.\n\n# Acceptance\n\n- Zero SYN-NNN injections for Sirius A-C/D/E/F WDS entries (verify via grep '06451-1643' data/multiples.tsv).\n- Sirius A-B retained with both resolve_via=orb6_hip (or athyg_gaia_native) and source_id surfaced.\n- α Cen A-B retained with both source_ids.\n- A random known optical pair (TBD during execution) correctly rejected via gate 2 or 3.\n- Coverage: optical_rejected_rate \u003e 5% of total pairs (matches WDS literature estimates that ~5-10% of WDS pairs are optical contamination).\n- Stats written to build-counts-expected.json for tier-B consumption.\n\n# Smoke\n\n- npm run build:binaries --force\n- Inspect data/multiples.tsv for new columns + Sirius/α Cen rows\n- Inspect Stage 7 stats log\n\n# Files to touch\n\n- scripts/build-binaries.py (Stages 5, 6, 7 sections — replaces existing optical-pair filter at lines ~700-750)\n- scripts/build-counts-expected.json (extend schema)\n- scripts/build-counts.test.ts (add assertions for the new fields — Tier B in Phase 4 builds on this)\n\n# Commit\n\nOne commit. This bead is the END of Phase 2. After it merges, data/multiples.tsv is ready for Phase 3 (binary format ingest).","status":"open","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:18:48Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:18:48Z","labels":["build-binaries","optical-filter","phase-2"],"dependencies":[{"issue_id":"stellata-dch.32","depends_on_id":"stellata-9mm.198","type":"blocks","created_at":"2026-05-19T12:27:27Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.32","depends_on_id":"stellata-9mm.200","type":"blocks","created_at":"2026-05-19T12:27:28Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.32","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:18:47Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.32","depends_on_id":"stellata-dch.31","type":"blocks","created_at":"2026-05-15T11:24:48Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.32","depends_on_id":"stellata-dch.61","type":"related","created_at":"2026-05-19T17:47:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":6,"comment_count":0}
{"_type":"issue","id":"stellata-dch.31","title":"Phase 2 — build-binaries.py Stage 4: NSS \u003e ORB6 orbital-element selection per system","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nFor each WDS pair, pick the BEST available orbital elements. Gaia NSS (when applicable) beats ORB6 because it's space-based and uses Gaia's astrometry. ORB6 (when no NSS) is the existing Regime 2/3 logic. Depends on stellata-dch.30.\n\n# Selection priority\n\nFor each WDS system:\n\n1. `gaia_nss` — Gaia NSS two-body solution is present for at least one component AND (period \u003c 3 yr OR semimajor_axis \u003c 1\") — within Gaia's observability regime. Convert NSS Thiele-Innes constants to canonical Campbell elements (see below), then to the v5 orbital-elements section schema (already used by dch.8). Include formal Gaia uncertainties.\n2. `orb6` — current Regime 2 logic (visual orbit) verbatim, with grade-tiebreak (lowest numeric grade wins; ties broken by most recent REF year).\n3. `orb6_spectroscopic` — current Regime 3 logic verbatim (no inclination; conventional defaults).\n4. `none` — Regime 1 (visual-only, no orbital elements).\n\n# Open design question (resolve here)\n\nThe 3-yr period / 1\" separation thresholds for NSS-vs-ORB6 are initial estimates. Some NSS solutions cover longer-period or wider systems (NSS max period observed in DR3 ≈ 9,936 d ≈ 27 yr; see dch.25 probe); we may want to broaden. Calibrate by counting NSS solutions per period bin and comparing against ORB6 coverage in the same bin. If NSS has reliable solutions out to e.g. P=5 yr, raise the threshold.\n\n# Thiele-Innes → Campbell conversion (do it ourselves)\n\nDecision (2026-05-18, Alex): implement the conversion in-repo rather than importing the ESA NSSTools package (https://gitlab.obspm.fr/gaia/nsstools — last updated 2+ years ago, no recent maintenance). Use NSSTools as a credit/sanity-check anchor: cite it in the helper's docstring, optionally pin a single-row fixture against its output for confidence.\n\nAlgebra (Heintz 1978 / Pourbaix; reproduced in Halbwachs+ 2023 Appendix B). Inputs A, B, F, G in mas; outputs a (mas), i (rad), Omega (rad), omega (rad).\n\n    u = (A² + B² + F² + G²) / 2\n    v = A·G − B·F\n    a       = sqrt(u + sqrt(u² − v²))           # apparent angular semi-major axis (mas)\n    tan²(i/2) = (u − sqrt(u² − v²)) / (u + sqrt(u² − v²))\n    ω + Ω   = atan2(B − F, A + G)\n    ω − Ω   = atan2(−B − F, A − G)\n\nVectorised numpy (~6 lines). Quadrant convention and Ω-by-π wrap match Halbwachs+ 2023.\n\nPhysical semimajor axis (AU) from photocentric a (mas): `a_AU = (a_mas / 1000) / plx_mas` for the photocentre orbit. Mass-ratio correction (NSS `mass_ratio` column where non-null) maps photocentre → primary semimajor axis if needed.\n\nSolution types dch.25 surfaces in `nss_solution_type` route the conversion:\n- `Orbital`, `OrbitalTargetedSearch*`, `OrbitalAlternative*` → astrometric Thiele-Innes → Campbell as above.\n- `SB1`, `SB1C`, `SB2`, `SB2C` → spectroscopic, no inclination from astrometry alone; mark `i` as null/unknown and fill (e, ω, T) from spectroscopic fit.\n- `AstroSpectroSB1` → combined; use Thiele-Innes + spectroscopic mass-ratio.\n- `EclipsingBinary`, `EclipsingSpectro` → use orbital + eclipse-derived i.\n\n# Deliverables\n\n- `scripts/build-binaries.py` — Stage 4 section. New function `select_orbit(system, indices) -\u003e (orbit_dict, orbit_via)` where `orbit_dict` is the canonical schema and `orbit_via in {gaia_nss, orb6, orb6_spectroscopic, none}`.\n- `nss_to_canonical_elements(nss_row, plx_mas) -\u003e orbit_dict` — Thiele-Innes → Campbell conversion routed by `nss_solution_type`. Pure helper, vitest-style coverage required.\n- Optional: one-row fixture spot-check against NSSTools output (committed reference values) for confidence.\n\n# Acceptance\n\n- Stats log: 'orbits sourced: gaia_nss=X, orb6=Y, orb6_spectroscopic=Z, none=W'. Expect X \u003e 0.\n- Spot-check NSS system (TBD during execution, picked from dch.25 spot-check pool): orbit_via=gaia_nss; period matches NSS published.\n- α Cen A+B: orbit_via=orb6 (P=79.9 yr, outside NSS regime).\n- Regime-3 spectroscopic-only ORB6: orbit_via=orb6_spectroscopic.\n- Visual-only pair (no ORB6): orbit_via=none.\n- Conversion helper has full unit-test coverage for each routing branch.\n\n# Smoke\n\n- `npm run build:binaries --force`.\n\nLABELS: phase-2, binaries, gaia, nss, orb6","status":"open","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:18:20Z","created_by":"Alex Marshall","updated_at":"2026-05-18T17:49:53Z","labels":["build-binaries","orbits","phase-2"],"dependencies":[{"issue_id":"stellata-dch.31","depends_on_id":"stellata-9mm.200","type":"blocks","created_at":"2026-05-19T12:27:26Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.31","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:18:20Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.31","depends_on_id":"stellata-dch.30","type":"blocks","created_at":"2026-05-15T11:24:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.30","title":"Phase 2 — build-binaries.py Stage 3: astrometry attachment with NSS-systemic + HIP2-long-baseline fallback","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nFor each WDS component resolved to a Gaia source_id (stellata-dch.28), attach the right astrometric measurement — defaulting to Gaia 5-parameter but falling back to NSS-systemic or HIP2-long-baseline when the 5p solution is unreliable. This is what makes Sirius A finally land at its true J2000 position. Depends on stellata-dch.28 + stellata-dch.29.\n\n# Astrometry routing priority\n\nFor each resolved component, in order:\n\n1. gaia_nss_systemic — when the source has a row in NSS two-body-orbit AND the 5-parameter solution is flagged unreliable (ruwe \u003e 1.4 OR ipd_frac_multi_peak \u003e 0.02). Use NSS's systemic position + PM (the orbit-removed motion of the center of mass) instead of the contaminated 5p solution.\n2. hip2_long_baseline — when the system has a known companion within 5\" AND |pmRA_gaia - pmRA_hip2| \u003e 50 mas/yr OR |pmDE_gaia - pmDE_hip2| \u003e 50 mas/yr. This is the classic bright-binary contamination case: Hipparcos's 1991.25 epoch + 4 yr baseline averages over a different window on the orbit than Gaia's 2014-2017 window, and the long-baseline PM is closer to the true single-star motion. Use HIP2's plx + PM + position (propagated to J2000 from HIP2's J1991.25 epoch).\n3. gaia_5p — default. Use Gaia DR3's 5-parameter solution.\n\nEach component records astrometry_via in multiples.tsv.\n\n# Open design question (resolve here)\n\nThe 50 mas/yr threshold for Hipparcos PM fallback is an initial estimate. After this bead lands, validate against the known bright-close-binaries list (Sirius, α Cen A+B, Castor A+B, Algol, Procyon A) and tune if needed. The threshold should engage for these systems but NOT for stars with genuinely revised single-star PMs between HIP and Gaia (e.g. high-PM stars where DR3 is just more accurate).\n\n# Deliverables\n\n- scripts/build-binaries.py — new Stage 3 section calling out to a helper function attach_astrometry(component, indices). Helper documented inline + tested via Python unittest.\n\n# Acceptance\n\n- For Sirius A: astrometry_via=hip2_long_baseline. Verified: pmra=-546.05 (HIP2) not -462 (Gaia 5p contaminated).\n- For α Cen A: astrometry_via=hip2_long_baseline.\n- For α Cen B: astrometry_via=hip2_long_baseline.\n- For Castor A primary: astrometry_via=hip2_long_baseline.\n- For Algol B (eclipsing binary): astrometry_via depends — NSS coverage would route to gaia_nss_systemic; otherwise hip2_long_baseline.\n- For Proxima Centauri (single star, isolated PM): astrometry_via=gaia_5p (no contamination).\n- For a random Tycho-only star: astrometry_via=gaia_5p (no HIP, no NSS).\n- Stats log: 'astrometry routing: gaia_5p=X, gaia_nss_systemic=Y, hip2_long_baseline=Z'.\n\n# Smoke\n\n- npm run build:binaries --force\n- grep '06451-1643' data/multiples.tsv -- shows Sirius A with astrometry_via=hip2_long_baseline\n- grep '14396-6050' data/multiples.tsv -- shows α Cen A+B with same\n\n# Files to touch\n\n- scripts/build-binaries.py (Stage 3 section)\n- scripts/build-binaries.py.test.py (extend with attach_astrometry tests)\n\n# Commit\n\nOne commit.","status":"in_progress","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:17:56Z","created_by":"Alex Marshall","updated_at":"2026-05-19T16:15:18Z","started_at":"2026-05-19T16:15:18Z","labels":["astrometry","build-binaries","phase-2"],"dependencies":[{"issue_id":"stellata-dch.30","depends_on_id":"stellata-9mm.197","type":"blocks","created_at":"2026-05-19T12:27:24Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.30","depends_on_id":"stellata-9mm.198","type":"blocks","created_at":"2026-05-19T12:27:25Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.30","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:17:56Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.30","depends_on_id":"stellata-dch.28","type":"blocks","created_at":"2026-05-15T11:24:46Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.30","depends_on_id":"stellata-dch.29","type":"blocks","created_at":"2026-05-15T11:24:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":4,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-dch.29","title":"Phase 2 — scripts/refresh-gaia-astrometry.py + commit data/gaia_dr3_astrometry.tsv","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nGaia DR3 astrometry for the union of source_ids resolved by Phase 2 Stage 2 (stellata-dch.28). By querying by source_id rather than position, we bypass the orbital-contamination problem entirely. Depends on stellata-dch.21 (refresh_lib) AND stellata-dch.28 (must know which source_ids to query).\n\n# Deliverables\n\n- scripts/refresh-gaia-astrometry.py (new) — reads data/gaia_astrometry_source_id_request.tsv (produced by Stage 2), chunks the source_id list, queries gaiadr3.gaia_source by source_id IN (...), writes the result.\n- data/gaia_dr3_astrometry.tsv (new, LFS) — columns: source_id, ra, ra_error, dec, dec_error, parallax, parallax_error, pmra, pmra_error, pmdec, pmdec_error, ref_epoch, ruwe, ipd_frac_multi_peak, phot_g_mean_mag, phot_bp_mean_mag, phot_rp_mean_mag. Note: ruwe + ipd_frac_multi_peak are used by Stage 3 (stellata-dch.30) to flag unreliable single-star solutions.\n- package.json — \"refresh:gaia-astrometry\" alias.\n\n# ADQL query template\n\nSELECT source_id, ra, ra_error, dec, dec_error, parallax, parallax_error,\n       pmra, pmra_error, pmdec, pmdec_error, ref_epoch, ruwe,\n       ipd_frac_multi_peak, phot_g_mean_mag, phot_bp_mean_mag, phot_rp_mean_mag\nFROM gaiadr3.gaia_source\nWHERE source_id IN ({chunk_of_5000_ids})\n\n(Chunked via refresh_lib batching helper; Gaia archive caps IN-list size ~5000.)\n\n# Acceptance\n\n- Output row count matches input source_id list size ±5% (some IDs may resolve to retracted sources or have no astrometry).\n- Schema validated.\n- Spot-check at least one pinned source_id confirmed to be in the Stage-2 request file. The original draft of this bead suggested Sirius A (source_id 2947050466531873024) at parallax ~374 mas / pmra ~-462 mas/yr — that is correct as a fact about Gaia DR3, but Sirius A is NOT in the Stage-2 request file. Stage 2 dch.28 resolves components via gaiadr3.hipparcos2_best_neighbour / gaiadr3.tycho2tdsc_merge_best_neighbour, and Sirius A (V=-1.46) saturates Gaia and is absent from both cross-match tables. Pick a source from the actual request file at implementation time.\n\n# Smoke\n\n- After stellata-dch.28 has run once, npm run refresh:gaia-astrometry writes the TSV.\n- Re-run stellata-dch.28 (build-binaries.py); tier-4 (position_pm) now becomes active (uses this astrometry for HIP-less components).\n\n# Files to touch\n\n- scripts/refresh-gaia-astrometry.py (new)\n- package.json (npm alias)\n\n# Commit\n\nOne commit. After this lands, re-run build-binaries to engage tier 4.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:17:32Z","created_by":"Alex Marshall","updated_at":"2026-05-19T16:12:11Z","started_at":"2026-05-19T15:07:25Z","closed_at":"2026-05-19T16:12:11Z","close_reason":"Closed","labels":["astrometry","gaia","phase-2","refresh-scripts"],"dependencies":[{"issue_id":"stellata-dch.29","depends_on_id":"stellata-9mm.196","type":"blocks","created_at":"2026-05-19T12:27:23Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.29","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:17:32Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.29","depends_on_id":"stellata-dch.28","type":"blocks","created_at":"2026-05-15T11:24:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-dch.28","title":"Phase 2 — build-binaries.py Stage 2: resolve each WDS component to a Gaia source_id","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nReplace the existing HIP-resolution chain (cone-match-first) in scripts/build-binaries.py with a source-ID-first five-tier resolution chain. The CORE of the rewrite. Depends on stellata-dch.27 (Stage 1 loader + indices). Note: stellata-dch.29 (refresh-gaia-astrometry.py) depends on THIS bead's output — write the resolved source_ids to a side file during this stage's run so Bead I can query Gaia for the union of needed source_ids.\n\n# Resolution chain (strict priority)\n\nFor each WDS component:\n\n1. orb6_hip — ORB6 entry's published HIP -\u003e HIP cross-walk lookup -\u003e gaia_source_id (existing ORB6 hip field path, now ID-anchored).\n2. wds_notes_hip — WDS Notes prose 'HIP NNNNN' regex extract -\u003e HIP cross-walk -\u003e gaia_source_id (existing prose-regex path, now ID-anchored).\n3. athyg_gaia_native — AT-HYG row's 'gaia' field (~98% of catalog rows have it natively) -\u003e use directly. No cross-walk needed.\n4. position_pm — PM-propagated position match against data/gaia_dr3_astrometry.tsv at ~2\" residual tolerance. ONLY runs when stellata-dch.29 has landed (astrometry.tsv exists). Until then, this tier is stubbed and always returns unresolved.\n5. position_nopm — Position match without PM, for faint stars with no Gaia PM in the astrometry table.\n\nEach component records its resolve_via strategy. Components that fail all five tiers are recorded as 'unresolved'.\n\n# Deliverables\n\n- scripts/build-binaries.py — replace resolve_primary_hip() (lines ~1323-1340) and the secondary cone-match block (lines ~1356-1365) with the new resolution chain. New function signature returns (gaia_source_id, resolve_via) per component.\n- Side-file emission: data/gaia_astrometry_source_id_request.tsv — list of source_ids resolved in tiers 1-3 (whatever Gaia astrometry we DO need to query). Bead I consumes this.\n- Resolution-strategy stats logged at end of Stage 2.\n\n# Acceptance\n\n- Build log line: \"Resolution: orb6_hip=X, wds_notes_hip=Y, athyg_gaia_native=Z, position_pm=A, position_nopm=B, unresolved=C\" with non-zero counts for each non-stub tier.\n- For Sirius A (HIP 32349, WDS 06451-1643): resolve_via=orb6_hip (or wds_notes_hip), gaia_source_id=2947050466531873024.\n- For Proxima Centauri (HIP 70890, WDS 14396-6050 C): resolve_via=athyg_gaia_native OR orb6_hip.\n- For a HIP-less faint WDS component: resolve_via=position_pm (after Bead I) or unresolved.\n- Coverage target: \u003e90% of WDS components in tiers 1-3 (source-ID anchored, not position). Tier 4+5 \u003c 10%.\n\n# Smoke\n\n- npm run build:binaries --force\n- Inspect resolution stats log\n- grep Sirius / α Cen / Proxima rows in data/multiples.tsv\n\n# Files to touch\n\n- scripts/build-binaries.py (Stage 2 section)\n- scripts/build-binaries.py.test.py (NEW — Python unittest for the resolution chain, with fixture-based testing of each tier)\n\n# Commit\n\nOne commit. Bead H lands BEFORE Bead I; the tier-4 path is stubbed until Bead I produces the astrometry file.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:17:13Z","created_by":"Alex Marshall","updated_at":"2026-05-19T14:57:40Z","started_at":"2026-05-19T09:52:24Z","closed_at":"2026-05-19T14:57:40Z","close_reason":"Shipped in PR #100 (squash-merged as 61da9ae). Four-tier resolution chain implemented; tier 2 prose-parser deferred to dch.60.","labels":["build-binaries","cross-match","phase-2"],"dependencies":[{"issue_id":"stellata-dch.28","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:17:13Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.28","depends_on_id":"stellata-dch.27","type":"blocks","created_at":"2026-05-15T11:24:44Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-dch.27","title":"Phase 2 — build-binaries.py Stage 1: load all reference data + build identifier indices","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nCreate `scripts/build-binaries.py` from scratch with Stage 1: load all reference catalogs and build the identifier indices that Stages 2-7 (stellata-dch.28..32) consume. Per the epic NOTES § \"Foundation commits preserved on archive/dch-pipeline-foundation\", Phase 2 rewrites build-binaries.py from scratch rather than evolving the throwaway dch.5 version on the archive branch — that file is reference-only, not a starting point.\n\nThe Stage 2-7 resolution / matching / orbit-picking / optical-filter logic lands in subsequent beads. This bead only brings the data INTO the script and the indices needed for downstream lookup.\n\n# Prerequisite — cherry-pick dch.4\n\nWDS and ORB6 source files (`wds_summ.txt`, `wds_notes.txt`, `wds_refs.txt`, `orb6_orbits.txt`) are not yet on main; per the epic foundation table they are preserved on `archive/dch-pipeline-foundation`. Cherry-pick commit `b7eed29` as the first commit of this PR; the rest of the inputs (AT-HYG, GCVS, hip_ccdm, HIP2, Gaia HIP/Tyc xmatch, Gaia NSS) are already on main.\n\n# Deliverables\n\n- `scripts/build-binaries.py` — new file, Stage 1 only:\n  - Load WDS pairs (~157k systems from `wds_summ.txt`).\n  - Load WDS notes (prose HIP supplement and optical-flag indicators).\n  - Load ORB6 orbits (~4k visual-binary orbital fits).\n  - Load AT-HYG — Python `AthygRow` dataclass surfaces hip + tyc + gaia (Gaia DR3 source_id) alongside ra/dec/x0/y0/z0/dist/absmag/spect/etc.\n  - Load GCVS (`gcvs5.txt` + `crossid.txt`).\n  - Load CCDM TSV.\n  - Load HIP2 van Leeuwen astrometry.\n  - Load Gaia DR3 ↔ HIP cross-walk.\n  - Load Gaia DR3 ↔ Tycho-2 cross-walk.\n  - Load Gaia DR3 NSS two-body solutions.\n  - (Gaia DR3 astrometry TSV — optional, present once stellata-dch.29 lands; skip with informational log line if missing.)\n- Build identifier indices:\n  - `hip → gaia_source_id` (from `gaia_dr3_hip_xmatch.tsv`)\n  - `tyc → gaia_source_id` (from `gaia_dr3_tyc_xmatch.tsv`)\n  - `gaia_source_id → nss_row` (from `gaia_dr3_nss_two_body.tsv`)\n  - `hip → at_hyg_row`, `tyc → at_hyg_row`, `gaia_source_id → at_hyg_row`\n  - `hip → hip2_row` (for the Stage 3 fallback)\n  - `gaia_source_id → astrometry_row` (empty pre-Stage-2-I; the index is constructed so callers always see the same shape).\n- Idempotency: skip when `data/multiples.tsv` exists AND is newer than `scripts/build-binaries.py` AND newer than every input file. `--force` bypasses.\n- Build log: one line per loaded catalog with row count and (where built) index cardinality. Example: `loaded 99k Gaia HIP xmatch rows; built hip → gaia_source_id of cardinality 99k`. Same shape for every loaded catalog so a fresh reader can scan.\n- Add `npm run build:binaries` script entry: `\"build:binaries\": \"python3 scripts/build-binaries.py\"`.\n\n# Tests\n\n- `scripts/build-binaries.test.py` (unittest module, same pattern as `refresh_lib.test.py`) — pin parser correctness on tiny fixture rows for each loader: AT-HYG row → AthygRow fields populated; WDS summary record → WdsPair fields; ORB6 line → Orb6Entry; HIP2 line; Gaia HIP / Tyc / NSS rows. Pin a known constant (e.g. AT-HYG Sol row has empty tyc + null gaia + hip 0) and at least one row that has all three classical IDs populated.\n\n# Acceptance\n\n- `npm run build:binaries -- --force` runs end-to-end and prints one load + index-cardinality log line per source.\n- AT-HYG inspection: log line \"X / Y AT-HYG rows carry gaia\" — expected ~98% per AT-HYG documentation; flag if outside [0.90, 1.00].\n- `python3 scripts/build-binaries.test.py` exits green.\n- `npm run typecheck` + `npm test` stay green (no TypeScript changes in this bead).\n\n# Smoke\n\n- `npm run build:binaries -- --force`\n- Inspect log for the per-catalog load + index-cardinality lines.\n- `python3 scripts/build-binaries.test.py`.\n\n# Files to touch\n\n- `scripts/build-binaries.py` (new).\n- `scripts/build-binaries.test.py` (new).\n- `package.json` (`build:binaries` script entry).\n\n# Not in scope (deferred to later Phase 2 beads)\n\n- Resolution chain WDS-component → Gaia source_id (stellata-dch.28).\n- Astrometry attachment / NSS-systemic fallback (dch.29-30).\n- Orbit picking (dch.31).\n- Optical-pair filter (dch.32).\n- TypeScript-side `gaia_source_id` surfacing in `scripts/build-catalog.ts` / `scripts/catalog-pure.ts` / `public/catalog.bin` (Phase 3, stellata-dch.33).\n\n# Commit\n\nTwo commits: (1) cherry-pick `b7eed29` (dch.4 data files); (2) new `scripts/build-binaries.py` + `scripts/build-binaries.test.py` + `package.json` script entry.\n\nLABELS: build-binaries, phase-2","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:16:51Z","created_by":"Alex Marshall","updated_at":"2026-05-18T21:21:00Z","started_at":"2026-05-18T20:58:50Z","closed_at":"2026-05-18T21:21:00Z","close_reason":"Closed","external_ref":"archive/dch-pipeline-foundation","labels":["build-binaries","phase-2"],"dependencies":[{"issue_id":"stellata-dch.27","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:16:50Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.27","depends_on_id":"stellata-dch.22","type":"blocks","created_at":"2026-05-15T11:24:41Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.27","depends_on_id":"stellata-dch.23","type":"blocks","created_at":"2026-05-15T11:24:42Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.27","depends_on_id":"stellata-dch.24","type":"blocks","created_at":"2026-05-15T11:24:43Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.27","depends_on_id":"stellata-dch.25","type":"blocks","created_at":"2026-05-15T11:24:43Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":4,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-dch.25","title":"Phase 1 — scripts/refresh-gaia-nss.py + commit data/gaia_dr3_nss_two_body.tsv","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nGaia DR3 NSS (Non-Single-Star) two-body-orbit catalogue. Pulls the full `gaiadr3.nss_two_body_orbit` table and writes a thin TSV with upstream columns verbatim — no Thiele-Innes → Campbell conversion at this stage (that lives downstream in dch.31). Phase 2 Stage 4 (stellata-dch.31) prefers NSS over ORB6 inside Gaia's observability regime (P \u003c ~10 yr, separation \u003c ~1\"). Depends on stellata-dch.21 (refresh_lib).\n\n# Live-probe schema correction (2026-05-18)\n\nBead spec drifted from the actual `gaiadr3.nss_two_body_orbit` schema. Live 1-row probe surfaced 4 items; corrected below.\n\n1. **Row count: ~443,205, NOT ~169k.** Original spec cited \"~169k binaries\" — wrong by 2.6×. The 169k figure may have referred to a subset (e.g. astrometric-only `Orbital` type = 134,598). Bounds re-pinned at [440k, 446k].\n\n2. **Schema uses Thiele-Innes constants, NOT Campbell elements.** DR3 NSS encodes the orbit via:\n   - `a_thiele_innes, b_thiele_innes, f_thiele_innes, g_thiele_innes` (mas)\n   - `c_thiele_innes, h_thiele_innes` (AU) — non-null only for AstroSpectro* solution types\n   - Plus errors on each\n   The classical Campbell elements `semimajor_axis` and `ascending_node` from the original bead spec are NOT stored columns — they are DERIVED from Thiele-Innes via Heintz 1978 algebra in the downstream consumer (dch.31). `inclination` and `arg_periastron` ARE stored columns but are NULL for `Orbital`-type rows (inclination is encoded in Thiele-Innes constants); populated only for AstroSpectroSB1 / SB1+spectroscopic solutions.\n\n3. **`nss_solution_type` is a categorical with 12 distinct values, not \"two-body orbit\".** Distribution (DR3 frozen, 2026-05-18 probe):\n   - SB1: 181,327 (spectroscopic single-lined)\n   - Orbital: 134,598 (pure astrometric)\n   - EclipsingBinary: 86,918\n   - AstroSpectroSB1: 33,467 (astrometric + spectroscopic combined)\n   - SB2: 4,630\n   - SB2C, OrbitalAlternative, OrbitalTargetedSearch, SB1C, OrbitalTargetedSearchValidated, EclipsingSpectro, OrbitalAlternativeValidated: \u003c 1k each\n   The TSV must surface `nss_solution_type` so downstream can filter / route conversion logic per type.\n\n4. **70 Oph (HIP 88601) spot-check is impossible.** Two independent failures: (a) HIP 88601 = V 4.0, below the Gaia saturation limit — not present in `hipparcos2_best_neighbour`; (b) 70 Oph's P = 88 yr is far beyond NSS's max period (~9,936 d ≈ 27 yr). Replaced with a self-consistency spot-check against a bright HIP star confirmed in NSS by live probe (matches the Sirius PM pattern in refresh-hipparcos2.py).\n\n# Deliverables\n\n- `scripts/refresh-gaia-nss.py` (new) — ADQL pull from `gaiadr3.nss_two_body_orbit` (full table). Backend: ESA Gaia archive (refresh_lib default ESA → CDS fallback). Idempotent (`is_up_to_date`).\n- `data/gaia_dr3_nss_two_body.tsv` (new, LFS) — columns:\n  - `source_id` (int)\n  - `nss_solution_type` (string)\n  - `period`, `period_error` (float, days)\n  - `t_periastron`, `t_periastron_error` (float, days)\n  - `eccentricity`, `eccentricity_error` (float)\n  - `a_thiele_innes`, `a_thiele_innes_error` (float, mas)\n  - `b_thiele_innes`, `b_thiele_innes_error` (float, mas)\n  - `f_thiele_innes`, `f_thiele_innes_error` (float, mas)\n  - `g_thiele_innes`, `g_thiele_innes_error` (float, mas)\n  - `c_thiele_innes`, `c_thiele_innes_error` (float, AU) — null except AstroSpectro\n  - `h_thiele_innes`, `h_thiele_innes_error` (float, AU) — null except AstroSpectro\n  - `inclination`, `inclination_error` (float, deg) — null for `Orbital` type\n  - `arg_periastron`, `arg_periastron_error` (float, deg) — null for `Orbital` type\n  - `mass_ratio`, `mass_ratio_error` (float) — non-null for SB2*\n  - `goodness_of_fit` (float)\n  - `significance` (float)\n  - Total: 27 columns\n- `package.json` — `refresh:gaia-nss` alias.\n\n# Acceptance\n\n- Row count in [440_000, 446_000] (DR3 frozen — observed 443,205 on 2026-05-18).\n- Schema validated against EXPECTED_SCHEMA (subset assertion on the 27 columns we keep).\n- Spot-check: bright HIP star confirmed in NSS via live probe — period and eccentricity self-consistent within tolerance (matches `refresh-hipparcos2.py`'s Sirius pattern).\n- Deterministic regen with no source changes produces byte-identical TSV.\n\n# Smoke\n\n- `npm run refresh:gaia-nss` writes the TSV.\n- `wc -l data/gaia_dr3_nss_two_body.tsv` shows ~443k + header.\n- Re-run is a no-op (idempotency log line).\n- Spot-check row passes self-consistency check.\n\n# Commit\n\nOne commit. Standard PR.\n\nLABELS: gaia, nss, phase-1, refresh-scripts","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:16:07Z","created_by":"Alex Marshall","updated_at":"2026-05-18T19:17:26Z","started_at":"2026-05-18T17:41:11Z","closed_at":"2026-05-18T19:17:26Z","close_reason":"Closed","labels":["gaia","nss","phase-1","refresh-scripts"],"dependencies":[{"issue_id":"stellata-dch.25","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:16:06Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.25","depends_on_id":"stellata-dch.21","type":"blocks","created_at":"2026-05-15T11:24:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.24","title":"Phase 1 — scripts/refresh-hipparcos2.py + commit data/hip2_van_leeuwen.tsv","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nHipparcos van Leeuwen 2007 reduction — improved astrometric solution covering all 118k HIP stars. Critical for the bright-binary fallback (Phase 2 Stage 3 stellata-dch.30): when Gaia's 5-parameter PM is corrupted by orbital wobble in close binaries (Sirius, α Cen, Castor, Algol) or by saturation (Sirius A, Polaris, Vega), HIP2's longer-baseline PM averages over a different window on the orbit and is often the truer single-star PM. Depends on stellata-dch.21 (refresh_lib).\n\n# Deliverables\n\n- scripts/refresh-hipparcos2.py (new) — VizieR I/311/hip2 query via CDS TAP (whole-catalog dump; not source_id-restricted, so no batching).\n- data/hip2_van_leeuwen.tsv (new, LFS ~10 MB) — schema and column naming per the live VizieR I/311/hip2 probe (column names in the published HIP2 paper differ from what the VizieR TAP table exposes; the script renames on write).\n- package.json — \"refresh:hip2\" alias.\n\n# Schema (live VizieR I/311/hip2 → TSV mapping)\n\nVizieR exposes the table with these case-sensitive column names; the TSV uses Stellata-canonical lowercase names matching the rest of the catalogue pipeline:\n\n  HIP    → hip                (int)\n  RArad  → ra_icrs            (float, deg)   # name is a misnomer — values are in degrees, not radians\n  DErad  → de_icrs            (float, deg)\n  Plx    → plx                (float, mas)\n  e_Plx  → e_plx              (float, mas)\n  pmRA   → pm_ra              (float, mas/yr)\n  pmDE   → pm_de              (float, mas/yr)\n  e_pmRA → e_pm_ra            (float, mas/yr)\n  e_pmDE → e_pm_de            (float, mas/yr)\n  F2     → goodness_of_fit    (float, dimensionless — HIP2's chi² GoF statistic; ~N(0,1) for good solutions)\n  Ntr    → n_transits         (int)          # # of HIP transits used in the astrometric solution\n\nref_epoch is NOT a column — it is a global constant J1991.25 for the whole catalogue, documented in the script docstring rather than written per-row.\n\n# Acceptance\n\n- Row count between 117_000 and 118_500 (HIP2 has 117,955 stars; small drift per Vizier mirror).\n- Schema validated via refresh_lib.validate_schema against the actual VizieR column names.\n- Spot-check Sirius A (HIP 32349): pm_ra ≈ -546.01 mas/yr, pm_de ≈ -1223.07 mas/yr (live VizieR I/311/hip2 values; the paper's nominal -546.05 / -1223.14 differs by ~0.05 mas/yr from the VizieR mirror). Compare against Gaia DR3's smaller pmRA values to confirm the saturation-cliff discrepancy that motivates the fallback.\n\n# Smoke\n\n- npm run refresh:hip2 writes the TSV\n- wc -l matches the expected row count band\n- Spot-check Sirius A row matches the live values within 0.1 mas/yr\n\n# Commit\n\nOne commit. Standard PR.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:15:51Z","created_by":"Alex Marshall","updated_at":"2026-05-18T17:38:56Z","started_at":"2026-05-18T17:25:13Z","closed_at":"2026-05-18T17:38:56Z","close_reason":"Closed","labels":["hipparcos","phase-1","refresh-scripts"],"dependencies":[{"issue_id":"stellata-dch.24","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:15:51Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.24","depends_on_id":"stellata-dch.21","type":"blocks","created_at":"2026-05-15T11:24:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.23","title":"Phase 1 — scripts/refresh-gaia-tyc-xmatch.py + commit data/gaia_dr3_tyc_xmatch.tsv","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nTycho-2 -\u003e Gaia DR3 source_id cross-walk. Catches stars without HIP coverage (Tycho-2 reaches V~11.5 vs HIP's V~9-10). Depends on stellata-dch.21 (refresh_lib).\n\n# Deliverables\n\n- scripts/refresh-gaia-tyc-xmatch.py (new) — ADQL pull from gaiadr3.tycho2tdsc_merge_best_neighbour via refresh_lib.TapClient (~2.52M rows; single-query — no batching needed, the merge_best_neighbour table is small enough for one async TAP job).\n- data/gaia_dr3_tyc_xmatch.tsv (new, LFS-tracked) — columns below, sorted by tyc (lexicographic; matches source).\n- package.json — new \"refresh:gaia-tyc\" alias.\n\n# ADQL query\n\nSELECT\n  original_ext_source_id AS tyc,\n  source_id              AS gaia_source_id,\n  angular_distance,\n  number_of_neighbours,\n  xm_flag\nFROM gaiadr3.tycho2tdsc_merge_best_neighbour\nWHERE source_id IS NOT NULL\nORDER BY original_ext_source_id\n\n# TSV schema (5 columns)\n\n- tyc (string, \"NNNN-NNNN-N\", no \"TYC \" prefix) — Tycho-2 / TDSC identifier\n- gaia_source_id (int64) — Gaia DR3 source_id\n- angular_distance (float, 6 decimals) — match separation in arcsec\n- number_of_neighbours (int) — ambiguity flag (1 = unique Gaia neighbour)\n- xm_flag (int) — Gaia cross-match flag\n\nMirrors the dch.22 (HIP) TSV's 5-column shape so Stage 1 indexers can ingest both with the same parser.\n\n# Acceptance\n\n- Row count between 2_510_000 and 2_530_000 (pinned around observed 2,518,330).\n- Schema validated by refresh_lib.validate_schema.\n- Deterministic regen: re-running produces a byte-identical TSV.\n\n# Smoke\n\n- npm run refresh:gaia-tyc writes the TSV deterministically.\n- wc -l shows ~2,518,331 lines (rows + header).\n- Spot-check: TYC 425-2502-1 (Barnard's Star) -\u003e source_id 4472832130942575872, angular_distance ~= 0.201663 arcsec (cross-validates against the HIP xmatch — both refer to the same Gaia DR3 source).\n\n# Commit\n\nOne commit. Standard PR with skip-version-bump label (pure infrastructure).\n\n# Drift corrected from original bead spec (2026-05-15 author had three table-schema errors caught by live TAP probe)\n\n- Table name was `tyco2tdsc_merge_best_neighbour` — actual is `tycho2tdsc_merge_best_neighbour` (missing 'h'). Both `gaiadr3.*` and `gaiaedr3.*` variants exist; we use the DR3 one.\n- ADQL column `magnitude_difference` does NOT exist in this best-neighbour table — same shape of error as dch.22's HIP table. Replaced with `number_of_neighbours` + `xm_flag` (the only ambiguity flags published here), matching the HIP TSV's 5-column shape exactly.\n- TYC format is `NNNN-NNNN-N` (no \"TYC \" prefix). Three components: GSC region (1-9999), GSC number (1-12119), component letter encoded as int (1-4). Stored as a single string column.\n- Row count was specified as [2_400_000, 2_600_000] — actual is 2,518,330. Tightened to [2,510,000, 2,530,000] (DR3 is frozen).\n\nLABELS: gaia, phase-1, refresh-scripts, tycho","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:15:37Z","created_by":"Alex Marshall","updated_at":"2026-05-18T10:40:14Z","started_at":"2026-05-18T10:23:13Z","closed_at":"2026-05-18T10:40:14Z","close_reason":"Merged in PR #91","labels":["gaia","phase-1","pr-91","refresh-scripts","tycho"],"dependencies":[{"issue_id":"stellata-dch.23","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:15:37Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.23","depends_on_id":"stellata-dch.21","type":"blocks","created_at":"2026-05-15T11:24:31Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.22","title":"Phase 1 — scripts/refresh-gaia-hip-xmatch.py + commit data/gaia_dr3_hip_xmatch.tsv","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nRefresh script + committed TSV for the HIP -\u003e Gaia DR3 source_id cross-walk. This is THE cornerstone of the new pipeline: every WDS component with a HIP id resolves to a Gaia source_id via this table without ever touching a position match. Depends on scripts/refresh_lib.py (stellata-dch.21).\n\nGaia DR3's `hipparcos2_best_neighbour` saturates on the brightest stars: Sirius (HIP 32349), Polaris (HIP 11767), Vega (HIP 91262) and other V \u003c= 4 stars are ABSENT from this cross-walk. The Hipparcos-2-anchored fallback in dch.24 handles them.\n\n# Deliverables\n\n- scripts/refresh-gaia-hip-xmatch.py (new) — ADQL pull from gaiadr3.hipparcos2_best_neighbour. Idempotent (--force to override). Documents the full ADQL query in module docstring.\n- data/gaia_dr3_hip_xmatch.tsv (new, LFS-tracked; ~3.7 MB) — columns below, sorted by hip.\n- package.json — new \"refresh:gaia-hip\" npm alias.\n- .gitattributes — add data/*.tsv LFS pattern.\n\n# ADQL query\n\nSELECT\n  original_ext_source_id AS hip,\n  source_id              AS gaia_source_id,\n  angular_distance,\n  number_of_neighbours,\n  xm_flag\nFROM gaiadr3.hipparcos2_best_neighbour\nWHERE source_id IS NOT NULL\nORDER BY original_ext_source_id\n\n# TSV schema (5 columns)\n\n- hip (int) — Hipparcos identifier\n- gaia_source_id (int64) — Gaia DR3 source_id\n- angular_distance (float, 6 decimals) — match separation in arcsec\n- number_of_neighbours (int) — ambiguity flag (1 = unique Gaia neighbour)\n- xm_flag (int) — Gaia cross-match flag\n\n# Acceptance\n\n- Row count between 99_400 and 99_600. Empirical observation: 99,525 rows. Pinned in script as EXPECTED_ROW_COUNT_MIN / MAX; out-of-range fails the build.\n- Schema validated by refresh_lib.validate_schema.\n- Deterministic regen: re-running with the source unchanged produces a byte-identical TSV.\n\n# Smoke\n\n- npm run refresh:gaia-hip writes the TSV deterministically\n- wc -l data/gaia_dr3_hip_xmatch.tsv shows ~99,526 lines (99,525 rows + header)\n- Spot-check: HIP 87937 (Barnard's Star) -\u003e source_id 4472832130942575872, angular_distance ~= 0.394425 arcsec\n- Sirius (HIP 32349) is correctly ABSENT (Gaia saturation)\n\n# Commit\n\nTwo topical commits on one PR (per commit-granularity):\n1. fix(refresh_lib): numpy 2.x dtype + float matching — np.issubdtype(int32, int) is False in numpy 2.x, broke validate_schema; isinstance(np.float32(...), float) is False, broke write_tsv's round_floats. Both fixed in lib + tests.\n2. feat: refresh-gaia-hip-xmatch.py + TSV (dch.22).\n\nPR labelled skip-version-bump (no user-visible behavior; pure infrastructure).\n\n# Drift corrected from original bead spec (2026-05-15 author had two table-schema errors)\n\n- Row count was specified as [110_000, 118_000] — actual is 99,525 (~117k published HIP count includes stars Gaia could not observe).\n- ADQL columns `hip` and `magnitude_difference` do not exist in gaiadr3.hipparcos2_best_neighbour. The HIP id column is `original_ext_source_id`; no magnitude difference is published in this best-neighbour table. Schema confirmed via TAP probe.\n- Spot-check Sirius source_id 2947050466531873024 is not retrievable from this table (Sirius absent). Replaced with Barnard's Star.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:15:23Z","created_by":"Alex Marshall","updated_at":"2026-05-18T10:21:41Z","started_at":"2026-05-18T10:02:00Z","closed_at":"2026-05-18T10:21:41Z","close_reason":"Merged in PR #90","labels":["gaia","hip","phase-1","refresh-scripts"],"dependencies":[{"issue_id":"stellata-dch.22","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:15:22Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.22","depends_on_id":"stellata-dch.21","type":"blocks","created_at":"2026-05-15T11:24:30Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.21","title":"Phase 1 — scripts/refresh_lib.py: shared TAP client + retry + batching + schema validation","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nShared infrastructure for all Phase 1 refresh scripts (HIP-Gaia, Tyc-Gaia, Hipparcos2, NSS, Astrometry, SIMBAD, Bailer-Jones, Apsis — 7+ scripts). Well past the two-call-site DRY threshold (memory stellata-named-constants-and-dry), so the shared lib lands first.\n\n# Deliverables\n\n- scripts/refresh_lib.py (new) — TAP client supporting both astroquery.gaia (ESA TAP) and CDS TAP (pyvo) with auto-fallback on transient (5xx / network) errors; exponential-backoff retry with injectable sleep/rand for deterministic tests; batching helper for chunked IN-list queries; output-schema-validation helper (column names + dtypes match expected); idempotency-check helper (mtime-based, mirrors scripts/build-clouds.py:is_up_to_date()); canonical TSV writer.\n- scripts/requirements-refresh.txt (new) — pinned (lower-bound) versions of astropy, astroquery, numpy, pyvo, requests. Documented venv setup in module docstring.\n- scripts/refresh_lib.test.py (new) — Python unittest covering retry behaviour, schema validation, idempotency, batching, TSV writer, and TapClient fallback. (Vitest not used because astroquery isn't Node-amenable.)\n- .gitignore — add scripts/__pycache__/ (refresh_lib gets imported by the test, generating .pyc files the existing build-* scripts never produced).\n\n# Acceptance\n\n- importable from each Phase 1 refresh script (verified when those land)\n- `python3 scripts/refresh_lib.test.py` green (the `.test.py` suffix matches the project's *.test.ts convention but conflicts with `python -m unittest`'s dotted-module loader — run via the __main__ block instead)\n- module docstring documents the two TAP backends + the auto-fallback policy + the venv setup\n\n# Smoke\n\n- Activate venv: `python3 -m venv .venv \u0026\u0026 .venv/bin/pip install -r scripts/requirements-refresh.txt`\n- Run tests: `.venv/bin/python scripts/refresh_lib.test.py` (or just `python3 scripts/refresh_lib.test.py` — the suite uses only stdlib so the venv isn't strictly required for the tests themselves; the deps are for the live refresh scripts that will land in dch.22-25, dch.29, dch.46, dch.48).\n\n# Files to touch\n\n- scripts/refresh_lib.py (new)\n- scripts/requirements-refresh.txt (new)\n- scripts/refresh_lib.test.py (new)\n- .gitignore (add scripts/__pycache__/)\n\n# Commit\n\nOne commit. PR labelled skip-version-bump (no user-visible behavior; pure infrastructure).","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:15:03Z","created_by":"Alex Marshall","updated_at":"2026-05-18T10:00:23Z","started_at":"2026-05-18T09:43:34Z","closed_at":"2026-05-18T10:00:23Z","close_reason":"Closed","labels":["phase-1","python","refresh-scripts"],"dependencies":[{"issue_id":"stellata-dch.21","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:15:03Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":7,"comment_count":0}
{"_type":"issue","id":"stellata-dch.18","title":"Run Gaia DR3 ADQL upload + commit gaia_dr3_binaries.tsv to engage WDS optical-pair filter","description":"# Why\n\nbuild-binaries.py's optical-pair filter cascade has three tiers:\n1. WDS-Notes flag chars (S/U/X/Y → optical; T/V/Z → physical) — set on\n   maybe 40% of pairs.\n2. Gaia DR3 parallax 3σ overlap + ~5 mas/yr common-PM — the strong gate.\n3. Mag-gap sanity heuristic — weak fallback.\n\nWithout `data/gaia_dr3_binaries.tsv` the cascade runs in degraded mode\n(tiers 1 + 3 only). Optical WDS pairs without an explicit Notes flag\nslip through as \"unknown:kept\", and build-binaries.py injects SYN-NNN\nsecondaries for them at the primary's heliocentric distance. Those are\ngenuine fake stars in the model — most visibly the ~4-5 spurious\ncompanions around Sirius (WDS A-C, A-D, A-E, A-F, A-G entries that are\nreally kpc-scale background stars, not the 2.64 pc binary partner).\n\nSirius B (the real white dwarf, WDS A-B) is unaffected — its\nparallax / PM agreement with A would survive either filter.\n\n# Scope\n\nUser-driven manual data refresh — no Stellata code changes.\n\n1. Run `npm run build:binaries` once to regenerate `data/wds_upload.csv`\n   (component ra/dec list).\n2. Upload `wds_upload.csv` to the Gaia archive\n   (https://gea.esac.esa.int/archive/) as a user table.\n3. ADQL JOIN against `gaiadr3.gaia_source` within 1″ (procedure\n   detailed in SCIENCE.md \"Multiple-star pipeline\" section).\n4. Download the JOIN result as `data/gaia_dr3_binaries.tsv` with at\n   minimum columns: `wds_id, comp, parallax, parallax_error, pmra, pmdec`.\n5. `git lfs track 'data/gaia_dr3_binaries.tsv'` if not already covered\n   by the `data/*.tsv` LFS pattern.\n6. Re-run `npm run build:catalog` and verify the build log shows\n   `gaia:optical` / `gaia:physical` filter tags appearing in the\n   per-pair source field.\n\n# Smoke / acceptance\n\n- `data/gaia_dr3_binaries.tsv` committed (LFS).\n- Build log shows Gaia filter engaged (non-zero `gaia:optical` rejects).\n- Spurious SYN-NNN count around Sirius drops to zero (visual smoke:\n  focus Sirius A in the rendered scene, only A + B visible at the\n  binary scale; no kpc-distance background-star injections).\n- α Cen + Toliman + other famous physical pairs still pass through.\n- No regression in `npm test` (renderer side is unchanged).\n\n# Why this blocks dch.10\n\ndch.10 lights up live orbital motion for every Regime 2/3 binary in\nthe catalog. If the catalog contains SYN-NNN injections from optical\npairs (no real orbit physics; their orbital elements are nonsense),\nthey'd visibly drift / oscillate alongside real binaries and amplify\nthe spurious-star problem under warp. Land this first so dch.10\noperates on a clean catalog.","status":"closed","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T14:14:28Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:14:13Z","closed_at":"2026-05-15T10:14:13Z","close_reason":"superseded by Phase 1 refresh scripts (stellata-dch.22, 23, 24, 25). Position-anchored Gaia upload approach replaced with HIP/Tycho/source_id-anchored ADQL queries. The 43k-row TSV produced 2026-05-14 is discarded; refresh-gaia-binaries.py likewise.","dependencies":[{"issue_id":"stellata-dch.18","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-12T15:14:27Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.13","title":"Perf regression: idle frame.total 0.25→0.70ms after multiples catalog grows 9%","description":"# Symptom\n\nAfter `stellata-dch.6` lands, idle-frame CPU cost more than doubles:\n\n|                    | main (313k records) | dch.6 (341k records) |\n|--------------------|---------------------|----------------------|\n| FPS, idle          | 1500-1800           | 800-1000             |\n| `onFrame.total`    | ~0.25 ms            | ~0.70 ms             |\n| `gpu.render`       | ~0.25 ms            | ~0.25 ms             |\n\nCatalog grew 9% (`recordCount` 313,242 → 341,146, mostly SYN secondary\ninjections from `data/multiples.tsv`). GPU draw cost barely shifted —\nthe regression is entirely on the CPU side, in onFrame handler chain.\n\n# Hypothesis\n\nTwo candidates worth pinning down before changing code:\n\n1. **Cache-miss inflection.** The 14 MB catalog + 13 MB search-index\n   working set may have crossed L2/L3 cache size on the test machine,\n   making memory access on every star-position read more expensive\n   even though no per-frame loop walked all 341k records.\n2. **Per-frame walk that scales with N.** Some onFrame handler I'm\n   not aware of iterates over `cat.count`. Per the docs/performance.md\n   chart-mode optimisations, the sorted-distance core-mask window\n   should be bounded — but worth confirming under the new catalog\n   size.\n\n# Procedure\n\n1. Open `?perf` (or the keyboard shortcut that toggles the perf HUD)\n   on both branches at the same focus / camera state.\n2. Compare per-section breakdown (`controls.update`, `coreMask`,\n   `pre-render`, `gpu.render`, individual chart-* if chart on).\n3. Whichever section regressed by ~0.45 ms is the culprit.\n\n# Likely fixes (gate on profile)\n\n- If `coreMask` regressed: tighten the sorted-distance window or\n  early-exit on `distFromCamera \u003e threshold`.\n- If a specific handler regressed: cache its work in a dirty-tracked\n  way per `stellata-consistency-at-the-seam` (sentinel-init rules).\n- If no single section accounts for it: cache-miss, more involved —\n  consider splitting the catalog into hot (frequently-touched per\n  frame) vs cold (looked up only on focus / search) typed-array views.\n\n# Acceptance\n\n- `onFrame.total` returns to within 10% of the main-branch value at\n  the same focus / camera state.\n- No correctness regressions in the existing test suite.\n- A pinned vitest on whatever pure-helper changed (per\n  `stellata-test-coverage-discipline`).","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-12T10:50:39Z","created_by":"Alex Marshall","updated_at":"2026-05-18T19:25:03Z","started_at":"2026-05-12T14:18:55Z","closed_at":"2026-05-18T19:25:03Z","close_reason":"No longer reproduces after stellata-9mm.190.1 (PR #68, merged 2026-05-16) made perf-hud tear down realMark/realMeasure/realFrame on debug-panel close. The original measurement was likely contaminated by the perf-hud install staying warm for the rest of the session once the panel had been opened — the ~12 perfMark/perfMeasure/perfFrame call sites per animate() frame kept doing real Map ops/ring writes even with the panel closed, which is exactly the kind of small-constant per-frame regression that scales with catalog growth. No perf issue observed in routine use since the 190.1 fix landed; re-file with a fresh capture if it reappears.","labels":["multiples","perf","regression"],"dependencies":[{"issue_id":"stellata-dch.13","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-12T11:50:38Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2mi","title":"Debug panel: hidden triple-tap D shortcut + fold debug.arrows() into main panel","description":"Two small debug-panel UX changes for Alex's own testing convenience.\n\n# 1. Hidden triple-tap 'D' to open the debug panel\n\nAdd a triple-tap on the 'D' key as a hidden affordance that opens the unified debug panel (same target as `window.debug.panel()`). Detection: three keydown events on KeyD within a short rolling window (e.g. ~500ms total). Wire into the existing global keydown dispatch in `keyboard-shortcuts.ts`.\n\nAcceptance:\n- Triple-tapping D anywhere (outside text inputs) opens the debug panel.\n- Single/double tap does nothing (no spurious panel opens).\n- Modifier keys (Cmd/Ctrl/Alt/Shift) suppress the trigger so it doesn't interfere with browser shortcuts.\n- NOT documented in `keyboard-shortcuts.ts` `help-modal.ts`, `docs/ui-and-controls.md`, README, or the in-app `?` help overlay. Hidden affordance — keep it that way.\n\n# 2. Fold debug.arrows() into the unified debug panel\n\n`arrow-fade-debug-hud.ts` is currently its own floating panel hosted by `window.debug.arrows()` (see CLAUDE.md module roster). Move its UI into the main debug panel as a regular section (alongside `star-tuning`, `milkyway-tuning`, `perf-hud`, `pin-debug-hud`) and remove the standalone `debug.arrows()` entry point + its floating-panel chrome.\n\nAcceptance:\n- `debug.panel()` (and the new triple-tap) reveals an \"Arrows\" section showing the same Sol/GC arrow draw state + fade telemetry currently in `arrow-fade-debug-hud.ts`.\n- `window.debug.arrows()` is removed; no callers remain (`grep -rn \"debug.arrows\" src/`).\n- `arrow-fade-debug-hud.ts` either deleted or refactored to a section module matching the shape of the other tuning sections (see `debug-panel.ts` chrome + section helpers).\n- CLAUDE.md module roster updated to reflect the consolidation.\n\n# Smoke\n- `npm run typecheck \u0026\u0026 npm test`\n- Manual: open page, triple-tap D, confirm panel appears with the new Arrows section showing live draw state.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-12T10:48:41Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:37:19Z","started_at":"2026-05-15T11:17:09Z","closed_at":"2026-05-16T21:37:19Z","close_reason":"merged via PR #65 (Triple-tap D + fold arrow HUD into debug panel)","labels":["debug","ui"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ml8","title":"Sol/GC arrows flash on HUD enable when close-zoomed; distance-vector fade gate likely mis-wired","description":"# Two related symptoms (likely one underlying defect)\n\n## 1. Sol/GC pointer arrows flash on HUD enable (core bug)\n\nWhen the user toggles the HUD on while already close-zoomed on a star, the Sol and GC reference arrows render briefly before the close-approach fade hides them — a visible flash.\n\nHypothesis: the toggle-on path in \\`galactic-overlay.ts\\` makes the arrows visible without first evaluating the fade alpha (\\`coverage\\` over \\`refLenPx\\`). The fade is computed on the next animation tick, so frame 0 after toggle-on paints at alpha=1 even though steady-state alpha is 0.\n\n## 2. Distance-vector arrow doesn't appear to obey the fade gate at all\n\nIndependent of the flash: with the HUD enabled the distance-vector chevron is hidden, and with the HUD disabled it is shown — *regardless of star disc size*. That contradicts the design intent from [[stellata-a7d.2.2]] (closed, PR #22), which specified a single fade alpha computed from \\`max(solLenPx, gcLenPx)\\` and applied to **all three reference arrows: Sol, GC, and distance-vector**.\n\nSo the distance-vector either:\n- never had the a7d.2.2 fade alpha wired in (peer-coverage miss at original implementation time), or\n- had it wired and lost it in a later refactor (e.g. stellata-77m EventBus rewrite, stellata-akk SVG dirty-track perf pass, stellata-a7d.2.11 origin-recentre changes), or\n- is hidden via a separate gate keyed to HUD-visibility instead of disc-coverage.\n\n# Decision left open for the implementer\n\nThe user noted he isn't sure what the *correct* policy for the distance-vector arrow should be — it's a longer arrow than Sol/GC chevrons by design, so it can reasonably outlast them. Options to evaluate as part of the fix:\n\n- **A. Match a7d.2.2's stated design**: distance-vector fades with Sol/GC on the same \\`coverage\\` from \\`max(solLenPx, gcLenPx)\\`. Pro: matches intent. Con: distance-vector can be much longer than the chevrons; fading it on chevron coverage may hide a still-readable arrow.\n- **B. Per-arrow coverage**: each arrow uses its own \\`projAlong / lenPx\\` against the disc radius. Distance-vector fades only when the disc would actually start covering *it*.\n- **C. Keep current asymmetry, but document it**: distance-vector visibility tied to HUD toggle, Sol/GC visibility tied to fade. Probably the least defensible — user is currently surprised by it.\n\nPick one, justify it in the PR body, and update [[stellata-a7d.2.2]]'s closure rationale prose if the chosen policy contradicts the original a7d.2.2 description (per [[stellata-rename-and-stale-prose-sweep]]).\n\n# Files\n\n- \\`src/client/galactic-overlay.ts\\` — Sol/GC arrow rendering and fade computation\n- \\`src/client/distance-vector-overlay.ts\\` — distance-vector chevron rendering; check whether the a7d.2.2 fade alpha reaches this overlay\n- \\`src/client/arrow-fade-debug-hud.ts\\` — \\`debug.arrows()\\` HUD; useful for visual diagnosis of which alpha each arrow is computing at toggle-on\n\n# Repro\n\n1. Focus a nearby star (e.g. Sirius), zoom until its disc is visibly large at the focus-ring rim.\n2. Symptom 1: toggle HUD off, toggle HUD on with keyboard shortcut — observe Sol/GC arrows flash briefly before fading out.\n3. Symptom 2: with HUD enabled, observe distance-vector hidden; toggle HUD off — distance-vector now visible regardless of how close-zoomed you are.\n\n# Acceptance\n\n- Toggling HUD on while close-zoomed produces no flash on Sol or GC arrows — the fade alpha is evaluated synchronously at toggle-on (or the arrows start hidden and animate in only if alpha \u003e 0).\n- Distance-vector arrow visibility policy is consistent and documented — whichever of (A/B/C) is chosen, the behaviour is stable across HUD toggle, across close-zoom range, and across navigate↔observe transitions.\n- \\`debug.arrows()\\` HUD reflects the chosen policy: any per-arrow alpha shown there matches what's rendered.\n- [[stellata-a7d.2.2]] prose is updated if the chosen distance-vector policy diverges from \"all three arrows share fade alpha\".\n\n# Test plan\n\nManual smoke (per [[alex-stellata-smoke-tests]]):\n- Sirius close-zoom, HUD toggle off/on, no flash. With HUD on, also verify the distance-vector behaves per the chosen policy.\n- Repeat with Vega + observer mode entry/exit to confirm no observe-mode regression.\n- Open debug.arrows() and confirm alpha values match rendered state in both navigate and observe.\n\nVitest: if any pure helper is extracted from the fade-alpha computation (per [[stellata-test-coverage-discipline]]), unit-test it for the toggle-on initial-state edge case.","status":"closed","priority":1,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-12T07:32:30Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:37:24Z","started_at":"2026-05-16T15:05:39Z","closed_at":"2026-05-16T21:37:24Z","close_reason":"merged via PR #70 (per-arrow fade alpha — HUD toggle-on flash fix + distance-vector policy); retrospective-review fallout in PR #71","labels":["bug","overlays","ux"],"dependencies":[{"issue_id":"stellata-ml8","depends_on_id":"stellata-a7d.2.2","type":"related","created_at":"2026-05-12T08:37:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-r9q.2","title":"Smooth focus-park behavior + generic mechanism, applied to stars","description":"# Problem\n\n`focusStar()` at `src/client/stellata.ts:2169-2176` teleports the camera to `minDistForStar(idx)` unconditionally. Result:\n\n- When already close to a star: jarring snap.\n- When far away: no sense of 3D travel.\n\nThis is the user-visible UX fix for the parent epic `stellata-r9q`.\n\n# Spec (per Alex's 2026-05-11 walk-through)\n\n## New park distance formula\n\n\\`\\`\\`\nparkDist = max(AU_PC + R_pc, dMinFloor [, extraFloor])\n\\`\\`\\`\n\n- `AU_PC` imported from the shared astronomy-constants file (Child 1).\n- `R_pc` for stars = `Reff = catalog.physicalRadius[idx] * R_SUN_PC * peakAmplitudeFactor(idx)`. **Use Reff** (handles variable stars correctly — Alex confirmed this is canonical, no debate).\n- `dMinFloor` for stars = `distAtFillFraction(Reff, fovMinor, ZOOM_FLOOR_FRACTION=0.9)` — the current 90 %-fill floor exposed by `minOrbitDistForStar` at `src/client/stellata.ts:3136-3141`.\n- `extraFloor` for stars = `binaryCompanionFloorPc(idx)`.\n\nFor Sol (R ≈ 0.0046 AU): parks at ~1.005 AU, just outside Earth's orbit.\nFor Betelgeuse (R ≈ 4.65 AU): `dMinFloor` clamps so star fills ~90 %, not 100 %.\n\n## Branching in focusStar\n\nCompute `parkDist`. Then:\n\n- If `currentEyeDist \u003c= parkDist`: **do not move the camera.** Update `controls.target` and `controls.minDistance`, broadcast focus state, but the camera stays put.\n- If `currentEyeDist \u003e parkDist`: animate position (and any needed reorient) over `CAMERA_LERP_MS = 2000` ms. Fast for very distant stars is intentional — conveys scale.\n\n## One canonical 2 s constant\n\nToday there are two `= 2000` literals for camera-move durations:\n\n- `WARP_REORIENT_MS = 2000` at `src/client/stellata.ts:272`\n- `AIM_T_MAX_MS = 2000` at `src/client/stellata.ts:283`\n\n(`WARP_T_K_MS = 2000` at line 271 stays separate — it is a log-scale flight coefficient with different semantics.)\n\nConsolidate to ONE definition that all three usages (warp reorient, aim, new focus-lerp) import. Per Alex: \\\"set that in one place and then alias it as you suggest so it's clear that it's a shared constant across the app.\\\" Pick a clear canonical name (e.g. `CAMERA_LERP_MS`) — implementation detail; the constraint is one literal, not the spelling.\n\n## Generic-from-day-one mechanism\n\nPer Alex: \\\"I want the same approach for future objects: clouds, planets, nebulae, etc. We MUST write code that is reusable.\\\"\n\nExtract pure helpers in a new module (e.g. `src/client/focus-transition.ts`):\n\n\\`\\`\\`ts\n// Pure, testable.\nparkDistance(opts: { R_pc: number; fovMinorRad: number; dMinFloor: number; extraFloor?: number }): number;\n\n// Animation primitive — small named helper that takes the target, current camera state,\n// duration, and emits the lerp tween. focusStar / focusCloud / focusPlanet all call this\n// with their own parkDistance(...) inputs.\nfocusLerpTo(target, parkDist, durationMs): void;\n\\`\\`\\`\n\nApply these to stars in this bead. Children stellata-r9q.3 and stellata-r9q.4 will route clouds and planets through the same primitives.\n\n# Files\n\n- `src/client/stellata.ts`\n  - `minDistForStar()` at l. 3167-3174 — either rewrite or replace by a sibling `parkDistForStar(idx)` that delegates to the generic helper. Decide whether the existing manual-zoom floor (`minOrbitDistForStar`) stays untouched (it should — that's the user-visible 90 %-fill floor; the change is only to where the camera *automatically lands* on focus).\n  - `focusStar()` at l. 2159-2186 — branch on currentEyeDist vs parkDist.\n  - Constant definitions at l. 272 and l. 283.\n- New: `src/client/focus-transition.ts` housing `parkDistance(...)` + focus-lerp animation primitive.\n- Imports `AU_PC`, `R_SUN_PC` from astronomy-constants file (depends-on stellata-r9q.1 / Child 1).\n\n# Open implementation questions (resolve at work-time)\n\n- Naming for the canonical 2 s constant — `CAMERA_LERP_MS`, or keep `WARP_REORIENT_MS` as the canonical name and alias the others to it. Either is fine; the constraint is exactly one literal in the codebase.\n- How the focus-lerp interacts with the warp state machine (likely fully separate: focus-lerp is the close-range nudge; warp is the log-scaled long-haul fly).\n- Whether `binaryCompanionFloorPc(idx)` stays in the `max(...)` for stars (probably yes).\n\n# Smoke (manual — per `alex-stellata-smoke-tests`, no dev server, no Playwright)\n\n1. Park near Sol at ~0.5 AU via `?v=` URL; click-focus Sol → camera does NOT move; just the focus indicators appear.\n2. Default Sol view (~5 AU); focus Sol → smooth ~2 s glide inward to ~1 AU.\n3. From Sol, focus Betelgeuse → ~2 s glide; park clamps to `dMin`; Betelgeuse fills ~90 % of viewport, not 100 %.\n4. From Sol, focus a faint distant star (via search) → ~2 s glide over many parsecs (intentionally fast — conveys distance).\n5. Already-focused star, re-focus via click or search → no double-snap.\n6. URL-restore focus from a saved view → arrives parked correctly (no animation on URL restore).\n7. Existing long-haul warp behavior unaffected — `WARP_T_K_MS` and the log-scaled flight unchanged.\n\n# Test coverage (per `stellata-test-coverage-discipline`)\n\n- Unit tests for `parkDistance(...)` covering: Sol (~1.005 AU result, pinned with `toBe` not `toBeLessThanOrEqual`), a giant (dMin clamps), a star with binary companion floor, a variable (Reff \u003e R).\n- Read-back assertion that on focus-when-already-inside-park, camera position is unchanged (within float epsilon).\n- Test that the canonical 2 s constant is exactly one literal in the codebase.\n\n# Depends on\n\nstellata-r9q.1 / Child 1 (constants consolidation).\n\n# Blocks\n\nstellata-r9q.3 / Child 3 (cloud migration) and stellata-r9q.4 / Child 4 (planet migration, deferred).","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T12:13:42Z","created_by":"Alex Marshall","updated_at":"2026-05-12T07:29:00Z","started_at":"2026-05-11T20:18:59Z","closed_at":"2026-05-12T07:29:00Z","close_reason":"Closed","labels":["pr-61","ux"],"dependencies":[{"issue_id":"stellata-r9q.2","depends_on_id":"stellata-r9q","type":"parent-child","created_at":"2026-05-11T13:13:42Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-r9q.2","depends_on_id":"stellata-r9q.1","type":"blocks","created_at":"2026-05-11T13:27:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-r9q","title":"Smooth focus-park UX, generic across focusable objects","description":"# Problem\n\nFocusing a star currently teleports the camera to `minDistForStar(idx)` (`src/client/stellata.ts:2169-2176`). When the camera is already close, this snaps backwards/forwards jarringly. When far away, the user loses the sense of moving *through* 3D space.\n\n# Goal\n\nReplace the teleport-on-focus snap with a lerp-or-noop, using a **generic mechanism** that applies uniformly to stars (now), molecular clouds, planets, and future focusable types (nebulae, etc.). Reusable from day one — don't reinvent the wheel for every object type.\n\n# Behavior (per Alex's spec)\n\n1. **New park distance formula:** `parkDist = max(1 AU + R_star, dMin)` where `dMin` is the existing 90 %-fill manual-zoom floor (`minOrbitDistForStar` = `distAtFillFraction(R, fov, ZOOM_FLOOR_FRACTION=0.9)`).\n   - For Sol (R ≈ 0.0046 AU): parks at ~1.005 AU, just outside Earth's orbit.\n   - For giants (e.g. Betelgeuse, R ≈ 4.65 AU): `dMin` clamps the formula so the star doesn't fill the viewport.\n2. **On focus, branch on current eye-to-object distance:**\n   - If `eyeDist \u003c= parkDist`: leave camera in place. Update `controls.target` and `minDistance` but don't move the camera.\n   - If `eyeDist \u003e parkDist`: animate camera to the park position over **2 s**. Fast for very distant stars is intentional — it conveys the scale of interstellar distances.\n3. **2 s constant must be the existing `WARP_REORIENT_MS` / `AIM_T_MAX_MS = 2000`** consolidated to one canonical name — no new `= 2000` literals. (`WARP_T_K_MS = 2000` stays separate; it's a log-scale flight coefficient, not a camera-move duration.)\n4. **`Reff = R * peakAmplitudeFactor(idx)`** is the canonical radius for the star formula (handles variable stars correctly).\n\n# Why P1\n\nPer `stellata-prioritization-framework`: \"Visible UX latency / jank that hits hot user paths.\" Star focus is the central interaction; the snap affects every user.\n\n# Structure\n\n- Child 1 — Consolidate astronomical constants into a single source file (blocks Child 2).\n- Child 2 — Smooth focus-park + generic mechanism + apply to stars (the user-visible UX fix; blocks Children 3 \u0026 4).\n- Child 3 — Apply to molecular cloud focus.\n- Child 4 — Apply to planet focus (filed under `stellata-3re`, deferred until planets become focusable; related-link back here).\n\nPlus a separate sweep bead under `stellata-9mm` covering codebase-wide consolidation of astronomical constants (shaders, build scripts, tests), depending on Child 1.","status":"closed","priority":1,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-11T12:12:23Z","created_by":"Alex Marshall","updated_at":"2026-05-12T07:29:00Z","closed_at":"2026-05-12T07:29:00Z","close_reason":"Closed","labels":["ux"],"dependencies":[{"issue_id":"stellata-r9q","depends_on_id":"stellata-3re.21","type":"related","created_at":"2026-05-11T13:27:27Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-8ee","title":"Heliopause label visible on first-load (camera inside heliopause shell)","description":"# Symptom\n\nOn a bare-URL first load (no `?v=` fragment), the \"Heliopause\" SVG label is visible at the top of the screen even though the camera is parked inside the heliopause shell. From inside the bubble there is no projected silhouette to anchor against, so the label should not be shown.\n\n# Why this happens\n\n- First-load parks the camera at 5 AU from Sol (`src/client/first-load.ts` — `PARK_DIST_PC = 5 * AU_PC`).\n- The heliopause shell extends to ~115 AU equatorial and ~122 AU upwind / 200 AU downwind (`src/client/heliopause.ts:46-49`).\n- Camera at 5 AU is therefore deep inside the shell on first load.\n\nThe label-hide logic in `createHeliopauseLabel` (`src/client/heliopause.ts:274-283`) checks each sample and bails with `setVisible(false)` when any sample sits behind the near plane (the \"camera straddles the egg\" regime). That check fires correctly when inside the bubble — *but the resulting hide is a no-op on the very first frame*:\n\n```ts\nlet visible = false;                   // initial state\n...\nconst setVisible = (on: boolean): void =\u003e {\n  if (on === visible) return;          // ← early-out\n  text.style.display = on ? '' : 'none';\n  ...\n};\nsetVisible(false);                     // ← no-op, never writes display:none\n```\n\nThe SVG element `\u003ctext id=\"heliopause-label\"\u003e` in `src/client/index.html:67` has no inline `display: none` and no explicit `x`/`y`, so it renders at its SVG default position (x=0, y=0 with `text-anchor=\"start\" dominant-baseline=\"hanging\"`) until a real visibility transition writes `display: none`. On a session where the camera never crosses the bail threshold (i.e. starts and stays inside the egg), that transition never happens.\n\n# Acceptance criteria\n\n- Bare URL `/` load with no `?v=` fragment: \"Heliopause\" label is NOT visible at any point on first paint or on subsequent frames while the camera remains inside the shell.\n- After warping to a star outside the shell and back to Sol focus, the label appears anchored to the silhouette as designed (no regression on the existing in-flight behaviour).\n- Warping back inside the shell hides the label again.\n\n# Suggested fix\n\nSmallest change: hide the label in markup, either by adding `style=\"display: none\"` to the `\u003ctext id=\"heliopause-label\"\u003e` element in `index.html:67`, OR by writing `text.style.display = 'none'` unconditionally before the initial `setVisible(false)` call in `heliopause.ts:244`.\n\nEither eliminates the \"visible-by-default until first transition\" gap. The same shape (sentinel-init that doesn't force the first write to land) is called out in the `stellata-consistency-at-the-seam` memory under rule 3 (sentinel-init for dirty-track) — same class of bug.\n\n# Smoke step\n\n- Hard-reload `/` with no `?v=` fragment. Confirm no \"Heliopause\" text anywhere.\n- Focus Sol → ensure a planet is in view (label should now appear, anchored to the silhouette bottom-right).\n- Zoom back inside the heliopause via free fly / `minDistance` relax → label hides again.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T09:51:48Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:37:22Z","started_at":"2026-05-15T10:58:46Z","closed_at":"2026-05-16T21:37:22Z","close_reason":"merged via PR #64 (Fix heliopause label visible on first-load)","labels":["phase-12a","solar-system"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.10","title":"Multiple-star matching: BinaryOrbitField runtime — live per-frame orbital evolution (headline feature)","description":"Bead 7/7 of the stellata-dch decomposition (see parent for full spec).\n\n# Goal\n\nThe headline feature. Per-frame walk of stars with `orbitIdx != NO_ORBIT`, evaluate Kepler offset against `Stellata.getT()`, write into the GPU star position buffer via `bufferSubData`. Algol's 2.87-day cycle, α Cen's 79.9-yr orbit, Castor Aa1↔Aa2's ~9-day cycle visibly orbit each other under warp.\n\n# Scope\n\n- `src/client/binary-orbit.ts` (NEW):\n  - `BinaryOrbitField` class — analogue of `PlanetBodyField` (`src/client/planet-body-field.ts` is the reference architecture). Constructor takes the catalog's orbital-elements typed array + the list of star indices with `orbitIdx != NO_ORBIT`.\n  - Per-frame `update(t)`: walk index list, call `evaluateBinaryOffset` from dch.9's pure module, compute final xyz = primary_base_xyz + offset (using FLAG_BINARY_SECONDARY to pick the right side via mass ratio), write perturbed (x,y,z) into the GPU star position buffer via `bufferSubData` (slice update — only the binary subset, ~6000 floats × 3)\n  - Lifecycle: `dispose()` clears any cached buffers (per `stellata-consistency-at-the-seam` lifecycle pairing rule)\n  - Reuses `Stellata.getT()` + `tToJDE` (`src/client/time.ts`) per time-consistency rule\n- `src/client/stellata.ts`:\n  - Construct `BinaryOrbitField` once at catalog load\n  - Register `update()` in the per-frame loop before the GPU draw\n  - Dispose on scene teardown (paired with `bus.on`-style lifecycle rules)\n\n# Final stale-prose sweep\n\nBefore commit: grep entire repo for \"static binary positions\" / \"J2000-only\" / \"build-time computation\" prose introduced by earlier beads (dch.5–dch.8 documented their pieces against an in-progress story); update to reflect the now-live runtime. Per `stellata-rename-and-stale-prose-sweep`.\n\n# Smoke\n\n- `npm test` green (no new unit tests in this bead; logic is already covered by dch.9; this bead is glue + GPU plumbing)\n- Manual (user-driven, no dev server by Claude):\n  1. Focus Algol Aa1 → advance `t` under warp through ~3 days → Aa1 and Aa2 visibly orbit each other\n  2. Focus α Cen A → advance `t` through several decades → A and B relative position drifts along published orbit\n  3. Castor Aa1/Aa2 cycles through ~9 days at warp\n  4. `debug.panel()` perf-hud shows `BinaryOrbitField.update` \u003c 1 ms/frame at default mag cutoffs\n  5. Static (non-orbital) stars unaffected — chart-mode wings glyph still one-per-system; non-binary star positions unchanged\n- Verify `bufferSubData` writes do not interfere with the static star instanced-quad pass\n\n# Doc updates\n\n- `docs/solar-system.md` cross-ref to binary orbital math (shared Kepler pattern between ephemeris.ts and binary-orbit-pure.ts)\n- Either new `docs/binary-orbits.md` or fold into `docs/architecture.md` / `docs/build-and-data.md` — depending on how large the BinaryOrbitField section grows\n- `CLAUDE.md` — `binary-orbit.ts` line in the module roster under `src/client/`\n- Final pass: re-read SCIENCE.md + all `docs/*.md` touched in earlier beads; correct any \"static-only\" / \"build-time-only\" prose","notes":"# Design contracts (filed at dch.8 close)\n\nTwo implementation invariants the runtime must enforce so the orbital elements stored in v5's elements section produce correct on-screen motion, plus one UX consideration triggered by dch.10's headline feature.\n\n## Sky-plane → ICRS projection\n\nORB6's i, ω, Ω are sky-plane angles (i=0 face-on relative to the line of sight; Ω is the position angle of the ascending node, east of north). The Kepler eval produces (north, east) separation on the sky in arcsec. The runtime must:\n\n1. Recover the system's (RA, Dec) from the stored xyz of either component (orbital motion \u003c\u003c position, so either works).\n2. Build the local tangent-plane basis at (α, δ):\n   - east_hat = (-sin α, cos α, 0)\n   - north_hat = (-sin δ cos α, -sin δ sin α, cos δ)\n3. Convert (north_arcsec, east_arcsec) at distance d into a parsec Δxyz: `Δ = (n·north_hat + e·east_hat) · ARCSEC2RAD · d`.\n\n`binary-orbit-pure.ts` (dch.9) is the natural home for this helper — same shape as `sky_offset_to_icrs_xyz` in build-binaries.py, but TypeScript and vitest-covered.\n\n## ΔR(t) baseline, not R(t)\n\nThe stored xyz are the J2000 snapshot of each component's actual ICRS position. A sits at `X_com − q·R(J2000)`; B at `X_com + (1−q)·R(J2000)`. For a Kepler eval at time t, the per-frame offset is:\n\n  ΔR(t) = R(t) − R(J2000)\n  A_offset(t) = −q · ΔR(t)\n  B_offset(t) = +(1−q) · ΔR(t)\n\nApply A_offset / B_offset to the stored xyz. Both R(t) and R(J2000) come from the same Kepler call — cache the per-orbit J2000 baseline at attach time so each frame is a single solve, not two.\n\n## Why these contracts live here, not in dch.8\n\nThe binary's orbital-elements section stores runtime-ready elements but does NOT store the J2000 baseline or the (RA, Dec). Both are reconstructed at runtime from xyz. If dch.10 landed without these contracts, all 896 binaries would orbit on the wrong plane and drift unbounded from their published J2000 positions.\n\n## Clock visibility (UX)\n\ndch.10 makes wall-clock `t` (`Stellata.getT()`) load-bearing for any focused binary, not just Sol's planets. The current `time-readout.ts` gate (\"planet-host focus OR warp active\") was scoped to the solar-system layer per stellata-3re.11. With dch.10 evolving 896 binary systems against the same `t`, the readout needs to be visible whenever orbital motion is meaningfully on-screen.\n\nTwo options:\n\n1. **Extend the gate.** Add a \"any focused star has orbitIdx != NO_ORBIT\" predicate to the existing visibility condition. Pros: minimal change, preserves the chrome-when-needed principle. Cons: another conditional that could miss cases.\n2. **Always show the clock.** Now that `t` matters across multiple domains (solar system, binary orbits, future deep-space probes), the gating may be more confusion than help. Pros: simpler mental model, future-proofs further `t`-driven layers. Cons: chrome present even in pure-stellar exploration.\n\nUser leans toward (2). Worth a design decision at dch.10 implementation time; either way, the readout shouldn't disappear during binary-focused viewing.\n\nIf (2), the change is small and lands inside dch.10 — drop the gating predicate in `time-readout.ts`. If (1), it's still a one-liner in the same file.","status":"open","priority":1,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-11T09:37:37Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:47:34Z","external_ref":"archive/dch-pipeline-foundation","dependencies":[{"issue_id":"stellata-dch.10","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-11T10:37:37Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.10","depends_on_id":"stellata-dch.44","type":"blocks","created_at":"2026-05-15T11:25:40Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.10","depends_on_id":"stellata-dch.8","type":"blocks","created_at":"2026-05-11T10:38:00Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.10","depends_on_id":"stellata-dch.9","type":"blocks","created_at":"2026-05-11T10:38:01Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.9","title":"Multiple-star matching: binary-orbit-pure.ts Kepler solver + projection math + tests","description":"Bead 6/7 of the stellata-dch decomposition (see parent for full spec).\n\n# Goal\n\nPure functions for the Kepler-solve + sky-plane projection math. Runs in vitest; validated against literature fixtures. **No runtime hookup** at this bead — runtime integration lands in dch.10.\n\n**Parallel to dch.7 and dch.8** — no dependency on the v5 format bump; can be picked up in any order.\n\n# Scope\n\n- `scripts/binary-orbit-pure.ts` (NEW):\n  - `solveKepler(M, e): number` — Kepler's equation iteration (Newton's method or Markley 1995). Mirror the existing pattern in `src/client/ephemeris.ts`; consolidate into a shared utility if natural, otherwise sibling-symmetric implementations with the same convergence test pinned per `stellata-consistency-at-the-seam`.\n  - `evaluateBinaryOffset(elements, t, isSecondary): { x, y, z }` — solve for eccentric anomaly E at time t, project (ρ, θ) on sky-plane, return xyz offset from system barycenter scaled to system distance in pc. Pure; no global state.\n- `scripts/binary-orbit-pure.test.ts` (NEW):\n  - Kepler convergence for e ∈ {0, 0.3, 0.9}: result satisfies `M = E - e·sin(E)` within 1e-9\n  - Periodicity: `|offset(t) - offset(t + P)| \u003c 1e-6` at multiple sampled t\n  - Barycenter consistency: `primary_offset + secondary_offset ≈ 0` (within 1e-6) at multiple sampled t (note: this uses the mass-ratio q and signs along the orbit)\n  - α Cen J2000 fixture: computed (ρ, θ) at J2000 matches Wiegert+1997 measurement within published uncertainties\n  - Algol's 2.87-day period: offset magnitude cycles through full range within one P (sample at 10 phases, assert max-min spans the expected scale)\n\n# Smoke\n\n- `npm test` green; fixture tests pass\n- No runtime / shader / build changes — pure-math module only","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T09:37:16Z","created_by":"Alex Marshall","updated_at":"2026-05-12T14:01:57Z","started_at":"2026-05-12T13:51:10Z","closed_at":"2026-05-12T14:01:57Z","close_reason":"Pure Kepler + Thiele-Innes math landed in src/client/binary-orbit-pure.ts; Newton solver extracted to shared src/client/kepler-solver.ts and consumed by ephemeris.ts. 675 tests pass.","dependencies":[{"issue_id":"stellata-dch.9","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-11T10:37:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.8","title":"Multiple-star matching: emit orbital elements into v5 binary","description":"Bead 5/7 of the stellata-dch decomposition (see parent for full spec).\n\n# Goal\n\nPopulate the orbital-elements table introduced in dch.7. build-binaries.py emits per-system orbital elements; build-catalog.ts writes them into catalog.bin; catalog-loader.ts exposes them as a typed-array view. **Still no runtime evolution** — that lands in dch.10.\n\n# Scope\n\n- `scripts/build-binaries.py`:\n  - For Regime 2+3 entries (rows already getting orbital data from ORB6 or spectroscopic derivation in dch.5), emit additional element columns in `multiples.tsv`: P (days), T (JD), e, a (AU), q (mass ratio m_B/(m_A+m_B)), i (rad), ω (rad), Ω (rad), distance (pc)\n  - Mark primary as `orbit_role=primary`, secondary as `orbit_role=secondary` so build-catalog.ts knows which records get `FLAG_BINARY_SECONDARY`\n- `scripts/build-catalog.ts`:\n  - Parse element columns from `multiples.tsv`\n  - Build per-system orbital-element table: 9 × float32 = 36 bytes per system entry; ~3000 systems total → ~108 KB\n  - Write orbital-elements section to catalog.bin after name table (replace the empty section header from dch.7)\n  - Per-record `orbitIdx` points to system entry; secondary records OR `FLAG_BINARY_SECONDARY` into flags byte\n- `src/client/catalog-loader.ts`:\n  - Parse orbital-elements section into Float32Array view\n  - Expose `orbitalElements: Float32Array` + per-record `orbitIdx` mapping in the Catalog interface\n- `scripts/catalog-pure.test.ts` (new cases):\n  - Element-table round-trip (write + read) preserves float32 values within epsilon\n  - Primary + secondary records share the same `orbitIdx`; only secondary has FLAG_BINARY_SECONDARY set\n\n# Smoke\n\n- `npx tsx scripts/verify-catalog.ts` dumps orbital-elements section: α Cen has P≈79.91 yr, e≈0.5179, a≈17.57 AU (per Wiegert+1997); Algol shows P≈2.87 d\n- `npm test` green\n- Renderer in dev server unchanged (loader reads new section but doesn't act on it yet)\n\n# Doc updates\n\n- `docs/build-and-data.md` § \"Orbital-elements section\" — table schema (P, T, e, a, q, i, ω, Ω, distance), entry size, count, units","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T09:37:02Z","created_by":"Alex Marshall","updated_at":"2026-05-12T13:16:59Z","started_at":"2026-05-12T13:02:25Z","closed_at":"2026-05-12T13:16:59Z","close_reason":"v5 orbital-elements section populated: 896 unique pairs (Regime 2+3), 31.5 KB. P/T/i/ω/Ω in runtime units (days/JDE/radians). Spot-checked Sirius (50.13yr, e=0.591, a=19.77 AU) and α Cen (79.76yr, e=0.519, a=23.17 AU).","dependencies":[{"issue_id":"stellata-dch.8","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-11T10:37:02Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.8","depends_on_id":"stellata-dch.7","type":"blocks","created_at":"2026-05-11T10:37:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.7","title":"Multiple-star matching: v5 binary format scaffolding (orbitIdx + orbital-elements section; no behaviour change)","description":"Bead 4/7 of the stellata-dch decomposition (see parent for full spec).\n\n# Goal\n\nBump binary catalog format to v5 to make room for the orbital-elements table (populated in dch.8) and per-record `orbitIdx` field. **No behaviour change** at this bead — `orbitIdx = NO_ORBIT` for all records; orbital-elements table empty.\n\n# Scope\n\n- `scripts/catalog-pure.ts`:\n  - `BINARY_VERSION = 5`\n  - `MAGIC = 'HYG5'`\n  - `RECORD_SIZE` 44 → 48\n  - New RECORD_LAYOUT field `orbitIdx: uint32` at byte offset 44\n  - `NO_ORBIT = 0xFFFFFFFF` sentinel\n  - New `FLAG_BINARY_SECONDARY = 0x20` (currently RESERVED bit per stellata-v2j)\n  - Update RECORD_FIELD_SIZES + binary-format invariant tests\n  - **Bonus:** one-line `MAGIC.endsWith(String(BINARY_VERSION))` coupling assertion — closes stellata-9mm.103 alongside\n- `scripts/build-catalog.ts`:\n  - Write `orbitIdx = NO_ORBIT` for every record\n  - Append empty orbital-elements section after name table with section header (size = 0)\n- `src/client/catalog-loader.ts`:\n  - Parse v5 layout (existing v4 path remains for compatibility tests; primary path is v5)\n  - Expose `orbitIdx` per record (Uint32Array; -1 sentinel for NO_ORBIT)\n  - Parse orbital-elements section header (skip empty body)\n- `scripts/verify-catalog.ts`:\n  - Dump `orbitIdx` field\n  - Print orbital-elements section header\n\n# Smoke\n\n- `npm run build:catalog` produces v5 catalog.bin\n- `npx tsx scripts/verify-catalog.ts` shows: HYG5 magic, RECORD_SIZE=48, orbitIdx=NO_ORBIT for all records, orbital-elements section header with size=0\n- `npm test` green — layout invariants pin the new offsets; MAGIC/BINARY_VERSION coupling test passes\n- Renderer in dev server unchanged (verifies catalog-loader v5 path)\n\n# Doc updates\n\n- `docs/build-and-data.md` — update RECORD_LAYOUT table to v5; add new orbital-elements section reference\n\n# Close alongside\n\n- stellata-9mm.103 (MAGIC/BINARY_VERSION coupling test) — folded into this bead","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T09:36:48Z","created_by":"Alex Marshall","updated_at":"2026-05-12T11:40:16Z","started_at":"2026-05-12T11:33:04Z","closed_at":"2026-05-12T11:40:16Z","close_reason":"v5 scaffolding landed in 24c34cb; coupling assertion folded in","dependencies":[{"issue_id":"stellata-dch.7","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-11T10:36:47Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.7","depends_on_id":"stellata-dch.6","type":"blocks","created_at":"2026-05-11T10:37:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.6","title":"Multiple-star matching: applyMultipleOverrides + MIN_RENDER_SEPARATION_PC (first user-visible fix)","description":"Bead 3/7 of the stellata-dch decomposition (see parent for full spec).\n\n# Goal\n\nTS-side ingest of `data/multiples.tsv` into the AT-HYG build pipeline. **First user-visible payoff** — α Cen A and B render distinct; Sirius B appears in the catalog.\n\n# Scope\n\n- `scripts/catalog-pure.ts`:\n  - `MIN_RENDER_SEPARATION_PC = 5e-6` — new named const (~1 AU), exported, with comment documenting calibration intent per `stellata-named-constants-and-dry`\n  - `applyMultipleOverridesPure(stars, overrides)` — pure helper:\n    - HIP rows → locate existing BinaryStar by HIP, replace xyz/absmag/ci/spect/name from override values\n    - SYN-NNN rows → construct + append new BinaryStar (HIP=0, no GCVS lookup, no constellation membership)\n  - `inferBinaries` extension: if mutual pair below `MIN_RENDER_SEPARATION_PC` AND neither component is in the overrides set, drop the fainter member with warn-log naming both HIPs. Above threshold: existing behaviour intact.\n- `scripts/build-catalog.ts`:\n  - `applyMultipleOverrides` wrapper — read `data/multiples.tsv`, call pure helper\n  - Log overrides applied + synth injections + per-regime breakdown\n  - Wire after `readStars`, before absmag sort\n- `scripts/catalog-pure.test.ts` (new cases):\n  - `applyMultipleOverridesPure` replaces position for existing HIP record\n  - `applyMultipleOverridesPure` injects synthetic SYN-NNN row into output\n  - `inferBinaries` drops fainter of below-threshold pair when neither overridden\n  - `inferBinaries` keeps both components distinct when fixture has overrides applied first\n  - Combined end-to-end fixture: α Cen-style co-located rows + override → two distinct records at 17 AU\n\n# v4 binary format unchanged at this bead. Format bump lives in dch.7.\n\n# Smoke\n\n- `npm run build:catalog` log reports overrides applied + synth injections counts\n- `npx tsx scripts/verify-catalog.ts` dumps α Cen A, α Cen B, Sirius A, Sirius B at distinct xyz with correct absmag + spect class\n- `npm test` green\n- Manual (user-driven, no dev server by Claude): focus α Cen A in browser → A and B visible distinct at 17 AU; focus Sirius A → Sirius B visible at ~20 AU; search bar returns \"Sirius B\"\n\n# Doc updates\n\n- `docs/build-and-data.md` — applyMultipleOverrides flow + MIN_RENDER_SEPARATION_PC rule + log line shape","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T09:36:35Z","created_by":"Alex Marshall","updated_at":"2026-05-12T11:30:55Z","started_at":"2026-05-12T10:19:53Z","closed_at":"2026-05-12T11:30:55Z","close_reason":"Landed in worktree-stellata-dch-binaries commit b23810a; tests + typecheck green; build-counts assertion passing; α Cen A/B distinct at 18.7 AU, Sirius A/B at 11.8 AU verified via verify-catalog.ts.","dependencies":[{"issue_id":"stellata-dch.6","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-11T10:36:34Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.6","depends_on_id":"stellata-dch.5","type":"blocks","created_at":"2026-05-11T10:37:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.5","title":"Multiple-star matching: build-binaries.py cross-match pipeline (Regimes 1+2+3 + overrides scaffold)","description":"Bead 2/7 of the stellata-dch decomposition (see parent for full spec).\n\n# Goal\n\nNew Python preprocessor that cross-matches WDS + ORB6 + Gaia DR3 against AT-HYG and emits `data/multiples.tsv` (per-component rows). Mirrors the `build-clouds.py` / `build-dust.py` pattern: Python for science compute, output is a clean TSV that TS-side `build-catalog.ts` ingests.\n\n# Scope\n\n- `scripts/build-binaries.py` (NEW):\n  - Read 4 source catalogs from dch.4\n  - Cross-match WDS pair primary → AT-HYG via HIP (fallback HD); skip pairs whose primary isn't in AT-HYG\n  - Optical-pair filter (priority order): WDS notes flag → Gaia parallax 3σ check → Gaia common PM (~5 mas/yr) → spectral/mag sanity fallback\n  - Compute J2000 component positions:\n    - Regime 1 (visually resolved): WDS ρ + θ at most recent epoch → tangent-plane projection\n    - Regime 2 (ORB6 orbit): solve Kepler at J2000 with elements (P, T, e, a, i, ω, Ω) → project\n    - Regime 3 (spectroscopic-only): a·sin i for separation magnitude + conventional PA=0\n  - Apply hand-curated `data/multiples-overrides.tsv` last with precedence\n  - Emit `data/multiples.tsv` with schema: `system_id, comp, hip, x_pc, y_pc, z_pc, absmag, ci, spect, name, source, regime`\n  - Orbital elements columns also emitted for Regime 2+3 rows (consumed by dch.8) — but at this bead, downstream still ignores them\n- `data/multiples-overrides.tsv` (NEW, schema header + comment block; initially empty)\n- `.gitignore` — add `data/multiples.tsv` (generated output)\n- `package.json` — `build:binaries` npm script; `build:catalog` invokes it first\n\n# Smoke\n\n- `python3 scripts/build-binaries.py` runs to completion, prints stats: N pairs kept, M optical rejected, K SYN-NNN injections, per-regime breakdown\n- Generated `data/multiples.tsv` contains:\n  - alf_cen A (HIP 71683) and B (HIP 71681) at distinct xyz\n  - sirius A (HIP 32349) and B (SYN-001) at distinct xyz, B at ~20 AU offset from A\n  - castor's 6 components\n  - algol Aa1 / Aa2 at ~0.06 AU (Regime 3)\n- Override scaffold (`data/multiples-overrides.tsv`) parses without error (header-only)\n- `npm run build:catalog` still works — TS ingest still ignores multiples.tsv until dch.6\n\n# Doc updates\n\n- `SCIENCE.md` § \"Multiple-star pipeline\" — describe the 3-regime model + optical-pair filter\n- `docs/build-and-data.md` § \"Build-binaries pipeline\" — schema of multiples.tsv, script invocation, scope","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T09:36:18Z","created_by":"Alex Marshall","updated_at":"2026-05-12T10:13:49Z","started_at":"2026-05-11T20:27:08Z","closed_at":"2026-05-12T10:13:49Z","close_reason":"build-binaries.py landed in worktree as commit 27636dc. Smoke targets verified: α Cen A/B with correct HIPs, Sirius A+B (Sirius B = SYN with name), Castor 8 rows incl. correctly-anchored Ba/Bb sub-pair, Algol Aa1/Aa2 via ORB6. Sub-pair anchoring via WDS components field implemented in same pass. SYN secondaries get search-friendly names from primary. Branch not yet pushed; PR opens at dch.10.","dependencies":[{"issue_id":"stellata-dch.5","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-11T10:36:18Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.5","depends_on_id":"stellata-dch.4","type":"blocks","created_at":"2026-05-11T10:37:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.4","title":"Multiple-star matching: pull and commit binary-system data sources (WDS, ORB6, Gaia DR3)","description":"Bead 1/7 of the stellata-dch decomposition (see parent for full spec). Pure data + docs commit; no code changes.\n\n# Goal\n\nLand the four frozen external catalogs that the cross-match pipeline (dch.5) will consume. Frozen-external-data convention applies: pulled manually, committed under data/, no live fetch at build.\n\n# Scope\n\n- `data/wds.tsv` — Washington Double Star Catalog, VizieR `B/wds/wds`. ~150,000 pairs with ρ, θ, magnitudes, spectral types, HD/HIP cross-IDs. ~10 MB, LFS.\n- `data/wds-notes.tsv` — supplementary `B/wds/notes` + `B/wds/refs5` for HIP/HD cross-ref resolution. ~5 MB, LFS.\n- `data/orb6.tsv` — Sixth Catalog of Orbits of Visual Binary Stars, VizieR `B/orb6`. ~3000 systems with full orbital element fits. ~1 MB.\n- `data/gaia_dr3_binaries.tsv` — filtered Gaia DR3 subset: parallax + proper motion for both components of each WDS pair, retrieved by querying every Gaia source within 1 arcsec of every WDS A/B coordinate. ~20 MB, LFS.\n\n# Doc updates\n\n- `SCIENCE.md` — new section \"Multiple-star data sources\" citing each catalog with VizieR ID, paper reference, retrieval date.\n\n# Smoke\n\n- `git lfs ls-files | grep -E \"(wds|orb6|gaia_dr3_binaries)\"` shows all four files tracked under LFS\n- `wc -l data/wds.tsv` returns roughly the expected ~150,000 line count\n- `grep -i \"alf cen\\|HIP 71683\" data/wds-notes.tsv` finds the α Cen cross-ref\n- `grep \"alpha cen\\|HIP 71683\" data/orb6.tsv` finds the α Cen orbital fit (P~80yr)\n- `npm test` + `npm run build` unchanged (no code touched)\n\n# Acceptance\n\n- Files committed under LFS with valid TSV/text structure\n- SCIENCE.md cites each source with VizieR ID + retrieval date\n- No build pipeline or renderer changes — those land in dch.5+","notes":"Implementation deviations from original bead description (decided 2026-05-11\nwith Alex):\n\n1. Filenames + format: raw fixed-width .txt from astro.gsu.edu (not VizieR\n   TSV). Matches gcvs5.txt / crossid.txt convention; trivially re-fetchable\n   via single URL per file. Final names: wds_summ.txt, wds_notes.txt,\n   wds_refs.txt, orb6_orbits.txt. wds_refs.txt added (not in original) —\n   it's the discoverer-code dictionary the parser needs to resolve\n   designations.\n\n2. Gaia DR3 deferred to dch.5. The Gaia ADQL query requires WDS\n   coordinates as a user-table upload, but extracting those needs a WDS\n   parser — which is dch.5's build-binaries.py. Cleaner partition: dch.4\n   is pure upstream mirror; dch.5 emits the upload CSV as an intermediate,\n   Alex runs the Gaia query manually, build-binaries.py consumes the\n   result.\n\n3. SCIENCE.md \"Multiple-star data sources\" entry covers WDS + ORB6 only.\n   Gaia procedure documentation lands in dch.5 alongside the upload-CSV\n   generator.\n\nUpdated smoke checks:\n- git lfs ls-files | grep -E \"(wds_|orb6_)\" shows all 4 files\n- wc -l data/wds_summ.txt ~ 100k+ entries\n- grep \"14396-6050\\|RIG  6\" data/wds_summ.txt finds α Cen\n- grep \"14396-6050\" data/orb6_orbits.txt finds α Cen's 79.9-yr orbit","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T09:36:02Z","created_by":"Alex Marshall","updated_at":"2026-05-11T20:27:04Z","started_at":"2026-05-11T09:48:45Z","closed_at":"2026-05-11T20:27:04Z","close_reason":"Raw WDS + ORB6 catalogs committed as data/{wds_summ,wds_notes,wds_refs,orb6_orbits}.txt under LFS. Alex spot-checked α Cen AB row in orb6_orbits.txt (P=79.762 yr, Ake2021 grade 2). SCIENCE.md updated. Filenames diverged from original TSV plan — fixed-width raw, matches gcvs5.txt convention. Gaia DR3 deferred to dch.5 (needs WDS parser to extract upload coords).","dependencies":[{"issue_id":"stellata-dch.4","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-11T10:36:02Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.3","title":"Multiple-star matching: enforce threshold + manual override list in build pipeline","description":"**Goal:** apply the threshold rule from sibling **stellata-dch.1** and the data-source decision from sibling **stellata-dch.2** to the build pipeline. Famous close pairs render at distinct, physically-correct coordinates.\n\n### Implementation\n- **Threshold enforcement** in `scripts/catalog-pure.ts:inferBinaries`: reject (or merge per dch.1's chosen action) groupings whose components fall below the threshold.\n- **Manual override list** under `data/` per the `KNOWN_MISSING_HIPS` pattern in `scripts/build-catalog.ts`. v1 list at minimum:\n  - Proxima Centauri AB (component coordinates from chosen dch.2 source)\n  - Sirius A + Sirius B\n  - α Cen A + α Cen B\n  - Any other famous close pairs surfaced during dch.2's audit.\n- **Optional external dataset ingest** if dch.2's recommendation goes beyond a manual list (e.g. WDS subset).\n- **Doc updates**: SCIENCE.md (data sources for multiple stars + threshold rule citation), `docs/build-and-data.md` and / or CLAUDE.md (pipeline behaviour) per `defer-doc-updates` convention. Write these as part of this commit, not earlier.\n\n### Acceptance\n- Build pipeline rejects (or merges) multiple-system groupings whose components fall below the chosen threshold from dch.1.\n- Proxima AB, Sirius AB, α Cen AB render at distinct coordinates matching published component positions.\n- Visual: discs/glows for each component are distinguishable at typical zoom; no full-overlap rendering.\n- `npm test` covers the new override-list ingest + threshold rule.\n\n### Depends on\n**stellata-dch.1** (threshold rule), informed by **stellata-dch.2** (data source).","status":"closed","priority":1,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:08:53Z","created_by":"Alex Marshall","updated_at":"2026-05-11T09:35:02Z","closed_at":"2026-05-11T09:35:02Z","close_reason":"Superseded by detailed decomposition into stellata-dch.4–.10. Decisions: MIN_RENDER_SEPARATION_PC = 5e-6 pc + drop-fainter action; programmatic WDS + ORB6 + Gaia DR3 cross-match with thin hand-curated overrides; v5 binary format with per-system orbital elements + live per-frame orbital evolution via BinaryOrbitField runtime. See parent stellata-dch description for full spec.","dependencies":[{"issue_id":"stellata-dch.3","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-10T09:08:53Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.3","depends_on_id":"stellata-dch.1","type":"blocks","created_at":"2026-05-10T09:10:50Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.3","depends_on_id":"stellata-dch.2","type":"relates-to","created_at":"2026-05-10T09:10:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.2","title":"Multiple-star matching: alt data-source investigation (WDS / CCDM / Gaia DR3)","description":"**Goal:** evaluate alternative data sources for resolved double/multiple stars with reliable component coordinates so famous close pairs can render at distinct positions. Spec context in parent **stellata-dch**.\n\n### Sources to evaluate\n- **WDS** (Washington Double Star catalog) — most comprehensive resolved-double-star catalog; per-pair separation + position-angle.\n- **Hipparcos CCDM** — already partially used via `data/hip_ccdm.tsv` for the visual-doubles flag. Coverage limited to Hipparcos-era pairs.\n- **Gaia DR3 binary catalog** — modern, but Gaia has its own bright-star saturation issues for famous close pairs.\n- **Per-system scientific literature** — for the very small set of canonical pairs (Proxima AB, Sirius AB, α Cen AB) where catalog data is patchy.\n\n### `frozen-external-data` rule applies\nAny chosen source committed under `data/`, no live fetch at build. Document retrieval URL + date.\n\n### Deliverable\nRecommendation attached to this issue:\n- Which source(s) to use, at what coverage cut.\n- Per-source: schema, fields needed (component positions in ICRS or angular sep + position angle relative to primary), retrieval steps.\n- Whether a hand-curated manual override list (à la `KNOWN_MISSING_HIPS`) is sufficient for the v1 famous-close-pair set, or a wholesale catalog ingest is justified.\n\n### Independent of dch.1\nCan run in parallel; the recommendation feeds **stellata-dch.3** (implementation).\n\n### Pointers\n- Existing CCDM ingest: `data/hip_ccdm.tsv`, `scripts/build-catalog.ts` `applyDoublesFlag`.\n- Coordinate with **stellata-9mm.34** (in-flight `applyDoublesFlag` test coverage) so we don't duplicate work on the same flag bit.\n- Catalog format / flag-bit registry: **stellata-v2j**.","status":"closed","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:08:52Z","created_by":"Alex Marshall","updated_at":"2026-05-11T09:35:01Z","closed_at":"2026-05-11T09:35:01Z","close_reason":"Superseded by detailed decomposition into stellata-dch.4–.10. Decisions: MIN_RENDER_SEPARATION_PC = 5e-6 pc + drop-fainter action; programmatic WDS + ORB6 + Gaia DR3 cross-match with thin hand-curated overrides; v5 binary format with per-system orbital elements + live per-frame orbital evolution via BinaryOrbitField runtime. See parent stellata-dch description for full spec.","dependencies":[{"issue_id":"stellata-dch.2","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-10T09:08:52Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.2","depends_on_id":"stellata-dch.3","type":"relates-to","created_at":"2026-05-10T09:10:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.1","title":"Multiple-star matching: separation-threshold + grouping rule (decision)","description":"**Goal:** decide a sensible minimum separation threshold below which a \"multiple system\" grouping should NOT be created during the cross-match in `scripts/catalog-pure.ts:inferBinaries`. Spec context in parent **stellata-dch**.\n\n### Decisions to settle\n- Threshold form: angular (arcsec at component distance), physical (pc / AU), or both?\n- Action when below threshold: merge the rows into one catalog entry, suppress one component, or render at distinct positions if a true-geometry source is available?\n- Whether the rule differs by system age / type (e.g. tight visual binaries with known interferometric components vs spectroscopic-only).\n\n### Inputs to consider\n- Current `BINARY_MAX_SEP_PC = 0.005 pc` ceiling for geometric-binary inference.\n- Behaviour for famously-close systems: Proxima AB (~0.27 pc), Sirius AB (binary, 7.5 AU separation), α Cen AB (~17 AU).\n- How chunked extinction + per-star PSF interact when two components are physically co-located.\n\n### Deliverable\nWritten decision attached to this issue (rule + action + rationale). Drives sibling **stellata-dch.3** (implementation). Independent of sibling **stellata-dch.2** but can borrow from its data-source survey if the chosen action requires resolved component coordinates.\n\n### Doc placement\nPer parent: rule lands in SCIENCE.md (data-source-level decision) and `docs/build-and-data.md` (pipeline behaviour) per `defer-doc-updates` convention — write the doc updates as part of dch.3, not now.","status":"closed","priority":1,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:08:51Z","created_by":"Alex Marshall","updated_at":"2026-05-11T09:35:00Z","closed_at":"2026-05-11T09:35:00Z","close_reason":"Superseded by detailed decomposition into stellata-dch.4–.10. Decisions: MIN_RENDER_SEPARATION_PC = 5e-6 pc + drop-fainter action; programmatic WDS + ORB6 + Gaia DR3 cross-match with thin hand-curated overrides; v5 binary format with per-system orbital elements + live per-frame orbital evolution via BinaryOrbitField runtime. See parent stellata-dch description for full spec.","dependencies":[{"issue_id":"stellata-dch.1","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-10T09:08:50Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-3re.19","title":"Orbit rings render over planet disc (depth/draw-order bug)","description":"Per-host orbit rings draw straight through planet body discs instead of being occluded by them. Reproduced viewing Mercury from the back (non-illuminated side) — the orbit ring line passes through the planet rather than disappearing behind it.\n\nLikely a draw-order or depthTest/depthWrite mismatch between orbit-rings-layer.ts and planet-body-field.ts. The disc pass should occlude the ring where the body covers it.\n\nFiles: src/client/orbit-rings-layer.ts, src/client/planet-body-field.ts, src/client/shaders/planet.{vert,frag}.glsl.","notes":"Fixed in PR #48 — added planet outer-disc occluder pass (uRenderMode == 3) at renderOrder 1.5. Awaiting manual smoke before close.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-09T09:41:02Z","created_by":"Alex Marshall","updated_at":"2026-05-10T16:12:37Z","started_at":"2026-05-10T08:24:57Z","closed_at":"2026-05-10T16:12:37Z","close_reason":"Shipped in PR #48 (stellata 1.4.5).","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.19","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-09T10:41:02Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-xec","title":"Alula Australis (ξ UMa) un-clickable — no visible neighbour, possible binary-stack occlusion","description":"# Bug\n\nAlula Australis (ξ UMa, a famous visual binary) cannot be clicked. No\nvisible companion star is rendered nearby, even with the magnitude\nslider pushed to 'all'.\n\n# Reported by Alex 2026-05-08\n\n\u003e I can't click on Alula Australis, no visible stars nearby (even on\n\u003e 'all' stars magnitude), so I'm wondering if the bug here is some\n\u003e kind of stacking where because it's a double star, somehow the\n\u003e double is covering the clickable star, something like that?\n\n# Repro\n\n1. Search 'Alula Australis' (or ξ UMa) and warp / focus.\n2. Push magnitude slider to its faintest setting ('all' stars).\n3. Try to click on the star — the click does not resolve to it.\n4. Observe: no second visible disc/glow nearby that would hint at\n   what's intercepting the click.\n\n# Hypotheses to investigate\n\nThis *might* be the same class as stellata-au3 (resolved-pair pick\nocclusion at the Double-Double), but the no-visible-neighbour\ndetail makes it potentially distinct:\n\n- **Stacking variant of au3**: a catalog companion exists at the\n  same position, has a hitbox, but is too faint to render at\n  on-screen pixel size — so it silently steals every click that\n  should land on Alula Australis itself, and the user has no\n  visual cue. If true, the fix may be the same hitbox cap\n  proposed under au3, but the *symptom* (no visible neighbour)\n  needs to be reproducible-by-design before that fix lands.\n\n- **Catalog mismatch**: the AT-HYG entry pointed to by the search\n  index might be a non-rendered companion, or the rendered entry\n  might be at a slightly different position than the catalog\n  entry the search/click resolves to.\n\n- **Binary inference**: scripts/catalog-pure.ts inferBinaries logic\n  may have collapsed/expanded the pair in a way that mismatches\n  what gets rendered vs what gets indexed for picking.\n\n# Diagnostics worth collecting\n\n- `bd memories pickStar` — surface any prior notes on the pick path.\n- Verify-catalog dump for ξ UMa (HIP 55203 + companion HIP) to\n  confirm both components are present and at expected separations.\n- Check whether one of the components has its disc-render disabled\n  while still contributing a hitbox.\n\n# Relation\n\nRelated to stellata-au3 — same overall theme (binary-star pick\nhitbox occluding the intended target). Whether this is a duplicate\nor a distinct bug depends on whether a faint companion is silently\nstealing the click. To be confirmed during investigation.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T20:58:33Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:59:08Z","started_at":"2026-05-10T09:07:48Z","closed_at":"2026-05-10T21:59:08Z","close_reason":"Shipped in PR #53 (merge commit 63da7a6, v1.6.2)","dependencies":[{"issue_id":"stellata-xec","depends_on_id":"stellata-au3","type":"related","created_at":"2026-05-08T21:58:38Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-zja","title":"PR #43 regression: focus typeahead dropdown re-opens in nav panel after X-clear inside Go modal","description":"Reported by Alex during smoke testing of PR #43.\n\nRepro:\n1. Focus a star (e.g. Sirius). Focus input shows 'Sirius'; X clear button visible.\n2. Press G → Go modal opens with the focus row relocated into it.\n3. Click the X (search-focus-clear) inside the modal.\n4. Modal closes 'as expected'.\n5. Bug: the focus typeahead's dropdown re-opens in the main navigate panel and stays open.\n6. Workaround: click into the focus input then click out, OR press ESC.\n\nDoesn't happen on main (pre-PR-#43). All other typeahead smoke tests pass.\n\nLikely cause (read-only investigation; NOT empirically reproduced):\n\nThe new src/client/typeahead.ts (Typeahead\u003cT\u003e class) dropped two guards that the prior SearchBox class had:\n\na) render() no longer early-exits on empty/whitespace query:\n   - Main's SearchBox.render: 'const q = query.trim(); if (!q) { resultsEl.hidden = true; ...; return; }'\n   - PR #43's Typeahead.render: unconditionally calls runQuery(query) → renderResultsDom() → setHover(0) → hidden = (results.length === 0).\n\nb) focus listener no longer guards on empty input.value:\n   - Main: 'input.addEventListener(\"focus\", () =\u003e { activeBox = this; if (input.value) this.render(input.value); });'\n   - PR #43: 'input.addEventListener(\"focus\", () =\u003e { this.group.active = this; this.render(input.value); });'\n\nIn the X-clear flow, setName('') is called, but because group.active === focusBox at that moment (the blur defer hasn't fired yet), 'input.value' is left at its prior value ('Sirius'). The clearBtn handler then calls 'input.focus()', which fires a focus event → render('Sirius') → results visible. The trick is that for unknown reasons on the user's setup, the modal does close (perhaps blur defer wins / pendingClose isn't cancelled), DOM relocates the widget back to the navigate panel, and the dropdown — visible from the focus-render — ends up visible in the navigate panel.\n\nTactical hypotheses for the fix (any one likely sufficient):\n- In Typeahead.clearBtn handler, also clear input.value and displayName explicitly before refocusing, so the resulting focus → render path runs render('') which produces 0 results.\n- In Typeahead.setName, when called with name='' and group.active === this, also set input.value = '' (overrides the 'preserve user typing' behaviour when the name is being cleared).\n- Re-introduce the empty-query early-exit in render() and/or the empty-value guard in the focus listener, matching main's SearchBox behaviour.\n\nFiles to edit on PR #43's branch (worktree-stellata-xbj-typeahead-unify):\n- src/client/typeahead.ts — Typeahead\u003cT\u003e class.\n\nVerification: Alex's existing kb-modal smoke note (PR #43 body, 'X-clear inside the modal does not tear it down mid-edit') is the closest existing case. Add a follow-up smoke step: 'After X-clear inside the Go modal, the focus typeahead dropdown should NOT remain open in the navigate panel after the modal closes.'","notes":"Reopened 2026-05-10. Initial fix (commit c70d735) was based on wrong root-cause assumption: it cleared input.value before refocus on the assumption that runQuery('') would produce 0 results. Verified empirically that Fuse v7's search('') returns the ENTIRE corpus, not [] — so render('') opens a dropdown with ~10 arbitrary stars. Combined with the modal-close path (G with focus visible focuses toInput, X-clear's input.focus targets focusInput, so onInputFocus on toInput never fires, 180ms close timer fires and tears the modal down) and modern Chrome's DOM-move-preserves-focus behaviour (so focusInput's deferred-blur-hide never fires after the move), the dropdown ends up persistently open in the main UI panel. Real fix: src/client/search.ts runQuery returns [] on empty trimmed query — focus/destination box should be silent until user types (constellation typeahead has its own runQuery already handling empty correctly).","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T20:32:18Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:03:43Z","started_at":"2026-05-08T20:35:25Z","closed_at":"2026-05-10T08:03:43Z","close_reason":"Real fix in commit 5a7e3d0 on worktree-stellata-xbj-typeahead-unify (PR #43). Root cause: Fuse v7's search('') returns the entire corpus, not [], so render('') was opening a dropdown with ~10 arbitrary stars on focus. Combined with the modal-close path (G-with-focus focuses toInput, X-clear's input.focus targets focusInput so onInputFocus on toInput never fires, 180ms close timer fires) and modern Chrome's DOM-move-preserves-focus, the dropdown stays open in the main panel until the user clicks elsewhere. Fix: src/client/search.ts runQuery returns [] on empty trimmed query — pushes empty-query semantics to the runQuery callback (constellation typeahead's filterConstellations already handles empty correctly with NONE+top9). Verified with quick node script that fuse.search('') returns the whole corpus, confirming the root cause. Initial commit c70d735's input.value='' still needed alongside (otherwise focus listener reads stale 'Sirius' and runQuery('Sirius') still returns matches).","dependencies":[{"issue_id":"stellata-zja","depends_on_id":"stellata-xbj","type":"related","created_at":"2026-05-08T21:32:24Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-fqw","title":"Observe-mode warp arrival shows destination-star jitter","description":"When warping between two stars while in OBSERVE mode, the destination star has significant jitter as the camera rotates to point to the same direction as before starting the warp.\n\nSame root cause shape as the earlier navigate-mode warp jitter that was fixed by engaging the focused-star pin (uPinFocusToCenter) on arrival. Suggestion from Alex: apply the same solution — pin the destination star on arrival — and consider DRY'ing it into a shared mechanism so navigate-mode and observe-mode warps share one pin-on-arrival path rather than two parallel ones.\n\nExpected: destination star is rock-solid on arrival as the camera completes its post-warp rotation.\n\nLikely files:\n- src/client/stellata.ts — warp state machine, pin guard in animate(), setFocus's post-recenter snap-to-origin\n- src/client/observe-controls.ts — look-around controller (rotation phase after warp completes)\n- src/client/shaders/star.vert.glsl — uPinFocusToCenter use site\n- docs/camera-modes.md — warp 3-phase state machine, OBSERVE camera mode\n- pin-debug-hud (debug.pin() in dev console) — read latched extremes on observe-warp arrival to confirm whether target.lengthSq() crosses below the 1e-12 engage threshold or not\n\nHistorical: navigate-mode warp arrival snap was added as part of an earlier 'warp jitter' fix (see PR #35 v1.1.4 'warp + variable-pulse bug fixes ...'); observe-mode arrival likely missed that treatment, or the snap fires but the observe-mode rotation phase moves target away from origin before the per-frame pin guard re-engages.\n\nReported by Alex during smoke testing of unrelated work.","notes":"Fix in PR #50. Recenter floating origin at phase-3 start (instead of finishWarp) so the parallax slerp runs in clean float32-local coords. Refactored swapObserveAnchor to be idempotent and share the recenter+focus half via new recenterFocusToStar helper.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T20:21:54Z","created_by":"Alex Marshall","updated_at":"2026-05-10T20:02:05Z","started_at":"2026-05-10T08:36:19Z","closed_at":"2026-05-10T20:02:05Z","close_reason":"merged in PR #50 (commit 783d396, v1.6.1)","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-rmo","title":"Constellation lines render on top of unfocused star disc (correct when focused)","description":"When a star is focused, the renderer correctly draws constellation stick-figure lines behind the star's disc — the disc occludes line segments that pass behind it. When the same star is unfocused, the lines render on top of the disc instead.\n\nExpected: lines occluded by disc regardless of focus state.\n\nRepro:\n1. Focus a constellation member star where a stick-figure line passes behind it (e.g. a star inside Orion).\n2. Confirm: lines are correctly hidden behind the disc.\n3. Unfocus the star (Esc, or focus elsewhere).\n4. Bug: same line segments now render on top of the disc.\n\nLikely files:\n- src/client/constellation-overlay.ts (SVG asterism layer)\n- src/client/disc-mask.ts (the SVG mask that tracks the focused star + companion discs)\n- docs/overlays.md (read first — disc-mask.ts probably clips the SVG behind the focused disc; non-focused stars have no equivalent treatment)\n\nLikely root cause: the disc-mask is keyed to the focused star only; without focus, every star's disc is below the SVG layer in z-order, so lines unconditionally cover any non-focused disc behind them. A general fix would be to mask SVG strokes against all visible star discs (expensive — many stars), or only against discs above some apparent-size threshold, or to render constellation lines on the canvas with depth-test instead of as SVG.\n\nReported by Alex during smoke testing of unrelated work.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T20:21:45Z","created_by":"Alex Marshall","updated_at":"2026-05-11T07:44:34Z","started_at":"2026-05-10T08:57:49Z","closed_at":"2026-05-11T07:44:34Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-au3","title":"Star pick hitbox occludes visually-distinct neighbours (Double-Double / ε¹ ε² Lyr)","description":"# Bug\n\nStar-pick hit areas are too aggressive: when two stars are visually\nresolved on screen as separate discs/glow blobs, the hitbox of one\ncan completely cover the other, making the occluded star\nun-clickable.\n\n# Repro (found during PR #42 smoke testing, 2026-05-08)\n\n1. Search \"ε¹ Lyr\" and focus the pair (the Double Double in Lyra,\n   ~3.5 arcmin separation).\n2. Try to click ε² Lyr — the *brighter* of the wide pair — directly\n   on its visible disc/glow.\n3. The click resolves to ε¹ instead. ε¹'s hitbox extends past ε²'s\n   on-screen position, so ε² cannot be picked.\n\n# Why this matters\n\nThe Double Double is a famous test case but the same pattern hits\nany tight resolved pair where two stars are unambiguously distinct\nto the eye yet sit inside each other's hit radius. The contract\nshould be: if both stars are visually resolved on screen, both must\nbe independently clickable. Selection is currently asymmetric —\none star \"wins\" silently and the user has no recourse.\n\n# Suggested fix direction\n\nThe pick path is in `pickStar` (the recent v1.1.4 PR #35 mentioned\n\"hot-path Vector3 perf\" there). Options to explore:\n\n- Cap hitbox radius below the on-screen distance to the nearest\n  neighbour, so two visually-resolved stars never share interior\n  hit area.\n- When the cursor falls inside multiple stars' hitboxes, pick the\n  one whose visible disc centre is closest in screen pixels (rather\n  than e.g. brightest or first-tested).\n- Shrink hitbox aggressively below a per-pixel threshold once\n  neighbour discs are within N px of each other.\n\nThe first / second options compose; either alone fixes ε¹ vs ε²\nLyr but the combination is the more robust behaviour.\n\n# Out of scope\n\nThis is about resolved pairs only. Unresolved pairs (single visible\ndisc representing two stars, e.g. a Bayer-tagged binary that\ndoesn't separate at typical FOV) should still pick the catalog\nrepresentative — that's the existing behaviour.","notes":"Surfaced by Alex during PR #42 (stellata-v2j) manual smoke. Not a regression of #42 — pre-existing pick-path behaviour, exposed by the Double-Double smoke step. Filed at P2 since it's an edge-case hit-test bug; per stellata-prioritization-framework user-reported bugs default to P1, so this may warrant a bump if Alex hits more cases.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T19:58:39Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:59:07Z","started_at":"2026-05-10T09:07:47Z","closed_at":"2026-05-10T21:59:07Z","close_reason":"Shipped in PR #53 (merge commit 63da7a6, v1.6.2)","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch","title":"Catalogue pipeline — source-ID-anchored cross-match across all ingest stages","description":"# Why this epic exists\n\nPosition-based cross-matching has hit a structural limit: famous bright close binaries (Sirius, α Cen, Castor, Algol, Procyon) systematically miss any Gaia-based filter because Gaia's published 5-parameter PM for these stars is corrupted by orbital wobble, so backward-propagating it to J2000 lands them tens of arcsec off their WDS positions. The \"tier just beyond the famous ones\" fails the same way. The fix isn't a tighter heuristic — it's switching the **entire catalogue ingest** (AT-HYG / GCVS / CCDM / WDS+ORB6+Gaia) to **source-identifier-anchored** cross-matching, using canonical cross-walks (HIP↔Gaia DR3, Tycho-2↔Gaia DR3) that Gaia publishes for exactly this purpose. Position-match is retained only as a tertiary fallback for catalog rows with no classical identifier at all.\n\nThe bar: memory `stellata-data-fidelity-principle` — \"best possible model based on current observational data and knowledge\". Set by Alex 2026-05-15. An astronomer asking about Stellata's catalogue pipeline at any level should hear an answer we're proud of.\n\n# Architecture — five layers\n\n**Layer 1 — Committed external reference data** (frozen, under `data/`, LFS where \u003e1 MB). Includes the existing WDS/ORB6/AT-HYG/GCVS/CCDM plus new committed files: `gaia_dr3_hip_xmatch.tsv` (HIP → Gaia source_id, ~118k rows), `gaia_dr3_tyc_xmatch.tsv` (Tycho-2 → Gaia, ~2.5M rows), `gaia_dr3_astrometry.tsv` (per-source RA/Dec/plx/PM + errors + ref_epoch), `gaia_dr3_nss_two_body.tsv` (orbital binaries Gaia detected), `hip2_van_leeuwen.tsv` (improved Hipparcos astrometry — long-baseline PM fallback for bright-binary Gaia contamination), `simbad_sample.tsv` (validation snapshot), `bailer-jones-dr3.tsv` (Bailer-Jones 2021 Bayesian distance posteriors per Gaia DR3 source_id — VizieR I/352, ~300k rows for the AT-HYG.gaia subset, LFS), `gaia_dr3_apsis.tsv` (Gaia DR3 astrophysical_parameters / Apsis: gspphot ∪ gspspec — Teff / logg / [M/H] / A0 per source_id, ~266k matched rows, LFS).\n\n**Layer 2 — Manual-run refresh scripts** (one per dataset, idempotent, NOT in `npm run build` per memory `frozen-external-data`). All share `scripts/refresh_lib.py` (TAP client + retry + batching + schema validation). Python deps pinned in `scripts/requirements-refresh.txt`. Each script supports both astroquery.gaia (ESA TAP) and CDS TAP with auto-fallback on archive 5xx.\n\n**Layer 3 — Catalogue builder** (`scripts/build-binaries.py` rewrite): seven stages with explicit per-component provenance.\n\n1. Load all reference data; build identifier-keyed indices.\n2. Resolve each WDS component to a Gaia source_id, strict priority order: (1) ORB6 published HIP → HIP cross-walk; (2) WDS Notes prose `HIP NNNNN` → HIP cross-walk; (3) AT-HYG row's `gaia` field (native); (4) PM-propagated position match against gaia_dr3_astrometry.tsv at ~2\"; (5) position match without PM. Records `resolve_via`.\n3. Attach astrometry: Gaia 5-parameter (default) → Gaia NSS-systemic (when 5p solution flagged unreliable: ruwe \u003e 1.4 or ipd_frac_multi_peak \u003e 2%) → HIP2 long-baseline PM (when known close binary AND |pmRA_gaia − pmRA_hip2| \u003e 50 mas/yr OR |pmDE| \u003e 50 mas/yr). Records `astrometry_via`.\n4. Select orbital elements per system: Gaia NSS two-body \u003e ORB6 (grade tiebreak) \u003e ORB6 spectroscopic-only \u003e none. NSS used when system in NSS catalog AND period \u003c ~3 yr AND separation \u003c ~1\". Records `orbit_via`.\n5. Optical-pair filter cascade (ID-anchored): (1) WDS Notes flag chars unchanged; (2) both-components-Gaia parallax-3σ + PM-5-mas/yr gate (engages for \u003e95% of pairs vs ~73% in dch.18 position-based); (3) NEW asymmetric Gaia gate (when only secondary has Gaia, test plx vs primary's known distance from AT-HYG/HIP2 — catches Sirius A-C directly); (4) magnitude-gap sanity backstop.\n6. Emit `multiples.tsv` with new columns: `resolve_via`, `astrometry_via`, `orbit_via`, `gaia_source_id`. Existing schema preserved otherwise.\n7. Statistics output → `build-catalog-expected.json` per memory `stellata-test-coverage-discipline` tier-2.\n\n**Layer 4 — Validation harness** — three tiers:\n- Tier A: hand-curated `scripts/known-stars.tsv` (~50-80 systems incl. single stars + binaries) + vitest assertions per memory `stellata-test-coverage-discipline` (numeric headlines pinned with `toBe`).\n- Tier B: extend `scripts/build-counts.test.ts` with population statistics (gaia_resolve_rate ≥ 0.95, optical_rejected_rate, NSS-vs-ORB6 fractions).\n- Tier C: manual-run `scripts/validate-simbad-sample.py` random-10k cross-check.\n\n**Layer 5 — Documentation** (at commit time per memory `defer-doc-updates`): SCIENCE.md pipeline sections rewritten with citations; new `docs/cross-match.md`; `docs/build-and-data.md` Layer 1+2 updates; CLAUDE.md module roster + Local Commands updates; v1.0 prose sweep across all docs (Phase 0 prerequisite).\n\n# Phasing\n\n- Phase 0 — v1.0 prose sweep (doc-only, skip-version-bump)\n- Phase 1 — Layer 2 infrastructure + Layer 1 acquisition\n- Phase 2 — build-binaries.py rewrite (stages 1-7 across multiple beads)\n- Phase 3 — build-catalog.ts integration + v6 binary format (gaia_source_id surfaced per record)\n- Phase 4 — validation harness (Tiers A/B/C)\n- Phase 5 — spectral / mass refinement (folds existing dch.15-17 work)\n- Phase 6 — docs sweep + landing\n- Phase 7 — dch.10 (BinaryOrbitField runtime, existing bead) unblocked; executes against the new pipeline\n\n# Coverage across the full catalogue\n\nMulti-star ingest was the trigger but the rewrite uplifts single-star ingest as well. Single-star benefits concentrated in: v6 binary format (every star gets gaia_source_id surfaced), GCVS cross-match (every star benefits from gaia_source_id keying), **multi-layer distance refinement** (Bailer-Jones Bayesian posteriors for every star with a Gaia DR3 source_id + LMC kinematic override for Magellanic supergiants — replaces naive 1/π with a principled stack; fixes the \"outside the disc\" outlier class and the \"stars in the void between MW and LMC\" outlier class without per-star carve-outs), known-stars validation corpus (Sol/Vega/Polaris/Altair + faint Tycho-population samples + LMC B-supergiant samples + the named B-J regression cases), SIMBAD random-10k validator (all star types), v1.0 docs sweep (whole project), Hipparcos van Leeuwen reduction (every HIP star), **Gaia DR3 Apsis astrophysical parameters** (gspphot ∪ gspspec; ~85% (Teff, logg) coverage per zsr.1 empirical probe — 2.87× uplift over status-quo spectral-class parsing; surfaces via dch.49 for downstream consumption by Tier 1 colour LUT, Phase 5 mass / radius refinement, and dust-map cross-checks). Multi-star is where the architecture pays off most visibly because position-match fails worst there, but the architecture is uniform across the ingest.\n\n# Open design questions to resolve at execution time\n\n1. Binary format gaia_source_id surfacing: bump to v6 with 8-byte field per record (~16 MB total impact). Resolved at start of Phase 3.\n2. NSS vs ORB6 tie-break: NSS for P \u003c ~3 yr and separation \u003c ~1\"; ORB6 otherwise. Resolved at start of Phase 2 Stage 4.\n3. Hipparcos PM fallback threshold: engage when system has companion within 5\" AND |Δ pmRA OR Δ pmDE| \u003e 50 mas/yr. Resolved at start of Phase 2 Stage 3.\n\n# Bead structure\n\nPhase 0–6 are decomposed into 30+ children (live count via `bd list --parent=stellata-dch`). Existing children dch.10, dch.11, dch.12, dch.13, dch.14 remain in-flight and independent. Existing dch.15, dch.16, dch.17 are superseded by Phase 3 / Phase 5 beads (closed with reasons). Existing dch.18, dch.19 are superseded by Phase 1 / Phase 2 beads (closed with reasons). The full execution plan + bead manifest + dependency graph lives in this epic's NOTES block (use `bd show stellata-dch` to view).\n\n# Connects to memories\n\n`stellata-data-fidelity-principle` (the bar), `stellata-prioritization-framework` (priority bands), `frozen-external-data` (committed inputs, no live fetch in build), `defer-doc-updates` (docs at commit time), `stellata-rename-and-stale-prose-sweep` (sweep stale prose at commit time), `subagent-bd-prime-protocol` (subagents need `bd prime` instruction), `stellata-test-coverage-discipline`, `stellata-bd-operations`, `bead-authoring-scope`, `commit-granularity`, `stellata-pr-mechanics`, `stellata-stellata-smoke-tests`.","design":"# Foundation commits preserved on archive/dch-pipeline-foundation\n\nPushed to origin 2026-05-15 from the now-superseded worktree-stellata-dch-binaries branch. Seven commits, HEAD at 5bec24e:\n\n| SHA       | Bead     | Status in new plan                                                  |\n| --------- | -------- | ------------------------------------------------------------------- |\n| b7eed29   | dch.4    | KEEP — data files (wds_summ.txt, wds_notes.txt, orb6_orbits.txt)    |\n| ea8149b   | dch.5    | THROW (logically) — initial build-binaries.py; replaced by Phase 2  |\n| 0c2e1de   | dch.6    | THROW (logically) — multiples-overrides + safety net; replaced      |\n| 7542fd6   | dch.7    | KEEP — v5 binary format scaffolding (orbitIdx + orbital section)    |\n| 4d36cc0   | dch.8    | KEEP — emits orbital elements into v5 binary                        |\n| 296d10f   | dch.8.f  | KEEP — Regime 3 conventional-orientation docs follow-up             |\n| 5bec24e   | dch.9    | KEEP — binary-orbit-pure.ts (Kepler + Thiele-Innes math, tested)    |\n\n# How fresh sessions should consume this branch\n\nThe new plan's Phase 2 beads (dch.27, dch.28, etc.) rewrite build-binaries.py from scratch. They do NOT need to cherry-pick dch.5 or dch.6 — those commits are preserved as historical lineage only.\n\nThe new plan's Phase 3 bead dch.33 (v6 binary format) evolves the v5 format from dch.7+dch.8 into v6. Fresh-session workflow: branch from main, merge or cherry-pick the dch.7+dch.8+dch.8-followup commits from archive/dch-pipeline-foundation as the v5 foundation, then add the v6 gaia_source_id field on top.\n\nThe existing in-flight bead dch.10 (BinaryOrbitField runtime) and the new dch.41 (eclipse photometry) both build on dch.9's binary-orbit-pure.ts. Same pattern: cherry-pick or merge dch.9 (5bec24e) from archive/dch-pipeline-foundation when starting those beads.\n\n# Why this branch exists rather than merging to main\n\nThe dch.5+dch.6 commits introduce build-binaries.py logic that the new plan's Phase 2 explicitly replaces. Merging them to main and then rewriting would create churn (commit then revert pattern). Keeping them on the archive branch means:\n- The KEPT commits (dch.4, dch.7, dch.8, dch.8-followup, dch.9) are safely preserved on a durable, semantically-named origin branch.\n- The THROWN commits (dch.5, dch.6) remain as historical context without polluting main.\n- Per-bead workflows can cherry-pick only the keepers cleanly: `git cherry-pick b7eed29 7542fd6 4d36cc0 296d10f 5bec24e`.\n\n# What to do with the old branch\n\nThe original worktree-stellata-dch-binaries branch on origin still points at the same SHA (5bec24e). It's now redundant with archive/dch-pipeline-foundation. Either branch can be deleted at any time; recommend keeping archive/dch-pipeline-foundation as the canonical one (durable name) and deleting worktree-stellata-dch-binaries after confirming no other references.","notes":"# Stellata catalogue pipeline rewrite — bead manifest\n\n## Context\n\nPosition-based cross-matching has hit a structural limit (dch.18: famous bright close binaries miss the Gaia gate because Gaia's published 5-parameter PM is corrupted by orbital wobble). The fix isn't a tighter heuristic — it's switching the entire catalogue ingest (AT-HYG / GCVS / CCDM / WDS+ORB6+Gaia) to **source-identifier-anchored** cross-matching per memory `stellata-data-fidelity-principle`.\n\nThis plan's output is **bd operations only**. Each new bead carries enough scope to be executed in a fresh `/clear`ed session via `bd show \u003cid\u003e`. The epic's description carries the architecture; each child's body carries focused per-task scope, files to touch, and acceptance criteria. On plan approval, the current session runs the listed `bd` commands and stops — no code work this session.\n\nScope answers from plan-mode questions (Alex 2026-05-15):\n- **Full catalogue ingest** rewrite (not just multi-star matching).\n- **Keep dch.7–9** (v5 binary format, orbital-elements section, pure Kepler math); throw dch.5–6 and refresh-gaia-binaries.py.\n- **No version horizon** — quality over speed; ship at whatever version is current when done.\n\nCritical memories that govern this work: `stellata-data-fidelity-principle` (the bar), `stellata-prioritization-framework` (priority bands), `frozen-external-data` (committed inputs, no live fetch in build), `defer-doc-updates` (docs at commit time), `stellata-rename-and-stale-prose-sweep` (sweep stale prose at commit time), `subagent-bd-prime-protocol` (subagents need `bd prime` instruction), `stellata-test-coverage-discipline`, `stellata-bd-operations`, `bead-authoring-scope`.\n\n---\n\n## Architecture (compressed — goes into epic description verbatim)\n\nFive layers. Layer 1 = committed reference data. Layer 2 = manual-run refresh scripts. Layer 3 = catalogue builder (Python). Layer 4 = validation harness. Layer 5 = docs.\n\n### Layer 1 — Committed external reference data (`data/`, LFS where \u003e1 MB)\n\n| File | Source | Purpose |\n| --- | --- | --- |\n| Already committed | WDS, ORB6, AT-HYG, GCVS, CCDM | core ingest |\n| `data/gaia_dr3_hip_xmatch.tsv` | `gaiadr3.hipparcos2_best_neighbour` | HIP → Gaia DR3 source_id |\n| `data/gaia_dr3_tyc_xmatch.tsv` | `gaiadr3.tyco2tdsc_merge_best_neighbour` | Tycho-2 → Gaia DR3 source_id (~2.5M rows, LFS) |\n| `data/gaia_dr3_astrometry.tsv` | `gaiadr3.gaia_source` queried by source_id list | per-source RA/Dec/plx/PM + errors + ref_epoch |\n| `data/gaia_dr3_nss_two_body.tsv` | `gaiadr3.nss_two_body_orbit` | orbital elements for binaries Gaia detected |\n| `data/hip2_van_leeuwen.tsv` | VizieR I/311/hip2 | improved Hipparcos astrometry; long-baseline PM is the bright-binary fallback when Gaia is contaminated |\n| `data/simbad_sample.tsv` | SIMBAD random 10k snapshot | validation corpus |\n| `data/bailer-jones-dr3.tsv` | VizieR I/352 (`gedr3dis`) | Bailer-Jones 2021 Bayesian distance posteriors per Gaia DR3 source_id (~300k rows for AT-HYG.gaia subset, LFS) |\n| `data/gaia_dr3_apsis.tsv` | `gaiadr3.astrophysical_parameters` (Apsis: gspphot ∪ gspspec) | Teff / logg / [M/H] / A0 spectroscopic stellar parameters per source_id (~266k matched rows, LFS) |\n\n### Layer 2 — Refresh scripts (manual, idempotent, NOT in `npm run build`)\n\nOne per dataset. All share `scripts/refresh_lib.py` (TAP client + retry + batching + schema validation). Python deps pinned in `scripts/requirements-refresh.txt` (astropy, astroquery, numpy). Each script supports both astroquery.gaia (ESA TAP) and CDS TAP with auto-fallback on archive 5xx.\n\n### Layer 3 — Catalogue builder (`scripts/build-binaries.py` rewrite)\n\nSeven stages with explicit per-component provenance:\n\n1. **Load all reference data** into memory; build identifier-keyed indices (`hip → gaia_source_id`, `tyc → gaia_source_id`, `gaia_source_id → astrometry_row`, etc.).\n2. **Resolve each WDS component to a Gaia source_id**, strict priority order:\n   1. ORB6 published HIP → HIP cross-walk\n   2. WDS Notes prose `HIP NNNNN` extraction → HIP cross-walk\n   3. AT-HYG row carries `gaia` field natively → use directly\n   4. PM-propagated position match against `data/gaia_dr3_astrometry.tsv` (narrow tolerance, ~2″)\n   5. Position match without PM (faint, no PM available)\n   \n   Each component records `resolve_via` strategy used.\n3. **Attach astrometric data** with three sub-tiers per component: Gaia 5-parameter (default) → Gaia NSS-systemic (when single-star solution is flagged unreliable) → HIP2 long-baseline PM (when system is a known close binary AND |pmRA_gaia − pmRA_hip2| \u003e 50 mas/yr OR |pmDE| \u003e 50 mas/yr). Records `astrometry_via`.\n4. **Select orbital elements per system**: Gaia NSS two-body \u003e ORB6 (grade tiebreak) \u003e ORB6 spectroscopic-only \u003e none. Records `orbit_via`.\n5. **Optical-pair filter cascade** (ID-anchored):\n   1. WDS Notes flag chars (unchanged)\n   2. Both-components-Gaia gate (parallax 3σ + PM 5 mas/yr) — engages for \u003e95% of pairs vs ~73% in dch.18 position-based\n   3. **Asymmetric Gaia gate** (NEW) — when only secondary has Gaia, test plx vs primary's known distance. Catches Sirius A-C optical pairs directly.\n   4. Magnitude-gap sanity backstop\n6. **Emit `data/multiples.tsv`** with provenance columns (`resolve_via`, `astrometry_via`, `orbit_via`) + `gaia_source_id` column. Existing schema preserved otherwise.\n7. **Statistics output** — N resolved via each strategy, NSS-vs-ORB6 orbit counts, optical-rejected counts per tier, position-fallback residual histogram. Stats land in `build-catalog-expected.json` per [[stellata-test-coverage-discipline]] tier-2.\n\n### Layer 4 — Validation harness\n\n- **Tier A — Known-stars corpus** (`scripts/known-stars.tsv`): ~50–80 systems with hand-verified expected values (HIP, Gaia source_id, distance, absmag, spectral class, companions, periods). Test asserts catalog.bin matches within tolerances. Pinned with `toBe(N)` per [[stellata-test-coverage-discipline]].\n- **Tier B — Population statistics** (extend `scripts/build-counts.test.ts`): gaia_resolve_rate ≥ 0.95, optical_rejected_rate, NSS-vs-ORB6 fractions. Snapshot-pinned, refreshed via `UPDATE_BUILD_COUNTS=1`.\n- **Tier C — SIMBAD sample cross-check** (`scripts/validate-simbad-sample.py`, manual-run): random 10k from snapshot, compare distance/PM/absmag against SIMBAD published. Residual histograms.\n\n### Layer 5 — Documentation (at commit time per [[defer-doc-updates]])\n\n- `SCIENCE.md` \"Multiple-star pipeline\" + \"Stellar catalog ingestion\" sections rewritten with citations for every cross-walk table.\n- `docs/cross-match.md` NEW developer-facing walk-through of stage-2 resolution.\n- `docs/build-and-data.md` updated for Layer 1 + Layer 2.\n- `CLAUDE.md` module roster + Local Commands sections updated.\n- v1.0 prose sweep (CLAUDE.md:252, :275; SCIENCE.md:77, :79, :187, :449, :636; docs/chart-mode.md:28, :30; docs/molecular-clouds.md:3, :13; docs/rendering.md:51; docs/solar-system.md:24, :61, :181) — Phase 0 prerequisite.\n\n### Open design questions (resolve at start of relevant phase, no external research needed)\n\n1. **Binary format for gaia_source_id surfacing** — bump to v6 with 8-byte field per record (~16 MB total impact). Resolved at start of Phase 3.\n2. **NSS vs ORB6 tie-break** — NSS for P \u003c ~3 yr and separation \u003c ~1″; ORB6 otherwise. Resolved at start of Phase 2 Stage 4.\n3. **Hipparcos PM fallback threshold** — engage when system has companion within 5″ AND PM discrepancy \u003e 50 mas/yr (one-axis). Resolved at start of Phase 2 Stage 3.\n\n### Phasing summary\n\n- **Phase 0** — v1.0 prose sweep (doc-only, skip-version-bump).\n- **Phase 1** — Layer 2 infrastructure + Layer 1 acquisition.\n- **Phase 2** — `build-binaries.py` rewrite (stages 1–7 across multiple beads).\n- **Phase 3** — `build-catalog.ts` integration + v6 binary format.\n- **Phase 4** — validation harness (Tiers A, B, C).\n- **Phase 5** — spectral / mass refinement (folds existing dch.15, .16, .17).\n- **Phase 6** — docs sweep + landing.\n- **Phase 7** — dch.10 (BinaryOrbitField runtime, existing bead) executes against the new pipeline.\n\nEach phase's beads can largely parallelise within phase; phases land sequentially.\n\n### Coverage across the full catalogue (not just multi-star systems)\n\nThe rewrite was triggered by multi-star matching failures (dch.18), but the architecture uplifts single-star ingest as well. Single-star benefits are concentrated in:\n\n- **Bead M (v6 binary format with `gaia_source_id`)** — every star in the 313k-star catalog, not just binaries, surfaces a Gaia source_id through to `catalog.bin`. Single-star downstream code (search index, GCVS lookup, future POI / observation features) can use it.\n- **Bead N (GCVS cross-match prefers `gaia_source_id`)** — variability matching for every star benefits; the GCVS match rate is expected to rise for stars without HIP/HD identifiers because AT-HYG carries Gaia source_id for Gaia-DR3-sourced rows (the ~98% of the catalog with Gaia distances).\n- **Distance refinement (Beads Y + AA — Bailer-Jones posteriors + LMC kinematic override)** — three-layer principled distance stack replaces naive 1/π for every Gaia-source-id-bearing star:\n  - **Bead Y (B-J Bayesian)**: `r_med_photogeo` with `r_med_geo` fallback from VizieR I/352. Direction-dependent Galactic prior + CMD prior. Self-targets the noisy-parallax regime. Applies Lindegren et al. (2021) zero-point correction.\n  - **Bead AA (LMC kinematic)**: sky-cone + PM gate identifies ~60 LMC supergiants in AT-HYG; distance overridden to Pietrzyński 2019's 49.59 kpc. Fills the gap B-J's Galactic prior can't (LMC isn't in the prior).\n  - Layers compose: B-J runs first across the catalog, LMC kinematic runs second on B-J's output, the `MAX_DIST_PC = 50_000` safety gate runs last as a bounded-scope filter.\n  - `dist_src` enum extends to `{HIP, GJ, T, N, G_R3, BJ, LMC_KIN, ...}` — future satellite refinements (SMC, Sgr, M31) follow the same pattern.\n  - Fixes two distinct outlier classes: \"stars outside the disc wireframe\" (B-J catastrophic-inversion fix) AND \"stars in the void between MW and LMC\" (LMC kinematic fix).\n- **Bead Q (known-stars validation corpus)** — required to include single non-binary canonical stars (Sol, Vega, Polaris, Procyon-as-single-not-double-test, sample faint Tycho-population stars) alongside the binaries. Tier-A vitest (Bead R) asserts on both.\n- **Beads F + S (SIMBAD random 10k + Tier-C validator)** — random sample stratified by magnitude covers all star types, validating the full catalogue not only the binary subset.\n- **Phase 0 (v1.0 prose sweep)** — touches the entire docs surface, not multi-star-scoped.\n- **Phase 1 Bead D (Hipparcos van Leeuwen)** — improved astrometry for every HIP star, not just close binaries.\n- **Phase 1 Bead BB + Phase 3 Bead CC (Gaia DR3 Apsis astrophysical parameters — dch.48 + dch.49)** — Apsis (gspphot ∪ gspspec) brings (Teff, logg) coverage from 29.3% (status-quo spectral-class parsing) to 84.8% across the catalog. Per stellata-zsr.1 empirical probe (1000-star sample, seed 20260517) — 2.87× uplift. Tier 1 colour LUT (zsr.3), Phase 5 mass / radius refinement, and azero_gspphot dust-map cross-check all benefit from Apsis-direct parameters where available; spectral-class fallback retained for the ~15% gap.\n\nThe multi-star pipeline is where the source-ID-anchored architecture pays off most visibly (because position-match fails worst there), but the architecture itself is uniform across the ingest.\n\n---\n\n## Bead operations\n\nAll operations target the existing `stellata-dch` epic. Rename epic title, rewrite description with the architecture above, close superseded beads, reparent existing in-flight refinement beads, create new children.\n\nThroughout: priorities use the project framework (`stellata-prioritization-framework`). The catalogue rewrite is **physical-reality** work → P1 for in-flight stages, P2 for refinements that aren't blocking the spine. The rewrite as a whole is a single unbroken sequence, but Phase 0 + Phase 1 can parallelise.\n\n### 1. Update epic\n\n```\nbd update stellata-dch \\\n  --title=\"Catalogue pipeline — source-ID-anchored cross-match across all ingest stages\" \\\n  --description=\"\u003cthe architecture content above, verbatim\u003e\"\n```\n\nThe new description starts with a \"Why\" section anchored on memory `stellata-data-fidelity-principle`, then includes the architecture as written. References to dch.10 (the headline-feature child that runs against the new pipeline) preserved.\n\n### 2. Close superseded beads\n\n```\nbd close stellata-dch.18 --reason=\"superseded by Phase 1 refresh scripts (stellata-dch.22–24). Position-anchored Gaia upload approach replaced with HIP/Tycho/source_id-anchored ADQL queries. The 43k-row TSV produced today is discarded; refresh-gaia-binaries.py likewise.\"\n\nbd close stellata-dch.19 --reason=\"folded into Phase 2 Stage 2 source-ID resolution chain (stellata-dch.27). AT-HYG↔Gaia dedupe now an output of the resolution chain rather than a separate pass.\"\n```\n\n### 3. Reparent existing in-flight refinement beads (already children, no parent change needed)\n\nExisting children dch.10, dch.11, dch.12, dch.13, dch.14, dch.15, dch.16, dch.17 stay under the epic. Verify with `bd dep list stellata-dch` after operations 1–2.\n\n### 4. Update dch.10's blocked-by\n\n```\nbd dep add stellata-dch.10 stellata-dch.44 --type=blocks\nbd dep remove stellata-dch.10 stellata-dch.18  # if the edge exists\n```\n\ndch.10 blocked-by switches from the (now closed) dch.18 to the final Phase 6 bead (placeholder ID dch.44, actual ID assigned by bd at creation).\n\n### 5. Create new children\n\nFor each new bead below: file with `bd create --parent=stellata-dch --priority=\u003cP\u003e --type=\u003ctype\u003e --labels=\u003clabels\u003e --description=\"\u003cbody\u003e\"`. Use HEREDOC pattern per memory `stellata-bd-operations`. After creation, add inter-bead dependencies via `bd dep add`. Verify with `bd dep list \u003cid\u003e` and `bd ready` per memory.\n\nBead bodies below — each is the FULL content to file. Each body opens with \"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\" so fresh sessions reading `bd show \u003cid\u003e` immediately know to load the epic.\n\n#### Phase 0 — v1.0 prose sweep (1 bead)\n\n**Title**: `Phase 0 — Sweep v1.0 prose from CLAUDE.md, SCIENCE.md, docs/*.md`\n**Priority**: P2\n**Type**: task\n**Labels**: `phase-0`, `docs`, `cleanup`, `skip-version-bump`\n**Body**:\n```\nPart of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nStellata is past v1.0. Remove all v1.0 / v1-era prose from project docs so future doc updates don't have to navigate around stale framing. Set by Alex 2026-05-15.\n\n# Files + lines to update\n\nConfirmed via grep on 2026-05-15:\n\n- CLAUDE.md:252 — \"Temporarily shelved (v1.0)\" section heading\n- CLAUDE.md:275 — \"WASD / flight controls (removed after v1 review)\"\n- SCIENCE.md:77 — \"Molecular cloud sources shelved for v1.0\"\n- SCIENCE.md:79 — \"ellipsoid layer, which is committed but not rendered in v1.0\"\n- SCIENCE.md:187 — \"for v1 this is documented and accepted\"\n- SCIENCE.md:449 — \"In v1 t is pinned to now with no scrubber\"\n- SCIENCE.md:636 — \"framings v1 affords\"\n- docs/chart-mode.md:28 — \"v1.0 caveat\"\n- docs/chart-mode.md:30 — \"Milky Way isobar is also disabled for v1.0\"\n- docs/molecular-clouds.md:3 — \"Status (v1.0): Shelved\"\n- docs/molecular-clouds.md:13 — \"in v1.0 the toggle is gone\"\n- docs/rendering.md:51 — \"Molecular clouds (shelved v1.0)\"\n- docs/solar-system.md:24 — \"v1 hardwires planets ⇔ Sol\"\n- docs/solar-system.md:61 — \"v1 pins it to now\"\n- docs/solar-system.md:181 — \"v1 places the\"\n\nReplacement guidance: where the prose describes a current limitation, rephrase to a tense-agnostic \"currently\" or \"in the present implementation\" form. Where it implies \"will lift the constraint in v1.1+\", rewrite to describe the constraint without the version anchor (state the limit, not the timeline). For \"shelved\" status (molecular clouds, MW isobar), keep the \"shelved\" status as a current fact.\n\n# Acceptance\n\n`grep -rn \"v1.0\\\\|until v1\\\\|(v1)\\\\|v1 review\\\\|v1 affords\\\\|v1 hardwires\\\\|v1 pins\\\\|v1 places\" CLAUDE.md SCIENCE.md docs/` returns no hits.\n\n# Smoke\n\n- `npm run typecheck` clean (no .md changes affect TS, but defensively verify).\n- `npm test` green.\n- One reviewer reads each touched doc cold to verify the prose still makes sense.\n\n# Commit\n\nSingle commit, PR labelled `skip-version-bump` (docs-only).\n```\n\n#### Phase 1 — Layer 2 infrastructure + Layer 1 acquisition (7 beads)\n\n**Bead A — refresh_lib.py shared infrastructure**\n- Title: `Phase 1 — scripts/refresh_lib.py: shared TAP client + retry + batching + schema validation`\n- Priority: P1, Type: task, Labels: `phase-1`, `refresh-scripts`, `python`\n- Body covers: TAP client supporting both astroquery.gaia and CDS TAP with auto-fallback on 5xx; retry-with-backoff; batching helper; output-schema-validation helper; idempotency-check helper (mtime + manifest). Files: `scripts/refresh_lib.py` (new), `scripts/requirements-refresh.txt` (new — pinned astropy + astroquery + numpy). Acceptance: importable from each Phase-1 refresh script; covered by a small `scripts/refresh_lib.test.py` (Python unittest, since astroquery isn't vitest-amenable). Smoke: `python3 -m unittest scripts/refresh_lib.test.py` green.\n\n**Bead B — HIP↔Gaia cross-walk**\n- Title: `Phase 1 — scripts/refresh-gaia-hip-xmatch.py + commit data/gaia_dr3_hip_xmatch.tsv`\n- Priority: P1, Type: task, Labels: `phase-1`, `refresh-scripts`, `gaia`, `hip`\n- Depends on Bead A (refresh_lib).\n- Body covers: ADQL pull of `gaiadr3.hipparcos2_best_neighbour` (~118k rows) → TSV with columns (hip, gaia_source_id, distance_arcsec, angular_distance, magnitude_difference). Idempotent. Commits the TSV via LFS only if \u003e1 MB (likely ~6 MB so LFS). Files: `scripts/refresh-gaia-hip-xmatch.py` (new), `data/gaia_dr3_hip_xmatch.tsv` (new, LFS-tracked). Acceptance: row count within ±5% of expected (pin in script as `EXPECTED_ROW_COUNT`); manual smoke `npm run refresh:gaia-hip` (new npm script alias) regenerates the TSV deterministically. Documents the ADQL query in a docblock at the top of the script.\n\n**Bead C — Tycho↔Gaia cross-walk**\n- Title: `Phase 1 — scripts/refresh-gaia-tyc-xmatch.py + commit data/gaia_dr3_tyc_xmatch.tsv`\n- Priority: P1, Type: task, Labels: `phase-1`, `refresh-scripts`, `gaia`, `tycho`\n- Depends on Bead A.\n- Body: similar to Bead B but for `gaiadr3.tyco2tdsc_merge_best_neighbour` (~2.5M rows). LFS-tracked. Chunked retrieval per refresh_lib batching helper. Files: `scripts/refresh-gaia-tyc-xmatch.py`, `data/gaia_dr3_tyc_xmatch.tsv` (LFS). Acceptance + smoke: same shape as Bead B.\n\n**Bead D — Hipparcos van Leeuwen reduction**\n- Title: `Phase 1 — scripts/refresh-hipparcos2.py + commit data/hip2_van_leeuwen.tsv`\n- Priority: P1, Type: task, Labels: `phase-1`, `refresh-scripts`, `hipparcos`\n- Depends on Bead A.\n- Body: VizieR I/311/hip2 query (~118k rows). Columns: hip, ra_icrs, de_icrs, plx, e_plx, pm_ra, pm_de, e_pm_ra, e_pm_de, ref_epoch (J1991.25 for HIP2). Source URL documented in docblock. Acceptance + smoke: row count ±5%, deterministic regen, npm alias.\n\n**Bead E — Gaia NSS two-body-orbit**\n- Title: `Phase 1 — scripts/refresh-gaia-nss.py + commit data/gaia_dr3_nss_two_body.tsv`\n- Priority: P1, Type: task, Labels: `phase-1`, `refresh-scripts`, `gaia`, `nss`\n- Depends on Bead A.\n- Body: ADQL pull of `gaiadr3.nss_two_body_orbit` (~169k rows in full; commit whole table since size is manageable). Columns: source_id, period, t_periastron, eccentricity, a, inclination, arg_periastron, ascending_node, period_error, ... (full element set with formal errors). Acceptance + smoke: row count ±5%, NSS field semantics documented in docblock referencing Gaia DR3 documentation paper.\n\n**Bead F — SIMBAD validation sample**\n- Title: `Phase 1 — scripts/refresh-simbad-sample.py + commit data/simbad_sample.tsv`\n- Priority: P2 (not blocking Phase 2), Type: task, Labels: `phase-1`, `refresh-scripts`, `simbad`, `validation`\n- Depends on Bead A.\n- Body: SIMBAD TAP random-10k query with stable seed for reproducibility. Columns: simbad_id, hip, gaia_source_id, ra, dec, plx, plx_err, pm_ra, pm_de, distance_pc, absmag, spectral_type, otype. Random sample stratified by magnitude. Acceptance: 10k rows with non-null gaia_source_id for \u003e95%; smoke: deterministic regen with seed.\n\n**Bead Y — Bailer-Jones distance posteriors**\n- Title: `Phase 1 — scripts/refresh-bailer-jones.py + commit data/bailer-jones-dr3.tsv`\n- bd ID: stellata-dch.46\n- Priority: P1, Type: task, Labels: `data-pipeline`, `catalog`, `distance`\n- Depends on Bead A (refresh_lib = dch.21).\n- Body: VizieR TAP pull of I/352 (`gedr3dis`) for rows where `source_id ∈ AT-HYG.gaia` subset. Output schema: `source_id, r_med_geo, r_lo_geo, r_hi_geo, r_med_photogeo, r_lo_photogeo, r_hi_photogeo, flag`. Batched IN-clause via refresh_lib batching helper. Output: `data/bailer-jones-dr3.tsv` (LFS, ~15-20 MB for ~300k rows). Per [[frozen-external-data]]: manual-run, NOT in `npm run build`. Acceptance: ≥90% of AT-HYG.gaia source_ids resolve to a B-J row; spot-check HIP 25733 (r_med_photogeo notably smaller than AT-HYG 14327 pc), HIP 23785 (within 5% of AT-HYG 93 pc). Smoke: deterministic regen with logged row count + coverage percentage. Blocks Bead Z (Phase 3 B-J distance override).\n\n**Bead BB — Gaia DR3 Apsis astrophysical parameters**\n- Title: `Phase 1 — scripts/refresh-gaia-apsis.py + commit data/gaia_dr3_apsis.tsv`\n- bd ID: stellata-dch.48\n- Priority: P2, Type: task, Labels: `apsis`, `gaia`, `phase-1`, `physics`, `refresh-scripts`\n- Depends on Bead A (refresh_lib = dch.21).\n- Body: ADQL pull of `gaiadr3.astrophysical_parameters` for source_ids ∈ AT-HYG.gaia subset. Output schema: `source_id, teff_gspphot, logg_gspphot, mh_gspphot, azero_gspphot, teff_gspspec, logg_gspspec, mh_gspspec`. Batched via refresh_lib (TAP-limit-aware). Output: `data/gaia_dr3_apsis.tsv` (LFS, ~15-25 MB for ~266k rows). Per [[frozen-external-data]]: manual-run, NOT in `npm run build`. Acceptance: row count ~266k ± 5% (projected from `stellata-zsr.1`'s 1000-star empirical probe — 84.8% gspphot ∪ gspspec coverage); coverage log line shows union ≥80%; deterministic regen. Smoke: `npm run refresh:gaia-apsis`; spot-check Sol's nearest neighbours via `research/star-spectral-rendition/apsis_sample.py`. **Out of scope: binary-format surfacing — owned by Bead CC (dch.49) per the Bailer-Jones precedent (Y + Z pair).** Blocks Bead CC.\n\n#### Phase 2 — build-binaries.py rewrite (6 beads)\n\n**Bead G — Stage 1 (load + index)**\n- Title: `Phase 2 — build-binaries.py Stage 1: load all reference data + build identifier indices`\n- Priority: P1, Type: task, Labels: `phase-2`, `build-binaries`\n- Depends on Phase 1 Beads B, C, D, E. Does NOT depend on F (SIMBAD).\n- Body: replace existing load logic in build-binaries.py with multi-catalog loader. Load WDS pairs, ORB6 orbits, AT-HYG (surfacing `gaia`, `tyc`, `hip` columns — current ingest only surfaces `hip`), GCVS, CCDM, HIP2 astrometry, Gaia HIP cross-walk, Gaia Tycho cross-walk, Gaia astrometry (when present), Gaia NSS (when present). Build indices: `hip → gaia_source_id`, `tyc → gaia_source_id`, `gaia_source_id → astrometry_row`, `gaia_source_id → nss_row`, `hip → at_hyg_row`, `tyc → at_hyg_row`, `gaia_source_id → at_hyg_row`. Idempotency: rebuild only if any input is newer than output. Files: `scripts/build-binaries.py` (Stage 1 section), `scripts/catalog-pure.ts` (AT-HYG row schema extension for tyc + gaia). Acceptance: log lines for each catalog loaded with row counts; indices report cardinality. Smoke: `npm run build:binaries --force`, inspect log.\n\n**Bead H — Stage 2 (component resolution chain)**\n- Title: `Phase 2 — build-binaries.py Stage 2: resolve each WDS component to a Gaia source_id`\n- Priority: P1, Type: task, Labels: `phase-2`, `build-binaries`, `cross-match`\n- Depends on Bead G.\n- Body: implement the five-tier resolution chain (priority 1: ORB6 published HIP → HIP xwalk; priority 2: WDS Notes prose HIP → HIP xwalk; priority 3: AT-HYG `gaia` field; priority 4: PM-propagated position match against `data/gaia_dr3_astrometry.tsv` if it exists; priority 5: position match without PM). Each component records `resolve_via` (one of `orb6_hip`, `wds_notes_hip`, `athyg_gaia_native`, `position_pm`, `position_nopm`, `unresolved`). Resolution depends on `data/gaia_dr3_astrometry.tsv` — bead I produces this; this bead can land first with the position-fallback path stubbed (always returning unresolved) and be retro-fitted with the actual astrometry when bead I lands. Acceptance: at end of run, log \"N components resolved: orb6_hip=X, wds_notes_hip=Y, athyg_gaia_native=Z, position_pm=A, position_nopm=B, unresolved=C\". Smoke: `bd show stellata-dch.\u003cid\u003e` for this bead's id, then build, then inspect log + first 100 rows of multiples.tsv.\n\n**Bead I — Gaia astrometry by source_id**\n- Title: `Phase 2 — scripts/refresh-gaia-astrometry.py + commit data/gaia_dr3_astrometry.tsv`\n- Priority: P1, Type: task, Labels: `phase-2`, `refresh-scripts`, `gaia`, `astrometry`\n- Depends on Bead H (must know which source_ids to query).\n- Body: read the union of source_ids that Bead H's stage-2 resolution actually resolved (write them to a side file `data/gaia_astrometry_source_id_request.tsv` during Bead H's run). ADQL pull `SELECT source_id, ra, dec, parallax, parallax_error, pmra, pmra_error, pmdec, pmdec_error, ref_epoch, ruwe FROM gaiadr3.gaia_source WHERE source_id IN (...)`. Chunked to fit query limits. Output: `data/gaia_dr3_astrometry.tsv` (LFS-tracked, expected ~10–20 MB). Acceptance: every requested source_id resolves OR the script logs misses; row count matches request input ±5%; ruwe column included for unreliability flagging in Bead J. Smoke: regen deterministic; npm alias.\n\n**Bead J — Stage 3 (astrometry attachment + NSS-systemic + HIP2 fallback)**\n- Title: `Phase 2 — build-binaries.py Stage 3: astrometry attachment with NSS-systemic + HIP2-long-baseline fallback`\n- Priority: P1, Type: task, Labels: `phase-2`, `build-binaries`, `astrometry`\n- Depends on Beads H + I.\n- Body: per-component astrometry routing in priority order: (1) Gaia NSS-systemic when source has NSS two-body orbit AND ruwe \u003e 1.4 OR ipd_frac_multi_peak \u003e 2% (flagged unreliable single-star solution); (2) HIP2 long-baseline PM when system has companion within 5″ AND |pmRA_gaia − pmRA_hip2| \u003e 50 mas/yr OR |pmDE| \u003e 50 mas/yr; (3) Gaia 5-parameter as-is. Each component records `astrometry_via` (one of `gaia_nss_systemic`, `hip2_long_baseline`, `gaia_5p`). Acceptance: for Sirius A, α Cen A/B, Castor primary — `astrometry_via=hip2_long_baseline` (verify in multiples.tsv output). For Proxima Centauri (no contamination) — `astrometry_via=gaia_5p`. Smoke: build + grep for these systems in multiples.tsv.\n\n**Bead K — Stage 4 (orbital element selection)**\n- Title: `Phase 2 — build-binaries.py Stage 4: NSS \u003e ORB6 orbital-element selection per system`\n- Priority: P1, Type: task, Labels: `phase-2`, `build-binaries`, `orbits`\n- Depends on Bead J.\n- Body: per-system orbital-element selection. Priority order: (1) Gaia NSS two-body solution (P, T, e, a, i, ω, Ω, plus formal uncertainties) when present AND period within Gaia's observability regime (P \u003c 3 yr, separation \u003c 1″ a-conversion); (2) ORB6 orbit grade-tiebreak (current Regime 2 logic, kept verbatim); (3) ORB6 spectroscopic-only (current Regime 3, kept verbatim); (4) no orbit (Regime 1 visual-only). Records `orbit_via` (one of `gaia_nss`, `orb6`, `orb6_spectroscopic`, `none`). Acceptance: log \"N orbits sourced: gaia_nss=X, orb6=Y, orb6_spectroscopic=Z, none=W\". Spot-check at least one system known to be in NSS (TBD during execution — likely a short-period spectroscopic binary near Sol).\n\n**Bead L — Stage 5 + 6 + 7 (filter cascade + multiples.tsv schema + stats)**\n- Title: `Phase 2 — build-binaries.py Stages 5–7: optical-pair filter, multiples.tsv schema, stats output`\n- Priority: P1, Type: task, Labels: `phase-2`, `build-binaries`, `optical-filter`\n- Depends on Beads H, J, K.\n- Body: Stage 5 — implement four-tier optical-pair filter cascade as described in epic. New: asymmetric-Gaia gate (when only secondary has Gaia astrometry, test plx vs primary's known distance from AT-HYG/HIP2). Catches spurious-Sirius-A-C without requiring primary in Gaia. Stage 6 — extend multiples.tsv schema with `resolve_via`, `astrometry_via`, `orbit_via`, `gaia_source_id` columns. Stage 7 — log per-strategy + per-tier statistics; write to `scripts/build-counts-expected.json`. Acceptance: zero SYN-NNN injections for Sirius A-C/D/E/F entries (verify by grepping multiples.tsv). Famous physical pairs (α Cen A-B, Sirius A-B, 61 Cyg A-B) survive the cascade (verify). Smoke: build + verify-catalog inspection.\n\n#### Phase 3 — build-catalog.ts integration + v6 binary format (7 beads)\n\n**Bead M — v6 binary format with gaia_source_id**\n- Title: `Phase 3 — Binary format v6: surface gaia_source_id per record (8 bytes)`\n- Priority: P1, Type: task, Labels: `phase-3`, `binary-format`\n- Depends on Bead G (AT-HYG row schema extension already lands there).\n- Body: bump binary format from v5 to v6 with 8-byte gaia_source_id field per record. Update header magic (HYG5 → HYG6). Record size 48 → 56 bytes. Update `scripts/catalog-pure.ts` packers/unpackers, `scripts/build-catalog.ts` emit, `scripts/verify-catalog.ts` reads, `src/client/catalog-loader.ts` decode. Update v5-format-regression tests in `scripts/catalog-pure.test.ts` to assert v6 layout. Migration: rebuild from scratch (no in-place upgrade). Acceptance: typecheck + tests green; `npx tsx scripts/verify-catalog.ts` reports format v6, dumps sample records with gaia_source_id values.\n\n**Bead N — GCVS cross-match uses gaia_source_id**\n- Title: `Phase 3 — build-catalog.ts GCVS cross-match: prefer gaia_source_id over hip/hd`\n- Priority: P1, Type: task, Labels: `phase-3`, `gcvs`, `cross-match`\n- Depends on Bead M.\n- Body: extend `applyVariability()` in `scripts/build-catalog.ts:569-587` to use gaia_source_id as primary key (when AT-HYG row carries it) and hip/hd as fallback. Update `byGaia` map alongside existing `byHip` and `byHd`. Acceptance: log \"GCVS matches via gaia_source_id: N, via hip: M, via hd: K\". Increase in match rate is expected (gaia_source_id resolves stars without HIP/HD). Smoke: verify-catalog dumps variable star count and sample.\n\n**Bead O — CCDM cross-match alignment**\n- Title: `Phase 3 — build-catalog.ts CCDM cross-match uses HIP via new cross-walks`\n- Priority: P2 (less critical than GCVS), Type: task, Labels: `phase-3`, `ccdm`, `cross-match`\n- Depends on Bead M.\n- Body: align CCDM cross-match pass in `build-catalog.ts` with the new HIP cross-walk infrastructure. Acceptance: log + sample inspection.\n\n**Bead P — Split combined WDS spectral types per component (folds dch.16)**\n- Title: `Phase 3 — parseSpectral splits composite WDS types (A1Vm+DA2 → per-component)`\n- Priority: P2, Type: task, Labels: `phase-3`, `spectral`\n- This bead supersedes dch.16. After creating bead P, close dch.16 with `--reason=\"superseded by stellata-dch.\u003cP-id\u003e\"`.\n- Depends on Bead M.\n- Body: extend `parseSpectral` in `scripts/catalog-pure.ts` to split composite types on `+`, parse each side, attach the right one per WDS component. Each multiples.tsv row gets its own parsed spectral class. Acceptance: known-stars corpus systems with composite types (Sirius A G2V + Sirius B DA2; 40 Eri B DA+M4) parse correctly per component. Tests in catalog-pure.test.ts.\n\n**Bead Z — Bailer-Jones distance override in build-catalog.ts**\n- Title: `Phase 3 — build-catalog.ts: Bailer-Jones distance override for stars with Gaia DR3 source_id`\n- bd ID: stellata-dch.47\n- Priority: P1, Type: task, Labels: `data-pipeline`, `catalog`, `distance`\n- Depends on Bead Y (dch.46 — B-J data file).\n- Body: load `data/bailer-jones-dr3.tsv`; build `Map\u003cgaia_source_id, distance_pc\u003e` keyed on source_id with `r_med_photogeo` (preferred) || `r_med_geo` (fallback). For each AT-HYG row with non-empty `gaia` AND a B-J entry: override `dist`, recompute `x0/y0/z0` from `ra/dec + new dist`, recompute `absmag` from `mag + new dist` (so disc/glow sizing in `star-physics.ts` stays consistent), set `dist_src = \"BJ\"`. Stars without a B-J entry retain AT-HYG values unchanged. Pure helpers (ICRS spherical→Cartesian, absmag formula) lift to `*-pure.ts` siblings per [[stellata-test-coverage-discipline]]. **Apply override BEFORE the existing `MAX_DIST_PC = 50_000` cutoff** — cutoff stays as the final safety gate (`scripts/build-catalog.ts:47`). **Known limitation: B-J's Galactic-density prior doesn't cover the LMC; LMC supergiants get pulled to intermediate-wrong distances by B-J. Bead AA (stellata-8l0) is the LMC-specific second layer that fixes this.** Without Bead AA, this bead introduces a transient LMC regression vs today's behaviour — the two beads ship in lockstep. No binary format change — v6 format from Bead M unaffected; this bead and Bead M can land independently within Phase 3. Acceptance: ≥90% override coverage; HIP 22365/25733/38430/46144 distances drop ≥25% (pinned with `toBe(...)` in tier-A vitest under Bead R); HIP 23785 distance within ±5% of current value; `sqrt(x0²+y0²+z0²) ≈ dist` and `mag − 5·log10(dist/10) ≈ absmag` post-override.\n\n**Bead AA — LMC kinematic distance refinement** (Phase 3 distance-refinement layer)\n- Title: `Kinematic distance refinement for LMC supergiants in AT-HYG`\n- bd ID: stellata-8l0 (reparented under stellata-dch as part of this update)\n- Priority: P1 (bumped from P2 — couples with dch.47 release window), Type: task, Labels: `build-pipeline`, `catalog`, `gaia`, `local-group`\n- Soft awareness link to Bead Z (dch.47) via `--type=related`: B-J runs first, LMC kinematic runs second on B-J's output.\n- Body clarifications (additive to the existing 8l0 body):\n  - **Ordering: runs AFTER Bead Z (dch.47 B-J override).** B-J first fixes catastrophic 1/π inversion across the bulk catalog; then this bead overrides LMC-cone stars (~60 entries) to Pietrzyński 2019's 49.59 kpc. Order matters: if B-J ran second, it would clobber the kinematic value because LMC stars have Gaia source_ids.\n  - **Why B-J alone doesn't fix this**: B-J's prior is a smooth Galactic-density model; the LMC isn't in the prior. B-J posteriors for LMC stars peak somewhere intermediate (5-20 kpc), not at 49.59 kpc. The kinematic sky-cone + PM filter is the principled fix.\n  - **`dist_src = \"LMC_KIN\"`** — joins the enum `{HIP, GJ, T, N, G_R3, BJ, LMC_KIN}`.\n  - **Apply override BEFORE the `MAX_DIST_PC = 50_000` cutoff**. After this bead lands, LMC stars (refined to 49.59 kpc) survive the cutoff; pre-refinement they were dropped or misplaced. Cutoff stays as bounded-scope final gate per the multi-layer distance-refinement story.\n  - **Extends to SMC/Sgr/M31** when those populations enter AT-HYG depth — same kinematic + sky-cone pattern; cutoff bumps with each new modelled population.\n\n**Bead AB — CCDM Gaia-optical-double filter** (Phase 3 supplement, P3-deferred)\n- Title: `Gaia-DR3-aware filter for CCDM-flagged optical doubles (Albireo, etc.)`\n- bd ID: stellata-dch.45\n- Priority: P3, Type: task, Labels: `phase-3`, `ccdm`, `optical-doubles` (add at execution time — currently unlabelled)\n- No hard blockers (P3-deferred, independent of source-ID rewrite).\n- Soft awareness link to Beads Z + AA via `--type=related`: predicate must accept all Gaia-derived dist_src values.\n- Body: extend `applyDoublesFlag` in `build-catalog.ts` to compute 3D separation between both components of a CCDM-flagged system when BOTH have **Gaia-derived distances (`dist_src ∈ {G_R3, BJ, LMC_KIN}`)**. Suppress the chart-mode binary-wings glyph when 3D separation \u003e 1.0 pc. Albireo (β¹ at 111.34 pc, β² at 122.11 pc — 10.77 pc apart) gets the glyph removed; Mizar+Alcor (0.21 pc), ε¹+ε² Lyr (0.29 pc), α Cen A+B (~0 pc) survive. Suppressed-glyph set logged at build time for review against literature.\n\n**Bead CC — Apsis astrophysical parameters surfaced in catalog binary**\n- Title: `Phase 3 — Surface Gaia DR3 Apsis (Teff/logg/[M/H]/A0) per record in catalog binary`\n- bd ID: stellata-dch.49\n- Priority: P2, Type: task, Labels: `apsis`, `gaia`, `phase-3`, `physics`, `binary-format`\n- Depends on Bead BB (dch.48 — Apsis TSV) + Bead M (dch.33 — v6 binary format).\n- Body: load `data/gaia_dr3_apsis.tsv`, build `Map\u003cgaia_source_id, ApsisRow\u003e`, attach Apsis fields to v6 records keyed off gaia_source_id. **Open design question at execution time**: inline fields (~10 MB delta on catalog.bin) vs sparse side-table keyed on source_id (~8.5 MB at 85% coverage — saves 1.5 MB at the cost of a second binary section). Likely inline given the delta isn't worth the complexity; may trigger a v6→v7 bump or fold into a single v6 bump if dch.33 hasn't shipped yet. Pure helpers (TSV parser, source_id → ApsisRow indexer) lift to `*-pure.ts` siblings per [[stellata-test-coverage-discipline]]. Routing downstream: Tier 1 colour LUT (`stellata-zsr.3`) re-keys Teff source from Ballesteros(B-V) → Apsis-direct where available; Phase 5 Bead T (mass-ratio) gains direct logg for giants/subgiants; Phase 5 Bead U (eclipse photometry) gains direct Teff for limb-darkening; future dust-map cross-check uses azero_gspphot. None of those re-routings are in this bead's scope — each is filed separately when desired. Acceptance: ≥80% of records have non-null teff_apsis OR teff_gspspec; spot-check Sirius / α Cen / Vega Apsis values match the TSV byte-for-byte; round-trip via verify-catalog passes. Smoke: `npm run build:catalog`; `npx tsx scripts/verify-catalog.ts` reports format version + dumps Apsis fields per sample record.\n\n#### Phase 4 — validation harness (3 beads)\n\n**Bead Q — Known-stars corpus**\n- Title: `Phase 4 — scripts/known-stars.tsv: 50+ hand-verified system corpus`\n- Priority: P1, Type: task, Labels: `phase-4`, `validation`, `corpus`\n- No dependencies (can run in parallel with Phases 1–3 once epic lands).\n- Body: curate 50–80 systems with hand-verified expected values. Columns: wds_id (nullable for single stars), system_name, primary_hip, primary_gaia_source_id, primary_distance_pc, primary_distance_pc_err, primary_absmag, primary_spectral, companions (semicolon-separated of comp_letter:hip:gaia_id:absmag, EMPTY for single stars), orbital_period_days (where applicable, NULL for non-orbital), notes_source (citation: SIMBAD ref, ORB6 entry, Gaia DR3 ADQL spot-check, etc.). **Coverage requirement: explicitly include single non-binary stars alongside the binaries.** Suggested mix: ~15 nearby single stars (Sol, Vega, Polaris-as-test, Altair, Sirius-A-treated-as-just-the-A-component-for-single-star-tests, faint Tycho-population samples chosen for magnitude diversity), ~25 famous visual binaries (Sirius AB, α Cen AB, 61 Cyg AB, Castor system, Algol, Procyon AB, etc.), ~15 systems where current Stellata is known to mis-render or where DR3 NSS provides orbital elements. **Distance-refinement regression cases:** include HIP 22365, 25733, 38430, 46144 as B-J distance-override regression cases (currently displaced outside the disc wireframe by 1/π catastrophic inversion); include HIP 23785 as a well-measured F-dwarf regression guard (post-B-J |Δdist|/dist \u003c 5%); include 2-3 LMC B-supergiants from the HDE 268xxx range as LMC-kinematic-refinement regression cases (Bead AA). Effort: ~25–40 hours hand verification work (the single largest manual task). Acceptance: 50+ rows, every row has a `notes_source` citation, no row matches another by accident (each is independently verified), single-star and multi-star coverage both present. Smoke: hand spot-check 10 random rows against SIMBAD live.\n\n**Bead R — Tier A vitest assertions**\n- Title: `Phase 4 — scripts/known-stars.test.ts: tier-A assertions against built catalog`\n- Priority: P1, Type: task, Labels: `phase-4`, `validation`, `tests`\n- Depends on Beads M (v6 format), Q (corpus). Also benefits from build-binaries pipeline being landed (Bead L).\n- Body: vitest test reads `scripts/known-stars.tsv`, loads `public/catalog.bin` via verify-catalog reused as library (refactor verify-catalog.ts to expose load + lookup functions per [[stellata-named-constants-and-dry]]). For each corpus row, look up the primary by HIP and gaia_source_id and assert distance within ±0.01 pc OR 1σ, absmag within ±0.05, spectral class index + subclass match. For companions: assert each companion present with matching mass-ratio side / distance / absmag. For orbital systems: assert orbit_via not 'none', period within ±5%. Pin numeric headlines with `toBe(N)` per [[stellata-test-coverage-discipline]]. Acceptance: all 50+ corpus rows pass; failing rows show clear diagnostic. Smoke: `npm test`.\n\n**Bead S — Tier C SIMBAD sample validator + Tier B population stats**\n- Title: `Phase 4 — scripts/validate-simbad-sample.py + extend build-counts.test.ts with statistical bounds`\n- Priority: P2, Type: task, Labels: `phase-4`, `validation`, `simbad`\n- Depends on Bead F (SIMBAD sample), Bead L (build-binaries pipeline).\n- Body: Tier C — Python script reads `data/simbad_sample.tsv`, looks up each entry in built catalog.bin by HIP / gaia_source_id, compares distance / PM / absmag against SIMBAD published. Outputs residual histograms + flags \u003eNσ outliers. Manual-run (npm alias `validate:simbad`). Tier B — extend `scripts/build-counts.test.ts` with pinned ratios: gaia_resolve_rate ≥ 0.95, optical_rejected_rate, nss_orbit_rate. Snapshot-pinned, refreshed via `UPDATE_BUILD_COUNTS=1`. Acceptance: validate-simbad reports median absmag residual \u003c 0.05, median plx residual \u003c 1σ; build-counts.test green.\n\n#### Phase 5 — spectral / mass refinement (folds existing dch.15, dch.17)\n\n**Bead T — Spectral-class-aware mass-ratio q (folds dch.15)**\n- Title: `Phase 5 — Spectral-class-aware mass-ratio q (WD/giant/subgiant fallbacks)`\n- Supersedes dch.15. After creating, close dch.15 with reason.\n- Priority: P2, Type: task, Labels: `phase-5`, `mass-ratio`, `spectral`\n- Depends on Beads L, P.\n- Body: replace L^(1/3.5) main-sequence q estimate in build-binaries.py with spectral-class-aware lookup tables: WD ~ 0.5 M☉ regardless of luminosity; M subgiants vs main-sequence vs giants per Cox 2000 / Allen 2000 tables. Acceptance: Sirius B q ≈ 0.33 (currently wrong at 0.07). Tested via known-stars corpus assertions.\n\n**Bead U — Eclipse photometry from orbital elements (folds dch.17)**\n- Title: `Phase 5 — Eclipse photometry for EA/EB/EW from orbital elements`\n- Supersedes dch.17. After creating, close dch.17 with reason.\n- Priority: P2, Type: feature, Labels: `phase-5`, `eclipsing-binaries`, `photometry`\n- Depends on dch.14 (pulsation suppression) + Bead L (orbital elements available).\n- Body: circle-circle occlusion math for EA/EB/EW variable types, using dch.14's pulsation suppression + Bead L's orbital elements to produce real eclipse-shaped photometry. Original dch.17 description carried forward.\n\n#### Phase 6 — documentation sweep (3 beads)\n\n**Bead V — SCIENCE.md catalogue-pipeline section rewrite**\n- Title: `Phase 6 — SCIENCE.md \"Multiple-star pipeline\" + \"Stellar catalog ingestion\" sections rewrite`\n- Priority: P1, Type: task, Labels: `phase-6`, `docs`, `science`\n- Depends on all Phase 2–5 beads landed.\n- Body: rewrite both sections top-to-bottom reflecting the source-ID-anchored architecture. Citations for every cross-walk table (Gaia DR3 schema URLs, Hipparcos van Leeuwen ADS, NSS catalog reference). Priority chains documented. NSS-vs-ORB6 decision recorded with literature support. Multi-layer distance-refinement pipeline documented: Bailer-Jones (2021) Bayesian posterior (Galactic + CMD prior), LMC kinematic + sky-cone override (Pietrzyński 2019 LMC distance), and `MAX_DIST_PC = 50_000` reframed as bounded-scope final safety gate (NOT a primary include/exclude filter — SCIENCE.md:98-99 prose updated accordingly). `dist_src` enum `{HIP, GJ, T, N, G_R3, BJ, LMC_KIN}` documented with layer-per-value semantics. Acceptance: outside reader can understand the pipeline end-to-end from these two sections.\n\n**Bead W — docs/cross-match.md authoring**\n- Title: `Phase 6 — docs/cross-match.md: developer walk-through of source-ID resolution`\n- Priority: P1, Type: task, Labels: `phase-6`, `docs`\n- Depends on Phase 2 beads.\n- Body: new file. ~10-min read. Covers stage-2 resolution chain in detail, every `resolve_via` / `astrometry_via` / `orbit_via` value's meaning, validation harness structure, how to add new known-stars corpus entries, how to refresh data when DR4 lands. Covers the multi-layer distance-refinement stack (Phase 3, `build-catalog.ts`): where B-J sits, where LMC kinematic refinement sits, the ordering constraint between them (B-J first, then LMC kinematic), what stars are uncovered (fallback to AT-HYG), the role of the `MAX_DIST_PC` cutoff as a bounded-scope gate, and how to extend the stack for future satellite populations (SMC, Sgr, M31). Acceptance: a fresh contributor can read it cold and pick up a phase-2 or phase-3 bead.\n\n**Bead X — CLAUDE.md / docs/build-and-data.md / RELEASING.md sweep**\n- Title: `Phase 6 — CLAUDE.md module roster + Local Commands + docs/build-and-data.md + RELEASING.md`\n- Priority: P1, Type: task, Labels: `phase-6`, `docs`\n- Depends on all earlier phases landed.\n- Body: CLAUDE.md module roster — new refresh scripts (incl. `scripts/refresh-bailer-jones.py`), refresh_lib, requirements-refresh.txt, known-stars.tsv, validate-simbad-sample.py listed. New data file `data/bailer-jones-dr3.tsv` listed alongside the rest of the Phase 1 refresh-script outputs. Local Commands section gets new `npm run refresh:*` (incl. `refresh:bailer-jones`) and `npm run validate:simbad` aliases. docs/build-and-data.md adds Layer 1 file catalog, Layer 2 refresh-script protocol, venv setup steps. RELEASING.md: note that catalogue refresh is a manual pre-release step (not in `npm run build`). Doc index in CLAUDE.md adds docs/cross-match.md row. Acceptance: every change touches docs that point at it; no orphan refs.\n\n### 6. Set dependencies\n\nAfter all beads exist, wire dependencies (depends-on direction per memory `stellata-bd-operations`):\n\n```\n# Phase 0 has no dependencies (independent prerequisite cleanup)\n\n# Phase 1 internal deps (all depend on Bead A refresh_lib)\nbd dep add \u003cBead-B-id\u003e \u003cBead-A-id\u003e\nbd dep add \u003cBead-C-id\u003e \u003cBead-A-id\u003e\nbd dep add \u003cBead-D-id\u003e \u003cBead-A-id\u003e\nbd dep add \u003cBead-E-id\u003e \u003cBead-A-id\u003e\nbd dep add \u003cBead-F-id\u003e \u003cBead-A-id\u003e\nbd dep add stellata-dch.46 stellata-dch.21    # Bead Y depends on Bead A (refresh_lib)\nbd dep add stellata-dch.48 stellata-dch.21    # Bead BB (Apsis) depends on Bead A (refresh_lib) — already wired at creation\n\n# Phase 2 internal deps\nbd dep add \u003cBead-G-id\u003e \u003cBead-B-id\u003e\nbd dep add \u003cBead-G-id\u003e \u003cBead-C-id\u003e\nbd dep add \u003cBead-G-id\u003e \u003cBead-D-id\u003e\nbd dep add \u003cBead-G-id\u003e \u003cBead-E-id\u003e\nbd dep add \u003cBead-H-id\u003e \u003cBead-G-id\u003e\nbd dep add \u003cBead-I-id\u003e \u003cBead-H-id\u003e     # astrometry refresh depends on resolution result\nbd dep add \u003cBead-J-id\u003e \u003cBead-H-id\u003e\nbd dep add \u003cBead-J-id\u003e \u003cBead-I-id\u003e\nbd dep add \u003cBead-K-id\u003e \u003cBead-J-id\u003e\nbd dep add \u003cBead-L-id\u003e \u003cBead-K-id\u003e\n\n# Phase 3 internal\nbd dep add \u003cBead-M-id\u003e \u003cBead-G-id\u003e\nbd dep add \u003cBead-N-id\u003e \u003cBead-M-id\u003e\nbd dep add \u003cBead-O-id\u003e \u003cBead-M-id\u003e\nbd dep add \u003cBead-P-id\u003e \u003cBead-M-id\u003e\n# Bead Z (dch.47) blocked by Bead Y (dch.46) — wired at creation\n# Bead AA (stellata-8l0) softly aware of Bead Z (dch.47)\nbd dep add stellata-8l0 stellata-dch.47 --type=related\n# Bead AB (dch.45) softly aware of Beads Z + AA (dist_src enum extensions)\nbd dep add stellata-dch.45 stellata-dch.47 --type=related\n# Bead CC (dch.49) blocked by Bead BB (dch.48 — Apsis TSV) and Bead M (dch.33 — v6 binary format)\nbd dep add stellata-dch.49 stellata-dch.48\nbd dep add stellata-dch.49 stellata-dch.33\n\n# Phase 4 internal\n# Q is independent (corpus authorship parallel to everything)\nbd dep add \u003cBead-R-id\u003e \u003cBead-M-id\u003e\nbd dep add \u003cBead-R-id\u003e \u003cBead-Q-id\u003e\nbd dep add \u003cBead-R-id\u003e \u003cBead-L-id\u003e\nbd dep add \u003cBead-S-id\u003e \u003cBead-F-id\u003e\nbd dep add \u003cBead-S-id\u003e \u003cBead-L-id\u003e\n\n# Phase 5 internal\nbd dep add \u003cBead-T-id\u003e \u003cBead-L-id\u003e\nbd dep add \u003cBead-T-id\u003e \u003cBead-P-id\u003e\nbd dep add \u003cBead-U-id\u003e stellata-dch.14\nbd dep add \u003cBead-U-id\u003e \u003cBead-L-id\u003e\n\n# Phase 6 internal\nbd dep add \u003cBead-V-id\u003e \u003cBead-L-id\u003e\nbd dep add \u003cBead-V-id\u003e \u003cBead-S-id\u003e\nbd dep add \u003cBead-W-id\u003e \u003cBead-L-id\u003e\nbd dep add \u003cBead-X-id\u003e \u003cBead-V-id\u003e\nbd dep add \u003cBead-X-id\u003e \u003cBead-W-id\u003e\n\n# dch.10 (existing) becomes blocked by final Phase 6 bead\nbd dep add stellata-dch.10 \u003cBead-X-id\u003e\n\n# Verify\nbd dep list stellata-dch\nbd ready  # confirm Phase 0 + Beads A + Q surface as ready\n```\n\n### 7. Paste plan into epic notes (so it survives outside this session)\n\nAfter all beads are created and dependencies wired, paste the full content of this plan file into the epic's `--notes` block. The architecture content is already in `--description` (operation 1); the notes block carries the bead-operations manifest + execution instructions + phasing summary + open design questions, so any future fresh session running `bd show stellata-dch` sees everything without needing access to `/Users/alexm/.claude/plans/idempotent-enchanting-meerkat.md` (which is a fragile cross-session path).\n\n```\nbd update stellata-dch --notes=\"$(cat /Users/alexm/.claude/plans/idempotent-enchanting-meerkat.md)\"\n```\n\nAfter this operation, `bd memories` + `bd show stellata-dch` is the complete on-disk record of both the architectural intent and the execution plan, independent of the planning-session artefact.\n\n### 8. Verify the bead graph\n\nAfter all operations:\n\n```\nbd show stellata-dch          # epic with new architecture\nbd dep list stellata-dch      # full child graph\nbd ready                      # what's unblocked NOW\nbd list --parent=stellata-dch --status=open  # all open children\nbd doctor --check=conventions # lint check\n```\n\nExpected at this point:\n- `bd ready` lists Phase 0 (v1.0 prose sweep), Phase 1 Bead A (refresh_lib), Phase 4 Bead Q (known-stars corpus) as unblocked. Everything else has a dependency edge.\n- dch.10 listed as blocked by Phase 6 final bead.\n- dch.18, dch.19 closed with stated reasons.\n- dch.15, dch.16, dch.17 closed with `superseded by stellata-dch.\u003cnew-id\u003e` reasons.\n- dch.11, dch.12, dch.13, dch.14 still open and independent.\n\n---\n\n## Execution on plan approval\n\nThis session, after ExitPlanMode approval, runs the bd commands above (operations 1–6) and stops. **No code work this session.** Then Alex `/clear`s and picks up beads one at a time in fresh sessions per the project's standard flow (each fresh session starts with `bd ready` → `bd show \u003cid\u003e` → claim → execute → commit → close).\n\nThe execution step is mechanical: a sequence of `bd update`, `bd close`, `bd create`, `bd dep add` calls. Memory `stellata-bd-operations` covers the multi-line HEREDOC pattern needed for each `--description` body. Memory `subagent-bd-prime-protocol` covers what to instruct any future subagent that touches bd.\n\nIf any new design question emerges during fresh-session execution that isn't covered by this plan or by the bead bodies, the answer is: read the relevant memory, then read this plan (still at `/Users/alexm/.claude/plans/idempotent-enchanting-meerkat.md`), then if still unclear ask Alex. The plan stays as a living reference until all phase-6 beads are closed.","status":"in_progress","priority":1,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:54:28Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:26:46Z","started_at":"2026-05-16T15:05:14Z","external_ref":"archive/dch-pipeline-foundation","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.42","title":"solLabel / gcLabel click listeners attached in constructor are never removed in dispose()","description":"The constructor wires aimAt-on-click handlers onto the HUD labels:\n\n  src/client/stellata.ts:757  solLabel.addEventListener('click', () =\u003e { this.aimAt(...); });\n  src/client/stellata.ts:760  gcLabel.addEventListener('click', () =\u003e { this.aimAt(...); });\n\nBoth arrow functions close over  (the Stellata instance). Stellata.dispose() (src/client/stellata.ts:2911-2940) does not removeEventListener these, so on teardown the SVG label nodes hold references back to the Stellata + camera + scene — defeating the rest of dispose's GC goals.\n\nSame audit-miss family as the GPU leaks PR #14 fixed.\n\nDirection: keep the arrow functions as named instance fields (this.onSolLabelClick = () =\u003e ...; this.onGcLabelClick = () =\u003e ...) so dispose() can removeEventListener them. Or: have HudOverlay own these click bindings and expose dispose() — preferable architecturally since hud-overlay.ts already owns the labels' visual state.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:12:28Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:45Z","started_at":"2026-05-05T22:28:10Z","closed_at":"2026-05-06T23:06:45Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.42","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:12:27Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.40","title":"Stellata.dispose() leaves observeControls' window listeners attached","description":"Stellata.dispose() (src/client/stellata.ts:2911-2940) carefully removes every canvas listener Stellata itself attached, but never calls this.observeControls.disable() — and ObserveControls owns its own pointerdown/pointerup/pointercancel/pointermove/wheel listeners (src/client/observe-controls.ts:105-127, attached on enable()).\n\nIf dispose() runs while the user is in OBSERVE mode, four window listeners and one canvas listener survive. Each closes over the ObserveControls instance, which retains the camera + canvas — so the entire teardown the rest of dispose() does is undone for the parts those listeners reach.\n\nSame family of audit miss as the GPU leaks the PR fixed (Stellata.dispose() doesn't release everything it owns). Listeners aren't GPU resources but the audit was implicitly the broader 'walk the dispose chain' the c2l description called for.\n\nDirection: have Stellata.dispose() unconditionally call this.observeControls.disable(). disable() is already idempotent (early-returns when not enabled), so no caller-side mode check is needed.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:12:15Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:44Z","started_at":"2026-05-05T22:28:09Z","closed_at":"2026-05-06T23:06:44Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.40","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:12:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.38","title":"DustField has no dispose() — Data3DTexture (~128 MiB) leaks past Stellata.dispose()","description":"The c2l audit fixed every star-pipeline / cloud / milkyway / galactic resource but missed the largest single GPU allocation in the app: the dust voxel grid.\n\nDustField (src/client/dust-loader.ts:134, texture allocated at line 154 — Data3DTexture, RedFormat/UnsignedByte, 64 chunks × 2 MiB ≈ 128 MiB) has no dispose() at all. The texture is wired into shader uniforms by Stellata.attachDust (src/client/stellata.ts:1124-1142) and shared with the Milky Way layer; nothing releases it when Stellata.dispose() runs (src/client/stellata.ts:2911-2940).\n\nThis is a hot variant of exactly the leak class PR #14 enumerated — the audit description listed coreMaskMaterial / particleMesh / clouds / galactic{Disc,Grid} but didn't crawl into the dust loader's owned texture.\n\nWhy it matters: today dispose() runs only on page navigation so the leak is academic, but the c2l ticket framed itself as 'walk the dispose chain end-to-end'. Missing the 128 MiB texture is the biggest miss to backfill before any hot-reload or scene-swap path lands.\n\nDirection: add DustField.dispose() that calls this.texture.dispose() (and clears the listener array); call it from Stellata.dispose() — most cleanly via a kept reference to the attached DustField (currently only the renderer holds it through the uniform).","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:12:01Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:43Z","started_at":"2026-05-05T22:28:10Z","closed_at":"2026-05-06T23:06:43Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.38","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:12:01Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.29","title":"Roll-snap fix (stellata-a8w) only covers animated exit; hard-switch + observe bail-out paths still misalign camera.up","description":"PR #23 added a camera.up realignment in finishObserveTransition (stellata.ts:2893) that only runs after the animated observe→navigate lerp.\n\nThe same root cause exists at two other observe-exit sites that bypass that animation:\n\n1. **stellata.ts:983-990** (startObserveExit hard-switch branch). When called with animate=false, or when focusedStar===null at exit, controls.target is snapped, controls.update() runs lookAt, and TrackballControls re-enables — all without realigning camera.up. A user who pitches in observe and exits via this path gets the same roll snap a8w fixed elsewhere.\n2. **stellata.ts:1029-1037** (setFocus while in observe). When focus changes mid-observe, the code flips cameraMode to navigate and re-enables controls without realigning camera.up. The next controls.update() will roll-snap.\n\nWhy it matters: a8w identified the root cause as 'camera.up stale after observe pitch + lookAt re-resolves roll'. The fix is one-line; applying it on only one of three exit paths leaves a regression latent for any non-animated exit.\n\nSuggested direction: extract `alignCameraUpToQuaternion()` and call from all three exit points — finishObserveTransition (already done), the hard-switch branch, and the setFocus-while-observe branch.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:16Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:41Z","started_at":"2026-05-05T22:27:39Z","closed_at":"2026-05-06T23:06:41Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.29","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:16Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.11","title":"Unfocusing while close to a star disengages uPinFocusToCenter — focal star jumps on-screen","description":"# Problem\n\nWhen the user unfocuses while sitting close to a focused star (sub-µpc orbit), the focal star visibly jumps to a different screen position even though no camera motion was intended. Diagnosed via debug.pin() HUD across two screenshots:\n\n**Pre-unfocus (focused, pin engaged):**\n- focus: 32 (Rigel), pin: YES, mode: navigate, warp/aim: false\n- target.lengthSq() \u003c 1e-12: true\n- target = (0, 0, 0)\n- camera = (1.85e-6, -2.61e-6, 3.95e-5)\n- distCam = 3.96e-5 pc, controls.minDistance = 4.19e-6 pc\n- Star is centred via the shader pin path (uPinFocusToCenter).\n\n**Post-unfocus (focus cleared, pin disengaged):**\n- focus: null, pin: NO, flips: 2, off-frames: 142\n- target.lengthSq() \u003c 1e-12: false\n- target = (51.6, 257, -37.7) pc — former focal star's absolute world position\n- camera = (51.6, 257, -37.7) (same, plus a sub-µpc residual)\n- distCam = 3.96e-5 pc (preserved)\n- Rigel renders well off-centre — visible jump on the canvas.\n\n# Root cause\n\n`uPinFocusToCenter` engages only when `controls.target.lengthSq() \u003c 1e-12` — see the per-frame pin guard in stellata.ts:872 and the GLSL bypass in star.vert.glsl. The guard was designed around the invariant that a focused star sits at local (0,0,0) after `recenterOrigin`. On unfocus, `setFocus(null)` calls `recenterOrigin(0,0,0)` to put the floating origin back at Sol so URL-serialised camera poses are in absolute space (intentional — see the comment at stellata.ts:1119). That recentre shifts target by the former focal star's full world-position, breaking the lengthSq invariant. Without the pin, the float32-cancellation drift in the projection chain (the exact problem the pin was created to fix in stellata-a7d.2 / commit 5035edf) becomes visible — the focal star appears to jump.\n\nThis is the same class of bug as stellata-a7d.2.1 (warp arrival residual) and stellata-a7d.2.8 (default-load focus offset), but triggered by the unfocus path.\n\n# Secondary observation: HUD ring disappears, arrows remain\n\nReported simultaneously: when the HUD is on and the user unfocuses from a star in front of them, the focus ring disappears (expected — no focus) but the Sol/GC arrows stay drawn. The arrows are mode-bound (navigate), not focus-bound; the ring is focus-bound. The asymmetry feels odd from a UX standpoint — either both should persist or both should retire on unfocus. Bundled here because it's the same user-visible event (unfocus while close).\n\n# Possible fixes (for design exploration; user has not yet picked)\n\nA. **Don't recentre on unfocus.** Leave worldOffset at the former focal star until the user moves significantly. Pin disengages cleanly (focus=null), but target stays at (0,0,0) so float32 cancellation doesn't drift. URL serialisation needs to encode worldOffset alongside camera (or always recompute from camera_local + worldOffset).\n\nB. **Recentre to camera position on unfocus** instead of (0,0,0). Camera ends up at local (0,0,0); former focal star is at small negative camera offset. Float32 stays clean. URL still encodes camera_world (camera_local + worldOffset) so no semantic change.\n\nC. **Animate the unfocus transition** so the float32 drift is masked by motion (smooth zoom-out). Doesn't fix the underlying invariant break, just hides it.\n\nD. **Extend the pin guard** to fire on unfocused-but-target-near-a-star cases, projecting through that star's local position. Complex, edge cases.\n\nB feels cleanest — it preserves the invariant the pin needs without changing URL semantics. Verify against floating-origin design assumptions before committing.\n\n# Where to look\n\n- src/client/stellata.ts:872 (per-frame pin guard)\n- src/client/stellata.ts:1100-1140 (setFocus null branch — recenterOrigin(0,0,0))\n- src/client/stellata.ts:1179-1208 (recenterOrigin)\n- src/client/shaders/star.vert.glsl (uPinFocusToCenter use)\n- src/client/pin-debug-hud.ts (debug.pin() — used to diagnose this; both screenshots from this HUD)\n- bd memory `stellata-a7d2-pin-mechanism` for full pin-design context\n\n# Branch\n\nWill land on the same a7d.2 worktree / PR #22 if scoped tight; otherwise a follow-up branch.","notes":"Secondary observation (HUD ring vs Sol/GC arrows asymmetry on unfocus) split out to stellata-3c8 — UX decision, not part of the numerical-precision fix.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T17:29:09Z","created_by":"Alex Marshall","updated_at":"2026-05-05T18:13:22Z","started_at":"2026-05-05T17:33:05Z","closed_at":"2026-05-05T18:13:22Z","close_reason":"Fixed: setFocus(null) no longer recenters worldOffset to Sol — origin stays at the former focal object so the projection chain remains float32-clean across unfocus (no visible jump). URL serialisation generalised: new optional worldOffset field (Float32 vec3, FIELDS_V2 bit 20, appended for graceful fwd-compat) carries the absolute anchor when no focus is active, so close-orbit unfocus poses round-trip exactly. Anchor concept is type-agnostic — extends naturally to clouds/planets/probes/etc. without coupling to catalog ref space. Verified in browser: pre/post-unfocus screen position is invariant; URL share-and-reopen lands at the same close-orbit pose. Memory stellata-a7d2-pin-mechanism updated. Secondary observation (HUD ring vs Sol/GC arrow asymmetry) split to stellata-3c8.","dependencies":[{"issue_id":"stellata-a7d.2.11","depends_on_id":"stellata-a7d.2","type":"parent-child","created_at":"2026-05-05T18:29:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-08f","title":"build-catalog.ts crashes on fresh checkout because public/ dir doesn't exist","description":"On the first GitHub Actions run of deploy.yml (PR #25 merge → main bumped 1.0.7→1.0.8), npm run build:catalog failed with ENOENT on public/catalog.bin. Cause: public/ is gitignored entirely and build-catalog.ts (lines 806/813/833) writes there without ensuring the directory exists. Local builds only work because public/ already exists from prior runs. Other build scripts (sync-dust.ts, build-clouds.py) already mkdir correctly. Fix: add fs.mkdir(dirname(OUT_BIN), { recursive: true }) before the first writeFile in scripts/build-catalog.ts. This unblocks deploy.yml and also makes the project buildable from a fresh clone.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T20:10:50Z","created_by":"Alex Marshall","updated_at":"2026-05-03T20:27:27Z","started_at":"2026-05-03T20:10:59Z","closed_at":"2026-05-03T20:27:27Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-7gs","title":"Rigil Kentaurus / Toliman render as a single star (and warp Rigil→Toliman is a no-op)","description":"While testing stellata-dx7 (61 Cyg disc-edge fix) on 2026-05-03, observed that α Cen A (Rigil Kentaurus) and α Cen B (Toliman) appear as a single star in render rather than the expected close pair.\n\nTwo distinct symptoms (may share a root cause):\n\n1. Visually only one star is present at the α Cen system, when both A and B should be visible as a tight resolved pair.\n\n2. With Rigil Kentaurus focused, attempting to warp to Toliman is a no-op — nothing happens. This raises the question of whether Toliman is even being rendered (i.e. did it survive ingest at all, or is it dropped during catalog build / cross-match / binary inference).\n\n# Possible interactions\n\n- Could be at least partially confounded with stellata-a7d (physical sizes for binary components). With per-component physical sizing not yet correct, A's disc may simply be visually swallowing B even when both are rendered. But the 'warp does nothing' symptom suggests B is missing from the search/render pipeline entirely, not just visually occluded.\n\n# Next debugging steps\n\n- Verify both HIPs (HIP 71683 = α Cen A, HIP 71681 = α Cen B) are in the binary catalog written by scripts/build-catalog.ts. Run scripts/verify-catalog.ts and grep for both.\n- Check that Toliman is in the search index (public/search-index.json) — if not, the typeahead can't find it and warp won't dispatch.\n- If Toliman is in the catalog but warp dispatch is silently failing, instrument the warp / search→focus path.\n- Coordinate with stellata-a7d — sizing fix may make the visual one-star-vs-two question moot, but won't fix the warp issue if Toliman is genuinely missing.\n\n# Relates\n\n- stellata-a7d (binary component physical sizes)\n- stellata-dx7 (the bug fix that surfaced this — disc edge blending; closed)","notes":"# Investigation 2026-05-04 — root cause is identical catalog positions\n\nBoth HIPs are present and intact in the build pipeline:\n\n- AT-HYG `data/athyg_33_classic_ids.csv` rows 1451151 (Toliman, HIP 71681, K1V) and 1451193 (Rigil Kentaurus, HIP 71683, G2V) — both have full coords, dist=1.3248 pc.\n- Both rows have IDENTICAL x0/y0/z0 = (-0.495, -0.414, -1.157). AT-HYG stores α Cen A and B at the same system barycentric position; real physical separation is ~17.6 AU ≈ 8.5e-5 pc, below catalog precision.\n- Both make it through the build into `public/search-index.json` as separate catalog indices: i=289911 (Rigil Kentaurus) and i=305856 (Toliman).\n\nSo Toliman is NOT missing — both are rendered, both are searchable.\n\n# Why one star is visible\n\nWith identical world positions, both stars project to the same pixel cluster every frame. The brighter Rigil A (V=-0.01) opaque disc draws over the dimmer Toliman B (V=1.35) opaque disc. User sees one star.\n\n# Why the warp is a no-op\n\n`startWarp` at src/client/stellata.ts:1727 has `if (distPc \u003c 1e-6) return;`. With A and B at identical positions, distPc = 0 → early return → no warp dispatched, no error, no UI feedback. From the user's POV: nothing happens.\n\n# Fix paths (scope-graded)\n\n**Smaller, scoped (independent of a7d.2):**\n1. Make warp-between-co-located degrade gracefully: still call `setFocus(destIdx)` so the focused star changes even when camera doesn't move. User sees the search row, focus ring, scale bar, etc. switch over to the \"other\" star.\n\n**Larger (proper physical-reality fix, would need its own issue):**\n2. Synthesise per-component offsets at build time for known close binaries (α Cen A/B and ~hundreds of others). Requires a binary-separation data source — Washington Double Star Catalog (WDS) is the canonical catalogue. Build pipeline gains a `data/wds.txt` ingest, plus per-pair logic to nudge component positions perpendicular to line-of-sight by their physical separation. Both stars become visually + warpably distinct.\n\nPath (1) is a 5-line fix and unblocks the immediate UX. Path (2) is a follow-up — possibly under stellata-a7d (which owns binary-component physical sizing).\n\n# Confounds with a7d.2\n\nThe visual-merge fix likely lands as part of a7d.2's binary-component physical sizing work, OR through path (2) above. Either way: a7d.2 is in_progress (different session, PR #22), so the rendering side stays out of bounds for this session.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T20:04:45Z","created_by":"Alex Marshall","updated_at":"2026-05-07T09:13:49Z","started_at":"2026-05-07T08:49:43Z","closed_at":"2026-05-07T09:13:49Z","close_reason":"Landed in PR #35 (v1.1.4): warp degrades gracefully for co-located stars (setFocus on coincident A/B); orbit floor + parking distance use peak-amplitude effective radius; star vertex shader skips the 48-tap dust raymarch for visibility-prefilter rejects with a 1.5 mag DUST_AV_HEADROOM. Smoke tests pass per Alex 2026-05-07.","dependencies":[{"issue_id":"stellata-7gs","depends_on_id":"stellata-a7d","type":"related","created_at":"2026-05-03T21:05:04Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-7gs","depends_on_id":"stellata-dx7","type":"related","created_at":"2026-05-03T21:05:05Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dy2","title":"Observe-mode HUD direction-finder arrows disappear when target is behind camera","description":"In observe mode with the HUD enabled, the direction-finding arrows for off-axis targets (POIs, Sol, galactic centre, etc.) disappear when the target appears to be behind the camera. This breaks the implicit contract that there should always be a way to navigate back to a tracked target — once the user yaws/pitches the camera so the target falls behind them, the arrow vanishes and they're stranded with no visual cue to point them back.\n\nNavigate mode handles this correctly (off-screen targets keep their direction indicator); observe mode should match.\n\n# Expected\n\nArrows for tracked targets remain visible at the screen edge regardless of where the target is in the camera's hemisphere — front, side, or fully behind. The arrow should point toward the projected great-circle direction the user needs to rotate to bring the target back into view.\n\n# Investigation entry points\n\n- src/client/poi-overlay.ts — observe-mode pinned-star labels + arrows + rings. Probably the place that gates arrow visibility on a positive z (in front of camera) check that should be removed or replaced with a behind-camera projection.\n- src/client/hud-overlay.ts — Sol / GC HUD arrows. Likely the same gating pattern.\n- src/client/arrow-path.ts — shared arrow geometry helper. May or may not be the source of the visibility logic.\n- src/client/observe-controls.ts — look-around controller (for context on what 'behind camera' means in observe-mode coords).\n\n# Likely fix shape\n\nThe standard fix is: when a target's view-space z is positive (behind camera in OpenGL convention), don't try to perspective-project it (the projection sign-flips and the on-screen position lies). Instead, project the direction vector onto the camera's near-plane hemisphere and clamp to the screen edge in the direction the user needs to turn. Navigate mode's existing distance-vector / focus-ring overlays handle this — see distance-vector-overlay.ts for prior art on near-plane clipping (referenced in docs/overlays.md).\n\n# Acceptance\n\n- POI arrows in observe mode remain visible at the screen edge when the POI is behind the camera, pointing toward the rotation needed to recover the target.\n- Sol / GC HUD arrows behave the same.\n- Forward-of-camera behaviour unchanged (no regression on visible-target arrow placement).\n- Mobile + desktop both behave the same way.","notes":"# Implementation 2026-05-04 — PR #27 (worktree-fix-dy2-behind-camera-arrows)\n\nRoot cause: `projectToScreen` in both hud-overlay.ts and poi-overlay.ts returns null when target view-space z \u003e= -near (target behind camera). Both direction derivation paths (aux-step and direct target projection) depended on it. When both failed, the existing code hid the arrow — the bug.\n\nFix: added `viewSpaceScreenDir` to src/client/arrow-path.ts. Transforms the world-space direction into camera-local space (rotation only, no perspective divide) and uses (x, -y) as screen direction. Returns null only when direction is exactly along camera axis. Wired as third-tier fallback after aux-step and target-projection in both overlays.\n\nForward-of-camera behaviour unchanged because the new fallback only fires when both prior paths return null.\n\nFiles:\n- src/client/arrow-path.ts (+ helper, + import THREE type)\n- src/client/arrow-path.test.ts (NEW, 8 vitest cases)\n- src/client/hud-overlay.ts (+ third fallback in updateOne)\n- src/client/poi-overlay.ts (+ third fallback in onFrame loop)\n- docs/galactic-overlay.md (third path documented)\n- package.json (1.0.9 → 1.0.10)\n\nMechanical gates: typecheck clean, 254/254 vitest, build clean, all CI green on PR #27.\n\n# Browser smoke pending\n\nMath is unit-tested but I (Claude) can't drive a browser interactively. Acceptance items requiring user verification:\n- Pin a POI in observe, yaw past 90° — arrow stays at screen edge pointing to required rotation.\n- Sol arrow when focused on non-Sol star and yawed away.\n- GC arrow when galactic centre falls behind camera.\n- Confirm no regression on forward-of-camera arrow placement.\n\nCloses on PR merge.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T19:41:15Z","created_by":"Alex Marshall","updated_at":"2026-05-04T09:00:18Z","started_at":"2026-05-04T08:50:11Z","closed_at":"2026-05-04T09:00:18Z","close_reason":"Merged in PR #27","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.7","title":"Investigate why Barnard's Star and Sol render similar size at unfocused min distance","description":"User report: at GLOBAL_MIN_DIST_PC (5e-3 pc) viewing Sol vs Barnard's Star side-by-side in NAVIGATE mode (not chart mode), the two stars appear similar in apparent size. Expected ratio is ~5.4× by physical radius (Sol 1.035 R☉ vs Barnard 0.190 R☉ ≈ 1.9 R♃).\n\n# Investigation outcome (2026-05-03)\n\nCatalog data is **correct**. Confirmed via direct binary inspection:\n\n| Star | R (R☉) | absmag | spect/lum |\n|---|---|---|---|\n| Sol | 1.035 | 4.85 | G/Ib |\n| Barnard | 0.190 | 13.23 | M/Iab |\n| Proxima | 0.085 | 15.44 | M/Ib |\n| Sirius A | 1.816 | 1.45 | A/? |\n| Vega | 2.687 | 0.60 | A/Ib |\n| Rigel | 74.67 | -6.93 | B/VII |\n| Betelgeuse | 697.5 | -5.47 | M/VII |\n\nThe radius ratio is faithfully encoded. The discrepancy is in the rendering pipeline.\n\n# Why both stars look the same at d = 5e-3 pc\n\nAt ~1000 AU separation, both stars are sub-pixel in physical disc size (even Rigel-class giants are sub-arcsec at that range). The shader does `pxSize = max(appSize, physSize)`, so when physSize is sub-pixel the rendered size is determined entirely by `appSize`.\n\n`appSize` in navigate mode (NOT chart mode) uses a physically-grounded Gaussian-PSF model: perceived radius ∝ √(brightness above threshold). The formula:\n\n```glsl\nbrightness = clamp((uMaxAppMag - appMag) / sizeSpan, 0, 1);\nappSize = mix(uSizeMin, uSizeMax, sqrt(brightness));\n```\n\nThis √Δm shape models how a true point source appears as the eye integrates intensity above its detection threshold. The shape is correct.\n\nThe problem is the **`clamp(0, 1)`**. At d = 5e-3 pc:\n- Sol appMag ≈ -16.7 (DM = -21.5 + absmag 4.85)\n- Barnard appMag ≈ -8.3 (-21.5 + 13.23)\n\nBoth are far brighter than `uMaxAppMag` (≈ 6.5) → `brightness` clamps to 1 for both → both get `uSizeMax`. The ~8-magnitude (≈1500×) brightness difference is invisible because of the saturation, AND the physical radius signal is sub-pixel and discarded by the max().\n\n# Physical fidelity question\n\nThe PSF √brightness shape is correct in principle, but the hard saturation breaks the physics at the bright end — a real point source at appMag -16 doesn't 'cap out' at the same perceived size as one at appMag -2. The bloom/diffraction footprint keeps growing. The clamp is probably defensive UX (don't let one star fill the screen) but it loses the differentiation the user expects.\n\nUser intent: keep the foundation physical, use the existing star-exaggeration slider (K knob) to amplify differences for visualization purposes — not bake exaggeration into the formula itself.\n\n# Proposed direction\n\n1. **Drop or soften the brightness saturation** in the navigate-mode appSize formula. The √brightness PSF shape stays; the cap becomes a soft asymptote (`appSize = uSizeMax * (1 + log(1 + extraBrightness))` or similar) instead of a hard clamp. Bright stars genuinely differ in size again.\n\n2. **Wire the exaggeration slider** so users who want stronger discrimination can amplify the spread without distorting the underlying physics. The slider modulates a power on the brightness term: `appSize = mix(uSizeMin, uSizeMax, brightness^(0.5 / K))` — K=1 is the physical case, K\u003e1 exaggerates.\n\n3. **Verify** by checking pairs across magnitude regimes:\n   - Sol vs Barnard at 5e-3 pc (should be ~5×, currently identical)\n   - Sirius A vs Sol from 10 pc (should be modest difference)\n   - Rigel vs Vega from 100 pc (should be order-of-magnitude bigger)\n\n# Out of scope (filed separately)\n\nSpectral/temperature-aware perceived brightness — see stellata-a7d.2.10. V-band absmag is approximately photopic-weighted already, but cool/hot stars have small but real corrections.\n\n# Files\n\n- src/client/shaders/star.vert.glsl — appSize formula (~line 299)\n- src/client/stellata.ts — uSizeMax, uSizeSpan, K knob plumbing\n- docs/rendering.md — physical-fidelity claims would need updating\n\n# Acceptance\n\n- Sol vs Barnard at unfocused min distance show visibly different sizes, ratio in the right direction (Sol bigger).\n- Existing chart-mode behaviour unchanged (chart-mode uses a separate code path).\n- Star-exaggeration slider (K) modulates the spread monotonically and predictably.\n- docs/rendering.md updated to describe the soft-saturation choice and reference the FAQ (stellata-doc-rendering).","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T19:07:08Z","created_by":"Alex Marshall","updated_at":"2026-05-04T08:33:14Z","started_at":"2026-05-03T20:36:38Z","closed_at":"2026-05-04T08:27:59Z","close_reason":"Soft-knee saturation implemented with uSizeKnee = 16 fixed (commit ee23ade / 9e9f5cf). Calibrated against Sol/Sirius/Barnard at 5e-3 pc and against the night-sky baseline; Alex confirmed the value feels right. Knee remains tunable in the debug panel for future re-calibration.","dependencies":[{"issue_id":"stellata-a7d.2.7","depends_on_id":"stellata-a7d.2","type":"parent-child","created_at":"2026-05-03T20:07:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.4","title":"Unify auto-park distances to minDistForStar (focusStar default + warp source)","description":"User feedback during a7d.2 validation: \"There seem to be lots of different distances that we use from stars, I would like to unify all of them so that there is a consistent experience for the user.\" Audit found the system is mostly already unified — most auto-park scenarios use minDistForStar(idx) (10% disc fill) — but several outliers remain.\n\n# Existing distance-rules audit\n\n| Scenario | Distance | Status |\n|---|---|---|\n| Warp arrival at destination | minDistForStar(dest) | ✓ |\n| Observe→Navigate parking | minDistForStar(focal) | ✓ |\n| Observe travel | minDistForStar(target) | ✓ |\n| **Clean-slate app startup (no URL params)** | **hardcoded large default (≫ minDistForStar(Sol))** | ⚠ inconsistent |\n| **Click-to-focus / search-select / URL restore** | **hardcoded 2 pc** | ⚠ inconsistent |\n| **Warp source departure (pStart)** | **minDistForStar(DEST), not source — places camera INSIDE the source star when source is large (e.g. Betelgeuse)** | ⚠ inconsistent and wrong |\n| Manual zoom floor | minOrbitDistForStar (90% disc fill) | DIFFERENT — and intentionally so |\n\n# What to change\n\n1. **Clean-slate startup**. App boot with no URL params currently parks the camera at a hardcoded distance from Sol that's much farther than minDistForStar(Sol). Investigate where the boot pose is set (likely in stellata.ts init or url-state.ts when no fragment is present) and replace the hardcoded distance with minDistForStar(solIdx) so first-load matches every other auto-park.\n\n2. focusStar(starIndex, distancePc=2) at src/client/stellata.ts:1657. Default 2 pc → `this.minDistForStar(starIndex)`. Verify callers (search.ts:486, stellata.ts:2467, url-state.ts:692) still produce expected behavior. URL restore is the trickiest — if URL serialises a specific cam pose, focusStar might not even apply. Trace url-state.ts logic.\n\n3. **startWarp pStart at stellata.ts:1770**. Currently `pStart = A + dirBack * endOffset` where endOffset = minDistForStar(destIdx). Should use minDistForStar(sourceIdx) for the source-side reorient point. **Failure mode this causes: warping FROM a giant star (Betelgeuse, Antares) to a small star (Sol) places the source-side reorient point INSIDE the giant** — minDistForStar(Sol) ≈ 5e-8 pc but Betelgeuse's radius is ≈ 4e-6 pc, so the camera starts the warp embedded inside Betelgeuse. The source-side park distance must scale with the SOURCE's radius.\n\n   Concrete fix: pass sourceIdx through startWarp (or read from this.focusedStar at start and capture in warpState). Compute endOffsetSource = (state.destKind === 'star' \u0026\u0026 sourceIdx !== null) ? minDistForStar(sourceIdx) : endOffset. pStart = A + dirBack * endOffsetSource. pEnd unchanged (uses dest's endOffset).\n\n# Why\n\nKeeps the visual \"park me at this star\" experience consistent: every auto-arrival lands at 10% disc fill of the relevant target. Warp from Betelgeuse (huge R) to Sol (tiny R) shouldn't swoop close to Betelgeuse during the source-side reorient — that happens today because endOffset uses Sol's tiny park distance.\n\n# Acceptance\n\n- App boot lands at minDistForStar(Sol) — same as warping back to Sol from elsewhere.\n- focusStar() callers (click, search, URL) park at minDistForStar(idx) — 10% disc fill of target — for both small and giant stars.\n- Warp source-side reorient stays at sensible distance from the source (10% of source's disc fill) regardless of destination's size, so the camera is never embedded inside a giant source star.\n- No regression for warp from Sol-class to Sol-class (where it didn't matter).\n- Manual smoke: app boot, warp Betelgeuse→Sol, Sol→Betelgeuse, Sirius→Vega; observe→navigate park; search-select; click-to-focus.\n\n# Branch\n\nworktree-a7d.2-physical-disc-geometry, PR #22.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T18:55:29Z","created_by":"Alex Marshall","updated_at":"2026-05-05T17:28:33Z","started_at":"2026-05-05T11:34:43Z","closed_at":"2026-05-05T17:28:33Z","close_reason":"Implemented and pushed in commit 1599624 on PR #22. Boot park, focusStar default, click-from-unfocused, and warp source-side reorient now all use minDistForStar(idx) (or cloudViewingDistancePc for cloud sources). Two control-side bugs surfaced and fixed during smoke: minDistance ordering inside focusStar (was clamping against old focal star's minOrbit) and the unfocus jump-out (minDistance bump pushed camera outward when sitting close). Click-from-unfocused on a star now calls focusStar instead of setOrbitTarget for unified UX. setOrbitTarget remains for URL-restore where cam/tgt are explicit. Discovered during smoke: unfocusing while close to a focal star disengages the uPinFocusToCenter pin (target.lengthSq() \u003c 1e-12 invariant breaks when recenterOrigin shifts target to former focal star's world position), causing the star to jump on-screen. Also HUD ring disappears while Sol/GC arrows remain. Filed separately.","dependencies":[{"issue_id":"stellata-a7d.2.4","depends_on_id":"stellata-a7d.2","type":"parent-child","created_at":"2026-05-03T19:55:29Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.2","title":"Fade Sol/GC/distance arrows on close approach (disc covers standard chevrons)","description":"After a7d.2's angular-diameter formula, on close approach to a focused star the rendered disc grows large enough to start covering the navigate-mode reference arrows that emanate from the focused-star anchor. The Sol arrow, GC arrow, and distance-vector chevron all attach at SHAFT_START_PX = 28 px (focus-ring rim + halo gap) and extend outward; once the disc crosses that radius the proximal end of every arrow disappears under the disc.\n\n# Why\n\nThis is a UX problem in two directions:\n\n1. Mid-zoom the partially-buried arrows look broken — half-shaft sticking out of the star disc.\n2. At very close range distant context becomes irrelevant. Sol/GC are far-field references; the user is now in a near-field regime where future content (planets, exoplanets, binary orbits) is the relevant story. Hide the far-field chrome so the local view reads cleanly.\n\n# What\n\nCompute a single fade alpha per frame in navigate mode and apply it to **all three reference arrows** (Sol, GC, distance-vector). Trigger the fade on coverage of the **standard chevrons** (Sol/GC), not the distance-vector — the distance-vector can be very long, so basing the trigger on it would leave Sol/GC fully buried before fade engages.\n\n# Fade rule\n\n\\`\\`\\`\nshaftStartPx       = FOCUS_RING_RADIUS_PX + RING_HALO_GAP_PX = 28\nsolLenPx, gcLenPx  = current rendered shaft length of each\n                     (= clamp(projAlong - shaftStart - targetMargin, 0, ARROW_PIXEL_LENGTH))\nrefLenPx           = max(solLenPx, gcLenPx)\ndiscRadiusPx       = renderedSizePx(focusedIdx) / 2\ncoverage           = max(0, discRadiusPx - shaftStartPx) / refLenPx\nfadeT              = smoothstep(0.5, 0.75, coverage)   // cubic ease (3t^2-2t^3)\nalpha              = 1 - fadeT\n\\`\\`\\`\n\nUse the **dynamic max** of the two standard arrows (not a constant 110) so that when one arrow is shortened by Sol/GC projecting close to the focused star, the fade engages earlier — visually consistent. If both are zero (Sol focused → Sol arrow hidden, GC clipped) treat alpha as 0.\n\n# Scope\n\n- **Navigate mode only.** Observe mode unaffected (camera is parked at the focal star, no focal-disc on screen). Function returns 1 outside navigate.\n- **Rings stay as-is.** Focus ring's existing hide-when-disc\u003e48 is correct UX (the ring identifies the star against the starfield; redundant when the star fills your view). HUD ring (observe-mode) and POI rings unchanged.\n- **POI arrows (observe-mode) unchanged** — same reason as observe-mode HUD arrows: no focal-disc problem there.\n\n# Acceptance\n\n- Approach Betelgeuse / supergiant: as the disc grows, Sol/GC chevrons and any active distance-vector smoothly fade out together, reaching invisible before the disc fills the screen.\n- Approach Sol-class star to orbit floor: same behaviour.\n- Pull back: arrows fade back in at the same disc-radius thresholds.\n- Sol/GC/distance-vector all share one alpha — never one fading while the others are full opacity.\n- Observe mode: zero behavioural change.\n\n# Files\n\n- src/client/stellata.ts — new public method getNavigateArrowFadeAlpha(); fade calc; called from updateGalacticLayers before hudOverlay.update so both consumers see the same value within a frame.\n- src/client/hud-overlay.ts — Sol + GC arrow path/bg/label opacity = alpha. Two-pass refactor in update() to compute both shaft lengths first, then derive alpha (or accept it as opts), then render.\n- src/client/distance-vector-overlay.ts — dist-line/lineBg/label opacity = alpha (read via stellata.getNavigateArrowFadeAlpha()).\n- src/client/focus-ring-overlay.ts — untouched.\n\n# Branch\n\nworktree-a7d.2-physical-disc-geometry, PR #22.","notes":"Distance-vector policy refined by stellata-ml8 (2026-05-16): the\n\"all three arrows share one alpha\" framing in the description and\nacceptance was the bug — keying the distance-vector on\nmax(solLenPx, gcLenPx) tied its visibility to whether the HUD was\nenabled (so showHud=off → Sol/GC drawn lengths reset to 0 →\nshared alpha forced to 1 → distance-vector unintentionally\nvisible regardless of disc size). ml8 adopts option B from its\ndescription: Sol+GC keep their shared alpha (paired chevrons),\ndistance-vector computes its OWN alpha against its OWN drawn\nshaft length and the focused-star peak disc. A long\nfocal-star → destination arrow now outlasts the short Sol/GC\nchevrons by design. Per-frame geometry feeds the alpha (no more\nlast-frame-drawn-lengths lag), which also removes the\ntoggle-on flash on Sol/GC.","status":"closed","priority":1,"issue_type":"feature","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T18:54:45Z","created_by":"Alex Marshall","updated_at":"2026-05-16T15:17:34Z","started_at":"2026-05-04T10:11:40Z","closed_at":"2026-05-05T11:18:19Z","close_reason":"Implemented in PR #22. Navigate-mode arrow fade gates on max(actually drawn sol shaft, actually drawn gc shaft) with smoothstep over coverage [0.5, 0.75]. View-space dir replaces aux-step for direction derivation, eliminating per-arrow snap when targets are behind the camera. Peak-amplitude disc keeps fade stable across variability cycle. Hold source-mode alpha through observe transition; snap on completion when disc hides. Debug HUD: debug.arrows().","dependencies":[{"issue_id":"stellata-a7d.2.2","depends_on_id":"stellata-a7d.2","type":"parent-child","created_at":"2026-05-03T19:54:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.1","title":"Remaining centering issues after uPinFocusToCenter pin (warp arrival + other transitions)","description":"After commit 5035edf shipped the uPinFocusToCenter mechanism (force-pins focused star to NDC (0,0) bypassing float32 matrix cancellation in star.vert.glsl), the user confirmed Sol-on-default-load drift is fixed. But they report remaining centering problems on warp arrival and likely other camera transitions.\n\n# Context\n\nPR #22, branch worktree-a7d.2-physical-disc-geometry. Latest commit 5035edf \"Force focused star to NDC (0,0); raise unfocused floor to 5e-3 pc\".\n\nThe pin works as follows:\n- Shader (src/client/shaders/star.vert.glsl): `if (gl_InstanceID == uPinFocusToCenter) centreClip = projectionMatrix * vec4(0.0, 0.0, -dPc, 1.0);` — replaces the matrix-multiply path with a single canonical projection.\n- JS (src/client/stellata.ts:2702-2710): per-frame guard. Pin engages when (focusedStar !== null) \u0026\u0026 (cameraMode === 'navigate') \u0026\u0026 !warpState \u0026\u0026 !aimState \u0026\u0026 controls.target.lengthSq() \u003c 1e-12.\n- The target.lengthSq() check disengages the pin if the user pans the target away from the focused star's local origin.\n\nAlso raised GLOBAL_MIN_DIST_PC from 1e-9 → 5e-3 pc (line 170): unfocused zoom can no longer enter the float32-cancellation regime. To get closer than 5e-3 pc, focus a star (then per-star physical orbit floor applies).\n\n# Why warp arrival might still drift\n\nfinishWarp() (stellata.ts:1841) sequence on navigate-mode arrival:\n1. line 1873: camera.position = B - forward * endOffset (B in source-local frame; origin still on source A)\n2. line 1874: camera.lookAt(B)\n3. line 1876: controls.target = B (still in source-local frame, non-zero)\n4. line 1877: warpState = null\n5. line 1894: setFocus(destIdx) → recenterOrigin to dest. After recenter: camera at -forward*endOffset (local origin = dest), target = (0,0,0).\n\nAfter step 5 returns, animate() resumes at line 2698 with the recentered state. uPinFocusToCenter update at lines 2702-2710 sees focusedStar=destIdx, target.lengthSq()=0, mode=navigate → pin engages on first post-warp frame.\n\nSo the pin SHOULD work for warp arrival. Either:\n(a) my analysis is wrong somewhere\n(b) pin engages but the visible drift has a different cause\n(c) the centering issue is mid-warp (not arrival), and the user is interpreting it as arrival\n\nMid-warp: floating origin stays on source A through the animation. Destination B is at local position (B_world - A_world), not (0,0,0). Pin can't engage on B because focusedStar = source. The destination drifts naturally as the camera moves toward it; whether that reads as \"drift\" or \"expected motion\" depends on the visual expectation.\n\n# Investigation steps\n\n1. Reproduce. Warp from default view to several stars: Sol, Sirius A, Vega, Rigel, Betelgeuse. Note exactly when/where the off-center happens — last frame of warp? First frame post-arrival? Subtle vs gross?\n2. Verify the pin is engaging post-arrival. Add a temporary console.log to the per-frame uPinFocusToCenter update, or check the uniform's value via debug.\n3. Check if focusedStar matches gl_InstanceID for the rendered star (should be, since gl_InstanceID is the catalog index = star's row in instance buffer).\n4. Other transitions to also check while at it:\n   - aimAt (stellata.ts:2004) — animated rotation\n   - focusStar() teleport (stellata.ts:1657) — instant jump\n   - double-click teleport (stellata.ts:2528 dblClickAimPoint)\n   - observe-mode entry / exit (observe-transition)\n   - search-result selection (search.ts:486 → focusStar)\n   - URL-state restore on load (url-state.ts:692)\n\n# Possible additional pin variant\n\nIf mid-warp drift toward dest matters: extend the pin to ALSO engage on the destination during warp's final phase. Mechanism: pass uPinFocusToCenter = warpState.destIdx during warp's last ~30%, but with a different invariant — the destination is at controls.target which during warp = B in source-local frame ≠ origin. Would need a generalised pin: \"this gl_InstanceID's vertex projects to whatever NDC the controls.target would project to,\" i.e. force its centreClip = projection * (cam-relative-target-position, 1). More complex; only attempt if simpler pin doesn't fix the report.\n\n# Acceptance\n\n- Destination star sits at viewport center on warp arrival (last animation frame and first post-arrival frame indistinguishable).\n- focusStar/aimAt/double-click/search-select/URL-restore similarly center their target on first frame.\n- Verified across stars of varying R: Sol (1 R_sun), Sirius A (1.7), Vega (2.4), Rigel (79), Betelgeuse (~887).\n- No regression of the working Sol close-approach (commit 5035edf).\n\n# Files\n\n- src/client/stellata.ts:1841 finishWarp, :1735 startWarp, :2702 per-frame pin update, :2004 aimAt, :1657 focusStar\n- src/client/shaders/star.vert.glsl (uPinFocusToCenter use site)","notes":"Root cause of warp-arrival drift identified and fixed in finishWarp() navigate-mode path.\n\n# Diagnosis\n\nfinishWarp() wrote camera.position and controls.target in **source-local frame** using B = starLocalPosition(destIdx) — this reads from _localPositions, a Float32Array. setFocus(destIdx) was then called, whose recenterOrigin shifts target/camera by dx = catalog.positions[destIdx] - worldOffset, computed fresh in float64.\n\nB (Float32-truncated) and dx (float64-fresh) are the 'same' displacement only to within Float32 ULP. After the shift:\n- controls.target should be (0,0,0) in dest-local frame\n- It's actually (B - dx) ≈ ±|AB|·1e-7 magnitude residual\n\nFor Sol→Rigel (|AB| ≈ 265 pc): residual ≈ 1-5×10⁻⁵ pc. Camera distance to Rigel = endOffset = minDistForStar(Rigel) ≈ 3.4×10⁻⁵ pc. **Residual is comparable to camera distance** → significant NDC offset (~30% of screen, matches user report).\n\nPin guard at stellata.ts:2702-2710 requires target.lengthSq() \u003c 1e-12 to engage. Residual lengthSq ~1e-9 to 1e-8 → guard fails → pin disengages → dest renders at the actual (wrong) projected position.\n\nFor consecutive Rigel→Sol warp: endOffset = minDistForStar(Sol) ≈ 5×10⁻⁸ pc. Residual ≈ 5×10⁻⁵ pc → ratio ~1000× → Sol fully off-screen. Matches the second user report.\n\n# Fix\n\nIn finishWarp's navigate-star branch, after setFocus(destIdx) recenters origin onto the destination, snap controls.target to (0,0,0) and camera.position to -forward·endOffset directly in the clean dest-local frame. No round-trip through the source-local→dest-local subtract → no float32 residual → pin engages correctly on first post-arrival frame.\n\n# Other transitions audited\n\n- focusStar (click/search/URL restore): same shape of bug, but endOffset = 2 pc default makes residual/distance ratio ~1e-7 — sub-pixel, not visually observable. Will become observable if/when stellata-a7d.2.4 unifies focusStar's default to minDistForStar(idx). At that point apply the same pattern.\n- aimAt: no recenter, not affected.\n- observe arrival: target placement in source-local frame too, but observe doesn't use trackball orbit so target offset doesn't affect rendering.\n\n# Verification\n\n- npm run typecheck: clean\n- npm test: 246/246 pass\n- Dev server: in browser, focus Sol (default), warp to Rigel — should land centred and stay centred at full zoom-in. Then warp back to Sol — Sol should be on-screen and centred.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T18:54:22Z","created_by":"Alex Marshall","updated_at":"2026-05-03T19:43:10Z","started_at":"2026-05-03T19:21:49Z","closed_at":"2026-05-03T19:43:10Z","close_reason":"Fixed in commit d6c97da. Root cause: finishWarp wrote target/camera in source-local frame (B from Float32 _localPositions) then setFocus's recenterOrigin shifted by dx computed fresh in float64 — Float32 ULP mismatch left controls.target at a ~|AB|·1e-7 residual that disengaged the pin guard on long warps. Fix re-anchors target/camera in clean dest-local frame after setFocus runs. Verified by user: Sol→Rigel→Sol round-trip stays centred.","dependencies":[{"issue_id":"stellata-a7d.2.1","depends_on_id":"stellata-a7d.2","type":"parent-child","created_at":"2026-05-03T19:54:22Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-eya","title":"Search/typeahead: keep DOM, only toggle .active class on arrow-nav","description":"Surfaced in code review (stellata-y84).\n\nsrc/client/search.ts:94 and src/client/constellation-typeahead.ts:69 both use the same anti-pattern: every keystroke AND every ArrowUp/ArrowDown nav rebuilds the entire results `\u003cul\u003e` via `resultsEl.innerHTML = ''` followed by per-result `appendChild`. Arrow-nav only changes which row has the `active` class; tearing down and rebuilding the whole list is unnecessary and noticeable as jank with the typeahead's 88-entry constellation list.\n\nFix: For arrow-nav, just toggle the `active` class on the previously-hovered and newly-hovered `\u003cli\u003e` in place. Rebuild only when the query changes.\n\nSeverity: HIGH (visible UX latency on arrow-nav).\n","notes":"Fix shipped in PR #29 (branch worktree-eya-typeahead-arrow-nav, commit 8cbb926). Approach: added a setHover() helper in both src/client/search.ts (SearchBox method) and src/client/constellation-typeahead.ts (closure). Toggles .active on prev and new \u003cli\u003e in place; ArrowUp/ArrowDown call it instead of the full rebuild. renderResultsDom/renderDom still run on query change. Typecheck clean, 254/254 tests pass. Browser smoke pending review.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:33:57Z","created_by":"Alex Marshall","updated_at":"2026-05-05T22:21:19Z","started_at":"2026-05-05T21:28:54Z","closed_at":"2026-05-05T22:21:19Z","close_reason":"Closed","labels":["perf"],"dependencies":[{"issue_id":"stellata-eya","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:33:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-0a7","title":"Star vertex shader: move dustExtinctionAV below visibility prefilter","description":"Surfaced in code review (stellata-y84).\n\nsrc/client/shaders/star.vert.glsl:225 — `dustExtinctionAV` runs a 48-tap 3D-texture raymarch through the dust field for **every star instance every frame**, including stars that fail the visibility prefilter (`visible`/`magOk`/`distOk`/`spectOk`) further down the shader (line 244+). With 313k stars, this is the dominant vertex-shader cost.\n\nFix: Move the visibility prefilter above the dust raymarch. Compute the unextincted apparent magnitude first, fail-fast on stars that are out of band, then refine with extinction only for the survivors.\n\nCaveat: stars near the magnitude threshold could pop in/out as extinction is applied/skipped. May need a small magnitude buffer (e.g. allow extra 0.5 mag headroom in the prefilter so dust-darkened stars within tolerance still get the raymarch).\n\nSeverity: HIGH (single biggest per-frame GPU win in the shader chunk).\n","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:33:04Z","created_by":"Alex Marshall","updated_at":"2026-05-07T09:13:50Z","started_at":"2026-05-07T09:06:52Z","closed_at":"2026-05-07T09:13:50Z","close_reason":"Landed in PR #35 (v1.1.4): warp degrades gracefully for co-located stars (setFocus on coincident A/B); orbit floor + parking distance use peak-amplitude effective radius; star vertex shader skips the 48-tap dust raymarch for visibility-prefilter rejects with a 1.5 mag DUST_AV_HEADROOM. Smoke tests pass per Alex 2026-05-07.","labels":["perf"],"dependencies":[{"issue_id":"stellata-0a7","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:33:04Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-7is","title":"Catalog binary: 0x10 flag bit semantics + inferBinaries pairing semantics","description":"Surfaced in code review (stellata-y84).\n\nTwo distinct issues in scripts/build-catalog.ts and scripts/catalog-pure.ts:\n\n**A. 0x10 flag bit semantics diverge between two writers**\n- `applyDoublesFlag` (build-catalog.ts:367) sets `0x10` on every star whose HIP appears in the CCDM doubles set — applies to both components.\n- `inferBinaries` (catalog-pure.ts:287) sets `0x10` only on the brighter member of a geometric pair.\n\nThe renderer reads bit `0x10` with one assumed meaning, but receives a mix. Either align both writers to the same semantics (mark all components, or only primaries) or split into two bits.\n\n**B. inferBinaries produces a directed nearest-neighbour graph, not mutual pairs**\n- catalog-pure.ts:269-282 finds each star's nearest neighbour and assigns it as `companionIdx` without checking that the relationship is mutual. The header comment (line 226-229) implies pair semantics (\"record as this star's companion and flag the brighter of the pair\").\n- Result: A→B and B→C is possible. The \"primary\" pick at line 287 is per-i and overlaps incoherently in 3-star clusters.\n\nFix: Decide between mutual-only pairs (post-process to require A→B ∧ B→A) or document the directed-graph behaviour explicitly. Either way, align the renderer's interpretation.\n\nSeverity: CRITICAL — the binary feeds every client.\n","notes":"Fix landed in PR #13 (claude/fix-binary-flag-semantics). Both writers now treat 0x10 as 'primary anchor of a system, at most one per system': inferBinaries flags only the brighter member of mutual pairs (companionIdx remains directed for renderer use), applyDoublesFlag groups CCDM HIPs by CCDM_ID and picks brightest per group. KNOWN_VISUAL_DOUBLES restructured into {components, reason} systems so 61 Cyg A+B share one group. Tests updated; rebuild stats: 14 mutual geometric pairs + 12384 new CCDM primaries on top, 9-system overlap shows agreement between writers.","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:32:05Z","created_by":"Alex Marshall","updated_at":"2026-05-03T08:35:42Z","started_at":"2026-05-03T08:00:38Z","closed_at":"2026-05-03T08:35:42Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-7is","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:32:05Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-c2l","title":"GPU resource disposal audit — Stellata.dispose() leaks materials/geometries","description":"Surfaced in code review (stellata-y84).\n\n`Stellata.dispose()` (src/client/stellata.ts:2912-2914) currently disposes only a subset of GPU resources. The following are not released:\n\n- `coreMaskMaterial` (separate from the three star materials)\n- `particleMesh.geometry` and `particleMaterial`\n- `clouds` (MolecularClouds layer)\n- `galacticDisc`\n- `galacticGrid`\n- `hudOverlay` if it owns any GPU resources\n\nIn Three.js, every Material/Geometry/Texture/RenderTarget allocates GPU memory and must be `.dispose()`d explicitly. The current dispose() leaks all of the above on teardown. Today this is academic since dispose() runs only on page navigation, but it would matter for any future hot-reload / re-init path and for the in-flight tests around scene swapping.\n\nFix: Walk the dispose chain end-to-end and verify each layer/material is released. Easiest path: have each owned layer expose its own `dispose()` and call it from `Stellata.dispose()`.\n\nSeverity: CRITICAL (real leak, but currently low-impact).\n","status":"closed","priority":1,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:32:04Z","created_by":"Alex Marshall","updated_at":"2026-05-03T08:38:03Z","started_at":"2026-05-03T08:35:27Z","closed_at":"2026-05-03T08:38:03Z","close_reason":"Fixed in PR #14 — dispose() now releases coreMaskMaterial, particleMesh+particleMaterial, clouds, galacticDisc, galacticGrid alongside the previously-disposed star geometry/materials and milkyway.","dependencies":[{"issue_id":"stellata-c2l","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:32:04Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-y84","title":"Thorough code review of production code for quality issues","description":"Conduct a comprehensive code review of all production code in src/ and scripts/ looking for:\n\n- Inefficiencies (hot path waste, redundant work, suboptimal data structures)\n- Bad style (inconsistent naming, unclear control flow, awkward abstractions)\n- DRY violations (duplicated logic, repeated patterns that should be extracted)\n- Dead code (unused exports, unreachable branches, stale flags)\n- Premature abstractions or over-engineering\n- Missing or misplaced comments (WHY comments needed; WHAT comments to remove)\n- Unnecessary complexity (could be simpler without losing capability)\n- Type-safety gaps or any-typed escape hatches\n- Resource handling (event listener leaks, observer cleanup, GPU resource disposal)\n\nOutput: a triaged list of findings, each with file:line references and a recommended fix. Group by severity. File follow-up beads issues for non-trivial fixes; apply trivial cleanups directly.","notes":"Reviewed all 54 production source files (12,735 LOC) across src/ and scripts/. Triaged findings into 22 follow-up issues filed with `discovered-from:stellata-y84`.\n\n**P1 (critical)** — 2 issues:\n- stellata-c2l: GPU disposal audit (Stellata.dispose leaks)\n- stellata-7is: 0x10 flag bit + inferBinaries pairing semantics\n\n**P2 (high)** — 11 issues:\n- Bugs: stellata-3s2, stellata-470, stellata-6oy, stellata-adr, stellata-mu9\n- Perf: stellata-0a7 (shader dust prefilter), stellata-20w (pickStar), stellata-63f (Vector3 alloc), stellata-a0n (url-state hash), stellata-akk (SVG dirty-track), stellata-eya (typeahead arrow-nav)\n\n**P3 (medium)** — 9 issues:\n- DRY: stellata-snv, stellata-vjj, stellata-wk2, stellata-77m, stellata-v2j, stellata-075\n- Cleanup: stellata-r48, stellata-t6l, stellata-nqb (rollup of small comment/WHY/dead-code findings)\n\n**Trivial cleanups applied directly** (single commit):\n- chart-labels.ts:240 — `let` → `const lastCentroidCamPos`\n- chart-mode.ts:33 — `bindChartMode` return type `: void`\n- molecular-clouds.ts:184 — `idx ?? null` simplify\n- theme-toggle.ts — drop unused `getTheme()` + `currentMode` write-only state\n- milkyway.ts:120 — collapse unused `GAL_TO_ICRS_M3` into one-line `ICRS_TO_GAL_M3` derivation\n\nTypecheck clean, 244/244 tests pass.\n\n**Overall impressions** from agents (synthesized):\n- Hot-path discipline is generally strong (scratch quaternions, sortedDistFromSol prefilter for the core-mask gate, instanced star pipeline) but a recurring theme is per-frame Vector3 allocation across stellata.ts, the SVG overlays, and url-state. Adopting the `xxxInto(idx, out)` and dirty-tracked attribute write patterns from chart-labels.ts is the single biggest perf win available.\n- Shader hygiene is good (calibration comments on every magic uniform); biggest single GPU win is moving the 48-tap `dustExtinctionAV` raymarch below the visibility prefilter.\n- Type safety has one concrete hole (url-state FilterState patch) plus untyped CSV row + search-index entry in build-catalog.\n- Binary format hygiene is solid but constants are duplicated across writer/verifier/reader; a shared layout map would prevent silent drift.\n- DRY: clearest wins are modal dismissal (×3 modals), hud-overlay/poi-overlay arrow algorithm (~80 LOC duplicated), SearchBox vs constellation-typeahead (parallel implementations of the same UI), eight on*Handlers arrays in stellata.ts.","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:20:48Z","created_by":"Alex Marshall","updated_at":"2026-05-02T19:35:58Z","started_at":"2026-05-02T19:21:37Z","closed_at":"2026-05-02T19:35:58Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2","title":"Replace disc formula, orbit floor, and parking distance with true angular geometry","description":"Replace the log-mapped disc formula with the physical-angular-diameter formula `θ = 2·atan(R · radiusFactor / d)`, and drive the manual-zoom orbit floor + warp parking distance from the same per-star physical radius.\n\nDepends on the log-depth-buffer setup — without it, Sol-class stars will z-fight on close approach.\n\n# Geometry\n\n```\nθ_rad      = 2 · atan(R · radiusFactor / d)\nphysSizePx = θ_rad · viewport.y / fov_y_rad\n```\n\nSlots in at `star.vert.glsl:294` (current `physSize = sizeAtRef * (uRefDistPc / dPc) * radiusFactor`). Same swap also needed inside the variability headroom precompute (`star.vert.glsl:203-206`).\n\n`pxSize = max(appSize, physSize)` continues to give the natural blob↔disc crossover. `vPhysRatio = clamp(physSize / pxSize)` continues to drive the disc-pass / glow-pass split.\n\n# Manual zoom floor (90% of fill-the-viewport)\n\nReplaces `minOrbitDistForStar()` (`stellata.ts:2306-2316`):\n\n```\nZOOM_FLOOR_FRACTION = 0.9\nfov_y = camera.fov · π/180\nfov_x = 2 · atan(tan(fov_y/2) · w/h)\nfov_minor = min(fov_x, fov_y)\nd_min(idx) = R_idx / tan(ZOOM_FLOOR_FRACTION · fov_minor / 2)\n```\n\nPreserve the binary half-angle bump (`Math.max(d_min, sep × BINARY_MIN_DIST_FACTOR)`).\n\nRecompute on:\n- Focus change (already wired at `stellata.ts:1045` and `:1876`).\n- FOV change — wire into `setCameraFov()` (`stellata.ts:1473`).\n- Window resize.\n\n# Warp / observe-exit parking distance\n\nReplaces `minDistForStar()` (`stellata.ts:2329-2350`):\n\n```\nTARGET_PARK_FRACTION = 0.10\nd_park(idx) = R_idx / tan(TARGET_PARK_FRACTION · fov_minor / 2)\n```\n\nFloor at `2 × d_min(idx)` so the orbit floor never exceeds the parking distance. Preserve binary half-angle bump.\n\n# CPU mirror\n\nRewrite `renderedSizePx()` (`stellata.ts:2237-2297`) to mirror the new geometry exactly. Variability `radiusFactor` continues to multiply `R` before the `atan`. `appSize` (perceptual blob) path stays unchanged.\n\n# Uniforms — remove\n\n| Removed                       | Reason                                                        |\n|-------------------------------|---------------------------------------------------------------|\n| `uPhysMinPx` / `uPhysMaxPx`   | No more pixel-range mapping.                                  |\n| `uLogRMin` / `uLogRMax`       | No more log-span normalisation.                               |\n| `uRefDistPc`                  | No artificial reference distance.                             |\n| `DEFAULT_MIN_DIST_PC = 0.005` | Replaced by per-star `d_min(R)`. Keep a small global default (e.g. `1e-9` pc) for the unfocused-around-origin orbit floor. |\n| `TARGET_APPROACH_DISC_PX = 10`| Replaced by `TARGET_PARK_FRACTION = 0.10`.                    |\n\n# Uniforms — add\n\n- `uFovYRad` (float) — fed from `camera.fov · π/180`. Updated in the constructor and in `setCameraFov()`.\n- (Alternative: derive from `uViewport` + a `uPxPerRad` scalar — `uFovYRad` is simpler.)\n\n# Files\n\n- `src/client/shaders/star.vert.glsl` — replace physSize formula at line ~294 and ~203-206; drop dead uniform declarations; add `uFovYRad`.\n- `src/client/stellata.ts`:\n  - Line 468 — the log-depth-buffer setup already touched this.\n  - Lines 483-488 — the log-depth-buffer setup already touched this.\n  - Line 501 — `controls.minDistance` initial value uses the new global default.\n  - Lines 593-616 (shared uniforms) — drop the dead uniforms; add `uFovYRad`.\n  - Lines 2227-2229 — delete `computePhysMaxPx()`.\n  - Lines 2237-2297 — rewrite `renderedSizePx()`.\n  - Lines 2306-2316 — rewrite `minOrbitDistForStar()`.\n  - Lines 2329-2350 — rewrite `minDistForStar()`.\n  - Constants block (lines 171-190): remove `DEFAULT_MIN_DIST_PC`, `TARGET_APPROACH_DISC_PX`; add `TARGET_PARK_FRACTION`, `ZOOM_FLOOR_FRACTION`, `GLOBAL_MIN_DIST_PC` (e.g. `1e-9`).\n  - Line 246 export — `DEFAULT_MIN_DIST_PC` is exported; `grep -rn DEFAULT_MIN_DIST_PC src/` and migrate or drop.\n  - `setCameraFov()` (line 1473) — push the new `fov_y_rad` into `uFovYRad`.\n- `src/client/distance-vector-overlay.ts:100` — switch destination offset from `getFilter().sizeMax` to `stellata.renderedSizePx(destIdx)` so the chevron lands on the disc edge regardless of star size.\n- Docs (commit alongside): `docs/rendering.md`, `docs/camera-modes.md`.\n\n# Verification (manual smoke + tests)\n\n1. **Background stars unchanged.** Default view, naked-eye preset. Sirius / Betelgeuse / Vega pixel-identical to current `main`.\n2. **Resolved-disc ratio matches physics.** Warp to Betelgeuse and Sirius A in turn at the same FOV. On-screen disc ratio at equal `d/R` matches `R_Betel / R_Sirius`. (Old behaviour: identical 10 px discs.)\n3. **Close approach to Sol-class star.** Focus a Sol-like star, zoom in fully. Disc grows until ~90% of viewport minor axis, then stops. No clipping, no z-fighting.\n4. **Close approach to a white dwarf** (40 Eridani B, Sirius B). Same — fills ~90% of viewport at d_min.\n5. **Close approach to Betelgeuse.** Disc fills ~90% of viewport at much greater absolute distance than Sirius A. Pulsation visibly grows/shrinks the radius.\n6. **FOV change at zoom limit.** While zoomed to fill viewport, change FOV via `+`/`-`. Orbit floor recomputes; star tracks the new \"fills 90% of viewport minor axis\" target.\n7. **Overlays follow the new disc.** Disc mask, focus ring, distance vector chevron all align with the rendered disc edge for both small and giant destination stars.\n8. **No depth artefacts.** At d_min on Sol-class, distant stars (200 kpc) still render correctly behind the focused disc.\n9. **Mechanical:** `npm run typecheck`; `npm test` (244 regression tests); smoke-test warp, observe mode, chart mode.\n\n# Acceptance\n\nAll nine verification steps pass.\n\n# Plan file\n\n`~/.claude/plans/resume-planning-from-users-alexm-claude-reflective-wombat.md`\n","notes":"Implementation landed on branch worktree-a7d.2-physical-disc-geometry, PR #22. Mechanical gates pass (typecheck, 246 tests, build).\n\n# Status as of 2026-05-03 (post-context-clear handoff)\n\nScope expanded mid-validation: a7d.2 itself shipped the geometry rewrite, but user-facing UX work surfaced several follow-ups. All filed as sub-issues:\n\n- stellata-a7d.2.1 (P1, BUG) — Remaining centering issues post-pin (warp arrival flagged). NEXT WORK.\n- stellata-a7d.2.2 (P2) — Overlays follow rendered disc (focus-ring grows; audit poi/hud).\n- stellata-a7d.2.3 (P2) — Scale-bar UX revisit + camera-to-focus distance indicator. NEEDS USER DECISION on options A/B/C.\n- stellata-a7d.2.4 (P2) — Unify auto-park distances (focusStar default 2pc + warp source pStart).\n- stellata-a7d.2.5 (P3) — Refresh docs at PR-ready time.\n\na7d.2 closes only after all five sub-issues close. All work continues on the SAME branch/PR (#22).\n\n# Current commits on branch (HEAD = 5035edf)\n\n- 8e9820d — Rewrite star sizing to true angular geometry (the main landing)\n- 1be9c0c — docs: refresh rendering.md + camera-modes.md (STALE — see .5)\n- d711a77 — Fix: convert physicalRadius from solar radii to pc\n- dd1d219 — Fix: lower dPc clamp from 0.001 → 1e-30\n- 5035edf — Force focused star to NDC (0,0); raise unfocused floor to 5e-3 pc\n\n# Rebased onto main 3c39414 (post-PR23/24)\n\n# Validation status\n\nUser confirmed working:\n- Background stars (unchanged)\n- Disc-size variation between stars\n- 90% close-approach fill\n- FOV adjustment\n- Sol stays centered (after 5035edf)\n\nUser reported still broken:\n- Warp arrival centering (filed as .1)\n- Overlays don't track disc (filed as .2)\n- Scale-bar / distance UX confusing (filed as .3)\n- Multiple inconsistent park distances (filed as .4)","status":"closed","priority":1,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:07:32Z","created_by":"Alex Marshall","updated_at":"2026-05-08T11:12:23Z","started_at":"2026-05-03T10:02:05Z","closed_at":"2026-05-06T22:05:36Z","close_reason":"Landed via PR #22 (worktree-a7d.2-physical-disc-geometry). Disc formula, orbit floor, parking distance, and binary-companion handling all driven by physical angular geometry. Five follow-up commits during PR review tightened the surface area: a7d.2.3 (scale-bar AU/perspective z-axis), a7d.2.5 (docs refresh), a7d.3 (distance vector lands on cloud silhouette via renderedCloudSizePx). PR #22 review fallout filed under stellata-9mm.52-63 (P3 cleanup queue, gated on PR #22 merge per pr-30-merge-gate-9mm-batch-2 memory).","dependencies":[{"issue_id":"stellata-a7d.2","depends_on_id":"stellata-a7d","type":"parent-child","created_at":"2026-05-02T18:07:32Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-a7d.2","depends_on_id":"stellata-a7d.1","type":"blocks","created_at":"2026-05-02T18:08:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-a7d","title":"Physical-scale rewrite of star sizing \u0026 close approach","description":"Rewrite the star disc / close-approach pipeline so that resolved discs match true angular geometry (`θ = 2·atan(R/d)`) and the orbit + parking distances scale with each star's physical radius.\n\n# Why\n\nStellata's stated intent is *physical reality with perceptual exaggeration only where the eye demands it.* Today the close-approach pipeline breaks that contract in three places:\n\n1. **Disc-pass size formula is non-physical.** `physSize = sizeAtRef × (uRefDistPc / dPc) × radiusFactor`, where `sizeAtRef` is a *log-mapped pixel target* — the catalogue's full radius span (white-dwarf → hypergiant) is compressed into `[2 px, 50% of viewport]` at an arbitrary reference distance of `0.005 pc`. The `1/d` falloff is correct geometry, but it's applied to a perceptually-warped base — so the on-screen ratio between Sol and Betelgeuse does **not** match `R_Sol / R_Betelgeuse`.\n2. **Reference distance is locked to `controls.minDistance`.** `uRefDistPc` and the orbit floor are the same number (`DEFAULT_MIN_DIST_PC = 0.005 pc`). Past that floor, growth saturates instead of continuing as physics demands.\n3. **Auto-park targets a uniform 10 px disc.** `TARGET_APPROACH_DISC_PX = 10` collapses the physical-radius hierarchy at warp arrival: a white dwarf and Betelgeuse arrive identical-looking. UX-coherent, physics-incoherent.\n\nBackground-star calibration (point-source perceptual blob driven by arcsec-per-pixel × exaggeration K) is **correct** and must remain pixel-identical after this change. Only the resolved disc and the close-range floors change. Future work (exoplanets, solar-system mode) needs this physical foundation.\n\n# Approach (summary)\n\nFor a star of physical radius `R` (pc) at camera distance `d` (pc):\n\n```\nθ_rad      = 2 · atan(R · radiusFactor / d)        (true angular diameter)\nphysSizePx = θ_rad · viewport.y / fov_y_rad        (pixel diameter, screen-aligned quad)\n```\n\n`pxSize = max(appSize, physSize)` continues to give the natural crossover between the perceptual blob and the physical disc. Variable pulsation multiplies `R` (`R_eff = R · radiusFactor`). The `vPhysRatio` disc-pass / glow-pass split is now driven by *real* angular size.\n\nClosest manual zoom (90% of fill-the-viewport):\n```\nZOOM_FLOOR_FRACTION = 0.9\nfov_minor = min(fov_x, fov_y)\nd_min(star) = R / tan(ZOOM_FLOOR_FRACTION · fov_minor / 2)\n```\n\nAuto-park (warp arrival, observe-exit):\n```\nTARGET_PARK_FRACTION = 0.10\nd_park(star) = R / tan(TARGET_PARK_FRACTION · fov_minor / 2)\n```\n\nZ-buffer precision is the critical enabler: at d_min for Sol-class stars (`R ≈ 2.25e-8 pc`, `d_min ≈ 5e-8 pc`), standard depth precision collapses. the log-depth-buffer setup enables `logarithmicDepthBuffer: true` on the renderer and adds `\u003clogdepthbuf\u003e` chunks to every custom GLSL3 ShaderMaterial.\n\n# Phasing\n\nTracked as two child tasks:\n- **Prerequisite (`stellata-a7d.1`)** — Enable log depth buffer + add `\u003clogdepthbuf\u003e` chunks to all shaders. Pure prerequisite, no visual change expected.\n- **Main rewrite (`stellata-a7d.2`)** — Replace disc formula, orbit floor, parking distance. Depends on the log-depth-buffer setup landing first.\n\n# Out of scope\n\n- Star colour / luminosity-class softness (untouched).\n- Background-star calibration (untouched — confirmed correct).\n- Chart mode disc sizing (`uMonochrome \u003e 0.5` path is independent).\n- `star-tuning.ts` debug panel (its sliders don't expose the removed uniforms).\n\n# Acceptance\n\n1. Background stars pixel-identical to current `main` from default view (naked-eye preset).\n2. Resolved-disc ratio between Betelgeuse and Sirius A at equal `d/R` matches `R_Betel / R_Sirius`.\n3. Manual zoom to a focused star grows the disc until ~90% of viewport minor axis, then stops. Works for white dwarfs, Sol-class, and supergiants.\n4. Warp arrival: every star arrives at 10% of viewport minor axis. Parking distance scales with R.\n5. FOV `+`/`-` while at zoom limit recomputes the orbit floor live.\n6. Disc mask, focus ring, distance-vector chevron all track the new disc size correctly.\n7. No z-fighting at d_min for Sol-class stars; no banding artefacts in the milky-way / cloud / dust-particle layers.\n8. `npm run typecheck` clean; `npm test` (244 regression tests) green.\n\n# Plan file\n\nFull plan at `~/.claude/plans/resume-planning-from-users-alexm-claude-reflective-wombat.md`.\n","status":"closed","priority":1,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:06:26Z","created_by":"Alex Marshall","updated_at":"2026-05-08T11:13:15Z","closed_at":"2026-05-07T09:26:27Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-a7d","depends_on_id":"stellata-zxy","type":"relates-to","created_at":"2026-05-02T18:14:21Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.61","title":"Phase 2 — Stage 2 secondary HIP resolution for high-PM and saturated-primary systems","description":"# Why\n\ndch.30 (Stage 3 astrometry attachment) landed with a no-Gaia HIP2 fallback that successfully attaches astrometry to Gaia-saturated bright primaries (Sirius A, α Cen A, Procyon A, Algol Aa). But the bead's full acceptance also expected secondaries (α Cen B, Sirius B-like cases) and Proxima Centauri (component of LDS 494) to route via hip2_long_baseline / gaia_5p — those currently emit `via=unresolved` because Stage 2's secondary resolution chain doesn't reach them.\n\nRoot causes surfaced during dch.30 validation:\n\n1. **WDS precise_coord is at date_last epoch, not J2000.** For high-PM stars (α Cen ≈ -3614 mas/yr, Sirius ≈ -546 mas/yr), the WDS precise coord at observation date drifts ~tens-to-hundreds of arcsec from AT-HYG's J2000 position over the catalog's lifetime. parse_athyg's 2″ position tolerance silently misses these — the AT-HYG row for α Cen A (HIP 71683) at ra=219.92041 sits 66″ away from WDS RHD 1 AB's precise coord 219.902.\n\n2. **No HIP-resolution path for secondaries.** ORB6 publishes one HIP per orbit row (the primary's by convention). Tier 2's HIP-branch only runs for primaries. The position-match secondary path predicts secondary position via WDS (ρ, θ) offset but inherits the same 2″ tolerance gap, AND for very wide pairs uses WDS's 999.9″ overflow sentinel as ρ (Proxima Cen vs α Cen).\n\n3. **CCDM cross-references unused.** data/hip_ccdm.tsv carries per-component HIP-to-CCDM mapping. For systems like α Cen (CCDM 14396-6050), CCDM tells us HIP 71681 = component B and HIP 71683 = component A. Tier 2 could consult CCDM as a fourth identifier-anchored path before falling back to position match.\n\n# Failing-now acceptance cases (from dch.30 diagnostic)\n\n| Star | Expected | Actual | Root cause |\n| - | - | - | - |\n| α Cen B (HIP 71681) | hip2_long_baseline | unresolved | CCDM gap + 2″ position-match miss |\n| Sirius B | none in HIP2 anyway | unresolved | Sirius B has no HIP (Hipparcos didn't resolve A/B) — fine to stay unresolved |\n| Procyon B | hip2_long_baseline if HIP'd | unresolved | Procyon B has no HIP (similar) |\n| Proxima Centauri | gaia_5p | unresolved | WDS ρ=999.9″ overflow + wide-pair secondary prediction breaks |\n| Castor B+C secondaries | hip2_long_baseline | mostly unresolved | Mix of WDS hierarchy 'Aa' vs 'A' propagation gap + secondary resolution |\n\n# Acceptance\n\n1. Stage 2 consults CCDM (data/hip_ccdm.tsv) as a per-component HIP source. For each WDS pair, look up the system's CCDM row(s) and map known HIPs to the matching component letter. New tier between current tiers 2 and 3.\n2. WDS precise_coord position-match path PM-propagates AT-HYG positions to WDS date_last epoch before the 2″ comparison. Reuses the gaia astrometry already loaded for proper-motion (or AT-HYG's pmra/pmde columns if Gaia astrometry is missing).\n3. WDS ρ=999.9″ is recognised as the wide-pair overflow sentinel and short-circuits the predicted-secondary position match (use the AT-HYG row matching the secondary's known coord directly).\n4. propagate_within_system handles WDS component-letter hierarchy: 'A' inherits from 'Aa' (Castor STF1110 A inherits the binding from CIA 29 Aa).\n5. Re-run build-binaries.py and verify α Cen B, Proxima, Castor secondaries route correctly. Update the dch.30 acceptance table here once satisfied.\n\n# Smoke\n\n```\nnpm run build:binaries -- --force\n```\n\nVerify via the dch.30 diagnostic script (in PR body / handoff) that the named acceptance stars route correctly.\n\n# Files to touch\n\n- scripts/build-binaries.py — Stage 2 (resolve_component, resolve_via_position, propagate_within_system)\n- scripts/build-binaries.test.py — tests for CCDM tier, PM-propagated position match, 999.9 sentinel, A vs Aa propagation\n\n# Commit\n\nOne commit. Smoke before commit.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T16:46:50Z","created_by":"Alex Marshall","updated_at":"2026-05-19T16:46:50Z","labels":["build-binaries","phase-2","resolution","stage-2"],"dependencies":[{"issue_id":"stellata-dch.61","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-19T17:46:50Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.203.2","title":"Implement build-binaries spot-check harness against ground-truth TSV","description":"Second child of stellata-9mm.203. Implement the end-to-end harness that reads the TSV from .1 and asserts.\n\n# Deliverable\n\n- `scripts/build-binaries-spotcheck.py` (or extend the existing test module with a `--spotcheck` flag — pick whichever fits cleaner, NOT a separate language).\n- An npm script (`npm run test:spotcheck` or `npm run check:spotcheck`) that invokes it.\n- One pretty-print row per missed assertion: `[category] name (wds_id comp) — expected (source_id, resolve_via) got (source_id, resolve_via)`.\n- Aggregate summary: `N/M assertions passed`. Non-zero exit when M-N \u003e 0.\n\n# How\n\nRun the existing Stage 1 loaders + `resolve_all_pairs` once at module top; iterate the ground-truth TSV; index resolutions by `(wds_id, component)`; compare. Reuse — do not re-implement — the loaders + resolver from `build-binaries.py`.\n\n# Performance budget\n\nEnd-to-end ≤ 15s on a clean laptop (full Stage 1 load is ~8s today, resolution another ~2s, assertions are trivial). If it grows past 30s, factor the loaders so they cache between unit + spotcheck runs.\n\n# Acceptance\n\n- All TSV rows assert green on current committed data.\n- Adding a new row to the TSV passes without harness edits (data-driven).\n- Failure output is triageable in one read (no traceback dump).\n- README / CLAUDE.md mentions when to run it: before merging any Stage 2-7 change.\n\n# Smoke\n\n- `npm run test:spotcheck` exits 0.\n- Temporarily edit one expected_gaia_source_id in the TSV → harness fails with a clear single-line diff for that row.\n- Temporarily edit one expected_resolve_via to a wrong tier → harness fails on the tier mismatch separately from the source_id mismatch.\n\n# Blocked by\n\nstellata-9mm.203.1 (the TSV must exist before the harness can read it).","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T14:00:10Z","created_by":"Alex Marshall","updated_at":"2026-05-19T14:00:10Z","labels":["build-binaries","cross-match","test-coverage"],"dependencies":[{"issue_id":"stellata-9mm.203.2","depends_on_id":"stellata-9mm.203","type":"parent-child","created_at":"2026-05-19T15:00:10Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.203.2","depends_on_id":"stellata-9mm.203.1","type":"blocks","created_at":"2026-05-19T15:00:16Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.203.1","title":"Curate SIMBAD-derived ground-truth TSV for build-binaries spot-checks","description":"First child of stellata-9mm.203. Curate the ground-truth TSV the end-to-end harness reads.\n\n# Deliverable\n\n`data/spot_check_ground_truth.tsv` (path tentative) with ≥50 rows across the categories listed in the parent. Schema:\n\n    category    name        wds_id          component   expected_resolve_via    expected_gaia_source_id     note\n\nColumn notes:\n- `category` — one of `naked_eye_double`, `gaia_saturated_bright`, `high_pm`, `cluster_member`, `distant_supergiant`, `variable`, `multi_component`, `obscure_resolved`. Loose, but pinned so reports can group failures by regime.\n- `name` — canonical name (proper or HD/HIP if no proper). Display-only.\n- `wds_id` — WDS positional id (`HHMMm±DDMM`); blank for non-WDS spot-checks if any.\n- `component` — WDS component letter (`A`, `B`, `Aa`, etc.); blank for system-level checks.\n- `expected_resolve_via` — one of `RESOLVE_VIA_VALUES` from `scripts/build-binaries.py`. Tier-drift detector.\n- `expected_gaia_source_id` — canonical SIMBAD-reported Gaia DR3 source_id. Blank when SIMBAD has no DR3 for the object (Sirius A class) — in that case, `expected_resolve_via=unresolved` (or whatever dch.30 promotes it to) and the row asserts that.\n- `note` — free-form provenance / why this star is in the set.\n\n# Acceptance\n\n- ≥50 rows.\n- ≥4 rows per category.\n- Each `expected_gaia_source_id` traced to a SIMBAD query result (link in `note` or in a sibling `docs/spot-check-provenance.md` if cleaner).\n- Commit message lists which categories landed and how many entries each.\n\n# How to source\n\nSIMBAD's TAP interface (or the per-object web page) gives the Gaia DR3 cross-ID for any named object. Bulk-pull via `astroquery.simbad` or by hand for the small set. For multi-component systems, query each component separately (`* alf Cen A`, `* alf Cen B`).\n\n# Smoke\n\n- `head -5 data/spot_check_ground_truth.tsv` shows the expected schema.\n- Eyeball ~10 rows against the bead's category list — every category present, no obvious typos.\n- The harness in .2 can be a placeholder `tail -n +2 data/spot_check_ground_truth.tsv | wc -l` ≥ 50 as the only assertion for the TSV-only PR.\n\n# Connects\n\nSibling of stellata-9mm.203.2 (harness implementation). Ships independently; the file is dead data until .2 lands, but committing it first lets review of the ground truth happen separately from review of the harness logic.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T13:59:59Z","created_by":"Alex Marshall","updated_at":"2026-05-19T13:59:59Z","labels":["build-binaries","cross-match","research","test-coverage"],"dependencies":[{"issue_id":"stellata-9mm.203.1","depends_on_id":"stellata-9mm.203","type":"parent-child","created_at":"2026-05-19T14:59:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.203","title":"End-to-end build-binaries spot-check harness against curated SIMBAD ground truth","description":"Add a small end-to-end harness that runs `scripts/build-binaries.py` against the committed catalogues and asserts the resolved gaia_source_id for a curated SIMBAD-derived ground-truth set of well-known stars. Unit fixtures pin tier mechanics in isolation; this harness pins WHICH STARS resolve and HOW after every change. Without it the resolution chain can drift in any direction (a parser regression, a new tier accidentally over-firing, an input-data refresh) and only show up at user-visible time.\n\n# Why now\n\nSurfaced in PR #100 review (stellata-dch.28). The \"Low (needs eyeballs)\" call-out on the WDS Notes regex set was the symptom — the parser was hand-rolled prose-matching with no real-data assertions, only synthetic fixtures. Deferring tier 2 to dch.60 removed that specific surface, but the broader gap remains: the entire resolution chain (tier 1 ORB6 → tier 2 AT-HYG → eventually tier 3/4 Gaia astrometry + tier 2 SIMBAD supplement) is tested only against synthetic fixtures. Each new tier landing in dch.29/.30/.32/.60 adds another surface where a regression on a famous star could go unnoticed.\n\nComplementary to `stellata-9mm.200` (post-parse parser sanity nets) and `stellata-9mm.202.5` (per-parser cardinality bounds) — both catch input-shape drift but neither asserts on resolved outputs.\n\n# Categories of stars to cover\n\nGround-truth set should span the regimes the chain has to handle (target ~50-150 systems, no smaller — small samples won't catch drift):\n\n- **Nearby naked-eye doubles** with ORB6 entries (Albireo, Mizar/Alcor, ξ UMa, ε Lyr, γ Leo, …)\n- **Bright Gaia-saturated systems** (Sirius, Procyon, α Cen, Castor, Capella) — should hit dch.30's HIP2 long-baseline fallback once that lands\n- **High-PM stars** (Barnard's, Proxima, Kapteyn's) — tier 4 PM-propagated path, pre-correction\n- **Cluster members** at moderate distance (Pleiades / Hyades named entries, 47 Tuc associates) — dense-field stress test\n- **Distant supergiants** with known DR3 IDs (Deneb, Rigel, Betelgeuse, Antares)\n- **Variables** with WDS entries (Algol, β Lyr, δ Cep) — cross-tier consistency\n- **Multi-component systems** with letter-anchored resolution (η Cas A/B, Castor A/B/C, ν Sco AB/CD)\n- **Obscure-but-resolved** WDS pairs (the kind currently in the wds_notes_hip 10) — confirm they still resolve once dch.60 lands\n\nSource the IDs from SIMBAD's TAP / query interface — for each system, record the canonical Gaia DR3 source_id per component letter.\n\n# Scope (split across two children)\n\nThis is filed as an epic because curation and implementation are independent shippable units (per [[bead-authoring-scope]]'s data-pipeline → runtime seam). The TSV ships once and rarely changes; the harness reads it and is re-run on every Stage 2-7 change.\n\n- **.1 — Curate the ground-truth TSV.** Pull from SIMBAD by canonical name → Gaia DR3 source_id, organise by category, commit to `data/spot_check_ground_truth.tsv` (or similar). Schema: `category, name, wds_id, component, expected_resolve_via, expected_gaia_source_id, note`. Include `expected_resolve_via=\u003ctier name\u003e` so tier drift (e.g. a star moving from tier 1 to tier 4) is detectable too, not just source_id mismatch.\n- **.2 — Implement the harness.** Reads the TSV, runs `resolve_all_pairs` (or the higher-level driver) against the committed catalogues once, asserts each row's `(gaia_source_id, resolve_via)` matches the expected values. Pretty-print failures with category context so a regression is easy to triage. Likely a new `scripts/build-binaries-spotcheck.test.py` (slow / `unittest` skip flag) plus an npm script to run it explicitly.\n\n# Acceptance (parent)\n\n- `data/spot_check_ground_truth.tsv` committed with ≥50 entries across the categories above.\n- `npm run test:spotcheck` (or equivalent) runs the harness against committed catalogues and exits 0 with all expected resolutions matching.\n- Failures print one row per miss with category, expected, actual.\n\n# Smoke\n\nEnd-to-end run takes ~10s (one full build-binaries pass + assertions). Cost is acceptable for occasional pre-merge runs, not every commit. The TSV format makes it easy to add a star later — single-line append + commit.\n\n# Connects\n\n- [[stellata-test-coverage-discipline]] — same shape as the discipline (pin headline numerical claims).\n- Blocks no in-flight work today; should land **after** dch.29 + dch.30 so the harness can pin tier 3/4 resolutions too (otherwise the bright-Gaia-saturated stars stay unresolved in the assertions).","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-19T13:59:34Z","created_by":"Alex Marshall","updated_at":"2026-05-19T13:59:34Z","labels":["build-binaries","cross-match","test-coverage"],"dependencies":[{"issue_id":"stellata-9mm.203","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-19T14:59:34Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.203","depends_on_id":"stellata-dch.29","type":"related","created_at":"2026-05-19T15:00:17Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.203","depends_on_id":"stellata-dch.30","type":"related","created_at":"2026-05-19T15:00:19Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.192.3","title":"DRY: shared DistanceOverride interface + buildDistanceOverride helper across B-J / LMC / future override layers","description":"# Why\n\n`scripts/catalog-pure.ts` ships two structurally identical interfaces and two near-identical apply-functions for the distance-override stack — exactly the \"two implementations differ only in their predicate\" shape that [[stellata-named-constants-and-dry]] (rule 2) says to consolidate at second usage.\n\n```\nexport interface BailerJonesOverride { dist; x; y; z; absmag; }\nexport interface LmcKinematicOverride { dist; x; y; z; absmag; }\n```\n\nBoth `applyBailerJonesOverride` and `applyLmcKinematicOverride` end the same way:\n\n```\nconst { x, y, z } = icrsSphericalToCartesian(raHours, decDegrees, dist);\nreturn { dist, x, y, z, absmag: apparentToAbsoluteMagnitude(mag, dist) };\n```\n\nThe override interfaces are identical, the assembly is identical, only the *predicate* (when to fire + what distance to snap to) differs. CLAUDE.md's \"Code conventions — DRY overrides the system prompt\" explicitly mandates extraction at second usage; we are already past it.\n\n`scripts/build-catalog.ts` also carries the parallel-implementation tax at the call site: both override blocks share the apply-then-mutate-then-increment-counter shape (`if (ovr) { x = ovr.x; y = ovr.y; z = ovr.z; absmag = ovr.absmag; dist = ovr.dist; ...overridden++; }`). This block will be copy-pasted again for every queued override layer unless a driver helper consolidates it alongside the type extraction.\n\n# Why now (P2)\n\nThree more override layers are queued and will copy-paste the same shape unless this lands first:\n\n- `stellata-dch.55` — Phase 3: SMC kinematic distance refinement (mirrors LMC kinematic for the SMC).\n- `stellata-dch.56` — Phase 3: LMC structural-disc fallback distance (where OGLE cross-match misses).\n- `stellata-dch.58` — Phase 3: OGLE Cepheid cross-match for AT-HYG LMC members.\n\nEach is a new `{predicate, distance} → DistanceOverride` pair. Without consolidation, by Phase 3 close-out the catalog will carry 5+ parallel `*Override` interfaces and 5+ parallel `apply*Override` functions that drift independently.\n\nP2 because (a) it sits on the critical path of in-flight Phase-3 work; (b) the refactor pays back immediately at dch.55 (the next override layer); (c) the parallel-implementation count grows mechanically with the override roadmap.\n\n# Scope (one commit/PR)\n\nIn `scripts/catalog-pure.ts`:\n\n1. Replace `BailerJonesOverride` and `LmcKinematicOverride` with one exported `DistanceOverride` interface:\n   ```\n   export interface DistanceOverride { dist: number; x: number; y: number; z: number; absmag: number; }\n   ```\n2. Extract a `buildDistanceOverride(raHours, decDegrees, mag, distPc): DistanceOverride` helper that does the `icrsSphericalToCartesian` + `apparentToAbsoluteMagnitude` composition. Single source of truth for \"given a snapped distance, assemble the override payload.\"\n3. Refactor `applyBailerJonesOverride` to return `DistanceOverride | null` and call `buildDistanceOverride` for assembly.\n4. Refactor `applyLmcKinematicOverride` to return `DistanceOverride | null` and call `buildDistanceOverride` for assembly.\n5. Update the import site in `scripts/build-catalog.ts` (the `ovr.x; ovr.y; ovr.z; ovr.absmag; ovr.dist` reads are unchanged; the union of the two interfaces is the new type).\n\nWhile doing the wiring-side refactor (step 5 / a follow-on step), also confront the duplicate-predicate-state finding from PR #99 review:\n\n6. `build-catalog.ts` computes `angularSeparationDeg(...)` twice per row in the LMC path — once for the `lmcCandidates++` counter (denominator of the override-coverage log line), then again inside `applyLmcKinematicOverride`'s cone check. Per-row × 313k = 313k duplicate calls. Tiny per-call cost (Math.acos + 8 trig calls); structurally, the counter and the gate are duplicating predicate state. Either:\n   - Have the override function return a richer `{ override: DistanceOverride | null; inCone: boolean }` shape so the counter is computed from the same evaluation, OR\n   - Expose a separate `isInLmcCone(ra, dec)` predicate the counter calls, and have the override function reuse it internally.\n   The first option fits the \"DistanceOverride driver\" framing better — the driver can carry a `{ inGate: boolean }` flag the counters consume. Resolve at implementation time; either is acceptable.\n\n# Acceptance\n\n- `npm test` green; specifically the existing fixture suites for both `applyBailerJonesOverride` and `applyLmcKinematicOverride` pass unchanged (no test rewrites needed — the contract is identical).\n- `npm run typecheck` clean.\n- `npm run build:catalog` produces the same `build-catalog-expected.json` snapshot (`bjOverridden`, `lmcOverridden`, `lmcCandidates`, `recordCount` unchanged).\n- `grep -nE \"BailerJonesOverride|LmcKinematicOverride\" scripts/ src/` returns only the export-removal hits, no consumer hits.\n- `angularSeparationDeg` is called at most once per row in the LMC path (verified by a unit test or by code inspection — counter and gate share evaluation).\n\n# Smoke\n\n- `npm test` + `npm run typecheck` + `npm run build:catalog`. No user-visible scene change.\n\n# Connects\n\n- [[stellata-named-constants-and-dry]] — rule 2 (parameterise differences between near-identical implementations).\n- [[stellata-pattern-coverage-across-peers]] — applies sister-layer extension: dch.55/56/58 are the queued peers; ensure each lands using the shared helper.\n- `stellata-dch.55`, `stellata-dch.56`, `stellata-dch.58` — the imminent peers. This bead should land before any of them merge.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:24:42Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:39:55Z","labels":["build-pipeline","catalog","distance","pr-98","pr-99"],"dependencies":[{"issue_id":"stellata-9mm.192.3","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-19T11:24:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.200","title":"build-binaries: post-parse sanity nets for WDS_SUMM / ORB6 fixed-offset parsers","description":"`parse_wds_summ` (`build-binaries.py:207-218`) and `parse_orb6` (`:299-328`) are fully positional fixed-width parsers. If WDS_SUMM or ORB6 shifts a column by even one character (which has historically happened on these catalogues), every numeric field becomes garbage and `safe_float` returns None. The script reports \"loaded N rows\" with no flag that 100% of `P_val` is None or every `theta_last` is garbage.\n\n## Acceptance\n\nFor each parser, after parsing assert that the non-null rate of at least one representative field is within a known band. Example shapes:\n\n- `parse_orb6`: at least 90% of rows must have non-None `P_val` (period is the headline column; nearly every ORB6 entry carries one). If \u003c90%, raise `SystemExit` with a column-offset-drift diagnostic.\n- `parse_wds_summ`: at least 95% of rows must have non-None `precise_ra_deg` / `precise_dec_deg` (the precise-coord block has been mandatory since 2008-era WDS releases). If \u003c95%, raise `SystemExit`.\n\nCalibrate the thresholds against current committed data — empirically these should pass at ~100%.\n\n## Smoke\n\nRe-run `build-binaries` on committed inputs; both sanity nets pass with current data. Then test the failure mode: deliberately shift the offsets in a local copy of one parser, confirm the sanity net raises with the diagnostic message.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:05:13Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:05:13Z","labels":["pr-97"],"dependencies":[{"issue_id":"stellata-9mm.200","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-19T11:05:13Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.199","title":"Refresh scripts: pin spot-check rows for hip-xmatch, tyc-xmatch, bailer-jones","description":"Three refresh scripts have only structural guards (schema + row-count bounds). A future column rename, unit shift, or value drift that preserves row count would pass silently:\n\n- `refresh-gaia-hip-xmatch.py` — only row count [99,400, 99,600]\n- `refresh-gaia-tyc-xmatch.py` — only row count [2,510,000, 2,530,000]\n- `refresh-bailer-jones.py` — row count + 90% coverage floor, no per-row pin\n\n`refresh-hipparcos2` / `refresh-gaia-nss` / `refresh-gaia-apsis` all pin specific rows. The three uncovered scripts are precisely the foundational ones: Bailer-Jones is the distance override for ~300k AT-HYG stars; HIP/Tyc xmatches are how every classical identifier reaches a Gaia source.\n\n## Acceptance\n\nFor each of the 3 scripts:\n\n- Pin 2-3 well-known rows (e.g. a couple of HIP→Gaia pairs documented externally, a Bailer-Jones distance for a famous Gaia source).\n- Spot-check format follows `refresh-gaia-nss`'s existing `check_spot_row` pattern (delta-list, all-mismatches-in-one-failure). This pattern should be lifted to `refresh_lib` per the refactor bead (sibling under 9mm); land that helper first OR file as a follow-up to keep this bead scoped.\n- For Bailer-Jones, at minimum cross-check one source's `r_med_geo` / `r_med_photogeo` against the values in the published paper (Bailer-Jones et al. 2021) so the script asserts agreement with the upstream of record, not just internal self-consistency.\n\n## DR4 forward-compatibility (H8 from the retrospective)\n\nPinned Gaia DR3 source_ids could be retired in a maintenance re-release. Two options:\n\n- Pin 5-6 IDs per script and tolerate 1-2 retirements as soft failures.\n- Resolve pinned IDs through an external anchor (HIP X → look up its Gaia source_id at runtime, then assert pinned values).\n\nRecommended approach: external-anchor for the xmatch scripts; pin-with-tolerance for Bailer-Jones. Document the choice in each script's spot-check comment block.\n\n## Smoke\n\nRe-run each script with `--force`. All three should pass with current pinned values.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:04:57Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:04:57Z","labels":["pr-90","pr-91","pr-92"],"dependencies":[{"issue_id":"stellata-9mm.199","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-19T11:04:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.198","title":"AT-HYG ingest: stop swallowing KeyError, unify '0 = missing' convention, lift read_athyg_source_ids","description":"The AT-HYG CSV is consumed by three scripts (`refresh-bailer-jones`, `refresh-gaia-apsis`, `build-binaries.py:parse_athyg`). Four coupled defects surfaced in the retrospective on PRs 90-97.\n\n**H4 — `parse_athyg` swallows KeyError.** `build-binaries.py:115-123` catches `KeyError` alongside `ValueError`, so an AT-HYG column rename (e.g. `absmag` → `absMag`) silently drops every row. The driver then logs `\"loaded 0 AT-HYG rows\"` with no error. The except clause should not swallow `KeyError`; a missing header column is a fatal misconfiguration, not per-row dirty data.\n\n**H5 — `\"0\"` sentinel handled inconsistently.** AT-HYG uses `\"\"` / `\"0\"` as the missing-sentinel for `hip` / `tyc` / `gaia`. `refresh-bailer-jones` (line 133) and `refresh-gaia-apsis` (line 187) filter on `cell != \"0\"`. `parse_athyg` in `build-binaries` does NOT — `safe_int(\"0\") → 0` and the row gets installed under `hip=0` / `gaia=0` keys. Any downstream `src_to_athyg.get(0)` returns a sentinel-0 star instead of None. Convention must live in ONE place and be applied everywhere.\n\n**D1 — `read_athyg_source_ids` is duplicated.** Verbatim copy between `refresh-bailer-jones` (lines 120-135) and `refresh-gaia-apsis` (lines 172-189). The apsis docstring even says \"Same contract as refresh-bailer-jones.read_athyg_source_ids; identical selection.\" Canonical \"extract at second usage\" case from CLAUDE.md DRY rules.\n\n**D7 — Asymmetric `safe_int` / empty-string handling.** `parse_athyg`'s `tyc` handling uses `(r.get(\"tyc\") or \"\").strip() or None` (correctly converts empty to None) but the int fields don't — `safe_int(\"0\")` returns `0` silently. Symmetric handling required across the row.\n\n## Acceptance\n\n1. Lift `read_athyg_source_ids(path) -\u003e list[int]` to `refresh_lib` with the \"skip empty AND skip '0'\" convention documented in the docstring. Both refresh scripts call it.\n2. `parse_athyg`'s `try/except` no longer catches `KeyError` — a missing required column raises and surfaces immediately.\n3. `parse_athyg` installs hip/gaia indices keyed only by truthy non-zero ids: `if row.hip not in (None, 0)`, same for `gaia`. Document the \"0 = missing\" convention in one CLAUDE.md / docstring location.\n4. Tests: (a) fixture with a renamed AT-HYG column → `parse_athyg` raises (not silently empty); (b) fixture with `hip=\"0\"` and `gaia=\"0\"` → both excluded from indices; (c) `refresh_lib.read_athyg_source_ids` round-trip on a 3-row fixture (one valid, one empty, one `\"0\"`).\n\n## Smoke\n\nRe-run `build-binaries.py` on committed AT-HYG; cardinality of `hip_to_athyg` / `src_to_athyg` unchanged or off by the count of any pre-existing `hip=0` / `gaia=0` sentinel rows (which would be a finding, not a regression).","status":"open","priority":2,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:04:47Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:04:47Z","labels":["pr-92","pr-95","pr-97"],"dependencies":[{"issue_id":"stellata-9mm.198","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-19T11:04:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-dch.58","title":"Phase 3: OGLE Cepheid cross-match for AT-HYG LMC members","description":"Phase 3 refinement on top of stellata-dch.8l0. Replace the flat 49.594 kpc LMC anchor with per-star distances from OGLE-IV's LMC Cepheid catalog where the AT-HYG row cross-matches a Cepheid. Layered AFTER dch.8l0 (which catches the population) and BEFORE dch.56 (structural-disc fallback for non-Cepheid LMC members).\n\n# Goal\n\nFor each AT-HYG LMC supergiant flagged by dch.8l0 (~57 stars post-PM-widen), check if it's an LMC Cepheid in OGLE-IV. If yes, override its distance with OGLE's PL-derived per-star distance modulus (precision ±1-2%, i.e. ±0.5-1 kpc at 50 kpc — far better than dch.56's structural disc ±3 kpc or dch.8l0's flat anchor).\n\n# Coverage estimate\n\nOf the ~57 AT-HYG LMC supergiants:\n- Most are B-supergiants (HDE 268xxx range, non-pulsating) — no OGLE entry.\n- ~5-15 are likely classical Cepheids (long-period, V ~11-12 — brightest end of the LMC Cepheid PL relation overlaps AT-HYG's bright cut). These get OGLE individual distances.\n- Remainder fall back to dch.56's structural disc, or to dch.8l0's flat anchor if neither lands.\n\n# Data ingest\n\n- **Source**: OGLE-IV Magellanic System Variables — LMC Cepheids (Soszyński et al. 2015, AcA 65, 297).\n- **Public download**: http://ogledb.astrouw.edu.pl/~ogle/OCVS/data/lmc/cep/ident.dat (or via VizieR mirror).\n- **Refresh script**: `scripts/refresh-ogle-lmc-cepheids.py` — fetches the catalog, parses to TSV, writes to `data/ogle-lmc-cepheids.tsv` (LFS-tracked since it's ~1-5 MB).\n- **Columns needed**: source ID (OGLE-LMC-CEP-NNNN), RA, Dec, period, mode (FU/FO/multi), V_max, I_max, distance modulus, Gaia DR3 source_id (if available — the OGLE team has cross-matched).\n- **Cross-match key**: Gaia DR3 source_id when present; fall back to (RA, Dec) within 1″ for any rows without Gaia ID.\n\n# Build-catalog.ts integration\n\nOrder in `readStars()`:\n1. B-J override (dist_src gated, dch.47 + dch.47-fix).\n2. dch.8l0 LMC kinematic cone+PM filter (sets a star's distance to a temporary flat anchor + flags it as LMC).\n3. **NEW: OGLE Cepheid cross-match** — if the LMC-flagged star has a matching OGLE-LMC-CEP entry, override flat anchor with OGLE PL distance. Tag dist_src = `OGLE_CEP` or similar.\n4. (Future dch.56) structural-disc fallback for LMC-flagged stars not matched by OGLE.\n5. MAX_DIST_PC cutoff.\n\nPer-OGLE-row distance: `d = 10^((m - M - μ_0)/5 + 1) pc` where μ_0 is the OGLE-published distance modulus, m the observed mag, M the absolute mag. OGLE publishes μ_0 directly per Cepheid — use it; don't re-derive from PL relation.\n\n# DRY note\n\ndch.55 (SMC kinematic) will want the same OGLE cross-match for SMC Cepheids. File a sibling SMC-OGLE bead later; the cross-match helper should be generic (population-parameterised) from this PR. Per CLAUDE.md \"Extract at second usage\" — extract the helper now, parameterise on `OGLE_LMC_CEPHEIDS` vs future `OGLE_SMC_CEPHEIDS`.\n\n# Acceptance\n\n- `scripts/refresh-ogle-lmc-cepheids.py` exists and produces `data/ogle-lmc-cepheids.tsv` deterministically.\n- `scripts/catalog-pure.ts` exports `parseOgleCepheidTsv` and `applyOgleCepheidDistance` (or similar names matching existing B-J pattern).\n- New tests in `catalog-pure.test.ts`:\n  - Cross-match round-trip: a fixture Cepheid with known μ_0 → distance recomputed within 1%.\n  - Non-Cepheid LMC star (no OGLE entry) → null override (falls through to dch.56 / flat anchor).\n  - Cepheid outside LMC (foreground Galactic Cepheid) → null override (gated by LMC flag from dch.8l0, not by sky position alone).\n  - Cross-match priority test: OGLE wins over dch.8l0's flat anchor on the same star.\n- `npm run build:catalog` logs new line: `OGLE Cepheid override: N / M LMC-flagged stars (X%) → dist_src='OGLE_CEP'`.\n- BuildCounts schema gains `ogleCepheidMatched` and `ogleLmcEligible` fields.\n- Manual smoke: a known LMC long-period Cepheid (e.g. HV 12198 / OGLE-LMC-CEP-0451 if it's in AT-HYG) ends up at its OGLE distance, not the flat 49.594 kpc.\n\n# Out of scope\n\n- Non-Cepheid distance refinement (dch.56 structural disc).\n- SMC Cepheid OGLE cross-match (file separately once dch.55 lands).\n- RR Lyrae cross-match — they're ~3 mag fainter than Cepheids, very few would overlap AT-HYG's bright cut.\n- Full LMC stellar-population overlay (filed as separate epic).\n\n# References\n\n- Soszyński et al. 2015, AcA 65, 297 — OGLE-IV LMC Classical Cepheids\n- Pilecki et al. 2018, ApJ 862, 43 — OGLE Cepheid PL relation calibration\n- OGLE Collection of Variable Stars: http://ogle.astrouw.edu.pl/","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T09:40:25Z","created_by":"Alex Marshall","updated_at":"2026-05-19T09:40:25Z","labels":["build-pipeline","catalog","gaia","local-group"],"dependencies":[{"issue_id":"stellata-dch.58","depends_on_id":"stellata-8l0","type":"related","created_at":"2026-05-19T10:41:14Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.58","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-19T10:40:24Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.58","depends_on_id":"stellata-dch.56","type":"related","created_at":"2026-05-19T10:41:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.57","title":"Distance-regression QA: SIMBAD + self-consistency build gate","description":"Post-mortem from the dch.47 dist_src bug (fixed in PR #99 alongside dch.8l0): the bug rendered 11 stars at 16-40 kpc instead of their correct 0.2-1 kpc Hipparcos distances. It went undetected through dch.47's merge because nothing in the pipeline cross-checks our final pipeline distance against an authoritative external source.\n\nAlex flagged this as a high-priority QA gap: \"I'm constantly worried that we are letting bugs like this in\".\n\n# Two regression-detection approaches, file as one bead with two acceptance criteria\n\n# (1) SIMBAD distance-consistency check\n\nWe already ingest `data/simbad_sample.tsv` (committed via dch.26 / commit 7f2c150). For each star in the sample:\n\n- Pipeline final distance (from catalog.bin) vs SIMBAD's recommended distance (from parallax / spectral classification / standard candle).\n- Flag any star where the ratio is \u003e 5× off in either direction.\n- Flag if SIMBAD's object type is incompatible with our absmag (e.g. SIMBAD calls it a star but our absmag implies it's a galaxy, or vice versa).\n\nFor the four spotcheck HIPs that exposed the dch.47 bug:\n- HIP 25097: SIMBAD ~300 pc, pipeline 18.5 kpc → 62× — TRIPS THE CHECK.\n- HIP 23527: SIMBAD ~250 pc, pipeline 36.4 kpc → 146× — TRIPS THE CHECK.\n- HIP 7912:  SIMBAD ~800 pc, pipeline 16.2 kpc → 20× — TRIPS THE CHECK.\n- HIP 3934:  SIMBAD ~700 pc, pipeline 34.2 kpc → 47× — TRIPS THE CHECK.\n\nEven with the small sample (`simbad_sample.tsv` is a sample, not the full SIMBAD), all four would have been flagged.\n\n# (2) Self-consistency check (no external dependency)\n\nFor each star: compare AT-HYG `dist` (the input value, with its `dist_src` tag) against the pipeline final distance:\n\n- Compute `|log10(final / athyg_dist)|`.\n- If the star's dist_src is HIP or GJ AND |ratio| \u003e 0.5 (factor of 3+), flag — Hipparcos / Gliese-Jahreiß distances are reliable for nearby stars; a 3× shift from those sources indicates an override misfire.\n- If the star's dist_src is G_R3 or G_R2 AND |ratio| \u003e 1.5 (factor of 30+), flag — even catastrophic-inversion outliers shouldn't shift by more than 30×.\n\nPrint the top 20 most-extreme distance shifts at the end of the build (sorted by ratio descending), grouped by dist_src.\n\nThis catches the dch.47 bug from first principles, without needing SIMBAD at all.\n\n# Implementation\n\nBoth checks live alongside `assertOrUpdateBuildCounts` in `build-catalog.ts`. They run after the binary is written; produce a structured report; fail the build only on the most extreme outliers (configurable threshold). Outliers below the threshold are logged but don't fail — a star intentionally moved 20% by B-J shouldn't fail the gate.\n\nSame pattern as the existing build-counts assertion: committed `build-distance-outliers-expected.json` (or `.json.gz` for size) tracks the known-acceptable outliers; the build fails if the set changes. Refresh deliberately with `UPDATE_DISTANCE_OUTLIERS=1`.\n\nFor SIMBAD: enable as opt-in (`SIMBAD_CHECK=1 npm run build:catalog`) since `simbad_sample.tsv` is a sample, not exhaustive — but bind to CI so a regression on the sample fails the workflow.\n\n# Acceptance\n\n- New build-time module `scripts/distance-regression-check.ts` exports `runDistanceRegressionCheck(catalog: Star[], simbadSample?: Map\u003c...\u003e): RegressionReport`.\n- Report has structured fields: top-N outliers per dist_src category, SIMBAD mismatches (when enabled), threshold-passing failures.\n- `build-catalog.ts` calls the check at the end of main() and exits non-zero on threshold-passing failures.\n- Tests in `scripts/distance-regression-check.test.ts`: \n  - Synthetic catalog where a HIP-sourced star is shifted 20× → check trips.\n  - Synthetic catalog where a G_R3 star is shifted 5× → check passes (within G_R3 tolerance).\n  - Synthetic catalog with no outliers → empty report.\n  - SIMBAD-disabled run produces only the self-consistency portion.\n- CI workflow (`.github/workflows/test.yml` or equivalent) runs `SIMBAD_CHECK=1 npm run build:catalog` as a separate gate.\n- The committed `build-distance-outliers-expected.json` snapshot ships with PR; bead description includes a one-paragraph explanation of how to refresh.\n\n# Out of scope\n\n- A full SIMBAD batch-query of every AT-HYG star — would require a large data download and live querying. We use the existing committed sample.\n- Comparison against Gaia DR3 separately (would duplicate B-J's role).\n- Spectral-class consistency: AT-HYG vs SIMBAD spectral classes. Useful but a separate check, file separately if useful later.\n\n# References\n\n- `data/simbad_sample.tsv` — committed sample (added in dch.26).\n- `scripts/refresh-simbad-sample.py` — refresh script.\n- The four HIPs flagged in this bead's preamble — use as the canonical regression fixtures.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T09:30:15Z","created_by":"Alex Marshall","updated_at":"2026-05-19T09:30:15Z","labels":["build-pipeline","catalog","test-coverage"],"dependencies":[{"issue_id":"stellata-dch.57","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-19T10:30:14Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.57","depends_on_id":"stellata-dch.47","type":"related","created_at":"2026-05-19T10:30:24Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.55","title":"Phase 3: SMC kinematic distance refinement","description":"Phase 3 sibling of stellata-dch.8l0. After the Bailer-Jones DR3 override (dch.47), AT-HYG carries a small number of SMC supergiants that get pulled to ~28 kpc by B-J's Galactic-density prior tail instead of their true ~62 kpc.\n\n# Exemplar found by dch.8l0 spot-check\n\n| Star     | dist_src | AT-HYG dist | B-J photogeo | sep° from SMC | pm_ra | pm_dec | Δpm vs SMC bulk |\n|----------|----------|-------------|--------------|---------------|-------|--------|-----------------|\n| HIP 5267 | G_R3     | 82 084 pc   | 28 353 pc    | 1.14          | 0.87  | -1.26  | (0.18, 0.03)    |\n\nSky position 1.1° from SMC centre, PM matches SMC bulk to (0.18, 0.03) mas/yr — well inside any reasonable tolerance. After B-J fix it renders at 28 kpc; should be at ~62 kpc.\n\n# Why a separate bead from dch.8l0\n\ndch.8l0 was deliberately scoped to LMC because SMC supergiants are at the bright edge of AT-HYG's brightness cut and the population is much smaller (single-digit candidates vs. ~60 for LMC). Mirrors the dch.8l0 pattern with different parameters.\n\n# SMC parameters (literature)\n\n- **Distance**: SMC mean distance 62.1 ± 1.9 kpc (Graczyk et al. 2014, ApJ 780, 59 — eclipsing binaries; mirrors Pietrzyński's LMC method).\n- **Photometric centre**: RA ≈ 13.16° (0.877 h), Dec ≈ -72.80°.\n- **Bulk PM**: μ_α* cos δ = +0.69, μ_δ = -1.23 mas/yr (Kallivayalil et al. 2013, ApJ 764, 161; van der Marel \u0026 Sahlmann 2016 / 2018 give consistent values).\n- **Cone half-angle**: SMC's optical extent is smaller than LMC's (~3-4° vs ~5-7°). A 10° half-angle should be more than enough while keeping confusion with the SGP / Magellanic Bridge low.\n\n# Approach (mirrors dch.8l0)\n\nIn `scripts/build-catalog.ts` `readStars()`, after the LMC kinematic override and before the `MAX_DIST_PC = 50_000` cutoff:\n\n1. Test sky position: inside SMC cone (centre RA 13.16°, Dec -72.80°, half-angle 10°)?\n2. Test proper motion: `(pm_ra, pm_dec)` within ±0.5 mas/yr of `(+0.69, -1.23)`?\n3. If both pass, override the distance to 62 100 pc. Recompute x0/y0/z0 from (ra, dec, distance_override). Recompute absmag.\n4. Tag the row's dist_src as `SMC_KIN`, joining the enum `{HIP, GJ, T, N, G_R3, BJ, LMC_KIN, SMC_KIN}`.\n\nParameters live as named exports in `catalog-pure.ts` alongside LMC_*:\n- `SMC_DISTANCE_PC = 62_100`\n- `SMC_CENTRE_RA_HOURS = 0.877`\n- `SMC_CENTRE_DEC_DEG = -72.80`\n- `SMC_CONE_HALF_ANGLE_DEG = 10`\n- `SMC_PM_RA_CENTRE = 0.69`, `SMC_PM_DEC_CENTRE = -1.23`\n- `SMC_PM_TOLERANCE = 0.5`\n\n# DRY consideration\n\nLMC and SMC overrides share the same shape — same checks, different parameters. Per CLAUDE.md \"Extract at second usage\" rule: at the second call site (this bead), factor out `applyKinematicOverride(params, ra, dec, mag, pmRa, pmDec)` taking a `KinematicPopulation` struct (centre RA/Dec/PM + cone half-angle + PM tolerance + target distance + dist_src tag), with LMC and SMC as two `KinematicPopulation` constants. The bead's PR includes this refactor as a topical commit.\n\n# MAX_DIST_PC = 50_000 cutoff\n\nThe cutoff was set at 50 kpc for LMC scope. SMC at 62 kpc would be dropped by the current cutoff. Bump to `60_000` would still drop SMC. Bump to `70_000` to admit SMC plus a safety margin for Magellanic Stream stars (if any later land in scope).\n\nDocument the new scope: \"bounded-scope final gate after refinement layers; admits LMC + SMC modelled populations.\" Update lives in dch.42 (SCIENCE.md rewrite) per the parent epic.\n\n# Acceptance\n\n- `npm run build:catalog` runs cleanly.\n- New tests in `catalog-pure.test.ts` exercising the SMC override:\n  - SMC-direction + SMC-PM star → distance overridden to 62 100 pc.\n  - SMC-direction + NON-SMC-PM star → unchanged.\n  - NON-SMC-direction + SMC-like PM star → unchanged.\n- HIP 5267 ends up at 62 100 pc post-build.\n- LMC override remains unchanged (no regression — 54 LMC stars still caught).\n- Ordering: SMC override runs after LMC override; if a star somehow passes both (impossible given disjoint cones), document the precedence.\n- `DIST_SRC_SMC_KIN = 'SMC_KIN'` exported and distinct from BJ / LMC_KIN / AT-HYG namespace.\n- Refactor: `applyKinematicOverride(params, ...)` accepts a `KinematicPopulation` struct; LMC + SMC reduce to constants in the same module.\n\n# Visual smoke\n\nIn the SMC sky direction (RA 0-2 h, Dec -70 to -76°), confirm any \"smear\" of stars stretched between 5-100 kpc converges to a concentrated 62 kpc shell.\n\n# References\n\n- Graczyk et al. 2014, ApJ 780, 59 — SMC eclipsing-binary distance\n- Kallivayalil et al. 2013, ApJ 764, 161 — SMC bulk PM\n- van der Marel \u0026 Sahlmann 2016 — SMC PM internal kinematics\n\n# Out of scope\n\n- LMC Dec PM widening (filed as a separate bead).\n- LMC structural-disc per-star distance (filed as a separate bead).\n- Magellanic Bridge / SMC Stream stars — most are below AT-HYG's brightness cut.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T09:28:58Z","created_by":"Alex Marshall","updated_at":"2026-05-19T09:28:58Z","labels":["build-pipeline","catalog","gaia","local-group"],"dependencies":[{"issue_id":"stellata-dch.55","depends_on_id":"stellata-8l0","type":"related","created_at":"2026-05-19T10:30:22Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.55","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-19T10:28:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.54","title":"dch.8l0 follow-up: widen LMC Dec PM tolerance","description":"Phase 3 follow-up to stellata-dch.8l0 (PR #99). Spot-check on the LMC kinematic override surfaced three real LMC supergiants whose pm_dec values sit just over the +0.70 cutoff and are therefore not caught by the override:\n\n| Star      | dist_src | AT-HYG dist | B-J photogeo | sep° from LMC | pm_ra | pm_dec | Δpm_dec |\n|-----------|----------|-------------|--------------|---------------|-------|--------|---------|\n| HIP 27655 | G_R3     | 85 470 pc   | 15 065 pc    | 3.33          | 2.09  | 0.78   | 0.57    |\n| HD 269953 | G_R3     | 24 893 pc   | 16 974 pc    | 2.26          | 1.89  | 0.72   | 0.52    |\n| HD 270100 | G_R3     | 62 206 pc   | 22 040 pc    | 3.23          | 1.57  | 0.74   | 0.54    |\n\nAll three have tight pm_ra (Δ ≤ 0.28), distances consistent with LMC supergiants whose B-J posterior pulled them in to ~15-22 kpc (B-J's prior has no LMC), and HDE/HD numbers in the LMC catalog range. They are almost certainly LMC members rendered as a residual smear at 15-22 kpc.\n\n# Root cause\n\nThe dch.8l0 PM gate is symmetric ±0.5 mas/yr around (+1.85, +0.20). The actual LMC Dec PM distribution per van der Marel \u0026 Kallivayalil 2014 + per-source DR3 scatter clusters higher than +0.20 — empirically a chunk of the population sits at pm_dec ∈ [+0.6, +0.8]. The +0.20 centre is dominated by the literature centre-of-mass value; the spread is genuine LMC internal kinematics + disc rotation + per-source noise.\n\n# Approach (options)\n\n1. **Widen Dec tolerance to ±0.6 mas/yr** (asymmetric on the LMC params not yet supported). Catches the three outliers. Risk of admitting halo runaways scaling with the tolerance.\n2. **Shift Dec centre to +0.25 mas/yr with ±0.5 tolerance**. Same window width, recentred. Worth checking the actual empirical centre on the 54 currently-flagged LMC stars before committing.\n3. **Make PM gate asymmetric / per-component** (allow `pm_dec ∈ [-0.30, +0.85]` directly). Most flexible, ugliest API.\n\nLean: option 2 (shift centre) — empirically validate the centre against the 54 stars already caught, then expand tolerance only if needed.\n\nEmpirical check (recommended before committing): query the catalog for the 54 LMC_KIN stars after rebuild, compute mean pm_dec across them, compare against the literature +0.20. If empirical mean is ~+0.4, shift centre and keep ±0.5.\n\n# Acceptance\n\n- All three outlier stars (HIP 27655, HD 269953, HD 270100) end up at 49 594 pc post-build.\n- The 54 currently-caught LMC stars remain caught (no regression).\n- The halo controls in catalog-pure.test.ts (in-cone but not LMC, out-of-cone with LMC-like PM) remain rejected.\n- Empirical mean pm_dec across the post-fix LMC_KIN population is documented in the commit message or bead notes.\n\n# Out of scope\n\n- HIP 28618 (in cone, dist 20 kpc, pm_dec +4.7): inconclusive, possibly Magellanic Cloud hypervelocity escapee or DR2 mismeasurement. Not addressable by widening tolerance — would need fresh Gaia DR3 / DR4 astrometry. Leave alone.\n- SMC kinematic layer (filed separately).\n- LMC structural disc per-star distance refinement (filed separately).\n\n# References\n\n- van der Marel \u0026 Kallivayalil 2014, ApJ 781, 121 — LMC bulk PM and dispersion\n- Pietrzyński et al. 2019, Nature 567, 200 — distance anchor","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T09:28:16Z","created_by":"Alex Marshall","updated_at":"2026-05-19T09:28:16Z","labels":["build-pipeline","catalog","gaia","local-group"],"dependencies":[{"issue_id":"stellata-dch.54","depends_on_id":"stellata-8l0","type":"related","created_at":"2026-05-19T10:30:21Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.54","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-19T10:28:16Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ty4.1","title":"Bayestar19 3D voxel supplement to Edenhofer in per-star dust raymarch","description":"**Goal:** add Bayestar19 (Green / Schlafly et al. 2019, ApJ 887:93) as a 3D dust-density supplement to the Edenhofer voxel cube in the per-star raymarch. Fills the high-fidelity-3D shell between the 1.25 kpc Edenhofer cube and the analytical slab fallback (stellata-74e). Spec context in parent **stellata-ty4**.\n\n# Why\n\nEdenhofer's 1.25 kpc cube is the current high-fidelity 3D source. Bayestar19 reaches out to ~4-5 kpc with useful precision (HEALPix nside=1024 ≈ 3.4 arcmin pixels, 120 log-spaced distance bins, ~3/4 sky north of Dec ≈ -30°). Inside that shell, Bayestar gives measured 3D extinction that captures large-scale structure (including arm overdensities as a side effect — see the Open question in stellata-ty4) instead of the smooth analytical slab.\n\n# Data + pipeline\n\n- **Source:** `dustmaps` Python package (Green 2018, JOSS). Same access shape as the Edenhofer pipeline already in `scripts/build-dust.py`.\n- **Frozen-data convention:** download once, commit Bayestar19 chunks under `data/dust/bayestar19/`. The `dustmaps` package supports caching a local copy; bake from cache at build time.\n- **Build script:** extend `scripts/build-dust.py` to emit a second chunked 3D texture for the Bayestar volume, with its own manifest entry alongside Edenhofer. Reuse the chunking machinery (LFS-friendly 2 MiB chunks already supports a larger AABB).\n- **Shader:** `star.vert.glsl` raymarches camera→star. Two texture samples per step (Edenhofer if inside its AABB else Bayestar if inside its AABB else zero), accumulate. Per-vertex AABB tests gate which texture is read.\n\n# Voxelisation choice\n\nBayestar's native projection is angular (HEALPix) × log-distance, not Cartesian. Resampling onto a regular Cartesian grid for shader sampling requires a resolution choice. Probably 10-25 pc voxels so the 8 kpc cube fits in a GPU-budget-sane texture (~512³ × 1B = ~128 MiB at 16 pc, comparable to the existing Edenhofer texture). Document the choice in `data/dust/manifest.json`.\n\n# Coverage gap — southern sky\n\nBayestar has no coverage below Dec ≈ -30°. Options:\n- Leave the southern shell as analytical-slab-only (74e fallback). Simplest; acceptable for v1.\n- Use a different southern-sky 3D map (Lallement+2022 explores both hemispheres at ~3 kpc) as a follow-up bead if/when the gap matters visually.\n\n# Seam handling at the Edenhofer cube boundary\n\nInside Edenhofer cube → Edenhofer voxels. Outside Edenhofer but inside Bayestar AABB → Bayestar voxels. The two maps should agree on absolute A_V at their shared boundary — needs calibration:\n- Compute mean A_V across each face of the Edenhofer cube in both Edenhofer and Bayestar.\n- Shift Bayestar by the offset so boundary values match (analogous to mosaic photometric zero-point pinning).\n- Sanity: a sightline that slowly crosses the boundary should show no extinction discontinuity.\n\n# Acceptance\n\n- A northern-sky star at 2 kpc shows reddened/dimmed colour consistent with its Bayestar A_V (verified against a published Bayestar sample, e.g. `dustmaps.bayestar.BayestarQuery.query()` against the same RA/Dec/distance).\n- Slow sightline traverse across the Edenhofer cube boundary has no visible extinction discontinuity in renderer.\n- No regression in near-Sol fidelity (Edenhofer values inside the cube unchanged).\n- Vertex-shader texture-read count documented in PR; if it doubles in the worst case, profile against the 313k-star draw and decide if chunk-skip optimisation is needed.\n\n# Files\n\n- `scripts/build-dust.py` — extend with Bayestar chunk pipeline.\n- `data/dust/bayestar19/*` — committed via Git LFS.\n- `data/dust/manifest.json` — Bayestar manifest entry alongside Edenhofer.\n- `src/client/shaders/star.vert.glsl` — second texture sample in `dustExtinctionAV`.\n- `src/client/loaders/dust-loader.ts` — load Bayestar chunks alongside Edenhofer.\n- `SCIENCE.md` — add Bayestar19 to the data-source citations.\n\n# Related\n\n- **stellata-74e** — analytical slab fallback for sightlines outside both 3D maps.\n- **stellata-ju3** — arm-overdensity contributions; overlap discussed in parent ty4 (Bayestar measures arm structure within its volume as a side effect).\n- **stellata-ju3.1** — already evaluates Bayestar as a candidate for arm dust; that evaluation should settle the overlap question for both beads.\n\n# Out of scope\n\n- Southern-sky 3D coverage (Lallement+2022) — separate follow-up bead.\n- Arm-overdensity modelling outside Bayestar's volume (kept in ju3).","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-18T11:55:33Z","created_by":"Alex Marshall","updated_at":"2026-05-18T11:55:33Z","labels":["build-pipeline","extinction","ism","shader"],"dependencies":[{"issue_id":"stellata-ty4.1","depends_on_id":"stellata-ju3.1","type":"related","created_at":"2026-05-18T12:56:50Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-ty4.1","depends_on_id":"stellata-ty4","type":"parent-child","created_at":"2026-05-18T12:55:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ty4","title":"Dust modelling — multi-source extinction stack (Edenhofer / Bayestar / slab / arms)","description":"**Goal:** treat dust extinction as a multi-source layered stack rather than the current single-source implementation. `star.vert.glsl` raymarches one texture (Edenhofer); anything beyond the 1.25 kpc cube is unattenuated by default. As stellata expands toward extragalactic scales (stellata-0hx, stellata-36y), this gap becomes visible.\n\n# The stack — lowest fidelity → highest\n\nApplied along the camera→star sightline; later layers override earlier ones inside their AABB.\n\n1. **Smooth analytical disc slab** (everywhere). ρ(R, z) = exp(-(R-R₀)/L_R) × exp(-|z|/h_z), Galactic frame, h_z ≈ 125 pc. The `analyticalDustDensity` chunk already in `milkyway.frag.glsl` is the canonical source — lift into a shared dust include. Tracked by **stellata-74e**.\n2. **Bayestar19 3D voxel shell** (≤ ~4-5 kpc, northern sky only). Pan-STARRS + 2MASS + Gaia DR2 stellar photometry → 3D dust (Green+2019, ApJ 887:93). Distributed via the `dustmaps` Python package. Fills the shell between the Edenhofer cube and the analytical slab. **NEW bead.**\n3. **Edenhofer voxel cube** (≤ 1.25 kpc, all-sky around Sol). Current star.vert.glsl source. Unchanged.\n4. **Arm-overdensity contributions** modulating the above. Tracked by **stellata-ju3** + children.\n\n# Why now\n\nTriggered 2026-05-18 by an exploratory Q: \"would the apparent magnitude of a MW star be correct viewed from M31?\" The distance modulus is correct (camera-to-star, star.vert.glsl line 217) but the dust raymarch zeros out beyond the 1.25 kpc cube — so an intergalactic-vantage sightline through the MW disc is physically under-attenuated by orders of magnitude. The multi-layer stack closes the gap.\n\n# Seam handling (TBD per child)\n\nThe cube boundary needs Edenhofer and Bayestar (and the slab) to read continuously — no jump in extinction as a sightline crosses an AABB. Options to evaluate per-bead:\n- Hard switch at AABB boundary; calibrate normalisations so boundary values agree.\n- Smooth crossfade across a thin shell at the AABB.\n- Higher-fidelity source per sample with normalisation pinned at boundary voxels.\n\n# Open question — Bayestar shell vs arm overdensity baking\n\n`stellata-ju3.1` evaluates Bayestar19 as a candidate data source for *arm overdensities*, alongside Drimmel \u0026 Spergel 2001, Marshall 2006, Lallement 2022. The new Bayestar near-shell bead uses Bayestar as a *general* 3D extinction supplement to Edenhofer — which already captures arm overdensities within its volume as a side effect of being a measured (not modelled) dust map. ju3's scoping should settle whether:\n- Bayestar near-shell subsumes the near-Sol portion of ju3's arm work, leaving ju3 to model arms outside Bayestar's volume; or\n- The two are independent and stack (Bayestar supplies 3D fidelity, ju3 supplies analytic arm-overdensity correction beyond data coverage).\n\n# Children\n\n- **stellata-74e** — analytical slab fallback (already filed; now positioned as one layer of the stack)\n- **stellata-ju3** — galactic arm dust (sub-epic; ju3.1 + ju3.2 unchanged)\n- **NEW** — Bayestar19 3D voxel supplement to Edenhofer\n- **NEW** — Extragalactic-vantage extinction for non-stellar layers (P3)\n\n# Out of scope\n\n- Replacing Edenhofer near-Sol. The high-fidelity local data stays the source of truth inside its cube.\n- Cosmological-distance reddening (z-dependent dust attenuation across the observable universe). Negligible compared to MW slab even at Mpc scales.\n- Per-object visualisation overlays showing dust-layer boundaries — file under stellata-io3 if wanted.\n- Wavelength-selective extinction physics refinements per molecular cloud — tracked by stellata-c7u.3 under the MC epic, not here.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-18T11:54:56Z","created_by":"Alex Marshall","updated_at":"2026-05-18T11:54:56Z","labels":["extinction","ism"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-gxx.1","title":"Local Group luminosity — design gate + LVDB completeness survey","description":"# Scope\n\nDesign gate for the [[stellata-gxx]] epic. Outputs a written spec\n(new section in docs/local-group.md) and a per-object data plan.\nNo code lands in this bead other than the docs.\n\n# Deliverables\n\n1. **LVDB completeness survey.** For every object in `local-group.json`\n   today (LMC, SMC, Sagittarius, M32, NGC 205, M31, M33 standalones +\n   all LVDB dwarfs), tabulate which of these fields are populated and\n   to what precision:\n   - `apparent_magnitude_v`, `M_V`\n   - `rad_sersic`, `n_sersic`\n   - `surface_brightness_rhalf`\n   - `rhalf_physical` (parsec half-light radius)\n   - `ellipticity`, `position_angle`\n   Identify rows with missing photometry or missing Sérsic params and\n   pick a fallback (King profile, half-light-radius spheroid with\n   default n=1, or hand-curated override row).\n\n2. **Profile-family assignment per object class.**\n   - Sérsic spheroid for dSphs, M32, NGC 205, Sagittarius dSph.\n   - Exponential thin disc + Sérsic bulge for M31, M33.\n   - LMC: decide whether disc or spheroid is more honest at LG-render\n     scale (current 4500/4500/1000 pc inclined-disc shell suggests disc;\n     interior bar+arms not modelled — see epic out-of-scope note).\n   - SMC: spheroid with extreme line-of-sight elongation (current\n     3730/4960/6000 pc shell — keep matched profile).\n\n3. **Calibration scheme spec.** Derive the per-object DENSITY0 solver:\n   given catalog `apparent_magnitude_v`, distance, and profile geometry\n   (R_eff, n, axis ratios), what offline integral solves for the\n   density normalisation so that a fragment raymarch through the proxy\n   mesh integrates to the right total flux? Mirror milkyway's approach\n   (docs/milky-way.md § Magnitude consistency with stars) — the\n   conversion path is the same; only the integration over the profile\n   shape differs per family.\n\n4. **Camera-inside-vs-outside decision.** milkyway's shader handles\n   \"camera inside the proxy mesh\" (we live in the disc) — LG objects\n   are normally viewed from outside, but the user CAN warp into LMC /\n   SMC / M31. Decide the split: ray entry = camera position when\n   inside, front-face intersection when outside. Same shape as\n   milkyway's logic; document the parallel.\n\n5. **Dust handling.** None — LG objects are well outside the MW dust\n   slab, and their internal dust is below the photometric tolerance.\n   Confirm and write this into the spec so the shader doesn't carry\n   needless dust uniforms.\n\n6. **Per-object data plan.** Concrete list of what fields need to be\n   plumbed through the build pipeline for each object. Drives the\n   pipeline bead (next child).\n\n# Acceptance\n\ndocs/local-group.md gains a \"Physical luminosity model\" section with\nall six items above resolved. PR review-ready (no TODOs, no\n\"decide later\" gaps).\n\n# Smoke\n\nDocs read review by Alex.\n\n# Connects\n\nParent: [[stellata-gxx]]. Blocks the pipeline + shader beads.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T09:31:43Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:31:43Z","labels":["local-group","physics","research","shader"],"dependencies":[{"issue_id":"stellata-gxx.1","depends_on_id":"stellata-gxx","type":"parent-child","created_at":"2026-05-18T10:31:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-gxx","title":"Local Group physical luminosity — integrated apparent V-mag from any camera position","description":"# Problem\n\nThe Local Group layer (`src/client/local-group/local-group.ts`) renders\nstructural wireframes (LMC inclined disc, SMC line-of-sight elongation,\ndSph projected ellipses, M31/M33 tilted discs, etc.) but emits NO light.\nEach object's apparent V-magnitude is implicitly +inf from every camera\nposition. From any viewpoint near the Local Group this is unphysical:\nM31 (m_V ≈ 3.4) should read as a bright extended glow from inside the\nMW, the LMC (m_V ≈ 0.9) should dominate parts of the southern sky, and\nthe dSph host's brightness should rise as the camera approaches.\n\nThis is a tier-2 \"physical reality fix\" per [[stellata-prioritization-framework]].\n\n# Scope\n\nPer LG object, render an analytically-modelled luminous volume such\nthat the integrated apparent V-magnitude observed from the camera\nmatches the catalog measurement for that object, from ANY camera\nposition inside or outside the Local Group.\n\nParallel to the existing volumetric Milky Way (`milkyway.ts` +\nshaders/milkyway.{vert,frag}.glsl, see docs/milky-way.md):\nraymarched proxy mesh + analytical density profile per fragment +\nemission converted to apparent V-mag via the same magnitude-\nconsistency conversion the star renderer + MW already use. The LG\nvariant extends this to N per-object instances with two profile\nfamilies:\n\n- **Sérsic spheroid** — for dSphs, compact ellipticals (M32, NGC 205),\n  and bulges. LVDB rows already carry `rad_sersic` (arcmin),\n  `n_sersic`, `surface_brightness_rhalf`. Where Sérsic params are\n  missing, fall back to a King / half-light-radius spheroid using the\n  fields already present.\n- **Exponential thin disc + Sérsic bulge** — for M31, M33, and\n  potentially LMC. Disc scale length sourced from papers (DOIs in\n  overrides.tsv per existing override schema). M31 R_d ≈ 5.3 kpc,\n  M33 R_d ≈ 1.8 kpc.\n\nThe structural wireframe layer continues to render as a low-opacity\noverlay defining each object's silhouette. The volumetric pass adds\nemission inside that silhouette. Both share the per-object\n`LocalGroupObject` record so structural edits propagate to both.\n\n# Calibration target\n\nEach fragment converts analytical emission → effective apparent V-mag\nusing the same path as `milkyway.frag.glsl`'s magnitude-consistency\nblock (see docs/milky-way.md § Magnitude consistency with stars).\nPer-object DENSITY0 is solved offline (in the build pipeline) so that\nthe analytical surface-brightness integral over the proxy mesh equals\nthe catalog `apparent_magnitude_v` at the object's known distance.\nTolerance: ±0.1 mag from any camera viewpoint.\n\n# Children (sequential — each is one commit/PR + smoke step)\n\n1. **Design gate (P2)** — LVDB completeness survey per object,\n   profile-family assignment, calibration scheme spec, dust handling\n   (none — LG objects are well outside the MW dust slab), camera-\n   inside-vs-outside split decision. Output: docs/local-group.md\n   physical-luminosity section + per-object data plan.\n2. **Data pipeline extension** — `scripts/build-local-group.ts` +\n   `overrides.tsv` surface V-mag + profile-type + profile-params into\n   `local-group.json`. Add M31/M33 photometry + structural details\n   (Sérsic bulge, exponential disc scale length, paper DOIs) to\n   overrides. Offline DENSITY0 solver runs at build time.\n3. **Sérsic-spheroid shader** — `local-group-emission.{vert,frag}.glsl`\n   raymarched per-instance Sérsic profile (per-instance density0,\n   n_sersic, R_eff, axis ratios). LMC/SMC/M32/NGC 205/Sgr + all dSphs.\n4. **Exponential-disc shader (M31/M33)** — disc + Sérsic-bulge\n   composite per-instance. Likely shares the spheroid pass with the\n   bulge component layered on the disc proxy mesh.\n5. **Calibration test** — automated vitest places synthetic camera at\n   N viewpoints (deep MW interior, at Sun, near LMC, between MW and\n   M31, beyond M33), samples rendered flux per object silhouette,\n   converts back to apparent V-mag, asserts ≤ 0.1 mag deviation.\n   Pinned numbers per [[stellata-test-coverage-discipline]] rule (b).\n6. **Debug panel + URL flag + chart-mode** — `local-group-tuning.ts`\n   section, `lge=0` to disable, chart-mode handling (likely hide the\n   volumetric layer in chart mode, mirroring milkyway's chart-mode\n   isobar swap).\n\nChildren 3 and 4 may execute in parallel after the design gate +\npipeline land. Per [[bead-authoring-scope]] they stay split because\nthe shaders are distinct sub-systems even when they share a parent.\n\n# Out of scope\n\n- Per-star resolution of LG objects (statistical/aggregate regime per\n  [[stellata-cds-data-sources-and-detail-gradient]]).\n- Internal structure (spiral arms in M31, bar+arms in LMC). Smooth\n  axisymmetric profile is the right detail tier — same call milkyway\n  made when shelving spiral-arm overdensities.\n- Variable / time-dependent emission.\n- Colour decomposition by stellar population (single per-object\n  population colour, configurable via overrides if needed).\n- Dust extinction internal to LG objects. MW dust slab does not\n  reach them; their own dust is below the photometric tolerance.\n\n# Connects\n\n- [[stellata-data-fidelity-principle]] — solve the class of problem;\n  no hand-tuned per-object calibration outside the LVDB-derived path.\n- [[stellata-cds-data-sources-and-detail-gradient]] — integrated\n  brightness is the right detail tier for far-field objects.\n- [[stellata-defer-detail-until-zoom-affordance]] — integrated profile,\n  not per-star.\n- [[frozen-external-data]] — all photometry lives in committed\n  data/local-group/ files.\n- [[stellata-folder-and-controller-conventions]] rule 1 — new files\n  land in src/client/local-group/.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-18T09:31:15Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:31:15Z","labels":["local-group","physics","shader"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.49","title":"Phase 3 — Surface Gaia DR3 Apsis (Teff/logg/[M/H]/A0) per record in catalog binary","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture). Phase 3 sibling to `stellata-dch.48` (Phase 1 Apsis ingest). Parallels the `dch.46` (B-J ingest) + `dch.47` (B-J distance override) split.\n\nBrings Gaia DR3 Apsis astrophysical parameters (Teff / logg / [M/H] / A0 from gspphot ∪ gspspec) into the catalog binary so the runtime shader path can re-key onto Apsis-direct values. Coverage uplift validated empirically by `stellata-zsr.1`: (Teff, logg) goes from 29.3% (status-quo spectral-class parsing) to 84.8% with Apsis — a 2.87× gain.\n\n# Scope\n\nSurface Apsis fields per record in the catalog binary, keyed by the v6 `gaia_source_id` field that lands in `stellata-dch.33` (Bead M). Apsis coverage is ~85%, so the layout decision is non-trivial.\n\n# Open design question (resolve at start of execution)\n\nInline vs sparse side-table.\n\n- **Inline**: 7 floats (teff_gspphot, logg_gspphot, mh_gspphot, azero_gspphot, teff_gspspec, logg_gspspec, mh_gspspec) × 4 bytes × 313k = ~10 MB delta. Simpler; O(1) per-record access; null sentinels for the 15% gap.\n- **Side-table**: keyed on gaia_source_id; ~85% × ~10 MB = ~8.5 MB delta (saves ~1.5 MB). Costs a second binary section + a hashmap lookup at decode.\n\nLikely choice: **inline** — 1.5 MB savings doesn't pay for the complexity given catalog.bin is already ~13 MB. May trigger a v6→v7 bump or fold into a single v6 bump if dch.33 hasn't shipped yet (decide based on whether dch.33 is in flight or already merged when this bead starts).\n\n# Deliverables\n\n- **`scripts/build-catalog.ts`** — load `data/gaia_dr3_apsis.tsv`, index by `source_id`, attach Apsis fields to each v6/v7 record keyed off `gaia_source_id`. Null-row handling for the 15% gap.\n- **`scripts/catalog-pure.ts`** — packer / unpacker updates for the new fields. Field offsets + record stride constants bumped. Pure helpers (TSV parser, source_id → ApsisRow indexer) lift to `*-pure.ts` siblings per [[stellata-test-coverage-discipline]].\n- **`scripts/catalog-pure.test.ts`** — field-offset + null-sentinel + round-trip regression tests. Byte-signature pin on a fixture record.\n- **`scripts/verify-catalog.ts`** — recognise the new format version; dump Apsis fields in record-inspection output.\n- **`src/client/catalog-loader.ts`** — decode the new fields; expose `teff_apsis` / `logg_apsis` / `mh_apsis` / `azero_apsis` / `teff_gspspec` / `logg_gspspec` / `mh_gspspec` on the Catalog struct.\n\n# Downstream re-routings (NOT in this bead's scope — each filed separately)\n\n- **Tier 2 colour-LUT re-routing (`stellata-zsr.4`)** re-keys Teff source for the blackbody → sRGB LUT (shipped via `stellata-zsr.3` / PR #89) from Ballesteros(B-V) → Apsis-direct where available (Apsis preferred; Ballesteros fallback; spectral-class fallback; WD-subclass fallback; solar fallback). Folds in the per-WD Teff routing that PR #89 explicitly deferred (Sirius B / Procyon B / 40 Eri B) because Apsis carries Teff for WDs natively.\n- **Phase 5 Bead T (mass-ratio, `stellata-dch.40`)** gains Apsis-direct logg for giants / subgiants — improves the mass estimate for the giant/subgiant tier of the mass-ratio fallback table.\n- **Phase 5 Bead U (eclipse photometry, `stellata-dch.41`)** gains direct Teff for limb-darkening coefficient selection.\n- **Stefan-Boltzmann radius refinement** using direct logg — filed separately when desired.\n- **azero_gspphot cross-check against Edenhofer dust map** — filed separately when desired.\n\n# Acceptance\n\n- ≥80% of records have a non-null `teff_apsis` OR `teff_gspspec` (matches the 84.8% empirical-probe figure from `stellata-zsr.1` within sampling tolerance).\n- Spot-check Sirius / α Cen A/B / Vega Apsis values match `data/gaia_dr3_apsis.tsv` byte-for-byte after round-trip through pack → unpack.\n- `npx tsx scripts/verify-catalog.ts` reports the new format version and dumps Apsis fields per sample record.\n- typecheck + vitest green.\n\n# Smoke\n\n- `npm run build:catalog` regenerates `public/catalog.bin` (~16 + 10 = ~26 MB).\n- `npx tsx scripts/verify-catalog.ts` shows the new Apsis fields.\n- Manual: load the app and verify the colour LUT path doesn't regress at the hot / mid / cool extremes (the actual user-visible chromaticity shift lands when `stellata-zsr.4` consumes these fields; this bead alone only surfaces the data — no rendered change yet).\n\n# Confidence (per `alex-large-pr-honesty` at PR-open time)\n\n- **High**: deterministic build script + field-offset regression tests + byte-signature fixture pin.\n- **Medium**: client decoder integration (uniform feeds unchanged; routing logic adjacent to today's code).\n- **Low (needs eyeballs)**: actual catalog.bin size delta + null-sentinel behaviour for the 15% gap. Manual smoke step — Alex inspects the size + spot-checks a few faint stars without Apsis to confirm graceful fallback.\n\n# Origin\n\nFiled alongside `stellata-dch.48` (Phase 1 Apsis ingest) when the epic was locked. Mirrors the `dch.46` + `dch.47` split for Bailer-Jones: Phase 1 produces the TSV, Phase 3 surfaces it into the binary build.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T09:27:56Z","created_by":"Alex Marshall","updated_at":"2026-05-18T19:41:51Z","labels":["apsis","binary-format","gaia","phase-3","physics"],"dependencies":[{"issue_id":"stellata-dch.49","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-18T10:27:55Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.49","depends_on_id":"stellata-dch.33","type":"blocks","created_at":"2026-05-18T10:28:01Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.49","depends_on_id":"stellata-dch.48","type":"blocks","created_at":"2026-05-18T10:28:00Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.48","title":"Phase 1 — scripts/refresh-gaia-apsis.py + commit data/gaia_dr3_apsis.tsv","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture). Phase 1 sibling to dch Beads B/C/D/E (HIP xmatch, Tycho xmatch, HIP2 reduction, NSS orbits).\n\nBrings Gaia DR3 spectroscopic stellar parameters (Apsis) into Stellata's per-star data. Empirical coverage probe in `stellata-zsr.1` showed (Teff, logg) goes from 29.3% (status quo via spectral-class parsing) to 84.8% with gspphot ∪ gspspec — a 2.87× improvement.\n\nFull research and rationale: `research/star-spectral-rendition/RECOMMENDATION.md` § Tier 2 + § Coordination with stellata-dch. **Read it before starting.**\n\n# Scope\n\nRefresh script + committed TSV for Gaia DR3 Apsis astrophysical parameters per source_id. Depends on `scripts/refresh_lib.py` (stellata-dch.21).\n\nSurfacing into the binary catalog is a SEPARATE follow-on — owned by `stellata-dch.49` (Bead CC), filed alongside this bead as the Phase 3 surfacing sibling (parallels the `dch.46` ingest + `dch.47` surface pattern for Bailer-Jones). This bead is TSV-only first per the lighter-integration path in RECOMMENDATION.md.\n\n# Deliverables\n\n- **`scripts/refresh-gaia-apsis.py`** (new) — ADQL pull from `gaiadr3.astrophysical_parameters`. Idempotent (skip if output newer than script). Documents the full ADQL query in module docstring.\n- **`data/gaia_dr3_apsis.tsv`** (new, LFS-tracked — ~15-25 MB for ~266k matched rows) — columns: `source_id` (int64), `teff_gspphot` (float), `logg_gspphot` (float), `mh_gspphot` (float), `azero_gspphot` (float), `teff_gspspec` (float), `logg_gspspec` (float), `mh_gspspec` (float). Null-valued cells emitted as empty fields.\n- **`package.json`** — new `refresh:gaia-apsis` npm alias. Manual run, NOT part of `npm run build` (per `frozen-external-data`).\n- **`.gitattributes`** — verify `data/*.tsv` LFS pattern covers it.\n\n# ADQL query\n\n```sql\nSELECT source_id,\n       teff_gspphot, logg_gspphot, mh_gspphot, azero_gspphot,\n       teff_gspspec, logg_gspspec, mh_gspspec\nFROM gaiadr3.astrophysical_parameters\nWHERE source_id IN (\u003csource_id batch list from AT-HYG.gaia\u003e)\nORDER BY source_id\n```\n\nBatched to fit Gaia TAP query limits (refresh_lib handles batching + retry per dch.21).\n\n# Acceptance\n\n- Row count ~266,000 ± 5% (projected from the 1000-source-id empirical probe, seed 20260517). Pinned in script as `EXPECTED_ROW_COUNT_MIN / MAX`; out-of-range fails the build.\n- Coverage stats logged: % of input source_ids returned by Apsis, % with gspphot (Teff AND logg), % with gspspec, % union. Expected ~85% union from probe.\n- TSV columns exactly: source_id, teff_gspphot, logg_gspphot, mh_gspphot, azero_gspphot, teff_gspspec, logg_gspspec, mh_gspspec. Schema validated by refresh_lib helper.\n- Deterministic regen: re-running with source unchanged produces a byte-identical TSV.\n\n# Smoke\n\n- `npm run refresh:gaia-apsis` writes the TSV deterministically.\n- `wc -l data/gaia_dr3_apsis.tsv` shows expected row count (~266k + header).\n- Coverage log line shows union ≥80%.\n- Spot-check: Sol's nearest neighbours (Proxima, α Cen A/B) appear with Apsis parameters that round-trip through `research/star-spectral-rendition/apsis_sample.py`.\n\n# Commit\n\nOne commit. Single bead. Standard PR.\n\n# Downstream uses (NOT in this bead's scope)\n\n- v6 binary surfacing of `teff_apsis` etc. per record — owned by `stellata-dch.49` (Bead CC). Inline-vs-side-table layout decision deferred to dch.49 execution time.\n- Tier 1 LUT re-keyed from Ballesteros(B-V)-derived Teff to Apsis-direct Teff where available (re-uses Tier 1's shader path — only the upstream Teff source changes).\n- `azero_gspphot` cross-check against Edenhofer dust map (filed separately when desired).\n- Stefan-Boltzmann radius refinement using direct logg (filed separately).\n- Phase 5 (spectral / mass refinement) gains Apsis-grade Teff + logg for stars with spectra.\n\n# Confidence (per `alex-large-pr-honesty` at PR-open time)\n\n- **High**: deterministic refresh script + row-count assertions + LFS-tracked output.\n- **Medium**: schema validation against refresh_lib helper.\n- **Low (needs eyeballs)**: manual smoke step — Alex runs `npm run refresh:gaia-apsis` and inspects the log line + spot-checks Sol's neighbours.\n\n# Origin\n\nFiled from `stellata-zsr.1` research (RECOMMENDATION.md § Tier 2 + § Coordination with stellata-dch). Apsis ingest is the natural Phase 1 sibling: TAP query, source_id-anchored, idempotent TSV output — matches the existing dch pattern exactly.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-18T09:07:28Z","created_by":"Alex Marshall","updated_at":"2026-05-18T20:23:32Z","started_at":"2026-05-18T19:23:30Z","closed_at":"2026-05-18T20:23:32Z","close_reason":"Closed","labels":["apsis","gaia","phase-1","physics","refresh-scripts"],"dependencies":[{"issue_id":"stellata-dch.48","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-18T10:07:28Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.48","depends_on_id":"stellata-dch.21","type":"blocks","created_at":"2026-05-18T10:07:38Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.48","depends_on_id":"stellata-zsr.1","type":"related","created_at":"2026-05-18T10:07:37Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.48","depends_on_id":"stellata-zsr.3","type":"related","created_at":"2026-05-18T10:07:37Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-zsr.3","title":"Tier 1 — replace ciToColor with Ballesteros + blackbody → sRGB LUT","description":"Implement the Tier 1 recommendation from `stellata-zsr.1` research: swap the 3-stop piecewise B-V gradient in `src/client/shaders/star.vert.glsl` for a build-time-precomputed blackbody → sRGB LUT, keyed on B-V-derived Teff (Ballesteros 2012). Fixes the visible cool/red oversaturation without waiting on any external data ingest.\n\nFull research and acceptance rationale: `research/star-spectral-rendition/RECOMMENDATION.md` § Tier 1. **Read it before starting.** The recommendation file also carries the Mu Cephei case study, FAQ / Terminology, and the empirical evidence behind the Tier 1 case (ΔE \u003e 50 on Antares / Betelgeuse / Aldebaran).\n\n# Scope\n\nSingle PR. No catalog schema bump. One shader change + one LUT helper + tests + SCIENCE.md citation.\n\n# Deliverables\n\n- **`scripts/blackbody-lut.ts`** (new) — TypeScript port of `research/star-spectral-rendition/blackbody_color.py`'s `blackbody_to_srgb`. Same Planck + CIE 1931 (Wyman 2013) + sRGB D65 transform. Emits a 256-entry RGB LUT spanning B-V ∈ [-0.4, 2.0] via Ballesteros 2012 (B-V → Teff) + blackbody → sRGB. Output embedded as a static `Uint8Array` constant in `src/client/shaders/blackbody-lut.ts` (build-time precomputation, no runtime cost).\n- **`src/client/shaders/blackbody-lut.ts`** (new, generated) — the LUT data + index helpers. Generated by `blackbody-lut.ts` at build time (mirror the existing build-time codegen pattern in `scripts/`).\n- **`src/client/shaders/star.vert.glsl`** (modified) — replace the `ciToColor` function with a LUT sample. Lines 137-143 today.\n- **Routing logic** in `star.vert.glsl`:\n  - if `ci` present: Teff = Ballesteros(B-V), sample LUT.\n  - elif `spect` parseable: Teff = T_TABLE(class, subclass), sample LUT.\n  - if white-dwarf: Teff = 50400 / wd_subclass (existing `tempKelvin` in `catalog-pure.ts`), sample LUT.\n  - else: solar fallback (B-V = 0.65 → existing path, no behaviour change).\n- **Dust reddening**: untouched — existing `effectiveCi = iCi + absorbAV / R_V` path remains; reddened B-V feeds Ballesteros and the LUT. Interface shape identical; observer-position-dependent rendering preserved (per the case study in RECOMMENDATION.md § Mu Cephei).\n- **SCIENCE.md** — new citation for Ballesteros 2012 + Wyman 2013 CIE fits + blackbody → sRGB derivation. Add at commit time per `defer-doc-updates`.\n\n# Acceptance\n\n- HR diagram in-scene matches `research/star-spectral-rendition/hr_panels.png` panel C (continuous, physical chroma).\n- Per-star RGB values for Antares / Betelgeuse / Aldebaran / Sirius B / Mintaka / Mu Cephei match the expected column from `research/star-spectral-rendition/sample_comparison.txt` (with Mu Cep validated against the case study, ~(255, 178, 96) Earth-perspective).\n- No regression for warm A/F/G main sequence stars (smallest ΔE bucket — current shader is already close).\n- LUT byte signature pinned by vitest — future changes go through a conscious update.\n\n# Tests (per `stellata-test-coverage-discipline`)\n\n- **Unit test for `scripts/blackbody-lut.ts`**: verify Ballesteros formula matches the Python reference within float epsilon at 5 sample B-V values (-0.4, 0.0, 0.65, 1.5, 2.0).\n- **Unit test for the LUT generator**: byte-signature pin (`expect(lutBytes).toEqual(...)`). Anchored so any chromaticity or transform change goes through a conscious bump.\n- **Shader output test** (if feasible without a GPU smoke): nominally pin Antares / Betelgeuse / Aldebaran / Sirius B / Mintaka / Sol expected RGBs via the same LUT-sample path the shader uses. Pure helpers — extract LUT sampling logic so it's testable from a `*-pure.ts` sibling.\n\n# Smoke (per `alex-stellata-smoke-tests`)\n\nManual only — Alex tests in own browser, no dev server, no Playwright. After typecheck + vitest pass, Alex pulls the branch and inspects:\n\n- **Cool / red end (the main fix):** Antares, Betelgeuse, Aldebaran, Mu Cephei. Expected: noticeably less aggressively red than today, warm peach / pumpkin / amber rather than near-pure red.\n- **Mu Cephei specifically**: confirm observer-position-dependent dust reddening still works — colour drifts toward intrinsic peach as you warp closer. The case study in RECOMMENDATION.md walks through expected behaviour.\n- **Hot end (no regression):** Mintaka, Sirius A, Vega. Should be blue-white, similar to today (current shader is already close at the hot end).\n- **Mid range (no regression):** Sol-anchor, Procyon, Capella. Solar-ish whites unchanged.\n- **HR diagram qualitative shape:** density distribution along B-V should match panel C continuity.\n\n# Confidence (per `alex-large-pr-honesty` at PR-open time)\n\n- **High**: build-time LUT generator (deterministic, byte-signature pinned in unit tests).\n- **Medium**: shader integration (uniform-feed unchanged; routing logic adjacent to today's code).\n- **Low (needs eyeballs)**: actual rendered colour vs expectation — only manual smoke catches a per-star visual regression. Hence the explicit named-star smoke list above.\n\n# Out of scope (filed elsewhere)\n\n- Gaia DR3 Apsis ingest — Tier 2 lives under `stellata-dch` as a separate bead.\n- Display P3 wide-gamut — filed `stellata-zsr.2`, deferred.\n- Pecaut \u0026 Mamajek 2013 extended T_TABLE — Tier 3, deferred.\n- Metallicity-aware colour offsets — Tier 3, deferred.\n\n# Origin\n\nFiled from `stellata-zsr.1` research. Single-commit single-PR. Ships standalone — not a `stellata-dch` dependency.","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T09:07:04Z","created_by":"Alex Marshall","updated_at":"2026-05-18T19:35:55Z","closed_at":"2026-05-18T19:35:55Z","close_reason":"Shipped in PR #89 (merged 2026-05-18). Bead status drift only — code is on main.","labels":["physics","pr-89","shader","ux"],"dependencies":[{"issue_id":"stellata-zsr.3","depends_on_id":"stellata-zsr","type":"parent-child","created_at":"2026-05-18T10:07:03Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-zsr.3","depends_on_id":"stellata-zsr.1","type":"related","created_at":"2026-05-18T10:07:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-io3.3","title":"Volume-complete Gaia inner sphere: catalog ingest expansion + 'you can wander here' representational overlay","description":"Gaia DR3 has made the inner ~50–100 pc around Sol effectively **volume-complete** for stellar surveys — every star down to G ~17 (catches all M dwarfs, most brown dwarfs, many faint white dwarfs) is detected by Gaia. This is unprecedented in stellar astronomy: a sphere of space where Stellata could honestly claim *every single star you can see in this volume is in the model, no selection bias.*\n\n# The exciting framing\n\nThe inner sphere becomes a **scientifically honest navigation zone** — the user can fly to any point within it and trust that what they see is what really exists. Powerful framing for Stellata's data-fidelity principle ([[stellata-data-fidelity-principle]]). The boundary is the edge of \"what we currently know about every star.\"\n\n# Why the current catalog doesn't have this\n\n`scripts/build-catalog.ts` consumes the AT-HYG **classic-IDs** subset — stars with HD/HR/HIP/Bayer/Flamsteed/Gl/TYC identifiers. This filter discards the bulk of faint M dwarfs and white dwarfs that exist in Gaia DR3 but lack classical names. So even within the inner volume-complete sphere, Stellata today represents only the brighter named subset, not the full Gaia census.\n\n# Two coupled deliverables\n\n## Part 1 — Catalog ingest expansion\n\n- Supplementary pull from Gaia DR3 of all `gaia_source` rows within R pc of Sol that aren't already in AT-HYG classic-IDs.\n- Required columns: source_id, ra, dec, parallax, pmra, pmdec, phot_g_mean_mag, bp_rp colour, plus Apsis Teff where available.\n- New `data/gaia_dr3_inner_sphere.tsv` (LFS-tracked, idempotent refresh script — per [[frozen-external-data]]).\n- Binary-catalog integration: concatenate with AT-HYG records. Likely needs a per-star \"in-volume-complete-region\" flag bit so the renderer can distinguish supplement stars from the bright AT-HYG core.\n\n## Part 2 — Representational boundary overlay\n\nThe user *sees* where the survey-complete region is. Options to evaluate:\n\n- Translucent sphere wireframe at the completeness radius (like Local Group wireframes).\n- Density-based gradient: opacity falls off as a function of survey-completeness fraction at distance.\n- Chart-mode-specific: different isobar / colour treatment inside vs outside the sphere.\n- Navigation hint: brief HUD readout on entry / exit (\"entering volume-complete zone\").\n\nThe overlay slots into the artificial-visualisation framework (parent stellata-io3). Pattern parallel to stellata-io3.2 (dataset boundary overlay for Edenhofer dust volume) — same idea, different dataset.\n\n# Research questions to settle before implementation\n\n1. **Right R?** Gaia DR3 completeness ladder:\n   - **50 pc** — gold-standard complete including faint WDs (GCNS — Gaia Catalogue of Nearby Stars).\n   - **100 pc** — complete for all M dwarfs and most WDs (the standard \"100-pc sample\" used in galactic archaeology).\n   - **200+ pc** — substantially incomplete at the faint end; would need careful selection-function modelling.\n2. **Ingest strategy?** Push upstream to AT-HYG project (David Nash) vs maintain our own supplementary Gaia volume-complete TSV.\n3. **Boundary representation?** Crisp sphere vs. fading gradient vs. chart-mode-only.\n4. **Beyond-R interaction?** Stars outside R are still magnitude-limited; the user needs visual cue distinguishing the two regimes.\n5. **Chart-mode handling?** Chart treats all stars uniformly today — should it highlight the volume-complete region?\n6. **Scale of representation?** Just a radius (one number) vs a survey-completeness-fraction-per-shell layer (more sophisticated).\n\n# Why deferred\n\n- **stellata-dch** (catalog-architecture rewrite) in flight changes ingest mechanics significantly; doing this before dch lands means redoing it.\n- **stellata-3f8** (DR4 refresh) extends volume-completeness depth — DR4 reaches further with more confidence. Doing it now means re-deciding R when DR4 lands.\n- The **stellata-io3** artificial-visualisation framework is still scoping; boundary-overlay pattern needs to settle there first.\n\nWhen un-deferred: **P2** (high user excitement, squarely on the data-fidelity-principle critical path). Currently P2 to mark high interest, but deferred status keeps it out of `bd ready`.\n\n# Origin\n\nFiled during the `stellata-zsr.1` wrap-up conversation on 2026-05-18. Surfaced while discussing why field-star HR diagrams can't give individual ages without volume-complete sampling — the Gaia 100-pc volume-complete sample is the gold standard, and Stellata could *physically* represent that as a navigable region inside the model. Conversational origin preserved in `research/star-spectral-rendition/RECOMMENDATION.md` § FAQ.\n\n# Related\n\n- [[stellata-dch]] — catalog rewrite; ingest mechanics overlap.\n- [[stellata-3f8]] — DR4 refresh; extends volume-completeness depth.\n- [[stellata-3f8.1]] — FLAME ages; same Gaia data pipeline.\n- [[stellata-io3]] — artificial-viz layer; parent for the overlay.\n- [[stellata-io3.2]] — dataset boundary overlay for dust; parallel pattern.\n- [[stellata-zsr.1]] — research conversation that surfaced this.","status":"deferred","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T08:41:05Z","created_by":"Alex Marshall","updated_at":"2026-05-18T08:41:16Z","labels":["artificial","catalog","gaia","ingest","ism","research","ux","visualisation"],"dependencies":[{"issue_id":"stellata-io3.3","depends_on_id":"stellata-3f8","type":"related","created_at":"2026-05-18T09:41:14Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-io3.3","depends_on_id":"stellata-3f8.1","type":"related","created_at":"2026-05-18T09:41:15Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-io3.3","depends_on_id":"stellata-io3","type":"parent-child","created_at":"2026-05-18T09:41:05Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-io3.3","depends_on_id":"stellata-io3.2","type":"related","created_at":"2026-05-18T09:41:13Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-io3.3","depends_on_id":"stellata-zsr.1","type":"related","created_at":"2026-05-18T09:41:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-6ki","title":"Focus engagement gives no visible feedback when camera is already at park pose","description":"# Problem\n\nWhen the user clicks to focus a star (search-select, in-3D click-to-focus, URL restore) and the camera is already at or very close to the focus-park pose for that star, the focus-park lerp runs through its motion but the visual delta is below user-perceivable thresholds. The user receives no signal that their action registered — search box updates synchronously and the focus ring engages, but those signals are quiet enough that on a busy screen the user can think the app didn't respond.\n\nExtreme case: clicking to focus a star while exactly at its `parkDistForStar` pose (e.g. re-clicking the same focused star, or focusing from a URL restore that landed on the same pose). The lerp runs ~2 s of near-identity motion and the camera doesn't move a perceivable pixel.\n\nUX principle (Alex): affordances need a visible effect, otherwise the app reads as broken.\n\nThis is a pre-existing issue, not a regression from any recent PR.\n\n# Proposed solution — two coupled changes, ship together\n\n1. **Skip near-zero focus-park lerps.** When the camera→park-pose delta (eye distance error + angular error) is below a small visual threshold, snap to the parked pose without animating. Honest camera — no fake motion on a physics-accurate viz.\n\n2. **Add a focus-ring \"engage pulse\" to `focus-ring-overlay.ts`.** On the `'focus'` event, the ring fires a one-shot animation (radius bump-and-settle, or a colour flash) over ~200 ms regardless of whether the camera moved. Gives the user a \"I did a thing\" signal that's independent of camera state.\n\nCritical: the pulse should ALSO fire when the camera DOES glide (the non-degenerate cases). Single consistent affordance — focus engaged = ring pulses — instead of branching the affordance on \"did the camera move.\"\n\n# Acceptance\n\n- Clicking to focus a star at its current parked pose: snaps (no lerp), focus ring pulses.\n- Clicking to focus a star far from its parked pose: glides as today (focus-park lerp animates over `FOCUS_LERP_MS`), focus ring pulses on engagement.\n- Clicking to focus a star at intermediate distance: animates if the visual delta exceeds the threshold, snaps if not; pulse fires either way.\n- Pulse plays across navigate-mode focus, search-select focus, URL-restore focus. SHOULD NOT play during observe-mode anchor swaps (`swapObserveAnchor` in WarpController) where the user perception is different (camera was already pinned at local origin).\n- Tests:\n  - `focus-transition.test.ts` (or a paired pure helper) covers the early-exit threshold — pin the threshold value as a constant, test the \"snap when below\" / \"lerp when above\" branches.\n  - `focus-ring-overlay.test.ts` covers the pulse animation lifecycle (engage on `'focus'`, decay to baseline after ~200 ms, no pulse on null→null).\n\n# Smoke\n\n- Sirius is the canonical test — focus from Sol park pose (close), then from Vega park pose (far).\n- Focus Sirius via search while already parked at Sirius: ring should pulse, camera should snap silently.\n- Focus Sirius from across the sky: ring pulses, camera glides.\n- URL-restore landing on a focus that matches the current pose: pulse fires, no console warning.\n\n# Files likely touched\n\n- `src/client/camera/focus-transition.ts` — early-exit threshold in `newFocusLerpFrom` or at the call site in `Stellata.focusStar`.\n- `src/client/overlays/focus-ring-overlay.ts` — pulse animation state + tick.\n\n# Connects\n\n- [[alex-stellata-smoke-tests]] — manual smoke after.","status":"open","priority":2,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-17T10:32:36Z","created_by":"Alex Marshall","updated_at":"2026-05-17T10:32:36Z","labels":["camera","ux"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2br.9","title":"Hybrid linear-d + angular-size arrival curve","description":"## Why\n\nSmoke testing of stellata-2br.8's trapezoidal curve revealed that no\nsingle-regime profile on log-d space can produce the perceptual rhythm\nAlex wants. Camera linear velocity is `d · f' · |log_ratio| / T_warp`;\nbecause `d` shrinks geometrically, linear velocity peaks early (~1.3 s\ninto an 8 s Fly) and decreases monotonically regardless of curve\nshape. Background-star parallax flow tracks linear velocity, so the\ncamera always reads as decelerating after the early peak. No knob in\narrival-curves.ts can move that peak past the first quarter of the\nwarp.\n\n## Goal\n\nTwo-regime hybrid:\n- **Far approach** (parallax-driven): linear-d piecewise-quadratic\n  (`f = 2τ² | 1 − 2(1−τ)²` on `d_target = d_0 − f·(d_0 − d_seam)`).\n  Reproduces pre-2br.3 main-branch rocket-impulse feel exactly, on\n  the truncated `[d_0, d_seam]` range.\n- **Close approach** (angular-driven): quintic smootherstep on\n  angular size `θ(σ) = θ_seam + S(σ)·(θ_end − θ_seam)` with\n  `S(σ) = 10σ³ − 15σ⁴ + 6σ⁵`. Zero velocity AND zero acceleration\n  at u = 1 → clean perceptual landing.\n\nSeam at `d_seam = seam_k · parkDist`, default `seam_k = 500`.\nTime split is auto-computed:\n`u_seam = clamp(log(d_0/d_seam) / log(d_0/d_end), 0.3, 0.85)`.\n\n## Encoding trick\n\nHybrid is exposed as a fifth arm of `resolveArrivalCurve` that\nreturns a closure `(u) → number`. The closure encodes the hybrid\ntrajectory in real distance space but returns the log-d-equivalent\n`f(u) = log(d_target(u)/d_0) / log(d_end/d_0)`. Consumer\n(`camera-motion.ts:tickArrival`) is unchanged — it still applies\n`d(u) = d_0 · (d_end/d_0)^f(u)`.\n\n## Files\n\n- `src/client/arrival-curves.ts` — add `easeHybrid` + `'hybrid'`\n  arm of `resolveArrivalCurve`; refactor signature to take a params\n  object + optional `ArrivalCurveContext`.\n- `src/client/focus-target.ts` — new `physicalRadius(): number | null`\n  method on the interface; star impl reads\n  `catalog.physicalRadius[idx] * R_SUN_PC`, cloud impl returns null.\n- `src/client/warp-tuning.ts` — `'hybrid'` in dropdown; `seam k`\n  slider (default 500, range 100–2000); thread context into\n  `warpArrivalEaseFn(ctx?)`.\n- `src/client/stellata.ts` (4 call sites) and\n  `src/client/focus-transition.ts` — compute and pass `ctx` at\n  resolution time.\n- `src/client/arrival-curves.test.ts` — endpoints, seam value, seam\n  velocity, regime parity, quintic landing, degenerate-d_0, null-R\n  fallback.\n- `docs/camera-arrival.md` — § \"Hybrid linear-d + angular-size\n  profile\" addendum.\n- `docs/camera-warp.md` § Fly — extend cross-ref.\n- `package.json` — 1.11.0 → 1.12.0.\n\n## Decisions made (record so future-Alex isn't confused by missing knobs)\n\n1. **Default `seam_k = 500`** (geometric midpoint of the empirical\n   100–1000 × parkDist range cited in docs/camera-arrival.md).\n   Rejected: per-star angular threshold (R varies 4+ orders of\n   magnitude → fixed θ_seam is non-uniform across stars).\n2. **`u_seam` is auto-computed, NOT a slider.** Log-proportional with\n   `[0.3, 0.85]` clamp. Rejected: slider override (would let u_seam\n   drift from seam_k, confusing the model) and fixed 0.5 (doesn't\n   adapt to warp distance).\n3. **Inner uses quintic smootherstep** (`10σ³ − 15σ⁴ + 6σ⁵`), not\n   cubic. Quintic has `S''(1) = 0` — matches Alex's \"derivative of\n   velocity = 0 at the end\" intent. Cubic has nonzero acceleration\n   at landing → faint \"snap to stop.\"\n4. **Hybrid is opt-in in 1.12.0, not the default.** Trapezoidal\n   (1.11.0) stays in the panel as a backup. Cubic-Hermite remains\n   default until manual smoke confirms hybrid feels right end-to-end.\n   Promotion is a one-line change later.\n5. **Cloud / null-R fallback is silent, to cubic-Hermite.** Avoids\n   force-switching per call site; one console.debug per warp so\n   panel users see why hybrid isn't active.\n6. **Outer→inner handoff at v=0**, not velocity-continuous at v\u003e0.\n   Both regimes naturally arrive at the seam with zero velocity\n   (piecewise-quad ends at v=0; quintic starts with `S'(0)=0`).\n   Sidesteps the velocity-continuity constraint that killed the\n   previously-rejected dWindow split.\n7. **LINEAR smoothstep on θ, not on log θ.** Smoothstep on log θ is\n   identical to smoothstep on log d (the current cubic-Hermite\n   default) which is exactly what Alex's complaint is about. Linear\n   in θ means the on-screen angular growth follows S(σ) directly.\n\n## Smoke (manual, Alex's browser, no dev server)\n\n- `/?debug=1`, expand warp section. Curve → `hybrid`. Verify\n  `seam k` slider appears.\n- Scenarios 5 (Betelgeuse → Sol) and 9 (Xi1 Sgr → Sol) from PR #66:\n  outer phase should feel like pre-2br.3 main-branch (parallax\n  sweep); inner phase: Sol's disc grows smoothly to fill screen\n  with no perceptual standstill.\n- Seam handoff should be imperceptible — momentary v=0 coast\n  perceptually masked by parallax-collapse / angular-onset overlap.\n- `seam_k = 100` vs `1000` slider sweep to find the right\n  transition point.\n- Short-warp edge: Alpha Centauri → Sol (~1.3 pc) — u_seam clamp\n  should give inner ≥ 30 % of time.\n- Cloud destination: silent fallback to cubic-Hermite.\n\n## PR\n\nStack on PR #66 (still open) or open new PR if merged.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T19:42:28Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:01:30Z","started_at":"2026-05-16T19:42:36Z","closed_at":"2026-05-16T21:01:30Z","close_reason":"shipped in PR #66 (commit 25e4b8b)","labels":["arrival-curve","camera","camera-warp","pr-66","ux"],"dependencies":[{"issue_id":"stellata-2br.9","depends_on_id":"stellata-2br","type":"parent-child","created_at":"2026-05-16T20:42:28Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.194.9","title":"Extract star-physics helper from stellata.ts (parkDistForStar, renderedSizePx, fovMinorRad, etc.)","description":"Lift the cluster of stellata.ts methods that compute per-star camera/screen geometry into a new pure module `camera/star-physics.ts` (post-194.1 folder reorg). Consumers: Picker, FocusController, chart-mode pipeline, overlays.\n\n# Code to move\n\n- renderedSizePx (stellata.ts:3066)\n- renderedDiscPxAtPeak (stellata.ts:3259)\n- fovMinorRad (stellata.ts:3182)\n- peakAmplitudeFactor (stellata.ts:3196)\n- getChartDiscParams (stellata.ts:2229)\n- getFocusedStarPeakDiscRadiusPx (stellata.ts:3175)\n- parkDistForStar (stellata.ts:3241)\n- minOrbitDistForStar (stellata.ts:3213)\n- binaryCompanionFloorPc (stellata.ts:3223)\n\nThese currently read from `this.material.uniforms`, `this.camera`, `this.catalog`, `this._localPositions`, `this.filter`. After extraction they take those as plain arguments — pure functions parameterised on `(catalog, idx, camPos, uniforms, filter)`.\n\n# Public surface\n\n```ts\n// Per-star pixel/distance geometry. All inputs explicit; no `this` capture.\nrenderedSizePx(args: { catalog, idx, camPos, localPositions, uniforms, filter, now }): number\nrenderedDiscPxAtPeak(args: { catalog, idx, camPos, localPositions, uniforms }): number\nfovMinorRad(camera: PerspectiveCamera): number\npeakAmplitudeFactor(catalog, idx): number\nparkDistForStar(args: { catalog, idx, fovMinorRad }): number\nminOrbitDistForStar(args: { catalog, idx, fovMinorRad }): number\nbinaryCompanionFloorPc(catalog, idx): number\ngetChartDiscParams(uniforms): { maxPx, minPx, magBright }\n```\n\n# Existing extracts to compose\n\n- `star-geometry.ts` already exports `physSizePx`, `varEffectiveAmplitude`, `distAtFillFraction`, `peakAmplitudeFactor` (pure). star-physics.ts is the thin per-frame wrapper around those + shader uniforms + the per-star catalog read.\n\n# Independence\n\n- Blocked by 194.1 (folder reorg lands first so `camera/star-physics.ts` lives in its final home).\n- Blocks 194.8 (FocusController consumes parkDistForStar / minOrbitDistForStar / peakAmplitudeFactor; cleaner if it imports from star-physics.ts rather than carrying them inline).\n- Parallel-trackable with 194.5, .6, .7 — touches helper code, not animation state.\n\n# Acceptance\n\n- stellata.ts no longer holds renderedSizePx / parkDistForStar / etc. as methods.\n- camera/star-physics.test.ts pins numeric outputs:\n  - `expect(parkDistForStar({catalog, idx: SIRIUS_IDX, fovMinorRad: ...})).toBe(N)`\n  - `expect(renderedSizePx({...})).toBe(M)` for a representative star + camera configuration\n  - peakAmplitudeFactor edge cases (period=0, period\u003e0+amplitude=0, large amplitude)\n- All callers (Picker, FocusController, chart-mode, overlays) import from camera/star-physics.ts.\n- typecheck + vitest pass.\n\n# Smoke\n\n- Visual: focus a few stars (Sirius, Betelgeuse, Algol) and confirm orbit floor + park distance look identical to pre-extract.\n- Focus a binary (alpha Cen) and confirm BINARY_VIEWPORT_HALF_ANGLE_RAD floor still applies.\n- Variable star (Algol) at peak + trough — disc envelope matches.\n\n# Connects\n\n- [[stellata-folder-and-controller-conventions]] rule (4) — pure helpers extract at second use, never duplicated.\n- [[stellata-named-constants-and-dry]] — DCAM_LOG_FLOOR_PC + R_SUN_PC must come from canonical modules, not be redefined here.\n- [[stellata-test-coverage-discipline]] — numeric outputs pinned with toBe(N), never toBeLessThanOrEqual.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T17:37:42Z","created_by":"Alex Marshall","updated_at":"2026-05-17T11:19:42Z","started_at":"2026-05-17T10:38:44Z","closed_at":"2026-05-17T11:19:42Z","close_reason":"Closed","labels":["architecture","cleanup","docs"],"dependencies":[{"issue_id":"stellata-9mm.194.9","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T18:37:41Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.194.9","depends_on_id":"stellata-9mm.194.1","type":"blocks","created_at":"2026-05-16T18:37:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.194.8","title":"Extract FocusController from stellata.ts (focus FSM + pin engage + lerp)","description":"Move focus FSM ownership + pin-engage state + lerp orchestration from stellata.ts into `camera/focus-controller.ts`. Absorbs the FocusOps shim that WarpController has been carrying since .5; WarpController's + ObserveTransition's import seams update in one line each.\n\n# Code to move\n\n- `focusedStar`, `focusedCloud`, `focusedPlanetSystem`, `planetSystemToken`, `focusLerpState` state fields.\n- `setFocus`, `setFocusedCloud`, `focusStar`, `flyToCloud`, `setOrbitTarget`, `setOrbitTargetCloud`, `unfocus`.\n- `startFocusLerp` / `endFocusLerp` / `cancelFocusLerp` / `cancelUnfocusLerp`.\n- `updateFocusLerp` → `tick`.\n- `refreshPlanetSystem`.\n- `recenterFocusToStar` (internal seam used by WarpController).\n- `isPinEngaged`, `getPinEngageThresholdSq`.\n- `makeStarFocusTarget`, `makeCloudFocusTarget`, `currentFocusTarget` (from #66 — these ARE the FocusOps surface).\n- `parkDistForStar` / `minOrbitDistForStar` / `binaryCompanionFloorPc` — IF 194.9 hasn't landed yet. If 194.9 has landed (preferred), import from `camera/star-physics.ts`.\n- Constants: `GLOBAL_MIN_DIST_PC`, `ZOOM_FLOOR_FRACTION`, `VAR_TROUGH_FLOOR_FRACTION`, `PIN_ENGAGE_THRESHOLD_SQ_PC`, `BINARY_VIEWPORT_HALF_ANGLE_RAD`, `BINARY_MIN_DIST_FACTOR`. (`FOCUS_LERP_MS` and `DCAM_LOG_FLOOR_PC` are in `camera/timing.ts` per 194.1.)\n\n# FocusOps interface (the cross-controller seam)\n\nFocusController's public surface IS the `FocusOps` interface other controllers (WarpController, ObserveTransition) consume. Export the interface from `camera/focus-controller.ts`. The runtime swap in `Stellata` from `focus: this` to `focus: this.focus` is the integration-shell change in this PR.\n\n# Public surface (finalised in 194.2; this IS the FocusOps interface)\n\n```ts\nimport type { FocusTarget } from './focus-target';\n\ninterface FocusOps {\n  // queries\n  getFocusedStar(): number | null\n  getFocusedCloud(): number | null\n  getFocusedPlanetSystem(): PlanetSystem | null\n  isPinEngaged(): boolean\n  isFocusLerpActive(): boolean\n  currentFocusTarget(): FocusTarget | null\n  makeStarFocusTarget(idx: number): FocusTarget\n  makeCloudFocusTarget(idx: number): FocusTarget | null\n  parkDistForStar(idx: number): number\n  minOrbitDistForStar(idx: number): number\n\n  // commands\n  focusStar(idx: number, opts?: { animate?: boolean }): void\n  setFocus(idx: number | null): void\n  setFocusedCloud(idx: number | null): void\n  setOrbitTarget(idx: number): void\n  setOrbitTargetCloud(idx: number): void\n  flyToCloud(idx: number, opts?: { animate?: boolean }): void\n  unfocus(opts?: { animate?: boolean }): void\n  cancelFocusLerp(): void\n  cancelUnfocusLerp(): void\n\n  // internal seams (called by warp / observe — exported via @internal)\n  recenterFocusToStar(idx: number): Vector3 | null\n  startFocusLerp(state: FocusLerpState): void\n}\n\nclass FocusController implements FocusOps {\n  constructor(deps: {\n    camera: PerspectiveCamera\n    controls: TrackballControls\n    catalog: Catalog\n    bus: EventBus\n    frameAnchor: FrameAnchor               // recenterOrigin + getWorldOffset\n    observe: ObserveTransition             // for startUnfocusLerp dispatch\n    uHideFocusIdxRef: { value: number }\n    getCameraMode: () =\u003e CameraMode\n    getClouds: () =\u003e MolecularClouds | null\n  })\n\n  // ...all FocusOps members + tick(nowMs), dispose()\n}\n```\n\n# Bus events\n\n`'focus': number | null`, `'cloudFocus': number | null`, `'planetSystem': PlanetSystem | null`, `'focusLerp': boolean`.\n\n# FrameAnchor interface (stays on Stellata)\n\n```ts\ninterface FrameAnchor {\n  recenterOrigin(newOrigin: Vector3): Vector3 | null\n  getWorldOffset(): Readonly\u003cVector3\u003e\n}\n```\n\n`Stellata implements FrameAnchor` — `worldOffset`, `_localPositions`, the `iPositionAttr.needsUpdate` write, and the per-frame pin uniform write all stay on the composition shell. Cleaner extraction is coupled to the StarPipeline extract 9mm.43 and deferred until then.\n\n# Acceptance\n\n- stellata.ts under 1500 lines — primary epic-level acceptance gate. (Bigger lift now after #66 + #72; .9 helps reduce by lifting star-physics.)\n- `camera/focus-controller.test.ts` covers near-focus (no warp) + far-focus (triggers warp) + cancel mid-lerp + pin engage threshold + observe-cleanup branch + unfocus close-zoom + worldOffset preservation on unfocus + FocusTarget round-trip (makeStarFocusTarget → applyFocus → emitFocusEvents).\n- WarpController's `focus: FocusOps` swap (from `stellata` to FocusController) is in this PR.\n- ObserveTransition's `focus: FocusOps` swap is the same.\n- typecheck + vitest pass.\n\n# Smoke\n\n- Focus a near star (Sirius from a wide view): short lerp, no warp.\n- Focus a distant star (Deneb): triggers warp.\n- Focus a cloud (Taurus complex).\n- Focus a Local Group object (LMC) — distance + lerp interplay.\n- Unfocus by close-zoom past minDistance.\n- Unfocus by clicking blank sky.\n- Unfocus from observe via search X (clearFocusOnExit path).\n- Pin engage threshold: zoom in until pin engages, confirm transition is clean.\n- Sol-as-default-focus (catalog.solIndex) auto-engage on first paint.\n\n# Dependencies\n\nBlocked by 194.6 (ObserveTransition extracted first — FocusController.unfocus() dispatches into observe.startUnfocusLerp).\nBlocked by 194.9 (star-physics helper) — FocusController imports parkDistForStar / minOrbitDistForStar / peakAmplitudeFactor from `camera/star-physics.ts`.\n\n# Doc updates landing in this PR (per defer-doc-updates)\n\n- `docs/architecture.md` § \"FocusTarget contract\" — point at FocusController as the canonical producer (makeStarFocusTarget / makeCloudFocusTarget / currentFocusTarget).\n- `docs/architecture.md` § \"Floating origin\" — name the FrameAnchor interface seam.\n- `docs/architecture.md` § \"Pin-to-center\" — point at FocusController.isPinEngaged.\n\n# Connects\n\n- [[stellata-folder-and-controller-conventions]] rule (3).\n- [[stellata-consistency-at-the-seam]] single-source-of-truth — recenterOrigin caller still uses Stellata's FrameAnchor; no duplicate worldOffset state.\n- docs/architecture.md — pin-engage invariant, floating-origin contract, FocusTarget contract. The contract FocusController inherits.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:41:06Z","created_by":"Alex Marshall","updated_at":"2026-05-17T13:26:10Z","started_at":"2026-05-17T11:21:55Z","closed_at":"2026-05-17T13:26:10Z","close_reason":"Closed","labels":["architecture","camera","cleanup","docs"],"dependencies":[{"issue_id":"stellata-9mm.194.8","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T17:41:06Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.194.8","depends_on_id":"stellata-9mm.194.6","type":"blocks","created_at":"2026-05-16T17:42:40Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.194.8","depends_on_id":"stellata-9mm.194.9","type":"blocks","created_at":"2026-05-16T18:37:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-lo5.8","title":"Chart-mode tooltip styling","description":"Restyle `#tooltip` for the paper aesthetic when chart mode is engaged.\n\n# Scope\n\n- Verify whether chart-mode already toggles a `body.chart-mode` class. If yes, reuse; if no, add it in `chart-mode.ts` engage/disengage paths.\n- `src/client/styles.css` — add `body.chart-mode #tooltip { … }` selector chain mirroring the chart-mode paper aesthetic (font, border, background — match the chart-labels.ts glyph styling already in place).\n- No engine change; payload identical.\n\n# Acceptance\n\n- Toggle chart mode; hover each class (star / planet / LG / heliopause apex) → tooltip restyles to paper aesthetic; payload unchanged from non-chart hover.\n\n# Smoke\n\n- Toggle chart mode (C key), hover representative object from each class.\n\nShips as part of lo5 single-PR rollup; close on PR merge. Depends on lo5.2.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:41:05Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:39:01Z","started_at":"2026-05-16T20:26:09Z","closed_at":"2026-05-16T21:39:01Z","close_reason":"merged via PR #72 (commit 13: chart-mode tooltip satisfied by existing body.monochrome .tooltip rule; convention captured as Rule 4 in stellata-lo5-hover-conventions)","labels":["chart-mode","picker","ui","ux"],"dependencies":[{"issue_id":"stellata-lo5.8","depends_on_id":"stellata-lo5","type":"parent-child","created_at":"2026-05-16T17:41:05Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.194.5","title":"Extract WarpController from stellata.ts (3-phase warp FSM, consolidate warp-pure)","description":"Move WarpState + the 3-phase warp state machine into `camera/warp-controller.ts` (folder home from 194.1). `camera/warp-pure.ts` stays as the sibling pure module. WarpController also imports `camera/arrival-curves.ts` + `camera/camera-motion.ts` for the hybrid arrival profile that landed in PR #66.\n\n# Code to move\n\n- `WarpState` — refactored on main to carry `dest: FocusTarget` instead of `{destKind, destIdx}`, plus the new `recenteredToDest: boolean` field.\n- `startWarp`, `finishWarp`, `updateWarp`, `tryMidFlyRecentre` (new from #66 — mid-Fly origin recentre)\n- `warpTo`, `warpToCloud`, `skipWarp`, `getWarpPhase` (new from #66), `getWarpInfo`\n- `swapObserveAnchor` — only consumer is `finishWarp`.\n- `warpQ0`, `warpQ1`, `warpTmp`, `_tmpAnimateLocal` scratch state.\n\n(`destLocalPositionInto` collapses — WarpState.dest is a FocusTarget so `dest.localPositionInto(out)` replaces it.)\n\n# Constants\n\nAll from `camera/timing.ts` (renamed from warp-constants.ts in 194.1): `WARP_T_MIN_MS`, `WARP_T_MAX_MS`, `WARP_T_K_MS`, `WARP_REORIENT_MS`, `OBSERVE_TRANSITION_MS`, `WARP_BASE_DIR`.\n\n# FocusOps shim (the cross-controller coupling)\n\nWarpController's `startWarp` and `finishWarp` call into focus state that hasn't been extracted yet (FocusController is 194.8). For this PR, WarpController takes a `stellata: Stellata` reference (typed as `FocusOps` so the seam is interface-shaped from day 1) and calls:\n\n- `stellata.currentFocusTarget()` (returns FocusTarget | null; from #66's refactor)\n- `stellata.makeStarFocusTarget(idx)`, `stellata.makeCloudFocusTarget(idx)` (also from #66)\n- `stellata.recenterFocusToStar(idx)` for the mid-Fly + phase-3 recentre\n- `stellata.setVectorTo(null)` / `stellata.setVectorToCloud(null)`\n- `stellata.parkDistForStar(idx)` (until 194.9 lifts to camera/star-physics.ts; then imported directly)\n\nIn 194.8, `FocusOps` becomes FocusController's interface; WarpController's import seam updates in one line.\n\n# Public surface (finalised in 194.2)\n\n```ts\nclass WarpController {\n  constructor(deps: {\n    camera: PerspectiveCamera\n    controls: TrackballControls\n    observeControls: ObserveControls\n    uHideFocusIdxRef: { value: number }    // material.uniforms.uHideFocusIdx\n    bus: EventBus\n    getCameraMode: () =\u003e CameraMode\n    getClouds: () =\u003e MolecularClouds | null\n    focus: FocusOps                         // = stellata (shim) in .5, = FocusController in .8\n  })\n\n  warpTo(destIdx: number): void\n  warpToCloud(destIdx: number): void\n  skip(): void\n  tick(nowMs: number): void\n  isActive(): boolean\n  getWarpInfo(): WarpInfo | null\n  getWarpPhase(): WarpPhase                  // (new public surface from #66)\n  dispose(): void\n}\n```\n\n`WarpInfo` + `WarpPhase` types stay as they exist on main; exported alongside the controller for `docs/camera-warp.md` references.\n\n# Bus events\n\n`'warp': boolean` — emitted at startWarp + finishWarp.\n\n# Acceptance\n\n- stellata.ts no longer holds warp state fields, the tick methods, swapObserveAnchor, or tryMidFlyRecentre.\n- `camera/warp-controller.test.ts` covers:\n  - 3-phase FSM transitions (reorient → Fly → phase 3 / post-arrival).\n  - Hybrid arrival profile in Fly phase (cubic-Hermite log-d default + the 'hybrid' linear-seam-then-angular curve when FocusTarget.physicalRadius() returns a value).\n  - Source/dest variants: star→star, star→cloud, cloud→star, observe→observe.\n  - skipWarp during each phase.\n  - Mid-Fly recentre — tryMidFlyRecentre is exercised when the camera crosses into the destination's local frame.\n  - Coincident-source bail (distPc \u003c 1e-6).\n  - getWarpPhase outputs across phases.\n- WARP_T_* constants imported from camera/timing.ts; never redefined.\n- typecheck + vitest pass.\n\n# Smoke\n\n- Warp star→star: Sirius → Deneb (long; tests log-linear duration cap + hybrid profile if applicable).\n- Warp star→cloud: Sirius → LMC cloud (cloud arrival distance applies).\n- Warp cloud→star: any cloud → Sol.\n- Warp observe→observe: focused-and-observing → another star.\n- Skip during each phase → arrival behaviour matches non-skipped warp.\n- Confirm Sol / GC arrow fade behaviour during warp unchanged (per stellata-ml8 retrospective).\n- Focus engage after warp arrival; observe→observe stays in observe mode throughout.\n\n# Dependencies\n\nBlocked by 194.4 (AimController extracted first — startWarp cancels any in-flight aim).\n\n# Connects\n\n- [[stellata-folder-and-controller-conventions]] rule (3).\n- [[stellata-consistency-at-the-seam]] sentinel-init — WarpState's optional fields (flyEndQuaternion, reorientEndQuaternion, recenteredToDest) are sentinel-init poison-checked.\n- docs/camera-warp.md, docs/camera-arrival.md — the contracts WarpController inherits.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:41:02Z","created_by":"Alex Marshall","updated_at":"2026-05-17T10:33:56Z","started_at":"2026-05-17T08:54:39Z","closed_at":"2026-05-17T10:33:56Z","close_reason":"Closed","labels":["architecture","camera-warp","cleanup","docs"],"dependencies":[{"issue_id":"stellata-9mm.194.5","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T17:41:01Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.194.5","depends_on_id":"stellata-9mm.194.4","type":"blocks","created_at":"2026-05-16T17:42:37Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.194.6","title":"Extract ObserveTransition from stellata.ts (navigate↔observe orchestrator)","description":"Move ObserveTransitionState + the navigate↔observe mode-switch orchestrator from stellata.ts into `camera/observe-transition.ts` (folder home from 194.1). The state slot is OWNED here even though `Stellata.unfocus()` writes into it — ObserveTransition exposes `startUnfocusLerp(...)` from day 1, and the focus path calls in.\n\n# Code to move\n\n- `ObserveTransitionState` (stellata.ts:298)\n- `setCameraMode` (stellata.ts:1335) → `setMode` on the controller\n- `startObserveExit` (stellata.ts:1383) → `startExit` on the controller\n- `updateObserveTransition` (stellata.ts:3817) → `tick` on the controller\n- `finishObserveTransition` (stellata.ts:3826)\n- `getObserveTransitionProgress` (stellata.ts:1239) → `getProgress`\n- `isObserveTransitionActive` (stellata.ts:1147) → `isActive` (excludes 'unfocus' kind, matches existing contract)\n- New: `isAnyActive()` includes 'unfocus' kind — used by Stellata.isCameraBusy().\n- `alignCameraUpToQuaternion` (stellata.ts:3889) lifts to `camera/up-align-pure.ts` so the controller imports it without inheriting from Stellata.\n- Constant `OBSERVE_TRANSITION_MS` lives in `camera/constants.ts` per 194.1.\n\n# Code that STAYS in stellata.ts\n\n- `unfocus()` (stellata.ts:1889) — orchestration spanning vector clearing + observe-exit + navigate-close-zoom + focus clear. Calls `observe.startExit(...)` or `observe.startUnfocusLerp(...)` depending on path.\n- `observeUpdateTarget` (stellata.ts:3894) — animate-loop per-frame target re-pin for observe mode; lives with the animate dispatcher.\n\n# Public surface (finalised in 194.2)\n\n```ts\nclass ObserveTransition {\n  constructor(deps: {\n    camera: PerspectiveCamera\n    controls: TrackballControls\n    observeControls: ObserveControls\n    uHideFocusIdxRef: { value: number }\n    bus: EventBus\n    focus: FocusOps                        // for startExit's clearFocusOnExit + getFocusedStar\n  })\n\n  setMode(mode: CameraMode, opts?: { animate?: boolean }): void\n  startExit(opts: { animate: boolean; clearFocusOnExit: boolean }): void\n  startUnfocusLerp(fromPos: Vector3, toPos: Vector3, finalMinDist: number): void\n  tick(nowMs: number): void\n  isActive(): boolean                     // excludes 'unfocus' per current contract\n  isAnyActive(): boolean                  // includes 'unfocus'; drives isCameraBusy\n  getProgress(): { f: number; kind: 'enter' | 'exit' } | null\n  dispose(): void\n}\n```\n\n# Bus events\n\n`'cameraMode': CameraMode` — emitted at setMode + startExit (every exit path goes through startExit, including the navigate-mode toggle and search-X clear). `'state'` is emitted in lockstep with cameraMode.\n\n# Acceptance\n\n- stellata.ts no longer holds ObserveTransitionState or the setCameraMode / startObserveExit / updateObserveTransition / finishObserveTransition methods.\n- `camera/observe-transition.test.ts` covers all 4 mode-switch entry points + the swap-anchor edge case + the unfocus-lerp variant with its `finalMinDistance` write-back + dispose clears state without flicker.\n- `camera/up-align-pure.test.ts` exists for the lifted helper (paired with the existing camera-up-align.test.ts fixture).\n- 9mm.159 (swapObserveAnchor naming) — close as superseded by 194.5's swapObserveAnchor → WarpController-private move, OR fold the rename into .5. Decide before opening .5 PR.\n- typecheck + vitest pass.\n\n# Smoke\n\nMode-switch entry points to verify:\n- Mode pill click (top-right card).\n- Keyboard O.\n- Click while focused in NAVIGATE → enters OBSERVE.\n- Double-click while focused in OBSERVE → exits to NAVIGATE.\n- Chart mode engage / disengage in OBSERVE.\n- Search-row X button (clearFocusOnExit=true path).\n- Navigate-mode close-zoom unfocus (the 'unfocus' lerp kind).\n\n# Dependencies\n\nBlocked by 194.5 (WarpController extracted first — observe transitions cancel any in-flight warp's observeControls state).\n\n# Connects\n\n- [[stellata-folder-and-controller-conventions]] rule (3).\n- docs/camera-observe.md — the contract this controller inherits.\n- docs/integration-shell-decomposition.md § \"ObserveTransition\".","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:41:02Z","created_by":"Alex Marshall","updated_at":"2026-05-17T10:33:57Z","started_at":"2026-05-17T09:38:54Z","closed_at":"2026-05-17T10:33:57Z","close_reason":"Closed","labels":["architecture","cleanup","docs","observe"],"dependencies":[{"issue_id":"stellata-9mm.194.6","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T17:41:02Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.194.6","depends_on_id":"stellata-9mm.194.5","type":"blocks","created_at":"2026-05-16T17:42:39Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.194.4","title":"Extract AimController from stellata.ts (aim slerps + observe-aim)","description":"Move aim state machines from stellata.ts into `camera/aim-controller.ts` (folder home from 194.1). Stellata composes AimController.\n\n# Code to move\n\n- `AimState` (stellata.ts:327)\n- `ObserveAimState` (stellata.ts:339)\n- `aimAt` (stellata.ts:2760) — the mode-aware entry point; both navigate (orbit-pivot slerp) and observe (in-place quaternion slerp) branches move with it.\n- `updateAim` (stellata.ts:2846)\n- `updateObserveAim` (stellata.ts:2883)\n- `aimQ`, `aimTmpDir`, `observeAimQ` scratch state.\n- `AIM_T_MIN_MS` constant. `AIM_T_MAX_MS` lifts to `camera/constants.ts` in 194.1; AimController imports it.\n- `WARP_BASE_DIR` is shared with warp; it lives in `camera/constants.ts` too.\n\n# Code that STAYS in stellata.ts\n\n- `aimAtConstellation` — the constellation-centroid computation. Stays as a thin dispatcher into `this.aim.aimAt(centroid)`.\n\n# Public surface (finalised in 194.2)\n\n```ts\nclass AimController {\n  constructor(deps: {\n    camera: PerspectiveCamera\n    controls: TrackballControls\n    observeControls: ObserveControls\n    bus: EventBus\n    getCameraMode: () =\u003e CameraMode\n  })\n\n  aimAt(pointLocal: Vector3): void\n  cancel(): void                         // called by warp.start, focus changes\n  isActive(): boolean                    // navigate-mode aim\n  isObserveAimActive(): boolean\n  tick(nowMs: number): void              // navigate slot\n  tickObserve(nowMs: number): void       // observe slot\n  dispose(): void\n}\n```\n\n# Bus events\n\nNo new bus event in this bead. An `aim: boolean` event is a follow-up bead, filed only if/when an overlay or HUD needs it. Keeping .4 mechanically pure (move-only) reduces review risk.\n\n# Acceptance\n\n- stellata.ts no longer holds AimState / ObserveAimState fields or the aim tick methods.\n- `camera/aim-controller.test.ts` covers slerp lifecycle, cancellation mid-slerp, navigate vs observe branches, dispose tear-down.\n- The `AIM_T_MIN_MS` (250 ms) floor case + the `2·acos(|q0·q1|)` formula are pinned with toBe(N) per stellata-test-coverage-discipline.\n- typecheck + vitest pass.\n\n# Smoke\n\n- Keyboard R / G / C (aim slerps to a target in navigate).\n- HUD Sol/GC arrow labels (observe + navigate).\n- aimAtConstellation in navigate (constellation picker change handler).\n- observe-mode double-click → aimAt(unprojected ray).\n- Camera busy mid-aim: try to launch another aim → suppressed.\n\n# Dependencies\n\nBlocked by 194.3 (Picker extracted first — bottom-up rule).\n\n# Connects\n\n- [[stellata-folder-and-controller-conventions]] rule (3) — controllers extracted at write time.\n- [[stellata-consistency-at-the-seam]] lifecycle pairing — every `bus.on(...)` and `observeControls.disable()` pair with their teardown in the same diff.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:41:01Z","created_by":"Alex Marshall","updated_at":"2026-05-17T10:33:54Z","started_at":"2026-05-17T08:31:31Z","closed_at":"2026-05-17T10:33:54Z","close_reason":"Closed","labels":["architecture","camera","cleanup","docs"],"dependencies":[{"issue_id":"stellata-9mm.194.4","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T17:41:01Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.194.4","depends_on_id":"stellata-9mm.194.3","type":"blocks","created_at":"2026-05-16T17:42:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-lo5.6","title":"Heliopause apex hover provider + pickbox","description":"Add hover pick + label for the heliopause apex marker.\n\n# Scope\n\n- `src/client/heliopause.ts` — new `pickApex(clientX, clientY, pxThreshold) → HoverHit | null`:\n  - Project `HELIOPAUSE_APEX_LOCAL_PC` to screen space.\n  - Fallback-only (treat as a labelled point — no rendered disc).\n  - Gated on the same visibility predicate the apex label uses — drawn iff the apex marker would be visible. NO additional focus-state gate per [[stellata-lo5-hover-conventions]] (Rule 2: visibility ⇒ hoverable).\n- `src/client/hover/formatters/heliopause-hover-format.ts` — fields: \"Heliopause apex\"; distance from Sol (122 AU constant via `UPWIND_APEX_AU`); short boundary descriptor (\"Upwind termination region (asymmetric ~115–161 AU)\"). Apply [[stellata-lo5-hover-conventions]] Rule 1 (spell out terms — e.g. \"Distance from Sol 122 AU\" rather than \"D 122 AU\").\n- `src/client/hover/formatters/heliopause-hover-format.test.ts` — golden output (string-stable).\n\n# Acceptance\n\n- Pan camera so apex marker is on-screen → hovering it surfaces the apex card; hovering elsewhere does not.\n- Provider gates on the apex marker's visibility predicate, not on focus state.\n\nShips as part of lo5 single-PR rollup; close on PR merge. Depends on lo5.2.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:40:47Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:57Z","started_at":"2026-05-16T19:37:14Z","closed_at":"2026-05-16T21:38:57Z","close_reason":"merged via PR #72 (commits 10-11: heliopause apex provider + silhouette/label hit surface)","labels":["picker","solar-system","ui","ux"],"dependencies":[{"issue_id":"stellata-lo5.6","depends_on_id":"stellata-lo5","type":"parent-child","created_at":"2026-05-16T17:40:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-lo5.5","title":"Local Group hover provider + pickbox","description":"Add hover pick + label for Local Group wireframes.\n\n# Scope\n\n- `src/client/local-group.ts` — new `pick(clientX, clientY, pxThreshold) → HoverHit | null`:\n  - Project each object's `centerAbs - worldOffset` to screen space.\n  - Estimate screen-extent from semi-axes + orientation quat (max projected radius).\n  - Two-tier: prime if cursor inside the projected envelope; fallback if within `pxThreshold` of projected centroid.\n- `src/client/hover/formatters/local-group-hover-format.ts` — fields: name, kind (dSph / dE / dIrr / disc — `kind` field already in JSON, plus override-display-name rules from local-group-loader), distance from Sol via `fmtDist` (auto Mpc/kpc/pc), size summary (semi-axes major × minor in pc / kpc / Mpc).\n- `src/client/hover/formatters/local-group-hover-format.test.ts` — golden output for M31 (disc kind), LMC (override), Sgr dSph (ellipsoid kind), a faint LVDB dwarf.\n\n# Acceptance\n\n- Hovering each LG object surfaces correct name/kind/distance/size.\n- LMC display name resolves via override (not LVDB's raw entry).\n\n# Out of scope\n\n- Apparent V mag — blocked by stellata-7de (LG JSON schema has no `M_V` plumbed yet). Add the line in a follow-up bead once 7de lands.\n\n# Smoke\n\n- Camera at Local Group scale; hover M31, LMC, Sgr dSph, a faint dwarf.\n\nShips as part of lo5 single-PR rollup; close on PR merge. Depends on lo5.2.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:40:40Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:52Z","started_at":"2026-05-16T17:46:09Z","closed_at":"2026-05-16T21:38:52Z","close_reason":"merged via PR #72 (commit 9: Local Group provider + pickbox + formatter)","labels":["picker","ui","ux"],"dependencies":[{"issue_id":"stellata-lo5.5","depends_on_id":"stellata-lo5","type":"parent-child","created_at":"2026-05-16T17:40:39Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-lo5.4","title":"Sol planet hover provider + per-instance pickbox","description":"Add per-instance hover pick + label for Sol's planets.\n\n# Scope\n\n- `src/client/planet-body-field.ts` — `pick(camera, rect, x, y, t)` walking every attached host's planets (no focus gate per Rule 2 of [[stellata-lo5-hover-conventions]]):\n  - Project each per-instance `iLocalRel + iHostLocalPos` to screen space.\n  - Compute screen-pixel distance to centroid and the rendered disc radius from `iRadiusPc` (mirrors star approach).\n  - Two-tier: prime if inside disc (cursor \u003c rendered radius); fallback if within `pxThreshold`.\n  - Skip planets whose appMag exceeds the slider cutoff + 0.5 mag (same kill condition the planet vertex shader uses to drop the quad).\n- Expose a per-instance apparent V mag getter that mirrors the shader-side calc (already mirrored by `planetApparentMagnitude` in `perceptual-magnitude.ts`; reuse rather than duplicating).\n- `src/client/hover/formatters/planet-hover-format.ts` — `formatPlanetHover(planetIdx, ctx)`. Final layout per [[stellata-lo5-hover-conventions]] Rule 1a:\n\n      \u003cPlanet name\u003e\n      \u003chost distance\u003e · Vmag \u003cm\u003e\n      Period \u003cyears\u003e yr\n      Radius \u003ckm\u003e km\n\n- `src/client/hover/formatters/planet-hover-format.test.ts` — golden output for Mercury / Earth / Jupiter / Pluto across `fmtDistAuto` tier transitions + +sign positive-mag case + null-input degenerate guard.\n\n# Visibility gate (Rule 2 of [[stellata-lo5-hover-conventions]])\n\nNo focus-state gate. The provider walks every attached host; visibility is the only predicate (matches the renderer's draw decision). v1 attaches Sol once at startup and Sol stays attached for the session, so the picker effectively walks Sol's planets — regardless of which star (if any) is focused.\n\n# Acceptance\n\n- Hovering each visible Sol planet surfaces correct name/distance/Vmag/period/radius.\n- Vmag matches the shader-side disc calculation exactly.\n- Provider does NOT gate on Sol being the focused host. Hovering a Sol planet while focused on (say) Vega still surfaces the planet card if the planet is visible.\n\n# Smoke\n\n- Default first-load view (Sol focus); hover each of the 9 planets through Pluto.\n- Focus a distant star; verify Sol's planets still hover-pickable when they're on-screen and visible.\n\nShips as part of lo5 single-PR rollup; close on PR merge. Depends on lo5.2.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:40:30Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:49Z","started_at":"2026-05-16T17:10:30Z","closed_at":"2026-05-16T21:38:49Z","close_reason":"merged via PR #72 (commits 4-5: hoist MIN_DISC_HIT_RADIUS_PX + planet provider/pickbox)","labels":["picker","solar-system","ui","ux"],"dependencies":[{"issue_id":"stellata-lo5.4","depends_on_id":"stellata-lo5","type":"parent-child","created_at":"2026-05-16T17:40:30Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-lo5.3","title":"Star hover provider (extract existing path)","description":"Extract the existing star hover affordance into a `StarHoverProvider` implementing `HoverProvider`.\n\n# Scope\n\n- `src/client/hover/formatters/star-hover-format.ts` — port `describeStarDetailed` (`main.ts:225–247`) verbatim. Pure function `formatStarHover(idx, ctx) → { name, lines[] }`.\n- `src/client/hover/formatters/star-hover-format.test.ts` — golden output for: (1) a named non-variable (e.g. Vega), (2) a variable (e.g. Mira: period + Δmag line present), (3) an unnamed catalog star (HIP/HD fallback in name line).\n- `StarHoverProvider` (lives in the hover module) wraps `Stellata.pickStar(x, y, 14)` and the existing pickbox path verbatim — no behavioural change.\n\n# Acceptance\n\n- Star hover output byte-identical to pre-refactor.\n- Star formatter unit-tested.\n\nShips as part of lo5 single-PR rollup; close on PR merge. Depends on lo5.2 (engine + types).","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:40:22Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:47Z","started_at":"2026-05-16T17:01:38Z","closed_at":"2026-05-16T21:38:47Z","close_reason":"merged via PR #72 (commit 3: star provider + pure formatter)","labels":["picker","ui","ux"],"dependencies":[{"issue_id":"stellata-lo5.3","depends_on_id":"stellata-lo5","type":"parent-child","created_at":"2026-05-16T17:40:21Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-lo5.2","title":"Hover engine + disambiguator hoist","description":"Implement the shared hover-label engine and hoist the picker disambiguation logic.\n\n# Scope\n\n- New directory `src/client/hover/`:\n  - `hover-types.ts` — HoverProvider, HoverHit, HoverPayload types.\n  - `hover-engine.ts` — pointermove listener + 280 ms timer + provider registry + dispatch + #tooltip render. Replaces bindHoverTooltip body in main.ts.\n  - `hover-pick-disambiguator.ts` — cross-provider rule: prime always beats fallback; within tier, smallest cameraDistancePc wins. Sits on top of the generalised pickFromCandidates in star-geometry.ts.\n  - `hover-pick-disambiguator.test.ts` — prime \u003e fallback; camera-distance tie-break; empty/single/multi-hit.\n- Generalise `pickFromCandidates` in `star-geometry.ts:127–150` with a `scoreFn` parameter so non-star providers can pass their own scorer. Existing star caller passes `pickScore`. `pickScore` stays in star-geometry.ts (star-specific sub-pixel mag bias).\n- Replace the hover-side inline pick at `main.ts:285-286` with engine dispatch through the disambiguator. Only the star provider is registered at this bead's landing (others arrive in lo5.3–lo5.6).\n- Click-side inline pickStar→pickCloud at `stellata.ts:3309–3310` stays as-is — it has a richer downstream click-state machine that doesn't share scope with hover. `observeSingleClick` (`stellata.ts:3404`) is already star-only; nothing to change there.\n- Preserve `HOVER_DELAY_MS = 280` and 14 px threshold verbatim.\n\n# Acceptance\n\n- All existing star hover behaviour identical (visually + manual smoke).\n- `hover-pick-disambiguator` unit-tested.\n- `pickFromCandidates` now reusable by future providers via scoreFn parameter.\n\n# Smoke\n\n- Hover Vega + Mira + an unnamed catalog star: tooltip shows same fields as before.\n\nShips as part of the lo5 single-PR rollup; close on PR merge.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:40:14Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:44Z","started_at":"2026-05-16T16:41:37Z","closed_at":"2026-05-16T21:38:44Z","close_reason":"merged via PR #72 (commits 1-2: star-geometry refactor + hover engine/disambiguator)","labels":["picker","ui","ux"],"dependencies":[{"issue_id":"stellata-lo5.2","depends_on_id":"stellata-lo5","type":"parent-child","created_at":"2026-05-16T17:40:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.194.1","title":"Folder reorganisation — flat src/client/* → thematic subfolders","description":"Mechanical move of src/client/*.ts files into thematic subfolders per the 194.2 design-gate taxonomy. SINGLE PR — partial folder moves create transitional confusion. Also renames `warp-constants.ts` → `camera/timing.ts` (the file already holds camera-wide lerp constants beyond just warp) and lifts `WARP_BASE_DIR` + `DCAM_LOG_FLOOR_PC` from stellata.ts into it.\n\n# Taxonomy (finalised in 194.2; reconciled with origin/main as of PR #72)\n\nTwo folder categories. Per-subsystem folders own the renderer + loader + tuning + pure helpers + tests for that subsystem. Cross-cutting type folders own plumbing that isn't owned by any single subsystem. hover/ already exists from #72 and stays as-is (validates rule 1; its formatters/ sub-subfolder is a precedent).\n\n- **Per-subsystem folders:**\n  - `solar-system/` — planet-system, orbit-rings-layer, planet-body-field, perceptual-magnitude, perceptual-magnitude.test, planet-labels, time, time.test, time-readout, time-readout.test, ephemeris, ephemeris.test, astronomy-constants, astronomy-constants.test, heliopause, heliopause.test, first-load, first-load.test, phase-function, phase-function.test\n  - `local-group/` — local-group, local-group.test, local-group-loader, local-group-loader.test, local-group-tuning\n  - `milkyway/` — milkyway, milkyway-tuning\n  - `galactic/` — galactic-disc, galactic-fade, galactic-fade.test, galactic-grid, galactic-coords\n  - `molecular-clouds/` — molecular-clouds, molecular-clouds.test, cloud-loader (moved from loaders/)\n  - `chart-mode/` — chart-mode, chart-labels, chart-labels.test, **chart-disc-pure, chart-disc-pure.test** (new from #66)\n  - `hover/` — UNCHANGED (already in place from #72): hover-engine, hover-types, hover-pick-disambiguator + test, star/cloud/planet/local-group/heliopause-hover-provider, formatters/* (5 files + 5 tests)\n\n- **Cross-cutting type folders:**\n  - `overlays/` — constellation-overlay, disc-mask, disc-mask-pure, disc-mask-pure.test, distance-vector-overlay, distance-vector-overlay.test, focus-ring-overlay, hud-overlay, hud-overlay.test, poi-overlay, dirty-attr, dirty-attr.test, overlay-project, arrow-fade, arrow-fade.test, arrow-path, arrow-path.test\n  - `camera/` — controls, controls.test, observe-controls, observe-controls.test, focus-transition, focus-transition.test, **focus-target** (new from #66), **arrival-curves, arrival-curves.test** (new from #66), **camera-motion, camera-motion.test** (new from #66), warp-pure, warp-pure.test, warp-button, mode-toggle, camera-up-align.test, star-geometry, star-geometry.test, **warp-tuning** (new from #66), **NEW: camera/timing.ts (renamed from warp-constants.ts; adds WARP_BASE_DIR + DCAM_LOG_FLOOR_PC lifted from stellata.ts)**\n  - `loaders/` — catalog-loader, catalog-loader.test, dust-loader (cloud-loader moved to molecular-clouds/, local-group-loader stays in local-group/)\n  - `ui/` — panel-layout, scale-bar, theme-toggle, unit-toggle, distance-util, distance-util.test, distance-gated-label, distance-gated-label.test, keyboard-shortcuts, keyboard-shortcuts-pure, keyboard-shortcuts-pure.test, dom-util, dom-util.test\n  - `util/` — event-bus, event-bus.test, url-state, url-state.test\n  - `typeahead/` — typeahead, typeahead-util, typeahead-util.test, constellation-typeahead, constellation-typeahead.test, search, search.test\n  - `modals/` — info-modal, brand-modal, help-modal, modal-dismiss\n  - `debug/` — debug, debug-panel, perf-hud, perf-hud.test, pin-debug-hud, arrow-fade-debug-hud, star-tuning\n\n- **Root kept flat:** main.ts, stellata.ts, index.html, styles.css, globals.d.ts, shaders/, stellata-events.test.ts, disc-blend.test.ts (last moves with 9mm.43).\n\n# camera/timing.ts (renamed from warp-constants.ts in this PR)\n\nThe on-main `warp-constants.ts` already covers camera-wide timing — its first comment says \"Warp + camera-lerp timing constants.\" Rename clarifies. Module owns:\n- CAMERA_LERP_MS (canonical 2 s; FOCUS_LERP_MS aliases it)\n- WARP_T_MIN_MS, WARP_T_MAX_MS, WARP_T_K_MS, WARP_REORIENT_MS\n- AIM_T_MIN_MS, AIM_T_MAX_MS\n- OBSERVE_TRANSITION_MS\n- **NEW (lifted from stellata.ts in this PR):** WARP_BASE_DIR (Vector3), DCAM_LOG_FLOOR_PC\n\nTests import from here; never redefine. warp-tuning.ts already imports from warp-constants today — its import seam updates.\n\n# Co-location\n\nTests + *-pure.ts siblings move alongside their parent file. Shaders stay in src/client/shaders/.\n\n# Acceptance\n\n- src/client/ root contains only main.ts, stellata.ts, index.html, styles.css, globals.d.ts, shaders/, stellata-events.test.ts, disc-blend.test.ts.\n- All imports updated to new paths.\n- camera/timing.ts exports the constants listed above (including the two new lifts); stellata.ts imports from it.\n- typecheck passes.\n- vitest passes.\n- CLAUDE.md \"Repo layout\" block updated to reflect new layout (per defer-doc-updates rule, this is the right PR for that doc edit).\n\n# Smoke\n\nFull manual UX pass per stellata-smoke-tests:\n- Focus a star (Sirius), warp to it, unfocus.\n- Toggle navigate ↔ observe.\n- Open debug panel + a couple of tuning sections (including warp-tuning).\n- Toggle chart mode in observe.\n- Constellation overlay on, then off.\n- Hover over stars / Local Group dwarfs / heliopause apex → tooltip works (no regression from the import-path churn).\n\n# Dependencies\n\nBlocked by 194.2 (taxonomy + camera/timing.ts naming finalised in design gate).\n\n# Connects\n\n- [[stellata-folder-and-controller-conventions]] — rules (1) and (2); CLAUDE.md update reinforces.\n- [[stellata-named-constants-and-dry]] — camera/timing.ts is the canonical source application.","notes":"Implemented in PR #74 (bundled with 194.2 per Alex's directive to use the existing design-gate worktree). 117 git mvs into 14 thematic folders + warp-constants → camera/timing rename + WARP_BASE_DIR/DCAM_LOG_FLOOR_PC lift + CLAUDE.md repo-layout refresh. typecheck + 870 tests + production build all clean.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:39:53Z","created_by":"Alex Marshall","updated_at":"2026-05-17T07:50:06Z","started_at":"2026-05-16T21:49:15Z","closed_at":"2026-05-17T07:50:06Z","close_reason":"Closed","labels":["cleanup","docs"],"dependencies":[{"issue_id":"stellata-9mm.194.1","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T17:39:52Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.194.1","depends_on_id":"stellata-9mm.194.2","type":"blocks","created_at":"2026-05-16T17:42:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":5,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.194.2","title":"Design gate — controller boundaries + folder taxonomy + extraction sequence","description":"Design gate for stellata-9mm.194 (layout \u0026 integration-shell decomposition). Produces concrete bd updates that replace the \"TBD in 194.2\" sections of children .1/.3/.4/.5/.6/.8 with finalised public surfaces. No code change in this bead; no standalone design doc either — the controller architecture lives in code, the conventions in CLAUDE.md + bd memory, and each extraction PR updates its corresponding docs/*.md per [[defer-doc-updates]] as the boundary it documents stabilises.\n\n# Deliverables\n\n- bd updates to 194.1 / .3 / .4 / .5 / .6 / .8 with finalised public surfaces\n- New bd `stellata-9mm.194.9` filed for `camera/star-physics.ts` extraction (parallel-trackable post-.1, blocks .8)\n- New bd memory `stellata-folder-and-controller-conventions` (5 rules, loads every session via bd prime)\n- CLAUDE.md gets \"Folder \u0026 module conventions\" section (peer to \"Code conventions — DRY overrides the system prompt\"); section is self-contained and references the bd memory\n- Plan file in ~/.claude/plans/ holds the in-flight execution detail (which child PR consumes which dep, the FocusOps shim strategy in .5, etc.)\n\n# Scope of the design (in the plan file + child bead descriptions, not duplicated here)\n\n1. Folder taxonomy — per-subsystem folders + cross-cutting type folders + minimal root. Hovers/ already exists from PR #72; validates rule (1) of the conventions.\n2. Controller boundaries — Picker / Aim / Warp / ObserveTransition / Focus. Public surfaces, owned state, owned bus events, dispose contracts.\n3. Cross-cutting decisions — FrameAnchor stays on Stellata; click FSM stays on Stellata; camera/timing.ts (renamed from warp-constants.ts) is the shared-constants single source; star-physics extracts as own bead 194.9.\n4. Bottom-up sequence — .1 (folder reorg + camera/timing.ts rename) → .3 (Picker) → .4 (Aim) → .5 (Warp; takes stellata via FocusOps shim) → .6 (Observe) → .8 (Focus; absorbs the shim).\n5. Test strategy — per-controller state-machine + dispose tests; stellata-events.test.ts extended per step; star-physics numeric pinning in .9.\n\n# Reconciliation with origin/main\n\nTwo PRs merged during this session — #66 (stellata-2br, camera-arrival profile) and #72 (stellata-lo5, hover engine). Impact captured in the plan file + the refreshed child bead descriptions:\n- FocusTarget abstraction landed (focus-target.ts). FocusOps's surface uses currentFocusTarget() not currentFocusLocalPos().\n- WarpState refactored to use dest: FocusTarget; tryMidFlyRecentre + getWarpPhase + recenteredToDest field added.\n- warp-constants.ts grew to cover camera-wide timing constants — renames to camera/timing.ts in 194.1.\n- hover/ folder + 5 providers + 5 formatters + disambiguator already in place. The pickXxxHit methods on stellata.ts absorb into Picker (194.3).\n- New top-level files (arrival-curves, camera-motion, focus-target, warp-constants, warp-tuning, chart-disc-pure) added to 194.1's move list.\n\n# Acceptance\n\n- Children .1 / .3 / .4 / .5 / .6 / .8 carry concrete public surfaces in their descriptions (no \"TBD in 194.2\" remaining)\n- bd memory stellata-folder-and-controller-conventions is filed and readable via bd prime\n- CLAUDE.md \"Folder \u0026 module conventions\" section is in place and concise (rules 1-3 short form)\n- 194.9 (star-physics) filed with deps: blocked by .1, blocks .8\n\n# Smoke\n\nDesign review with Alex — no code changes in this bead. Doc-only PR with skip-version-bump label.\n\n# Connects\n\n- [[stellata-code-quality-epic]] — 9mm.194 sub-epic context.\n- [[stellata-folder-and-controller-conventions]] — the new memory this bead files.\n- [[defer-doc-updates]] — controller architecture goes in docs/*.md per child PR, not in a one-shot doc here.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:39:53Z","created_by":"Alex Marshall","updated_at":"2026-05-17T07:50:06Z","started_at":"2026-05-16T16:50:11Z","closed_at":"2026-05-17T07:50:06Z","close_reason":"Closed","labels":["architecture","cleanup","docs"],"dependencies":[{"issue_id":"stellata-9mm.194.2","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T17:39:52Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.194.3","title":"Extract Picker from stellata.ts (target resolution; click FSM stays in stellata.ts)","description":"Move all per-kind pick paths from stellata.ts into `camera/picker.ts` (folder home finalised in 194.1). Picker is a **pure resolver**: it answers \"what's under (x, y)?\" and nothing more. The click-state machine + observe single/double-click handlers STAY in stellata.ts as composition-layer orchestration. The hover engine (`src/client/hover/hover-engine.ts`, already in place from PR #72) wraps Picker's hover-pick methods via its provider classes.\n\n# Code to move\n\nClick picks:\n- `pickStar`\n- `pickCloud`\n\nHover picks (from PR #72; consumed by hover/*-hover-provider.ts):\n- `pickStarHit`\n- `pickCloudHit`\n- `pickPlanetHit`\n- `pickLocalGroupHit`\n- `pickHeliopauseHit`\n\nPlus:\n- `sortedDistRange` helper — used by pickStar AND by `shouldEnableCoreMask`. Stays exported from picker for the core-mask gate's use OR lifts to camera/star-geometry.ts as a windowed-scan primitive. Pick one in implementation; either is fine as long as there's no duplication.\n- `MIN_DISC_HIT_RADIUS_PX` constant.\n- `cloudRaycaster` + `tmpNdc` scratch state.\n- Heliopause sample-points scratch.\n\n# Code that STAYS in stellata.ts (intentionally)\n\n- `Target` type union + `sameTarget` (composition-shell state — used by onPointerUp).\n- `onPointerUp` click-state machine.\n- `handleObserveClick` + `observeSingleClick` + `observeDoubleClick`.\n- `dblClickRay` / `dblClickAimPoint` scratch state.\n\n# Existing extracts to compose\n\n- `star-geometry.ts` exports `pickFromCandidates`, `angularToPx`, `physSizePx`, type `PickCandidate`. Picker imports these.\n- `hover-pick-disambiguator.ts` consumes the HoverHits Picker returns (no Picker-side dependency on the disambiguator — Picker just produces hits).\n\n# Public surface (finalised in 194.2)\n\n```ts\nimport type { HoverHit } from '../hover/hover-types';\n\nclass Picker {\n  constructor(deps: {\n    domElement: HTMLElement\n    camera: PerspectiveCamera\n    catalog: Catalog\n    sortedByDistFromSol: Uint32Array\n    sortedDistFromSol: Float32Array\n    getLocalPositions: () =\u003e Float32Array\n    getFilter: () =\u003e Readonly\u003cFilterState\u003e\n    getClouds: () =\u003e MolecularClouds | null\n    getLocalGroupLayer: () =\u003e LocalGroupLayer | null\n    getHeliopause: () =\u003e Heliopause\n    getPlanetBodyField: () =\u003e PlanetBodyField\n    getWarpActive: () =\u003e boolean\n    renderedSizePxFn: (idx: number) =\u003e number   // until 194.9 lifts to camera/star-physics.ts\n    fovYRadRef: { value: number }\n    viewportRef: { value: Vector2 }\n  })\n\n  // Click picks\n  pickStar(clientX: number, clientY: number, pixelThreshold?: number): number  // -1 = miss\n  pickCloud(clientX: number, clientY: number): number | null\n\n  // Hover picks (HoverHit carries tier + cameraDistancePc for cross-provider disambiguation)\n  pickStarHit(clientX: number, clientY: number, pxThreshold?: number): HoverHit | null\n  pickCloudHit(clientX: number, clientY: number, pxThreshold?: number): HoverHit | null\n  pickPlanetHit(clientX: number, clientY: number, pxThreshold?: number): HoverHit | null\n  pickLocalGroupHit(clientX: number, clientY: number, pxThreshold?: number): HoverHit | null\n  pickHeliopauseHit(clientX: number, clientY: number, pxThreshold?: number): HoverHit | null\n}\n```\n\nNo state, no bus events, no dispose.\n\n# Acceptance\n\n- stellata.ts no longer holds pickStar / pickCloud / pickStarHit / pickCloudHit / pickPlanetHit / pickLocalGroupHit / pickHeliopauseHit as methods.\n- The click FSM in onPointerUp now calls `this.picker.pickStar(...)`.\n- hover/*-hover-provider.ts files update their pick() implementations to call `picker.pickStarHit(...)` etc. instead of `stellata.pickStarHit(...)`.\n- New `camera/picker.test.ts`: click picks (prime + fallback, cluster + Double-Double, spectral-mask, distance window); hover picks (basic round-trip + cameraDistancePc ordering correctness — the disambiguator depends on it).\n- typecheck + vitest pass.\n\n# Smoke\n\n- Click stars + clouds in NAVIGATE mode → focus + vector-to behaviour unchanged.\n- Click stars + clouds in OBSERVE mode → POI + aim behaviour unchanged.\n- Double-Double (ε¹/ε² Lyrae) — both members independently clickable when discs overlap.\n- Hover stars / planets / clouds / Local Group dwarfs / heliopause apex → tooltips render with correct content (no regression from the import-path churn).\n- 9mm.115 cloud-picker asymmetry is OUT of scope here — flag it in the PR description if the extraction touches that code path.\n\n# Dependencies\n\nBlocked by 194.1 (folder reorg + camera/timing.ts rename lands first).\n\n# Connects\n\n- [[stellata-folder-and-controller-conventions]] rule (3).\n- [[stellata-pattern-coverage-across-peers]] — every pick path landed; no per-kind hover/click pick remains on Stellata.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:39:53Z","created_by":"Alex Marshall","updated_at":"2026-05-17T08:30:11Z","started_at":"2026-05-17T07:52:48Z","closed_at":"2026-05-17T08:30:11Z","close_reason":"Closed","labels":["architecture","cleanup","docs"],"dependencies":[{"issue_id":"stellata-9mm.194.3","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T17:39:52Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.194.3","depends_on_id":"stellata-9mm.194.1","type":"blocks","created_at":"2026-05-16T17:42:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.194","title":"Layout \u0026 integration-shell decomposition","description":"Themed sub-epic of stellata-9mm focused on the two biggest LLM-context-cost reductions identified 2026-05-16: (a) flat src/client/ → thematic subfolders mirroring the docs/*.md taxonomy; (b) splitting the 3947-line stellata.ts integration shell into a thin composition layer + focused controller classes; (c) trimming multi-paragraph in-code prose by relocating physics/derivations to SCIENCE.md + docs/*.md.\n\n# Scope\n\n- Folder reorganisation: src/client/* → overlays/, solar-system/, camera/, local-group/, milkyway/, galactic/, chart-mode/, debug/, modals/, typeahead/, loaders/, ui/, util/. Single mechanical PR; no logic changes.\n- Controller extraction from stellata.ts: Picker, AimController, WarpController, ObserveTransition, FocusController. Bottom-up order — leaves first, integration shell last.\n- Render-pipeline extracts (StarPipeline = 9mm.43, DustParticleLayer = 9mm.70) reparented here as siblings; they can land in parallel with the controller chain because they touch resource ownership rather than animation state.\n- Comment density audit: multi-paragraph in-code derivations relocate to SCIENCE.md / docs/*.md with code-side pointers.\n\n# Why a dedicated epic\n\nTheme is 'reduce the cost of working in the integration shell' for BOTH LLM context (smaller files per task) AND human review (each controller in isolation, not one 4kloc class). Distinct from 9mm.192 which is heterogeneous 'core internals' with catalog + picker tests mixed in.\n\n# Extraction sequence (bottom-up)\n\nThe bd IDs assigned out of intent order — execution order is by dep graph (use `bd ready`), not by .N suffix:\n\n1. **194.2** — Design gate (folder taxonomy + controller boundaries + sequence)\n2. **194.1** — Folder reorganisation (mechanical, single PR)\n3. **194.3** — Extract Picker (leaf, no animation-state deps)\n4. **194.4** — Extract AimController\n5. **194.5** — Extract WarpController (consolidates warp-pure.ts)\n6. **194.6** — Extract ObserveTransition\n7. **194.8** — Extract FocusController (most integrated — last)\n\nParallel tracks:\n- **194.7** — Comment density audit (independent file edits; runs any time after 194.1 lands)\n- **9mm.43** (StarPipeline) + **9mm.70** (DustParticleLayer) — render-pipeline extracts; reparented here as siblings, can run in parallel with the state-controller chain (touch resource ownership, not animation state).\n\n# Acceptance\n\n- stellata.ts under 1500 lines (from 3947).\n- src/client/ root contains only main.ts, stellata.ts, index.html, styles.css, globals.d.ts, shaders/.\n- CLAUDE.md module roster reflects new layout.\n\n# Priority\n\nP2 default — context-cost reductions feed directly back into Alex's iteration speed. Individual children can sit at P3 if not blocking in-flight work.\n\n# Connects\n\n- [[stellata-code-quality-epic]] — meta umbrella; this memory needs the 194 reference added.\n- [[stellata-consistency-at-the-seam]] — lifecycle pairing in every extracted controller.\n- [[stellata-named-constants-and-dry]] — extracted constants stay at canonical source modules.\n- [[stellata-rename-and-stale-prose-sweep]] — 194.7 directly targets the same drift class.\n- [[defer-doc-updates]] — CLAUDE.md module roster updates land in 194.1's PR.","status":"closed","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:39:06Z","created_by":"Alex Marshall","updated_at":"2026-05-17T16:09:30Z","closed_at":"2026-05-17T16:09:30Z","close_reason":"All 11 children complete (9mm.194.1-9 + 9mm.43 StarPipeline + 9mm.70 DustParticleLayer). Integration-shell decomposed: stellata.ts dropped from ~3947 to ~2262 lines, six controller classes extracted to camera/, render-pipeline extracts to src/client/, folder taxonomy + comment-density audit landed.","labels":["cleanup","docs"],"dependencies":[{"issue_id":"stellata-9mm.194","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-16T17:39:05Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2br.8","title":"Trapezoidal arrival curve (log-d cruise zone)","description":"## Why\n\nSmoke testing of PR #66's `cubic-hermite` log-d arrival profile shows the\ncamera appears to come to a standstill in the last ~10–20 % of warp time\neven though log-d is still being covered. With `f(u) = u²(3 − 2u)` slope\nfalls from 1.5 at u=0.5 to 0 at u=1, while the disc renderer kicks in\nmid-warp (Sol from 10 pc: u ≈ 0.46; Betelgeuse from 200 pc: u ≈ 0.59).\nThe back half of every warp is spent inside \"disc visible\" territory\nwith progressively slower angular-size growth — that collapse-to-zero in\nlog-d velocity reads as \"we stopped.\"\n\n## Goal\n\nAdd a `'trapezoid'` arrival curve to `arrival-curves.ts`: quadratic accel\nramp → linear cruise → quadratic decel ramp, all in log-d. Two tunable\nwidths (t_accel, t_decel) parameterise the spectrum. Defaults\n`t_accel = 0.15`, `t_decel = 0.10` give a sustained cruise plus the\n~10 % decel window Alex asked for, with a smooth gentle launch and a\nzero-velocity arrival preserved.\n\n## Math\n\n    v = 1 / (1 − t_accel/2 − t_decel/2)\n\n    u ∈ [0, t_accel]:        f(u) = (v/(2·t_accel)) · u²\n    u ∈ [t_accel, 1-t_decel]: f(u) = v·u − v·t_accel/2\n    u ∈ [1-t_decel, 1]:       s = (u − (1−t_decel))/t_decel\n                              f(u) = 1 − (v·t_decel/2)·(1 − s)²\n\nC¹-continuous at both joins; f(0)=f'(0)=0, f(1)=1, f'(1)=0. The\ndegenerate case `t_accel=t_decel=0.5` recovers the legacy main-branch\npiecewise-quadratic feel (constant accel/decel, no cruise).\n\n## Plan reference\n\nFull design in\n`/Users/alexm/.claude/plans/ok-the-curve-definitely-indexed-ladybug.md`.\n\n## Files\n\n- `src/client/arrival-curves.ts` — add trapezoid curve + extend\n  `resolveArrivalCurve` signature for the two widths.\n- `src/client/arrival-curves.test.ts` — endpoints, slope continuity at\n  both joins, degenerate-case match against piecewise-quad, monotonicity.\n- `src/client/warp-tuning.ts` — add `'trapezoid'` to dropdown, two\n  sliders (gated like the `p` slider), summary line.\n- `docs/camera-arrival.md` § new \"Trapezoidal alternative\" subsection.\n- `docs/camera-warp.md` § Fly — curve description cross-ref.\n- `package.json` — 1.10.2 → 1.11.0.\n\n## Smoke\n\nManual on Alex's browser (no dev server):\n- `/?debug=1`, expand warp section. Switch curve to `trapezoid`.\n  Verify two new sliders appear, `p` slider hides.\n- Warp Sol from ~10 pc with trapezoid default — close-approach should\n  feel like sustained zoom-in until the last ~10 %.\n- `t_accel=t_decel=0.5` reproduces legacy main-branch warp feel.\n- `t_decel=0.05` — landing should still feel smooth, not slam.\n- Chart-mode warp (Scenario 10 from PR #66) — plateau trigger fires\n  against camera position, not u, so behaviour unchanged.\n\n## PR\n\nStack on PR #66 (umbrella).","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T15:49:15Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:01:30Z","started_at":"2026-05-16T15:49:24Z","closed_at":"2026-05-16T21:01:30Z","close_reason":"shipped in PR #66 (commit 25e4b8b)","labels":["arrival-curve","camera","camera-warp","pr-66","ux"],"dependencies":[{"issue_id":"stellata-2br.8","depends_on_id":"stellata-2br","type":"parent-child","created_at":"2026-05-16T16:49:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-0hx.2","title":"Camera envelope + distance formatting + fade-band refactor for Mpc/Gpc scales","description":"**Goal:** lift stellata's hard-coded camera envelope + distance formatter + fade bands so subsequent tier impls just pass a regime parameter rather than touch core code. Architectural enabler for stellata-0hx.\n\n# Why\n\nToday `PerspectiveCamera.far = 500000` and `TrackballControls.maxDistance = 250000` are magic literals in `src/client/stellata.ts:670-689`. `fmtDistAuto` in `src/client/distance-util.ts:40-50` switches at \"k pc\" past 10 kpc but has no Mpc/Gpc/Gly branch. `galactic-fade.ts` exports a single FADE_INNER_PC/FADE_OUTER_PC pair that works for MW + LG only.\n\nEach subsequent tier (.3 LV, .4 LSC, .5 Lan, .6 PCs, .7 BOSS, .8 CMB) would otherwise need a code edit to `stellata.ts`. Lift once, parameterise per regime.\n\n# Scope\n\n1. **Camera envelope: regime enum** — introduce `CameraRegime` = STELLAR / GALACTIC / SUPERCLUSTER / COSMIC with per-regime `{far, maxDistance, near}` tuples. The regime is chosen at runtime based on camera distance from Sol (with hysteresis bands to avoid flicker on the boundary). Initial set:\n   - STELLAR: near=1e-10, far=500_000 (today's values)\n   - GALACTIC: near=1e-6, far=3e6 (3 Mpc — stellata-1ui's reach)\n   - SUPERCLUSTER: near=1, far=3e8 (300 Mpc — Tier 3)\n   - COSMIC: near=1e3, far=2e10 (20 Gpc — Tiers 5–6)\n2. **fmtDistAuto extension** — add Mpc/Gpc + Mly/Gly branches to `src/client/distance-util.ts`. Today's pc/ly threshold logic stays; new format kicks in past 1 Mpc.\n3. **Fade-band builder** — refactor `src/client/galactic-fade.ts` to export `FadeBand(inner, outer, sample)` rather than free constants. Existing `FADE_INNER_PC` / `FADE_OUTER_PC` consumers (`galactic-disc.ts`, `local-group.ts`) migrate. New constants `LG_FADE_BAND`, `LV_FADE_BAND`, `LSC_FADE_BAND` defined for subsequent tiers. Per [[stellata-named-constants-and-dry]] — extract at second usage, parameterise the differences.\n4. **Log-depth precision smoke** — verify `near=1e-10` doesn't break log-depth at `far=2e10`. May need to raise near per regime (already in the regime tuple above).\n\n# Acceptance\n\n- `npm run typecheck` + `npm test` green.\n- New unit tests for `CameraRegime` resolution + `fmtDistAuto` Mpc/Gpc branches.\n- No visual regression: at default zoom (Sol focal), behavior identical to today.\n\n# Smoke (per [[alex-stellata-smoke-tests]])\n\n- Default first-load: identical to today.\n- Pull camera back past 250 kpc: regime auto-transitions to SUPERCLUSTER, no jitter.\n- Distance readout switches to Mpc past 1 Mpc, Mly past 3 Mly.\n- Manual zoom to 100 Mpc out: no log-depth precision failure (foreground stars still render correctly).\n\n# Out of scope\n\n- Actual far-field rendering — that's .3+. This bead is plumbing only.\n- Data pipeline — .3.\n\n# Blocks\n\nstellata-0hx's tier-impl children (.3 through .8).\n\n# Blocked by\n\nstellata-0hx.\u003cdesign-gate-child\u003e (the .1 child) — confirm regime cutoffs.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:53:30Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:53:30Z","labels":["camera","deep-field","extragalactic","galactic","refactor","research","visual"],"dependencies":[{"issue_id":"stellata-0hx.2","depends_on_id":"stellata-0hx","type":"parent-child","created_at":"2026-05-16T15:53:30Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.2","depends_on_id":"stellata-0hx.1","type":"blocks","created_at":"2026-05-16T15:56:03Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":6,"comment_count":0}
{"_type":"issue","id":"stellata-0hx.1","title":"Research + design-gate: extragalactic deep-field roadmap doc","description":"**Goal:** write `docs/extragalactic-roadmap.md` capturing data inventory, tier breakdown, naming policy, cosmology choice, manifest schema, and per-tier data-source URLs. The design-gate for the rest of stellata-0hx's children.\n\nPer [[stellata-prioritization-framework]] — design-gate of a layer epic = parent's priority (P2). All implementation children sit at P3 behind this.\n\n# Why\n\nThe parent epic (stellata-0hx) describes WHAT to build at each tier. This bead writes the durable in-repo design document that captures HOW: cosmology bake, naming-vs-PGC policy, manifest schema, per-tier data fetch instructions, selection-bias documentation for BOSS-tier. Future sessions execute against the doc instead of re-deriving from the epic body.\n\n# Scope\n\n`docs/extragalactic-roadmap.md` covering:\n\n1. **Per-tier data inventory** — UNGC, Pace LVDB, EVCC, 2MRS, Cosmicflows-4, Dupuy 2023 watersheds, NED-LVS, SDSS Main, BOSS/eBOSS, Planck SMICA. URL, retrieval steps, expected file size, licence, citation. Per [[frozen-external-data]], each must commit-once via a one-off `scripts/refresh-\u003ctier\u003e.{ts,py}` excluded from `npm run build`.\n\n2. **Manifest schema** for committed extragalactic catalogues under `data/extragalactic/`:\n   - Per-galaxy record: position (ICRS xyz pc), distance, M_K (or apparent K), morph_type (Hubble T), name (or null), PGC ID, source tier.\n   - Tier 3+ uses binary format (parallel to `public/catalog.bin`); Tier 1–2 uses JSON (parallel to `public/local-group.json`).\n   - Tier 4+ chunks: HEALPix Nside=4 sky cells + distance bands.\n\n3. **Naming policy** — labels only on named objects. Define which catalogues to harvest names from (NGC, IC, UGC, Messier; PGC is NOT a \"name\"). Label-rank function spec: spatial octree of named galaxies; per-frame ranking on the bucket containing camera + neighbours; 1-pixel hysteresis; ~30 visible cap across all tiers combined.\n\n4. **Cosmology choice for Tier 5+**: Planck 2018 (H0=67.4 km/s/Mpc, Ω_m=0.315, Ω_Λ=0.685) — decide and document the z→comoving-distance conversion (numerical integration baked into the build pipeline).\n\n5. **Selection-bias documentation** for Tier 5 (BOSS/eBOSS LRG/ELG/QSO sample is non-uniform). Document the selection function; consider a debug-panel toggle to visualise it.\n\n6. **Morphological-type → color palette** decision for the billboard renderer. Hubble T-type → RGB. Choose Buta/de Vaucouleurs convention (Sa = blue-ish, E = red-ish) or simple type-code bucket.\n\n7. **Architectural sketches** for the three cross-cutting changes (camera regime enum, billboard pipeline, label-rank with spatial bucketing) — point at the .2 and .3 beads for impl.\n\n# Acceptance\n\n- `docs/extragalactic-roadmap.md` committed.\n- Index entry added to `CLAUDE.md` § Documentation index.\n- All per-tier data-source URLs verified live at write time (capture retrieval dates inline).\n\n# Smoke\n\nThis is a docs bead — no runtime smoke. PR review by Alex IS the gate.\n\n# Out of scope\n\n- Any code changes. Pure docs.\n- Actual data fetches — that's the per-tier `refresh-*` script, filed under each impl bead.\n\n# Blocks\n\nstellata-0hx's other children (.2 through .8) — they reference decisions made here.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:53:09Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:53:09Z","labels":["deep-field","docs","extragalactic","galactic","research","visual"],"dependencies":[{"issue_id":"stellata-0hx.1","depends_on_id":"stellata-0hx","type":"parent-child","created_at":"2026-05-16T15:53:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":7,"comment_count":0}
{"_type":"issue","id":"stellata-lo5.1","title":"Hover labels — design gate (decide field shapes, trigger semantics, layout)","description":"**Status:** design / decision. No code lands until this clears.\n\n# Goal\n\nResolve the open design questions captured in the parent epic stellata-lo5 so the per-class implementation children have a settled payload shape, trigger model, and layout to build against.\n\n# Questions to answer (verbatim from the epic body)\n\n1. **Hover trigger semantics.** Always-on near-pointer? Modifier-key gated? Click-to-pin? Touch behaviour?\n2. **Layout — fixed corner vs near-cursor floating tooltip.** Interaction with the existing focus-info card?\n3. **Schema consistency vs class-specific richness.** Single field template or per-class?\n4. **Always-shown vs expanded-on-click split.** Hover ≤ 5 lines vs full modal?\n5. **Chart-mode interplay.** Same payload, styled subset, or hidden?\n6. **Picker priority on overlapping layers.** Star-in-front-of-cloud-edge — which wins?\n7. **Touch / mobile parity.** Long-press? Or defer to mobile pass entirely?\n\n# Per-class suggested fields (from epic — confirm or refine each)\n\nThe epic enumerates suggested fields for each class. The design gate either:\n- Confirms each class's list as-is and tags it as the implementation contract; OR\n- Trims / extends it, recording the final shape here so each implementation child has one source-of-truth field list.\n\n# Deliverable\n\nA short design doc (in this bead's description or a `docs/hover-labels.md` if it earns a doc), specifying:\n- Trigger + layout model.\n- Per-class field lists (final).\n- Picker-priority ordering.\n- Chart-mode behaviour.\n- Mobile / touch decision (implement vs defer).\n\nOnce shipped, implementation children spin up as separate beads, each parented to stellata-lo5.\n\n# Smoke / acceptance\n\nDoc reviewed by Alex; per-class lists frozen; implementation beads filed with explicit dependency on this gate's closure.","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:16:53Z","created_by":"Alex Marshall","updated_at":"2026-05-16T16:39:57Z","closed_at":"2026-05-16T16:39:57Z","close_reason":"Design gate resolved by approved plan: existing star hover trigger reused verbatim (280 ms delay, 14 px pickbox, near-cursor #tooltip, hide on leave); no click-to-pin; single #tooltip element (no separate focus card — focus info lives in scale-bar widget); shared engine with per-class HoverProvider contract (pick + format); 5-line cap, no expand-on-hover; chart-mode = CSS swap only; picker disambiguation hoisted from star-geometry.ts + stellata.ts to a shared module, prime-beats-fallback + closest-camera tie-break extended across all layers; mobile deferred to a follow-up bead. Per-class schemas frozen: stars unchanged; Sol planets = name/distance/V mag/radius/period; LG = name/kind/distance/size; heliopause apex = name/distance/description; clouds = formatter only, not registered at v1.0. Implementation beads filed as lo5.2-lo5.9.","labels":["picker","ui","ux"],"dependencies":[{"issue_id":"stellata-lo5.1","depends_on_id":"stellata-lo5","type":"parent-child","created_at":"2026-05-16T15:16:52Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-lo5","title":"Hover labels — generalised across stars / planets / LG / clouds / heliopause","description":"**Status:** design epic. Captures every renderable that is hover-targetable but has no hover info today, and proposes a generalised hover-label mechanism that covers stars, Sol's planets, future exoplanets, Local Group wireframes, molecular clouds, and the heliopause apex. **No decisions made — every shape below is a suggestion that needs Alex's call before implementation children land.**\n\n# Why a generalised mechanism\n\nToday the only hover affordance is the star pickbox (focused-star path). Adding hover labels per object class one at a time would split into N parallel implementations — same shape every time, each likely to drift. The goal of this epic is a single hover-label engine that:\n\n1. Owns the SVG / DOM surface for the floating tooltip.\n2. Routes pickbox hits from each layer's picker to a typed label payload.\n3. Has a per-class formatter so each kind of object surfaces the right summary.\n\nThe picker side already exists per-layer (stars + clouds + planets all have raycast / pickbox paths). The label side is the missing piece.\n\n# Candidate object classes + suggested fields per class\n\nFor each class below, the listed fields are **suggestions to evaluate, not decisions**. Star hover today shows roughly name / distance / app-mag / abs-mag / spectral / constellation; suggestions for other classes try to mirror that shape while swapping spectral for whatever's diagnostic for the class.\n\n## Stars (existing — re-audit at design time)\n\n- Name (preferred designation)\n- Distance from Sol\n- Apparent V mag\n- Absolute V mag\n- Spectral class\n- Constellation\n- (Suggestion to consider): variability type if GCVS-matched\n\n## Sol's planets (currently no hover)\n\n- Name (Mercury / Venus / …)\n- Distance from Sol (live, since it's animated by ephemeris)\n- Distance from camera (separate, useful when host-focused)\n- Apparent V mag at camera vantage\n- Physical radius\n- Orbital period (or current orbital phase)\n- (Suggestion to consider): JPL Standish ephemeris epoch + accuracy band\n\n## Exoplanets (future — parametric-on-focal-host)\n\n- Name (system + planet letter)\n- Apparent V mag (likely \"below threshold\" for most)\n- Physical radius / mass\n- Orbital period + semi-major axis\n- Host star context (distance from Sol via host)\n- Discovery method + reference\n\n## Local Group wireframes (stellata-1ui shipped, no hover yet)\n\n- Display name (M31, NGC 205, Sextans dSph, …)\n- Type (dSph / dE / dIrr / spiral disc) — derived from `kind` + the override / display-name rules\n- Distance from Sol (always pc / ly / Mpc via fmtDist)\n- Apparent V mag — depends on stellata-7de unblocking\n- Absolute V mag — from LVDB M_V (M31 / M33 hand-curated)\n- Half-light radius — for ellipsoid kind; disc radius for disc kind\n- (Suggestion to consider): heliocentric radial velocity from LVDB `vlos_systemic`; proper motion from `pmra` / `pmdec`. These feel \"deep dive\" rather than hover summary — consider a follow-up modal instead.\n\n## Molecular clouds (stellata-c7u — exists, hover currently absent)\n\n- Cloud name (Taurus, Orion A, …)\n- Distance from Sol\n- Physical extent (semi-axes from the ellipsoid model)\n- Mass (Zucker 2021 Table 3 — currently unused but committed)\n- (Suggestion): cloud type — diffuse vs molecular, when a type label is meaningful\n\n## Heliopause apex (Sol-focused only, currently has anchor label)\n\n- Anchor label is the existing apex marker — hover suggestion is to surface AU distance + estimated boundary type (termination shock vs heliopause vs bow shock).\n\n# Open design questions (the design-gate child below holds these — Alex decides)\n\n1. **Hover trigger semantics.** Always-on hover when pointer is near a pickable? Modifier-key gated (alt-hover)? Click-to-pin? Single-tap on mobile?\n2. **Layout — fixed corner vs near-cursor floating tooltip.** The existing focus-info card is fixed; hover preview probably wants near-cursor. How does it interact with the focus card?\n3. **Schema consistency vs class-specific richness.** Force every object class to fit the same field template, or per-class layouts?\n4. **What fields are \"always shown\" vs \"expanded on click\"?** Hover summary likely wants ≤ 5 lines; the rest of the data lives in a click-to-pin / focus modal.\n5. **Chart-mode interplay.** Chart mode has its own minimal aesthetic — does hover show the same payload, a chart-styled subset, or nothing?\n6. **Picker priorities.** When two layers overlap at the cursor (star in front of a cloud edge), which wins?\n7. **Touch / mobile parity.** Hover doesn't exist on touch — long-press? Card always visible after pick?\n\n# Children to file once the design gate clears\n\n- Hover-engine module (DOM / SVG plumbing + typed payload routing) — single PR.\n- Per-class hover formatter + picker wire-up: one bead each for stars, Sol planets, LG objects, molecular clouds, heliopause apex. Filed when the design gate clears so the expected payload shape is settled.\n- Mobile / touch parity bead (lower priority — desktop first per stellata-prioritization-framework).\n- Click-to-pin / focus-modal expansion bead (separate from hover summary).\n\n# Related work\n\n- stellata-7de — LG apparent magnitude depends on the magnitude column being plumbed; hover labels for LG depend on stellata-7de if \"show apparent V mag\" makes the suggested list.\n- stellata-c7u — molecular-cloud rendering epic.\n- stellata-3re — solar-system planets layer.\n- stellata-bk5 — future exoplanet layer.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:16:33Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:16:41Z","labels":["picker","ui","ux"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2br.6","title":"Cloud-warp arrivals: lookAt jitter (no recentre infrastructure)","description":"Same Float32-ULP lookAt jitter as stellata-2br.5 (navigate-mode star warp), but for cloud destinations. setFocusedCloud doesn't call recenterOrigin — the floating origin stays on the previous focus across a cloud-warp arrival. So lookAt(cloudCenter) in the source-local frame suffers the same precision degradation as star arrivals did pre-2br.5, but there's no existing infrastructure to fix it.\n\nThe cubic-Hermite log-d Fly profile exposes this for cloud destinations the same way it does for star destinations.\n\n# Possible approaches\n\n1. Extend setFocusedCloud to recentre origin onto the cloud's centroid at focus time. Mirror setFocus's geometric half.\n2. Add a recenterFocusToCloud sibling to recenterFocusToStar, called mid-Fly when destKind === 'cloud'. Needs WarpState.recenteredToDest and shiftWarpWaypoints / shiftArrivalWaypoints (both extant after 2br.5).\n3. Live with it — cloud warps are typically shorter (Zucker cloud distances are 100s of pc, similar regime to stellar warps).\n\nProbably (2) — extends 2br.5's pattern cleanly. But the floating-origin-tracks-cloud-centroid invariant needs cross-checking against the cloud overlay's render path (clouds are 3D ellipsoids in cloud-loader / molecular-clouds; the centroid may not be the centre of mass for picking).\n\n# Out of scope of PR #66\n\nDiscovered during 2br.5 design but not fixed there.","status":"closed","priority":2,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-16T08:59:56Z","created_by":"Alex Marshall","updated_at":"2026-05-16T09:25:27Z","closed_at":"2026-05-16T09:25:27Z","close_reason":"Folded into stellata-2br.5 — cloud-warp arrivals now recentre origin via FocusTarget contract","labels":["camera","clouds","ux","warp"],"dependencies":[{"issue_id":"stellata-2br.6","depends_on_id":"stellata-2br","type":"parent-child","created_at":"2026-05-16T09:59:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.191","title":"Modal \u0026 welcome UX polish","description":"Themed sub-epic of stellata-9mm covering small user-visible modal/welcome defects.\n\nScope: welcome-modal copy is a sentence fragment after #info-star-count substitution, welcome vs about modals use different headline framings ('physics-based' vs 'observational'), modal-dismiss querySelectorAll runs once at bind time so late-added dismiss buttons are unwired, modal-dismiss per-modal keydown listener means multiple open modals all close on a single ESC, plus the deferred 'show visible star count under total' HUD task.\n\nWhy a dedicated epic: only 5 beads but every one of them shows up in the user's first 30 seconds with the app. Pulling them out of 9mm makes them visible as a 'first-impression polish' bucket rather than buried in code-quality.\n\nPriority: P2 — small but user-facing.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-15T11:52:51Z","created_by":"Alex Marshall","updated_at":"2026-05-15T11:52:51Z","dependencies":[{"issue_id":"stellata-9mm.191","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-15T12:52:51Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.189","title":"Planet pipeline DRY, tests, halo correctness","description":"Themed sub-epic of stellata-9mm collecting follow-ups from the solar-system pipeline (stellata-3re).\n\nScope: PlanetMaterialUniforms duplicates 17 star-pipeline uniforms by hand (DRY), PlanetBodyField.update() pipeline zero test coverage past the cull gate, flushAttributeRange marks all 7 buffers dirty when only iLocalRel changes, getFocusedPlanetLocalPositions returns a subarray that's stale after growCapacity (lifecycle pairing gap), peakPhaseFactor name overpromises (only φ(0)), outer-disc near-z write defeats the disc-pass gl_FragDepth=1.0 halo trick, evaluate whether stars should adopt the planet-style outer-disc occluder.\n\nWhy a dedicated epic: the planet pipeline diverged from the star pipeline in PR #48 (3 passes vs 4) and the parallel implementations need decisions before more sister-host work (clouds, exoplanets) lands. Grouping makes those decisions one design pass instead of seven scattered judgement calls.\n\nPriority: P2 on the user-visible halo defect (123) and the test/cull regressions; rest default P3.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-15T11:52:39Z","created_by":"Alex Marshall","updated_at":"2026-05-15T11:52:39Z","dependencies":[{"issue_id":"stellata-9mm.189","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-15T12:52:39Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.188","title":"SVG overlay dirty-tracking \u0026 sentinel-init hygiene","description":"Themed sub-epic of stellata-9mm covering the rest of the SVG overlay dirty-tracking rollout (stellata-akk) plus the broader sentinel-init / cache-key hygiene findings.\n\nScope: focus-ring-overlay (the sixth peer missed in PR #55), scale-bar.ts whole-frame vs per-attribute strategy divergence, ATTR_DIRTY_PX hoist into a shared util, sentinel-init poison audit across overlay visibility tracking, hud-overlay/poi-overlay numeric-sentinel reset on hide, applyFade pointerEvents sentinel first-frame skip, disc-mask inverted threshold guard, getComputedTextLength cache-key missing font-load + CSS dimensions, projectWithNearClip single-caller-per-frame contract.\n\nWhy a dedicated epic: ~10 findings all in the same overlay layer that should converge on one canonical strategy. Memory rules at play: stellata-consistency-at-the-seam §3 (sentinel-init), stellata-pattern-coverage-across-peers (all SVG overlays must match), stellata-named-constants-and-dry rule 1 (hoist ATTR_DIRTY_PX at second usage).\n\nPriority: P2 — visible mis-paints + perf cliffs hit the user.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-15T11:52:32Z","created_by":"Alex Marshall","updated_at":"2026-05-15T11:52:32Z","dependencies":[{"issue_id":"stellata-9mm.188","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-15T12:52:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.187","title":"Event-bus \u0026 lifecycle hygiene","description":"Themed sub-epic of stellata-9mm carving out follow-ups from the event-bus consolidation (stellata-77m) and adjacent lifecycle/dispose hygiene work.\n\nScope: EventBus API hardening (clear(), EmitArgs typing, varargs allocations, mid-emit ordering), unsubscribe leaks at component teardown (createTimeReadout, perf-hud, debug HUDs), the eleven-event surface integration test, stale on*Change comment sweep, perf marker rename to match the new 'frame' event, and the dispose smoke test.\n\nWhy a dedicated epic: ~13 follow-ups all gated on the same mental model — the typed EventBus from stellata-77m is the central seam, and the lifecycle-pairing rule (see memory stellata-consistency-at-the-seam §1) wants every bus.on() to ship its unsub in the same diff. Grouping these makes it tractable to land them in 2-3 PRs rather than scattered across the meta umbrella.\n\nPriority: P2 because lifecycle leaks are real bugs (dispose() in test setups grows memory between runs); test/doc sub-items default to P3.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-15T11:52:25Z","created_by":"Alex Marshall","updated_at":"2026-05-15T11:52:25Z","dependencies":[{"issue_id":"stellata-9mm.187","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-15T12:52:25Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.41","title":"Phase 5 — Eclipse photometry for EA/EB/EW from orbital elements","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nSupersedes stellata-dch.17. Implement physically-grounded eclipse photometry for EA/EB/EW (GCVS eclipsing-binary classes) by combining: stellata-dch.14 (suppress intrinsic pulsation for eclipsing classes — currently in-flight) + the orbital elements emitted by Phase 2 (stellata-dch.32) + the corrected mass ratios from Phase 5 stellata-dch.40. Depends on stellata-dch.14 + stellata-dch.32 + stellata-dch.40.\n\n# Approach\n\nFor each EA/EB/EW system with orbital elements:\n\n1. Render the two stars as their actual physical-radius discs (already done in current shader).\n2. From the orbital elements (P, T, e, a, i, omega, Omega — now Gaia NSS or ORB6-sourced via Phase 2 Stage 4), compute the per-component sky-plane position at runtime t (the existing dch.9 / dch.10 binary-orbit machinery does this).\n3. Compute the per-frame fraction of the brighter component's disc occulted by the dimmer component's disc (circle-circle occlusion math: closed-form when both radii + center distance are known).\n4. Subtract the occulted luminosity from the apparent magnitude of the rendered primary. Real eclipse-shaped photometry: deep primary minimum when the cooler / dimmer star transits the hotter / brighter one; shallower secondary minimum on the other side of the orbit.\n\n# Deliverables\n\n- src/client/eclipse-photometry.ts (new) — closed-form circle-circle occlusion math; per-component luminosity subtraction; integration with the existing apparent-magnitude computation in the star vertex shader (uniform / per-instance attribute path TBD at impl).\n- src/client/shaders/star.vert.glsl — accept an eclipse_dim per-instance attribute (or uniform) that the eclipse-photometry layer writes per-frame. Wired into the existing magnitude path.\n- Integration with the binary-orbit runtime (the existing stellata-dch.10 in-flight bead): the orbital evaluator already computes per-frame component positions. Eclipse-photometry reads those and writes the dim factor.\n- src/client/eclipse-photometry.test.ts — unit tests for circle-circle occlusion math (degenerate cases: no occlusion, partial overlap, full coverage, equal radii).\n\n# Acceptance\n\n- Algol (β Persei, HIP 14576, EA, P=2.87 days): visible eclipses under warp animation; primary minimum drop ≈ 1.3 mag depth; secondary minimum drop ≈ 0.1 mag (matches published lightcurve).\n- β Lyrae (HIP 92420, EB, P=12.94 days): continuous brightness variation with two unequal-depth minima.\n- W UMa (HIP 56619, EW, P=0.33 days): near-continuous variation, near-equal-depth minima.\n- Non-eclipsing binaries (α Cen, Sirius, Castor) show no eclipse-photometry dip when orbiting under warp.\n- Variable types EA/EB/EW with no orbital elements (no Gaia NSS, no ORB6) gracefully fall back to GCVS amplitude-driven pulsation (current behavior).\n\n# Smoke (manual per memory stellata-stellata-smoke-tests)\n\n- Focus Algol -\u003e advance t under warp -\u003e primary minimum visible at t ≈ T + n*P.\n- Focus β Lyr -\u003e see continuous variation.\n- Compare against published lightcurves (AAVSO).\n- Verify no eclipses on non-EA/EB/EW systems.\n\n# Files to touch\n\n- src/client/eclipse-photometry.ts (new)\n- src/client/shaders/star.vert.glsl + star.frag.glsl (eclipse_dim attribute integration)\n- src/client/eclipse-photometry.test.ts (new)\n- src/client/stellata.ts (wire eclipse-photometry into the per-frame loop alongside the binary-orbit runtime)\n\n# After this lands\n\nClose stellata-dch.17 with reason=\"superseded by stellata-dch.41\".\n\n# Commit\n\nOne commit (or split if shaders + runtime are big enough to warrant separation per memory commit-granularity). Minor version bump (user-visible feature: real eclipse photometry).","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:22:10Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:47:34Z","external_ref":"archive/dch-pipeline-foundation","labels":["eclipsing-binaries","phase-5","photometry"],"dependencies":[{"issue_id":"stellata-dch.41","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:22:09Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.41","depends_on_id":"stellata-dch.14","type":"blocks","created_at":"2026-05-15T11:25:08Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.41","depends_on_id":"stellata-dch.32","type":"blocks","created_at":"2026-05-15T11:25:09Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.41","depends_on_id":"stellata-dch.40","type":"blocks","created_at":"2026-05-15T11:25:10Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.40","title":"Phase 5 — Spectral-class-aware mass-ratio q (WD/giant/subgiant fallbacks)","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nSupersedes stellata-dch.15. Replace the current L^(1/3.5) main-sequence mass-luminosity mass-ratio q estimate with a spectral-class-aware lookup that handles WD / giant / subgiant fallbacks correctly. Depends on stellata-dch.32 (Stage 5+6+7 lands the mass-ratio emission path) + stellata-dch.36 (composite-spectral-types-split per component).\n\n# Why\n\nCurrent implementation: q = M_B / (M_A + M_B), with M = L^(1/3.5) from absmag. This fails badly for evolved companions. Example: Sirius B (V=11.36 absmag) yields q ≈ 0.07; true value is q ≈ 0.33 (M_A=2.06 M_sun, M_B=1.02 M_sun). The orbit shape is unaffected but the split-of-motion between components is wrong by ~5×.\n\n# Approach\n\nPer-component mass estimation table-lookup by parsed spectral class:\n\n- WD (luminosity class VII / spect prefix 'D'): mass ≈ 0.6 M_sun ± 0.1 (a typical WD mass; main-sequence-progenitor info unavailable in WDS)\n- Giants (class III): use mass-from-MK-class table (e.g. K giants ≈ 1.2 M_sun, G giants ≈ 1.5 M_sun, etc.) per Cox 2000 §15.2 / Allen 2000 tables\n- Subgiants (class IV): interpolate between main-sequence and giant tables\n- Main-sequence (class V): current L^(1/3.5) relation (correct for these)\n- Supergiants (class I): higher-mass table\n\nq = M_B / (M_A + M_B) computed per-pair using these tables.\n\n# Deliverables\n\n- scripts/catalog-pure.ts — new function massFromSpectralClass(spect_str, absmag) returning estimated M / M_sun. Tables for WD / giant / subgiant / supergiant. Main-sequence remains L^(1/3.5).\n- scripts/build-binaries.py — Stage 4 / 6 — when computing q for a system, call the new helper instead of the inline L^(1/3.5). Per-component spectral classes (from stellata-dch.36) now feed in correctly.\n- scripts/catalog-pure.test.ts — pinned-value tests: Sirius B q ≈ 0.33, Procyon B q ≈ 0.4 (rough estimates; calibrate at impl from published masses).\n- scripts/known-stars.tsv (stellata-dch.37 corpus) — verify the corpus's expected q values for binaries with WD / giant companions match the new tables, refresh corpus where needed.\n\n# Acceptance\n\n- Sirius B q within ±0.05 of 0.33 (currently 0.07 = off by 4×).\n- Procyon B q within ±0.05 of expected from known-mass table lookup.\n- α Cen B q ≈ 0.45 (both main-sequence, current logic correct).\n- known-stars.test.ts (Phase 4 Bead R, stellata-dch.38) passes mass-ratio assertions for all corpus binaries with WD/giant companions.\n\n# Smoke\n\n- npm run build:binaries --force\n- grep '06451-1643' data/multiples.tsv shows Sirius A q + B q with the new values\n- npm test green\n\n# Files to touch\n\n- scripts/catalog-pure.ts (mass lookup helper)\n- scripts/build-binaries.py (Stage 4 / 6 q computation)\n- scripts/catalog-pure.test.ts (per-class assertions)\n\n# After this lands\n\nClose stellata-dch.15 with reason=\"superseded by stellata-dch.40\".\n\n# Commit\n\nOne commit. Minor version bump (user-visible: Sirius B's mass-ratio rendering will change).","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:21:40Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:21:40Z","labels":["mass-ratio","phase-5","spectral"],"dependencies":[{"issue_id":"stellata-dch.40","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:21:40Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.40","depends_on_id":"stellata-dch.32","type":"blocks","created_at":"2026-05-15T11:25:06Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.40","depends_on_id":"stellata-dch.36","type":"blocks","created_at":"2026-05-15T11:25:07Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.39","title":"Phase 4 — scripts/validate-simbad-sample.py + extend build-counts.test.ts with statistical bounds","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nTwo validation surfaces land in this bead: Tier-C SIMBAD random-sample cross-check (manual-run, against the snapshot from stellata-dch.26) and Tier-B population statistical bounds (snapshot-pinned in build-counts.test.ts). Depends on stellata-dch.26 (SIMBAD sample) + stellata-dch.32 (Phase 2 stats output).\n\n# Tier-C — scripts/validate-simbad-sample.py\n\nManual-run Python script (npm alias validate:simbad). Reads data/simbad_sample.tsv, for each row looks up the entry in public/catalog.bin by HIP / gaia_source_id, compares Stellata's distance / PM / absmag against SIMBAD published values. Outputs:\n- Residual histograms per axis (absmag, distance, pmra, pmdec) written to docs/validation-residuals-\u003cdate\u003e.md (or plain JSON; one or the other, decide at impl)\n- Top-50 outliers flagged: stars with absmag residual \u003e 0.5 OR plx residual \u003e 3-sigma\n- Summary stats: median + percentile residuals; pass/fail per axis against thresholds defined in script\n- Exits non-zero if median absmag residual \u003e 0.1 OR median plx residual \u003e 3-sigma (these are smoke thresholds for the script itself, not for the build)\n\n# Tier-B — extend scripts/build-counts.test.ts\n\nAdd pinned statistical bounds that the build must produce, snapshot-style per memory stellata-test-coverage-discipline:\n\n- gaia_resolve_rate (fraction of WDS components resolved via tiers 1-3, i.e. source-ID anchored): pin \u003e= 0.90 with current snapshot value\n- optical_rejected_rate (fraction of WDS pairs rejected by the cascade): pin actual value with ±20% tolerance\n- nss_orbit_rate (fraction of orbits sourced from NSS vs ORB6): pin actual ratio\n- hip2_fallback_rate (fraction of components routed via HIP2 fallback): pin actual rate\n- The existing tier-2 counts (recordCount, binaryPairs, gcvsMatched, ccdmFlagged, etc.) stay; this just adds the new metrics\n\nSnapshots refreshed via UPDATE_BUILD_COUNTS=1 (existing mechanism). Failing snapshots halt the build.\n\n# Acceptance\n\n- npm run validate:simbad runs against current catalog.bin, produces residual report. Median absmag residual \u003c 0.05; median plx residual \u003c 1-sigma; outlier count \u003c 1% of sample.\n- npm test green with new Tier-B assertions in build-counts.test.ts.\n- scripts/build-counts-expected.json updated with new fields.\n\n# Smoke\n\n- npm run build:binaries \u0026\u0026 npm run build:catalog (full pipeline)\n- npm run validate:simbad\n- Inspect residual histograms\n\n# Files to touch\n\n- scripts/validate-simbad-sample.py (new)\n- scripts/build-counts.test.ts (extend)\n- scripts/build-counts-expected.json (extend schema)\n- package.json (npm alias)\n\n# Commit\n\nOne commit. PR labelled skip-version-bump (validation, not user-visible).","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:21:15Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:21:15Z","labels":["phase-4","simbad","validation"],"dependencies":[{"issue_id":"stellata-dch.39","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:21:15Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.39","depends_on_id":"stellata-dch.26","type":"blocks","created_at":"2026-05-15T11:24:59Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.39","depends_on_id":"stellata-dch.32","type":"blocks","created_at":"2026-05-15T11:25:00Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.36","title":"Phase 3 — parseSpectral splits composite WDS types (A1Vm+DA2 → per-component)","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nSupersedes stellata-dch.16. Extends parseSpectral to handle composite WDS spectral types (e.g. 'G2V+DA2' for Sirius, 'A1V+M4' for 40 Eridani), splitting on '+' and parsing each component separately. The current implementation parses only the first half, which misassigns the spectral class of secondary components in close binaries. Depends on stellata-dch.33 (v6 binary format provides per-component spectral storage via multiples.tsv rows).\n\n# Deliverables\n\n- scripts/catalog-pure.ts — extend parseSpectral function. New signature: parseSpectral(input, componentLetter?='A') returns a per-component spectral classification. When the input contains '+', split it; the A component gets the first half, B gets the second half, etc. Default to first half if no component letter provided (backward-compatible).\n- scripts/build-binaries.py — when emitting multiples.tsv rows, pass each component's letter to parseSpectral so each row's spect column reflects the actual component's spectral class.\n- scripts/catalog-pure.test.ts — add tests covering: 'G2V+DA2' splits to G2V (A) and DA2 (B); 'A1V+M4' splits to A1V (A) and M4 (B); 'F5V' (no +) returns F5V for any component letter; 'G2V+DA2+M5' (triple) handles correctly.\n\n# Acceptance\n\n- Sirius A row: spect=G2V (existing behavior).\n- Sirius B row: spect=DA2 (NEW — currently inherits 'G2V' or composite).\n- 40 Eridani B (white dwarf): spect=DA (NEW).\n- Tests pin these specific values via toBe.\n\n# Smoke\n\n- npm test green with new assertions\n- npm run build:binaries --force; grep '06451-1643' data/multiples.tsv shows Sirius A=G2V, B=DA2 in spect column\n\n# Files to touch\n\n- scripts/catalog-pure.ts (parseSpectral)\n- scripts/catalog-pure.test.ts (per-component tests)\n- scripts/build-binaries.py (pass component letter through)\n\n# Commit\n\nOne commit. Close stellata-dch.16 with reason=\"superseded by stellata-dch.36\" after this bead lands.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:20:00Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:20:00Z","labels":["phase-3","spectral"],"dependencies":[{"issue_id":"stellata-dch.36","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:19:59Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.36","depends_on_id":"stellata-dch.33","type":"blocks","created_at":"2026-05-15T11:24:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.35","title":"Phase 3 — build-catalog.ts CCDM cross-match uses HIP via new cross-walks","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nAlign the CCDM (Catalogue of Components of Double and Multiple Stars) cross-match pass in scripts/build-catalog.ts with the new HIP-anchored infrastructure. CCDM is referenced via the existing data/hip_ccdm.tsv file; this bead ensures the cross-match path is consistent with Phase 2's HIP cross-walk usage. Depends on stellata-dch.33.\n\n# Deliverables\n\n- scripts/build-catalog.ts — verify the CCDM cross-match pass uses HIP cross-walk infrastructure consistently. If currently using ad-hoc HIP lookups, refactor to share the loaded HIP indices. Document any divergence (CCDM might use its own HIP keying scheme).\n- scripts/catalog-pure.test.ts — extend CCDM tests with assertions tied to the new indices.\n\n# Acceptance\n\n- CCDM match-rate stats logged at end of build (counts of CCDM rows successfully mapped to AT-HYG rows via HIP).\n- A known CCDM-flagged star (e.g. a famous visual double) renders with the ccdmFlag bit set in the v6 binary.\n- No regression in existing ccdmFlagged count in build-counts.test.ts (current snapshot pinned).\n\n# Smoke\n\n- npm run build:catalog\n- npx tsx scripts/verify-catalog.ts: ccdmFlagged count matches expected\n- Spot-check a CCDM-flagged star\n\n# Files to touch\n\n- scripts/build-catalog.ts (CCDM cross-match section)\n- scripts/catalog-pure.ts (if cross-id parsing needs adjustment)\n\n# Commit\n\nOne commit. Mostly internal alignment; likely no user-visible behavior change → skip-version-bump label OK if no functional difference.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:19:43Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:19:43Z","labels":["ccdm","cross-match","phase-3"],"dependencies":[{"issue_id":"stellata-dch.35","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:19:43Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.35","depends_on_id":"stellata-dch.33","type":"blocks","created_at":"2026-05-15T11:24:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.26","title":"Phase 1 — scripts/refresh-simbad-sample.py + commit data/simbad_sample.tsv","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nRandom 10k SIMBAD sample with stable seed for reproducible validation. Used by Phase 4 Tier-C validator (stellata-dch.39) to cross-check Stellata's catalog against an independent external authority. Depends on stellata-dch.21 (refresh_lib). NOT blocking Phase 2 (P2 priority).\n\n# Deliverables\n\n- scripts/refresh-simbad-sample.py (new) — SIMBAD TAP query with seeded random sample, stratified by V magnitude for diversity.\n- scripts/refresh_lib.py — add simbad_backend() factory + SIMBAD_TAP_URL constant (first non-Gaia/non-VizieR backend; refresh-bailer-jones.py's cds-only override is the precedent). Unit test added in refresh_lib.test.py.\n- data/simbad_sample.tsv (new, LFS ~1-2 MB).\n- package.json — \"refresh:simbad\" alias.\n\n# TSV columns (15)\n\nSchema fixes vs the original draft (live SIMBAD TAP probe 2026-05-18 — basic columns are not what the original spec assumed):\n\n- simbad_oid          int — SIMBAD basic.oid (stable primary key; useful for re-queries).\n- simbad_main_id      str — SIMBAD basic.main_id (e.g. \"* alf Car\"). Was \"simbad_main_id\"; preserved.\n- hip                 int|null — extracted from ident.id LIKE 'HIP %' (HIP integer; null if not present in Hipparcos).\n- gaia_source_id      int|null — extracted from ident.id LIKE 'Gaia DR3 %' (Gaia DR3 source_id; null if not in DR3).\n- ra                  float — basic.ra (deg, ICRS J2000). Was \"ra_icrs\"; basic.ra IS ICRS already by SIMBAD design, no _icrs suffix on the TAP column.\n- dec                 float — basic.dec (deg, ICRS J2000). Was \"dec_icrs\"; same reasoning.\n- plx_value           float|null — basic.plx_value (mas). Was \"plx\"; actual column is \"plx_value\".\n- plx_err             float|null — basic.plx_err (mas).\n- pmra                float|null — basic.pmra (mas/yr). Was \"pm_ra\"; actual column is \"pmra\".\n- pmdec               float|null — basic.pmdec (mas/yr). Was \"pm_de\"; actual column is \"pmdec\".\n- v_mag               float — allfluxes.V (mag). Was implicit; needed in the TSV so the validator can verify the stratification it received.\n- distance_pc         float|null — derived: 1000/plx_value when plx_value\u003e0; else null. (Original bead promised this as if a basic column — it isn't; the script computes it. mesDistance carries posterior distances but is too sparse and inconsistent for a sample TSV.)\n- absmag              float|null — derived: v_mag − 5*log10(distance_pc/10) when both exist; else null. (Original bead promised this as if a basic column — it isn't; the script computes it.)\n- sp_type             str|null — basic.sp_type. Was \"spectral_type\"; actual column is \"sp_type\".\n- otype               str — basic.otype (e.g. '*', 'PM*', 'EB*'; SIMBAD's hierarchical short code).\n\n# Sample stratification\n\n10k rows split as: 1000 with V \u003c 6 (naked-eye bright), 3000 with 6 \u003c= V \u003c 9 (HIP completeness regime), 4000 with 9 \u003c= V \u003c 11.5 (Tycho-2 completeness), 2000 with 11.5 \u003c= V \u003c 15 (faint Tycho-Gaia merge). Stable seed = 20260515 so the sample is reproducible.\n\nLive bin populations (filter: basic.otype IN (otypedef WHERE path LIKE '*%'); allfluxes.V NOT NULL):\n  V in [-,    6.0): 5,056 stars  → target 1000  → fetch all (no server-side subsample needed)\n  V in [6.0,  9.0): 125,047 stars → target 3000 → MOD(oid, K) subsample to ~9k candidates\n  V in [9.0, 11.5): 1,414,045    → target 4000 → MOD(oid, K) subsample to ~12k candidates\n  V in [11.5, 15.0): 2,321,952   → target 2000 → MOD(oid, K) subsample to ~6k candidates\n\nStars are identified via SIMBAD's otype hierarchy: query otypedef once (path LIKE '*%') to enumerate all star otype codes (~122 codes, ~720-char IN-clause). Note: SIMBAD's TAP does NOT support LIKE on basic.otype directly (live probe error: \"LIKE not supported on otype !\"), so the otypedef-precompute step is mandatory.\n\n# Backend — first non-ESA, non-VizieR refresh script\n\nSIMBAD TAP at https://simbad.cds.unistra.fr/simbad/sim-tap is its own service (not VizieR; not ESA). Add simbad_backend() to refresh_lib.py alongside esa_backend()/cds_backend() and pass backends=[rl.simbad_backend()] to TapClient — same single-backend-override pattern refresh-bailer-jones.py uses for cds_backend().\n\n# Acceptance\n\n- 10_000 rows exactly.\n- gaia_source_id non-null for \u003e=95% of rows overall (live probe: V\u003c6 bin runs ~94%, fainter bins run ~96-97%; weighted total ~96%). NOT per-bin — V\u003c6 stars hit Gaia's bright-end saturation cliff (e.g. Canopus is missing).\n- Deterministic regen with seed=20260515 — re-running with the same seed produces a byte-identical TSV.\n\n# Smoke\n\n- npm run refresh:simbad writes the TSV\n- Re-running with the same seed produces a byte-identical TSV\n- Spot-check 5 random rows against SIMBAD's web UI\n\n# Commit\n\nOne commit. Standard PR.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:16:20Z","created_by":"Alex Marshall","updated_at":"2026-05-18T20:53:50Z","started_at":"2026-05-18T20:26:11Z","closed_at":"2026-05-18T20:53:50Z","close_reason":"Closed","labels":["phase-1","refresh-scripts","simbad","validation"],"dependencies":[{"issue_id":"stellata-dch.26","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:16:20Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.26","depends_on_id":"stellata-dch.21","type":"blocks","created_at":"2026-05-15T11:24:33Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.20","title":"Phase 0 — Sweep v1.0 prose from CLAUDE.md, SCIENCE.md, docs/*.md","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture).\n\n# Scope\n\nStellata is past v1.0. Remove all v1.0 / v1-era prose from project docs so future doc updates don't have to navigate around stale framing. Set by Alex 2026-05-15.\n\n# Files + lines to update\n\nConfirmed via grep on 2026-05-15:\n\n- CLAUDE.md:252 — \"Temporarily shelved (v1.0)\" section heading\n- CLAUDE.md:275 — \"WASD / flight controls (removed after v1 review)\"\n- SCIENCE.md:77 — \"Molecular cloud sources shelved for v1.0\"\n- SCIENCE.md:79 — \"ellipsoid layer, which is committed but not rendered in v1.0\"\n- SCIENCE.md:187 — \"for v1 this is documented and accepted\"\n- SCIENCE.md:449 — \"In v1 t is pinned to now with no scrubber\"\n- SCIENCE.md:636 — \"framings v1 affords\"\n- docs/chart-mode.md:28 — \"v1.0 caveat\"\n- docs/chart-mode.md:30 — \"Milky Way isobar is also disabled for v1.0\"\n- docs/molecular-clouds.md:3 — \"Status (v1.0): Shelved\"\n- docs/molecular-clouds.md:13 — \"in v1.0 the toggle is gone\"\n- docs/rendering.md:51 — \"Molecular clouds (shelved v1.0)\"\n- docs/solar-system.md:24 — \"v1 hardwires planets ⇔ Sol\"\n- docs/solar-system.md:61 — \"v1 pins it to now\"\n- docs/solar-system.md:181 — \"v1 places the\"\n\n# Replacement guidance\n\nWhere the prose describes a current limitation, rephrase to a tense-agnostic \"currently\" or \"in the present implementation\" form. Where it implies \"will lift the constraint in v1.1+\", rewrite to describe the constraint without the version anchor (state the limit, not the timeline). For \"shelved\" status (molecular clouds, MW isobar), keep the \"shelved\" status as a current fact.\n\n# Acceptance\n\ngrep -rn 'v1.0\\|until v1\\|(v1)\\|v1 review\\|v1 affords\\|v1 hardwires\\|v1 pins\\|v1 places' CLAUDE.md SCIENCE.md docs/ returns no hits.\n\n# Smoke\n\n- npm run typecheck clean (no .md changes affect TS, but defensively verify)\n- npm test green\n- One reviewer reads each touched doc cold to verify the prose still makes sense\n\n# Commit\n\nSingle commit, PR labelled skip-version-bump (docs-only).","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-15T10:14:44Z","created_by":"Alex Marshall","updated_at":"2026-05-18T17:22:25Z","started_at":"2026-05-18T10:56:22Z","closed_at":"2026-05-18T17:22:25Z","close_reason":"Closed","labels":["cleanup","docs","phase-0","skip-version-bump"],"dependencies":[{"issue_id":"stellata-dch.20","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-15T11:14:43Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-36y.1","title":"Decide HiPS sampling strategy for far-field cumulative glow","description":"Design-doc gate for stellata-36y. Output: written decisions (in notes/design fields of this bead) covering:\n\n1. **Source HiPS map.** Default candidate: Gaia DR3 colour HiPS at CDS (alasky.u-strasbg.fr) — full-sky RGB from Gaia G/BP/RP. Alternatives to evaluate: DSS2 colour (older, well-sampled, includes nebulosity), 2MASS (NIR penetrates dust better), Gaia DR3 stellar density HiPS (counts not brightness).\n\n2. **HEALPix order for the reduction.** Trade-off pixel scale vs tile count:\n   - Order 5 ≈ 13.7 arcmin pixel, 12 288 tiles\n   - Order 6 ≈ 6.9 arcmin pixel, 49 152 tiles\n   - Order 7 ≈ 3.4 arcmin pixel, 196 608 tiles\n   Pick the lowest order that gives a convincingly structured glow; smaller artifact, faster GPU sampling.\n\n3. **Reduction artifact format.** Per-tile mean RGB → 2D HEALPix-projected texture, or per-tile scalar intensity, or both. Must subtract AT-HYG-resolved stars' flux contribution to avoid double-counting (each AT-HYG star already renders separately).\n\n4. **Blending / handoff.** Always-on background glow vs magnitude-gated (user's magnitude slider could control: glow = everything fainter than the slider). How it composites with the volumetric Milky Way and with dust extinction.\n\n5. **Render integration.** Three options: skybox sphere at far clip (cheapest, no parallax), sampled inside the MW raymarch (parallax-correct, integrates with dust), or both (skybox for sky-band-only contribution + MW raymarch for in-galaxy contribution).\n\n# On completion\n\n- Decisions live in this bead's notes/design.\n- File implementation children under stellata-36y at P3:\n  - Data pipeline child: one-shot HiPS reduction script + committed artifact under data/ per frozen-external-data.\n  - Runtime child: shader/material integration.\n\n# Out of scope for this bead\n\nNo code. Pure design output.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T15:22:09Z","created_by":"Alex Marshall","updated_at":"2026-05-12T15:22:09Z","labels":["catalog","ism","physics","research"],"dependencies":[{"issue_id":"stellata-36y.1","depends_on_id":"stellata-36y","type":"parent-child","created_at":"2026-05-12T16:22:09Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-36y","title":"Far-field statistical augmentation from CDS / Gaia HiPS","description":"Stellata's near-field pipeline (AT-HYG + GCVS + Edenhofer dust) is per-object resolved. The far-field — the cumulative glow of the ~1.8B Gaia DR3 stars below AT-HYG's mag cutoff — is currently missing or approximated only by the analytic Milky Way disc/bulge.\n\n# Goal\n\nAdd a statistical layer that represents the sky's true stellar density beyond the AT-HYG floor, sourced from CDS/Gaia HiPS data (or equivalent VizieR-hosted aggregate catalog), without competing with the per-star pipeline.\n\n# Why now\n\nPrompted 2026-05-12 by Alex's first encounter with the Gaia archive's Aladin HiPS visualisation — the visible stellar density is dramatically richer than AT-HYG suggests. The detail-gradient mission asks for highest-fidelity-near, statistical-far; far-field is currently the weakest link.\n\n# CDS / VizieR ecosystem (orientation)\n\n- **VizieR** — published catalog service. Already upstream of Stellata (AT-HYG, GCVS).\n- **SIMBAD** — per-object database; cross-IDs, classifications, references.\n- **Aladin** — interactive sky atlas + HiPS (Hierarchical Progressive Surveys) tiled all-sky maps including Gaia DR3 colour.\n- **CDS portal** — umbrella access.\n\n# In scope\n\n- Cumulative glow layer below the AT-HYG catalog floor (sky-brightness reconstruction from HiPS or binned Gaia counts).\n- Optional: refine the volumetric Milky Way disc/bulge profile against Gaia surface-brightness observations.\n\n# Out of scope (file separately if/when desired)\n\n- Direct Gaia DR3 NSS adoption (real binary orbits for AT-HYG-resolved stars).\n- Direct Gaia DR3 Apsis adoption (Teff/log g replacing spectral-string parsing).\n- Direct Gaia DR3 Variability adoption (supplement to GCVS).\n- Gaia DR4 refresh (filed as separate deferred bead).\n\n# Child structure\n\nFirst child is the research / design-doc gate at P2. Implementation children (data pipeline, runtime layer) get filed after the research bead lands its decisions, at P3 per stellata-prioritization-framework.\n\n# Acceptance\n\n- All children closed and merged.\n- Visible far-field glow at chart-mode and navigate modes.\n- No live network fetch at build (frozen-external-data compliance).\n- Documented in docs/milky-way.md or a new docs/far-field.md.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-12T15:21:53Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:28:15Z","labels":["catalog","ism","physics","research"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.19","title":"AT-HYG ↔ Gaia DR3 cross-match dedupe pass for native duplicate rows","description":"# Why\n\nAfter stellata-dch.18 lands the Gaia DR3 WDS optical filter, the\nremaining residual source of spurious stars is AT-HYG-native: rows\nin `athyg_33_classic_ids.csv` that map to the same physical Gaia\nsource but appear as separate entries (different HIP / HD / catalog\norigin, near-identical xyz).\n\nstellata-dch.11 found exactly one such pathology at xyz=(-8.7310,\n1.6110, 5.4470) — caught by `inferBinaries`'s sub-threshold safety\nnet. We don't know the catalog-wide rate; it could be 1, could be\n100. A systematic cross-match is the only way to know — and to\nremove any that exist.\n\nThe WDS optical filter doesn't help here because these duplicates\naren't WDS pairs — they're single rows that AT-HYG happens to\ncontain twice.\n\n# Scope\n\nAdds a new dedupe pass to the build pipeline (probably a\n`scripts/dedupe-athyg.py` script invoked before `build-catalog.ts`).\n\n1. Decide on Gaia DR3 sourcing — either:\n   a. Pre-built local Gaia subset bounded by AT-HYG's distance and\n      mag range (~30M sources, doable as a one-shot ADQL download +\n      data/ commit), OR\n   b. ADQL upload of all AT-HYG positions + JOIN against gaia_source\n      (similar to dch.18's WDS upload procedure, but for the full\n      catalog — uses a single user table).\n2. Cross-match: for each AT-HYG row, find the nearest Gaia DR3 source\n   within ~1-2″ tolerance with parallax overlap (3σ).\n3. Group AT-HYG rows that share a Gaia DR3 source. For each group of\n   size \u003e 1: log all members, drop the fainter rows, keep the brightest\n   (preferring HIP'd over un-HIP'd, then absmag tiebreak).\n4. Emit `data/athyg_duplicates.tsv` (or similar) listing every dropped\n   row + the kept master, for inspection.\n5. `build-catalog.ts` reads the dedupe TSV and skips the listed rows.\n\n# Acceptance\n\n- Documented procedure for refreshing the Gaia-side data (similar to\n  dch.18's manual upload step).\n- Dedupe pass runs as part of `npm run build:catalog` (or as a\n  prerequisite).\n- Log line gives final count of AT-HYG rows dropped.\n- All famous bright stars survive (Sirius A, α Cen A/B, Vega, Polaris,\n  Betelgeuse, Rigel, Antares — spot-check at minimum).\n- stellata-dch.11's xyz=(-8.7310, 1.6110, 5.4470) instance is one of\n  the dropped duplicates (validates the pass against a known case).\n\n# Why P2 (not P1)\n\nEmpirically the rate appears low — dch.11 surfaced one instance\nacross 313k rows, and the inferBinaries safety net catches the\ncollapsed case. The pipeline isn't broken; it has a residual gap.\nBump to P1 if post-dch.18 inspection reveals a non-trivial population\nof catalogue-native duplicates that aren't caught by the safety net.\n\n# Why this doesn't block dch.10\n\ndch.10's runtime evolution operates on the orbital-elements section\nof the binary, which only carries WDS-derived orbits. AT-HYG-native\nduplicates that are NOT part of a WDS pair carry no orbital elements\n(orbitIdx=NO_ORBIT) and won't move at runtime. The catalog still\ncontains spurious static rows, but they don't compound under live\norbital evolution.\n\n# Related\n\n- stellata-dch.11 — single-instance investigation that triggered this\n  bead.\n- stellata-dch.18 — Gaia upload (handles WDS-derived spurious; this\n  bead handles AT-HYG-native spurious).","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T14:14:51Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:14:21Z","closed_at":"2026-05-15T10:14:21Z","close_reason":"folded into Phase 2 Stage 2 source-ID resolution chain (stellata-dch.28). AT-HYG-Gaia dedupe now an output of the resolution chain rather than a separate pass.","dependencies":[{"issue_id":"stellata-dch.19","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-12T15:14:50Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.19","depends_on_id":"stellata-dch.11","type":"related","created_at":"2026-05-12T15:14:58Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.19","depends_on_id":"stellata-dch.18","type":"blocks","created_at":"2026-05-12T15:14:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.17","title":"Multiple-star matching: orbit-derived eclipse photometry for EA/EB/EW eclipsing binaries","description":"dch.14 will suppress GCVS intrinsic pulsation for EA/EB/EW eclipsing binaries, on the basis that dch.10's orbital evolution will produce the photometric dip naturally. But dch.10's scoped surface (BinaryOrbitField) only evolves *positions* — it doesn't subtract luminosity during occultation. Without this bead, eclipsing-binary classes would render with no brightness modulation at all (pulsation suppressed by dch.14, eclipses not generated by dch.10) — losing exactly the famous Algol-style behavior that motivated suppressing the GCVS pulsation in the first place.\n\n# What an eclipse looks like physically\n\nWhen B passes in front of A (line-of-sight crosses each disc's projected outline):\n- Geometric occultation: angular area of A covered by B → fractional luminosity removed.\n- Limb darkening modifies the dip shape; skip for v1 (near-circular dip approximation reads correctly).\n- Total eclipses → flat-bottomed dip (B's disc fully inside A's projected outline).\n- Secondary eclipse (A in front of B) → smaller dip from B's lower luminosity.\n\n# Implementation sketch\n\nPer-frame, for each (A, B) pair whose orbit fits the eclipsing criterion (i close to 90°, near-equal physical radii, current orbital phase in the eclipse window):\n- Compute B's center xy in A's local sky plane from R(t) (line-of-sight component → z; in-plane → xy).\n- Compute B's angular radius and A's angular radius at the system distance.\n- If |xy| \u003c r_A + r_B, compute geometric overlap (analytical for two circles — closed-form, no integration).\n- Subtract `(overlap_area / (π·r_A²)) * L_A` from A's effective luminosity.\n- Adjust rendered photometric output downward by the same fraction.\n\n# Touch points\n\n- New `src/client/binary-eclipse-pure.ts` — circle-circle overlap helper (vitest-covered, analytical).\n- BinaryOrbitField (dch.10) extended with `getEclipseAttenuation(starIdx, t): number` consumed by the renderer's brightness path.\n- The renderer multiplies the per-star apparent-magnitude term by the attenuation factor.\n- dch.16 (composite-spectrum split) must land first so B's physical radius is correct — otherwise the eclipse depth scales off the wrong disc area.\n\n# Acceptance\n\n- Algol shows a clear 2.87-day dip cycle when zoomed in enough to time it (warp speedup helps).\n- β Lyrae (EB type) shows a longer-period dip with smoother shoulders.\n- Non-eclipsing binaries (i far from 90°) show no dip — guard against false positives.\n- α Cen still looks photometrically constant.\n- The dip depth matches V-band amplitude from GCVS within ~30% (we can't model limb darkening + temperature contrast precisely; a rough match is the bar).\n\n# Couples with\n\n- stellata-dch.14 (pulsation suppression for EA/EB/EW): without dch.17, dch.14 leaves eclipsing stars flat. dch.17 fills in the dip; dch.14 stops double-counting.\n- stellata-dch.16 (composite spectrum split): B's radius needs to be physically correct for eclipse-depth scaling.\n- stellata-dch.10 (BinaryOrbitField runtime): supplies R(t) per frame; this bead extends BinaryOrbitField with the brightness pass.","status":"closed","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-12T13:32:02Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:24:10Z","closed_at":"2026-05-15T10:24:10Z","close_reason":"superseded by stellata-dch.41 (Phase 5 — Eclipse photometry for EA/EB/EW from orbital elements). Same scope but rephased to land AFTER Phase 2 (orbital elements) + Phase 5 (mass-ratio q) so the orbital + mass inputs are correct before photometry computation. --force used because dch.17 was blocked-by dch.10; the successor bead dch.41 carries the equivalent dep on dch.10's BinaryOrbitField runtime.","labels":["multiples-pipeline","physics","variability"],"dependencies":[{"issue_id":"stellata-dch.17","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-12T14:32:02Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.17","depends_on_id":"stellata-dch.10","type":"blocks","created_at":"2026-05-12T14:32:10Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.17","depends_on_id":"stellata-dch.14","type":"related","created_at":"2026-05-12T14:32:08Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.17","depends_on_id":"stellata-dch.16","type":"related","created_at":"2026-05-12T14:32:09Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.16","title":"Multiple-star matching: split combined WDS spectral types per component","description":"build-binaries.py inherits the WDS combined-spectrum string verbatim onto SYN-NNN secondary records. WDS publishes the *system* spectral type (e.g. `A1Vm+DA2` for Sirius) which parseSpectral then truncates at the leading half — Sirius B's record carries `A1Vm+DA2`, parseSpectral returns the A1Vm half, and B is treated as A-class MS instead of a white dwarf.\n\n# Visible impact\n\nFor every named WD secondary in WDS:\n- Renderer assigns A's spectral class to B → wrong color, wrong T_eff.\n- Stefan-Boltzmann radius uses A's class → wrong physicalRadius. Sirius B clamps to 0.08 R☉ (lower bound); real WD radius is ~0.013 R☉ — 6× too large.\n- parseSpectral.isWhiteDwarf returns false on B → dch.15's WD mass branch never fires on Sirius B et al.\n\nAffected pairs: Sirius B, Procyon B, 40 Eri B (WD secondaries); plus M-dwarf secondaries of hotter primaries.\n\n# Fix\n\nSplit the WDS spectral field on `+` (most common) and `/` (rarer). When the field has exactly two halves, assign half[0] to A and half[1] to B; when ambiguous or unsplittable, fall back to current behaviour (whole string carried, log a debug warning so the affected pairs surface).\n\nTouch points:\n- scripts/build-binaries.py — in process_pair, when constructing b_spect for the SYN path, split pair.spectral on '+' or '/' and pick the trailing component. For composite primary specs (e.g. `O5III+OB`) the split is by sequence: A gets the first token, B the second.\n- scripts/catalog-pure.ts — optional helper `splitCompositeSpectral(s: string): { a: string; b: string }` if the parsing complexity warrants a pure helper.\n\n# Acceptance\n\n- Sirius B parses as DA2 → isWhiteDwarf=true → renders as a tiny ~0.013 R☉ disc.\n- Procyon B and 40 Eri B parse as WDs.\n- M-dwarf secondaries of hot primaries inherit M class correctly.\n- No regression on MS+MS pairs where both halves give the same letter (most α Cen, Castor entries).\n- Add a vitest covering `A1Vm+DA2` → A, B halves; `K0V+DA4+M4V` → split on first `+` only or smarter? — decision in implementation.\n\n# Couples with\n\n- stellata-dch.15 (mass-ratio q): the WD mass branch needs this fix to fire at all on composite-spectrum WD secondaries. Together they form the full Sirius B fix.","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T13:31:49Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:23:46Z","closed_at":"2026-05-15T10:23:46Z","close_reason":"superseded by stellata-dch.36 (Phase 3 — parseSpectral splits composite WDS types per component). Scope is unchanged; execution gated on Phase 3 v6 binary format.","labels":["multiples-pipeline","physics"],"dependencies":[{"issue_id":"stellata-dch.16","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-12T14:31:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.15","title":"Multiple-star matching: spectral-class-aware mass-ratio q (WD/giant/subgiant fallbacks)","description":"build-binaries.py currently estimates q = m_B / (m_A + m_B) from each component's absmag via the main-sequence M-L relation M ∝ L^(1/3.5) (Cox 2000 §15.2). The relation is accurate to within ~20% on the bulk of the OBAFGKM main sequence but fails badly for non-MS components.\n\n# Worked example — Sirius B\n\n- M_V = 11.36 (real, WD luminosity)\n- L/L☉ = 10^((4.74-11.36)/2.5) ≈ 0.0023\n- MS-predicted M = L^(1/3.5) ≈ 0.18 M☉\n- True mass (from orbit) ≈ 1.02 M☉\n- q estimate at v5: 0.069 vs true ≈ 0.33 — factor of ~5 wrong\n\n# Why MS M-L fails for WDs\n\nWhite dwarfs aren't H-burning. Their luminosity is residual thermal cooling, decoupled from mass. The degenerate mass-radius relation R ∝ M^(-1/3) also inverts MS scaling: heavier WDs are *smaller*. The MS M-L is measuring the wrong physics entirely. Subgiants and giants are over-luminous for their mass (shell-burning), so the M-L over-estimates them — smaller error (≤2×) but still visualisation-relevant.\n\n# Visual consequence\n\nq controls the split of orbital motion between A and B about the system center of mass. With q wildly off, Sirius A barely wobbles and Sirius B swings far wider than reality. The relative orbit (A−B) is unaffected — P, e, a, i, ω, Ω all come from ORB6 — so the \"they orbit each other on a 50-year ellipse\" story stays right. The distortion only shows up zoomed in on one component watching it wobble against background.\n\ndch.10's render will reveal whether this is visible. File first, discard if invisible.\n\n# Proposed fix\n\n1. Spectral-class-aware MS mass table — Cox (2000) §15.2 per-(spectClass, subclass) MS mass; lookup via parseSpectral().\n2. WD branch — canonical 0.6 M☉ (mean of observed WD mass distribution) when parseSpectral returns isWhiteDwarf=true.\n3. Giant/subgiant branch — luminosity-class-aware lookup keyed on (spectClass, lumClass).\n4. Fold the new logic into a `componentMass(absmag, spectInfo): number` helper in catalog-pure.ts.\n\nparseSpectral already returns spectClass + lumClass + isWhiteDwarf — the helper exists; build-binaries.py just needs to consult it instead of using absmag alone.\n\n# Coupled concern — composite spectral inheritance\n\nCurrently SYN-NNN secondaries inherit the WDS combined-spectrum string (e.g. 'A1Vm+DA2' for Sirius B). parseSpectral takes the leading half ('A1Vm') and reports A-class MS. The WD branch in this bead only fires if the spect string starts with 'D' — so this bead either needs to come with a composite-split helper, or be co-filed with a separate 'split combined spectra per component' bead. See sibling follow-up.\n\n# Acceptance\n\n- Sirius B q ≈ 0.33 (currently 0.07).\n- Procyon B q ≈ 0.4 (currently underestimated; Procyon B is a WD).\n- 40 Eridani B q sensible.\n- α Cen q stays at ~0.45 (MS+MS, no regression).\n- SCIENCE.md 'Mass-ratio q' paragraph updated to describe the new fallback chain.\n\n# Discard criterion\n\nIf dch.10's render lands and the q distortion is not visually perceptible at any zoom level we afford, close as 'no visible impact'. Stellata is ground-truthed against physical reality but doesn't need to model effects below the perceptual threshold.","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T13:26:29Z","created_by":"Alex Marshall","updated_at":"2026-05-15T10:23:39Z","closed_at":"2026-05-15T10:23:39Z","close_reason":"superseded by stellata-dch.40 (Phase 5 — Spectral-class-aware mass-ratio q with WD/giant/subgiant fallbacks). Scope is unchanged but execution is gated on Phase 2 stages 5-7 landing first.","labels":["multiples-pipeline","physics"],"dependencies":[{"issue_id":"stellata-dch.15","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-12T14:26:28Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.15","depends_on_id":"stellata-dch.10","type":"related","created_at":"2026-05-12T14:26:34Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.15","depends_on_id":"stellata-dch.16","type":"related","created_at":"2026-05-12T14:32:07Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-5ub","title":"Clarify TaskCreate vs beads split in CLAUDE.md","description":"Current CLAUDE.md rule (\"Use bd for ALL task tracking — do NOT use TodoWrite, TaskCreate, or markdown TODO lists\") overshoots: it conflates \"ephemeral tracking is wasteful when work outlives the session\" (true) with \"ephemeral tracking is bad\" (not true). TaskCreate has a legitimate job — surfacing a live ticking checklist for multi-step work in this session — that beads doesn't do well. A bead per micro-step is noise in bd ready and noise in the JSONL.\n\nUpdate the Beads/Rules section in CLAUDE.md to:\n\n- Keep beads as the canonical tracker for anything outliving the session (BEFORE-writing-code rule preserved).\n- Permit TaskCreate / TodoWrite for in-session decomposition of an in-flight bead's sub-steps (too granular to file individually, no value next session).\n- Tie-break \"when in doubt, file a bead\" — and include a promote-to-bead escape hatch when a sub-step turns out to be follow-up work.\n- Keep \"never use markdown TODO files\".\n\nNo bd remember mirror — would just double up in session context.\n\nAcceptance: CLAUDE.md Beads/Rules block reflects the split; PR labelled skip-version-bump (docs-only).","notes":"Pivoted from CLAUDE.md edit to bd remember key 'task-tracking-policy'.\n\nThe original approach (PR #62, now closed) placed the new wording inside CLAUDE.md's auto-managed \u003c!-- BEGIN BEADS INTEGRATION --\u003e block — flimsy, easily clobbered by a future bd integration regenerate. The memory survives.\n\nSee bd memories task-tracking-policy for the policy itself.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-12T13:24:56Z","created_by":"Alex Marshall","updated_at":"2026-05-12T13:28:07Z","started_at":"2026-05-12T13:25:11Z","closed_at":"2026-05-12T13:26:39Z","close_reason":"PR #62 opened","labels":["docs"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.14","title":"Suppress intrinsic pulsation for GCVS eclipsing-binary classes (EA/EB/EW); let dch.10 orbit produce the photometric dip","description":"# Problem\n\nThe build pipeline reads GCVS `period` + `(min − max)` magnitude\namplitude and feeds both into every variable star regardless of GCVS\nvariability *type*. Our shader then pulses every variable's disc on\nits catalogued period via `R ∝ √L`, treating Algol-type eclipsing\nbinaries (EA) as if they were Mira-type radial pulsators.\n\nThis is the **right photometric signal** (the period and amplitude\nare correct) but the **wrong physical mechanism**:\n\n- EA / EB / EW types are eclipsing binaries — brightness drops happen\n  because one component transits in front of the other along an\n  edge-on orbit, not because the star physically swells.\n- Algol Aa1 (HIP 14576, B8V) appears pulsing in our current build\n  with `P = 2.87 d`. Physically Aa1 is a static B8V; the dip is Aa2\n  passing in front. ζ Aur (long-period eclipser), β Lyr (EB), W UMa\n  (EW contact) — same shape.\n\nDiscovered during smoke-testing of `stellata-dch.6`.\n\n# Fix outline\n\n1. Extend `parseGcvsMain` in `scripts/build-catalog.ts` to also read\n   the GCVS variability-type column (currently we only pull period\n   and max/min mag).\n2. Persist the variability type — repurpose reserved byte 37 in\n   `RECORD_LAYOUT` per `catalog-pure.ts`, with a small enum\n   (0=non-variable, 1=intrinsic pulsator [M, SR, L, DCEP, RR, RV,\n   δ Sct, …], 2=eclipsing [EA, EB, EW, ELL], …). Document the enum\n   alongside `RECORD_LAYOUT`.\n3. Renderer gates pulsation on `varType !== eclipsing`.\n4. Once `stellata-dch.10` lands the live orbital evolution, the EA\n   eclipse dip emerges naturally from one disc occluding the other\n   — no rendering hack needed, the physics carries it.\n\n# Important nuance\n\nThe gate is on **GCVS type**, not on \"is this star in a binary.\" Some\nintrinsic pulsators have binary companions (e.g. some Cepheids).\nThose should still pulse — the binary status is orthogonal to whether\nthe pulsation mechanism is intrinsic or geometric.\n\n# Order of operations\n\nBest to land this AFTER `stellata-dch.10` (live orbital evolution),\nso the eclipse dip is already in place before we remove the\nfake-pulsation surrogate. Landing it first would leave EA stars\nvisually stable for the duration between this bead and dch.10,\nwhich is worse than the current (physically-wrong but\nphotometrically-correct) signal.\n\n# Smoke\n\n- Algol: focus Algol Aa1, watch ~2.87-day cycle (engage warp). Should\n  see Aa2 transit Aa1 producing a brightness dip, not Aa1's own disc\n  pulsing.\n- β Lyrae (Sheliak): EB type, ~12.94 d period — similar test.\n- Mira (ο Cet): M type — should pulse intrinsically (radial\n  pulsator), behaviour unchanged.\n- δ Cephei: DCEP type — should pulse intrinsically, behaviour\n  unchanged.\n\n# Files likely touched\n\n- `scripts/catalog-pure.ts` — extend `RECORD_LAYOUT` with `varType`\n  byte; add the enum constant.\n- `scripts/build-catalog.ts` — parse the type column; map GCVS\n  abbreviations to enum.\n- `src/client/catalog-loader.ts` — read new field into typed array.\n- `src/client/shaders/star.frag.glsl` or vert — gate pulsation\n  uniform on `varType`.\n- Tests: `scripts/catalog-pure.test.ts` (enum + layout), shader\n  pulsation gating in `perceptual-magnitude.test.ts` (if mirrorable).\n- `SCIENCE.md` — note GCVS-type vs renderer-mechanism distinction.","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-12T11:30:15Z","created_by":"Alex Marshall","updated_at":"2026-05-12T11:30:15Z","labels":["multiples","physics","variability"],"dependencies":[{"issue_id":"stellata-dch.14","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-12T12:30:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dch.12","title":"Re-evaluate focus / zoom minDistance rule for stars with close binary companions","description":"# Symptom\n\nAfter `stellata-dch.6`, focusing α Cen A or Sirius A (or any newly\nbinary-flagged star) snaps minDistance out far enough to keep the\ncompanion in frame. Sirius A↔B = 11.8 AU, α Cen A↔B = 18.7 AU; the\ncamera now refuses to zoom closer than that, which is right for\n\\\"keep the companion visible\\\" but wrong for users who want to inspect\na single component up close.\n\nDiscovered during manual smoke-testing of dch.6 — Alex's first reaction\nafter seeing the pair render distinct was \\\"you can no longer zoom in\nclose to a star in a multi-star system due to the existing rule.\\\"\n\n# Existing behaviour\n\n`src/client/stellata.ts:252-267` (and adjacent) — when a focused star has\na binary companion, `controls.minDistance` is pushed out so the\ncompanion remains in the viewport. Previously this fired on ~14 mutual\npairs (a rounding error); after dch.6 it fires on ~14k. Includes every\nfamous nearby binary.\n\n# Design questions\n\n1. **Relaxation policy.** Always allow zoom past the companion-fits\n   bound? Allow only when the user explicitly opts in (double-click on\n   the focused star to \\\"isolate\\\")?\n2. **Companion-in-frame trigger.** Should the rule only kick in when\n   the companion is bright enough to be visible at the new\n   minDistance? Sirius B at absmag 11.36 may be too faint to render at\n   the 12-AU range anyway.\n3. **Companion-distance heuristic.** Maybe the rule should not kick in\n   at all when companion separation is below some threshold (e.g.\n   \u003c 50 AU) — those are inspector-only views where the user knows what\n   they're looking at, and the partner is always nearby on a small\n   pan/orbit.\n\nRecommend deciding (3) first since it's the cheapest fix and likely\ncovers the regression user-visibly.\n\n# Smoke\n\n- Focus Sirius A, zoom in close to inspect the disc — should be able\n  to fly past Sirius B's location without the controls bouncing.\n- Focus α Cen A, ditto.\n- Focus a wide visual binary (e.g. 61 Cyg A, sep ~ thousands of AU) —\n  prior \\\"keep partner in frame\\\" behaviour should still apply since\n  the partner won't be on-screen otherwise.\n\n# Files likely touched\n\n- `src/client/stellata.ts` around the binary minDistance enforcement\n  (lines ~252-267 + the setFocus path that calls it).\n- `docs/camera-controls.md` if the rule semantics change.","status":"open","priority":2,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-12T10:50:14Z","created_by":"Alex Marshall","updated_at":"2026-05-12T10:50:14Z","labels":["camera","multiples","ux"],"dependencies":[{"issue_id":"stellata-dch.12","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-12T11:50:13Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-8xd","title":"Defer focus lerp until search modal close animation finishes","description":"# Problem\n\nPR #61 (stellata-r9q) introduced a ~2 s glide-or-stay-put on focus\ntransitions. When the user opens the search typeahead via the `G`\nkeyboard shortcut and selects a star, the focus lerp starts\nimmediately on selection — but the search modal + its backdrop\ndarkening are still on screen (and fading out via CSS transition).\nThe first significant fraction of the camera animation is hidden\nbehind the dimming overlay, so the user only sees the tail end of the\nglide. The \"smooth focus-park\" UX is effectively wasted on the\nkeyboard-driven path that PR #61's release notes explicitly\nhighlighted.\n\n# Expected\n\nAfter a star is chosen via the search picker, the camera lerp should\nnot begin until the modal + backdrop have fully cleared from view.\nThe user sees the full ~2 s glide against the live scene, the way\nmouse-click focus already does.\n\n# Scope\n\nLikely applies to all search-driven focus paths, not just the `G`\nshortcut — keyboard `Enter` selection and mouse-click selection\nthrough the same picker hit the same close→focus sequence. Worth\nconfirming during fix; if only the keyboard path is affected, the\nfix is narrower.\n\n# Possible approaches\n\n1. Chain `focusStar(...)` off the modal's CSS transition-end event\n   (or a `transitionend` listener on the backdrop). Cleanest — ties\n   the camera start to the real close animation.\n2. `setTimeout(focusStar, MODAL_CLOSE_MS)` matching the modal's\n   transition duration. Simple but couples two literals; risks drift\n   if the CSS transition is later retuned.\n3. Restructure the search-select pipeline so the picker emits a\n   \"selection committed\" event after its close transition completes,\n   and `search.ts` / wherever the focus call lives subscribes to\n   that event rather than the immediate selection event.\n\nOption 3 is the most architecturally clean and matches Stellata's\nexisting event-bus patterns; option 1 is the cheapest fix. The bead\nimplementer should pick after looking at the actual modal close\nplumbing in `src/client/search.ts` / `typeahead.ts` /\n`keyboard-shortcuts.ts`.\n\n# Smoke\n\n- Default Sol view → press `G` → type \"Betelgeuse\" → Enter →\n  full ~2 s glide is visible against the live scene; no overlap\n  with the dimming overlay.\n- Same with mouse click on a search result.\n- Same with the constellation typeahead (`C` shortcut → DOM-relocated\n  picker), if applicable to focus there.\n\n# Notes\n\n- The keyboard-shortcut + DOM-relocate plumbing for the `G` / `C`\n  pickers is documented in `docs/ui-and-controls.md`.\n- The lerp primitives live in `src/client/focus-transition.ts`\n  (added in PR #61); no change there expected — the fix is in the\n  caller that decides *when* to invoke `focusStar`.","status":"open","priority":2,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:45:31Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:45:31Z","labels":["search","ux"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-anl","title":"Apply proper motion + radial velocity to star positions for current epoch","description":"# Problem\n\nAT-HYG positions are at a fixed reference epoch (likely J2000.0); stars\nwith high proper motion are visibly displaced from their true present-day\npositions. Barnard's star moves ~10.3 arcsec/yr — over the 26 years since\nJ2000.0 that's ~4.5 arcmin of angular drift, which is easily visible when\nthe user focuses on it in NAVIGATE mode. Other high-PM nearby stars\n(Kapteyn's, Groombridge 1830, 61 Cyg, Proxima, etc.) have similar issues.\n\nThe solar-system layer (stellata-3re) already updates planet positions\nto a wall-clock `t` via `tToJDE` / `isLive` (see `src/client/time.ts`,\n`src/client/ephemeris.ts`). The star catalog does not — so the rest of\nthe scene is anchored to \"now\" while stars are frozen at the catalog\nepoch.\n\n# Scope of this bead (research / design)\n\nProduce a written design before any code lands. Cover:\n\n1. **AT-HYG reference epoch.** Confirm the epoch for ra/dec/distance in\n   the `athyg_33_classic_ids` source. Document in SCIENCE.md.\n2. **Available velocity columns.** What proper-motion columns\n   (pm_ra*cosdec, pm_dec, in mas/yr or arcsec/yr) and what radial-\n   velocity column does the source CSV carry? What fraction of the\n   ~313k stars have full pm + RV, pm only, or neither? Spot-check via\n   `scripts/verify-catalog.ts` / a Python pass over the source CSV.\n3. **Correction math.** For stars with pm + RV, the rigorous correction\n   is a 3D linear extrapolation in barycentric Cartesian coordinates\n   from the catalog epoch to the target epoch (years × 3-vector\n   velocity). Without RV, only the tangential component is known; the\n   radial component must be zero-assumed (distance held fixed at\n   catalog epoch). Cite the standard references (e.g. ESA SP-1200 §1.5\n   for Hipparcos, or Butkevich \u0026 Lindegren 2014 for Gaia).\n4. **Stars with no velocity information.** Options to evaluate:\n   - Leave at J2000.0 position (simplest; visible error on high-PM\n     stars only, but those are precisely the ones the user notices).\n   - Use a spectral-class / luminosity-class mean velocity as a prior\n     (galactic-rotation correction). Risk: false precision.\n   - Mark them in a per-star flag and visually deprioritise (e.g. a\n     subtle uncertainty halo). Probably over-engineering for v1.\n   - Just leave them at J2000.0 — the no-velocity subset is dominated\n     by faint distant stars where angular drift is sub-arcsecond per\n     century anyway. Likely the right call; confirm via the data\n     distribution.\n5. **Build-time vs runtime correction.** Two options:\n   - **Build-time:** `scripts/build-catalog.ts` reads pm + RV, advances\n     positions to a fixed epoch (e.g. 2026.0) at preprocess time,\n     writes the corrected position into `public/catalog.bin`. Simple;\n     no shader / loader change. Rebuild required to advance the epoch.\n   - **Runtime:** Catalog binary carries J2000.0 position + velocity\n     columns. Per-frame (or per-second; positions move slowly) the\n     client applies `pos_now = pos_J2000 + v · (t - t0)` either on the\n     CPU (instance buffer update on the seconds timescale) or on the\n     GPU (uniform `t` + per-instance velocity attributes). Matches the\n     solar-system layer's wall-clock semantics; lets the user warp\n     time via the same `t` knob and see star drift; but adds memory\n     (3 extra float32 per star, ~3.5 MB on top of catalog.bin),\n     shader changes, and per-instance velocity in the GLSL pipeline.\n6. **Time reference baseline.** If build-time: pick the fixed target\n   epoch (2026.0 is the obvious choice — current observed year, aligns\n   with the time-readout convention). If runtime: route through\n   `Stellata.getT()` per stellata-consistency-at-the-seam rule 4\n   (single source of truth for time).\n7. **Test plan.** Pin Barnard's star (HIP 87937), Kapteyn's (HIP 24186),\n   61 Cyg A/B, Proxima, Groombridge 1830 — known motion vectors are in\n   the literature; the corrected positions should match published\n   2026 positions to ~arcsecond. vitest in `scripts/catalog-pure.ts`\n   for the pure math.\n\n# Output of this bead\n\nA design doc (likely `docs/proper-motion.md`) capturing the answers to\n1–6 above, plus follow-up implementation beads filed against the\nchosen approach (build-time vs runtime).\n\n# Why this matters\n\nCat 2 from stellata-prioritization-framework — physical reality\nfixes. The model claims to be a snapshot of the sky; an out-of-date\nsnapshot of the *nearby* (i.e. high-PM, low-distance, high-importance)\nstars contradicts that claim. The error is concentrated exactly in the\nstars users are most likely to recognise and focus on.\n\n# Related work\n\n- stellata-3re (solar system epic) — established the `t` / `tToJDE` /\n  `isLive` infrastructure; runtime approach would reuse it.\n- `src/client/time.ts`, `src/client/ephemeris.ts` — wall-clock plumbing\n  already in place.\n- SCIENCE.md — must be updated to document the chosen epoch and\n  correction model.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:04:05Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:04:05Z","labels":["catalog","physics","research"],"dependencies":[{"issue_id":"stellata-anl","depends_on_id":"stellata-3re","type":"related","created_at":"2026-05-12T10:04:09Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2br.3","title":"Log-distance arrival profile in camera-motion.ts","description":"## Why this exists\n\nThe visible UX change for stellata-2br. Once the extraction (sibling child 2) is in, this swaps the easing inside the shared helper. The three park-arrival sites (focus-park, warp Fly, unfocus) get the new feel for free.\n\nWarp Phase 3 is explicitly out of scope — it stays inline as the OBSERVE-mode handover. See `docs/camera-arrival.md` § Inventory + § Implementation plan.\n\n## What needs to be done\n\nInside `src/client/camera-motion.ts`, swap the legacy piecewise smoothstep for the cubic-Hermite log-distance profile from `docs/camera-arrival.md` § Profile:\n\n```\nu_eased = 3·u² − 2·u³\nd(u)    = d0 · (d_end / d0) ^ u_eased\npos     = target.center + (pStart − target.center).normalize() · d(u)\n```\n\nCamera distance from `target.center` evolves as a cubic-Hermite smoothstep over `log d`, giving equal time per decade and uniform angular-size growth. The orientation track (quaternion slerp) keeps its current ease — only the distance track changes shape. The same formula handles outbound (unfocus): when `d0 \u003c d_end`, `log(d_end/d0) \u003e 0` and the camera carries outward over the same `u_eased`.\n\nThe cubic-Hermite form (one polynomial, C¹-continuous, identical to GLSL's `smoothstep`) replaces the piecewise quadratic. Shape difference is invisible (~3% at quartiles) but collapses the implementation to a single line.\n\nCache `dir = (pStart − target.center).normalize()` at construction so tickArrival doesn't allocate or re-normalize per-tick.\n\n## Tests at `src/client/camera-motion.test.ts` (extend the file from child 2)\n\n- **Monotonicity**: `d` strictly decreases for inbound (`d0 \u003e d_end`), strictly increases for outbound (`d0 \u003c d_end`).\n- **Curve pin**: synthetic d0=5, dEnd=1 — pin `d(u)` at `u ∈ {0, 0.25, 0.5, 0.75, 1}` bit-exact via `toBe(d0 · (dEnd/d0)^f)`. Catches drift in the formula or the helper.\n- **Worked-example pinning**: pin the doc's Sol-from-1pc and Betelgeuse-from-200pc tables. Use `toBe` against the formula (bit-exact, locks helper-implements-formula) AND `toBeCloseTo` against the doc table's rounded values (3-sig-fig, locks doc-table-to-helper). Per `stellata-test-coverage-discipline` (b).\n- **Endpoint pinning**: `d(0) = d0`, `d(1) = d_end` exactly — implemented via explicit short-circuit in tickArrival so `camera.position.equals(pEnd)` stays bit-exact at u≥1.\n- **Zero-velocity endpoints**: numerical derivative at `u → 0` and `u → 1` must be at least an order of magnitude smaller than the slope at `u = 0.5` (where f'(0.5) = 1.5, the curve's peak rate). Absolute thresholds like \"within 1e-6 of zero\" are unachievable — the natural endpoint-slope scale is `O(d0 · ε · |log(dEnd/d0)|)`, e.g. ~2.76 for d0=100, dEnd=1, ε=0.001. The relative comparison vs midpoint slope is the right test.\n- **Degenerate `d0 == d_end`**: log ratio = 0, formula reduces to `d(u) = d0` for all u — graceful no-op, camera stays put. (Verify by sampling at u ∈ {0, 0.25, 0.5, 0.75, 1}.)\n\nThe \"already-at-park\" / `d0 ≤ d_end` short-circuit framing the previous bead version carried referenced a `tickFocusLerp` short-circuit that doesn't exist. The actual contract is: the **caller** guards (e.g. `stellata.ts:focusStar` checks `eyeDist \u003e parkDist` before calling `newFocusLerpFrom`). The helper itself runs the formula in all cases — necessary because outbound unfocus has `d0 \u003c d_end` and that's a valid input.\n\n## Acceptance\n\n- `npm run typecheck`, `npm test` pass.\n- Pinned-curve test from child 2 is now expected to **diverge** (no longer a no-op vs piecewise legacy). Replace those expectations with the new cubic-Hermite log-d values pinned bit-exact via `toBe`.\n\n## Manual smoke (per alex-stellata-smoke-tests)\n\n1. **Sol from 1 pc** — disc grows smoothly across the last ~3 decades, not in a final flash.\n2. **Betelgeuse from 200 pc** — slowdown visible well outside the giant disc; perceptual kick-in around d ≈ 0.01 pc.\n3. **Cloud focus** — ellipsoid arrival doesn't slam.\n4. **Warp Sol → Betelgeuse** (navigate) — Fly phase matches focus-park feel.\n5. **Unfocus from inside parkDist(Sol)** — outbound mirror does not snap.\n6. **Warp in observe mode** — Phase 3 (1200 ms) parallax slerp should be **unchanged** (it stays inline); only the Fly phase before it should feel decelerated.\n\n## Out of scope\n\n- Per-object-type calibration knobs. Single profile, no `dWindow`, no `WINDOW_FACTOR` — see `docs/camera-arrival.md` § What about a two-region split at `dWindow`?\n- Angular-rate-cap variant (`effectiveRadius` path is reserved for a future bead if wanted).\n- Warp Phase 3 (out of scope; stays inline).","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-12T08:01:42Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:01:29Z","started_at":"2026-05-15T14:52:50Z","closed_at":"2026-05-16T21:01:29Z","close_reason":"shipped in PR #66 (commit 25e4b8b)","labels":["camera","ux"],"dependencies":[{"issue_id":"stellata-2br.3","depends_on_id":"stellata-2br","type":"parent-child","created_at":"2026-05-12T09:01:42Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2br.3","depends_on_id":"stellata-2br.2","type":"blocks","created_at":"2026-05-12T09:02:02Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-2br.2","title":"Extract camera-motion.ts (behaviour-preserving refactor)","description":"## Why this exists\n\nImplementation step 1 of stellata-2br. Establish the shared module without changing any visible behaviour. Once this lands, the log-distance change (next bead) is a small, isolated diff inside one file with provable scope.\n\nDepends on the design doc (sibling child 1). See `docs/camera-arrival.md` § Helper API sketch + § Implementation plan.\n\n## Scope: three park-arrival sites (warp Phase 3 explicitly out)\n\nThe design doc concluded that warp Phase 3's position lerp (`pEnd → (0,0,0)`) is the OBSERVE-mode handover that absorbs the parkDist-sized gap between Fly's endpoint and OBSERVE's origin invariant — it is NOT a park-arrival in the angular-slam sense. It stays inline in `updateWarp` and is not routed through `tickArrival`. See `docs/camera-arrival.md` § Inventory.\n\n## What needs to be done\n\n1. **Create `src/client/camera-motion.ts`** with the API agreed in the design doc:\n   ```ts\n   export interface ArrivalTarget { center: Vec3; parkDist: number; effectiveRadius?: number; }\n   export interface ArrivalState { pStart, pEnd, qStart?, qEnd?, target, startMs, durationMs, d0, dEnd }\n   export function newArrival(...): ArrivalState;\n   export function tickArrival(s, nowMs, camera): { done: boolean };\n   ```\n   Initial implementation is the **current piecewise smoothstep** (`2t² / 1−2(1−t)²`) — no log-distance or cubic-Hermite swap yet (both land in 2br.3).\n\n2. **Route three arrival sites through it**:\n   - `src/client/focus-transition.ts:tickFocusLerp` becomes a thin wrapper that constructs an `ArrivalState` and delegates. Keep the existing exported signature so `stellata.ts` callers don't change.\n   - `src/client/stellata.ts:updateWarp` Fly phase — replace inline smoothstep with `tickArrival`.\n   - `src/client/stellata.ts:unfocus` outbound transition — replace inline lerp with `tickArrival`.\n\n3. **Warp Phase 3 stays inline.** Don't touch `updateWarp`'s post-arrival position+quaternion smoothstep block.\n\n4. **`parkDistance` stays in `focus-transition.ts`** — per-object property, not a motion concern.\n\n5. **Tests** at `src/client/camera-motion.test.ts`:\n   - **Curve-pinning**: at `u = 0, 0.25, 0.5, 0.75, 1.0`, asserted distances match the legacy piecewise smoothstep within 1e-9. Proves the refactor is a no-op.\n   - Endpoints match `pStart`/`pEnd` exactly.\n   - `done` flips at `u ≥ 1`.\n\n## Acceptance\n\n- `npm run typecheck`, `npm test` pass.\n- No file outside the touched ones changes behaviour.\n- `grep -n '2 \\* t \\* t' src/client/*.ts` returns ONE remaining match (warp Phase 3, explicitly excluded). The three park-arrival smoothsteps are gone.\n\n## Smoke (manual, per Alex's manual-smoke convention)\n\n1. Focus Sol from search — arrival looks identical to before.\n2. Warp Sol → Betelgeuse navigate — Fly phase looks identical.\n3. Inside parkDist of Sol, unfocus — outward lerp looks identical.\n4. Warp into Sol in observe mode — Phase 3 parallax looks identical (it should — it wasn't touched).\n\n## Out of scope\n\n- Log-distance profile and cubic-Hermite swap (next bead 2br.3).\n- Cross-host audit (2br.4).\n- Warp Phase 3 position lerp (stays inline by design — see § Scope).\n- Cloud `parkDist` refactor (clouds already have their own; leave alone).","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-12T08:01:31Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:01:28Z","started_at":"2026-05-15T13:47:13Z","closed_at":"2026-05-16T21:01:28Z","close_reason":"shipped in PR #66 (commit 25e4b8b)","labels":["camera","ux"],"dependencies":[{"issue_id":"stellata-2br.2","depends_on_id":"stellata-2br","type":"parent-child","created_at":"2026-05-12T09:01:31Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2br.2","depends_on_id":"stellata-2br.1","type":"blocks","created_at":"2026-05-12T09:02:01Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-2br.1","title":"Camera-arrival design doc — log-distance profile + calibration","description":"## Why this exists\n\nDesign gate for stellata-2br. Pin down the math + parameter choices before any code lands, so the implementation children have a doc to point at and tests have a target curve.\n\n## What needs to be done\n\nWrite `docs/camera-arrival.md` covering:\n\n1. **The angular-arrival problem**: `θ = 2·atan(R/d)` blow-up at small `d`; why time-parameterised smoothstep doesn't fix it.\n2. **Log-distance profile**:\n   ```\n   d(u) = parkDist · (dWindow / parkDist) ^ (1 − u_eased)\n   ```\n   - Derivation showing this gives equal time per decade of distance.\n   - Choice of `u_eased`: identity, smoothstep, or a tanh-edge for the in-window region.\n3. **`dWindow` formula**: default `512 × parkDist` (geometric midpoint of user's empirical 400–1000 range). Justify the factor. Note that for cloud arrivals where `parkDist` is centroid-based, the same multiplier applies.\n4. **Seam continuity at `d = dWindow`**: match position **and** velocity. Document the closed-form derivation that determines the outside-window time spent (so the smoothstep blends smoothly into the log-distance segment).\n5. **Edge cases**:\n   - `d0 ≤ dWindow` (already inside the window at start) — entire arrival is log-distance.\n   - `d0 ≤ parkDist` (already inside park) — no-op or instant settle, as today.\n   - Target without an effective radius (clouds) — `parkDist` is the only knob; log-distance still works.\n   - Outbound (unfocus) — time-reversed inbound; document the mirror property explicitly.\n6. **Add an entry to CLAUDE.md's doc index** pointing to the new file.\n\n## Acceptance\n\n- `docs/camera-arrival.md` exists, ~150–250 lines, with derivations and a worked example (Sol from 1 pc; Betelgeuse from 200 pc).\n- CLAUDE.md doc-index updated.\n- All math chosen here is what stellata-2br child (2) will lift into the helper.\n\n## Smoke / verification\n\nDoc-only commit. Verify by reading the worked examples and confirming the curve hits `parkDist` exactly at `u = 1` and `dWindow` exactly at the seam.\n\n## Out of scope\n\nNo code changes in this bead. Pure design.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-12T08:01:20Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:01:28Z","started_at":"2026-05-15T11:46:57Z","closed_at":"2026-05-16T21:01:28Z","close_reason":"shipped in PR #66 (commit 25e4b8b)","labels":["camera","docs","research","ux"],"dependencies":[{"issue_id":"stellata-2br.1","depends_on_id":"stellata-2br","type":"parent-child","created_at":"2026-05-12T09:01:20Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-2br","title":"Unified camera-arrival profile (log-distance deceleration)","description":"## Why this exists\n\nArriving at a focused star feels like getting slammed into it. Angular subtense scales as `θ = 2·atan(R/d)`, so the last decade of distance delivers nearly all of the perceptual size change. Every existing park-arrival interpolates position by **time** (smoothstep `2t² / 1 − 2(1−t)²` re-implemented inline at each site), which damps the time profile but does nothing about the `1/d` blow-up in angular space. The disc stays a pinprick for most of the arrival, then explodes into the frame.\n\nEmpirical user thresholds for where deceleration should kick in:\n- Sol: ~1000 AU remaining → `d / parkDist ≈ 1000`\n- Betelgeuse: ~0.01 pc remaining → `d / parkDist ≈ 295`\n\ni.e. somewhere in **10²–10³ × parkDist**.\n\n## Goal\n\n**Unification is the headline.** Every park-arrival that ends at a focusable object today re-implements its own easing inline. As future object layers land (planets, exoplanets, Local Bubble, nebulae, Radcliffe Wave, etc.) the arrival behaviour should be inherited from a single helper, not re-decided per object type. The deliverable is the helper as much as the deceleration itself.\n\n## Design output: `docs/camera-arrival.md` (stellata-2br.1)\n\nThe design doc concluded:\n\n1. **Single-region log-distance smoothstep**, no `dWindow` split. Velocity-continuity at a linear-d/log-d seam forces ~97 % of total time outside the window, which collapses the perceptual goal. The single-region log-d profile delivers \"decel kicks in over the last 3 decades\" property naturally from the smoothstep shape.\n2. **Cubic-Hermite easing** (`u_eased = 3u² − 2u³`) over `log d`. One polynomial, C¹-continuous, identical to GLSL's `smoothstep`. Replaces the codebase's legacy piecewise quadratic.\n3. **Warp Phase 3 stays inline** — it lerps `pEnd → (0,0,0)`, the OBSERVE-mode handover, not a park-arrival. Routing it through the helper would require a `d_end = 0` exception for zero perceptual gain.\n\n## Inventory of park-arrival sites (helper-routed)\n\n| Path | Location | Duration | Endpoint |\n|---|---|---|---|\n| Focus-park lerp (star, cloud) | `focus-transition.ts:tickFocusLerp` | `FOCUS_LERP_MS = 2000` | `parkDist` |\n| Warp Fly phase | `stellata.ts:updateWarp` | log-scaled `WARP_T_MIN_MS … WARP_T_MAX_MS` | `endOffset` (≈ destination `parkDist`) |\n| Navigate-mode unfocus | `stellata.ts:unfocus` | `OBSERVE_TRANSITION_MS = 1200` | `parkDist` (outbound) |\n\n## Out of scope (intentional)\n\n- **Warp Phase 3 (observe→observe arrivals).** Stays inline as the observe-mode handover. See `docs/camera-arrival.md` § Inventory.\n- **Observe-mode double-click aim slerp** (`aimAt`, `updateObserveAim`). Rotation-only, no angular-size problem.\n- **Pure quaternion swings in warp Reorient (Phase 1).** Rotation-only.\n\n## Shared module\n\nNew `src/client/camera-motion.ts`:\n\n```ts\nexport interface ArrivalTarget {\n  center: Vec3;\n  parkDist: number;\n  effectiveRadius?: number; // reserved for future angular-rate variants\n}\n\nexport interface ArrivalState {\n  pStart, pEnd, qStart?, qEnd?,\n  target,\n  startMs, durationMs,\n  d0, dEnd\n}\n\nexport function newArrival(...): ArrivalState;\nexport function tickArrival(s, nowMs, camera): { done };\n```\n\n`focus-transition.ts:tickFocusLerp`, the warp Fly branch of `updateWarp`, and the unfocus lerp delegate to it. `parkDistance` stays in `focus-transition.ts` — per-object property, not a motion concern. Future hosts supply `{center, parkDist}` and inherit the profile.\n\n## Children\n\n1. **stellata-2br.1** — Design doc at `docs/camera-arrival.md`. Doc-only commit. Blocks 2br.2.\n2. **stellata-2br.2** — Extract `camera-motion.ts` with legacy piecewise smoothstep preserved. Route the three park-arrival sites through it. Behaviour-preserving; vitest pins the legacy curve. Blocks 2br.3.\n3. **stellata-2br.3** — Swap to cubic-Hermite log-distance profile. Visible UX change. Blocks 2br.4.\n4. **stellata-2br.4** — Cross-host coverage audit for park-arrivals. Phase 3 explicitly out of scope.\n\n## Acceptance\n\n- All three park-arrival paths (focus-park, warp Fly, unfocus) route through a single helper.\n- Sol arrival from ≥1 pc and Betelgeuse arrival from ≥200 pc feel decelerated across the last ~3 decades of distance.\n- Cloud arrival inherits the new profile.\n- Cross-host audit confirms zero remaining inline smoothstep at park-arrival sites.\n- Warp Phase 3 untouched.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-12T08:01:02Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:05:22Z","labels":["camera","ux"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-r9q.3","title":"Apply smooth focus-park to molecular cloud focus","description":"# Problem\n\nCloud focus (in `src/client/molecular-clouds.ts` and the orchestrator in `stellata.ts`) currently has its own park / minDistance logic. After Child 2 (stellata-r9q.2) lands the generic `parkDistance(...)` + focus-lerp primitives, clouds should route through them instead of duplicating the snap behavior.\n\nPer Alex (2026-05-11): \\\"I want the same approach for future objects: clouds, planets, nebulae, etc.\\\"\n\n# Scope\n\n1. Identify the cloud-focus entry point (likely the click-handler in `molecular-clouds.ts` and the focus-set call in `stellata.ts`).\n2. Compute cloud `parkDistance(...)`:\n   - `R_pc` = cloud's bounding-ellipsoid radius (use the largest semi-axis, or the geometric mean — decide at work-time based on what reads best for the user).\n   - `dMinFloor` = whatever the current cloud manual-zoom floor is (or its analogue).\n   - No `extraFloor` for clouds (no binary-companion analogue).\n3. Replace the cloud-focus camera move with the shared `focusLerpTo(...)` primitive — same lerp-or-noop branching as stars.\n\n# Note on visibility\n\nPer CLAUDE.md, the molecular-cloud overlay is shelved in v1.0 — visibility off by default. The code path is intact (chart-mode still drives the invisible layer). This bead can still land; it just won't be user-visible until clouds re-enable. The point is to avoid divergence: when clouds light back up, the focus UX should already match stars.\n\n# Files\n\n- `src/client/molecular-clouds.ts` — cloud picking + focus path.\n- `src/client/stellata.ts` — wherever cloud focus is dispatched.\n\n# Smoke\n\nManual (when clouds are re-enabled, or via debug flag during the work):\n1. Toggle clouds on; click a distant cloud → ~2 s glide; parks at sensible distance.\n2. Toggle clouds on; navigate close to a cloud, then click-focus it → camera stays put.\n3. Re-focusing an already-focused cloud → no double-snap.\n\n`npm run typecheck` and `npm test` green.\n\n# Depends on\n\nstellata-r9q.2 / Child 2 (generic mechanism must exist).","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T12:16:08Z","created_by":"Alex Marshall","updated_at":"2026-05-12T07:29:00Z","started_at":"2026-05-11T20:19:00Z","closed_at":"2026-05-12T07:29:00Z","close_reason":"Closed","labels":["molecular-clouds","pr-61","ux"],"dependencies":[{"issue_id":"stellata-r9q.3","depends_on_id":"stellata-r9q","type":"parent-child","created_at":"2026-05-11T13:16:07Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-r9q.3","depends_on_id":"stellata-r9q.2","type":"blocks","created_at":"2026-05-11T13:27:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-r9q.1","title":"Consolidate astronomical constants into single source","description":"# Problem\n\nAstronomical constants are defined inline in multiple TS files:\n\n- `AU_PC = 1 / 206264.80624709636` at `src/client/ephemeris.ts:23` — file-local, not exported.\n- `R_SUN_PC = 2.2543e-8` at `src/client/stellata.ts:250`.\n\nPer Alex (2026-05-11): \\\"AU constant will only ever be one value, absolutely import it. Create an astronomical constants file if it helps to consolidate all of these constants.\\\"\n\nThe follow-up bead `stellata-r9q` (smooth focus-park) needs to use `AU_PC` from a shared location rather than redefine it.\n\n# Scope\n\n1. Create `src/client/astronomy-constants.ts` (or merge into an existing pure-helper file if a clear home exists) exporting at minimum:\n   - `AU_PC`\n   - `R_SUN_PC`\n2. Replace `src/client/ephemeris.ts:23` and `src/client/stellata.ts:250` with imports from the new file.\n3. Add a tiny unit test asserting the numeric values (guards against silent typo regression in future).\n\n**Zero behavior change.** Anything else discovered (shader / Python / test stragglers) is OUT of scope for this bead — those are handled by the companion 9mm sweep bead.\n\n# Files\n\n- New: `src/client/astronomy-constants.ts` (or chosen home)\n- Modified: `src/client/ephemeris.ts:23`, `src/client/stellata.ts:250`\n- New test: alongside the constants file\n\n# Smoke\n\n- `npm run typecheck` — green.\n- `npm test` — vitest green; includes the new constants-values test.\n- Build artifacts (catalog.bin, etc.) byte-identical before and after.\n\n# Blocks\n\n`stellata-r9q` Child 2 (smooth focus-park applied to stars) and the 9mm sweep bead.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T12:12:40Z","created_by":"Alex Marshall","updated_at":"2026-05-12T07:28:59Z","started_at":"2026-05-11T20:05:56Z","closed_at":"2026-05-12T07:28:59Z","close_reason":"Closed","labels":["pr-61","ux"],"dependencies":[{"issue_id":"stellata-r9q.1","depends_on_id":"stellata-r9q","type":"parent-child","created_at":"2026-05-11T13:12:39Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.183","title":"Automate build:catalog smoke check — assert record counts against committed expected values","description":"Recurring smoke-step pattern across PRs that touch build-catalog.ts: 'npm run build:catalog reports the same record counts (313242 records, 11316 Hip + 13926 HD GCVS xrefs, 3677 variables matched) as pre-PR.' Alex flagged 2026-05-11 that this is hard to eyeball — automate it programmatically.\n\nApproach: build-catalog.ts writes a structured summary of every headline count it logs (records, GCVS xrefs, variables, binaries, CCDM doubles, search-index entries, name-table size) and compares against scripts/build-catalog-expected.json. Mismatch exits non-zero with a clear diff. UPDATE_BUILD_COUNTS=1 env var refreshes the expected file when counts legitimately change.\n\nAcceptance:\n- scripts/build-catalog-expected.json committed with current shipped counts.\n- npm run build:catalog passes when counts match, fails clearly when they don't.\n- npm run build:catalog UPDATE_BUILD_COUNTS=1 rewrites the expected file.\n- PR-template smoke step for build-catalog.ts changes drops to 'build:catalog passes (count assertion is automated)' or similar.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T08:26:39Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:47:59Z","started_at":"2026-05-11T08:26:49Z","closed_at":"2026-05-11T08:47:59Z","close_reason":"Closed via PR #59 (Automate build:catalog count smoke check) — merged 2026-05-11","labels":["tooling"],"dependencies":[{"issue_id":"stellata-9mm.183","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-11T09:26:39Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-84z","title":"Split camera-modes.md and extract url-state.md","description":"Per-session context efficiency. Two doc restructures:\n\n1. Split docs/camera-modes.md (464 lines) into 3 files:\n   - docs/camera-warp.md — warp animation state machine\n   - docs/camera-observe.md — OBSERVE mode + look-around controller + POI dispatch\n   - docs/camera-controls.md — near-plane vs minDistance, TrackballControls tuning, two-finger roll\n\n2. Extract URL state from docs/architecture.md into docs/url-state.md (~85 lines).\n\nUpdate CLAUDE.md doc index and all inbound cross-doc references.\n\nPure-docs change: attach skip-version-bump label on PR.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T07:32:58Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:08:46Z","started_at":"2026-05-11T07:33:05Z","closed_at":"2026-05-11T08:08:46Z","close_reason":"merged in PR #57","labels":["docs","pr-57"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ace","title":"togglePoi silently fails on three reject paths — needs visible feedback","description":"**Problem**\n\n`togglePoi` (`src/client/stellata.ts:1227-1248`) has three silent-reject paths: Sol exclusion, missing HIP, and the POI hard-cap. Each only emits a `console.info` line — the user sees nothing on screen. Click feels broken.\n\nReported by Alex (2026-05-10) after a click on what appeared to be a normal star did nothing; only checking the console revealed `[POI] cannot pin a star without a Hipparcos ID.`.\n\n**Locations**\n\n- `src/client/stellata.ts:1229-1232` (Sol exclusion)\n- `src/client/stellata.ts:1233-1236` (no HIP)\n- `src/client/stellata.ts:1243-1246` (POI_HARD_CAP reached)\n\n**Suggested approach**\n\nSurface each rejection visibly without leaving observe-mode. Options:\n\n- (a) Toast / chip at top-right of the canvas, fades in ~2s. Existing styles in styles.css cover `.toast` shape; nothing similar wired today.\n- (b) Brief pulse on the cursor pointer + tooltip text override (\"Sol is shown via the bottom arrow\" / \"This star has no HIP — POIs require one\" / \"POI cap reached (16) — unpin one first\").\n- (c) Status line in the existing brand/info area.\n\n(a) is probably cleanest and matches the prevailing UI pattern. Wire from `togglePoi` via a callback or a fresh event-bus channel (`pois:rejected` carrying reason).\n\nSister-layer extension: `setPois` (`src/client/stellata.ts:1255-1270`) silently drops the same three reject paths during URL restore. That's by design (URL was already validated) but a one-line `console.info` could help future debugging — separate from this bead.\n\n**Smoke**\n\nAfter fix:\n1. Try to pin Sol in observe → reason surfaces.\n2. Try to pin a star with no HIP → reason surfaces.\n3. Pin 16 stars, click a 17th → cap message surfaces.\n\nCross-link to stellata-02d — if (a) of that bead lands (in-memory POIs for non-HIP stars), the no-HIP reject here goes away, leaving Sol + cap.","status":"open","priority":2,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-10T21:39:19Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:39:19Z","labels":["observe","poi","ux"],"dependencies":[{"issue_id":"stellata-ace","depends_on_id":"stellata-02d","type":"related","created_at":"2026-05-10T22:39:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-02d","title":"POIs silently reject stars without a HIP ID — decide non-HIP fallback strategy","description":"**Problem**\n\n`togglePoi` at `src/client/stellata.ts:1227-1248` rejects any catalog row where `catalog.hip[idx] === 0`. The gate exists because the POI URL-state encoding (`src/client/url-state.ts` ~line 640 and ~line 924) packs each POI as a 3-byte HIP ID, so a non-HIP star couldn't survive a URL reload anyway.\n\nThis produces a silent UX failure: stars in the AT-HYG classic-IDs subset that have a non-empty display label (Bayer, Flamsteed, HD, HR, or Gl) but no Hipparcos ID look perfectly pin-able to the user. Hover surfaces the data card; clicking does nothing visible. Only the console `console.info('[POI] cannot pin a star without a Hipparcos ID.')` reveals why. Surfacing the rejection is the sister bead stellata-ace; **this** bead is the architectural question of whether non-HIP stars should be pin-able at all.\n\n**Why this exists**\n\nAT-HYG v3.3 cross-matches stars across HIP / HD / GLI / TYC / etc. Many rows have null `hip` but a valid display label assembled from one of the other ID columns. The PR #53 picker resolves these correctly under the cursor, so the gate is the only thing preventing pinning.\n\n(Audit on 2026-05-10 confirmed AT-HYG's `hip` column is bounded to the real Hipparcos range 1–120404 — no synthetic / out-of-range HIP IDs are ever displayed.)\n\n**Decision needed**\n\n- (a) **In-memory-only fallback** — allow non-HIP POIs in the live session; they don't get serialised to the URL. Warn the user on share (or just silently drop them on URL save). Lightest-touch architectural change.\n- (b) **Expand URL encoding to row indices** — switch POI encoding from 3-byte HIP to 4-byte row index (or hybrid: HIP when available, fall back to row index). Adds ~25% URL bytes per POI but every star becomes pin-able and shareable. Requires a URL state version bump (currently v3 — see stellata-p69).\n- (c) **Keep the gate; surface visibly only** — defer the architectural question. Handled entirely by stellata-ace. User still can't pin non-HIP stars; just gets a clear reason.\n\n**Locations**\n\n- `src/client/stellata.ts:1227-1248` (togglePoi)\n- `src/client/stellata.ts:1255-1270` (setPois — URL-restore path; mirrors the gate)\n- `src/client/url-state.ts` POI encode/decode (search 'POI' and '3-byte HIP')\n\n**Smoke**\n\nAfter fix, attempt to pin a known non-HIP star (search the catalog for rows with `hip=0` and a non-empty display label — e.g. a Gliese-only or HD-only star). Behaviour must match the chosen option.","status":"open","priority":2,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-10T21:36:00Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:39:52Z","labels":["observe","poi"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.181","title":"[PR #54] startUrlSync change-detector EPS too coarse at solar-system scales","description":"The per-frame URL change detector in startUrlSync uses a fixed absolute EPS=1e-3 pc for cam/tgt component-delta comparison. 1e-3 pc ≈ 206 AU, so when the camera is at solar-system scales (e.g. first-load's 5 AU park) a zoom-out doesn't trip any axis until it reaches ~258 AU on the dominant component. Pre-existing bug exposed by stellata-3re; surfaced during PR #54 smoke testing.\n\nFix: scale the per-axis threshold to 1% of vector magnitude (clamped to [1e-9, 1e-3] pc), so AU-scale zoom triggers fire at ~0.05 AU resolution while scene-scale (\u003e= 0.1 pc) keeps the existing 1e-3 absolute floor (where float32 ULP can approach 5e-4 pc and would otherwise spam writes from steady-state noise).\n\nAffects URL write throttling, not encoding correctness — applies to both v2 and v3.","status":"closed","priority":2,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T18:10:49Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:35Z","started_at":"2026-05-10T18:11:02Z","closed_at":"2026-05-10T18:17:35Z","close_reason":"Shipped in PR #54","labels":["pr-54"],"dependencies":[{"issue_id":"stellata-9mm.181","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T19:10:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3vl","title":"Add Share affordance: button/icon that copies current URL to clipboard","description":"The current page URL already encodes the full view (focus, mode, time, filters via url-state.ts), so users can already share what they see by copying the address bar. But that affordance is invisible — users don't realise the URL is shareable.\n\nAdd a small Share control somewhere on the page (likely a share icon, e.g. in the top-right card alongside the navigate/observe pill, or in the brand box area). Clicking it copies window.location.href to the clipboard via navigator.clipboard.writeText and shows a brief 'Link copied' confirmation.\n\nThe point is purely advertising the affordance: we want users to share what they're looking at, and a visible Share button signals 'this URL captures my view, go ahead and send it'.\n\n# Acceptance criteria\n\n- Visible share icon on the page in a sensible location (placement decision noted in PR).\n- Click copies current URL to clipboard (navigator.clipboard.writeText).\n- Brief visual confirmation (toast / inline label flip / icon swap) so the user knows it worked.\n- Clipboard fallback or graceful degradation if navigator.clipboard is unavailable (e.g. old Safari, insecure context) — at minimum, don't throw.\n- Does not appear in chart mode if it visually clashes (judgement call at impl time).\n- Works on desktop; mobile is best-effort (mobile epic still deferred).\n\n# Smoke step\n\nOpen app, focus a star, click Share, paste into a new tab — should restore the same view.","status":"closed","priority":2,"issue_type":"feature","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T17:51:30Z","created_by":"Alex Marshall","updated_at":"2026-05-11T09:44:23Z","started_at":"2026-05-11T09:08:31Z","closed_at":"2026-05-11T09:44:23Z","close_reason":"Merged in PR #60","labels":["ui"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.178","title":"Lambertian phase function phi(α) lives only in vert shader — pure math, no CPU mirror, no test","description":"**Confidence: Medium**\n\n**Finding**\nThe Lambertian phase function `φ(α) = (sin α + (π − α)·cos α)/π` is computed at planet.vert.glsl:90:\n\n```glsl\nfloat phi = (sin(alpha) + (PI_CONST - alpha) * cos(alpha)) / PI_CONST;\n```\n\nThis is a closed-form pure scalar function — ideal for unit testing on the CPU — but no TS mirror exists, and no vitest covers it. The PR description explicitly tags this as \"Medium\" honesty: \"The Lambertian phase function … is the standard formula; not vitest-tested.\"\n\nThis is **distinct from 9mm.96** (which covers the integrated `m_planet = M_host + 5·log10(d/10) − 2.5·log10(...)` formula). 9mm.96 names the wider integration as the gap; phi(α) is the *factor inside* that integration that has its own boundary conditions worth pinning:\n\n- φ(0) = 1 (full illumination at opposition — used as the cull-distance proxy maximum).\n- φ(π/2) = 1/π ≈ 0.318 (quadrature — the half-lit Earth-from-Moon case).\n- φ(π) = 0 (back-illuminated — new Moon).\n- Monotonically decreasing on [0, π].\n\nThe PR even calls out a sister concept: future work via Mallama 2018 (PR #56) replaces phi() for Sol bodies. With phi() un-pinned, swapping in the Mallama polynomial wont have a pre-existing baseline to compare against — the polynomial→Lambertian boundary issue 9mm.171 raises is exactly the kind of regression a phi-unit-test would have caught.\n\n**Location**\n`src/client/shaders/planet.vert.glsl:84-90` (phi computation in shader); `src/client/perceptual-magnitude.ts` (where the mirror should land — alongside `apparentMagnitude`).\n\n**Suggested approach**\nAdd `lambertianPhaseFactor(alphaRad: number): number` to `perceptual-magnitude.ts`. Mirror it identical to the GLSL line. Pin tests against the four boundary conditions above plus a monotonicity sweep. When PR #56s `peakPhaseFactor` lands, it can call this same helper for the c0=0 default case instead of redefining phi inline.\n\n**Why**\nphi(α) is a small enough function that the cost is one TS function + ~5 unit tests, and it pays for itself the moment a CPU consumer wants to ask \"would this planet at this phase angle be visible?\" (e.g. focus-distance heuristics, search-rank ordering). Filing this as separate from 9mm.96 because the suggested fix shape differs (a tiny pure helper, not an integrated mirror), and because it lights up the Mallama integration in PR #56.\n\n**Smoke step**\n`npm test` includes the four-boundary tests + monotonicity sweep. Mutation-test by negating one term in the formula and confirm a test fails.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:23:23Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:50Z","started_at":"2026-05-11T08:02:07Z","closed_at":"2026-05-11T08:46:50Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-45"],"dependencies":[{"issue_id":"stellata-9mm.178","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:23:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.177","title":"PlanetBodyField.update() pipeline has zero test coverage beyond the cull-distance gate","description":"**Confidence: High**\n\n**Finding**\n`PlanetBodyField` has 9 lifecycle tests (`planet-body-field.test.ts:97-202`) but **none of them call `update()`** — the per-frame method is the load-bearing part of the class. The 9mm.102 bead covers the cull-distance gate specifically; the rest of `update()`s contract is unguarded:\n\n1. `update()` calls `host.positionsAt(t, scratch)`, then `rotateInto(scratch, bufLocalRel, base, host.orientation)` (lines 516-525). No test asserts the buffer actually receives the values.\n2. `recenter(newWorldOffset)` mutates each hosts `hostLocalPos = hostAbsPos − worldOffset` and writes it into `bufHostLocalPos` (line 268-275). The \"recenter shifts hostLocalPos\" test (test L136) only re-attaches and asserts `group.visible` — never actually peeks at `bufHostLocalPos` to confirm the shift landed.\n3. `rotateInto()` rotates the placeholder/ephemeris XYZ triples through `host.orientation`. Untested. A regression that flipped the quaternion or mis-applied the rotation would not be caught.\n4. The `host.positionsScratch` allocation gate (`positionsAt ? new Float32Array(n*3) : null`) is untested — both branches of `writeHostPositions` (positionsAt-driven vs placeholderEccentricAnomaly fallback).\n\nCombined with 9mm.102 (cull gate), this is the integration cluster the prior reviewer flagged with `alex-large-pr-honesty`: the math is unit-tested, the lifecycle is unit-tested, the *integration* of math + lifecycle into a per-frame buffer write is not.\n\n**Location**\n`src/client/planet-body-field.ts:296-313` (`update`); `src/client/planet-body-field.ts:265-275` (`recenter`); `src/client/planet-body-field.ts:512-535` (`writeHostPositions`); `src/client/planet-body-field.ts:539-556` (`rotateInto`); `src/client/planet-body-field.test.ts` (where the tests belong).\n\n**Suggested approach**\nThree cheap test additions (one per gap):\n\n1. `update()` calls a stub `positionsAt` that writes `[1, 2, 3]` for one planet. After update, assert `getHostLocalPositions(idx).slice(0, 3)` matches the rotated value (identity orientation for Sol → ecliptic-pole rotation). Mutation-test by changing the rotation axis and confirming the test fails.\n2. After `recenter(newOffset)`, peek `getHostLocalPositions` is irrelevant (its iLocalRel) — instead expose `getHostLocalPosForTesting(idx)` that returns the cached `host.hostLocalPos`, or peek `bufHostLocalPos[base..base+3]` directly. Assert `hostAbsPos − newOffset = hostLocalPos`.\n3. Placeholder branch: `attachHost` with `positionsAt: undefined`, then `update()`. Assert the buffer holds the placeholder Keplerian layout (one entry at perihelion +x for n=1).\n\n**Why**\nThis is exactly the integration-point gap the user called out: \"unit-test coverage probably has gaps.\" 9mm.96 covers the math-formula CPU mirror; 9mm.102 covers one branch of `update()` (cull). The other three branches are untested and form the real-world per-frame pipeline.\n\n**Smoke step**\n`npm test` passes the new tests; mutation-test by reverting the `applyQuaternion` line in `rotateInto` and confirming the rotation test fails.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:22:58Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:00Z","labels":["pr-45"],"dependencies":[{"issue_id":"stellata-9mm.177","depends_on_id":"stellata-9mm.189","type":"parent-child","created_at":"2026-05-15T12:53:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.171","title":"[PR #56] Polynomial→Lambertian boundary creates visible brightness step (especially Saturn)","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nAt α = alphaMaxDeg the renderer hard-switches from the empirical polynomial to Lambert with no smoothing, producing a brightness step in the worst cases. Quick numbers: Mars at α=50° polynomial gives φ ≈ 0.474, Lambert gives 0.708 — a ~50% jump (≈0.4 mag). Saturn at α=6.5°+ε is the more dramatic case: polynomial gives `10^((0.55 − 6.5·0.026)/2.5) = 10^((0.55 − 0.169)/2.5) ≈ 1.42`; Lambert at α=6.5° gives ~0.97 — a ~30% drop (≈0.4 mag). The PR test plan flags Mars (\"There's a known small brightness step at the polynomial→Lambertian handover — flag if jarring\") but doesn't mention Saturn. Saturn's case is mostly camera-irrelevant in v1 because typical Stellata viewing angles for Saturn from outside its orbit stay near α=0, but a viewer flying from Sol toward Saturn will cross the 6.5° boundary visibly.\n\n**Location**\n`src/client/phase-function.ts` line 82 (`if (aDeg \u003e coefs.alphaMaxDeg) return lambertianPhaseFactor(alphaRad);`) and the parallel branch in `src/client/shaders/planet.vert.glsl` line 104.\n\n**Suggested approach**\nThree options worth Alex's call. (a) Accept and document — note in the source comment that the discontinuity is a known v1 artefact; defer smoothing to 3re.x. (b) Smoothstep blend over a ~2° band on either side of `alphaMaxDeg` — cheap in shader, mirror in TS, mask the step. (c) Anchor the polynomial's α=alphaMaxDeg value as a one-shot constant brightness offset folded into Lambert beyond the boundary — preserves continuity, costs a single `mix`. Option (a) plus a smoke test plan note for Saturn at α≈6° is the lowest-cost path; option (b) is a small shader change.\n\n**Why**\nEmpirical phase curves with hard cutoffs are a classic source of \"why does the planet flicker\" bug reports. Saturn's c0 boost makes the discontinuity especially material because the polynomial sits well above 1 just inside the boundary and Lambert sits well below 1 just outside. Worth being explicit about whether v1 lives with it or smooths it.\n\n**Smoke step**\nManual: position camera so Saturn's α slowly crosses 6.5° (e.g. fly from outside Saturn's orbit through the inner solar system); look for a brightness step. If shipping option (a), add a note to the `docs/solar-system.md` phase-function section. If shipping (b)/(c), add a unit test pinning `mallamaPhaseFactor(SATURN_PHASE, 6.5° ± ε)` continuity.\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:13:17Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:04:15Z","started_at":"2026-05-10T16:23:31Z","closed_at":"2026-05-10T17:04:15Z","close_reason":"addressed in PR #56 (merged)","dependencies":[{"issue_id":"stellata-9mm.171","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:13:16Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.169","title":"[PR #56] No test that iPhaseCoefsA/iPhaseCoefsB buffer values are written correctly","description":"**Confidence: Medium**\n\n**Finding**\nThe PR adds two per-instance vec4 attribute buffers (`bufPhaseA`, `bufPhaseB`) plumbed through allocate, grow, write-static, flush, and shift-down code paths. The PR description rates this \"Medium confidence — exercised by 14 existing field tests... but the new buffer values themselves aren't directly read back.\" The lifecycle tests check attach/detach/grow/recenter mechanics but don't assert buffer contents — so a bug like swapping `pc.c4` and `pc.c5` in `writeHostStaticAttributes`, or forgetting to copy phase buffers in `growCapacity`, or shifting the wrong stride in `shiftInstancesDown`, would not be caught by the existing suite. The Sol-only v1 happens to land all 9 planets in slots 0..8, which means the grow path (capacity 16 → 32) and the shift-down path (compaction after detach) both execute on currently-unattested data.\n\n**Location**\n`src/client/planet-body-field.ts` lines 521–531 (writeHostStaticAttributes phase block), 388–397 (growCapacity copies), 636–637 (shiftInstancesDown phase shifts). Test file `src/client/planet-body-field.test.ts` (gap).\n\n**Suggested approach**\nAdd a small read-back test that attaches a host with a planet carrying a non-trivial `phaseCoefficients` (Saturn is the natural choice — non-zero c0, c1, alphaMaxDeg=6.5), then reads back the InstancedBufferAttribute arrays via `(geometry.attributes.iPhaseCoefsA as THREE.InstancedBufferAttribute).array` and asserts the Saturn slot's 8 floats match. One assertion across the static path catches the single-planet write bug; the existing grow/detach lifecycle tests will then exercise the copy/shift paths over the same data.\n\n**Why**\nThis is the single-largest piece of new mechanical plumbing in the PR (~40 lines split across allocate/grow/write/flush/shift), and it currently has zero direct coverage. A regression here is silent — the rendered Saturn just looks slightly off, no test fails — and the GPU side is the only consumer.\n\n**Smoke step**\nRun the new test against current code (passes). Swap `pc.c4` ↔ `pc.c5` in `writeHostStaticAttributes` and re-run (fails). Restore.\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:53Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:04:17Z","started_at":"2026-05-10T16:23:32Z","closed_at":"2026-05-10T17:04:17Z","close_reason":"addressed in PR #56 (merged)","dependencies":[{"issue_id":"stellata-9mm.169","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:12:53Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.169","depends_on_id":"stellata-9mm.96","type":"related","created_at":"2026-05-10T15:15:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.168","title":"No integration test pins the eleven-event surface against StellataEventMap","description":"**Confidence: Medium**\n\n**Finding**\nThe PR's headline contract is 'eleven typed events, named in StellataEventMap, emitted from stellata.ts at exactly the points the prior fire*Change methods used to fire.' `event-bus.test.ts` covers the `EventBus` core in isolation. There is no test that asserts the eleven events are actually emitted from stellata.ts (e.g. setting focus emits exactly `'focus'` then `'state'`; setVectorTo with a previous cloud destination emits exactly `'vectorCloud'(null)`, `'vector'(idx)`, `'state'` in that order). Per Alex's large-PR-honesty framing, integration points like this are exactly where the unit tests + a smoke step won't catch a missed wiring.\n\n**Location**\n`src/client/stellata.ts` — every `this.bus.emit('…', …)` site (~16 emits across 11 names)\nTest suite — gap\n\n**Suggested approach**\nAdd a Stellata-level test fixture that boots a minimal `Stellata` (or stubs the parts that need WebGL) and asserts emission sequences for the high-fanout entry points: `setFocus`, `setFocusedCloud`, `setVectorTo`/`setVectorToCloud`, `setFilter`, `enterObserve`/`exitObserve`, `startWarp`/`finishWarp`. If a full Stellata boot is too heavy for vitest, extract the pub/sub-fire portions into pure helpers and test those.\n\nAlternatively: a static checker that walks `stellata.ts` AST and confirms every keyof `StellataEventMap` has at least one `bus.emit` call site — catches the 'event added to map but never fired' regression cheaply.\n\n**Why**\nThe integration surface is what the smoke checklist in the PR description is leaning on, and a smoke checklist is the weakest layer in a regression-prevention strategy. Adding even one structural test would catch a future emit being deleted while the map still advertises the event.\n\n**Smoke step**\nNew test runs in `npm test`; mutating a single `bus.emit` call site (e.g. delete the focus emit) makes the test fail.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:52Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:15:14Z","started_at":"2026-05-15T12:05:30Z","closed_at":"2026-05-15T12:15:14Z","close_reason":"Folded into PR #51 (commits 00f39b0, 11d300a, 48e850f)","labels":["pr-51"],"dependencies":[{"issue_id":"stellata-9mm.168","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:18Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.166","title":"[PR #56] No test that peakPhaseFactor widens cull distance for Saturn-style hosts","description":"**Confidence: Medium**\n\n**Finding**\nThe new code path in `PlanetBodyField.attachHost` folds `peakPhaseFactor(planet.phaseCoefficients)` into the per-planet reflectance proxy that drives `cullDistancePc`. Saturn's c0=−0.55 boost raises Saturn's reflectance by ~1.66× and the cull distance by ~√1.66 ≈ 1.29×. The PR description acknowledges this widening but no test asserts it: the `cullDistancePc` block in `planet-body-field.test.ts` is a pure-function suite that doesn't exercise `attachHost`, and the lifecycle tests that do call `attachHost` use a synthetic `makePlanet` factory that never sets `phaseCoefficients`. The Saturn ring-boost path in production code is therefore untested at the integration level.\n\n**Location**\nProduction code: `src/client/planet-body-field.ts` lines 213–224 (the `for (const planet of ps.planets)` block computing `brightestReflectance`). Test file: `src/client/planet-body-field.test.ts` (gap — no test references `SATURN_PHASE` or the `AttachedHost.cullDistance` field after attach).\n\n**Suggested approach**\nAdd a focused test in the `PlanetBodyField lifecycle` describe block: attach a host whose `PlanetSystem` contains a single planet with `phaseCoefficients = SATURN_PHASE` and another identical planet with `phaseCoefficients` undefined; assert that the resulting `cullDistance` (read off the host map via a small accessor or via re-derived `cullDistancePc`) widens by the expected √(peakPhaseFactor(SATURN_PHASE)) factor. Three lines + an expected ratio. Related: `stellata-9mm.102` covers the broader cull-distance optimization gap; this is a separable per-planet test of the new ring-boost path.\n\n**Why**\nThe whole point of the `peakPhaseFactor` integration is to keep Saturn visible past the prior cull threshold. Without a test, a future refactor that drops the multiplication (e.g. \"the phase factor cancels out at α=0\") silently re-narrows Saturn's cull and the bug shows up as Saturn vanishing at distances where it should still render.\n\n**Smoke step**\nThe new test passes against current code; mutate the multiplication out (delete `* peakBrightness`) and watch the assertion fail.\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:30Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:04:17Z","started_at":"2026-05-10T16:23:32Z","closed_at":"2026-05-10T17:04:17Z","close_reason":"addressed in PR #56 (merged)","dependencies":[{"issue_id":"stellata-9mm.166","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:12:30Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.166","depends_on_id":"stellata-9mm.102","type":"related","created_at":"2026-05-10T15:15:05Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.153","title":"onFocusHandlers fire 1.2 s before warp lands on observe→observe","description":"**Confidence: Medium**\n\n**Finding**\nPulling the destination recentre forward to phase-3 start changes `focusedStar` and fans out `onFocusHandlers` immediately at the start of the 1.2 s parallax slerp, instead of at `finishWarp` (the prior behaviour for observe→observe). Listeners include the focus-search box (renames the 'Location' / 'Focus' row to the destination's name during the slerp), the mode-toggle pill (re-enables OBSERVE based on `hasFocus` — already enabled here, so a no-op), and the focus-ring overlay (visibility flips to show; hidden during warp via `body.warping` CSS so user-invisible). The most user-noticeable is the search row — it pops to the destination's name about a second before the camera arrives.\n\n**Location**\nsrc/client/stellata.ts:2473 (recenterFocusToStar's `for (const h of this.onFocusHandlers) h(newIdx)` called from updateWarp:3674)\n\n**Suggested approach**\nTwo options. (1) Defer the focus-event fan-out until `finishWarp` for the observe→observe path: have `recenterFocusToStar` accept a `fireFocusEvent: boolean` (or split out the geometry from the event fan-out), and let `finishWarp`'s `swapObserveAnchor` call fan out the event. (2) Accept the early event because phase 3 is the user's first frame at the new vantage and labels updating early reads as 'arrived' rather than 'arriving'. Option (1) preserves the prior contract; option (2) is what the PR ships. Worth Alex's call — this is a UX-visibility judgment, not a correctness bug.\n\n**Why**\nThe PR comments lean on 'mirrors the navigate-mode path (setFocus(destIdx) recentre at finishWarp)' for the rationale, but navigate fires onFocusChange at finishWarp, not at phase-3 start. So the observe path's event timing is now AHEAD of navigate's by `postArrivalMs` (1.2 s). The asymmetry is undocumented and is a behavioural shift even if benign.\n\n**Smoke step**\n1. Long observe→observe warp Sol → Betelgeuse with the search panel open: confirm whether the 'Location' field switching to 'Betelgeuse' partway through phase 3 reads as bug or as feature.\n2. Mode-toggle pill stays steady; focus-ring stays hidden until warp ends.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:53Z","created_by":"Alex Marshall","updated_at":"2026-05-10T20:02:06Z","started_at":"2026-05-10T18:24:56Z","closed_at":"2026-05-10T20:02:06Z","close_reason":"merged in PR #50 (commit 783d396, v1.6.1)","labels":["pr-50"],"dependencies":[{"issue_id":"stellata-9mm.153","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:52Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.149","title":"getComputedTextLength cache keyed only on text — misses font-load + CSS-class-change invalidations","description":"**Confidence: Medium**\n\n**Finding**\n`distance-vector-overlay.ts` introduces a `getComputedTextLength()` cache keyed solely on the rendered string (`labelWidthCacheText`). The cache invalidates on text change. It does not invalidate on:\n\n1. **Web-font load (FOUT/FOIT)**: if the page's typeface loads asynchronously after the first measurement (common on first cold load), the measured pixel width may be from a fallback font. Subsequent ticks with the same string will keep returning the stale fallback width, mis-anchoring the warp affordance and right-edge clamp.\n2. **CSS class change on `#dist-label`**: chart-mode toggles a body class that re-paints the label colour. If a future style rule on `.chart-mode .dist-label` changes weight/letter-spacing/font-size, the cache won't invalidate.\n3. **Window resize w/ media-query font-size scaling**: less likely but plausible.\n\nToday (a) is the realistic concern. The cache hit rate is high in steady-state, so this isn't a hot-path correctness problem; it's a one-frame-at-page-load mis-anchoring that may or may not be visible.\n\n**Location**\nsrc/client/distance-vector-overlay.ts:62-67 + :184-191.\n\n**Suggested approach**\nCheapest robust fix: invalidate the cache on `document.fonts.ready` (one-shot, settles within a few hundred ms of page load). If the fonts API isn't available, fall back to a frame-N measurement (re-measure once at frame ≥30 even if the cached text matches).\n\nHeavier-weight fix: hook `MutationObserver` on the label element's `class` attribute and invalidate on changes — overkill for the current threat model.\n\nOr just bump the cache key to include the body's chart-mode class — single line change.\n\n**Why**\nThe optimisation (cache the layout flush) is sound; the cache key is just under-specified for the actual surface area of \"things that change rendered text width.\" One reasonable answer is \"do nothing — FOUT is one frame of mis-anchoring nobody notices.\" Worth a deliberate decision rather than the implicit assume-text-only that the current code encodes.\n\n**Smoke step**\nReload the page on a slow connection (devtools network throttle, \"Slow 3G\"), focus a star, set a destination, and check whether the warp affordance lands at the correct x-offset on first paint vs. after the font finishes loading. If the text shifts but the warp doesn't, the cache is stale.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:41Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:34Z","started_at":"2026-05-16T10:02:26Z","closed_at":"2026-05-16T21:38:34Z","close_reason":"merged via PR #55 (getComputedTextLength cache invalidates on document.fonts.ready)","labels":["pr-55"],"dependencies":[{"issue_id":"stellata-9mm.149","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.148","title":"SCIENCE.md Mercury angular-rate sanity-check rounds the wrong way","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nSCIENCE.md (PR #38 add, \"Solar system\" → \"Time `t`\" subsection) justifies the 60 s ephemeris-cache granularity with:\n\u003e Mercury moves ~3e-5 rad seen from Earth in 60s ≈ 8″, well below pixel resolution at any zoom.\n\n3e-5 rad = **6.18″**, not 8″. The cache-granularity argument still holds at 6″, but the new science doc carries an off-by-30% rounding in a quoted angular-motion sanity check.\n\n**Location**\nSCIENCE.md \"Time `t`\" subsection (PR #38 add, near the end of \"## Solar system\")\n\n**Suggested approach**\nEither: (a) replace \"≈ 8″\" with \"≈ 6″\" and recompute if a different conjunction-config was intended; (b) replace the round-trip arc with a different self-consistent illustration (\"a 60 s motion of ~3e-5 rad falls below ~6″, well below sub-pixel\"). Same one-line edit either way.\n\n**Why**\nSCIENCE.md is positioned in this PR as the citation-grade source for the project's claims. A factual rounding error in a quoted arcseconds figure undermines the document's authority — exactly the surface where it's least defensible.\n\n**Smoke step**\n1 rad = 206264.806″ → 3e-5 rad = 6.188″. Confirm the arithmetic, pick the corrected value, edit one line.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:40Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:49Z","started_at":"2026-05-11T07:53:49Z","closed_at":"2026-05-11T08:46:49Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-38"],"dependencies":[{"issue_id":"stellata-9mm.148","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:40Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.146","title":"[PR #56] SCIENCE.md 'same DOI' cross-reference is incorrect","description":"**Confidence: High**\n\n**Finding**\nThe new `Planet phase functions` paragraph in `SCIENCE.md` says \"Mallama, Krobusek, Pavlov 2018 (Icarus 282; same DOI)\", referring back to the immediately-prior `Planet geometric albedos` paragraph which cites `https://doi.org/10.1016/j.icarus.2017.05.018`. The Mallama 2018 phase-curve paper (Icarus 282) is actually DOI `10.1016/j.icarus.2016.09.023` — and that DOI is what `phase-function.ts` and the new `index.html` credit entry both correctly cite. So either (a) the prior albedo block has an unrelated DOI that the phase block should not equate to, or (b) the albedo block's DOI was already wrong. Either way \"same DOI\" misleads readers chasing the citation.\n\n**Location**\n`SCIENCE.md` line ~277 (\"Mallama, Krobusek, Pavlov 2018 (Icarus 282; same DOI)\") versus the prior block at line ~270 (`https://doi.org/10.1016/j.icarus.2017.05.018`).\n\n**Suggested approach**\nEither replace \"same DOI\" with the correct phase-paper DOI (`10.1016/j.icarus.2016.09.023`), or restructure so the albedo + phase paragraphs both cite the same Mallama 2018 paper once with the right DOI (the comprehensive 2018 Icarus 282 paper publishes both). The existing `2017.05.018` reference is a different Mallama paper and may need its own audit — likely Mallama \u0026 Hilton 2018 photometry — but that's separate from this fix.\n\n**Why**\nCitation hygiene. SCIENCE.md is the index of \"every external data source (catalogues, papers, DOIs, licences)\" per CLAUDE.md, so a wrong DOI cross-reference undermines the file's purpose. Reader chasing the phase polynomials gets sent to a different paper than the one the code actually used.\n\n**Smoke step**\nResolve both DOIs in a browser; confirm the phase paragraph points to \"Comprehensive wide-band magnitudes and albedos for the planets\" (Mallama, Krobusek, Pavlov, Icarus 282, 2018) — the paper whose Tables A-1.2 through A-6.2 the coefficients quote.\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:36Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:04:16Z","started_at":"2026-05-10T16:23:32Z","closed_at":"2026-05-10T17:04:16Z","close_reason":"addressed in PR #56 (merged)","dependencies":[{"issue_id":"stellata-9mm.146","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.143","title":"no test coverage for warp arrival pipeline (recenterFocusToStar / swapObserveAnchor / phase-3 pEnd shift)","description":"**Confidence: High**\n\n**Finding**\nPR #50 fixes a destination-star jitter caused by float32 precision loss in the projection chain during the 1.2 s observe→observe parallax slerp. The fix relies on three correctness invariants: (1) `recenterFocusToStar` is idempotent w.r.t. `focusedStar` (so `swapObserveAnchor`'s late-recentre path is a no-op when phase-3 already ran), (2) the `state.pEnd` shift exactly matches the `(dx, dy, dz)` applied by `recenterOrigin` (so the lerp `pEnd → B` continues to read the same physical waypoint), (3) skip mid-phase-3 vs skip mid-fly-phase land at the same destination pose. None of these are exercised by the test suite — `grep -rn 'swapObserveAnchor|recenterFocusToStar|finishWarp|updateWarp' src/client/*.test.ts` returns nothing.\n\n**Location**\nNo file. The gap is in src/client/*.test.ts. Adjacent: src/client/stellata.ts:2466-2494 (recenterFocusToStar + swapObserveAnchor), src/client/stellata.ts:3644-3700 (updateWarp phase 3).\n\n**Suggested approach**\nAdd a thin testing seam over the warp pipeline. The cleanest cut is to extract the pure-math waypoints (pStart/pEnd computation given A, B, sourceOffset, endOffset; the dx/dy/dz delta application; the first-frame-of-phase-3 invariant) into stand-alone helpers — same shape as catalog-pure.ts / perceptual-magnitude.ts. Vitest covers the math in isolation: idempotency under repeated recentres, pEnd-frame consistency after a synthetic recentre, the phase-3 lerp endpoint = (0,0,0) within 1e-6. The full Stellata class doesn't need to boot in jsdom for this — the pure helpers are enough to lock the invariants.\n\n**Why**\nThe fix is a precision-pressure mitigation; a regression that flips the sign of the pEnd shift, drops the re-bind of B, or breaks idempotency would still pass typecheck and would only manifest as a re-introduction of jitter on long observe→observe warps — exactly the symptom this PR was filed to fix. Without test coverage that invariant is one careful refactor away from breaking again.\n\n**Smoke step**\n1. New tests pass.\n2. Mutation check: temporarily remove the `state.pEnd.x -= dx` shift — at least one new test fails.\n3. Mutation check: remove the `if (this.focusedStar !== newIdx)` guard in swapObserveAnchor — at least one new idempotency test fails.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:30Z","created_by":"Alex Marshall","updated_at":"2026-05-10T20:02:06Z","started_at":"2026-05-10T18:24:56Z","closed_at":"2026-05-10T20:02:06Z","close_reason":"merged in PR #50 (commit 783d396, v1.6.1)","labels":["pr-50"],"dependencies":[{"issue_id":"stellata-9mm.143","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:30Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.142","title":"EventBus.emit varargs trade allocations for ergonomics on the per-frame 'frame' event","description":"**Confidence: Medium**\n\n**Finding**\n`EventBus.emit` uses `(...args: EmitArgs\u003cM, K\u003e)` and reads `args[0]` / `args[1]` for the name and payload. Each call site allocates the rest-args array. For the `'frame'` event, which fires from `animate()` once per render (60–120 Hz), the allocation is on the steady-state hot path that every overlay walks today. The old code (`for (const h of this.onFrameHandlers) h();`) made zero allocations.\n\n`docs/performance.md` documents the perf-hud's section budgets; `onFrame.total` (now arguably `'frame'`) is the one section the budget guidance specifically calls out as 'the full handler loop … overlays + chart labels.' Adding a per-tick allocation under that umbrella is the kind of regression the rest of the file warns against (see e.g. the URL-state cam-snapshot rewrite in PR #39, which removed string allocations from the same idle path).\n\n**Location**\n`src/client/event-bus.ts:30-36`\n\n**Suggested approach**\nTwo-overload signature: `emit\u003cK extends NoPayloadKeys\u003cM\u003e\u003e(name: K)` + `emit\u003cK extends WithPayloadKeys\u003cM\u003e\u003e(name: K, payload: M[K])`. Single concrete implementation reads `name` and `payload` directly without spread. Keeps the same caller ergonomics while dropping the per-emit array allocation. Or accept the trade-off and add a comment in `event-bus.ts` explaining the choice ('the V8 inline cache erases the spread cost; benchmarked at …') so the next reader doesn't second-guess.\n\n**Why**\nHot paths in this codebase are explicitly defended (perf-hud, idle-state URL writes, overlay self-gating). Trading a tiny ergonomic win in `emit` syntax for an extra allocation per frame is the wrong direction by the project's own conventions.\n\n**Smoke step**\nPre/post benchmark: `debug.panel()`, watch the `onFrame.total` ring-buffer with the panel open at 60 fps for 30 s. Bonus: a microbenchmark in `event-bus.test.ts` (skipped by default) capturing 1M `emit('tick')` calls.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:28Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:04Z","labels":["pr-51"],"dependencies":[{"issue_id":"stellata-9mm.142","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:16Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.135","title":"createTimeReadout teardown does not unsubscribe its three new bus listeners","description":"**Confidence: High**\n\n**Finding**\n`createTimeReadout` subscribes to `'planetSystem'`, `'filter'`, and `'warp'` and returns a teardown — but the teardown only clears the per-second `setInterval`. The three bus subscriptions stay attached forever, capturing `el` and the `updateVisibility` closure. Because `stellata.on()` now returns a real unsubscribe (the headline win of this PR), the fix is mechanical and the migration is a natural moment to make this teardown actually complete.\n\nThis was a pre-existing leak (the old API also didn't return an unsubscribe for these three), but the PR header explicitly calls out 'generalises the unsubscribe return — previously only onFrame had it … to all events,' so leaving the most prominent teardown in the codebase that *should* have used the new affordance still wired to the old no-op pattern is a missed integration point.\n\n**Location**\n- `src/client/time-readout.ts:74-76` — three `stellata.on(...)` calls, none of their unsubscribes are stored\n- `src/client/time-readout.ts:81-86` — teardown only clears `timer`\n\n**Suggested approach**\nCapture each `stellata.on(...)` return and call all three from the teardown alongside the existing `clearInterval`. Pattern matches `pin-debug-hud.ts` and `arrow-fade-debug-hud.ts` (which the PR already updated). One commit, one file.\n\n**Why**\nPR description explicitly motivates the migration on lifecycle: 'generalises the unsubscribe return … to all events.' The migration is incomplete if subscribers that already expose a teardown contract don't adopt the new affordance. Concretely: `time-readout` is created/torn-down by main.ts on its module boundary, so this affects HMR cycles in the dev loop today.\n\n**Smoke step**\nAdd a vitest that calls `createTimeReadout(...)()` then asserts the three subscribers were removed (e.g. via a mock Stellata that records sub/unsub counts). Manual: HMR-edit the module a few times in dev and confirm the bus internal handler set for `'planetSystem'` doesn't grow.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:08Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:15:13Z","started_at":"2026-05-15T12:05:29Z","closed_at":"2026-05-15T12:15:13Z","close_reason":"Folded into PR #51 (commits 00f39b0, 11d300a, 48e850f)","labels":["pr-51"],"dependencies":[{"issue_id":"stellata-9mm.135","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.132","title":"[PR #54] No direct unit test for varintLen / writeVarint / readVarint helpers","description":"**Confidence: High**\n\n**Finding**\nThe LEB128 helpers (`writeVarint`, `readVarint`, `varintLen` — lines 343-379) are tested only indirectly through end-to-end round-trips and one truncation-rejection test (url-state.test.ts:872-874). Specifically:\n- No test asserts `varintLen(v) === bytes` returned by `writeVarint` for representative masks (so the duplication risk in 9mm.106 has no guardrail).\n- No test that `readVarint` rejects a varint \u003e 5 bytes (the `shift \u003e= 32` guard at line 367) — only the `runs past blob end` path is tested.\n- No test that `readVarint` correctly decodes a 4-byte varint produced by `writeVarint` (bit-21 `t` field would emit one once stellata-nmu lands).\n- No test that the empty mask varint is exactly 1 byte (`varintLen(0) === 1`).\n\n**Location**\nsrc/client/url-state.ts:343-379 (helpers); src/client/url-state.test.ts:824-875 (integration tests but no helper-level tests).\n\n**Suggested approach**\nAdd a focused `describe('LEB128 helpers')` block. Export the three helpers (or test through a small integration). Cover masks {0, 1, 0x7f, 0x80, 0x3fff, 0x4000, 0x1fffff, 0x200000, 0xfffffff, 0x10000000, 0xffffffff} for write-then-read symmetry and for varintLen consistency. Also add an explicit test for the `Varint mask too long` error path.\n\n**Why**\nThe varint is the only piece of v3 that doesn't have a fixed-size analogue in v1 or v2 — it is genuinely new code, with two error paths and a length-determining loop. Helper-level tests are cheap and catch regressions in code paths that the integration tests would only exercise once stellata-nmu enables emission of bit 21 `t`.\n\n**Smoke step**\n`npm test` runs the new helper tests.\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:02Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:33Z","started_at":"2026-05-10T17:50:43Z","closed_at":"2026-05-10T18:17:33Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.132","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:02Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.131","title":"No test coverage for new dirty-track sentinels — first-write + idle-skip behaviour untested","description":"**Confidence: High**\n\n**Finding**\nThe PR adds new dirty-track state across five overlays (chart-labels.ts already had it). The PR description correctly identifies sentinel-init bugs as \"the most likely culprits\" for regressions, and routes them to manual smoke checks. But none of the new state is covered by automated tests:\n\n- No assertion that identical inputs frame-to-frame skip the setAttribute write.\n- No assertion that the very first frame after a state change writes (sentinel-init).\n- No assertion that hide()/show() cycles correctly reset the dirty state so a stale value across visibility transitions doesn't survive.\n- `projectWithNearClip` has 18 unit tests but they predate the scratch-vector swap, and none assert \"caller's input vectors are not mutated\" — the implementation does `projVa.copy(worldA)` (correct) but a future refactor that drops the `.copy()` would not be caught.\n\nThe PR cites \"485/485 passing\" tests but the count is unchanged from baseline — no new tests were added for the new behaviour. The \"unit tests catch projection math; visual checks catch DOM-state regressions\" framing is honest, but it leaves the regression class the PR description itself flags (sentinel-init) as the one without an automated catch.\n\n**Location**\nsrc/client/distance-vector-overlay.test.ts (existing tests for projection only). No `hud-overlay.test.ts`, `poi-overlay.test.ts`, `disc-mask.test.ts`, `constellation-overlay.test.ts`, or `focus-ring-overlay.test.ts` exists.\n\n**Suggested approach**\nAdd lightweight unit tests around the dirty-track contract. Two practical shapes:\n\n1. **Per-overlay smoke**: a JSDOM-based test that creates the overlay, drives `onFrame` twice with identical inputs, and asserts `setAttribute` was called exactly once for each tracked attribute (use a spy on the SVG element). Test `hud-overlay.ts` (most state) and `poi-overlay.ts` (per-Entry state) at minimum.\n\n2. **`projectWithNearClip` mutation assertion**: extend the existing test file to assert that the `worldA` and `worldB` inputs are unchanged after the call returns (catches a regression where the module-scope scratch is replaced with direct mutation).\n\n(2) alone is one new `expect` per test, ~15 lines total. (1) is the meatier addition but is the only way to lock in the dirty-skip behaviour.\n\n**Why**\nThe PR adds a new failure mode (stale dirty-track state) and routes verification to manual eyeballs in seven distinct browser scenarios. The unit-test gap is exactly the area the PR description flags as highest-risk. Adding tests in a follow-up bead is appropriate; gating the merge on them is the EM's call.\n\n**Smoke step**\n`npm test` should grow by N tests; tests should fail if the dirty-track guards are removed or the sentinels are mis-initialised.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:56Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:05Z","labels":["pr-55"],"dependencies":[{"issue_id":"stellata-9mm.131","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:17Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.127","title":"EventBus has no clear() — Stellata.dispose() leaves subscribers attached","description":"**Confidence: Medium**\n\n**Finding**\n`EventBus` exposes only `on()` / `emit()`. There is no `clear()` or `off(name)` method, and `Stellata.dispose()` does not touch the bus. This matches the pre-PR behaviour (the old eleven `on*Handlers` arrays were also never cleared on dispose), so it isn't a regression — but the migration would have been the natural moment to introduce `bus.clear()` since the new central abstraction can clean up all eleven event types in one call.\n\nIn the current single-page architecture this is moot (`Stellata.dispose()` is only called by HMR teardown / tests), but the closures captured by every subscriber pin the entire `Stellata` instance through the bus's internal Set, defeating any GC of the disposed scene graph until the page is reloaded. Long-running dev sessions that HMR-recreate Stellata accumulate stale subscribers from prior generations.\n\n**Location**\n- `src/client/event-bus.ts:11–34` — bus class, no clear/off\n- `src/client/stellata.ts:3730–3769` — dispose() releases every subsystem but never touches `this.bus`\n\n**Suggested approach**\nAdd `clear()` to `EventBus` (`this.handlers.clear()`) and call `this.bus.clear()` from `Stellata.dispose()`. Also call subscriber-side cleanup in modules that already return a teardown (e.g. `createTimeReadout`, `pin-debug-hud`) so the bus side empties before the listener-owned timers do. Optional second method `off(name)` for symmetry but probably not needed today.\n\n**Why**\nThe bus is now the single chokepoint for stellata's pub/sub — adding a one-line lifecycle method makes the abstraction complete and lets future-Claude reason about teardown without scanning eleven handler arrays. Test coverage already pins `on/emit/unsubscribe`; `clear` is a natural sibling.\n\n**Smoke step**\nUnit test in `event-bus.test.ts`: register handlers, call `clear()`, emit — handlers don't fire. Manual: in dev tools call `stellata.dispose()` and confirm `bus.handlers.size === 0`.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:50Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:15:13Z","started_at":"2026-05-15T12:05:28Z","closed_at":"2026-05-15T12:15:13Z","close_reason":"Folded into PR #51 (commits 00f39b0, 11d300a, 48e850f)","labels":["pr-51"],"dependencies":[{"issue_id":"stellata-9mm.127","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.126","title":"HTML title and PR-body smoke step disagree (case + missing \"An\")","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nPR #38's test plan says:\n\u003e Browser tab title reads \"Stellata — An Observational 3D Model of Our Galaxy\".\n\nThe actual `\u003ctitle\u003e` element in `src/client/index.html` reads:\n\u003e \"Stellata — Observational 3D Model of our Galaxy\"\n\nDifferences: missing \"An\" prefix and \"our\" lowercased. The same string lives only in one place, so whichever spelling was intended, the literal smoke-test step doesn't match.\n\n**Location**\nsrc/client/index.html line 6\n\n**Suggested approach**\nPick one spelling and align both. Title-case style (\"An Observational 3D Model of Our Galaxy\") matches typical document titles and the PR-body checklist; the shipped lowercase-\"our\" form reads more colloquial and might be what Alex actually preferred. Either is defensible — flagging the inconsistency between the smoke step and the artifact, not picking a winner.\n\n**Why**\nThe PR's own checklist contained a literal-string assertion that the merged code does not satisfy. Future \"did the title regress?\" smoke checks will diverge from the PR record.\n\n**Smoke step**\nView `src/client/index.html` head; compare `\u003ctitle\u003e` to the PR body's smoke-step phrasing.","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:49Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:24:25Z","closed_at":"2026-05-11T08:24:25Z","close_reason":"shipped title 'Stellata — Observational 3D Model of our Galaxy' is canonical; PR #38's smoke-step phrasing was wrong in the PR body but is historical record. No code change needed.","labels":["pr-38"],"dependencies":[{"issue_id":"stellata-9mm.126","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.118","title":"[PR #54] Test gap: worldOffset sub-mask elision (per-component) only covered for the all-zero case","description":"**Confidence: High**\n\n**Finding**\nThe v3 vec3 sub-mask is the headline optimisation, but the test file only directly verifies sub-mask elision for **cam** (lines 134-178) and the all-zero default case for tgt/up/worldOffset (lines 121-132, 180-183). There is no test that worldOffset with one or two components diverging from the default emits the right per-component sub-mask byte count — e.g. `worldOffset=[5,0,0]` should produce sub=1 + 4 bytes payload (5 byte field), not 12 bytes.\n\nSame gap for tgt and up sub-mask elision: tests that check `up=[0.7071, 0.7071, 0]` round-trip exist, but none assert the byte count of the resulting blob is smaller than v2's flat 12-byte vec3.\n\n**Location**\nsrc/client/url-state.test.ts:36-205 (vec3 fields block).\n\n**Suggested approach**\nExtend the existing `blobBytes(blob)` helper from the LEB128 block to assert byte counts on partial-divergence cases for each of tgt, up, worldOffset:\n- `tgt=[5,0,0]` → 1 ver + 1 mask + 1 sub + 4 x = 7 bytes\n- `up=[0,0,1]` → 1 ver + 1 mask + 1 sub + 4 z = 7 bytes (1+0=elide y, but 0+1=non-default x and z)\n- `worldOffset=[100,0,0]` → 1 ver + 3 mask + 1 sub + 4 x = 9 bytes\n\n**Why**\nThe encoder's per-key `componentDefaults` is the single most regression-prone piece of v3 — a future change that flips a default constant or breaks the strict-equality predicate would silently revert the bytes-saved gain to zero, with all current tests still passing. Byte-count assertions catch this immediately.\n\n**Smoke step**\n`npm test` runs the new assertions.\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:28Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:33Z","started_at":"2026-05-10T17:50:42Z","closed_at":"2026-05-10T18:17:33Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.118","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:28Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.117","title":"disc-mask.ts: tryPlace closure rebuilt every frame; siblings (clearCircle/placeCircle) hoisted out of the tick","description":"**Confidence: Medium**\n\n**Finding**\n`tryPlace` is declared inside the per-frame `onFrame` callback, which means a fresh closure is allocated every animation frame. The other two helpers in the same file — `clearCircle` and `placeCircle` — sit at factory scope (allocated once per `createDiscMask` call) so they don't churn per frame. The new code mixes the two patterns.\n\n**Location**\nsrc/client/disc-mask.ts:94-99 (the `const tryPlace = (idx: number) =\u003e { … }` inside the onFrame callback)\nCompare: src/client/disc-mask.ts:46 (factory-scope `clearCircle`) and src/client/disc-mask.ts:54 (factory-scope `placeCircle`).\n\n**Suggested approach**\nHoist `tryPlace` to factory scope and have it read the `used` counter and `seen` Set from a small mutable `state` object captured by closure once per factory call (or pass them in as args). The factory-scope helpers already follow this pattern — `placeCircle` takes `c` and `idx` as args, with `v` and `stellata` captured once. Mirror that.\n\n**Why**\nArchitectural fit. The file's other helpers were deliberately lifted out of the tick (likely for the same v8 hidden-class / closure-allocation reason this matters). Mixing the two patterns is mildly confusing — the next reader has to wonder whether `tryPlace`'s in-tick definition is load-bearing (it isn't — it just reads `used`/`seen`/`circles`/`ensureCircles`/`placeCircle`, all stable references after factory init).\n\n**Smoke step**\nDiff is mechanical (move + capture); `npm run typecheck` clean; manual PR smoke unchanged. Optional: `debug.perf()` can confirm the per-frame timing of the disc-mask tick before/after — should drop slightly in long-lived sessions.","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:26Z","created_by":"Alex Marshall","updated_at":"2026-05-11T07:44:35Z","started_at":"2026-05-10T22:06:05Z","closed_at":"2026-05-11T07:44:35Z","close_reason":"Closed","labels":["pr-52"],"dependencies":[{"issue_id":"stellata-9mm.117","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:25Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.114","title":"Welcome-modal copy is a sentence fragment after #info-star-count substitution","description":"**Confidence: Medium**\n\n**Finding**\nThe new welcome-modal first paragraph reads:\n\u003e Stellata is a physics-based, empirical 3D model of our galaxy that simulates what it would be like to actually be there. \\\u003cspan id=\"info-star-count\"\u003e—\\\u003c/span\u003e real stars, the planets of our Solar System, the Sun's heliopause, ...\n\n`info-modal.ts` does `countEl.textContent = starCount.toLocaleString()`, so at runtime the prose becomes \"...to actually be there. 313,000 real stars, the planets of our Solar System, ...\" — a verbless sentence fragment. The previous wording wove the count into a complete clause: \"Stellata is an interactive 3D map of \\\u003cspan\u003e real stars within ~1250 parsecs of the Sun.\" The PR loses the grammatical hook.\n\n**Location**\n- src/client/index.html lines 119-126 (welcome-modal first paragraph)\n- src/client/info-modal.ts lines 9-11 (textContent replacement)\n\n**Suggested approach**\nRewrite the first paragraph so the span sits inside a complete clause, e.g. \"...to actually be there. The model includes \\\u003cspan id=\\\"info-star-count\\\"\u003e—\\\u003c/span\u003e real stars, the planets of our Solar System, the Sun's heliopause, local interstellar dust, and the Milky Way backdrop — every object comes from a published observational catalogue or direct measurement.\" Or split into two sentences: keep the framing sentence intact, then start a new sentence \"It includes \\\u003cspan\u003e… real stars, the planets…\".\n\n**Why**\nWelcome modal is the first surface a fresh user sees. The substitution-driven fragment reads as a typo. Caught only by reading the rendered result, not the source string — easy to overlook on review.\n\n**Smoke step**\nLocal dev run, clear localStorage `stellata.info-dismissed`, reload, read the first paragraph aloud. Should parse as a grammatical sentence with the star count substituted.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:15Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:08Z","labels":["pr-38"],"dependencies":[{"issue_id":"stellata-9mm.114","depends_on_id":"stellata-9mm.191","type":"parent-child","created_at":"2026-05-15T12:53:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.112","title":"[PR #54] No test for v2-\u003ev3 auto-upgrade rewrite path (applyFromUrl setTimeout debounce)","description":"**Confidence: High**\n\n**Finding**\n`applyFromUrl` (lines 1138-1159) detects `decoded.version !== SCHEMA_VERSION` and schedules a 300ms-debounced `writeUrl` to silently upgrade the URL to v3. This is the central piece of the PR's `no bookmarks break, address bar shrinks` claim, and the test plan's manual smoke step calls it out (`Confirm it renders correctly and the address bar silently rewrites to ?v=AwEA... after the 300 ms debounce`).\n\nThe test file has `v1 backward compatibility` and `v2 backward compatibility` blocks that verify the *decoder* path, but neither asserts that a v2/v1 blob, after being decoded and applied, re-encodes as a *v3* blob with byte 0 = 3. Behavioural drift here is silent: a future schema v4 that re-uses bit 0 differently would need this test to catch the upgrade rewrite producing the wrong shape.\n\n**Location**\nsrc/client/url-state.ts:1138-1159 (applyFromUrl auto-upgrade); src/client/url-state.test.ts has no test of this code path.\n\n**Suggested approach**\nAdd a test that (a) builds a v2 blob via the existing buildV2Blob helper, (b) round-trips it through `decodeBlob` to a view, (c) re-encodes the view via `encodeBlob`, and (d) asserts the result starts with version byte 3 (decode the new blob, assert version === 3) and that the view decodes back to the same DecodedView shape. This validates the upgrade-rewrite contract directly without needing applyFromUrl's location/setTimeout side effects.\n\n**Why**\nThe test plan calls this out as a manual smoke; it is a 5-line vitest. Manual smokes regress between releases more readily than automated ones.\n\n**Smoke step**\nThe test itself is the smoke. `npm test` should run an additional check.\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:11Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:32Z","started_at":"2026-05-10T17:50:41Z","closed_at":"2026-05-10T18:17:32Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.112","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:11Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.111","title":"disc-mask.ts reads stellata.getFilter() per frame instead of caching highlightCon via onFilterChange","description":"**Confidence: High**\n\n**Finding**\nThe new tick body reads `stellata.getFilter().highlightCon` every animation frame. The sibling overlay that consumes the same field — `constellation-overlay.ts` — caches it via `onFilterChange` (`current = f.highlightCon` inside an `update` registered with `stellata.onFilterChange(update)`) and reads only the cached number in its tick. Disc-mask should follow the same pattern.\n\n**Location**\nsrc/client/disc-mask.ts:101-109 (the `const conIdx = stellata.getFilter().highlightCon;` block inside `stellata.onFrame`)\nCompare: src/client/constellation-overlay.ts:19-29 (`update`/`tick` split with cached `current`)\n\n**Suggested approach**\nLift a module-scope `let highlightCon = -1` and register `stellata.onFilterChange((f) =\u003e { highlightCon = f.highlightCon })` alongside the initial DOM setup. Read `highlightCon` directly in the tick. Same shape as constellation-overlay.ts. The `getFilter()` accessor likely returns the live FilterState object so the cost is structural-property-access only, but the architectural fit win is the bigger lever — both overlays now react to filter mutations through one mechanism instead of one polling and one event-driven.\n\n**Why**\nDRY + architectural fit. Two SVG overlays consuming the same field should consume it the same way; mixed patterns make it harder for the next reader to know whether a filter mutation will reach the overlay synchronously or via callback. Caching also makes the tick's null-equivalent fast-path (no constellation highlighted, no focus) a couple of branches with no method dispatch.\n\n**Smoke step**\nAfter the refactor: highlight a constellation via the typeahead, confirm cutouts appear; clear it, confirm cutouts disappear within one frame; `npm run typecheck` clean; manual smoke from PR test plan still passes.","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:05Z","created_by":"Alex Marshall","updated_at":"2026-05-11T07:44:35Z","started_at":"2026-05-10T22:06:06Z","closed_at":"2026-05-11T07:44:35Z","close_reason":"Closed","labels":["pr-52"],"dependencies":[{"issue_id":"stellata-9mm.111","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:04Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.110","title":"SCIENCE.md/solar-system.md: Voyager wording is factually wrong","description":"**Confidence: High**\n\n**Finding**\nThe newly-authored Solar-system science section labels Voyager 1 2012-08-25 as a \"termination-shock crossing\" — that date is the *heliopause* crossing; the termination shock was crossed 2004-12-16. Similarly Voyager 2 2018-11-05 is described as a \"flank\" crossing, but V2 also crossed the heliopause along the spacecraft trajectory (the 115 AU figure is its heliocentric distance at that crossing, not a measured \"flank\" radius). The doc reads as if these two probes measured two different boundary surfaces; in reality both crossed the same surface at different points.\n\n**Location**\n- SCIENCE.md (PR #38 add) lines under \"Heliopause boundary\": \"Upwind boundary at 122 AU — Voyager 1 termination-shock crossing, 2012-08-25\" and \"Flank boundaries at 115 AU — Voyager 2 crossing, 2018-11-05\"\n- docs/solar-system.md lines 148-149 carry the same two bullets\n\n**Suggested approach**\nReword to: \"Upwind boundary at 122 AU — Voyager 1 heliopause crossing, 2012-08-25\" and \"Flank inferred at ~115 AU from Voyager 2 heliopause crossing 2018-11-05, combined with the apex-aligned ellipsoid model\". Or, if the \"flank vs upwind\" distinction is load-bearing for the 115 AU number, cite the asymmetric-bubble model that derives it (e.g. Opher et al.) explicitly. Both occurrences must move together because docs/solar-system.md mirrors SCIENCE.md.\n\n**Why**\nStellata is positioned in this very PR around the empirical-model principle (\"every object comes from a published observational catalogue or direct in-situ measurement\"). A factual mislabelling of the headline observation in the heliopause section directly undermines that framing. SCIENCE.md is the citation source the README and Credits modal point readers at.\n\n**Smoke step**\nCross-check Voyager 1 / Voyager 2 boundary-crossing dates against NASA mission summaries; confirm the 115 AU figure source. Build does not need re-running — text-only edit. `npm test` should remain 427/427.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:00Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:48Z","started_at":"2026-05-11T07:53:50Z","closed_at":"2026-05-11T08:46:48Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-38"],"dependencies":[{"issue_id":"stellata-9mm.110","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:09:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.108","title":"focus-ring-overlay.ts skipped — sixth SVG overlay still writes cx/cy/r every frame","description":"**Confidence: High**\n\n**Finding**\nPR #55's release notes claim coverage of \"all five remaining overlay modules\" but `focus-ring-overlay.ts` is a sixth per-frame SVG overlay (listed in CLAUDE.md and docs/overlays.md as a peer of the others) that is NOT touched. It unconditionally writes `cx`, `cy`, `r` and toggles `style.display` every animate tick, with the same `.toFixed(1)` precision floor and the same stationary-camera idle profile that motivated the rest of the pass. The exact pattern the PR mirrors from chart-labels.ts is missing on the one overlay that should have been mechanical to convert.\n\n**Location**\nsrc/client/focus-ring-overlay.ts:87-90 (per-frame setAttribute writes), :11-12 / :32 / :43 / :53 / :64 / :79 (display toggles via show/hide).\n\n**Suggested approach**\nAdd `lastCx / lastCy / lastR` (sentinel `-Infinity`) and a `lastDisplay` string to the closure, gate `ring.setAttribute('cx'|'cy'|'r', ...)` on `Math.abs(value - last*) \u003e= ATTR_DIRTY_PX`, and gate the `show()`/`hide()` writes on the cached display string the same way `hud-overlay.ts` does for the OBSERVE ring. One commit, mechanical mirror of the disc-mask Slot pattern.\n\n**Why**\nThis is the most-instantiated focus-ring overlay (visible whenever a star is focused in NAVIGATE — the primary mode) and it bypasses the very pattern the PR is selling. Leaves the per-frame DOM cost reduction inconsistent across the overlay layer that the PR title (\"perf(overlays): dirty-track + scratch-vector pass\") claims to cover.\n\n**Smoke step**\nFocus a star (Polaris) in NAVIGATE, sit idle, watch the focus-ring `cx/cy/r` setAttribute calls in DevTools' SVG element inspector — should not change while the camera is steady. Then orbit the camera; values should update smoothly.\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:09:51Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:27Z","started_at":"2026-05-16T10:02:19Z","closed_at":"2026-05-16T21:38:27Z","close_reason":"merged via PR #55 (dirty-track pass + sentinel hygiene under 9mm.188)","labels":["pr-55"],"dependencies":[{"issue_id":"stellata-9mm.108","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:29Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.102","title":"PlanetBodyField cull-distance optimisation has no test coverage","description":"**Confidence: High**\n\n**Finding**\nThe architectural payoff of the global PlanetBodyField is the per-host `cullDistancePc` — bk5 will scale to \"potentially hundreds of attached hosts\" and the per-frame `if (dToHost \u003e host.cullDistance) continue;` gate at `update():L320` is what makes that tractable. `cullDistancePc` itself has 5 unit tests; the *gate* it drives in `update()` has zero. There is no test that verifies \"a host past its cull distance does NOT have its positions buffer rewritten this frame\" — the entire optimisation could silently regress (e.g. someone changes `\u003e` to `\u003e=`, or the comparison runs against `host.hostAbsPos` instead of `host.hostLocalPos`) and 479 tests still pass.\n\n**Location**\n`src/client/planet-body-field.ts:311-327` (`update()` method); `src/client/planet-body-field.test.ts` (where the test belongs).\n\n**Suggested approach**\nAdd a test that attaches a host with `positionsAt` (e.g. a stub that increments a counter on each call) and a known small cullDistance, places the camera inside cullDistance + asserts the counter increments per `update()` call, then moves the camera past cullDistance + asserts the counter stays frozen. Two assertions, one bug class permanently nailed.\n\n**Why**\nThe integration point is exactly what the user flagged: \"unit-test coverage probably has gaps.\" The cull is the architectural promise; not testing it leaves the bk5 scaling claim un-pinned. This is the single most leverage-bearing test gap in the PR.\n\n**Smoke step**\n`npm test` passes the new test; mutation-test by flipping the comparison to `\u003c` in `update()` and confirming the test catches it.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:47Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:48Z","started_at":"2026-05-11T08:02:09Z","closed_at":"2026-05-11T08:46:48Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-45"],"dependencies":[{"issue_id":"stellata-9mm.102","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.98","title":"No unit tests for the new Typeahead\u003cT\u003e class (claim/release, blur-restore, X-clear sequence)","description":"**Confidence: High**\n\n**Finding**\nPR #43 introduced `Typeahead\u003cT\u003e` as the consolidated DOM-glue abstraction (focus/blur lifecycle, deferred hide, arrow-nav, mousedown picking, displayName reverse-sync, X-clear pre-clearing input.value, TypeaheadGroup ownership across shared resultsEl). The class has zero direct unit-test coverage. The existing suite covers only `applyHoverClass` (typeahead-util.test.ts, ex-9mm.6) and pure helpers `filterConstellations` / `splitBayer` — none of the actual glue the PR consolidates.\n\nThe PR description's 'Test plan' is entirely manual smoke. The class encodes several non-obvious invariants: (a) blur-deferred restore writes `displayName` back even when group ownership has flipped to a sibling typeahead, (b) X-clear must clear `input.value` BEFORE `onClear?.()` so the synthetic `input.focus()` doesn't re-render against an orphan query (comment at typeahead.ts:84-95 cites a real bug this guards), (c) `setName(name)` preserves user typing iff `group.active === this`, (d) ESC bails without preventDefault so global ESC handlers still see the key, (e) cross-input group claim/release prevents blur from hiding the dropdown when focus moves between focus+to inputs.\n\nNone of these have a regression net.\n\n**Location**\nsrc/client/typeahead.ts (entire file, no corresponding test file)\n\n**Suggested approach**\nAdd `src/client/typeahead.test.ts` using jsdom (vitest `environment: 'jsdom'` per-file via `// @vitest-environment jsdom` comment, or add jsdom dep). Cover at minimum:\n- setName updates input.value when not focused; preserves input.value when focused (group.active === this).\n- setName updates clearBtn.hidden iff clearBtn provided; no-op otherwise.\n- TypeaheadGroup claim on focus, release on blur; sibling typeahead in same group prevents blur-hide.\n- X-clear sequence: clears input.value, calls onClear, refocuses, render shows empty results (depends on runQuery).\n- ESC key: hides results, blurs input, does NOT call preventDefault.\n- pick() invokes onSelect, hides resultsEl, blurs input.\n- handleKey ArrowDown/Up wraparound, Enter on hoverIdx\u003c0 no-op.\n\nThe harness pattern is similar to how observe-controls.test.ts (closed 9mm.12) tests DOM event lifecycles — drives synthetic events on a stub input + ul.\n\n**Why**\nPR #43 is a straight refactor consolidating three parallel implementations. The unit suite is the project's documented regression-prevention layer (see CLAUDE.md). Without class-level tests, any future tweak to the focus/blur/group/X-clear interaction risks silently regressing one of the call sites the PR's manual smoke happened to cover. The closed bead 9mm.6 added tests for the `applyHoverClass` / `escapeHtml` helpers but explicitly did not extend to the binders' state machinery — that gap moves wholesale into typeahead.ts under PR #43.\n\n**Smoke step**\n`npm test` shows new typeahead.test.ts file with at least 6 cases covering the bullets above; existing tests still pass.","notes":"Reverting to open: claimed 2026-05-11 but no work landed — src/client/typeahead.test.ts does not exist in main. Stale in_progress state from an abandoned session.","status":"open","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:38Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:39:14Z","started_at":"2026-05-11T08:02:09Z","labels":["pr-43"],"dependencies":[{"issue_id":"stellata-9mm.98","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-15T12:54:02Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.96","title":"No CPU mirror of the planet apparent-mag formula — shader is the only test","description":"**Confidence: High**\n\n**Finding**\n`perceptual-magnitude.ts` ports the disc-radius math (`perceptualDmEff`, `perceptualAppSizePx`, `apparentMagnitude`) for vitest coverage, but the integration that actually defines a planet — `m_planet = m_host + 5·log10(d_vh/10) − 2.5·log10(p · (R/d_vp)² · (d_vh/d_hp)² · φ(α))` — lives only in `planet.vert.glsl:127-142`. The PR description acknowledges this gap explicitly (\"Medium\" honesty bucket): the formula was hand-verified against three reference points, no GLSL test harness. There is also no CPU helper a future debug-panel readout / search-rank consumer could call.\n\n**Location**\n`src/client/shaders/planet.vert.glsl:127-142` (sole production location); `src/client/perceptual-magnitude.ts` (where the mirror should land).\n\n**Suggested approach**\nAdd `planetApparentMagnitude(hostAbsmag, d_vh, d_vp, d_hp, albedo, radiusPc, phaseFactor)` to `perceptual-magnitude.ts` returning the same value the shader computes. Pin tests against the three reference points the bead description already uses (Jupiter from Earth opposition −2.7, Jupiter outside heliopause +5.2, Jupiter from α Cen +21). Bonus: pin against Lambert vs Mallama to lock the φ(α) branch behaviour.\n\n**Why**\nThis formula is the load-bearing physics contract of stellata-3re.16. Drift (e.g. a future refactor swapping the squared term order, or changing the d_vh denominator) currently has no automated guard — the only way to notice is to run the smoke checklist and eyeball Jupiter brightness ratios. A 5-line CPU port + 3 reference-point tests closes that.\n\n**Smoke step**\n`npm test` includes the new helper exercises; cross-check by tweaking one term in the mirror, observing the test fail, then verifying the shader still has the original term.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:35Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:46Z","started_at":"2026-05-11T08:02:08Z","closed_at":"2026-05-11T08:46:46Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-45"],"dependencies":[{"issue_id":"stellata-9mm.96","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.87","title":"PlanetMaterialUniforms duplicates 17 star-pipeline uniforms by hand","description":"**Confidence: High**\n\n**Finding**\n`PlanetMaterialUniforms` in `planet-body-field.ts` re-declares 17 uniform fields (uMaxAppMag through uFovYRad) — every single one a pointer to the same `{ value }` object the star pipeline already created in `stellata.ts` (the `sharedUniforms` literal at L751). The interface and the pickoff in `buildMaterials` (L437-455) duplicate that list. If a new star-pipeline uniform is added (or removed) the planet field silently desynchronises until somebody hand-syncs both call sites.\n\n**Location**\n`src/client/planet-body-field.ts:58-75` (interface) and `:438-455` (sharedPlanetUniforms pickoff). Source of truth at `src/client/stellata.ts:751-842`.\n\n**Suggested approach**\nExtract a `PerceptualDiscUniforms` type (e.g. in `perceptual-magnitude.ts` next to the math chunk it shadows) listing only the fields the planet pipeline actually consumes, and have stellata.ts construct `sharedUniforms` to include that subtype. Then planet-body-field can `Pick\u003c\u003e` from a single canonical definition. Or simpler: `keyof typeof sharedUniforms` extraction at the call site — drop the bespoke interface entirely. Either way the manual list-of-17 disappears.\n\n**Why**\nThe whole architectural point of the perceptual-disc chunk is that stars and planets share one source of truth for the math + uniforms. Duplicating the uniform shape contradicts that and is the most likely place a future tuning change drifts (e.g. someone adds uVisibleSomethingNew to the chunk + star side and forgets the planet side).\n\n**Smoke step**\nAfter refactor: `npm run typecheck \u0026\u0026 npm test` clean; manually verify in the browser that adjusting any star-disc tuning slider (visibleThreshold, sizeKnee, etc.) still propagates to planet bodies live.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:13Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:12Z","labels":["pr-45"],"dependencies":[{"issue_id":"stellata-9mm.87","depends_on_id":"stellata-9mm.189","type":"parent-child","created_at":"2026-05-15T12:53:41Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.78","title":"Row colour dirty-check compares hex string against serialized style.color (always mismatches)","description":"**Confidence: High**\n\n**Finding**\nThe per-row dirty-track for colour is `if (slot.line.style.color !== colour) slot.line.style.color = colour;` where `colour` is a 3-digit hex (`'#cfe' / '#fc8' / '#f88'` from `colourForAvg`). Per CSSOM, the inline `style.color` getter returns the **serialized** form — modern browsers serialize `#cfe` to `rgb(204, 238, 255)`. So `slot.line.style.color` after the first write is `rgb(204, 238, 255)`, which never equals `'#cfe'`. The comparison **always fails**, so the colour is rewritten every tick — defeating the dirty-track. Same pattern is correctly avoided for histograms (which cache the input hex in `histoLastColour[]` and compare against that, not against `style.background`).\n\n**Location**\nsrc/client/perf-hud.ts:308-309 (`const colour = colourForAvg(r.avg); if (slot.line.style.color !== colour) slot.line.style.color = colour;`)\n\n**Suggested approach**\nMirror the histogram pattern: maintain a parallel `rowLastColour: string[]` cache (or extend each row pool slot with a `lastColour` field), and compare against the cached hex rather than the serialized DOM value. Same fix shape as `histoLastColour`. Cost: one extra string per row × 8 rows.\n\n**Why**\nCorrectness — the whole point of the row's dirty-tracking is to skip identical-value writes for stable workloads (the bead's stated motivation). Without the fix, the row colour writes 8 times per tick at 5 Hz forever; small absolute cost but the abstraction lies. Also: the same hex-vs-rgb gotcha probably needs a comment so future dirty-tracks don't repeat it.\n\n**Smoke step**\n`npm run typecheck \u0026\u0026 npm test`. Add a vitest that builds the section, calls render twice with stable input, and asserts the second tick doesn't reassign `style.color` (stub via a Proxy / spy on the setter, or assert the cached colour matches one of the three known hex values).","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:04:45Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:13Z","labels":["pr-41"],"dependencies":[{"issue_id":"stellata-9mm.78","depends_on_id":"stellata-9mm.190","type":"parent-child","created_at":"2026-05-15T12:53:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dbl.2","title":"Calibration: debug-panel calibration section + cross-layer knob spotlight","description":"**Goal:** debug surface that supports the calibration procedure from sibling **stellata-dbl.1** by spotlighting the cross-layer-affecting knobs in one place. Spec context in parent **stellata-dbl**.\n\n### Scope (decide during design — extend existing per-layer panels OR add a unified calibration panel)\n- Spotlight the knobs that affect calibration across stars / MW / planets:\n  - magnitude cutoff\n  - MW brightness multiplier\n  - planet-disc floor\n  - dynamic-range exponent\n  - any preset-driving constants\n- Live readout of the current preset's numeric targets next to the sliders.\n- Optional side-by-side compare HUD: scene render vs reference image cropped to the same FOV.\n\n### Files\n- `src/client/star-tuning.ts`, `src/client/milkyway-tuning.ts`, `src/client/debug-panel.ts` — existing tuning surfaces.\n- New section module if going the unified-panel route.\n\n### Acceptance\n- The calibration procedure in `docs/calibration.md` can be executed without leaving the debug panel.\n- Each knob's current value + the active preset's target are readable side-by-side.\n- No regression on existing tuning sections.\n\n### Depends on\n**stellata-dbl.1** (procedure tells us which knobs need spotlighting).","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:10:14Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:10:14Z","dependencies":[{"issue_id":"stellata-dbl.2","depends_on_id":"stellata-dbl","type":"parent-child","created_at":"2026-05-10T09:10:14Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dbl.2","depends_on_id":"stellata-dbl.1","type":"blocks","created_at":"2026-05-10T09:10:52Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dbl.1","title":"Calibration: write docs/calibration.md procedure","description":"**Goal:** a documented, repeatable calibration procedure that ensures stars, Milky Way background, and planets are correctly sized + brightness-matched at each magnitude preset. Spec context in parent **stellata-dbl**.\n\n### Visual ground-truth benchmark\nESO eso0932a panorama: https://cdn.eso.org/images/screen/eso0932a.jpg\n\nUse this as \"what does the real sky look like at perceptually-good calibration?\" Compare Stellata's render at each magnitude preset against an image-stretching of this panorama matched to the same magnitude cutoff.\n\n### Deliverable: `docs/calibration.md`\nFor each magnitude preset (mag 6 / 5 / 4 / 3 etc.):\n- Scene to set up: camera position, FOV, observe vs navigate, mode selection.\n- Target star(s) and MW region to reference.\n- Ground-truth comparison image / known-star pair to match against.\n- Knobs to adjust and the order — likely stars first, then MW, then planets (or whichever ordering settles).\n- Expected visual outcome — quantified where possible (e.g. \"star X should be brighter than MW patch Y by N×\").\n\n### Acceptance\n- `docs/calibration.md` committed.\n- A reviewer can follow the doc end-to-end on any of the magnitude presets and produce the documented expected outcome.\n- Coordinated with `stellata-f9b` / `stellata-v3j` (preset-driven UX): the calibration procedure feeds back into what each preset's defaults should be.\n\n### Out of scope\n- Building per-preset auto-tuned coefficients.\n- Debug-panel updates — sibling **stellata-dbl.2**.\n\n### Coupling\n- **stellata-3re.16** (apparent-magnitude planet visibility) — planet calibration steps depend on this landing or co-evolves.\n- **stellata-zsr** (perceptual polish) — specific spectral-correction / dynamic-range work feeds into individual calibration steps.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:10:13Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:10:13Z","dependencies":[{"issue_id":"stellata-dbl.1","depends_on_id":"stellata-dbl","type":"parent-child","created_at":"2026-05-10T09:10:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-nxb.1","title":"Search index: V-number variable-star designations (audit + ingest)","description":"**Goal:** make GCVS V-numbers searchable so a user typing \"V645\" finds Proxima Centauri (V645 Centauri). Highest-priority designation class per parent **stellata-nxb** — GCVS data already flows through the build pipeline.\n\n### Scope\n1. **Audit** what alternate-name fields exist in AT-HYG v3.3 (`data/athyg_33_classic_ids.csv`) and what currently lands in `search-index.json` from `scripts/build-catalog.ts`. Document which classical designations are present today.\n2. **Greek-letter abbreviations** (Alf Cen C alongside Alpha Centauri C) — flag whether these are partly handled already or part of this scope.\n3. **Ingest V-numbers** from GCVS via the existing GCVS cross-match path. Add `V*` keys to search index entries for matched stars.\n4. **Verify** `src/client/search.ts` dual-input typeahead handles new keys (likely no change needed; just more entries).\n\n### Acceptance\n- `bd memories pickStar`-style verification: search for \"V645\" returns Proxima Centauri at top.\n- A handful of representative V-numbers (V1500 Cyg, V1280 Sco, V404 Cyg) resolve to their canonical stars.\n- `npm test` covers the V-number search-index entries.\n- No regression on existing typeahead behaviour.\n\n### Files\n- `scripts/build-catalog.ts` / `scripts/catalog-pure.ts` — search-index entry generation; add V-number keys.\n- Tests in `scripts/catalog-pure.test.ts`.\n\n### Out of scope\nGJ / Gliese, CCDM, esoteric proper-motion catalogues — sibling **stellata-nxb.2**.","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:09:20Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:09:20Z","dependencies":[{"issue_id":"stellata-nxb.1","depends_on_id":"stellata-nxb","type":"parent-child","created_at":"2026-05-10T09:09:19Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-ju3.2","title":"Galactic arm dust: bake arm contributions into voxel grid (build-dust.py)","description":"**Goal:** apply the data-source + scope decisions from sibling **stellata-ju3.1** to extend the dust voxel volume with arm-overdensity contributions. Spec context in parent **stellata-ju3**.\n\n### Approach\n- Modify the existing chunked 3D-texture pipeline rather than adding a parallel pass — same principle that **stellata-c7u.2** uses for Zucker MCs. Single source of truth for absorption in `star.vert.glsl`.\n- If grid extents need to grow (per ju3.1 decision), bump them in `manifest.json`; the chunking already supports larger grids.\n\n### Files\n- `scripts/build-dust.py` — overlay arm contributions into the voxel volume.\n- `data/dust/manifest.json` — bump version; record arm-source provenance.\n- `data/\u003carm-source-files\u003e` — committed per `frozen-external-data`.\n\n### Acceptance\n- Distant sightlines toward galactic-disc-plane targets show physically-plausible reddening/dimming relative to off-plane targets.\n- Comparable to the ESO Milky Way panorama benchmark (eso0932a) used by **stellata-dbl** calibration.\n- No regression in near-Sol fidelity (Edenhofer high-res unchanged inside its cube).\n\n### Out of scope\n- Per-arm visualisation overlays (UI showing where arms are) — file under **stellata-io3** if wanted.\n- Time-dependent or stochastic dust structure.\n\n### Depends on\n**stellata-ju3.1** (data source + scope must be settled).\n### Related\n**stellata-c7u.2** (same pipeline, Zucker MC overlay), **stellata-74e** (analytical-slab fallback at boundary), **stellata-cns** (more sightlines through arms once the bigger catalog lands).","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:07:37Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:07:37Z","dependencies":[{"issue_id":"stellata-ju3.2","depends_on_id":"stellata-ju3","type":"parent-child","created_at":"2026-05-10T09:07:37Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-ju3.2","depends_on_id":"stellata-ju3.1","type":"blocks","created_at":"2026-05-10T09:10:49Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ju3.1","title":"Galactic arm dust: scoping + sightline-coverage analysis (research)","description":"**Goal:** produce the scoping note that drives implementation in sibling **stellata-ju3.2**. Spec context in parent **stellata-ju3**.\n\n### Decisions to settle\n- **Whole-galaxy vs near-Sol scope.** The likely answer is near-Sol for v1 (rendered-object sightlines mostly stay within ~1–2 kpc of Sol). Confirm with the sightline-coverage analysis below.\n- **Data source choice.** Evaluate at minimum:\n  - **Drimmel \u0026 Spergel (2001)** — analytical 3D dust with explicit arm components, COBE-DIRBE based.\n  - **Marshall et al. (2006)** — 3D dust extinction from 2MASS, galactic plane to ~10 kpc, lower spatial resolution.\n  - **Green et al. (2019) Bayestar19** — 3D extinction from Pan-STARRS + 2MASS, ~3 kpc.\n  - **Lallement et al. (2022)** — high-res 3D dust beyond Edenhofer at ~3 kpc.\n- **Voxel grid extent.** Whether Edenhofer's existing 1.25 kpc cube needs to grow; the chunking + manifest already supports a larger grid.\n- **Frozen-data convention** applies — all candidates committed under `data/`, no live fetch at build.\n\n### Sightline-coverage analysis\n- For currently rendered objects (catalog stars + Zucker clouds + nebulae once 9hp lands), what fraction of sightlines passes through arm-overdense regions today missed by both Edenhofer (inside cube only) and the analytical slab (smooth axisymmetric)?\n- Quantify the visual gap so the scope decision (near-Sol vs whole-galaxy) is data-backed.\n\n### Deliverable\nShort scoping note attached to the issue (or written into `docs/build-and-data.md`) covering:\n1. Data source choice + retrieval steps.\n2. Scope decision (near-Sol vs whole-galaxy) with sightline-coverage rationale.\n3. Whether the voxel grid extents need to grow.\n\nNo code in this phase.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:07:36Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:07:36Z","dependencies":[{"issue_id":"stellata-ju3.1","depends_on_id":"stellata-ju3","type":"parent-child","created_at":"2026-05-10T09:07:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-kip.2","title":"Hide-chrome toggle (body[data-chrome-hidden] + show-UI affordance)","description":"**Goal:** independent toggle that hides UI chrome (panel, top bar, overlays except canvas + maybe scale bar) for clean screenshots / presentation. Works regardless of fullscreen state. Spec context in parent **stellata-kip**.\n\n### Scope\n- Single `body[data-chrome-hidden]` selector hook is the cleanest pattern; verify existing layout containers cooperate.\n- Reversible by same shortcut/control.\n- Small unobtrusive \"show UI\" affordance (corner button or bottom hint) so users aren't trapped after hiding.\n- Independent of fullscreen — composable in either order.\n\n### Files\n- `src/client/keyboard-shortcuts.ts` — shortcut.\n- `src/client/help-modal.ts` — list the shortcut.\n- `src/client/index.html`, `src/client/styles.css` — chrome targets (panel container, brand box, top-bar, overlays); add the \"show UI\" affordance.\n- `src/client/url-state.ts` — natural home if state persists (single bit). Skip persistence if first-load with no UI is confusing.\n- `src/client/brand-modal.ts` (or `controls.ts`) — visible toggle button.\n\n### Acceptance\n- Hide chrome via shortcut — UI disappears; canvas + scale bar remain.\n- Restore chrome via same shortcut and via the visible \"show UI\" affordance.\n- Help modal lists the shortcut.\n- `npm run typecheck` and `npm test` pass.\n\n### Out of scope\nBrowser fullscreen (sibling **stellata-kip.1**); kiosk auto-hide-on-idle; mobile-specific chrome rules.","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:06:25Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:06:25Z","dependencies":[{"issue_id":"stellata-kip.2","depends_on_id":"stellata-kip","type":"parent-child","created_at":"2026-05-10T09:06:25Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-kip.1","title":"Browser fullscreen toggle (Fullscreen API + Esc progression)","description":"**Goal:** Fullscreen API on the canvas container so panel + overlays stay visible inside fullscreen by default. Spec context in parent **stellata-kip**.\n\n### Scope\n- Wire `requestFullscreen()` / `exitFullscreen()` to a button (brand box or settings panel — decide during implementation) and a keyboard shortcut.\n- Esc progression: pressing Esc exits fullscreen first (if active), then falls through to the existing observe→navigate fallback in `keyboard-shortcuts.ts:184`.\n- On exit, layout returns exactly as it was — no layout drift.\n\n### Files\n- `src/client/keyboard-shortcuts.ts` — new shortcut + Esc precedence.\n- `src/client/help-modal.ts` — list the shortcut.\n- `src/client/brand-modal.ts` (or `controls.ts`) — visible toggle button.\n\n### Acceptance\n- Toggle works in Chrome, Safari, Firefox.\n- On exit, layout matches pre-toggle.\n- Help modal lists the shortcut.\n- `npm run typecheck` and `npm test` pass.\n\n### Out of scope\nHide-chrome (sibling **stellata-kip.2**); kiosk / auto-hide-on-idle; mobile-specific chrome rules.","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:06:24Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:06:24Z","dependencies":[{"issue_id":"stellata-kip.1","depends_on_id":"stellata-kip","type":"parent-child","created_at":"2026-05-10T09:06:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ywn.2","title":"Probes: runtime loader + rendering layer","description":"**Goal:** render the 5 probes in the scene at their position for the current `t`. Spec in parent **stellata-ywn**.\n\n### Scope\n- New `src/client/probe-loader.ts`:\n  - Fetch all 5 JSONs in parallel (small enough — fire-and-forget at startup or lazy on first solar-system-layer activation).\n  - For a given `t`, find bracketing entries and **linearly interpolate** x/y/z. Probes move ~3–4 AU/year — 30-day samples are visually indistinguishable from the true trajectory.\n  - Hide probe entirely if `t \u003c launchUnix`.\n  - Visual flag for \"extrapolated past last-contact\": Pioneer 10 after 2003-01-23, Pioneer 11 after 1995-09-30. Render dimmer, append \" (signal lost)\" to label.\n- Render layer (likely extends solar-system module or new `probe-system.ts`):\n  - Same instanced-quad pattern as planets, sized small.\n  - Always-labelled when visible (probes are a discovery feature).\n  - Optional: thin fading trail showing trajectory between launch and current `t` (no future projection).\n- Visibility gate: solar-system layer active (Sol-focused or unfocused at Sol).\n\n### Acceptance\n- All 5 probes render at plausible AU positions when Sol focused.\n- Pioneer 10/11 dim and pick up \"(signal lost)\" label suffix when `t \u003e lastContactUnix`.\n- Hidden when `t \u003c launchUnix` per probe.\n- No probe at `t = 0` (1970-01-01) extrapolating backward from first trajectory entry.\n\n### Comments to include in code\n- Linear interpolation between monthly samples is fine for visualisation but is NOT a precision ephemeris. Prevent future-self from mistaking it for one.\n- HORIZONS forward-projections past `now` are HORIZONS' projections, not certainties.\n\n### Files\n- New: `src/client/probe-loader.ts`, render module.\n- Touch: `src/client/stellata.ts` (visibility gate + register layer), `package.json`, `.gitignore`.\n- Post-landing: `CLAUDE.md` solar-system section gains probe data pipeline + last-contact handling.\n\n### Depends on\n**stellata-ywn.1** (data files must exist).","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:06:01Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:06:01Z","labels":["phase-12b","solar-system"],"dependencies":[{"issue_id":"stellata-ywn.2","depends_on_id":"stellata-ywn","type":"parent-child","created_at":"2026-05-10T09:06:00Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-ywn.2","depends_on_id":"stellata-ywn.1","type":"blocks","created_at":"2026-05-10T09:10:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ywn.1","title":"Probes: HORIZONS fetch + sync to public/","description":"**Goal:** static probe trajectory data committed under `data/probes/` and mirrored to `public/probes/` at build time. Spec in parent **stellata-ywn**.\n\n### Scope\n- New `scripts/fetch-probe-trajectories.ts` — manual `npm run fetch:probes`, **not** wired into default build chain (matches GCVS convention; `frozen-external-data` rule applies).\n- Per probe (Pioneer 10 / -23, Pioneer 11 / -24, Voyager 1 / -31, Voyager 2 / -32, New Horizons / -98) query HORIZONS:\n  ```\n  https://ssd.jpl.nasa.gov/api/horizons.api?format=json\n    \u0026COMMAND=\u003cid\u003e\u0026CENTER=500@10\n    \u0026MAKE_EPHEM=YES\u0026EPHEM_TYPE=VECTORS\n    \u0026VEC_TABLE=2\u0026REF_PLANE=FRAME\n    \u0026START_TIME=\u003claunch\u003e\u0026STOP_TIME=2050-01-01\u0026STEP_SIZE=30d\n  ```\n  `REF_PLANE=FRAME` returns ICRS equatorial directly — same frame as ATHYG. No ecliptic rotation.\n- Parse `\\$\\$SOE…\\$\\$EOE` block, extract `JDTDB` and `X, Y, Z` (AU). Defensive on schema drift; clear error if `\\$\\$SOE` block missing or columns unexpected.\n- Output `data/probes/{id}.json` per parent's schema (id, label, launchDate, launchUnix, lastContactUnix, trajectory[{jd,x,y,z}]).\n  - `lastContactUnix`: `2003-01-23` for Pioneer 10, `1995-09-30` for Pioneer 11, null for active probes.\n- ~225 KB total across 5 probes. Plain-text commit, no LFS.\n- New `scripts/sync-probes.ts` mirrors `data/probes/` → `public/probes/` (matches `sync-dust.ts` pattern). Add to dev/build chains.\n- Add `/public/probes/` to `.gitignore`.\n\n### Acceptance\n- `npm run fetch:probes` populates `data/probes/{voyager1,voyager2,pioneer10,pioneer11,new-horizons}.json` with valid trajectory arrays.\n- `npm run dev` / `npm run build` propagates them to `public/probes/`.\n- Schema spot-check: each JSON has `id`, `label`, `launchUnix`, `trajectory[]` with monotonic `jd` and finite `x,y,z`.\n\n### Out of scope\nRuntime probe rendering; that's the sibling **stellata-ywn.2**.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:06:00Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:06:00Z","labels":["phase-12b","solar-system"],"dependencies":[{"issue_id":"stellata-ywn.1","depends_on_id":"stellata-ywn","type":"parent-child","created_at":"2026-05-10T09:05:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dbl","title":"Calibration workflow: stars vs Milky Way vs planets across magnitude presets","description":"**Goal:** repeatable, documented calibration procedure that ensures stars, Milky Way background, and planets are correctly sized + brightness-matched relative to one another at each magnitude preset.\n\n# Concern motivating the work\n\nIt's not clear that the volumetric Milky Way background's apparent brightness is correctly calibrated against the star field as the magnitude slider moves. A user dimming the magnitude cutoff to mag ~4 (suburban-sky preset) should see the MW band at a correspondingly dimmer integrated brightness — not just fewer stars on the same MW. Same concern for planets at host-focus when the slider moves.\n\n# Visual ground-truth benchmark\n\nESO eso0932a panorama: https://cdn.eso.org/images/screen/eso0932a.jpg\n\nUse this as the benchmark image for \"what does the real sky look like at perceptually-good calibration?\" Compare Stellata's render at each magnitude preset against an image-stretching of this panorama matched to the same magnitude cutoff.\n\n# Deliverables\n\n## 1. Documented calibration procedure (step-by-step)\n\nA playbook (probably `docs/calibration.md`). For each magnitude preset (mag 6 / 5 / 4 / 3 etc):\n- What scene to set up (camera position, FOV, observe vs navigate, mode selection).\n- What target star(s) and MW region to reference.\n- What ground-truth comparison image / known-star pair to match against.\n- What knobs to adjust (in which order: stars first, then MW, then planets — or whichever ordering the doc settles on).\n- Expected visual outcome — quantified where possible (e.g. star X should be brighter than MW patch Y by N×).\n\n## 2. Debug-panel updates (likely)\n\nThe existing tuning surface lives in `src/client/star-tuning.ts`, `src/client/milkyway-tuning.ts`, `src/client/debug-panel.ts`. Calibration may need:\n- A new \"calibration\" debug section that spotlights the cross-layer-affecting knobs (mag-cutoff, MW brightness multiplier, planet-disc floor, dynamic-range exponent) in one place rather than buried per-layer.\n- Live readout of the current preset's numeric targets next to the sliders.\n- A side-by-side compare HUD: scene render vs a reference image cropped to the same FOV.\n\nDecide during design whether to extend the existing per-layer panels or add a unified calibration panel.\n\n## 3. Per-layer relative-magnitude formula fixes (if revealed)\n\nIf calibration uncovers that the MW or planet apparent-magnitude formulas are off, file the fixes as children of this bead.\n\n# Coupling\n\n- stellata-f9b / stellata-v3j (magnitude presets as primary UX): the calibration workflow should feed back into what each preset's defaults should be.\n- stellata-3re.16 (apparent-magnitude visibility cutoff for planets): planet calibration depends on this landing first or co-evolves with it.\n- stellata-zsr (perceptual polish epic): calibration is the umbrella process; specific spectral-correction / dynamic-range work under zsr feeds into individual calibration steps.\n\n# Out of scope\n\n- Building per-preset auto-tuned coefficients. The output is a documented manual procedure Alex follows; automation can come later.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-08T18:34:46Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:03:55Z","dependencies":[{"issue_id":"stellata-dbl","depends_on_id":"stellata-3re.16","type":"related","created_at":"2026-05-08T19:36:52Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dbl","depends_on_id":"stellata-f9b","type":"related","created_at":"2026-05-08T19:36:50Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dbl","depends_on_id":"stellata-zsr","type":"related","created_at":"2026-05-08T19:36:55Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-8hu.1","title":"Design: classify renderable layers (Physical-visible vs Representational); define 3-mode UI surface","description":"Design gate for the three-mode framework epic. Until this lands, no implementation children should ship — the layer classification and UI surface need approval first.\n\n# Deliverables\n\n## 1. Layer classification table\n\nFor every renderable layer in Stellata today, classify as one of:\n\n- **Physical-visible** — would be seen with human eyes from the camera's location.\n- **Representational** — informational/inferential, not visible.\n- **Style overlay** — chart-mode-only paper aesthetics. Orthogonal to the 3 modes.\n\nInitial pass (verify in design):\n\n| Layer | Category |\n|---|---|\n| Stars (point glow + disc) | Physical |\n| Planet bodies (reflected light) | Physical |\n| Galactic-MW volumetric band (integrated unresolved star light) | Physical |\n| Effect of dust extinction on starlight | Physical |\n| Heliopause shell | Representational |\n| Planet orbit rings | Representational |\n| Planet labels + apex labels | Representational |\n| Time-readout, scale-bar, distance-vector, focus-ring | Representational |\n| Constellation stick-figures | Representational |\n| Molecular-cloud ellipsoids | Representational |\n| Sol/GC SVG arrows, HUD ring, mode-toggle pill | Representational |\n| Dust voxel debug visualisations | Representational |\n| Chart-mode glyphs / isobars / hard-edged discs | Style overlay |\n\n## 2. Per-layer mode behaviour matrix\n\nFor each layer, specify visibility in (Pure Visual, Standard default, Advanced default-on, Advanced user-controlled). Standard is Alex's curated set; Advanced exposes per-layer toggles.\n\n## 3. Unfocused-host behaviour\n\nFor representational layers tied to a focused host (heliopause shell, orbit rings, planet labels, time-readout): hide when host unfocused. For physical layers (planet bodies): keep rendering, gated only by their own physics (apparent magnitude, etc.). This rule is implemented concretely by the unfocus-split bead.\n\n## 4. UI surface design\n\nWhere the 3-mode selector lives in the panel. How Advanced exposes per-layer toggles. Couples tightly with stellata-f9b / stellata-1yu (settings reorg) — coordinate so the panel doesn't end up half-redesigned.\n\n# Coupling\n\n- stellata-f9b / stellata-1yu / stellata-v3j / stellata-iuh: settings panel rework. The UI surface for this lives there.\n- stellata-zsr (perceptual polish): refinements like spectral correction / BC_photopic / limb darkening belong to Pure Visual / Standard *visual fidelity*, not to Advanced layer toggles. Clarify in the design doc.\n- stellata-3re: solar-system layer hosts the first concrete applications.\n- stellata-m40 (chart mode): orthogonal axis. Chart × any of 3 modes is a valid combination.\n\n# Out of scope here\n\nImplementation. This bead is the design doc — implementation children get filed once classification + UI is approved.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-08T18:08:47Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:08:47Z","dependencies":[{"issue_id":"stellata-8hu.1","depends_on_id":"stellata-8hu","type":"parent-child","created_at":"2026-05-08T19:08:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.16","title":"Apparent-magnitude visibility cutoff for planets (Sol planets + Pluto; generic to any host)","description":"**Goal:** treat planets like stars for visibility. Compute each planet's apparent magnitude as reflected light from its host star, then apply the existing magnitude slider as the cutoff. Drop the unconditional pixel-floor that keeps planets visible at any distance.\n\n# Why\n\nPlanets don't emit light — they reflect their host star's. At parsec distances, even Jupiter (the brightest planet from Earth) drops below naked-eye visibility:\n\n| View | Jupiter apparent mag (≈) |\n|---|---|\n| Earth at opposition | −2.7 |\n| Just outside Sol's heliopause (~150 AU) | +5.2 |\n| Alpha Cen (1.34 pc) | +21 |\n\nStellata already gates star visibility on apparent magnitude via the slider. Planets should obey the same rule.\n\n# Apparent-magnitude formula\n\nFor a planet of geometric albedo `p` and equatorial radius `R`, with the viewer at distance `d_vp` from the planet, `d_vh` from the host, and `d_hp` from the host to the planet:\n\n```\nm_host_at_viewer = M_host + 5·log10(d_vh / 10pc)\nm_planet         = m_host_at_viewer\n                 − 2.5·log10( p · (R/d_vp)² · (d_vh/d_hp)² · φ(α) )\n```\n\nwhere `α = ∠(viewer–planet–host)` is the phase angle and `φ(α) = (sin α + (π − α)·cos α)/π` is the Lambertian phase function (3re.17).\n\n# Treatment\n\n- Reuse the perceptual-disc abstraction (`shaders/perceptual-disc.glsl`) — same brightness-PSF saturation physics for any point of light.\n- Reuse the existing magnitude slider — no new control.\n- Drop the unconditional pixel-floor on planet bodies; sub-cutoff planets fade naturally.\n\n# Pluto\n\nAlready wired (PLANET_ORDER, ELEMENTS via Standish \u0026 Williams 2010, SOL_PLANETS). Albedo 0.49 added in this PR.\n\n# Geometric albedos (Mallama 2018 / NASA fact sheets)\n\nMercury 0.142 · Venus 0.689 · Earth 0.434 · Mars 0.170 · Jupiter 0.538 · Saturn 0.499 · Uranus 0.488 · Neptune 0.442 · Pluto 0.49\n\n# Generic to all hosts\n\nSame code path renders planets for any star with a `PlanetSystem`. Forward-compatible with stellata-bk5 (exoplanets).\n\n# Feeds chart mode\n\nOutput of this bead (per-planet apparent magnitude) is consumed by stellata-m40.3 (chart-mode planets at current `t`).\n\n# Files actually touched\n\n- `src/client/perceptual-magnitude.ts` (NEW), `shaders/perceptual-disc.glsl` (NEW): shared math chunk for the brightness → disc-pixel mapping.\n- `src/client/planet-system.ts`: Planet gains an `albedo` field; populated for all 9 entries.\n- `src/client/shaders/planet.vert.glsl` + `planet.frag.glsl`: full rewrite for apparent-mag math, three-pass parity (depth-mask + disc + glow), Lambertian phase.\n- `src/client/star-system.ts` → renamed to `orbit-rings-layer.ts` with body code stripped.\n- `src/client/planet-body-field.ts` (NEW): global instanced body field with per-host distance cull.\n- `src/client/stellata.ts`: drop warpDestStarSystem; thread shared uniforms; wire recenter/maxAppMag to the body field.","design":"# Formula correction (REPLACES the formula in this bead's description)\n\nThe description's formula\n    m_planet = m_host − 2.5·log10(p · (R/d_hp)² · (R/d_vp)²)\nis dimensionally wrong (R appears twice). The correct Lambertian-\nsphere formula has one R plus the viewer↔host leg ratio:\n\n  m_host_at_viewer = M_host + 5·log10(d_vh / 10pc)\n  m_planet = m_host_at_viewer\n           − 2.5·log10( p · (R/d_vp)² · (d_vh/d_hp)² · φ(α) )\n\nVerified against Jupiter (radius 69,911 km, p=0.538):\n- Earth at opposition (d_vh=1 AU, d_hp=5.2 AU, d_vp=4.2 AU): −2.7 ✓\n- ~150 AU outside heliopause (d_vh=150 AU, d_vp≈155 AU): +5.2 ✓\n- α Cen (1.34 pc): +21 — the bead's stated +28 is incorrect.\nThe bead's heliopause-range estimate (+5 to +6) is correct. The\nbead's α-Cen estimate (+28) is wrong; correct value is +21.\n\n# Albedo data to add to SOL_PLANETS (Mallama 2018 / NASA fact sheets)\n\nMercury 0.142 · Venus 0.689 · Earth 0.434 · Mars 0.170\nJupiter 0.538 · Saturn 0.499 · Uranus 0.488 · Neptune 0.442\nPluto 0.49\n\nAdd `albedo: number` field to the `Planet` interface; populate.\n\n# Pluto already wired\n\nThe bead's \"Two options for v1\" for Pluto's ephemeris is moot.\nPluto is already in PLANET_ORDER, ELEMENTS (Standish \u0026 Williams 2010\nmean elements), and SOL_PLANETS (radius 1188 km, type 'rocky',\nrepresentative tan-pink colour). The only data missing for Pluto is\ngeometric albedo (0.49 above).\n\n# Glow halo on planets (not atmospheric twinkle)\n\nDrop the unconditional PLANET_DISC_MIN_PX=2.0 floor. Apply the same\nbrightness-driven sizing stars use (pxSize = max(physSize, appSize),\nwhere appSize ∝ √(Δm / sizeSpan)). Add a glow halo pass mirroring\nthe star pipeline. The halo simulates ocular-PSF + display-saturation\nspreading, NOT atmospheric scintillation. Same physics for any point\nof light.\n\n# Cross-cutting architecture\n\nSee parent stellata-3re --design for the full plan: PlanetBodyField\n+ OrbitRingsLayer rename + drop warpDestStarSystem + shared\nperceptual-disc abstraction + per-host distance cull + three-commit\nordering. This bead lands across commits 2 and 3.\n\n# Files actually touched (revised)\n\n- src/client/perceptual-magnitude.ts (NEW) — TS helpers.\n- src/client/shaders/perceptual-disc.glsl (NEW) — GLSL chunk.\n- src/client/planet-system.ts — add `albedo` to Planet, populate.\n- src/client/shaders/planet.vert.glsl + planet.frag.glsl — apparent-\n  mag math, three-pass parity (depth-mask + disc + glow).\n- src/client/star-system.ts → OrbitRingsLayer (body-render code\n  removed; that moves to PlanetBodyField).\n- src/client/planet-body-field.ts (NEW) — global instanced layer.\n- src/client/stellata.ts — wire attachHost / per-host cull.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T18:08:30Z","created_by":"Alex Marshall","updated_at":"2026-05-09T09:03:59Z","started_at":"2026-05-08T19:37:21Z","closed_at":"2026-05-09T09:03:59Z","close_reason":"Landed in PR #45 (v1.4.0): planet apparent-magnitude pipeline + Lambertian phase + global PlanetBodyField architecture.","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.16","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-08T19:08:30Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.16","depends_on_id":"stellata-8hu","type":"related","created_at":"2026-05-08T19:08:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-3re.15","title":"Unfocus host: keep physical objects rendered, hide representational layers","description":"**Goal:** when the focused host changes away from Sol (or any future planet host), the *physical* objects of that system stay rendered. The *representational* layers used to explain the system disappear.\n\nFirst concrete application of the physical-vs-representational rule from the three-mode framework epic.\n\n# Concrete behaviour\n\nWhen a planet host (Sol now; any host with a `PlanetSystem` later via stellata-bk5) is no longer the focused star:\n\n| Layer | Category | Behaviour when host unfocused |\n|---|---|---|\n| Planet bodies (`star-system.ts`) | Physical | Keep rendering, subject to per-planet apparent-magnitude cutoff (separate bead) |\n| Heliopause shell (`heliopause.ts`) | Representational | Hide (Standard+ shows it only when host focused) |\n| Orbit rings (`star-system.ts`) | Representational | Hide |\n| Planet labels (`planet-labels.ts`) | Representational | Hide |\n| Apex label (`heliopause.ts`) | Representational | Hide |\n| Time readout (`time-readout.ts`) | Representational | Already host-gated, no change |\n\n# Why\n\nPlanets exist whether you're looking at them or not. Current behaviour drops them when host is unfocused, which is physically wrong — and forward-incompatible with exoplanets (when looking at Alpha Cen with planets from Sol, you'd want to see those planets).\n\n# Generic to all planet hosts\n\nImplementation must not hard-code Sol. Use the `PlanetSystem` data model (stellata-3re.6) so any future host with planets gets the same treatment automatically.\n\n# Files likely touched\n\n- `src/client/stellata.ts` — focus state machine; no longer flips solar-system layer entirely off on unfocus.\n- `src/client/star-system.ts` — split orbit-ring rendering (representational, host-gated) from planet-body rendering (physical, magnitude-gated).\n- `src/client/heliopause.ts` — gate render on host-focused.\n- `src/client/planet-labels.ts` — gate render on host-focused (probably already).\n- `src/client/time-readout.ts` — already host-gated, no change.\n\n# Watch out for\n\n- The \"Solar-system layer is only meaningful when focused star has planets … at all other focuses, the layer must not render\" line in stellata-3re's description is no longer accurate. Update there as part of this work.\n- Apparent-magnitude cutoff for planet bodies is a separate bead (its own implementation effort) — this bead is the gating split. **Land them together** if practical, otherwise apparent-mag must land first or planets will pop visible at unconditional pixel-floor from far hosts.\n\n# Depends on\n\nThe apparent-magnitude planet visibility bead.","design":"# Implementation context\n\nWhen focus changes away from any planet host, the representational\nlayers (rings, heliopause shell, apex label, planet labels) disappear;\nbodies keep rendering via the new global PlanetBodyField, gated only\nby apparent-magnitude visibility (3re.16) which in practice means the\nper-host distance cull collapses them to invisible from any non-Sol\nfocus at default presets.\n\n# Labels follow rings, not body apparent-mag cutoff\n\nIf the orbit ring for planet i is visible, the planet label for i is\nvisible — EVEN IF the body's apparent magnitude is below the slider\ncutoff and the body itself isn't being drawn. Labels answer \"what\nwould I be seeing here,\" not \"what am I rendering.\" planet-labels.ts\n:99-108 already gates on isOrbitRingVisible(i); preserve this. Do\nNOT add a body-app-mag gate on labels.\n\n# Cross-cutting architecture\n\nSee parent stellata-3re --design for the full plan: PlanetBodyField\n+ OrbitRingsLayer rename + drop warpDestStarSystem + perceptual-disc\nabstraction + per-host distance cull + three-commit ordering. This\nbead lands as part of commit 3 (architecture refactor).\n\n# Files actually touched (revised)\n\n- src/client/planet-body-field.ts (NEW) — global instanced layer.\n- src/client/star-system.ts → renamed/restructured to OrbitRingsLayer\n  with body code stripped.\n- src/client/heliopause.ts — visibility wiring already correct;\n  verify it fires on focus-away-from-Sol.\n- src/client/planet-labels.ts — verify (and preserve) the\n  isOrbitRingVisible gate.\n- src/client/stellata.ts — replace per-focus starSystem.setPlanetSystem\n  with PlanetBodyField.attachHost(...) at startup; drop\n  warpDestStarSystem entirely.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T18:08:27Z","created_by":"Alex Marshall","updated_at":"2026-05-09T09:04:09Z","started_at":"2026-05-08T19:37:20Z","closed_at":"2026-05-09T09:04:09Z","close_reason":"Landed in PR #45 (v1.4.0): planet apparent-magnitude pipeline + Lambertian phase + global PlanetBodyField architecture.","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.15","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-08T19:08:27Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.15","depends_on_id":"stellata-3re.16","type":"blocks","created_at":"2026-05-08T19:08:49Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.15","depends_on_id":"stellata-8hu","type":"related","created_at":"2026-05-08T19:08:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-8hu","title":"Physical vs representational rendering — three-mode framework (Pure Visual / Standard / Advanced)","description":"**Status:** design framework. Captures a structural distinction Stellata needs to draw between *what you would see with human eyes* (stars, planets, the *effects* of dust extinction on starlight, etc.) and *structures we are aware of but cannot see* — too diffuse, too small, non-luminous, or outside the visible spectrum (heliopause / astrospheres, molecular-cloud bounding ellipsoids, planet orbit rings, labels, HUD).\n\n**Goal:** every renderable layer is classified Physical-visible vs Representational, and the user controls them via a 3-mode selector that is independent of camera mode (nav/observe) and rendering style (chart/realistic).\n\n# Three modes\n\n1. **Pure Visual** — exactly what a human eye would see at the camera's location. No labels, no HUD, no orbit rings, no heliopause shell, no cloud ellipsoids, no constellation lines. Photons only. Intent: VFX-quality reference; screenshots that could pass for real astrophotography.\n2. **Standard** — Alex's curated mix. Adds the representational layers most users find helpful: heliopause shell (when host focused), planet orbit rings, focus ring, Sol/GC arrows, scale bar, distance vector, time readout, constellation stick-figures, molecular-cloud ellipsoids. Interstellar dust voxel structure is *not* shown directly — only its extinction effect on starlight, which is physically visible.\n3. **Advanced** — user picks layers individually. Per-layer toggles for everything in the representational set (orbit rings, heliopause, planet labels, MW volumetric band, cloud ellipsoids, constellation lines, dust voxel debug visualisations, etc.).\n\n# Visual-perspective controls (orthogonal axis)\n\nIndependent of mode: apparent-magnitude cutoff, dynamic range, FOV, theme. These shape *how* visible things are rendered — not *which* things. They apply equally in all 3 modes.\n\n# Analogy\n\nAnatomy book. Pure Visual = a photograph. Standard = the artist's chosen labelled / cutaway figure. Advanced = layer-by-layer transparent overlays for studying structure in detail.\n\n# Why this epic exists\n\nToday the app mixes representational and physical rendering ad-hoc:\n\n- The heliopause shell looks beautiful but isn't visible to human eyes (Voyager 1 detected it via plasma instruments — no photons emitted).\n- The whole solar-system layer disappears when Sol is unfocused, which drops the *physical* planets along with the *representational* orbit rings.\n- There's no path to a \"no annotations\" view for users who want a pure-photographic experience.\n\n# Children (initial)\n\n- Design gate: classify every layer, define the 3-mode UI surface, define unfocused-host behaviour. Filed as a child of this epic.\n- Unfocus split (parented in stellata-3re, related to this epic): first concrete application of the rule.\n- Apparent-magnitude planet visibility (parented in stellata-3re, related to this epic): falls out of \"physical objects render per physics, not per pixel-floor\".\n\n# Coexists with chart mode\n\nChart mode (`stellata-m40`) is a *rendering style* — paper-atlas glyphs, isobars, hard-edged discs. Orthogonal to this framework. Chart mode should be combinable with any of the 3 modes (e.g. Pure Visual + chart = a Sky-Atlas-style realistic-magnitude rendering with no labels).\n\n# Settings UX integration\n\nMode selection is the new top-level grouping for the settings panel. Couples with stellata-f9b's settings rework (mag presets primary, everything else Advanced) and stellata-1yu's contextual-by-camera-mode design — settings shown depend on (mode × camera mode). Coordinate the two designs so the panel doesn't end up half-redesigned.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-08T18:08:24Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:08:24Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ev5","title":"Solar-system follow-ups: 4 AU park + about/credits + SCIENCE/README/docs","description":"Follow-up tasks landing in a single PR after the solar-system layer (PR #36 / stellata-3re) merged.\n\n# What\n\n1. Default first-load park distance for Sol drops to 4 AU (was TARGET_PARK_FRACTION-derived, ~0.11 AU). 4 AU sits just inside Jupiter's orbit so the inner planets frame the scene with Jupiter on the rim — the empirical \"you are here\" anchor.\n2. About + Credits modal updated for the new layer: planets, heliopause, JPL Standish, NASA fact sheets, Voyager/IBEX, Frisch \u0026 Slavin 2013.\n3. Welcome modal + HTML title rewritten around the empirical principle (\"Stellata — An Observational 3D Model of Our Galaxy\") — every object comes from observation; theoretical-only structures (Oort cloud, beyond-Gaia conjectures) are out by design.\n4. README intro paragraph rewritten to match. Features list gains a solar-system bullet. Data-sources table gains JPL/NASA/Voyager rows.\n5. SCIENCE.md gets a new \"Solar system\" section covering Standish ephemerides (and the VSOP87 rejection), planet physical data sources, orbital-plane orientation rule (Sol ecliptic vs other-host galactic plane), `t = now` model, and the heliopause asymmetric ellipsoid + apex direction.\n6. New `docs/solar-system.md` topic doc following the per-feature doc pattern (milky-way.md, molecular-clouds.md). CLAUDE.md docs index points at it.\n\n# Why\n\nSolar-system layer (3re) shipped without doc / front-page / modal updates per the `defer-doc-updates` rule. This PR catches up: documents the new science in SCIENCE.md, surfaces it in the user-facing modals + README, and adds the per-feature topic doc.\n\nThe 4 AU first-load change is a UX adjustment driven by the same logic: now that planets exist, the prior auto-park (which sat the camera *inside* Mercury's orbit at 0.11 AU) hides the scene's headline. 4 AU surfaces it.\n\n# Relates to\n\n- `stellata-vjm` — broader \"default first-load view\" bead (Sol + Orion + HUD on + constellations on). This PR addresses only the distance piece. Whether the rest is still wanted is a separate decision.\n- `stellata-3re` — solar-system layer epic, now merged.","notes":"PR #38: https://github.com/alexmensch/stellata/pull/38","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T10:55:26Z","created_by":"Alex Marshall","updated_at":"2026-05-08T17:30:45Z","started_at":"2026-05-08T10:55:33Z","closed_at":"2026-05-08T17:30:45Z","close_reason":"Merged in PR #38","dependencies":[{"issue_id":"stellata-ev5","depends_on_id":"stellata-vjm","type":"relates-to","created_at":"2026-05-08T11:55:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.14","title":"Kuiper belt visualisation layer — design + data sources","description":"Add a Kuiper belt layer to Stellata as the next outward feature after the heliopause (stellata-3re.5) and the bridge between solar-system scale and the interstellar layers (Local Bubble stellata-dk3, etc.). The KB sits at 30–50 AU classical, with scattered/detached/sednoid populations extending to ~1000 AU.\n\n# Scope tension to resolve at design-gate\n\nstellata-3re's description deliberately excludes \"minor planets\". KBOs are minor planets. Two reasonable resolutions:\n1. Keep under 3re and amend the parent's deliberately-excluded list to read \"asteroids and inner-system minor planets\" (KBOs are an outer-system structural layer, not asteroid-belt content).\n2. Split into its own layer epic (sibling of c7u, dk3, 9hp).\n\nRecommendation: option 1 — the KB is part of the solar-system layer at the user's perception level (a ring of objects around the Sun), and splitting forces an epic for what is really one renderable structure. But this is a judgment call for Alex.\n\n# What this bead captures\n\nThe data-source survey + structural design that any future implementation builds from. Research already done as of 2026-05-08 — see Design and Notes sections for the substance. Implementation-children to be filed once design-gate is approved.","design":"# Structural design — what to render\n\n## Four overlays, not one\n\nThe \"Kuiper belt\" is four dynamical populations with different geometries. Render each as its own particle layer with separate uniforms:\n\n1. **Cold classical disk** — thin annulus 42–47 AU, scale-height H/r ≈ tan(2°) ≈ 0.035, sharp outer truncation at the 2:1 resonance (\"Kuiper cliff\" at 47.7 AU). Near-circular orbits (e ≲ 0.1).\n2. **Hot classical / scattering torus** — 30–80 AU radial extent (with tail to several hundred AU), H/r ≈ tan(15°) ≈ 0.27. Fat torus, almost a thick disk. Long sparse radial tail from high-e orbits.\n3. **Detached / extended scattered** — a ≈ 50–250 AU, q \u003e 40 AU. Sparse halo, not a disk.\n4. **Sednoids** — q \u003e 50, a \u003e 200 to 1000+ AU. 4 known objects (Sedna, 2012 VP113, Leleākūhonua, 2023 KQ14). Render as individual orbits, not a continuum.\n\n## Inclination distribution (Brown 2001 functional form)\n\n`sin(i) × [f·N(σ_cold) + (1−f)·N(σ_hot)]` — two Gaussians. Best modern values (Gulbis et al. 2010, DES): σ_cold = 2.0° ± 0.5, σ_hot = 8.1° (+2.6/−2.1). Brown's original σ_hot ≈ 17° is contested; literature spans 6–17° depending on whether the high-i tail is Gaussian or power-law.\n\n## Mid-plane warp\n\nVolk \u0026 Malhotra 2017 reported a ~13° warp at 40–42 AU. Van Laerhoven 2019 (OSSOS XIV) cannot reproduce it from a well-characterised sample. **Treat the mean plane as the invariable plane (i ≈ 1.58° to ecliptic) and skip the warp** — it's a research-frontier feature, not a robust visual element.\n\n## Resonances are NOT visually obvious in 3D\n\nIn real-space x-y-z snapshots, the 3:2 (plutinos), 2:1 (twotinos), 5:2 are sharp stripes only in (a, e, i) space. Resonant objects librate around fixed longitudes relative to Neptune — at any instant they form arcs, not rings. For a viewer that doesn't track Neptune's longitude, resonances disappear. **Don't try to render them as overdensities.**\n\n## Density and particle count\n\nTrue debiased counts: ~30,000 hot main-belt + ~10⁵ cold-classical (D \u003e 50 km), ~5×10⁴ detached, ~3×10⁴ scattering. Mean spacing of D \u003e 50 km objects in the cold band: ~0.08 AU. Volume density at 44 AU peak: ~50 objects per AU³ for D \u003e 50 km — empty by terrestrial standards but vastly denser than typical \"void\" diagrams imply.\n\nVisualisation peers (Eyes on the Solar System, SpaceEngine, AAS WWT, Celestia) all use **stylised 10²–10⁴ particle clouds**, not full debiased populations. Recommendation: 5,000–20,000 procedurally-sampled particles for visual structure, OR all ~6,000 known objects rendered as discrete points with their actual orbital elements (Eyes' approach for numbered TNOs).\n\n## Colour\n\nBimodality is real and visually significant (Tegler \u0026 Romanishin; Col-OSSOS taxonomy Pike/Marsset/Schwamb 2017–2024):\n\n- **Cold classicals**: uniformly ultra-red. RGB target ≈ #E04A1A region.\n- **Neutral hot/scattered/plutinos**: warm grey. RGB target ≈ #A89580.\n- Col-OSSOS cut: g–r = 0.75 separates BrightIR (broad optical, dominates dynamically excited population) from FaintIR (~all cold classicals).\n\nAlbedos: cold classicals p_V ≈ 0.15; hot/scattered p_V ≈ 0.05–0.10 (darker). Outliers: Eris 0.96, Pluto 0.62, Charon 0.41, Arrokoth 0.24.\n\n## Recommended primary data file\n\n**JPL Small-Body Database TNO subset** (`sb-class=TNO,Centaur` query, public domain US-Government data, safe to bundle):\n- ~6,000 objects, ~3–5 MB CSV, ~1 MB gzipped → `data/jpl-sbdb-tno-2026.csv`\n- Heliocentric J2000 ecliptic osculating elements at fixed epoch.\n- For TNOs with multi-decade orbits, **convert to barycentric** before propagating — heliocentric a oscillates ~0.5 AU as Sol moves around the SS barycentre.\n- Pick reference epoch JD 2461000.5 (2026-01-01 12:00 TT) and propagate all elements to it via SBDB.\n\nAugment with:\n- **OSSOS VII catalogue** (Bannister 2018, ApJS 236, 18) — 838 well-characterised TNOs, debiased structure. CC-BY supplementary, \u003c500 KB → `data/ossos-vii-2018.csv`.\n- **Col-OSSOS colour table** (Fraser/Schwamb 2023, PSJ 4, 98) — ~98 TNOs with full optical+NIR, CC-BY, \u003c50 KB → `data/col-ossos-colours-2023.csv`.\n- **Sednoids hand list** — Sedna, 2012 VP113, Leleākūhonua, 2023 KQ14, plus Eris/Gonggong if treating dwarf planets here → `data/sednoids.csv`.\n\n**Avoid** MPCORB.DAT for redistribution — MPC's \"personal use only\" license blocks it. SBDB is the redistributable equivalent.\n\n**Vera Rubin DP1** ships in 2026 with the first SSP TNO catalog. Worth re-evaluating data sourcing when DP1 lands.\n\n## Implementation children to file (after design-gate approval)\n\n1. Data-ingest script (`scripts/build-kuiper.py` or `.ts`) — fetch SBDB CSV (one-shot), commit raw `data/jpl-sbdb-tno-2026.csv`, derive `public/kuiper.bin` at build with classified populations + colour-class flag.\n2. Kuiper-particle render layer — instanced quads per object, billboarded, two colour-class shaders.\n3. Visibility heuristic — only render when camera is within solar-system zoom regime (probably tied to Sol-focus + camera distance threshold).\n4. Optional: Keplerian propagation at runtime via uTime (matches variable-star pulsation pattern) so orbits aren't frozen snapshots. Skip for v1 if static positions read fine.\n5. Chart-mode treatment — flat hard-edged dots vs. paper aesthetic.","notes":"# Citations (DOIs / arXiv / URLs)\n\nBrown 2001 inclination distribution — AJ 121, 2804, doi:10.1086/319734\nGulbis et al. 2010 DES inclination — arXiv:1005.1719\nPetit et al. 2011 CFEPS — AJ 142, 131\nPetit et al. 2023 OSSOS hot main-belt SFD — ApJL 947 L4, doi:10.3847/2041-8213/acc525\nVolk et al. 2016 OSSOS III plutinos — AJ 152, 23\nVolk \u0026 Malhotra 2017 mid-plane warp — AJ 154, 62, doi:10.3847/1538-3881/aa79ff\nVan Laerhoven 2019 OSSOS XIV plane — AJ 158, 49, doi:10.3847/1538-3881/ab24e1\nBeaudoin et al. 2023 OSSOS XXIX detached — PSJ 4, 145, arXiv:2306.12847\nBannister et al. 2018 OSSOS VII (838 TNOs) — ApJS 236, 18, doi:10.3847/1538-4365/aab77a\nBernardinelli et al. 2020 DES TNOs — ApJS 247, 32, doi:10.3847/1538-4365/ab6bd8\nCamargo-Molina et al. 2025 DES Y6 TNO photometry — arXiv:2504.16927\nFraser et al. 2024 Subaru-NH distant TNO survey — arXiv:2407.21142 (notes overabundance \u003e70 AU; possible \"second belt\")\nSpencer et al. 2024 Arrokoth discovery — PSJ doi:10.3847/PSJ/ad676d\nTegler et al. 2016 colour bimodality — AJ 152, 210\nFraser/Schwamb et al. 2023 Col-OSSOS Two Types — PSJ 4, 98, doi:10.3847/PSJ/acc844\nMarsset et al. 2024 Trojan-like q\u003c30 KBOs — PSJ doi:10.3847/PSJ/ad698a\nBuratti et al. 2017 Pluto/Charon albedos — arXiv:1604.06129\nPitjeva \u0026 Pitjev 2018 KB total mass — CeMDA 130, 57, doi:10.1007/s10569-018-9853-5\nLykawka et al. 2025 inner kernel at 43 AU — arXiv:2511.07512\nRubin/LSST commissioning Solar System release Dec 2025 — noirlab.edu/public/announcements/ann25024\nJPL SBDB Query API — ssd-api.jpl.nasa.gov/doc/sbdb_query.html\nMPC TNO list (reference, do NOT redistribute) — minorplanetcenter.net/iau/lists/TNOs.html\n\n# Caveats for implementation\n\n- **Survey selection effects**: every catalog is biased toward bright, low-i, near-perihelion objects. Plotting only known orbits drastically under-represents the high-i tail and the q \u003e 50 AU detached/sednoid population. For a \"catalog viewer\" this is fine if disclosed; for a structurally accurate visualisation, sample from a debiased OSSOS/CFEPS model rather than the raw SBDB list.\n- **Heliocentric vs barycentric**: SBDB returns heliocentric. Convert to barycentric before long-baseline orbit propagation — Sol moves ~0.5 AU around the SS barycentre and that aliases into oscillating heliocentric a values for TNOs.\n- **Reference plane**: invariable plane (i ≈ 1.58° to ecliptic, Ω ~107° in J2000 ecliptic) is what OSSOS warp papers use. Plain ecliptic is fine for rendering.\n- **Boundary conventions are not standardised**: \"Kuiper belt\" = 30–50 AU; \"scattered disk\" = a \u003e 50 AU with q ≈ 30–40 AU; \"detached\" = q \u003e 40 AU but a \u003c ~1000 AU; \"inner Oort\" = a \u003e 1000–2000 AU. Pick one and document it.\n- **Frozen-data rule**: per CLAUDE.md / `frozen-external-data` memory, fetch script must be a one-shot named explicitly (e.g. `scripts/refresh-kuiper.py`), gated from `npm run build`. Build reads only from committed `data/` files.\n- **Resonance kernel discovery (Lykawka 2025)**: a new \"inner kernel\" at 43 AU was reported Nov 2025. Real overdensity worth depicting if procedurally sampling — bumps to consider in addition to the well-known 44 AU kernel.\n\n# Decisions to confirm at design-gate\n\n1. Scope under 3re or split into its own epic? (See description.)\n2. Stylised particle cloud (5–20k procedural) vs. real-catalog rendering (~6k discrete objects from SBDB)? Hybrid is also viable: real catalog for bright/numbered, stylised cloud underneath for the unobserved population.\n3. Static positions or Kepler-propagated against uTime?\n4. Sednoid orbits as line traces, or particles only?\n5. Render condition — Sol-focus + camera distance threshold, or always-on inside some scale band?","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-08T10:12:23Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:12:23Z","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.14","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-08T11:12:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.13","title":"PR #36 review-pass polish","description":"Review-pass changes for PR #36 (epic 3re solar-system layer):\n\n- Version bump.\n- Orbit-ring orientation fix: rings currently sit flat on the host plane with perihelion at +x. Compose per-planet Rz(Ω)·Rx(I)·Rz(ω) (matching planetEclipticAU) before host orientation so rings align with ephemeris-driven body positions. Most visible on Mercury (I=7°, Ω+ω≈77°).\n- Heliopause: swap LineSegments+WireframeGeometry → Mesh+SphereGeometry with a translucent MeshBasicMaterial. User wants to see the solid shell first, then decide whether to suppress when camera is inside.\n- Time readout: relocate from .ui-bottom (next to scale-bar) to under the star-count in .meta.\n- Investigation only (no implementation this round): when does warp switch focus source→destination? Answer: only at finishWarp. The structural pin on focusedStar is load-bearing for the warp lerp's local-frame invariants. Cleanest fix for the readout-during-warp issue is for time-readout to subscribe to onWarpChange directly.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T08:43:21Z","created_by":"Alex Marshall","updated_at":"2026-05-08T09:56:13Z","started_at":"2026-05-08T08:43:27Z","closed_at":"2026-05-08T09:56:13Z","close_reason":"PR #36 review-pass polish landed: version bump, orbit-ring orientation, heliopause Fresnel shell + label tracking, Pluto, time readout placement + warp gate. Awaiting Alex's manual smoke + merge.","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.13","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-08T09:43:20Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.12","title":"Render Sol's planet positions live in observe mode","description":"**Goal:** when the user is in OBSERVE mode anchored on Sol, render the eight Sol planets at their real wall-clock positions so the user feels actually parked inside a working solar system.\n\nCherry-on-top finish for the stellata-3re epic. Should land last so it benefits from everything below it.\n\n**Depends on:**\n- stellata-3re.3 (VSOP87 ephemerides via astronomia) — without real positions this is just the placeholder layout we already have.\n- stellata-3re.11 (t time variable, JD conversion) — drives the position lookup.\n\n**Scope:**\n\n- Sol-only. Other hosts (exoplanets via stellata-bk5) intentionally stay on the placeholder eccentric-anomaly layout — no real ephemerides exist.\n- Only meaningful in observe mode (camera parked on Sol). Navigate-mode placeholder positions still work; the difference is too subtle at navigate-mode framings to justify the per-frame VSOP87 call.\n- Per-frame: query getPlanetPositions(t) (from 3re.3), update the per-instance iPosition buffer in StarSystem's body mesh, and the cached bodyLocalPositions array that planet-labels reads.\n- Time scale: t = now (per 3re.1 default). When the time-scrubber epic lands (stellata-nmu) the same code path picks up scrubbed time for free.\n\n**Out of scope:**\n\n- Animation of orbital motion at user-perceptible rates. Real Earth moves ~30 km/s; at parsec scale that's 6.5e-12 pc/s, which is invisible at any zoom we ship. The point is correct positioning at the user's wall-clock time, not visible motion.\n- Time-varying axial rotation, day/night phase shading, or any per-body attitude — that's stellata-2f6 territory.\n\n**Files likely touched:**\n\n- src/client/star-system.ts — wire a per-frame position-update path on the body geometry attribute (with .needsUpdate = true on the iPosition InstancedBufferAttribute when in observe + Sol-focus mode).\n- src/client/planet-labels.ts — already reads from the cached buffer, picks up the new positions for free.\n\nLABELS: phase-12a, solar-system, observe","notes":"Subsumed by stellata-3re.3 (commit 2c54dd2). The original bd gated per-frame VSOP87 to observe mode for perf; the actual implementation in 3re.3 caches per minute-bucket of t, making the per-frame cost negligible (8 array copies + 8 quaternion applies when the bucket changes; near-zero otherwise). Navigate mode also gets real positions as a result, which is strictly better than the planned placeholder-in-navigate / live-in-observe split — no step change when entering observe mode. Closes on epic-3re PR merge alongside its siblings.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-07T11:11:12Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:22:17Z","started_at":"2026-05-07T12:53:39Z","closed_at":"2026-05-08T10:22:17Z","close_reason":"subsumed by stellata-3re.3 — per-frame ephemeris is unconditional and the per-minute-bucket cache makes the original observe-only perf gate unnecessary","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.12","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-07T12:11:11Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.67","title":"Show visible star count under total in bottom-right HUD","description":"Quick win UX: the bottom-right HUD shows the total number of stars in the catalogue (e.g. '313,242 stars'). Add a second line directly below that with the count of stars currently visible on screen, e.g.\n\n  313,242 stars\n  1,298 visible\n\nVisibility means stars that pass current filters AND are within the camera frustum AND brighter than the magnitude cutoff (i.e. actually being drawn this frame). This gives the user immediate feedback on what magnitude / filter changes are doing to the rendered population, and provides a sense of scale for navigation choices.\n\nImplementation notes:\n- Counter source: the star renderer already has the per-frame draw count for the disc + glow passes; surface that to the HUD instead of recomputing on the CPU. Pick whichever pass is most representative (probably the union).\n- Update cadence: per-frame is fine (the number is small text and the value will be stable most frames). If updates feel jittery, debounce to ~10 Hz.\n- Format: same comma-grouped integer style as the existing total. Localise via toLocaleString() if the total already does.\n- Don't break the bottom-right layout — the new line should sit directly under the existing total without nudging the scale bar.","notes":"PR #37 opened against main; awaiting review + merge. Closes via PR body.\nConfirmed working in browser by Alex 2026-05-07: settle UX feels right, count resolves smoothly after camera stops.\n\n---\n\n## 2026-05-08: SHELVED after review (review session, PR #37)\n\nShelved because the CPU mirror cannot match GPU truth once dust extinction is in scope. The PR's `Stellata.countVisibleStars()` re-applies the shader's filter rules on the CPU but does NOT integrate dust extinction along the camera→star sightline. Through molecular cloud cores A_V is 1–2 mag, well above the 0.5-mag soft taper, so the count would over-report by potentially hundreds of stars when looking through Taurus/Orion/etc. The PR description's claim of \"accurate to within a handful\" was only true with dust off.\n\nA secondary gap (cheaply fixable on its own): the CPU mirror used strict `appMag \u003e maxAppMag` while the shader uses `appMag \u003e uMaxAppMag + 0.5` (soft taper). Under-counted the soft-taper population.\n\nAlex's call: \"if we can't get the count to be accurate to what is visible on screen, then I think I will probably just shelve this idea.\"\n\n## What was built (worktree branch: `worktree-visible-star-count`)\n\nPR #37 commits, in order:\n- `e5b4ad4` — Initial CPU `countVisibleStars()` + `main.ts` wiring + bottom-right HUD line. Throttled 10 Hz.\n- `ca568de` — Extracted `visible-count.ts` + dirty-check gate + `window.debug.visibleCount()` timing accessor.\n- `5102e1a` — Settle-debounce (220ms quiet window after change, em-dash placeholder during interaction). Tightened the inner loop: `_distSolByIdx` cache (no per-star sqrt), `dCam² + 2.5·log10(dCam²)` form (drops second sqrt), inline view·projection mat4×vec4 in clip space (no Vector3 alloc, no perspective divide).\n- `dcc76f2` — Fixed settle never resolving: TrackballControls' damping leaves residual sub-perceptible motion for ~10s after release; switched dirty-check from strict `!==` to per-frame epsilons (1e-4 pc position, ~0.001° rotation, 1e-3° fov).\n\nNet diff: stellata.ts +68, visible-count.ts +136 (new), main.ts +19. Files only.\n\n## Why GPU counting is hard in WebGL2 (explored 2026-05-08)\n\nThe renderer dispatches all 313k instances; vertex shader either kicks the quad outside clip space or rasterizes it. No record survives the discard.\n\nOptions reviewed:\n1. **Occlusion queries** (`ANY_SAMPLES_PASSED`, `SAMPLES_PASSED`) — return fragment-sample counts, not instance counts. A bright 100px star contributes 100. Useless.\n2. **Transform-feedback primitive count** (`TRANSFORM_FEEDBACK_PRIMITIVES_WRITTEN`) — TF captures pre-clip output regardless of frustum, and you can't conditionally skip emission. Returns dispatched count, not visible count.\n3. **Atomic counters / SSBOs / compute shaders** — not in WebGL2 / GLSL ES 3.00. Would be trivial in WebGPU.\n\nThe path that actually works in WebGL2: a counting render target (each instance writes 1.0 to its own texel when visible — ~1.2MB at R32F × 313k texels), a reduction shader (mipmap or parallel-sum), single-pixel readback with `fenceSync` + `clientWaitSync` polled across frames. Adds an RT, a reduction pass, and ~100 lines of fence/PBO machinery. Async semantics make the count 2–3 frames stale, but the existing settle-debounce already hides that.\n\nDecision: not worth the GPU plumbing for a sense-of-scale HUD readout. WebGPU would make this ~10 lines (atomic counter); WebGL2 makes it a real build.\n\n## What would justify reopening\n\n- Stellata migrates to WebGPU (then this is trivially solvable with a compute pass + atomic counter).\n- A user-facing perf or correctness need surfaces that justifies the WebGL2 counting-RT + async-readback build.\n- Dust extinction becomes uniformly small (won't happen — molecular cloud cores remain heavy).\n- Acceptable approximation defined: e.g. \"count passes pre-extinction filter; dust-dimmed survivors not subtracted\" — ship with explicit caveat in the HUD label rather than try to be accurate.\n\n## How to pick up\n\n1. The PR #37 implementation (settle-debounce, optimised inner loop, HUD wiring) is good baseline — preserve it.\n2. The hard part is the dust-corrected count. Pick from: WebGPU compute pass (post-migration), WebGL2 counting-RT + async fence readback, or accept-and-document-the-imprecision.\n3. Branch `worktree-visible-star-count` preserves all four commits; the PR was closed with a back-link to this bead.","status":"deferred","priority":2,"issue_type":"feature","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-07T10:09:42Z","created_by":"Alex Marshall","updated_at":"2026-05-08T09:06:55Z","started_at":"2026-05-07T10:14:35Z","dependencies":[{"issue_id":"stellata-9mm.67","depends_on_id":"stellata-0it","type":"relates-to","created_at":"2026-05-10T09:16:56Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.67","depends_on_id":"stellata-9mm.191","type":"parent-child","created_at":"2026-05-15T12:53:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.11","title":"Time readout in solar-system layer (display only, no scrubber)","description":"**Goal:** discreet readout in `.ui-bottom` showing the current `t` value. No edit affordance, no scrubbing — just shows what time the planets are positioned for. The full scrubber UI lives in the time-scrubber epic (stellata-nmu).\n\n### Format decision (must pick one)\n\nDeep-space ops convention is **UTC / Zulu time** — likely the right answer, but the bead captures the question explicitly so future-you does not pick at random:\n\n| Format | Example | Pros | Cons |\n|---|---|---|---|\n| UTC Zulu | `2026-05-07 18:23Z` | Standard for spaceflight, NASA, ESA, etc. Trailing `Z` is unambiguous. | Less familiar to casual users than wall-clock local. |\n| ISO 8601 with offset | `2026-05-07T18:23:45+00:00` | Machine-readable, copy-paste friendly. | Verbose. |\n| Julian Date | `JD 2461163.27` | Astronomy-native; what VSOP87 actually uses internally. | Opaque to users who do not know JD. |\n| Plain English | `7 May 2026, 18:23 UTC` | Most readable. | Locale-dependent format. |\n\nRecommendation: **UTC Zulu (`2026-05-07 18:23Z`)** — matches the project's \"honest physical scene\" tone, unambiguous, succinct.\n\n### Visibility\n\nOnly when solar-system layer is active (focused star has planets per 3re.A). Hidden otherwise — same contract as the rest of the layer.\n\n### State\n\nAlways reflects the current `t` from stellata-3re.1. Since v1 sets `t = Date.now() / 1000` and never changes it, the readout updates once per second to keep the displayed time live.\n\n### Files likely touched\n\n- New: small module or block in an existing file. Probably extends the `.ui-bottom` chrome alongside the scale bar.\n- Touches: `src/client/index.html`, `styles.css`, `src/client/stellata.ts` for the `t` subscription.\n\n### Out of scope\n\n- Any scrubber, edit, jump-to-date affordance — those go to stellata-nmu (time-scrubber epic).\n- Any URL parameter for `t` — v1 does not write `t=` to the URL since it is always \"now\"; that arrives with the scrubber.\n\nLABELS: phase-12a, solar-system, ui","notes":"Created 2026-05-07 to replace the original 3re.2 (full time scrubber UI) with a display-only readout for v1. The full scrubber moved to stellata-nmu and reparented 3re.2 lives there.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:50:12Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:22:25Z","started_at":"2026-05-07T12:17:35Z","closed_at":"2026-05-08T10:22:25Z","close_reason":"Closed","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.11","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-07T09:50:11Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.11","depends_on_id":"stellata-3re.1","type":"blocks","created_at":"2026-05-07T09:55:18Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.10","title":"Research: high-quality textures for Sol planets, decide whether to ship","description":"**Goal:** evaluate whether public-domain or permissively-licensed surface texture maps for Mercury through Neptune are available at sufficient quality for Stellata, and decide whether to ship them.\n\n### Context\n\nIn stellata-vjm's default first-load view, the largest planets at the framing distance render at roughly 24 px diameter at most. At closer zooms (stellata-3re.E lets the camera drop to ~0.5 AU from the focused planet) the discs grow significantly larger — texture would dominate the visual story.\n\nWithout textures: representative-colour discs (Mars rust, Jupiter banded yellow, Neptune blue, etc.) — fast to ship, low risk, reads as schematic.\nWith textures: real surface detail at close approach, dramatic emotional payoff. Cost: bundle size, license vetting, shader work for spherical mapping + lighting.\n\n### Sources to evaluate\n\n| Source | Likely license | Notes |\n|---|---|---|\n| USGS Astrogeology | Public domain | Best-of-class for solid bodies; Mercury, Venus, Mars, Moon. |\n| NASA Visible Earth / Solar System Treks | Public domain (NASA) | Earth, Mars, Moon, ice giants composites. |\n| Stellarium texture set | GPL | Reusable subject to GPL — may complicate Stellata licensing if Stellata is not GPL itself. |\n| ESA Hubble / JWST imagery | Permissive (with attribution) | Jupiter, Saturn, ice giants (atmospheric features). |\n| JPL planetary photojournal | Public domain (NASA-funded) | Saturn rings, Galilean moons (out of scope for v1 — no moons). |\n\n### Deliverable\n\nA yes / no decision documented in this issue's closing note plus, if yes, a shortlist of source files committed to `data/textures/` via Git LFS (per the frozen-external-data rule — no live fetch during build).\n\nIf no: stellata-3re.E ships representative-colour discs only and the question can be revisited later.\n\n### Out of scope\n\n- Atmospheric scattering, ring textures for Saturn, moon surface maps. Even if v1 ships planet textures, those are separate efforts.\n\nLABELS: phase-12a, solar-system, research","notes":"Created 2026-05-07 to capture the texture-quality investigation. Result feeds into stellata-3re.E's rendering scope — feasible to defer until 3re.E starts; do not block the rest of the epic on this answer.","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:49:56Z","created_by":"Alex Marshall","updated_at":"2026-05-07T11:05:27Z","closed_at":"2026-05-07T11:05:27Z","close_reason":"Deferred to stellata-2f6 (planet-as-object: select / focus / zoom). High-quality textures only earn their keep once the user can fly close enough to a planet to see them; with the current pixel-size-floor regime they would never be visible. Re-open and reconsider after 2f6's selection/zoom affordance lands.","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.10","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-07T09:49:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.9","title":"UX: indicate stars-with-planets at distance + label planets up close","description":"**Goal:** decide and implement two related UX problems. Both apply symmetrically to Sol (this epic) and exoplanets (stellata-bk5).\n\n### Part A: distance indicator — \"this star has planets\"\n\nWhen the user is in the open starfield, can they tell which stars carry planet systems? Open design questions:\n- A glyph / symbol next to the star's label (e.g. `·` cluster, ringed-planet pictogram)?\n- A subtle change to the disc (faint halo, ring, colour shift)?\n- An overlay marker that fades in only when the camera is within some radius?\n- Search-result decoration (the search dropdown already shows variable-star indicators — could parallel it)?\n\nTrade-off: too prominent and it competes with the star colours / brightness story; too subtle and the discovery affordance is lost. Default position: lean subtle, since planet existence is meant to be a \"visit and explore\" reward (per stellata-bk5's framing), not a primary navigation cue.\n\n### Part B: up-close labels for individual planets\n\nWhen focused on a host star and zoomed in enough that the planet bodies / rings are resolved, planets should be identifiable. Open questions:\n- Always-on labels for all 8 (Sol) or 1–N (exoplanet) planets, or only on hover / proximity?\n- Label format — \"Earth\" for Sol, \"Proxima b\" for exoplanets (NASA `pl_name`); should it include orbital info or just name?\n- Do labels stay anchored to the body or float to a label-engine layout (similar to the existing chart-labels / poi-overlay patterns)?\n- Click-through behaviour — does clicking a planet label do anything (focus the planet? open a side-panel readout)?\n\n### Why this is its own task\n\nBoth decisions ripple into stellata-3re.A (the `PlanetSystem` interface needs to carry whatever per-planet display fields the labels consume), 3re.B (rings may need a label too, e.g. \"Pluto / Kuiper Belt\"), and bk5.3 (the exoplanet renderer). Settling the design before any of those go to code avoids re-work.\n\n### Files likely touched\n\n- `src/client/solar-system.ts` (from 3re.E) — host of the planet-label layer.\n- Possibly `src/client/poi-overlay.ts` — the existing observe-mode label/arrow infrastructure may extend cleanly.\n- `src/client/chart-labels.ts` — chart-mode planet labels need a parallel decision (or can defer to stellata-m40.3).\n- Some shared affordance for \"this star is special\" indicator on the disc / search results.\n\n### Out of scope\n\n- The actual planet body rendering (3re.E).\n- Search-by-planet-name (deliberately excluded in stellata-bk5; same exclusion holds here for Sol — planets are visit-to-discover).\n\nLABELS: phase-12a, solar-system, ui, design-decision","notes":"Design recorded 2026-05-07 on PR #36 (epic 3re).\n\nPart A (distance indicator for planet-bearing stars) — DEFERRED to stellata-bk5.3. Sol is the only planet-bearing host in v1; the brand box, HUD Sol arrow, and default first-load view already privilege it. A discovery cue earns its keep when bk5 introduces hundreds of exoplanet hosts and only then.\n\nPart B (up-close planet labels) — CONTRACT for stellata-3re.4 to implement:\n- Always-on when focused on a planet-bearing host AND the host's planet bodies are rendering (3re.4's render gate).\n- Anchored to each body's projected screen position (not floated through a label-engine layout).\n- Name only — no orbital info; a side-panel readout for orbital details is a separate later concern.\n- No hover or click action in v1.\n- Hidden in chart mode (stellata-m40.3 owns the chart-mode planet-label aesthetic separately).\n- Label engine choice (per-frame DOM positioning vs. SVG inside #overlay) is open; 3re.4 picks at impl time, but the contract above is fixed.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:49:41Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:22:25Z","started_at":"2026-05-07T10:38:10Z","closed_at":"2026-05-08T10:22:25Z","close_reason":"Closed","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.9","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-07T09:49:41Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.9","depends_on_id":"stellata-3re.6","type":"blocks","created_at":"2026-05-07T09:55:12Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.9","depends_on_id":"stellata-bk5.3","type":"relates-to","created_at":"2026-05-07T09:55:55Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.8","title":"Design rule: Sol orbital plane = ecliptic, all other hosts = galactic plane","description":"**Goal:** capture the design decision now so future orbital-ring code does not ad-hoc this.\n\n### The rule\n\n- **Sol**: orbital rings drawn in the **true ecliptic plane** (J2000 obliquity ε = 23.4392911° tilted from ICRS equatorial). The ecliptic is tilted ~60° relative to the galactic plane, so Sol's planet system reads as a deliberately tilted disc against the Milky Way background — physically accurate.\n- **Every other host star** (initially exoplanets via stellata-bk5): orbital rings drawn in the **galactic plane**.\n\n### Why this asymmetry\n\nFor Sol we know the orbital plane exactly — the ecliptic. Drawing it correctly is free physical accuracy, and the tilt against the galactic plane gives the user a reference for \"this is the home system.\"\n\nFor exoplanets we almost never know the orbital plane. NASA Exoplanet Archive carries inclination relative to the line-of-sight from Earth (`pl_orbincl`) for transiting planets, but that does not pin down the *true 3D plane* without knowing the longitude of ascending node — which is essentially never measured. Picking arbitrary random planes per host would just look noisy.\n\nDrawing every non-Sol system flat in the galactic plane gives a single coherent visual story: every other star's orbits anchor to the same reference frame as the dust, the Milky Way disc, and the molecular cloud distribution. The user sees Sol's tilted disc against a sea of flat-galactic rings and immediately reads \"we are different from everyone else here, but only because we have privileged knowledge.\"\n\n### Implementation note\n\nFor each host star, compute the local rotation matrix from the chosen plane normal:\n- **Sol**: ecliptic normal in ICRS = `(0, sin ε, cos ε)` ≈ `(0, 0.3978, 0.9175)`. Apply to ring geometry built in the local xy-plane.\n- **Other host**: galactic North Pole direction in ICRS = approximately `(α=192.86°, δ=27.13°)` (J2000 IAU value). Convert to a unit vector once and reuse per host.\n\nApply per-star to the ring-instance geometry (and to the planet position vectors when stellata-3re.E gets there).\n\n### Out of scope\n\n- True orbital planes for individual exoplanet systems even when known (e.g. transiting systems with constraints on inclination). v1 always uses the galactic-plane fallback.\n\nLABELS: phase-12a, solar-system, design-decision","notes":"Created 2026-05-07. Replaces the previous \"ICRS xy-plane\" simplification used for the 1 AU / 50 AU scale rings in stellata-3re.5's description (those were a fudge; this rule is the deliberate choice).","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:49:19Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:22:25Z","started_at":"2026-05-07T10:21:42Z","closed_at":"2026-05-08T10:22:25Z","close_reason":"Closed","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.8","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-07T09:49:19Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.8","depends_on_id":"stellata-bk5.3","type":"relates-to","created_at":"2026-05-07T09:55:50Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-3re.7","title":"Per-planet orbit rings with angular-separation visibility heuristic","description":"**Goal:** render orbital rings around the focused host star — but only when neighbouring rings are perceptually distinct.\n\nDistinct from the 1 AU / 50 AU *scale* rings in stellata-3re.5. Those are reference circles always present at solar-system scale; these are per-planet ellipses gated on visibility.\n\n### Visibility gate (proposal — refine at impl time)\n\nFor each ring `i` with semi-major axis `a_i`, compute its on-screen angular separation from rings `i-1` and `i+1` at the current camera distance `d`. Render ring `i` only when both gaps exceed a pixel threshold (rough starting point: 4–6 px). Effects:\n- Far view (camera at hundreds of AU, looking at the inner system): outer rings (Jupiter+) visible; Mercury/Venus/Earth pile up → suppressed.\n- Close view (camera inside ~10 AU): inner rings spread out and become individually visible; outer rings drop off the visibility test (or simply lie outside the frustum).\n- Performance: only 8 rings × Sol means trivial cost; the heuristic also keeps GPU draw counts down once exoplanet systems with many close-in companions land.\n\nPer-planet ring data comes from stellata-3re.A's `PlanetSystem` (semi-major axis + eccentricity).\n\n### Render gate\n\nOnly when focused star has planets (3re.A). Visible only when solar-system layer is active.\n\n### Plane orientation\n\nGoverned by stellata-3re.C (Sol uses ecliptic, all other hosts use galactic plane). Each ring geometry is built in a local plane and rotated into the focused star's local frame.\n\n### Files\n\n- New rendering layer (likely `src/client/solar-system.ts` from 3re.E).\n- Reads `PlanetSystem` from 3re.A.\n- Reads camera distance from stellata.ts per frame.\n\nLABELS: phase-12a, solar-system, visual","notes":"Created 2026-05-07 to capture per-planet orbital rings. Distinct from the 1 AU / 50 AU scale rings already in 3re.5. Reusable by stellata-bk5.3 when exoplanet rings land — same heuristic, same render layer.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:49:02Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:22:33Z","started_at":"2026-05-07T10:12:06Z","closed_at":"2026-05-08T10:22:33Z","close_reason":"Closed","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.7","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-07T09:49:01Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.7","depends_on_id":"stellata-3re.6","type":"blocks","created_at":"2026-05-07T09:55:10Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.7","depends_on_id":"stellata-3re.8","type":"blocks","created_at":"2026-05-07T09:55:15Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.7","depends_on_id":"stellata-bk5.3","type":"relates-to","created_at":"2026-05-07T09:55:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.6","title":"Per-star planet data model + render gating (groundwork)","description":"**Goal:** introduce the abstraction that any focal star *can* carry planet data, even though only Sol uses it in v1. Lays the groundwork stellata-bk5 will reuse without changes.\n\nThe thing to avoid is baking Sol-specific assumptions into the planet rendering pipeline. If 3re.4 hardcodes \"if focused star is Sol, render planets,\" then bk5 has to refactor before it can plug in. Better to design the surface once.\n\n### What this lands\n\n- A per-star `hasPlanets` boolean exposed on the catalog record (initially: only Sol returns true; populated from a literal lookup, not a flag bit).\n- A typed `PlanetSystem` data record interface (host idx, list of planets with radius / orbit / type fields). Initially populated for Sol from a hardcoded constant (8 planets, real radii, real semi-major axes, real types — orbital phase deferred to stellata-3re.G/.3).\n- A render gate in stellata.ts: solar-system rendering layer activates only when `getFocusedStar().hasPlanets === true`.\n- A focus hook that loads / supplies the `PlanetSystem` for the focused star (synchronous for Sol, async-friendly so bk5 can plug in its lazy fetch later without API changes).\n\n### Why this comes first\n\nThree of the next tasks in this epic (orbital rings, planet bodies, indicator/labels UX) all consume `PlanetSystem`. Designing it up front avoids a re-shape pass after bk5 lands.\n\n### Out of scope\n\n- NASA Exoplanet Archive ingest + catalog flag bit + lazy json (lives in stellata-bk5.1).\n- Rendering itself — this task ends at the data structure + gating contract. Render layers consume it in 3re.B/3re.E.\n\nLABELS: phase-12a, solar-system, groundwork","notes":"Created 2026-05-07 to capture Alex's \"any star may have planets\" precursor. Bridges stellata-3re and stellata-bk5 — bk5.2 (loader) should fulfil this same contract when it lands.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:48:48Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:22:24Z","started_at":"2026-05-07T09:56:59Z","closed_at":"2026-05-08T10:22:24Z","close_reason":"Closed","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.6","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-07T09:48:48Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.6","depends_on_id":"stellata-bk5","type":"relates-to","created_at":"2026-05-07T09:55:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"stellata-nxb","title":"Expand search index with alternate designations (V-numbers, GJ, HIP variants, CCDM, etc.)","description":"Investigate adding a broader set of common names and alternate designations to the search index so users can find stars by names beyond the current Bayer/Flamsteed/HIP set.\n\nMotivating example: Proxima Centauri's Wikipedia page lists Alf Cen C, Alpha Centauri C, V645 Centauri, GJ 551, HIP 70890, CCDM J14396-6050C, LFT 1110, LHS 49, LPM 526, LTT 5721, NLTT 37460. We don't need all of these, but several would be high-value:\n\n- **GCVS variable-star designations** — strongest candidate because GCVS is already cross-matched into the catalogue; the variable designation is the natural query a variable-stars user would type. Covers both forms in the same GCVS sequence: V### numbers (V645 Cen → Proxima; V1500 Cyg) AND the older two-letter codes used by the first 334 variables per constellation (VY CMa, R CrB, SS Cyg, T Tau, RR Lyr). Should be near-trivial since GCVS data already flows through the pipeline.\n- **Greek-letter abbreviations** (Alf Cen C alongside Alpha Centauri C) — already partly handled?\n- **GJ / Gliese** catalogue numbers — common in nearby-stars literature.\n- **Multi-component CCDM letters** (Alpha Centauri C, A, B) — partly tied to the binary/multiple work.\n\nOut of scope (probably): LFT, LHS, LPM, LTT, NLTT — esoteric proper-motion catalogues; low search value.\n\nTasks:\n1. Audit what alternate-name fields exist in AT-HYG v3.3 and which we currently ingest into search-index.json.\n2. Decide which additional designation classes to support (GCVS designations prioritised).\n3. Update build-catalog.ts / search index generator to include them.\n4. Verify search.ts dual-input typeahead handles the new keys (likely no change needed; just more entries).","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:38:17Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:40:17Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-v88","title":"Release notes from PR body section + backfill v1.0.0–v1.1.2","description":"Today the deploy workflow runs 'gh release create --generate-notes' which collapses each PR to its title — uninformative for big PRs like #22. Switch to the option-B approach designed in the planning chat: each PR's body carries a '## Release notes' section that the deploy workflow extracts and uses for the GitHub release.\n\nGoing-forward process:\n- Add .github/pull_request_template.md with a '## Release notes' block scaffold (Summary / New features / Bugfixes / Changes).\n- Add .github/workflows/release-notes-guard.yml — a CI gate that fails any PR without the skip-version-bump label unless a non-empty '## Release notes' section is present in the body.\n- Update .github/workflows/deploy.yml — when a version-bump merge fires the deploy job, find the squash commit's PR number, fetch its body via 'gh pr view --json body', extract the '## Release notes' section, and pass to 'gh release create --notes-file'. Fall back to --generate-notes if the section is missing (defence in depth — guard already enforces it).\n- Document in RELEASING.md and add a one-liner to CLAUDE.md so it's part of the version-bump checklist.\n\nBackfill:\n- For each released tag (v1.0.0, v1.0.2, v1.0.10, v1.0.12, v1.1.0, v1.1.1, v1.1.2), identify the PR that bumped to that version, write a '## Release notes' block in the PR description, then 'gh release edit vX.Y.Z --notes-file ...' so existing release pages get the new content.\n- Tags pre-deploy-workflow (v1.0.0) may not map cleanly to a PR — handle case-by-case.","status":"closed","priority":2,"issue_type":"feature","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-07T07:58:22Z","created_by":"Alex Marshall","updated_at":"2026-05-07T08:08:46Z","started_at":"2026-05-07T07:58:43Z","closed_at":"2026-05-07T08:08:46Z","close_reason":"Implemented and PR opened (#34, CI green). Backfilled v1.0.2/.9/.10/.12, v1.1.0/.1/.2 release pages directly via gh release edit; v1.0.0 left as already hand-authored.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.3","title":"Distance vector to a cloud destination still uses filter.sizeMax instead of cloud's rendered edge","description":"Surfaced in PR #22 (stellata-a7d.2) review.\n\nPR #22 fixed the star-destination case in src/client/distance-vector-overlay.ts:102-104 so the chevron tip lands on the destination star's actual rendered disc edge:\n```\nconst destOffsetPx = toStar !== null\n  ? Math.max(stellata.renderedSizePx(toStar), 0)\n  : Math.max(stellata.getFilter().sizeMax, 0);\n```\n\nThe cloud branch falls back to `getFilter().sizeMax` — a UI-knob value, not the rendered cloud edge. So a distance vector pointed at a large nearby cloud can have its chevron land mid-cloud (or far past it), while a vector pointed at a similarly-sized star lands cleanly on the disc edge. Asymmetry that betrays the unified physical-scale story.\n\nFix: clouds need a `renderedEdgePx(cloudIdx)` analogue. Likely keyed off the projected ellipsoid extent at current FOV — `MolecularClouds` already has the per-cloud half-axes, and `cloudViewingDistancePc` factors are the same shape. Either expose a 'projected radius in pixels at current camera' getter on MolecularClouds and call it from distance-vector-overlay, or mirror the structure of `Stellata.renderedSizePx` with a `renderedCloudSizePx(cloudIdx)` method.\n\nCaveats:\n- Clouds are non-spherical; the projected silhouette depends on camera direction. A simplest-thing-that-works is largest-axis × foreshortening factor, matching `cloudViewingDistancePc`'s approach.\n- Cloud chart-mode rendering uses isobars rather than filled volumes; those have their own visual edge that may differ. Acceptable to use the volumetric edge for the chevron offset regardless of mode.\n\nFiled under stellata-a7d (parent epic of the close-approach rewrite) rather than stellata-9mm because this is a user-visible behaviour gap, not a refactor — the chevron lands in the wrong place for cloud destinations.\n\nNote: cloud rendering is shelved for v1.0 per CLAUDE.md (`showMolecularClouds` defaults false, toggle removed from settings). So this defect is currently invisible to end users. Worth fixing whenever clouds come back, hence P2 (sits with the rest of physical-reality follow-ups).\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:47:27Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:59:52Z","started_at":"2026-05-06T21:56:08Z","closed_at":"2026-05-06T21:59:52Z","close_reason":"Fixed in PR #22 commit 32077d7 — added Stellata.renderedCloudSizePx mirroring renderedSizePx, distance-vector overlay now uses cloud silhouette (largest semi-axis) instead of getFilter().sizeMax fallback.","dependencies":[{"issue_id":"stellata-a7d.3","depends_on_id":"stellata-a7d","type":"parent-child","created_at":"2026-05-06T22:47:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.60","title":"Test gap: zero unit coverage for new angular-diameter geometry (minOrbitDistForStar, minDistForStar, renderedSizePx)","description":"Surfaced in PR #22 (stellata-a7d.2) review.\n\nThe PR rewrites the close-approach geometry pipeline around `θ = 2·atan(R/d)` and four new pure-math helpers:\n- `renderedSizePx` (src/client/stellata.ts:2534) — composite app/phys disc diameter.\n- `renderedDiscPxAtPeak` (stellata.ts:2745) — physSize-only with peak amplitude.\n- `minOrbitDistForStar` (stellata.ts:2699) — manual zoom floor (90% fill).\n- `minDistForStar` (stellata.ts:2722) — auto-park distance (10% fill).\n\nAll four are pure functions of (R, dCam, FOV, viewport, variability). No DOM, no WebGL — trivially unit-testable with vitest. Currently all four have zero coverage.\n\nAcceptance criteria 2/3/4 of stellata-a7d.2 are guarded only by the manual smoke list:\n- (2) Resolved-disc ratio Betelgeuse:Sirius matches R_Betel:R_Sirius — testable as `renderedSizePx` ratio assertion at fixed dCam, fov, viewport.\n- (3) Manual zoom orbit floor fills 90% of viewport minor axis — testable as `renderedSizePx(idx)` at `d = minOrbitDistForStar(idx)` should equal `0.9 * min(viewport.x, viewport.y)` for any non-binary idx.\n- (4) Warp arrival 10% fill — `renderedSizePx(idx)` at `d = minDistForStar(idx)` ≈ `0.1 * min(viewport.x, viewport.y)` (with the 2×dMin floor caveat).\n\nFix: add src/client/star-geometry.test.ts (or extend an existing test file) with these assertions plus a few representative inputs. Will need a mock-Stellata-or-thin-extraction approach since the four methods currently sit on the class. Easiest path: extract the formulas into pure functions in a new src/client/star-geometry.ts module (alongside catalog-pure.ts), and have the class methods become thin wrappers. Already paired with the DRY refactor in the physical-size-formula-triplicated bead.\n\nWhy P2: pure-math math-fragility is exactly the kind of regression vector that's invisible in manual smoke until someone changes a constant by 10× and lands a Sirius-sized supergiant. Currently the only guard is reviewer-eye + visual smoke.\n","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:46:48Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:27Z","closed_at":"2026-05-06T22:35:27Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.60","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:46:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.58","title":"Stellata.onFrame has no unsubscribe API; pin-debug-hud and arrow-fade-debug-hud both leak handler closures on toggle","description":"Surfaced in PR #22 (stellata-a7d.2) review.\n\nStellata exposes `onFrame(handler)` (src/client/stellata.ts:873) but no corresponding off/unsubscribe method. The handler array is never pruned.\n\nTwo debug HUDs introduced/expanded in this PR rely on this API and acknowledge the gap:\n\n1. **src/client/pin-debug-hud.ts:148-153** — registers `stellata.onFrame(onFrame)`. Comment at line 150-152 says: \"No frame-handler removal API today; the closure keeps `panel` null after toggle-off so onFrame becomes a no-op.\"\n2. **src/client/arrow-fade-debug-hud.ts:77** — same pattern, doesn't even attempt to track unsubscribe.\n\nEffect: every `debug.pin()` / `debug.arrows()` toggle on/off cycle adds a closure to the onFrameHandlers array. Toggling 50 times leaves 50 effectively-no-op closures running every frame. The closures don't allocate (early-return on `panel === null`), but they accumulate forever until page reload.\n\nFix: add `offFrame(handler)` (and matching for the other event arrays — onFocus/onFilter/etc. have the same shape and likely the same gap). Each registration returns a disposer or accepts a handle for later removal. The two debug HUDs then call `unsubscribe()` on close.\n\nWhy P2: code-quality refactor that's mostly invisible until a debug session leaks enough closures to be noticeable, BUT it's user-introduced via the new debug HUDs. As more debug HUDs are added (likely — pin and arrows already follow the pattern) this gap compounds.\n\nOut of scope: the same gap likely exists for `onFocusChange`, `onFilterChange`, `onStateChange`, etc. (each is a private array with a single setter function that pushes). A general unsubscribe pattern (registering returns a disposer fn) would address all eight handler arrays at once. File a follow-up if a wider refactor is preferred.\n","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:46:20Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:26Z","closed_at":"2026-05-06T22:35:26Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.58","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:46:19Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-f9b","title":"Settings/UX panel rework","description":"Umbrella for the three coupled settings-panel changes that should land together:\n\n- stellata-1yu: contextual layout per camera mode (nav vs observe)\n- stellata-v3j: magnitude presets as primary UX, everything else Advanced\n- stellata-iuh: move star-size knobs into the Advanced section\n\nstellata-1yu is the design gate — it sets the layout grammar that v3j and iuh slot into. Don't ship one without the others or the panel ends up in a half-redesigned state.","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:19:49Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:49Z","dependencies":[{"issue_id":"stellata-f9b","depends_on_id":"stellata-8hu","type":"related","created_at":"2026-05-08T19:08:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.51","title":"Rebase PR #30 onto PR #22 after #22 lands — surgery plan","description":"PR #30 (epic 9mm cleanup batch 1) is open with 43 beads of work but cannot merge until PR #22 (stellata-a7d.2 physical disc geometry) lands first. PR #22 is the priority and supersedes ~5 of PR #30s refactors. This bead captures the rebase plan so the surgery is mechanical when the time comes.\n\n# 3-way merge surface\n\nMechanical conflicts: 4 files, 6 hunks total.\n- package.json — version bump (both bumped from 1.0.12; pick whichever is later)\n- docs/camera-modes.md — both edited the camera.near prose; PR #22s wording about GLOBAL_MIN_DIST_PC supersedes\n- src/client/hud-overlay.ts — constructor signature changed in #30 (Sol/GC click callbacks); cascade rewritten in #22\n- src/client/url-state.ts — #30 added single dirty flag + OBSERVE_CAM_LOCAL; #22 added worldOffset field\n\nFiles that auto-merge cleanly: stellata.ts, arrow-path.ts, distance-vector-overlay.ts, shaders/star.vert.glsl, url-state.test.ts. Pleasant surprise — #22s edits live in the star-sizing math; #30s edits live in dispose/constructor/helper extraction.\n\n# Semantically superseded beads (REOPEN these after rebase)\n\nThese #30 changes go against the grain of #22 and should be reverted during the rebase, with the matching bead reopened so a future session can address them under #22s new design:\n\n- **9mm.3 (3-tier cascade)** — PR #22 redesigned to 2 tiers (target-projection + view-space, dropping the aux-step tier) with per-axis FOV scaling. screenDirFromCascade is no longer the right shape. Reopen 9mm.3 with note: cascade should follow #22s 2-tier design.\n\n- **9mm.5 (projectToScreen hoist)** — PR #22 keeps projectToScreen local to hud-overlay.ts. The hoist to arrow-path.ts works against #22. Either drop the hoist (revert) or reopen 9mm.5 with note that consolidation should follow #22s structure.\n\n- **9mm.46 (OVERLAY_NEAR_CLIP_PC = 1e-3)** — PR #22 changes the orbit floor to GLOBAL_MIN_DIST_PC = 1e-9 and overlays still read camera.near. The decoupling is moot. Revert and reopen 9mm.46 with note: re-evaluate after #22 lands; may no longer be needed.\n\n- **9mm.20 / 9mm.24 (OBSERVE_CAM_LOCAL)** — PR #22 adds a worldOffset URL field and reframes the cam-default check entirely: when the frame isnt Sol-anchored, cam is always emitted regardless of value. The named constant lives inside a logic that #22 changes. Revert and reopen with note: re-evaluate the magic [0,0,0] under #22s worldOffset frame.\n\n- **9mm.30 (single dirty flag)** — PR #22 adds another condition (`view.worldOffset`) to the controls.update OR. The dirty flag refactor still works but needs the new branch folded in. Resolve the conflict by adding `controlsDirty = true` to the worldOffset branch in #22s decoder. Bead can be closed without reopening.\n\n# Beads that survive rebase intact\n\nThe other ~38 beads are independent of #22:\n- All P1 dispose-chain fixes (9mm.29, 9mm.38, 9mm.40, 9mm.42)\n- All catalog flag work (9mm.21, 9mm.25, 9mm.28, 9mm.32, 9mm.37) — touches scripts/catalog-pure.ts + scripts/build-catalog.ts which #22 doesnt touch\n- All test additions\n- Small cleanups: 9mm.2, 9mm.4, 9mm.8, 9mm.10, 9mm.13, 9mm.16, 9mm.18, 9mm.19, 9mm.23, 9mm.26, 9mm.31, 9mm.33, 9mm.35, 9mm.36, 9mm.41, 9mm.47, 9mm.48\n- Doc updates: 9mm.22, 9mm.45, 9mm.50 (need conflict resolution against #22s doc edits but the additions themselves are orthogonal)\n\n# Procedure when #22 lands\n\n1. From the worktree:\n   ```\n   git fetch origin main\n   git rebase origin/main\n   ```\n2. Conflict resolution per the four files above. For the superseded beads, prefer reverting #30s changes (i.e. keep #22s side) so the surgery is just `git checkout --theirs \u003cfile\u003e` for the parts under #22s ownership, then re-apply only the #30 changes that arent superseded.\n3. Reopen the 4 superseded beads (9mm.3, 9mm.5, 9mm.46, 9mm.20/24) via `bd update --status=open` and add a note like \"Reopened after PR #30 rebase onto PR #22; the original direction is superseded by #22s [specific change]; re-evaluate the new starting point.\"\n4. Bump package.json to whatever 1.0.X is next after #22.\n5. Verify `npm run typecheck` and `npm test`.\n6. Force-push the rebased branch.\n7. Re-run the manual-smoke checklist on the rebased PR — especially the highest-priority items (HudOverlay click rewiring, camera roll-snap on both exit paths, build-catalog log lines).\n\n# Why this strategy (vs the alternatives)\n\n- *Merge #30 first*: would force the user to rebase the bigger, more important #22 — and several of #30s helpers are about to be obsoleted anyway.\n- *Pull #22 into #30*: bundles the diffs and defeats the goal of reviewing #22 independently. Worst option.\n- *Cherry-pick safe parts of #30 now*: tempting but cleaner done as a focused rebase after #22 lands rather than splitting #30 prematurely.\n\nThe rebase is meaningful but tractable — clear deletions of obsolete pieces, not knotty hunk-by-hunk reconciliation. PR #30s bug fix (camera roll-snap on observe hard-switch / focus-change) is the only user-visible change and its against an internal side-project, so the delay isnt urgent.","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T06:52:54Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:48Z","closed_at":"2026-05-06T22:35:48Z","close_reason":"Surgery plan executed in PR #30 batch 2 commit 148dcb2. Conflicts resolved per the 4-file recipe; superseded beads (9mm.3, 9mm.5, 9mm.20, 9mm.24, 9mm.46) reopened with notes.","dependencies":[{"issue_id":"stellata-9mm.51","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T07:52:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.48","title":"Off-screen sentinel early-returns skip \u003clogdepthbuf_vertex\u003e — undocumented load-bearing invariant","description":"Three vertex shaders early-return with a fixed off-screen `gl_Position` BEFORE the trailing `#include \u003clogdepthbuf_vertex\u003e`:\n\n- src/client/shaders/star.vert.glsl:168 (uHideFocusIdx suppression) and :248 (visibility filter) — both `gl_Position = vec4(2.0, 2.0, 2.0, 1.0); … return;` ahead of the chunk include at line 320.\n- src/client/shaders/dust-particle.vert.glsl:43 — `gl_Position = vec4(2.0, 2.0, 2.0, 1.0);` then `return;` at line 45 ahead of the chunk include at line 69.\n\nThe chunk's job is to populate the `vFragDepth` varying that the matching frag shader's `\u003clogdepthbuf_fragment\u003e` reads. Skipping the chunk leaves `vFragDepth` undefined for that vertex.\n\nWhy this is currently safe: every vertex of the instanced quad goes through the same code path (the early-return condition reads only per-instance attributes, not per-vertex), so all 4 verts of the quad land at the same off-screen NDC and the entire primitive is clipped before rasterization. The fragment shader never executes for that quad, so undefined `vFragDepth` doesn't leak into a depth write.\n\nWhy it's worth flagging: the safety is a nontrivial invariant that depends on (a) the off-screen sentinel being applied to ALL verts of the primitive, (b) NDC clipping discarding the whole primitive. A future change — say, a per-vertex variation in the off-screen position, or a switch to non-clipping primitive types — could let one quad have valid `vFragDepth` on three verts and undefined on the fourth, producing interpolated garbage and a stray near-z stamp. Nothing in the shaders documents this.\n\nSuggested direction: cheapest fix is a one-line comment near each early-return ('// All 4 quad verts share this path; chunk skip is safe because the primitive is fully off-screen'). More defensive: include the chunk before each early-return so `vFragDepth` is always written. The chunk is cheap and idempotent.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:14:49Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:45Z","started_at":"2026-05-05T22:53:23Z","closed_at":"2026-05-06T23:06:45Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.48","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:14:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.39","title":"No regression-prevention tests for the 5 bugs in PR #23","description":"PR #23 fixed 5 bugs (stellata-3s2/6oy/mu9/adr/a8w) with zero new vitest coverage. Each is a regression candidate; the test suite is exactly where these should land so future refactors don't silently undo the fix. Per-bug:\n\n- **stellata-3s2** (unit-toggle): closure-captured buttons + idempotent bindUnitToggle. JSDOM test that calls bindUnitToggle twice on the same host and asserts only the second-bind's listeners fire is straightforward.\n- **stellata-6oy** (FilterState patch type-safety): purely compile-time so a runtime test is hard, but a small `tsd`-style assertion or a test that constructs a Partial\u003cFilterState\u003e patch with a bogus key and expects ts2353 would pin the contract.\n- **stellata-mu9** (setIsobar uniform rebind + blending restore): construct MolecularClouds with a stubbed catalog, call setIsobar(true, u1) → setIsobar(true, u1) → setIsobar(false, u1), and assert blending restores to AdditiveBlending and uMaxAppMag is === u1 throughout. No GL needed.\n- **stellata-adr** (particle byte-length validation): unit test on a pure helper would require extracting the validation; alternatively a parser test that feeds a truncated ArrayBuffer to loadDustParticles (mocking fetch) and expects null.\n- **stellata-a8w** (camera.up alignment): assert that after `camera.up.set(0,1,0).applyQuaternion(q)`, camera.up equals the camera's local +Y basis vector (a column of the rotation matrix). Pure linear algebra, no Three scene needed.\n\nWhy it matters: 5 bugs were caught by code review (stellata-y84) rather than by tests. Closing that loop is the difference between 'we found these once' and 'they're guarded'.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:12:04Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:44Z","started_at":"2026-05-06T06:24:08Z","closed_at":"2026-05-06T23:06:44Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.39","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:12:04Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.34","title":"applyDoublesFlag has zero test coverage despite being a core writer of the same bit","description":"catalog-pure.test.ts has solid coverage for `inferBinaries` (including the new mutual-pair regression cases added in PR #13). But `applyDoublesFlag` and `parseHipCcdm` (build-catalog.ts:310-430) have NO unit tests — they live in build-catalog.ts which has no test file, while everything testable was deliberately moved to catalog-pure.ts.\n\nThe whole point of stellata-7is was that two writers diverged in their semantics. Half of that surface (the CCDM half) is still untested. A future contributor could rewrite `applyDoublesFlag` to flag every component of a CCDM group, and the only signal would be the rebuild log + visual chart-mode review. The semantic contract — \"exactly one primary per CCDM group, picked by brightest absmag, idempotent with inferBinaries' bit\" — needs pinning down.\n\nDirection: move the pure parts of `parseHipCcdm` (TSV row parsing, MultFlag gate, group aggregation) and `applyDoublesFlag` (group → primary index) into catalog-pure.ts so they can be unit-tested. Then add tests covering: (1) brightest-of-group is the only one flagged, (2) groups with no catalog components are silently dropped, (3) override systems behave like file systems, (4) idempotency with a pre-existing 0x10 bit set by inferBinaries. The combined sibling 'unify behind markPrimary helper' refactor would naturally land most of this in catalog-pure too.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:44Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:15Z","started_at":"2026-05-05T22:51:49Z","closed_at":"2026-05-08T18:54:15Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.34","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:43Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.33","title":"setIsobar / setMonochrome blending state is duplicated and uncoordinated (stellata-mu9)","description":"PR #23's setIsobar fix (molecular-clouds.ts:123-142) restores blending on toggle-off using the inline expression `this.mono ? NormalBlending : AdditiveBlending` — copy-pasted from setMonochrome's blending logic at line 110.\n\nTwo issues:\n\n1. **DRY**: 'what blending mode should this material be in?' now lives in two places. Future changes to the per-mode blending (e.g. premultiplied-alpha tweak, new chart palette) have to be applied in both setMonochrome and setIsobar's else-branch in lockstep.\n\n2. **State coordination gap**: setMonochrome unconditionally writes blending without consulting an active isobar, and setIsobar's toggle-off ignores nothing about the live monochrome state at the moment of the call (it does read this.mono, but only for the off-branch). If isobar is on and setMonochrome is called (e.g. theme change while chart-mode is active), the cloud blending will get clobbered to whatever setMonochrome decides regardless of isobar being live.\n\nSuggested direction: Add a private `applyBlending()` that derives blending from (this.mono, this.isobar) — store `isobar` on the instance like `mono` — and have both setters mutate their flag and call applyBlending(). Single source of truth, no coordination race.\n\nNote: the cloud overlay is shelved for v1; both bugs are latent. Worth doing before the layer is re-enabled.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:32Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:42Z","started_at":"2026-05-05T22:51:49Z","closed_at":"2026-05-06T23:06:42Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.33","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:31Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.28","title":"Geometric and CCDM passes can double-flag stars in the same physical system","description":"PR #13 says the contract is \"each binary system gets exactly one chart-mode wings glyph.\" But the two passes run independently and there's no consolidation:\n\n1. `inferBinaries` (build-catalog.ts:676) flags the brighter of mutual geometric pairs.\n2. `applyDoublesFlag` (build-catalog.ts:703) then flags the brightest catalog component of each CCDM group.\n\nBoth pick \"brightest of group\" but their groups are defined differently (3D nearest-neighbour mutual pairs vs. CCDM_ID grouping). They can disagree on which star is the anchor of the same physical system. Concrete failure mode:\n\n- α Cen A and α Cen B are a mutual geometric pair → `inferBinaries` flags A (brighter).\n- A and B *also* share a CCDM ID → `applyDoublesFlag` walks that group and again picks the brightest in catalog (A). Idempotent here.\n\nBut:\n\n- Suppose for some triple system the geometric pass identifies a mutual pair (B, C) with C as primary because A isn't in a mutual pair (see catalog-pure.test.ts:375 \"non-mutual chains\" case).\n- The CCDM grouping for the same physical system contains {A, B, C} and `applyDoublesFlag` picks A as brightest.\n\nResult: both A *and* C carry `0x10` for the same physical system → two wings glyphs.\n\nThe PR's rebuild log shows \"9-system overlap\" between the two passes — that confirms agreement happens, not that disagreement can't.\n\nDirection: either (a) make `applyDoublesFlag` consult an existing-flag set and skip groups that already contain a `0x10` star (preserving the geometric pick), or (b) document explicitly that the contract is \"at most one per writer\" not \"at most one per system\" and update the chart-mode wings rendering to dedup.\n\nBumping to P2: the PR description states the invariant the code may violate, so this is a stated-behaviour gap, not a future risk.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:15Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:40Z","started_at":"2026-05-05T22:51:48Z","closed_at":"2026-05-06T23:06:40Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.28","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.27","title":"No regression test for cam=[0,0,0] omission in observe mode (PR #1 changed currentStateOf + applyDecodedView, added zero tests)","description":"src/client/url-state.test.ts covers `encodeBlob`/`decodeBlob` round-trips thoroughly (~500 lines) but not `currentStateOf` or `applyDecodedView` — the two functions PR #1 actually modified. As a result the entire optimisation (\"observe-mode share drops the 16 zero-bytes of cam\") and its delicate decoder counterpart (pre-snap to (0,0,0) before `controls.update()` so `lookAt` builds the right quaternion) are wholly untested.\\n\\nMissing coverage worth filing:\\n1. Encoder: when `mode === 'observe'` and camera is at (0,0,0), the produced blob must not have the cam presence bit set. Symmetric: in navigate mode at (0,0,0), cam SHOULD be encoded (because (0,0,0) ≠ DEFAULT_CAM=[0,0,30]).\\n2. Round-trip: starting from a focused observe-mode state, encode → decode → apply onto a fresh Stellata stub → the camera quaternion matches the original.\\n3. Legacy decode: a v1 URL that explicitly carries `cam=(0,0,0)` with mode=observe should still produce the same final orientation as a v2 URL that omits cam. (Comment at 715-719 implies this; nothing tests it.)\\n\\nWhy it matters: this PR's behaviour is exactly the kind that fails silently — share URL still works, but the address bar grows or shrinks unexpectedly, or the camera lands looking the wrong way after a refactor. Detecting that without a test means a human noticing \"why is my URL longer now\" months later.\\n\\nSuggested direction: `currentStateOf` and `applyDecodedView` need a Stellata test double (camera, controls, getFocusedStar, getCameraMode, setCameraMode). Then the cam-default-by-mode invariant can be asserted explicitly. Same harness would cover other state-vs-default decisions in the encoder.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:08Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:40Z","started_at":"2026-05-05T22:51:47Z","closed_at":"2026-05-06T23:06:40Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.27","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.25","title":"Unify the two binary-primary writers behind a single markPrimary() helper","description":"PR #13 made `applyDoublesFlag` (build-catalog.ts:400-430) and `inferBinaries` (catalog-pure.ts:244-316) *agree* on the 0x10 semantics but did not unify them. The two writers still independently:\n\n1. Iterate a group of stars (mutual pair vs. CCDM group)\n2. Run the same `bestMag = Infinity; if (m \u003c bestMag) ...` brightest-pick loop\n3. OR `flags |= 0x10` onto the winner\n\nThis is two implementations of the same conceptual operation: \"pick the brightest star in this group and mark it as the system anchor.\" A third source tomorrow (e.g. WDS, Gaia DR4 binaries, etc.) would have to re-implement the same idiom and remember the convention again — exactly the failure mode stellata-7is documented.\n\nDirection: extract a shared helper, e.g.:\n\n  // catalog-pure.ts\n  export function markPrimary(stars: BinaryStar[], indices: number[]): number | -1\n\n…that picks the brightest of `indices` (lowest absmag), ORs the binary-primary bit onto it, and returns its index (or -1 if the group is empty / unresolved). Both writers call this. Combined with the FLAG_BINARY_PRIMARY constant from the sibling refactor, the bit's meaning lives in exactly one place.\n\nThis is the structural form of the fix; PR #13 did the symptomatic form.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:57Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:39Z","started_at":"2026-05-05T22:51:46Z","closed_at":"2026-05-06T23:06:39Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.25","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.21","title":"Hoist catalog flag bit positions into a shared FLAG_* constants module","description":"PR #13 painstakingly aligned the *value* of `0x10` between two writers but left the bit positions themselves as magic numbers everywhere. The same literals (`0x01`, `0x02`, `0x04`, `0x10`) are sprinkled across at least eight sites:\n\n- scripts/build-catalog.ts:426-427 (applyDoublesFlag write)\n- scripts/build-catalog.ts:537-539 (initial flag construction in readStars)\n- scripts/build-catalog.ts:792 (Sol index detection)\n- scripts/catalog-pure.ts:312 (inferBinaries write)\n- src/client/catalog-loader.ts:138, 159 (reader)\n- src/client/chart-labels.ts:162 (consumer)\n- scripts/verify-catalog.ts:50 (debug tool)\n- src/client/catalog-loader.test.ts:245, 248 (tests)\n\nThe PR added a *comment* to each reader/writer describing the bit layout but did not introduce a single source of truth. `src/client/url-state.ts` (lines 65-72) already demonstrates the right pattern with `FLAG_GRID = 1 \u003c\u003c 0` etc.\n\nThis is the structural root cause of stellata-7is — a future writer that adds (say) a is_carbon_star bit can still drift, because there's no compile-time check that 0x10 means binary_primary at every site.\n\nDirection: define `FLAG_HAS_NAME / FLAG_IS_SOL / FLAG_HAS_BAYER / FLAG_BINARY_PRIMARY` (and the 0x08 reserved slot) once — either as exports from catalog-pure.ts (already imported by both build-catalog.ts and tests) or in a tiny new catalog-flags.ts module that both scripts/ and src/client/ import. Replace every literal with the constant. The reader's bit-layout comment then sits on the constants themselves.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:38Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:38Z","started_at":"2026-05-05T22:51:45Z","closed_at":"2026-05-06T23:06:38Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.21","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:37Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.19","title":"applyHoverClass scrolls unconditionally on every arrow-nav, even when the row is already visible and the dropdown isn't scrollable","description":"src/client/typeahead-util.ts:30 calls scrollIntoView({ block: 'nearest' }) every time the highlighted row changes — including the common case where the dropdown is short (3–5 rows fit entirely inside its 320px max-height) and no scroll is needed.\n\nscrollIntoView({ block: 'nearest' }) is documented as a no-op when the element is already in view, so this is correctness-safe. But:\n\n1. It pierces scroll containers above the dropdown. If the parent (e.g. the settings panel for #con-results) happens to be partially scrolled, 'nearest' walks up the ancestor chain and may shift the panel even when the dropdown row is fully visible inside its own scrollable \u003cul\u003e. Worth verifying this doesn't cause panel-scroll jitter on arrow-nav inside the constellation picker.\n\n2. It fires on every key, with no rAF batching. Cheap, but combined with the renderDom path on query-change you have two distinct scroll triggers; consolidating them later is easier if there's a single scroll path.\n\nSuggested direction: gate the call on `resultsEl.scrollHeight \u003e resultsEl.clientHeight` (i.e. the list is actually scrollable), or measure once on the new \u003cli\u003e with offsetTop / offsetHeight against scrollTop / clientHeight and only adjust scrollTop on the dropdown itself rather than calling scrollIntoView. Confines the scroll effect to the dropdown's own \u003cul\u003e.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:32Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:38Z","started_at":"2026-05-05T22:51:43Z","closed_at":"2026-05-06T23:06:38Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.19","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.18","title":"Handle window blur / visibilitychange mid-drag in ObserveControls","description":"PR #19 fixed pointercancel-induced stuck drag. Adjacent gap that the same review ought to flag: window-blur and document-visibilitychange mid-drag.\n\nIf the user Cmd-Tabs / switches tabs while holding down a drag, neither pointerup nor pointercancel necessarily fires before pointer events stop being delivered. On return, dragging=true and activePointerId is set; the next pointermove will resume the drag from a stale dGrabbed.\n\nReproducing scenarios:\n- Desktop: click-and-hold the canvas, Cmd-Tab to another app, return, move the cursor before clicking\n- Mobile: drag the canvas, swipe-up the app switcher, return\n\nTwo cheap fixes:\n1) window.addEventListener('blur', () =\u003e endDrag(false)) and document.addEventListener('visibilitychange', () =\u003e if (hidden) endDrag(false)).\n2) Migrate to setPointerCapture (see sibling bead) — lostpointercapture covers most of this for free.\n\nWhy it matters: same UX symptom as stellata-470 (camera rotates unexpectedly on next move), same fix shape. PR #19 took the narrow path; the broader 'pointer is no longer ours' coverage is one event listener away.\n\nOut of scope for the PR-19 patch but a P3 follow-up worth filing while the context is fresh.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:29Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:37Z","started_at":"2026-05-05T22:51:43Z","closed_at":"2026-05-06T23:06:37Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.18","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:28Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.14","title":"Add integration test that tier-3 viewSpaceScreenDir cascade fires in hud/poi overlays","description":"src/client/arrow-path.test.ts adds 8 unit tests on viewSpaceScreenDir in isolation (front/behind × on-axis/off-axis, on-axis returns null, yawed-camera flips). Solid coverage of the new helper. What's not tested: the wiring in src/client/hud-overlay.ts:272-289 and src/client/poi-overlay.ts:320-331 — that the 3-tier cascade actually reaches tier 3 when both prior tiers fail, and that an arrow path is produced and visible at the screen edge for a behind-camera target.\n\nWithout integration coverage, a future refactor that breaks the cascade ordering (e.g. accidentally inverts a 'if (!dirOk)' guard) won't trip CI even though the pure helper is still correct. The original bug was exactly this kind of integration miss — projectToScreen returning null left the cascade with no fallback.\n\nExisting distance-vector-overlay.test.ts is the prior art — it tests the projectWithNearClip helper plus its integration outputs (NDC → screen pixel mapping, off-screen clipping). Mirror that pattern for hud-overlay's updateOne and poi-overlay's onFrame loop, parameterised so a behind-camera input produces a non-empty SVG path 'd' attribute pointing in the expected direction.\n\nAcceptance check borrowed from the PR's manual smoke list (yaw 180° → POI arrow stays visible at screen edge): make that an automated assertion.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:13Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:49Z","started_at":"2026-05-06T06:24:07Z","closed_at":"2026-05-06T23:06:49Z","close_reason":"Superseded — PR #22 redesigned the HUD/POI arrow path to 2-tier inline (no third tier); arrow-path.test.ts now covers viewSpaceScreenDir directly.","dependencies":[{"issue_id":"stellata-9mm.14","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.8","title":"Wire pointercancel on the navigate-mode click detector in stellata.ts","description":"PR #19 added a pointercancel handler to ObserveControls but the parallel click-detector in src/client/stellata.ts (lines 2196-2197 register pointerdown/pointerup on the canvas; lines 2359-2367 read this.pointerDownAt) has no pointercancel.\n\nIf a touch is OS-cancelled between the pointerdown that sets pointerDownAt and the pointerup that consumes it, the pointerDownAt object lingers until the next pointerdown overwrites it. The next genuine pointerup might then satisfy the dx*dx+dy*dy\u003c=25 / \u003c500ms gates with a stale 'down' from a different gesture and fire a phantom click — same family of stuck-drag bug PR #19 fixed in the sibling controller.\n\nNote touchcancel IS wired (line 2204) for the two-finger roll. Pointer-events are missing the equivalent.\n\nWhy it matters: same bug class, different controller. PR #19 fixed observe-controls.ts in isolation when the canonical fix is 'all pointerdown handlers in this codebase need a pointercancel partner.'\n\nSuggested direction: add pointercancel listener mirroring the pointerup teardown (just clears pointerDownAt, no click dispatch). One line in attachEvents and detachEvents, ~3 lines for the handler.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:48Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:35Z","started_at":"2026-05-05T22:51:41Z","closed_at":"2026-05-06T23:06:35Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.8","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.6","title":"Add unit tests for typeahead-util.applyHoverClass and dom-util.escapeHtml","description":"PR #29 introduced two new shared modules with zero unit-test coverage despite the suite being the project's default regression-prevention layer.\n\nsrc/client/typeahead-util.ts:11 (TYPEAHEAD_MAX_RESULTS) is exercised indirectly via the constellation-typeahead tests, but src/client/typeahead-util.ts:18 (applyHoverClass) — the actual P1 fix — has no tests covering: (a) class toggling between two valid indices, (b) prev/new bounds protection (negative or out-of-range indices, which the helper guards against), (c) the prevIdx === newIdx no-op, (d) scrollIntoView behaviour on wrap-around. jsdom in vitest can verify class membership and a stub on Element.prototype.scrollIntoView covers the call.\n\nsrc/client/dom-util.ts:4 (escapeHtml) likewise has no direct test even though it now backs three call sites.\n\nWhy it matters: the PR description's own checklist marks the browser smoke as not driven (\"I did not drive the browser; the rest of the path is mechanical\"), so unit-level proof of the helper's behaviour is the only safety net. A future change that flips prev/new arg order would not be caught by typecheck.\n\nSuggested direction: small typeahead-util.test.ts using @testing-library/dom-style innerHTML setup; same for dom-util.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:42Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:35Z","started_at":"2026-05-05T22:51:40Z","closed_at":"2026-05-06T23:06:35Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.6","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:41Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.1","title":"DRY: disc-pass blend constants duplicated between init and chart-mode swap-back","description":"In src/client/stellata.ts the disc material's CustomBlending setup is spelled out in two places that must stay in sync:\n\n- Lines 685-688 (constructor): blendSrc=OneFactor, blendDst=OneFactor, blendEquation=MaxEquation, blending=CustomBlending.\n- Lines 1578-1581 (setMonochrome chart-mode -\u003e colour-mode swap-back): the same four assignments.\n\nPR #25 had to update both call sites for the AddEquation -\u003e MaxEquation switch, exactly the failure mode the duplication invites. A future tweak that misses one copy will silently regress chart-mode -\u003e navigate transitions.\n\nSuggested direction: extract a private helper (e.g. applyDiscBlendDefaults(this.material)) called from both call sites so the four assignments live in one place. Same pattern would also de-duplicate the glow material's depth/blend defaults if appropriate.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:10Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:34Z","started_at":"2026-05-05T22:51:38Z","closed_at":"2026-05-06T23:06:34Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.1","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:10Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-4je","title":"Orbit floor + parking distance should use peak-amplitude effective radius","description":"The closest-approach orbit floor (minOrbitDistForStar) and the auto-park distance (minDistForStar) in src/client/stellata.ts solve their distance equations with the static physical radius R, ignoring variable-star pulsation amplitude. For a high-amplitude variable (Mira-class, amp ≈ 5–9 mag), the peak pulse expands the disc by a factor of 10^(amp/10) — at orbit floor that overshoots ZOOM_FLOOR_FRACTION (90%) and the disc covers the whole screen for part of the cycle. The user wants closest approach to bound the *peak* fill at 90%, with the trough shrinking from there — full pulse range visible at closest zoom.\n\n# Why now\n\nSurfaced during stellata-a7d.2.2 (arrow-fade work). The arrow fade is now keyed off peak-amplitude disc size (renderedDiscPxAtPeak) so the alpha is stable across the cycle; that's already in PR #22. But the orbit floor / parking distance still let the peak overshoot the intended viewport bound, so the visualisation is inconsistent with what the calibration claims (\"90% fill at floor\"). Fix completes the physical-disc story.\n\n# What\n\nMultiply R by the peak-amplitude factor in both orbit-floor and parking-distance equations. The helper already exists in stellata.ts: `private peakAmplitudeFactor(idx)` returns `10^(amp/10)` for variables, 1 otherwise (added in stellata-a7d.2.2). Wire it into both methods.\n\n\\`\\`\\`\n// minOrbitDistForStar — currently:\nconst R = Math.max(this.catalog.physicalRadius[idx], 1e-9) * R_SUN_PC;\nconst base = R / Math.tan((ZOOM_FLOOR_FRACTION * fovMinor) / 2);\n\n// becomes:\nconst R = Math.max(this.catalog.physicalRadius[idx], 1e-9) * R_SUN_PC;\nconst Reff = R * this.peakAmplitudeFactor(idx);\nconst base = Reff / Math.tan((ZOOM_FLOOR_FRACTION * fovMinor) / 2);\n\n// Same substitution in minDistForStar (both dPark and dMin lines).\n\\`\\`\\`\n\nUpdate the docstrings on both methods to mention 'at peak amplitude for variables'.\n\n# Acceptance\n\n- Mira-class variable: at orbit floor, peak pulse fills exactly ZOOM_FLOOR_FRACTION of the viewport; trough is smaller. Full sinusoidal range of disc size visible without the peak being clipped by the screen edge.\n- Sol-class non-variable: identical behaviour to current (peakFactor = 1, expressions reduce to current form).\n- Parking distance for high-amp variable: lands further out than before, so the peak pulse reads at TARGET_PARK_FRACTION (10%) consistently. Distance change for non-variables: zero.\n- Binary companion floor (sep × BINARY_MIN_DIST_FACTOR) unchanged; still applied as Math.max with the new base.\n- No regression in warp arrival, observe-exit landing, search-select teleport — all of which call minDistForStar.\n\n# Implementation note\n\nThe full diff was prototyped during stellata-a7d.2.2 and reverted because the user wanted to test arrow fade in isolation first. To apply: edit minOrbitDistForStar (the `private` one near line ~2520) and minDistForStar (the public one just below) to multiply R by peakAmplitudeFactor — peakAmplitudeFactor itself is already on the class and active.\n\n# Files\n\n- src/client/stellata.ts — minOrbitDistForStar, minDistForStar, both docstrings.\n\n# Branch\n\nTo be filed as a follow-up under stellata-a7d.2 after a7d.2.2 lands.","status":"closed","priority":2,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-04T10:39:05Z","created_by":"Alex Marshall","updated_at":"2026-05-07T09:13:50Z","started_at":"2026-05-07T08:49:43Z","closed_at":"2026-05-07T09:13:50Z","close_reason":"Landed in PR #35 (v1.1.4): warp degrades gracefully for co-located stars (setFocus on coincident A/B); orbit floor + parking distance use peak-amplitude effective radius; star vertex shader skips the 48-tap dust raymarch for visibility-prefilter rejects with a 1.5 mag DUST_AV_HEADROOM. Smoke tests pass per Alex 2026-05-07.","dependencies":[{"issue_id":"stellata-4je","depends_on_id":"stellata-a7d","type":"parent-child","created_at":"2026-05-06T22:19:24Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-4je","depends_on_id":"stellata-a7d.2","type":"parent","created_at":"2026-05-04T11:39:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-wvl","title":"Distance-scale unit switch on close approach (sub-pc → AU / scientific notation)","description":"After a7d.2 brings physical close-approach distances into the sub-parsec / sub-light-year regime (orbit floor for a Sol-class star is ~5e-8 pc; Betelgeuse-class still small), the existing distance formatter in src/client/distance-util.ts (fmtDist) keeps reporting in pc/ly with progressively more decimals — confusing and visually unstable. Need a unit / formatting strategy for very small distances.\n\n# Why\n\nTied to a7d.2 specifically because the new orbit floor is what makes ridiculously small distances reachable. Without a7d.2 the GLOBAL_MIN_DIST_PC of 5e-3 pc bounded the smallest visible distance; now per-focus orbit floors go orders of magnitude lower. The distance label (camera-to-focus, or distance-vector to a destination) becomes unreadable.\n\n# Proposal (decide as part of the work)\n\nTwo plausible directions:\n\n1. **Auto-switch units to AU when |d| \u003c ~1000 AU** (~0.005 pc). 1 pc = 206 264.806 AU. Inside threshold show e.g. \\\"450 AU\\\", below 1 AU show R⊙ or km. Symmetric logic on the imperial / ly toggle.\n2. **Scientific notation below a threshold** (e.g. \\\"6.3e-5 pc\\\" or \\\"2.1e-4 ly\\\"). Less intuitive but unit-stable.\n\n(1) reads better for humans; (2) is simpler. Likely (1) for navigate-mode close approach; could be opt-in via the existing pc/ly toggle (add an AU mode, or auto-promote within pc).\n\n# Out of scope\n\nThis is **distance formatting / units only** — not what the scale bar represents conceptually. That's stellata-a7d.2.3 (\\\"Scale-bar UX revisit + camera-to-focus distance indicator\\\"), which is orthogonal and stays open separately.\n\n# Files\n\n- src/client/distance-util.ts — fmtDist + unit toggle plumbing.\n- Anywhere that calls fmtDist (scale-bar.ts, distance-vector-overlay.ts, hud-overlay.ts, poi-overlay.ts) — should be no changes needed since fmtDist is the single formatting point.\n- src/client/unit-toggle.ts — UI surface if a third unit is added.\n\n# Acceptance\n\n- Within close-approach orbit floor on a Sol-class star, distance label shows a readable AU value (e.g. \\\"~5 AU\\\") rather than \\\"0.00000005 pc\\\".\n- pc/ly toggle still works at outer-galactic ranges.\n- No regression in scale-bar at typical distances.\n\n# Branch\n\nTo be filed as a follow-up after a7d.2 lands. Doesn't block a7d.2 closure.","status":"closed","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-04T10:11:35Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:29:28Z","closed_at":"2026-05-06T21:29:28Z","close_reason":"Implemented as part of a7d.2.3 scale-bar widget. fmtDistAuto in distance-util.ts auto-switches pc/ly to AU below 0.01 pc (~2063 AU). Tier-aware AU formatting (3 decimals sub-1, 1 decimal 1-100, integer 100+). Both bar label and z-axis distance use the unified formatter so the readouts stay consistent across the threshold. Sub-AU distances (orbit floor for Sol-class is ~0.005 AU) stay in AU with 3-decimal precision; scientific notation deferred — never needed in normal use.","dependencies":[{"issue_id":"stellata-wvl","depends_on_id":"stellata-a7d.2","type":"related","created_at":"2026-05-04T11:11:39Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ju3","title":"Galactic arm dust extension to dust voxel model","description":"Extend the dust model to include large-scale galactic-arm structure beyond the local Edenhofer voxel volume. Sub-epic under **stellata-ty4** (the multi-source extinction stack).\n\n# Why\n\nEdenhofer (`data/dust/`) covers a ~1.25 kpc cube around Sol. Per-star sightlines that exit this volume currently get zero attenuation past the boundary (**stellata-74e** is the analytical-slab fallback for that). Neither the voxel volume nor the analytical slab encodes spiral-arm overdensities — sightlines through the Sagittarius / Perseus / Local arms are under-attenuated relative to physics.\n\nAdding spiral-arm dust would make distant sightlines (especially toward galactic-disc-plane targets) read as physically correct, complementing the volumetric MW (which the smooth disc profile already approximates) and the per-star raymarch.\n\n# Overlap with the Bayestar shell (stellata-ty4.1)\n\nty4.1 brings Bayestar19 in as a near-shell 3D voxel supplement to Edenhofer. Bayestar is a *measured* extinction map, so within its ~4-5 kpc volume it already captures arm overdensities as a side effect — without any explicit arm model. This forces a scoping question for ju3, to be settled in ju3.1:\n\n- **(option A)** Bayestar near-shell subsumes the near-Sol portion of arm work. ju3 then narrows to modelling arms *outside* Bayestar's volume (and in its southern coverage gap).\n- **(option B)** The two are independent and stack. Bayestar supplies measured 3D fidelity; ju3 supplies analytic arm-overdensity correction beyond data coverage.\n\nMost likely (A): no point modelling arm structure where measured data exists. ju3 then becomes \"extend arm overdensity into the analytical-slab region of the stack.\" But ju3.1 owns the call.\n\n# Open scoping question — whole galaxy vs near-Sol only\n\nTwo ways to land this:\n\n(a) **Whole galaxy.** A model of all major arms (Local/Orion Spur, Sagittarius, Perseus, Carina-Sagittarius, Norma, Outer) with arm-shaped overdensity functions. Most general; right answer for accurate sightlines toward galactic-plane targets at any distance. Bigger data + bigger build pipeline.\n\n(b) **Near-Sol only.** Only model the portion of arm structure that affects sightlines through the model's actually-rendered region (catalog stars + Zucker clouds + nebulae).\n\nThe likely answer is (b) for v1 — user-perceived dust matters along sightlines from the camera to rendered objects, which mostly stay within ~1-2 kpc of Sol. Confirm with the sightline-coverage analysis in ju3.1. As the model expands toward extragalactic scales (stellata-0hx, stellata-36y, parent ty4), (a) becomes more important.\n\n# Implementation hypothesis\n\nModify the existing dust-voxel volume rather than add a parallel pass. Bake arm-overdensity contributions into the same chunked 3D texture star.vert.glsl already raymarches. Single source of truth for absorption — same principle stellata-c7u uses for Zucker MCs.\n\nIf the volume needs to grow to cover modelled arm contributions outside the current 1.25 kpc cube, the chunking + manifest already supports a larger grid — bumping extents costs disk/bandwidth, not architectural rework.\n\n# Candidate data sources (to evaluate in ju3.1)\n\n- **Drimmel \u0026 Spergel 2001** — analytical 3D dust with explicit arm components, COBE-DIRBE based.\n- **Marshall et al. 2006** — 3D dust extinction from 2MASS, galactic plane to ~10 kpc, lower spatial resolution.\n- **Green et al. 2019 (Bayestar19)** — 3D extinction, ~3-5 kpc. Note: ty4.1 already brings this in as a *near-shell* supplement to Edenhofer; ju3's question is whether to use it again as an arm-overdensity source outside that scope.\n- **Lallement et al. 2022** — high-res 3D dust both hemispheres, ~3 kpc.\n\nDecision deferred to ju3.1: which source(s), at what resolution, in what frame, AND how the choice interacts with the option-A-vs-B question above.\n\n# Acceptance (to refine during scoping)\n\n- Scoping note documenting (a) data source choice + retrieval steps (frozen-data convention applies — committed under data/, no live fetch at build), (b) galaxy-wide vs near-Sol decision, (c) whether the existing voxel grid extents need to grow, (d) overlap resolution with Bayestar near-shell.\n- Sightline-coverage analysis — what fraction of currently-rendered objects has sightlines that pass through arm-overdense regions today missed by Edenhofer, Bayestar (post-ty4.1), and the analytical slab.\n- Implementation: arm contribution baked into the chunked 3D-texture pipeline (same path as Zucker MCs in c7u.2). No new shader pass.\n- Visual gate: distant sightlines toward galactic-plane targets show physically-plausible reddening/dimming relative to off-plane targets, comparable to the ESO Milky Way panorama benchmark.\n\n# Out of scope\n\n- Time-dependent or stochastic dust structure — static is fine.\n- Replacing Edenhofer near-Sol — keep the high-fidelity local data as source of truth inside its cube.\n- Per-arm visualisation overlays (UI showing where arms are as a feature) — that's an artificial-visualisation concern, file separately if wanted (related to io3 epic).","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-04T08:38:33Z","created_by":"Alex Marshall","updated_at":"2026-05-18T11:56:50Z","dependencies":[{"issue_id":"stellata-ju3","depends_on_id":"stellata-74e","type":"related","created_at":"2026-05-04T09:38:39Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-ju3","depends_on_id":"stellata-c7u","type":"related","created_at":"2026-05-04T09:38:42Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-ju3","depends_on_id":"stellata-cns","type":"related","created_at":"2026-05-04T09:38:44Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-ju3","depends_on_id":"stellata-ty4","type":"parent-child","created_at":"2026-05-18T12:55:07Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-v3j","title":"Restructure settings panel: magnitude presets as primary UX, everything else Advanced","description":"UX direction shift coming out of stellata-a7d.2.7 calibration: as Alex uses the model more, the natural mental model is 'pick how I'm looking at the sky' (naked eye, binoculars, telescope) and let the preset drive every viewing-related setting underneath. Per-knob tweaking should be reserved for power users in an Advanced section.\n\nAcceptance criteria:\n\n1. Three named magnitude presets surface as the primary settings-panel control, replacing the current preset selector + exposed star-knob layout:\n   - naked-eye (mag 6.5)\n   - binoculars (mag 10.5)\n   - telescope (replaces 'all', mag ~15 — adjust if perceptually better)\n   Pick a clear segmented-control or large-button affordance so the choice is obvious; this is the basic-mode entry point.\n\n2. Each preset drives the full bundle of viewing settings: maxAppMag, sizeMin, sizeMax, sizeSpan, starExaggerationK. PRESET_BASE + starExaggerationK[preset] already do this data-model wise (stellata.ts:96-147); the change is presentation — the per-knob sliders no longer surface in basic mode.\n\n3. Advanced section (collapsed by default, sessionStorage-persisted open/closed state) exposes:\n   - All star-rendering knobs already on the panel today (sizeMin/Max/sizeSpan, exaggeration K)\n   - FOV slider (currently main panel — debatable; revisit during design)\n   - Anything else that's a calibration knob rather than a viewing decision\n   Reverse-sync (controls.ts:218-221, 259) must keep working through both basic preset selection and Advanced-section overrides.\n\n4. Reset/override semantics still work: tweaking a value in Advanced sets the override flag (sizeMinOverridden etc.) — the basic preset chip should subtly indicate 'modified' if any override is set, with a one-click 'reset to preset' affordance.\n\n5. New 'telescope' preset replaces 'all'. Tune sizeSpan / starExaggerationK separately; defaults today are { naked-eye: 12, binoculars: 9, all: 5 }. URL state encoding (url-state.ts) needs the rename handled with a back-compat decode for any 'all' tokens already in the wild.\n\nFiles:\n- src/client/index.html (preset chips + panel structure around line 398)\n- src/client/controls.ts (slider wiring + reverse-sync)\n- src/client/stellata.ts (PRESET_BASE rename, MagPresetName type)\n- src/client/url-state.ts (preset encode/decode + back-compat for 'all')\n- docs/ui-and-controls.md (panel layout + magnitude-preset doc)\n\nRelated: stellata-iuh (Advanced-section reorg of star-size knobs — this bead supersedes it; close iuh once design is locked in here). stellata-1yu (nav-vs-observe contextual rework — orthogonal, can land before or after).\n\nOut of scope: nav/observe contextual presentation (1yu); new telescope-only data fields beyond the preset itself (visual tweaks only).","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-04T08:28:27Z","created_by":"Alex Marshall","updated_at":"2026-05-04T08:28:27Z","dependencies":[{"issue_id":"stellata-v3j","depends_on_id":"stellata-1yu","type":"related","created_at":"2026-05-04T09:28:33Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-v3j","depends_on_id":"stellata-f9b","type":"parent-child","created_at":"2026-05-06T22:19:54Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-v3j","depends_on_id":"stellata-iuh","type":"related","created_at":"2026-05-04T09:28:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-iuh","title":"Move star-size knobs into an Advanced section of the settings panel","description":"Today the right-side settings panel exposes power-user knobs at the top level: star size exaggeration K, sizeMin, sizeMax, sizeSpan (controls.ts:46-48, 178; index.html:398). These are calibration knobs — Alex sets them once to be right in most circumstances, and only someone who really wants to play with the rendering should tweak them. They don't belong alongside everyday controls like the magnitude preset and theme.\n\nDeliverable: collapse star size exaggeration + sizeMin/sizeMax/sizeSpan (and any future star-rendering calibration knobs) behind an 'Advanced' section that is collapsed by default. Reverse-sync (controls.ts:218-221, 259) must continue to work whether the section is open or not. The reset buttons stay in their current places.\n\nThe new uSizeKnee uniform from stellata-a7d.2.7 is debug-panel-only (star-tuning.ts) and does NOT belong in the main panel at all — it's a rendering tuning knob, not user-facing.\n\nOut of scope: the broader nav-vs-observe contextual rework (stellata-1yu). This bead is specifically the basic/advanced split for calibration knobs and can ship independently.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-04T08:01:04Z","created_by":"Alex Marshall","updated_at":"2026-05-04T08:01:04Z","dependencies":[{"issue_id":"stellata-iuh","depends_on_id":"stellata-1yu","type":"related","created_at":"2026-05-04T09:01:09Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-iuh","depends_on_id":"stellata-f9b","type":"parent-child","created_at":"2026-05-06T22:19:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.8","title":"Default-load focus on Sol is non-functional (claims focused but can't zoom past unfocused floor)","description":"When the app loads with no URL params, the UI says Sol is focused, but the user cannot zoom in any closer than GLOBAL_MIN_DIST_PC (the unfocused-floor distance). A truly focused star should allow close-approach down to its per-star physical orbit floor (minOrbitDistForStar — 90% disc fill).\n\nSo either (a) Sol isn't actually focused on default load and the UI label lies, or (b) Sol is focused but some piece of state (TrackballControls.minDistance, the focusedStar guard, etc.) hasn't been updated to reflect the focus and is still applying the unfocused floor.\n\n# Investigation entry points\n\n- src/client/stellata.ts init / boot path — what does the UI use to render \"focused on Sol\"? Is focusedStar actually set to Sol's index on boot?\n- TrackballControls.minDistance — when a star is focused, this should be set to minOrbitDistForStar(focusedStar). On default load, is it being set, or is it stuck at GLOBAL_MIN_DIST_PC?\n- setFocus() vs the boot flow — boot may be skipping setFocus(solIdx) and instead just labelling the UI.\n- url-state.ts:692 — how URL restore handles focus might be inverted from no-URL-params boot.\n\n# Likely fix shape\n\nEither (a) actually call setFocus(solIdx) on no-URL-params boot (which also fixes stellata-a7d.2.4's startup-distance unification — would naturally inherit minDistForStar(solIdx) and minOrbitDistForStar(solIdx)), or (b) wire the UI label to read from focusedStar so it stops claiming focus when there isn't one.\n\nProbably (a) — \"focused on Sol\" is the desired default UX, just needs to actually engage focus state.\n\n# Acceptance\n\n- On clean-slate load: focused on Sol, can zoom in to minOrbitDistForStar(Sol) (~physical orbit floor for a Sol-class star).\n- The pin (uPinFocusToCenter) engages and Sol stays at NDC (0,0) on close approach.\n- UI label and behavior agree.\n\n# Related\n\n- stellata-a7d.2.4 (auto-park unification) — the startup-distance bullet there overlaps with this. Closing this issue likely involves the same code path; coordinate.\n- stellata-a7d.2.1 (centering follow-up) — Sol-on-default-load centering was claimed fixed by 5035edf, but if focus wasn't actually engaging the pin couldn't have been the reason.\n\n# Files\n\n- src/client/stellata.ts (boot path, setFocus, TrackballControls config)\n- src/client/url-state.ts (compare URL-restore vs no-URL boot)","status":"closed","priority":2,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T19:21:44Z","created_by":"Alex Marshall","updated_at":"2026-05-03T20:35:45Z","started_at":"2026-05-03T19:44:55Z","closed_at":"2026-05-03T20:35:45Z","close_reason":"Fixed in commit 0e018af. Two parts:\n\n1. Constructor change: focusedStar = catalog.solIndex (raw assignment) → setFocus(catalog.solIndex). This wires up controls.minDistance to Sol's per-star floor; previously the unfocused GLOBAL_MIN_DIST_PC clamp stayed in place even though the UI claimed focus.\n\n2. setFocus snap (deeper invariant fix): after recenterOrigin runs in setFocus(idx), snap controls.target to (0,0,0) and shift camera by the same delta. Preserves camera-to-target relationship (user-visible pose unchanged) while eliminating any residual on target. This was the actual root cause of the visible drift — Sol's catalog position is (5e-6, 0, 0) pc, not exactly origin, so recenterOrigin's bulk subtract left target at (-5e-6, 0, 0) and the pin guard (target.lengthSq \u003c 1e-12) failed on the first frame. The same pattern protects every other setFocus(idx) call site (focusStar, finishWarp, setOrbitTarget) from analogous Float32-residual breakage.\n\nVerified via temporary debug.pin() HUD: target.x lands at exact 0 on first load and pin engages immediately. User confirmed Sol stays centred under zoom-in.\n\nSide effects of fix:\n- Stellata-a7d.2.4 startup-distance bullet: still NOT fully addressed — camera still parks at (0,0,30) at boot, not minDistForStar(Sol). Focus engagement is now correct, just the boot distance is unchanged. Continue addressing under a7d.2.4.\n- stellata-a7d.2.1 finishWarp residual fix (commit d6c97da) is now redundant given the setFocus snap; left in place as defensive code.\n\nAlso added in commit c9daa88: debug.pin() — a permanent diagnostic HUD with latched extremes for target/camera position. Kept in the codebase for future pin-related reports.","dependencies":[{"issue_id":"stellata-a7d.2.8","depends_on_id":"stellata-a7d.2","type":"parent-child","created_at":"2026-05-03T20:21:43Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.6","title":"Smooth unfocus zoom-out transition (currently teleports to GLOBAL_MIN_DIST_PC)","description":"When close-zoomed on a focused star (e.g. Sol filling ~90% of viewport), unfocusing causes an instant teleport-out to GLOBAL_MIN_DIST_PC (5e-3 pc — see src/client/stellata.ts:170 and the unfocused-distance clamp). Other camera transitions in the app (warp, aim, observe entry/exit) animate over ~2s with easing; the unfocus jump is the outlier and reads as jarring against the rest of the UX.\n\n# Expected\n\nUnfocus from a close-zoomed state should animate the camera distance from its current value to GLOBAL_MIN_DIST_PC over ~2s with the same easing/feel as warp/aim/observe transitions.\n\n# Investigation entry points\n\n- src/client/stellata.ts:170 — GLOBAL_MIN_DIST_PC = 5e-3\n- The unfocus path: search for setFocus(null) and any direct camera/controls writes that snap distance.\n- Existing animated camera transitions to mirror style:\n  - finishWarp / startWarp at stellata.ts:~1735, ~1841 (3-phase warp state machine)\n  - aimAt at stellata.ts:~2004 (animated rotation)\n  - observe-transition (yaw/pitch lerp into observe mode)\n- docs/camera-modes.md describes the warp animation phases — reuse the easing curve / duration constants if defined there.\n\n# Acceptance\n\n- Unfocus from any close-zoomed state animates over ~2s (or matches whatever shared duration constant the other transitions use).\n- Easing matches the warp / aim feel (no linear ramp).\n- No regression: unfocus from already-far distances should be a no-op (no animation if already at floor).\n- Cancellable: starting a new focus / warp during the animation interrupts cleanly without leaving the camera in a partial-transition state.\n\n# Out of scope\n\n- Tuning the floor itself (5e-3 pc) — separate concern.\n- Changing what 'unfocus' means semantically (still: drop focusedStar, return controls to free-fly).\n\n# Files likely touched\n\n- src/client/stellata.ts (unfocus handler + new animation state)\n- maybe docs/camera-modes.md if a new phase/state is added","status":"closed","priority":2,"issue_type":"feature","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T19:06:47Z","created_by":"Alex Marshall","updated_at":"2026-05-05T19:02:07Z","started_at":"2026-05-05T18:28:39Z","closed_at":"2026-05-05T19:02:07Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-a7d.2.6","depends_on_id":"stellata-a7d.2","type":"parent-child","created_at":"2026-05-03T20:06:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.3","title":"Scale-bar UX revisit + camera-to-focus distance indicator","description":"User feedback during a7d.2 validation: \"How do I know how far I am from a star? Does the scale at bottom left show angular distance or the distance the camera is from the focused object? It's actually really confusing.\" Needs design decision + implementation.\n\n# Current state (src/client/scale-bar.ts)\n\n- NAVIGATE mode (lines 31-44): shows physical width-of-space at the camera-target depth (e.g. \"100 px = 0.5 pc at the focused star's depth\"). It's the size of what you're looking at, NOT camera-to-target distance.\n- OBSERVE mode (lines 21-30): shows angular FOV in degrees of sky.\n\nSo \"neither pure angular nor camera-to-object distance\" — it's \"how big is what I'm seeing.\" Useful for \"is this constellation 100 pc or 1000 pc across\" questions but doesn't answer \"how far am I from the focused star.\"\n\n# Options (ask user before implementing)\n\nA) Add separate \"distance to focused {star/cloud}\" text indicator. Keep current scale bar.\nB) Replace navigate-mode scale-bar with camera-to-target distance. Loses \"scale of what I'm seeing\" info.\nC) Both: keep scale bar, add a separate camera-to-focus distance line. Most info-dense; my recommendation in the prior conversation.\n\nUser did not commit to one yet. Default proposal C unless they redirect.\n\n# Implementation hints (option C)\n\n- Could repurpose distance-vector-overlay's label, which already shows distance to a vector destination — extend to show camera-to-focused-star when no vector is set but a focus is.\n- Or add a small text element next to scale-bar in the bottom-left — fmtDist(camera.position.distanceTo(controls.target)) updated each frame.\n- Use existing fmtDist (src/client/distance-util.ts) for unit-consistent formatting (pc/ly toggle).\n\n# Acceptance\n\n- User can read camera-to-focus distance at a glance in navigate mode.\n- Scale bar still useful for \"size of what I'm seeing.\"\n- No regression in observe (degrees-of-sky reading).\n- Updates each frame; debounced for stability if numbers flicker.\n\n# Branch\n\nworktree-a7d.2-physical-disc-geometry, PR #22.","status":"closed","priority":2,"issue_type":"feature","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T18:55:04Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:28:46Z","started_at":"2026-05-06T19:34:22Z","closed_at":"2026-05-06T21:28:46Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-a7d.2.3","depends_on_id":"stellata-a7d.2","type":"parent-child","created_at":"2026-05-03T19:55:03Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-4oa","title":"Auto-deploy to Cloudflare on version-bump merges to main","description":"Add GitHub Actions workflow that, on push to main, detects whether package.json#version changed vs HEAD~1 and if so: builds (catalog + clouds + dust-sync + client), deploys via wrangler, tags v\u003cversion\u003e, and creates a GitHub release with auto-generated notes. Symmetric with version-guard.yml on PRs and folds in the manual RELEASING.md steps. Skip-version-bump label applied to this PR since CI workflow files don't ship runtime code.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T17:53:18Z","created_by":"Alex Marshall","updated_at":"2026-05-03T18:07:20Z","started_at":"2026-05-03T17:53:23Z","closed_at":"2026-05-03T18:07:20Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-byn","title":"Rename version-guard job key to match workflow name","description":"Branch protection's required-check rule `version-guard` never matched because the reported check name was `check` (the job key under `jobs:`), displayed as `version-guard / check` in the merge box. BP matches the job name, not the slash-display string. Renaming the job key to `version-guard` (mirroring `test.yml`'s self-named pattern) makes the reported check just `version-guard`, restoring the user's original BP rule.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T09:56:28Z","created_by":"Alex Marshall","updated_at":"2026-05-03T09:57:13Z","started_at":"2026-05-03T09:56:33Z","closed_at":"2026-05-03T09:57:13Z","close_reason":"PR #20 opened with the rename","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dx7","title":"61 Cyg: disc-edge falloff blends unnaturally where two star discs overlap","description":"Visual artifact observed in 61 Cyg A/B (and likely all close binaries with similar-sized discs and tight on-screen separation, e.g. α Cen) when the two discs are close enough on screen that their soft super-Gaussian edges intersect.\n\n# What you see\n\nUser-reported in 61 Cyg with two screenshots:\n- View 1: the smaller (B, K7V, absmag ≈ 8.33) and larger (A, K5V, absmag ≈ 7.49) discs partially overlap. Where their edges meet, the boundary forms a sharp lens-shaped intersection that reads as a seam rather than a natural blend.\n- View 2: the discs are nearly tangent. Where their fuzzy outer falloffs touch, the *junction* brightens unnaturally — the additive contribution of two falloffs in the same screen pixel exceeds either source individually, producing a visible bright cusp at the contact point.\n\n# What's happening (working theory)\n\nThe disc pass uses the unified super-Gaussian profile (see docs/rendering.md). Two overlapping disc instances both write to the same screen pixel, and the blend mode for the disc pass is *additive* on the falloff edge — the depth-mask core is opaque, but the outer ring contributes additively, so two falloffs sum.\n\nFor widely-separated stars this is invisible (no shared pixels). For tight binaries imaged near minimum-distance the falloffs overlap and sum.\n\n# Fix directions to consider (not for this ticket)\n\n- Switch the disc-edge blend from additive to a max() / replace, so two overlapping falloffs don't sum.\n- Per-system depth ordering: opaque disc of the nearer star occludes the falloff of the farther star.\n- Re-tune the super-Gaussian outer falloff so the blend region is sharper (less overlap zone in screen pixels).\n\n# Repro\n\nFocus 61 Cyg A in navigate mode, zoom toward minimum distance until both discs are visible side-by-side. Compare to a single isolated star (e.g. Sol) at the same fill-fraction — the edge does not look this way.","status":"closed","priority":2,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-03T08:35:07Z","created_by":"Alex Marshall","updated_at":"2026-05-03T20:05:56Z","started_at":"2026-05-03T19:55:35Z","closed_at":"2026-05-03T20:05:56Z","close_reason":"Fixed in PR #25 — switched disc-pass blend equation to MaxEquation so overlapping fragments resolve per-channel max instead of summing premul-alpha. Eliminates the core depth-test seam, the halo cusp at near-tangency, and the same additive lift over the Milky Way.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-6oy","title":"FilterState patch type-safety hole at url-state.ts:659","description":"Surfaced in code review (stellata-y84).\n\nsrc/client/url-state.ts:659 — the `patch` object passed to `setFilter` is typed as `Record\u003cstring, number | boolean\u003e`. This defeats `FilterState`'s field-name type checking: typos in keys like `'minDistSol'` or `'sizeMinOverridden'` would compile silently.\n\nFix: Declare `const patch: Partial\u003cFilterState\u003e = {};` directly. All keys assigned are already `keyof FilterState`, so the Partial form will catch typos at compile time.\n\nRelated (smaller): src/client/main.ts:74 casts `(window as unknown as { stellata: Stellata }).stellata = stellata;` — better to declare the global in src/client/globals.d.ts and assign without the cast.\n\nSeverity: HIGH (real type-safety hole, single-file fix).\n","status":"closed","priority":2,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:33:08Z","created_by":"Alex Marshall","updated_at":"2026-05-03T10:44:30Z","started_at":"2026-05-03T10:36:17Z","closed_at":"2026-05-03T10:44:30Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-6oy","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:33:07Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a0n","title":"url-state: drop per-frame toFixed×9 string hash for change detection","description":"Surfaced in code review (stellata-y84).\n\nsrc/client/url-state.ts:781-810 — `startUrlSync` `onFrame` callback constructs a 9-component string-formatted hash every frame via `toFixed(3) × 9` plus template literal. That is 10+ string allocations per frame just to detect change in the steady idle state.\n\nFix: Replace with component-wise epsilon comparison against scratch Float64s for cam/tgt/up. Schedule the URL update only on first non-equal check. No allocations on the no-change path.\n\nSeverity: HIGH (steady-state per-frame allocation, easy fix).\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:33:07Z","created_by":"Alex Marshall","updated_at":"2026-05-07T09:25:01Z","started_at":"2026-05-07T09:17:21Z","closed_at":"2026-05-07T09:25:01Z","close_reason":"Landed in PR #35 (v1.1.4): per-frame toFixed×9 hash replaced with Float64Array scratch + component-wise epsilon comparison. EPS=1e-3 matches prior toFixed(3) precision so threshold unchanged. Smoke pass per Alex 2026-05-07.","labels":["perf"],"dependencies":[{"issue_id":"stellata-a0n","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:33:06Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-20w","title":"pickStar 313k-star linear scan: prefilter window + hoist constants","description":"Surfaced in code review (stellata-y84).\n\nsrc/client/stellata.ts:2133-2178 — `pickStar` linearly scans all 313k stars on every hover (after 280ms throttle) and every click. For each star, it calls `renderedSizePx` (line 2237-2297) which re-reads ~10 uniforms and recomputes log/sqrt/pow per star.\n\nTwo improvements:\n\n1. **Bound the scan** — the existing `sortedDistFromSol` prefilter used by the core-mask gate can window the scan: binary-search the slice `[f.minDistSol, f.maxDistSol]` and iterate only stars in that range.\n\n2. **Hoist constants out of the loop** — `renderedSizePx` reads uniforms inside; in `pickStar`'s loop these are constant per call. Inline the disc-size formula with hoisted uniform reads, or pass an \"ambient state\" struct in.\n\nAlso flagged: `renderedSizePx` and `minDistForStar` (lines 2329-2350) duplicate the `physMinPx + logRatio*(physMaxPx-physMinPx)` math. The companion-separation block at lines 2306-2316 and 2342-2350 is also duplicated. Consider a private `_physSizeAtRef(idx)` helper.\n\nSeverity: HIGH (hover throttle masks the cost but click latency is user-visible).\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:33:06Z","created_by":"Alex Marshall","updated_at":"2026-05-07T09:33:31Z","started_at":"2026-05-07T09:25:02Z","closed_at":"2026-05-07T09:33:31Z","close_reason":"Landed in PR #35 (v1.1.4): pickStar windows the 313k scan via binary-search into sortedDistFromSol, dropping the per-star distSol sqrt + bounds check. Stale parts of the bead (physMin/physMax DRY, renderedSizePx uniform hoist) noted in commit message. Smoke pass per Alex 2026-05-07.","labels":["perf"],"dependencies":[{"issue_id":"stellata-20w","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:33:06Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-63f","title":"stellata.ts hot-path Vector3 allocations: adopt xxxInto(idx, out) pattern","description":"Surfaced in code review (stellata-y84).\n\nstellata.ts: several helpers return a fresh `THREE.Vector3` and are called from per-frame paths:\n\n- `starLocalPosition(idx)` (line 2093-2096) — called by overlays per frame, by HUD, by `pickStar`'s 313k loop, by `updateGalacticLayers` (line 2722), by `updateWarp` (lines 2784, 2802)\n- `cloudLocalPosition(idx)` — same problem in cloud-warp paths\n- `currentFocusLocalPos()` (line 1689-1696) — uses `.clone()`\n\nEach call allocates a new Vector3. In steady-state this is 5-10 GC-eligible allocations per frame.\n\nFix: Add `xxxInto(idx, out: THREE.Vector3): THREE.Vector3` variants that write into a caller-provided scratch vector. Migrate hot-path callers (`animate()`, `updateGalacticLayers`, `updateWarp`, `pickStar`'s loop, HUD update). Either remove the allocating shims or keep them only for cold paths (initial focus selection, click handlers).\n\nSeverity: HIGH (steady-state GC pressure on the per-frame path).\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:33:05Z","created_by":"Alex Marshall","updated_at":"2026-05-07T09:48:57Z","started_at":"2026-05-07T09:36:21Z","closed_at":"2026-05-07T09:48:57Z","close_reason":"Landed in PR #35 (v1.1.4): added starLocalPositionInto / cloudLocalPositionInto non-allocating helpers; per-frame internal callers (animate HUD, updateWarp fly + post-arrival, getWarpInfo, renderedCloudSizePx) and external per-frame callers (chart-labels per-cloud loop, distance-vector-overlay, scale-bar) migrated onto scratch APIs. Cold paths keep the allocating shims, which now delegate to the *Into variants. Smoke pass per Alex 2026-05-07 (focus/HUD, warp end-to-end, chart labels, distance-vector cloud endpoints with Aquila Rift + Coalsack).","labels":["perf"],"dependencies":[{"issue_id":"stellata-63f","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:33:05Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3s2","title":"unit-toggle: hidden module-level singleton + redundant applyUnit shim","description":"Surfaced in code review (stellata-y84).\n\nsrc/client/unit-toggle.ts has two issues:\n\n1. **Hidden module-level singleton**: line 3 caches `buttons` at module scope. `bindUnitToggle` populates it. A second call to `bindUnitToggle` would silently overwrite the cache and abandon the previously-attached listeners — causing them to fire on the wrong button references. Currently called only once, so the bug is latent, but the API doesn't enforce single-call.\n\nFix: Capture `buttons` in a closure inside `bindUnitToggle`; pass it explicitly to `syncButtons`.\n\n2. **`applyUnit` shim** (line 15): one-line wrapper around `setUnit`. It IS used by `url-state.ts:655`, so it cannot be deleted, but it's a redundant indirection.\n\nFix: Inline `applyUnit` callers to call `setUnit` directly, then delete `applyUnit`.\n\nSeverity: HIGH (hidden singleton is a real bug-in-waiting; wrapper is style).\n","status":"closed","priority":2,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:32:08Z","created_by":"Alex Marshall","updated_at":"2026-05-03T10:44:29Z","started_at":"2026-05-03T10:36:16Z","closed_at":"2026-05-03T10:44:29Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-3s2","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:32:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-470","title":"observe-controls: missing pointercancel handler causes stuck drag state","description":"Surfaced in code review (stellata-y84).\n\nsrc/client/observe-controls.ts handles `pointerdown`/`pointermove`/`pointerup` for look-around drag. It is missing a `pointercancel` handler. If the OS preempts the pointer mid-drag (touch interrupted by a phone call, system gesture stealing the pointer, browser cancelling the touch), `dragging` stays `true` and the next `pointermove` continues a phantom drag — sometimes with momentum applied.\n\nFix: Add a `pointercancel` listener that mirrors `onPointerUp` (probably without the momentum-promotion path — a cancelled drag shouldn't feel like the user released).\n\nSeverity: HIGH (real UX bug, hits mobile users specifically).\n","status":"closed","priority":2,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:32:07Z","created_by":"Alex Marshall","updated_at":"2026-05-03T10:01:32Z","started_at":"2026-05-03T09:38:43Z","closed_at":"2026-05-03T10:01:32Z","close_reason":"merged in PR #19","dependencies":[{"issue_id":"stellata-470","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:32:07Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-mu9","title":"MolecularClouds.setIsobar: uniform reference mutation + missing blending restore","description":"Surfaced in code review (stellata-y84).\n\nmolecular-clouds.ts:117-129 (`setIsobar`):\n\n1. `mat.uniforms.uMaxAppMag = magnitudeUniform` — replaces the `{value:...}` reference every call. If a caller passes a different shared uniform on a subsequent invocation, prior bindings are silently abandoned. Three.js convention is to mutate `.value`, not the wrapper object.\n2. When `on === false`, `mat.blending` isn't restored. Clouds remain in their isobar blending mode after the toggle off, until `setMonochrome` is called again (which today only happens on theme change).\n\nNote: the molecular-cloud overlay is shelved for v1 (CLAUDE.md), so this is latent — it will bite when the layer is re-enabled.\n\nFix:\n- Set the uniform reference once at construction (or first call) and afterwards only mutate `.value`.\n- Restore `mat.blending` to the appropriate non-isobar mode when `on === false`.\n\nSeverity: HIGH (latent bug — would manifest on chart-mode toggle once clouds are re-enabled).\n","status":"closed","priority":2,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:32:07Z","created_by":"Alex Marshall","updated_at":"2026-05-03T10:44:30Z","started_at":"2026-05-03T10:36:17Z","closed_at":"2026-05-03T10:44:30Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-mu9","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:32:06Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-adr","title":"dust-loader: validate buffer size + handle WebGL context loss","description":"Surfaced in code review (stellata-y84).\n\n**dust-loader.ts:73-81** — When parsing a `particles.bin` file, the count check at line 73 only warns; a length mismatch causes either a Float32Array constructor throw (\"byte length should be a multiple of 4\") or an out-of-bounds view. The header `count` is taken from the `manifest.json` but the actual file size isn't validated.\n\nFix: After parsing the header `count`, assert `ab.byteLength === 16 + count * 16` and bail out (return null) on mismatch, instead of using `meta.count` as the source of truth for the allocation size.\n\n**dust-loader.ts:226-258** — `uploadChunk` reaches into `renderer.properties.get(tex).__webglTexture` (undocumented Three.js private API). There is no fallback on context loss; the code logs and silently drops the chunk, leaving permanent holes in the dust grid.\n\nFix: Add `webglcontextlost` / `webglcontextrestored` listeners to the canvas; on restore, re-upload all already-loaded chunks from cached source data. Or at minimum surface the failure prominently.\n\nSeverity: HIGH (correctness on edge cases — bad files, context loss).\n","status":"closed","priority":2,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:32:06Z","created_by":"Alex Marshall","updated_at":"2026-05-03T10:44:30Z","started_at":"2026-05-03T10:36:18Z","closed_at":"2026-05-03T10:44:30Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-adr","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:32:05Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-38w","title":"First test bug","description":"**Type**: bug\n**Priority**: 1\nBody text 1.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-02T19:30:43Z","updated_at":"2026-05-02T19:30:47Z","closed_at":"2026-05-02T19:30:47Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-4pu","title":"Second test feature","description":"**Type**: feature\n**Priority**: 0\nBody text 2.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-02T19:30:43Z","updated_at":"2026-05-02T19:30:47Z","closed_at":"2026-05-02T19:30:47Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-84j","title":"Test issue alpha","description":"Type: task\nPriority: 2\nDescription body for alpha goes here.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-02T19:30:27Z","updated_at":"2026-05-02T19:30:35Z","closed_at":"2026-05-02T19:30:35Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-lny","title":"Test issue beta","description":"Type: bug\nPriority: 1\nDescription body for beta goes here.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-02T19:30:27Z","updated_at":"2026-05-02T19:30:35Z","closed_at":"2026-05-02T19:30:35Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a8w","title":"observe→navigate exit: camera roll snap proportional to look-around in observe","description":"When exiting OBSERVE → navigate after rotating the camera (yaw/pitch via drag) inside observe, there is a visible camera jump at the end of the exit lerp. The magnitude of the jump scales with how much the camera was rotated in observe mode.\n\n# Likely root cause (from a quick code read — confirm during fix)\n\n`stellata.ts:startObserveExit` (~line 990) translates the camera backward along its current forward vector by `minDist` — preserving the eye→target axis (target = star, at fromPos). `finishObserveTransition` then runs `this.controls.target.copy(state.fromPos); this.controls.update();`.\n\nThe translation is correct (camera and target are collinear with the rotated look direction, so lookAt's rotation about the eye→target axis would be a no-op). What jumps is **roll**: TrackballControls.update() runs lookAt(target), which uses `camera.up` to resolve the roll axis. The observe look-around controller pitches the camera quaternion without updating `camera.up`, so when lookAt re-aligns to `camera.up = (0,1,0)` it removes the accumulated pitch's tilt as a roll snap.\n\nReproduces on the deployed build — pre-exists the a7d.1 log-depth-buffer change.\n\n# Suspected fix surfaces\n\n- `src/client/observe-controls.ts` — keep `camera.up` consistent with the look quaternion as the user pitches; or\n- `src/client/stellata.ts:startObserveExit` / `finishObserveTransition` — set `camera.up` from the current quaternion before re-enabling controls so lookAt is roll-stable.\n\n# Verification\n\n- Focus a star, enter observe, drag yaw + pitch noticeably, exit observe via the mode toggle. The end-of-lerp frame should match the previous frame's roll exactly — no snap.\n- Repeat with the search-X clear path (`clearFocusOnExit=true`) and the no-animate hard-switch path (no focus).\n- `npm test` + `npm run typecheck` clean.","status":"closed","priority":2,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:04:00Z","created_by":"Alex Marshall","updated_at":"2026-05-03T10:44:30Z","started_at":"2026-05-03T10:36:18Z","closed_at":"2026-05-03T10:44:30Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.5","title":"Heliopause shell + label (drop scale rings — orbit rings cover the spatial-scale role)","description":"**Goal:** render the heliopause as an asymmetric wireframe shell with a single text label at the upwind apex. Provides spatial context for the inner solar-system view.\n\n### Scope reduction (2026-05-07)\n\nThe original scope included faint reference rings at 1 AU and 50 AU. **Dropped per Alex's UX call** — the per-planet orbit rings from stellata-3re.7 already give the user a clear sense of solar-system scale (Earth's orbit is the 1 AU reference; Neptune's is at 30 AU). Adding more rings + labels would clutter the view without informational gain.\n\n### Heliopause (kept)\n\nAsymmetric ellipsoid, best-known shape per IBEX observations + Voyager 1/2 crossing data:\n\n- **Apex (upwind, toward solar apex):** ~122 AU. Voyager 1 crossed at ~121 AU on 2012-08-25.\n- **Flanks (perpendicular to apex direction):** ~115 AU. Voyager 2 crossed at ~119 AU on 2018-11-05 at ~67° from apex.\n- **Heliotail (downwind):** ~200 AU. Recent IBEX/Cassini ENA data + global heliospheric models suggest 200–350 AU; the tail is the least-constrained direction. 200 AU is a defensible mid-estimate.\n\nApex direction: solar apex of motion through the local interstellar medium. ICRS approx **RA 17h53m, Dec +27.4°** (Frisch \u0026 Slavin 2013 LISM inflow direction).\n\n### Implementation as a clean ellipsoid + offset\n\n- Total length along apex–tail axis = 122 + 200 = 322 AU.\n- Major semi-axis (along apex–tail) = 161 AU.\n- Equatorial semi-axes (in flank plane) = 115 AU.\n- Centre of ellipsoid offset from Sol by (200 − 122)/2 = 39 AU toward the heliotail (anti-apex).\n\nConstruction: `SphereGeometry(1, ...)` → wireframe → non-uniform scale `(115, 115, 161)` along its local axes → orient so local +Z is the anti-apex direction → translate by 39 AU along that direction. Convert AU → pc.\n\n### Render style: wireframe mesh\n\nSparse latitude/longitude lines via `WireframeGeometry(SphereGeometry(...))` with low segment counts (~16x8). Very dim colour (matches existing dim chrome). Reads as 'boundary' rather than 'medium'. No backface culling concerns; both inside-out and outside-in viewing should look clean.\n\n### Label\n\nText label `Heliopause` anchored to the upwind apex point, projected to screen via the existing SVG-overlay pattern (`planet-labels.ts` style). Same dim styling as other static labels.\n\n### Visibility\n\nActive only when solar-system layer is active (focused star = Sol per stellata-3re.6). Hidden in chart mode (matches the rest of the layer's contract).\n\n### Files\n\n- New: `src/client/heliopause.ts` (mesh + label).\n- Touches: `stellata.ts` (instantiate + visibility gate), `index.html` + `styles.css` (label container + styling).\n\n### Independent of `t`\n\nHeliopause is static geometry — no time dependence on human timescales.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:07:35Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:22:24Z","started_at":"2026-05-07T12:46:50Z","closed_at":"2026-05-08T10:22:24Z","close_reason":"Closed","labels":["phase-12a","solar-system","visual"],"dependencies":[{"issue_id":"stellata-3re.5","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-02T19:07:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.4","title":"Sol planet bodies: correct diameters, type-aware disc edge, representative colour","description":"**Goal:** render Sol's 8 planet bodies as physically-sized discs with correct diameters, type-appropriate appearance (gas giant vs solid, Earth's atmosphere), and representative colour.\n\n**Important:** orbital positions are placeholders in this task. Real VSOP87 positions backfill via stellata-3re.3 — the two are decoupled. This means a planet might initially render at any reasonable position around its orbit (e.g. at +x AU on its semi-major axis); 3re.3 lands later and replaces the placeholder with `getPlanetPositions(t)`.\n\n### Disc sizing\n\nUse `θ = 2·atan(R/d)` from stellata-a7d.2 (already merged). Each planet has a known equatorial radius in AU; convert to parsecs:\n\n| Planet | R (km) | R (pc) |\n|---|---|---|\n| Mercury | 2,440 | 7.91e-11 |\n| Venus | 6,052 | 1.96e-10 |\n| Earth | 6,371 | 2.06e-10 |\n| Mars | 3,390 | 1.10e-10 |\n| Jupiter | 69,911 | 2.27e-9 |\n| Saturn | 58,232 | 1.89e-9 |\n| Uranus | 25,362 | 8.22e-10 |\n| Neptune | 24,622 | 7.98e-10 |\n\nSame `max(appSize, physSize)` floor as stars so planets stay visible at typical zoom levels even when truly sub-pixel.\n\n### Type and appearance\n\nEach planet record (in the `PlanetSystem` from stellata-3re.6) carries:\n- `type`: `\"rocky\"` | `\"gas_giant\"` | `\"ice_giant\"` (Sol-internal categorisation; bk5 collapses ice/gas in its own enum)\n- `colour`: representative RGB sourced from observed average tones (Mars rust, Jupiter banded yellow, Neptune blue, etc.). Single colour per planet — no banding / atmospheric scattering yet.\n- `hasAtmosphere`: boolean. When true, the disc gets a faint additive halo (Earth, Venus, the giants).\n- `solidity`: subtle shader difference between solid bodies and gas giants — gas giants get softer disc edges (super-Gaussian profile with lower exponent), solid bodies get hard-edged discs.\n\n### Render gate\n\nOnly when focused star has planets per stellata-3re.6. Visible only when solar-system layer is active.\n\n### Implementation\n\n- New rendering module `src/client/solar-system.ts` (or extend if 3re.7 lands first).\n- New shader pair, or extend the existing star shaders if instance attributes can encode the difference cleanly.\n- Reads `PlanetSystem` from 3re.6.\n- Reads positions from `getPlanetPositions(t)` once 3re.3 lands; until then, hardcoded placeholder positions per planet.\n\n### Out of scope\n\n- Lighting. v1 uses unlit / self-illuminated discs — planets are bright enough at the framing distances that real shadowing is not the visual story. Defer until needed.\n- Surface texturing. Decided in stellata-3re.10 (texture research). v1 ships representative-colour discs unless 3re.10 says otherwise.\n- Ring systems (Saturn, faint rings of the other giants). Defer.\n- Moons.\n\n### Depends on\n\n- `stellata-3re.6` (the `PlanetSystem` data contract).\n- `stellata-a7d.2` ✓ (true angular geometry — already merged).\n\n### No longer depends on\n\n- `stellata-3re.3` (positions). Decoupled — 3re.3 backfills real positions later.\n","notes":"Per stellata-3re.9 design (2026-05-07): planets render with always-on body-anchored labels. Name only. No hover, no click action in v1. Hidden in chart mode (m40.3 handles chart-mode planet labels separately). Per-planet labels also hide when their associated orbit ring is suppressed by the pixel-gap heuristic — keeps labels in lockstep with rings; bodies stay rendered at the pixel-size floor regardless.\n\nAtmosphere halo + Earth-specific shading DEFERRED to stellata-2f6 (planet-as-object: select / focus / zoom) on 2026-05-07. Reason: at any reasonable host-relative viewing distance, planet bodies project below the pixel-size floor and clamp to a 2 px disc — Jupiter and Mercury are visually indistinguishable. Atmospheric haloes / banding / scattering / texture maps all depend on a close-zoom affordance the renderer doesn't currently support, so the v1 scope is the disc + colour + type-aware edge only. The full Planet schema dropped its hasAtmosphere field as part of this scope cut; restore it (and add per-planet texture / banding / scattering fields) when 2f6's design gate clears.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:07:34Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:22:33Z","started_at":"2026-05-07T10:41:06Z","closed_at":"2026-05-08T10:22:33Z","close_reason":"Closed","labels":["phase-12a","solar-system","visual"],"dependencies":[{"issue_id":"stellata-3re.4","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-02T19:07:34Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.4","depends_on_id":"stellata-3re.6","type":"blocks","created_at":"2026-05-07T09:55:08Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.4","depends_on_id":"stellata-a7d.2","type":"blocks","created_at":"2026-05-02T19:08:01Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-3re.3","title":"JPL Standish planet positions (ecliptic, per-frame caching)","description":"**Goal:** compute heliocentric ICRS positions for the 8 planets at any `t`. No network calls; fully offline; no new npm dependencies.\n\n### Implementation: JPL Standish 1992 mean-elements + Kepler\n\nPer-planet J2000 mean orbital elements (a, e, i, Ω, ω, M0) plus per-century linear secular rates, with cubic Jupiter–Neptune correction terms (b, c, s, f). Designed validity ±3000 years from J2000 to ~arcminute accuracy — well below the angular size of a billboarded planet disc at any zoom we render at.\n\nBeyond ±3000 years the model degrades; see stellata-1gh for the deep-time research follow-up.\n\n### Per-frame (when solar-system layer is visible)\n\n1. `T = (tToJDE(t) - 2451545.0) / 36525` — centuries since J2000.\n2. For each planet, compute current elements: `a(T) = a0 + ȧ·T`, etc.\n3. Mean anomaly `M = L − ϖ` (modulo 2π); for Jupiter–Neptune apply the cubic Standish correction.\n4. Solve Kepler's equation `M = E − e·sin(E)` iteratively for eccentric anomaly E (Newton's method, 5 iterations is plenty).\n5. In-orbit cartesian: `x = a·(cos E − e)`, `y = a·√(1 − e²)·sin E`, `z = 0`.\n6. Rotate by ω (in-plane), i (inclination), Ω (longitude of node) → heliocentric ecliptic cartesian.\n7. AU → parsecs (`/ 206264.806`).\n8. Caller (orbital plane orientation in star-system.ts) rotates ecliptic → ICRS by applying the J2000 obliquity around +x.\n\n### Caching\n\nPer-frame results cached, recomputed only when `t` changes meaningfully (e.g. rounded to 60-second buckets). At billboarded-disc scale, sub-minute time resolution is invisible. Time scrubber (stellata-nmu) updates `t` in user-facing increments that always invalidate the cache.\n\n### Output\n\nAPI `getPlanetPositions(t): { mercury: Vec3, venus: Vec3, …, neptune: Vec3 }` in **heliocentric ecliptic** parsecs (not ICRS — the orbital-plane-orientation quaternion in star-system.ts handles the ecliptic→ICRS rotation downstream, mirroring the per-host plane dispatch from stellata-3re.8).\n\nConsumed by stellata-3re.4 (planet body rendering) — backfills the placeholder eccentric-anomaly positions currently shipped.\n\n### Position of this task in the epic order\n\nLands **after** stellata-3re.4 (planet bodies) per the order Alex set on 2026-05-07. The rendering ships first with placeholder positions; this task backfills real ephemerides.\n\n### Depends on\n\n- `stellata-3re.1` for `t` + JDE conversion.\n\n### Why JPL Standish over VSOP87 / astronomia\n\nVSOP87 (sub-arcsec accuracy ±4000 years) is overkill for billboarded planet discs. The astronomia npm package would add ~500 KB gzipped to the bundle for accuracy that's invisible at the rendering scale. JPL Standish 1992 is ~50 lines + an 8-row element table, no dep, accuracy floor ~arcminute over ±3000 years — already sub-pixel for any zoom. See stellata-1gh for the deep-time follow-up if scrubbing past ±3000 years is ever in scope.","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:07:33Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:22:24Z","started_at":"2026-05-07T11:43:40Z","closed_at":"2026-05-08T10:22:24Z","close_reason":"Closed","labels":["phase-12a","physics","solar-system"],"dependencies":[{"issue_id":"stellata-3re.3","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-02T19:07:33Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.3","depends_on_id":"stellata-3re.1","type":"blocks","created_at":"2026-05-02T19:08:00Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.1","title":"The 't' time variable: state, URL semantics, JD conversion","description":"**Goal:** introduce the wall-clock time variable `t` (Unix-seconds, double precision) that drives planet positions. Foundational for the rest of the solar-system layer.\n\n### State\n\n- Lives on the stellata filter state alongside other filter fields.\n- Default at startup: `Date.now() / 1000`.\n- v1 keeps `t` pinned to \"now\" (re-evaluated each frame, or once per second) — no scrubbing yet.\n- Public setter exists from day one so the time-scrubber epic (stellata-nmu) can plug in without API changes.\n\n### URL semantics\n\nv1: `t` is never written to the URL (always \"now\"). The URL writer code path in `url-state.ts` should be wired but pass-through, so the time-scrubber epic just enables it.\n\nEventual contract (deferred to stellata-nmu):\n- URL state writes `t=\u003cunix\u003e` only when the user has scrubbed away from \"now\".\n- Absence in URL = \"live, defaults to current wall-clock at load time.\"\n- Shared link with `t=` preserves the sender's chosen date; without `t=` resolves to receiver's local-now.\n\n### Conversion utilities\n\n- `tToJDE(t: number): number` → `(t / 86400) + 2440587.5` (Unix-seconds → Julian Date in the TDB scale, accurate enough for VSOP87).\n- \"Live\" detection helper: `isLive(t)` returns true when `|t - Date.now() / 1000|` is below a small tolerance — used by stellata-3re.11's readout to label \"Live\" vs an explicit timestamp.\n\n### Out of scope\n\n- The scrubber UI (`stellata-3re.2`, now in stellata-nmu).\n- The static readout (`stellata-3re.11`).\n- Variable-star animation continues using its independent `uTime` cosmetic clock — `t` and `uTime` are orthogonal.\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:07:30Z","created_by":"Alex Marshall","updated_at":"2026-05-08T10:22:23Z","started_at":"2026-05-07T11:23:59Z","closed_at":"2026-05-08T10:22:23Z","close_reason":"Closed","labels":["phase-12a","solar-system","time"],"dependencies":[{"issue_id":"stellata-3re.1","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-02T19:07:30Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"stellata-9hp.1","title":"Unified nebulae data ingest pipeline (PNe + RNe + H II + SNR)","description":"**Goal:** produce `public/nebulae.json` from the four catalogues, filtered to within ~3 kpc to match the dust volume, with consistent per-nebula schema.\n\n**Sources** (full details in parent `stellata-9hp`):\n- Strasbourg-ESO PNe (Acker et al., VizieR V/84) — ~1,500 PNe with multiple distance estimates\n- Magakian RNe (J/A+A/399/141) — 913 reflection nebulae, illuminator cross-match needed\n- Sharpless H II (VII/20) — 313 entries, pair with Avedisova (V/115) for distances\n- SIMBAD TAP query as the unified fallback for object types PN/RNe/HII/SNR\n\n**Per-nebula schema:** ID, common name, type (PN|RNe|HII|SNR), RA/Dec, distance, → xyz, angular size → physical radius, illuminating star ref (RNe only).\n\n**Output:** new `scripts/build-nebulae.py` (mirroring `build-clouds.py` pattern) emitting `public/nebulae.json`. Add to `.gitignore`. Wire into `dev` and `build` scripts.\n\n**Watch out for:**\n- Drop entries with no parsed distance\n- Filter to ATHYG-relevant volume; \u003e5 kpc clutters search without rendering usefully\n- RNe illuminators: parse HD/HIP from source, leave the cross-match to `stellata-9hp.4`","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:06:01Z","created_by":"Alex Marshall","updated_at":"2026-05-02T18:06:01Z","labels":["ism","nebulae","phase-3b"],"dependencies":[{"issue_id":"stellata-9hp.1","depends_on_id":"stellata-9hp","type":"parent-child","created_at":"2026-05-02T19:06:01Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-c7u.1","title":"A.1 Research + design doc: physics model for molecular cloud rendering","description":"**Goal:** produce the implementation design before any rendering code is written. Output is a written model in `docs/molecular-clouds.md` (replacing the current shelved-state blurb) that drives phases A.2–A.7.\n\n**Research scope:**\n- **Extinction physics**: τ = ∫κ·ρ dl. Map Zucker bbox + assumed density to optical depth at V band. Calibrate κ × ρ_total so a typical dense cloud (e.g. Taurus core) produces realistic Av (~5–25 mag). Sanity-check against published Av values for known clouds.\n- **Selective extinction (reddening)**: confirm RV ≈ 3.1 for diffuse ISM; note RV ≈ 5.5 for dense cloud cores. Cardelli-Clayton-Mathis (1989) parameterisation. Decide which RV to use (single value, or per-cloud-type, or per-cloud).\n- **Density structure**: log-normal PDF (Vazquez-Semadeni 1994; Padoan, Nordlund \u0026 Jones 1997) + power-law tail at high densities (Federrath \u0026 Klessen 2013). Mach-number dependence. Filamentary substructure (Hi-GAL, Herschel filament catalogues).\n- **Cloud taxonomy**: dark cloud vs GMC vs SF region. Which Zucker entries fall in each class? Use known classifications (Taurus dark-cloud, Orion A active SF, etc.) where available; default behaviour for unclassified.\n- **Embedded young stars + cavities**: Strömgren-sphere radius for an O/B star in a cold MC, scaling with stellar luminosity and ambient density. Stellar-wind bubbles. How sharp is the cavity edge?\n- **HII regions / reflection nebulosity**: when does an embedded star produce visible emission/scattering? Threshold spectral type / luminosity.\n- **Inside-cloud experience**: what does a real observer at the centre of Taurus see? Reddened/dimmed background, very dark ambient — confirm this is the correct mental model.\n\n**Design output (written into `docs/molecular-clouds.md`):**\n- The per-cloud parameter set we'll bake at build time (centroid, semi-axes, peak density, density-profile-shape, embedded-star list, cavity radii).\n- The shader-side model for the substructure noise (octaves, frequency, amplitude).\n- The reddening model (B vs R extinction ratio).\n- The integration strategy with the Edenhofer voxel field (replace, blend, or augment).\n- Decision on whether the shelved `cloud.frag.glsl` warm-glow becomes A.6 or is replaced.\n- Concrete equations, constants, and source citations.\n\n**No code in this phase** — but should produce enough specificity that A.2–A.7 implementers can work without ambiguity.","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:36:52Z","created_by":"Alex Marshall","updated_at":"2026-05-02T17:36:52Z","labels":["ism","molecular-clouds","research"],"dependencies":[{"issue_id":"stellata-c7u.1","depends_on_id":"stellata-c7u","type":"parent-child","created_at":"2026-05-02T18:36:51Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-c7u","title":"Physical molecular cloud rendering — extinction, reddening, density, embedded-star cavities","description":"**Goal:** render the ~96 Zucker MCs with physically-grounded extinction, reddening, density structure, and cloud-type variation — the experience of actually flying through these clouds, to the best of current scientific knowledge. Plus a *very* subtle stylised \"presence\" so an empty-sky background still hints at cloud shape.\n\n**Current state:** `cloud.frag.glsl` is decorative — `pow(|n·v|, 1.5)` × warm colour, additive blend. No extinction, no reddening, no real density. The layer is currently shelved (`FilterState.showMolecularClouds = false`). Re-enables once this epic lands.\n\n**Decisions captured (from scoping discussion):**\n\n1. **Realism-inspired.** Physically-correct extinction + reddening of background light is the primary visual. A *very subtle* stylised hint of cloud shape rides on top so users can perceive clouds against starless backgrounds. Stylisation must read as decorative, never compete with the physical signal.\n\n2. **Procedural substructure** acceptable for v1. We won't search for higher-resolution per-cloud data products as part of this epic. (Separate future issue is filed for that as a follow-up.)\n\n3. **Inside-the-cloud experience** should follow physics: stars whose line of sight traverses the cloud dim and redden, peripheral stars are unaffected, ambient gets very dark. Deep-research piece: surface ideas like *young massive stars carving low-density cavities via stellar winds and ionising radiation* — i.e., cloud density isn't a static envelope but is sculpted by embedded stars (Strömgren spheres, HII regions). Investigate whether modelling embedded O/B stars as low-density cavity centres with a radial density drop-off produces a believable inside-cloud experience.\n\n4. **Cloud-type / age variation.** Model differently based on whether the cloud hosts young stars. Spectral type → age proxy gives a rough cloud-region age. HII regions, reflection nebulosity, and cavity-carving should be type-dependent (e.g., a quiescent dark cloud renders differently from an Orion-Nebula-class active SF region).\n\n5. **Architecture: Zucker replaces Edenhofer in overlap regions.** In practice this likely means modifying the dust voxel volume at build time (`scripts/build-dust.py` + `manifest.json`) so the per-star extinction raymarch in `star.vert.glsl` becomes the single source of truth for absorption — Zucker supplies the higher-fidelity local density structure. Avoids double-counting.\n\n**Phases (filed as child issues):**\n\n- **A.1** Deep research + design doc — produce the physics model we'll implement and the equations/constants/references.\n- **A.2** Build pipeline — bake Zucker density into the Edenhofer voxel grid at build time.\n- **A.3** Wavelength-selective extinction in `star.vert.glsl` (B vs R, RV ≈ 3.1) — current path appears single-channel; confirm and add reddening.\n- **A.4** Procedural density substructure (3D fBm/Worley noise shaped by the ellipsoid envelope, modulating density inside the bbox).\n- **A.5** Embedded young-star cavities + cloud-type variations (cross-match O/B + YSO catalogues with cloud bboxes; reduced density near hot hosts; optional HII emission for the hottest).\n- **A.6** Subtle stylised \"presence\" pass — re-frame or supersede the shelved warm-glow renderer; very subtle by design.\n- **A.7** Inside-the-cloud immersion + ambient response tuning — confirm flying into Taurus reads as physics.\n\n**Scope notes:**\n- The currently shelved `cloud.frag.glsl` may be re-purposed as A.6's stylised presence or replaced — design-doc decision in A.1.\n- Re-enables `FilterState.showMolecularClouds` (and possibly the URL flag) once the epic lands.\n- No hard dependencies on other open issues.\n\n**References to seed research:**\n- https://en.wikipedia.org/wiki/Molecular_cloud\n- Cardelli, Clayton, Mathis (1989) — selective-extinction law, RV ≈ 3.1\n- Edenhofer et al. (2024) — current dust voxel field that this epic augments\n- Zucker 2020 / 2021 — the cloud catalogue already in use (`data/molecular-clouds/`)\n- Standard MC astrophysics: log-normal density PDF + power-law tail, Strömgren-sphere geometry, GMC vs dark-cloud taxonomy","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:36:27Z","created_by":"Alex Marshall","updated_at":"2026-05-02T17:36:27Z","labels":["ism","molecular-clouds"],"dependencies":[{"issue_id":"stellata-c7u","depends_on_id":"stellata-5nh","type":"relates-to","created_at":"2026-05-02T18:39:17Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u","depends_on_id":"stellata-io3","type":"relates-to","created_at":"2026-05-02T19:26:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-vjm","title":"Default first-load view: park near Sol with planets, Orion, and HUD on","description":"Replace the current \"no URL state → drop the user into a generic starfield at `(0, 0, 30)`\" default load behaviour with a deliberately framed first-impression view.\n\n# Why\n\nStellata's first three seconds need to do emotional work. Today when someone loads the bare URL — no `cam=`, no `to=`, no view state — they see a vast unlabelled starfield with no anchor: no obvious \"you are here,\" no implicit prompt to explore. Pretty, but inert.\n\nA first-load that parks the camera near Sol with the Sun's disc clearly visible — at a distance that also frames the inner-solar-system planet orbits and a recognisable constellation in the background (Orion is the obvious pick — everyone in the northern hemisphere recognises the belt; equatorial latitude makes it visible from most of Earth) — gives the visitor an immediate story:\n\n\u003e *That's our Sun. Those are our planets. Orion is over there. Now look around.*\n\nThat single framing converts \"what is this?\" into \"where do I want to go?\" — and the rest of the app's exploration affordances (warp, focus, observe mode) follow naturally.\n\n# What changes\n\nWhen the URL has no view state on initial load:\n\n1. **Camera focus = Sol** with disc clearly resolved.\n2. **Camera distance** in the regime where the inner-solar-system planet orbits (Mercury through Mars at minimum, Jupiter ideally) are simultaneously visible. Probably tens of AU.\n3. **Camera direction** chosen so Orion sits prominently in the background — likely centred, but a slightly off-centre framing might feel less staged. Defer the exact aim choice to implementation.\n4. **HUD on** (`hud-ring` + coord sphere) so the first-time visitor immediately gets a sense of the celestial reference frame.\n5. **Constellation overlay on** so Orion's stick figure is visible without the user having to discover the toggle.\n6. **URL stays empty** until the user interacts. The default doesn't write itself to the URL — the share-link-equals-current-pose contract still holds; first interaction is the first URL write.\n\nWhen the URL *does* carry view state, behaviour is unchanged.\n\n# Why this is blocked by the solar-system layer (and transitively by the rewrite)\n\n- Sol must be a focusable target with a sensible close-approach floor. Today `controls.minDistance = 0.005 pc` (≈1031 AU) means we cannot park anywhere near tens-of-AU range from Sol. **`stellata-3re`** introduces the Sol-focus minDistance relaxation that makes this regime reachable.\n- The Sun's disc must render at correct angular size at close range. **the physical-scale rewrite (`stellata-a7d`)** — which the solar-system layer already blocks on — replaces today's log-mapped pixel formula with `θ = 2·atan(R/d)`. Without it, the Sun's on-screen disc at the chosen distance won't reflect physical reality.\n- Planet orbits to draw rings around come from **the solar-system layer**.\n\nThis issue is the user-visible payoff of the physics + solar-system stack: the moment those two land, this becomes the natural default.\n\n# Implementation sketch\n\n- A new module (or a small block in `stellata.ts` bootstrap) decides the initial camera state when `url-state.ts` reports no view fragment present.\n- Fixed values, hard-coded for the default — not a slider or preset:\n  - Focus: Sol (catalog index TBD; the solar-system layer will have established how Sol is referenced).\n  - Distance: a single tuned constant (e.g. `~30 AU = ~1.45e-4 pc`) chosen so:\n    - Sun's disc reads as a clear orb (R_Sol ≈ 4.65e-8 pc → θ ≈ 6.4e-4 rad ≈ 2.2 arcmin → at 50° FOV in a 1080-tall viewport that's ~24 px diameter — comfortably resolved).\n    - Mars's orbit ring (1.52 AU) fits well inside the framing.\n    - Jupiter's orbit ring (5.2 AU) likely also visible toward the edge.\n  - Aim: a unit vector toward Orion's centroid (RA ≈ 5h 30m, Dec ≈ +0°). Compute once at startup or hard-code as an ICRS vector constant.\n- HUD: ensure the existing HUD-on default isn't overridden by the new bootstrap path.\n- Constellation overlay: same — turn on for Orion specifically, or all constellations (probably all, with Orion happening to be the focus of the framing).\n\n# Open questions / deferred to implementation\n\n- Whether to nudge the camera into a slow drift on first load (an autonomous \"look, the world is alive\" gesture) or leave it static. Lean static — drift is a separate Wander-mode-adjacent feature (`stellata-8r1`), and a static frame respects the user's first input as the start of *their* experience.\n- Exact distance + aim — needs a screenshot pass once the solar-system layer lands so we can pick numbers that feel right rather than just compute correctly.\n- Whether the chart-mode default should differ. Probably no — chart mode is opt-in and inherits the same view.\n- Mobile framing — viewport aspect changes the Orion centring story. `stellata-47c` (mobile UX audit) may need to feed back into this.\n\n# Acceptance\n\n1. Loading `stellata.app/` with no query string parks the camera at the framed Sol view.\n2. Sol's disc is clearly visible (resolved, not a point glow).\n3. At least the inner planet orbits (Mercury → Mars) are visible as orbit rings.\n4. Orion's belt is visible and recognisable in the background, with the constellation stick-figure overlay drawn.\n5. The HUD ring + coord sphere are on.\n6. The URL bar shows no view-state parameters until the user interacts (drags, clicks a star, opens a modal that writes state, etc.).\n7. Loading with view-state in the URL bypasses the framed default entirely — existing behaviour unchanged.\n\n# Related work\n\n- Blocked by `stellata-3re` — provides Sol-as-focus + planet orbits.\n- Transitively depends on the physical-scale rewrite (`stellata-a7d`) via 12a — provides Sol's disc at correct angular size.\n- Mobile layout caveat — `stellata-47c` may want to revisit framing on small viewports.\n- Wander mode (`stellata-8r1`) is a separate idea; this issue deliberately stays static on first load.\n","status":"closed","priority":2,"issue_type":"feature","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:21:06Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:13:54Z","started_at":"2026-05-08T17:42:07Z","closed_at":"2026-05-08T18:13:54Z","close_reason":"Shipped in PR #39 (merge daaeffc). First-load module parks camera at 5 AU aimed at the galactic centre, HUD on. Replaces the stellata.ts constructor 4 AU baseline.","dependencies":[{"issue_id":"stellata-vjm","depends_on_id":"stellata-3re","type":"blocks","created_at":"2026-05-02T18:21:28Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.1","title":"Enable log depth buffer + add \u003clogdepthbuf\u003e chunks to all shaders","description":"Prerequisite for the disc-formula rewrite. With per-star physical sizing, `d_min` for Sol-class stars drops to ~5×10⁻⁸ pc. Standard z-buffer precision collapses long before that. Without log depth buffer, the rewrite will z-fight on close approach.\n\n# Scope\n\n- `stellata.ts:468` — set `logarithmicDepthBuffer: true` on the `WebGLRenderer` constructor.\n- `stellata.ts:483-488` — drop `camera.near` to `1e-10` (log depth handles the dynamic range).\n- Add the standard log-depth chunks to every custom GLSL3 ShaderMaterial:\n  - `src/client/shaders/star.{vert,frag}.glsl`\n  - `src/client/shaders/cloud.{vert,frag}.glsl`\n  - `src/client/shaders/dust-particle.{vert,frag}.glsl`\n  - `src/client/shaders/milkyway.{vert,frag}.glsl`\n\n  Each pair needs:\n  - **vert pars**: `#include \u003ccommon\u003e` + `#include \u003clogdepthbuf_pars_vertex\u003e`\n  - **vert main** (after `gl_Position` is set): `#include \u003clogdepthbuf_vertex\u003e`\n  - **frag pars**: `#include \u003ccommon\u003e` + `#include \u003clogdepthbuf_pars_fragment\u003e`\n  - **frag main** (early): `#include \u003clogdepthbuf_fragment\u003e`\n\n  All four pairs already use `THREE.ShaderMaterial({ glslVersion: THREE.GLSL3, … })` — three.js will inject the chunks correctly under GLSL3 (verified in `stellata.ts` and `milkyway.ts`).\n\n# Verification\n\nNo visible change is expected. Run `npm run dev` and confirm:\n1. Default view looks identical to `main`. Diff a screenshot if in doubt.\n2. Warp / observe / chart mode still render correctly.\n3. Molecular clouds, dust particles, milky way all draw in the right depth order with no new banding.\n4. `npm run typecheck` clean; `npm test` green.\n\n# Acceptance\n\n- `WebGLRenderer` constructed with `logarithmicDepthBuffer: true`.\n- All 8 shader files include the log-depth chunks.\n- `camera.near` lowered to `1e-10`.\n- No visual regressions vs. `main`.\n\n# Reference\n\nThree.js docs: https://threejs.org/examples/#webgl_camera_logarithmicdepthbuffer\nPlan file: `~/.claude/plans/resume-planning-from-users-alexm-claude-reflective-wombat.md`\n","status":"closed","priority":2,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:06:45Z","created_by":"Alex Marshall","updated_at":"2026-05-08T11:13:16Z","started_at":"2026-05-02T18:45:31Z","closed_at":"2026-05-02T19:04:49Z","close_reason":"Landed in PR #7 (commit e6868e2). Verified: typecheck clean, 244 tests pass, runtime shader compile + render confirmed by user across navigate/observe/chart modes.","dependencies":[{"issue_id":"stellata-a7d.1","depends_on_id":"stellata-a7d","type":"parent-child","created_at":"2026-05-02T18:06:44Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-kip","title":"Fullscreen mode + optional 'hide chrome' toggle","description":"Two related affordances filed in one issue because they both serve the same end (immersive / presentation / screenshot use):\n\n1. Browser fullscreen — wire the Fullscreen API to a button in the brand box (or settings panel — decide during implementation) and a keyboard shortcut. Toggling enters/exits fullscreen on the canvas container so the panel and overlays are still visible inside fullscreen by default.\n\n2. Hide chrome — a separate toggle that hides UI chrome (panel, top bar, overlays except the canvas itself + maybe scale bar) so the user can grab a clean shot or present without the UI. This works independently of fullscreen — you can hide chrome without going fullscreen, and vice versa.\n\nCritical files:\n- src/client/keyboard-shortcuts.ts — Esc progression at line 184 (must precede the existing observe→navigate fallback when fullscreen is active); add the new shortcut(s) here.\n- src/client/help-modal.ts — must list both shortcuts.\n- src/client/index.html, src/client/styles.css — chrome targets (panel container, brand box, top-bar, overlays). A single body[data-chrome-hidden] toggle is the cleanest selector hook; confirm during implementation that the existing layout containers cooperate.\n- src/client/url-state.ts — natural home for state persistence (single bit each in the URL flag set). Skip persistence if it causes a confusing first-load (user lands on a page with no UI).\n- src/client/brand-modal.ts (or controls.ts) — wherever the user-visible toggle button lands.\n\nUX:\n- Pressing Esc exits fullscreen first (if active), then falls through to the existing Esc progression in keyboard-shortcuts.ts:184.\n- \"Hide chrome\" should be reversible by the same shortcut/control; show a small unobtrusive \"show UI\" affordance (corner button or bottom hint) so users aren't trapped.\n\nVerify before close:\n- Fullscreen toggle works in Chrome, Safari, Firefox.\n- On exit, layout returns exactly as it was.\n- Hide-chrome restores cleanly via shortcut and via the visible \"show UI\" affordance.\n- Help modal lists both shortcuts.\n- npm run typecheck and npm test pass.\n\nOut of scope: any kiosk/auto-hide-on-idle behaviour, mobile-specific chrome rules (those land under the mobile epic).\n","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:40:51Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:03:52Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-6qk","title":"'F' shortcut: find a star and point camera at it (no warp)","description":"Add a keyboard shortcut F that opens the same search modal used by G, but instead of warping the camera to the chosen star, only orients the camera at it.\n\nBehaviour by mode:\n- Observe: rotate the camera (yaw + pitch) to point at the selected star. Animate the rotation rather than snap. FOV unchanged. Camera position unchanged.\n- Navigate: orient the orbit gaze toward the star without travelling — i.e. rotate the trackball target so the star sits at the centre of the view, but do NOT warp the camera position to it (that's G's job) and do NOT mark it as focused (no focus-ring, no disc-mask). The animation should match observe-mode feel.\n\nCritical files \u0026 reuses:\n- src/client/keyboard-shortcuts.ts — existing G binding at line 89; add the F case adjacent. Modal-open + editable-target guards already in place around line 80.\n- goModal (instantiated in keyboard-shortcuts.ts) — confirm during implementation that it can be parameterised by intent (\"warp\" vs \"look\") rather than duplicated.\n- src/client/search.ts — the search/typeahead behind the modal.\n- src/client/observe-controls.ts — yaw/pitch source for the observe rotation animation.\n- src/client/stellata.ts — trackball target / orbit gaze; the navigate-mode \"swing target without travel\" lives here.\n- src/client/help-modal.ts — must be updated to list F.\n\nUX:\n- Pressing F while a modal is already open is a no-op (existing modal-open guard handles this).\n- Esc cancels the search modal as today.\n\nVerify before close:\n- In observe, F → pick a star → camera smoothly rotates to it.\n- In navigate, F → pick a star → orbit gaze swings to it; camera position unchanged; no focus indicators activate.\n- npm run typecheck and npm test pass.\n- Help modal shows F.\n\nOut of scope: changing what G does. Adding F to the panel as a button (keyboard-only is fine for v1).\n","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:40:35Z","created_by":"Alex Marshall","updated_at":"2026-05-04T08:33:16Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-1yu","title":"Design: rework settings panel to be contextual to camera mode (nav vs observe)","description":"The right-side settings panel today shows a single static control set regardless of which mode the user is in. Navigate and observe expose different affordances and want different things at the user's fingertips, so the panel ends up cluttered for both. This is a usability/UX issue: the user wants ready access to mode-relevant controls without scanning the whole panel.\n\nDeliverable: a written design spec (in docs/ or attached to this issue) covering, at minimum:\n- For each mode, which controls are primary vs secondary vs hidden.\n- Layout: are mode-specific sections inserted/removed, or shown via tab/segmented control, or via collapse-by-default rules?\n- Behaviour on mode-switch — animation, scroll position, collapse memory.\n- Are there observe-mode controls that don't yet exist and should be added (e.g. FOV preset buttons, label-density slider) — flag them for separate feature issues, do not add scope here.\n- Reverse-sync implications: today the panel reads back from Stellata.getFilter()/state; the design must keep that contract intact.\n\nCritical files to read before designing:\n- src/client/controls.ts — every existing widget + reverse-sync.\n- src/client/panel-layout.ts — top-level + per-group collapse behaviour the redesign must respect or replace.\n- src/client/mode-toggle.ts — the navigate↔observe pill that triggers the mode change the panel needs to react to.\n- src/client/observe-controls.ts — affordances that exist only in observe (drag, FOV wheel) and should inform what observe surfaces in the panel.\n- docs/ui-and-controls.md — documents existing layout containers, magnitude presets/override flags, CSS gotchas ([hidden] specificity, backdrop-filter stacking contexts).\n\nOut of scope: implementation. Once the design is approved, file an implementation issue and close this one.\n","status":"open","priority":2,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:40:21Z","created_by":"Alex Marshall","updated_at":"2026-05-02T16:40:21Z","dependencies":[{"issue_id":"stellata-1yu","depends_on_id":"stellata-8hu","type":"related","created_at":"2026-05-08T19:09:01Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-1yu","depends_on_id":"stellata-f9b","type":"parent-child","created_at":"2026-05-06T22:19:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ywn","title":"Deep-space probes","description":"**Status:** Depends on the solar-system layer's time-scrubber framework.\n\n**Goal:** render the five interstellar probes — Pioneer 10, Pioneer 11, Voyager 1, Voyager 2, New Horizons — at their actual positions for the current `t`. Visible only when solar-system layer is active. A small but emotionally significant addition: \"you can see how far they've gotten\" against the scale of the solar system and the nearest stars.\n\n### Probe data via NASA HORIZONS\n\n| Probe        | Launch     | HORIZONS ID | Status                |\n|--------------|------------|-------------|-----------------------|\n| Pioneer 10   | 1972-03-03 | `-23`       | Silent since 2003-01-23 |\n| Pioneer 11   | 1973-04-06 | `-24`       | Silent since 1995-09-30 |\n| Voyager 2    | 1977-08-20 | `-32`       | Active                |\n| Voyager 1    | 1977-09-05 | `-31`       | Active                |\n| New Horizons | 2006-01-19 | `-98`       | Active                |\n\n### Build script: `scripts/fetch-probe-trajectories.ts`\n\n**Manual** — `npm run fetch:probes` only, not part of the default `build` chain. Same convention as the GCVS files (committed once, refreshed when upstream updates).\n\nPer probe, query HORIZONS:\n```\nhttps://ssd.jpl.nasa.gov/api/horizons.api?format=json\n  \u0026COMMAND=\u003cid\u003e\n  \u0026CENTER=500@10\n  \u0026MAKE_EPHEM=YES\n  \u0026EPHEM_TYPE=VECTORS\n  \u0026VEC_TABLE=2\n  \u0026REF_PLANE=FRAME\n  \u0026START_TIME=\u003claunch date\u003e\n  \u0026STOP_TIME=2050-01-01\n  \u0026STEP_SIZE=30d\n```\n\n`REF_PLANE=FRAME` returns ICRS equatorial directly — same frame as ATHYG and the rest of the app. No ecliptic rotation needed.\n\nParse the `$$SOE…$$EOE` block, extract `JDTDB` and `X, Y, Z` (in AU) per step. Output `data/probes/{id}.json`:\n```json\n{\n  \"id\": \"voyager1\",\n  \"label\": \"Voyager 1\",\n  \"launchDate\": \"1977-09-05\",\n  \"launchUnix\": 242265600,\n  \"lastContactUnix\": null,\n  \"trajectory\": [\n    { \"jd\": 2443413.5, \"x\": 0.12, \"y\": -0.03, \"z\": 0.01 },\n    ...\n  ]\n}\n```\n\n`lastContactUnix` is null for active probes, populated for Pioneer 10 (`2003-01-23`) and Pioneer 11 (`1995-09-30`).\n\n30-day step from launch to 2050: ~880 entries × ~50 bytes = ~45 KB per probe, ~225 KB total for 5 probes. Plain-text commit, no LFS.\n\n### Sync to runtime\n\nMirror `data/probes/*.json` → `public/probes/*.json` via a small `scripts/sync-probes.ts` (same pattern as `sync-dust.ts`). Add to `build` and `dev` script chains. `public/probes/` gitignored.\n\n### Runtime: `src/client/probe-loader.ts`\n\n- Fetch all 5 JSONs in parallel (small enough — fire-and-forget at startup or lazy-load on first solar-system-layer activation).\n- For a given `t`, find bracketing entries and **linearly interpolate** x/y/z. Probes move ~3–4 AU/year — 30-day samples are visually indistinguishable from the true smooth trajectory.\n- Hide probe entirely if `t \u003c launchUnix`.\n- Visual flag for \"extrapolated past last-contact\": Pioneer 10 after 2003-01-23, Pioneer 11 after 1995-09-30. Render dimmer, with a \"(signal lost)\" label suffix.\n\n### Probe rendering\n\nSame instanced-quad pattern as planets. Tiny — a few px. Always-labelled when visible (probes are a discovery feature, the labels are the point). Optional: a thin fading trail showing the trajectory between launch and current `t`.\n\n### Files\n\n- New:\n  - `scripts/fetch-probe-trajectories.ts` — manual HORIZONS fetcher.\n  - `scripts/sync-probes.ts` — `data/probes/` → `public/probes/`.\n  - `src/client/probe-loader.ts` — JSON load + interpolation.\n  - `data/probes/{voyager1,voyager2,pioneer10,pioneer11,new-horizons}.json` — committed (no LFS).\n- Touch:\n  - `package.json` — `fetch:probes` script + `sync-probes` in chain.\n  - `solar-system.ts` (from 12a) — register probe layer alongside planets, share label/render plumbing where sensible.\n  - `.gitignore` — `/public/probes/`.\n- After landing, extend the `CLAUDE.md` \"Solar-system layer\" section with the probe data pipeline and last-contact handling.\n\n### Watch out for\n\n- HORIZONS rate-limits and occasional schema drift. Fetch script should be defensive — clear error if `$$SOE` block is missing or columns are unexpected.\n- Pre-launch hiding is per-probe based on `launchUnix`. Don't accidentally render a probe at `t = 0` (1970-01-01) where its trajectory array's first entry would extrapolate backward.\n- Linear interpolation between widely-spaced 30-day samples is fine for visual purposes but should not be billed as \"true ephemeris.\" Comment in code so future-you doesn't mistake it for a precision ephemeris API.\n- Active probes' projected trajectories past *now* are HORIZONS forward-projections, not certainties. The data is correct as far as projections go but the wording in any tooltips should be careful.\n- Annual-ish refresh: re-run `npm run fetch:probes`, commit the refreshed JSONs. Document the cadence in CLAUDE.md.\n\n### Estimate\n\nHalf a day to a day. Fetch script is the bulk; runtime interpolation + rendering is small once 12a's planet rendering is in place.\n\n### Deliberately excluded\n\n- Other interstellar probes / spacecraft (Parker Solar Probe, JUICE, etc.) — not yet at solar-system-edge scale where they'd be visually meaningful in this layer.\n- True ephemeris precision (sub-AU). Linear interpolation between monthly samples is good enough for visualisation.\n- Trajectory-line rendering past current `t` for active probes. Trail is launch-to-now only; future projection isn't shown (would need clearer \"this is a projection\" framing in UI).\n","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:47Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:03:51Z","labels":["phase-12b","solar-system"],"dependencies":[{"issue_id":"stellata-ywn","depends_on_id":"stellata-3re.1","type":"blocks","created_at":"2026-05-07T09:56:06Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-ywn","depends_on_id":"stellata-3re.6","type":"blocks","created_at":"2026-05-07T09:56:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re","title":"Solar-system layer (time + planets + heliopause + scale rings)","description":"**Status:** v1 solar-system layer. Builds on the physical-scale rewrite (stellata-a7d).\n\n**Goal:** when focused on Sol (or unfocused at Sol), render the local solar-system context — the 8 planets in their positions for the current wall-clock time `t`, faint per-planet orbital rings, the heliopause boundary as an asymmetric shell, and reference scale rings at 1 AU and 50 AU. The framework that lands here is generic enough to be reused for exoplanet host stars (stellata-bk5).\n\n### Order of work (per Alex's 2026-05-07 walk-through)\n\nThe children of this epic are sequenced to ship value early and defer complexity:\n\n1. **stellata-3re.6** — generic per-star \"has planets\" data model + render gating (groundwork; reused by bk5).\n2. **stellata-3re.7** — per-planet orbit rings with angular-separation visibility heuristic (Sol initially; reused for any host).\n3. **stellata-3re.8** — design rule: Sol's rings tilt to the ecliptic; all other host stars' rings lie in the galactic plane.\n4. **stellata-3re.9** — UX: how to indicate stars-with-planets at distance, how to label planets up close. Same UX feeds bk5.\n5. **stellata-3re.4** — Sol planet bodies: correct diameters, gas/solid distinction, Earth atmosphere, representative colour. **Placeholder orbital positions OK** — orbital location not yet driven by VSOP87.\n6. **stellata-3re.10** — research: do we ship texture maps for Sol's planets, or stay schematic?\n7. **stellata-3re.3** — VSOP87 ephemerides via `astronomia` populate real positions for `t = now`. Backfills the rendering from step 5.\n8. **stellata-3re.11** — discreet `t` readout in `.ui-bottom`. Display only — no scrubbing in v1. Format: UTC Zulu (default recommendation).\n9. **stellata-3re.5** — heliopause shell + 1 AU / 50 AU scale rings.\n\n### What's deferred to other epics\n\n- **Time scrubber UI** → `stellata-nmu` (its own epic). v1 sets `t = now` and shows the readout but does not let the user move time.\n- **Deep-space probes** → `stellata-ywn` (depends on `t` from 3re.1 + per-star data plumbing from 3re.6).\n- **Exoplanets** → `stellata-bk5` (reuses 3re.6 data model, 3re.7 ring renderer, 3re.8 plane rule, 3re.9 UX).\n- **Default first-load view framing the inner solar system** → `stellata-vjm` (depends on this epic landing).\n- **Chart-mode planets** → `stellata-m40.3` (depends on 3re.4).\n- **Physical-vs-representational split** → the three-mode framework epic. Concrete applications under this epic are the unfocus-split and apparent-magnitude planet visibility beads (filed as P2 children).\n\n### Sol-focus minDistance relaxation\n\nWhen focused on Sol, drop `controls.minDistance` from `0.005 pc` (≈1031 AU) down to `~0.5 AU` (≈ `2.4e-6 pc`) so the user can fly into the inner solar system and resolve individual planets. Other focal stars retain the existing `0.005 pc` floor.\n\nThis is safe specifically for Sol because Sol sits at the world origin — the float32 jitter that bites at small distances *from non-origin focal stars* (CLAUDE.md \"Camera near plane vs controls minDistance\") doesn't apply when the focal frame is also the world frame.\n\n`camera.near` already at `0.001 pc` is fine; for Sol-focus consider dropping to `1e-7 pc` (≈0.02 AU) so very close planet inspection isn't culled. Keep the strict-less-than invariant (`camera.near \u003c minDistance`).\n\n### Planet positions via VSOP87 (`astronomia` npm)\n\nDetail in stellata-3re.3. Adds the `astronomia` dependency (~500 KB; offline; no network). Computes heliocentric ICRS positions at any `t`.\n\n### Heliopause asymmetric ellipsoid\n\nDetail in stellata-3re.5. ~122 AU upwind (Galactic apex direction RA 17h53m, Dec +27.4°), ~115 AU flank, ~200 AU heliotail. Modelled as an ellipsoid offset 39 AU toward the heliotail. Static — no `t` dependence on human timescales. The shell is a *representational* layer (the heliopause itself is a particle-density boundary, not optically visible) — gated on host-focused per the unfocus-split bead.\n\n### Watch out for\n\n- **Sol-focus `minDistance` is a per-focus override.** When focus switches from Sol to another star, the floor must snap back to `0.005 pc` *before* the new focus's recenter pulls the camera in. `setFocus` is the right hook.\n- **`t` vs `uTime`.** `uTime` keeps ticking at `uSecondsPerDay = 0.2` regardless of where the scrubber is. Variable-star pulsation is cosmetic — don't accidentally couple `t` and `uTime`.\n- **Ecliptic ↔ equatorial obliquity.** Use J2000 ε = 23.4392911° consistently. Don't reach for the time-varying obliquity term.\n- **Physical objects render everywhere; representational layers gate on host-focused.** Planet bodies (physical) render whether or not their host is focused, gated only by their own apparent-magnitude visibility. Heliopause shell, orbit rings, planet labels, apex label, time readout (representational) gate on host-focused. See the unfocus-split bead for the concrete split.\n- **`camera.near` invariant.** Anything that drops `minDistance` must keep `camera.near \u003c minDistance`.\n\n### Deliberately excluded\n\n- Moons (Earth's Moon, Galilean satellites, Titan, etc.). VSOP87 doesn't cover these; satellite ephemerides are a separate effort.\n- Asteroids and minor planets.\n- Time-series proper motion of stars. Stars remain snapshot-only; `t` is solar-system-only in v1.\n- Earth-rotation / time-of-day effects. Heliocentric scene; daily rotation has no meaning.\n- Auto-advancing `t`. Scrubber + animation are the time-scrubber epic.","design":"# Architecture for the 3re.15 + 3re.16 + 3re.17 PR (planet visibility refactor)\n\nThis PR refactors the solar-system planet pipeline so it scales to\nthousands of planet-bearing hosts (stellata-bk5).\n\n## Layer split\n\n- **PlanetBodyField** (NEW) — global InstancedMesh of every attached\n  host's planet bodies. v1 attaches Sol; bk5 attaches each exoplanet\n  host as its `PlanetSystem` shard lands. Bodies are PHYSICAL — render\n  whenever attached, subject only to apparent-magnitude visibility.\n  Per-instance attributes: iHostLocalPos, iLocalRel, iRadiusPc,\n  iAlbedoP, iColour, iSolidity, iHostAbsmag. Vert shader derives\n  m_planet from these + camera, then maps to disc size via the shared\n  perceptual-disc abstraction.\n  API: attachHost(idx, ps, hostAbsPos), detachHost(idx),\n  recenter(deltaWorldOffset) on focus change, update(t) per-frame.\n\n- **OrbitRingsLayer** — RENAME of current StarSystem with body-rendering\n  stripped out. Single-host, focus-driven. Builds/tears down on\n  onPlanetSystemChange. Rings stay focused-host-only forever\n  (showing every host's rings simultaneously is visual noise).\n\n- **Heliopause** — Sol-only by data availability (no astropause data\n  for exoplanets in the foreseeable future). Stays as-is. Visibility\n  gates on focusedHost === solIndex.\n\n- **PlanetLabels** — focused-host-only via getFocusedPlanetSystem().\n  Unchanged.\n\n- **DROP warpDestStarSystem.** Existed only to preview destination's\n  bodies/rings during warp under the focused-host-owns-the-instance\n  model. Under PlanetBodyField, all hosts' bodies render whenever\n  attached. Rings just turn on at warp completion.\n\n## Per-host distance cull (critical for bk5 perf)\n\nFor each attached host, precompute cullDistancePc — the d_vh at\nwhich its brightest planet would just cross the current maxAppMag\nslider. Solved closed-form from host absmag + brightest-planet\nsqrt(p)·R/d_hp term. Recomputed when maxAppMag changes.\n\nPer frame: skip the host's positionsAt + orientation-rotate + buffer\nupload entirely if camera-to-host \u003e cullDistancePc. The cull is the\nprimary line of defense; per-fragment discard in the vert shader is\nthe backup. For Sol with naked-eye preset (m=6.5), Jupiter's d_vh\nceiling is sub-parsec — so any non-Sol focus already culls Sol's\nbodies, exactly as desired.\n\n## Shared perceptual abstraction (DRY)\n\nThe apparent-magnitude → perceptual disc/glow mapping is the SAME\nPHYSICS for any point of light: ocular PSF + display luminance\nsaturation. (NOT atmospheric scintillation; that's twinkle and\nisn't modelled.) Same treatment applies to stars, planets, future\nmoons/asteroids/etc.\n\n- TS helper — src/client/perceptual-magnitude.ts (NEW). Pure\n  functions for any CPU-side mapping (search ranking, label gating,\n  app-mag-driven sizing). Vitest-covered.\n- GLSL chunk — included in star.vert.glsl and planet.vert.glsl.\n  Same numerical output as today's star pipeline (star rendering\n  bit-for-bit unchanged).\n- Three-pass parity for planets — depth-mask + disc + glow,\n  mirroring stars. Disc and glow use the chunk; depth-mask is\n  trivial.\n\n## Three-commit ordering (independently revertable)\n\n1. Extract perceptual-disc chunk — pure refactor; star pipeline as\n   sole consumer. NO behavior change. Smoke against named stars\n   (Sirius, Sol from 1pc, faint near-cutoff binoculars-preset stars,\n   Milky Way diffuse-glow proportionality).\n\n2. Apparent-magnitude pipeline + glow + Lambert phase + albedo\n   applied to planets, on the EXISTING per-host StarSystem (still\n   focus-only). Drops the unconditional 2-px floor. Adds glow pass\n   + depth-mask pass. Forward-compat vert shader (iHostLocalPos\n   attribute always (0,0,0) in this commit; populated for real in\n   commit 3).\n\n3. Architecture refactor: PlanetBodyField + OrbitRingsLayer rename\n   + drop warpDestStarSystem. Bodies become global, always-on. From\n   this commit on, Sol's planet bodies render even when Sol unfocused\n   (subject to apparent-mag cutoff via per-host distance cull).\n\n## Formula correction (re-stated for cross-reference)\n\nThe 3re.16 description has a typo — apparent-mag formula written as\np · (R/d_hp)² · (R/d_vp)² (R appears twice) is dimensionally wrong.\nCorrect Lambertian-sphere formula has one R plus the viewer↔host\nleg ratio:\n\n  m_host_at_viewer = M_host + 5·log10(d_vh / 10pc)\n  m_planet = m_host_at_viewer\n           − 2.5·log10( p · (R/d_vp)² · (d_vh/d_hp)² · φ(α) )\n\nVerified against Jupiter: −2.7 from Earth at opposition, +5.2 from\n~150 AU outside heliopause, +21 from α Cen (1.34 pc). The bead's\nown α-Cen estimate of +28 is wrong; +21 is correct. Heliopause-range\nestimate (+5 to +6) is correct.\n\n## Glow halo intent (also a comment to add at the call site)\n\nGlow exists because a sufficiently bright point of light appears\nlarger on the retina (and on a non-HDR display) than its physical\ndisc — diffraction + ocular aberrations + display saturation PSF.\nNOT atmospheric twinkle. Treatment is identical for stars and\nplanets; Venus at -4 from Earth gets a perceptible halo for the\nsame reason Sirius does.\n\n## Labels follow rings, not bodies\n\nOrbit-ring visibility gates planet labels (already the case in\nplanet-labels.ts:99-108). Body apparent-magnitude cutoff does NOT\ngate labels — if the ring is up, the label is up, even if the body\nitself is below the slider cutoff. Labels answer \"what would I be\nseeing here,\" not \"what am I currently rendering.\" Don't accidentally\ncouple the two.","notes":"Now blocked by stellata-a7d (physical-scale rewrite). The Sol-focus close-approach floor described here (drop minDistance to ~0.5 AU) is a special case of the per-star d_min(R) = R/tan(fov_minor·0.9/2) that the rewrite introduces for every focusable star, and the suggested camera.near drop to 1e-7 pc requires the log depth buffer that the rewrite enables. Re-evaluate the Sol-specific values in this issue once stellata-a7d lands — they may collapse to 'just use the default per-star floor'.","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:36Z","created_by":"Alex Marshall","updated_at":"2026-05-08T19:24:49Z","labels":["phase-12a","solar-system"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-74e","title":"Per-star dust slab fallback for sightlines exiting the Edenhofer voxel volume","description":"**Goal:** correct per-star extinction along sightline segments outside the high-fidelity 3D dust volume(s). One layer of the multi-source dust stack under parent **stellata-ty4**.\n\n`star.vert.glsl` raymarches camera→star through the Edenhofer voxel cube (~1.25 kpc AABB). Lines of sight that exit the AABB get zero attenuation beyond — wrong at the edge. A star sitting just outside the cube renders unattenuated despite intervening dust along the sightline. From extragalactic vantages (e.g. M31, parent ty4 trigger case) the entire ~780 kpc sightline is unattenuated.\n\n**Fix:** lift the analytical disc profile already in `milkyway.frag.glsl` (`analyticalDustDensity` = `exp(-(R-R₀)/L_R) × exp(-|z|/h_z)` with h_z ≈ 125 pc, evaluated in Galactic frame) into a shared shader include, and integrate it over sightline segments outside the cube. Per-star extinction then has a sensible value everywhere — high fidelity inside the cube, smooth analytical fallback outside.\n\nOnce **stellata-ty4.1** (Bayestar shell) lands, this layer becomes the outermost fallback after both 3D maps are exhausted. The shader logic is the same; the AABB test gates whether to fall through to slab vs Bayestar.\n\n**Files:**\n- `src/client/shaders/star.vert.glsl` — call the shared slab function beyond the AABB(s).\n- New `src/client/shaders/dust-extinction.glsl` shared include (or extend `perceptual-disc.glsl`-style chunk).\n\n**Watch out for:**\n- Slab evaluation requires ICRS→Galactic rotation matching the MW volumetric layer. Keep it as one shared shader function so both phases use identical maths.\n- Boundary continuity — the analytical profile and Edenhofer voxel values should be roughly continuous at the AABB boundary (no sudden brightness jump as a star crosses the edge). Verify visually.\n- Long-sightline raymarch density — DUST_STEPS = 48 is fine at kpc scales but inefficient at Mpc scales. The analytical disc has a closed-form line integral over the segment intersecting the slab; consider using it instead of an even-step raymarch where the segment is much longer than the slab thickness. See **stellata-ty4.2** for the Mpc-scale discussion.\n\n**Estimate:** ~2 hours.\n\n**Note:** this issue used to bundle a UI status chip + dataset-boundary mesh under the same scope. Those have been extracted into the artificial-visualisation cluster (`stellata-io3`) since they're a different conceptual category — opt-in visualisation of dataset metadata, not a 'you're in degraded mode' indicator.","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:26Z","created_by":"Alex Marshall","updated_at":"2026-05-18T11:56:22Z","labels":["ism","phase-6"],"dependencies":[{"issue_id":"stellata-74e","depends_on_id":"stellata-io3.2","type":"relates-to","created_at":"2026-05-02T19:26:13Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-74e","depends_on_id":"stellata-ty4","type":"parent-child","created_at":"2026-05-18T12:55:05Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dk3","title":"Local Bubble shell mesh","description":"**Goal:** translucent surface mesh of the Local Bubble (low-density ISM cavity surrounding the Sun, ~200 pc radius). Subtle by default, toggleable for emphasis. Gives the user immediate context that the Sun sits *inside* a bubble.\n\n**Data sources:**\n- **O'Neill, Zucker, Goodman \u0026 Edenhofer 2024** (\"The Local Bubble Is a Local Chimney\", ApJ 973, 136 / arXiv:2407.18238). The paper should have a data availability section; if not, check Catherine Zucker's GitHub: https://github.com/catherinezucker\n- **Zucker et al. 2022** (Nature, \"Star formation near the Sun is driven by the expansion of the Local Bubble\") — earlier 3D mesh. Also linked from her interactive 3D gallery at https://catherinezucker.github.io/\n\n**Implementation sketch:**\n- Source format likely OBJ/PLY/HDF5 mesh. Convert at preprocessor time to a tiny binary (vertex positions + face indices). Few thousand vertices typically.\n- Three.js: `THREE.Mesh` with `THREE.MeshBasicMaterial({ transparent: true, opacity: 0.05, side: DoubleSide, blending: AdditiveBlending })`.\n- Subtle inner/outer gradient via vertex colours (front-facing vs back-facing) so users can perceive 3D shape.\n- Label \"Local Bubble\" at the centroid, similar to constellation labels.\n- UI toggle.\n\n**Watch out for:**\n- Coordinate frame — likely Galactic; rotate to ICRS on ingest.\n- Mesh might be in different units (kpc vs pc); verify scale.\n- Front-vs-back face ordering matters for the gradient effect.\n\n**Estimate:** 3–5 hours.\n","status":"open","priority":2,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:21Z","created_by":"Alex Marshall","updated_at":"2026-05-02T18:05:09Z","labels":["ism","phase-4a"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9hp","title":"Nebulae catalogs (PN, RNe, H II, SNR)","description":"**Goal:** classical named nebulae (Orion Nebula, Ring Nebula, Helix, Veil, Horsehead, etc.) as discrete blob objects with type-based colour coding and search-bar entries.\n\n**Data sources** (filter to within ~3 kpc to match the dust volume):\n- **Strasbourg-ESO Catalogue of Galactic Planetary Nebulae** (Acker et al. 1992, updated): VizieR `V/84` or HEASARC `PLNEBULAE`. ~1,500 PNe with up to four independent distance estimates each.\n- **Merged Catalog of Reflection Nebulae** (Magakian 2003): VizieR `J/A+A/399/141` or HEASARC `REFNEBULAE`. 913 reflection nebulae; cross-match the illuminating star to ATHYG via HD/HIP for distance.\n- **Sharpless H II catalog**: VizieR `VII/20`. 313 entries; pair with Avedisova's \"Catalogue of Star-Forming Regions\" (VizieR `V/115`) for derived distances since the original catalog has none.\n- **SIMBAD TAP query** for unified all-types fetch: object types `PN`, `RNe`, `HII`, `SNR` with a distance cut, via `astroquery.simbad`.\n\n**Per-nebula data:** ID, common name, type, RA/Dec/distance → xyz, angular size → physical radius, illuminating star (for RNe).\n\n**Rendering — this is where raymarching IS appropriate.** Each nebula occupies a small AABB (typically 1–50 pc), so per-nebula raymarching with 16–32 samples is dense in voxel-space and produces smooth volumetrics without the aliasing that killed the all-sky fog. See `memory/feedback_raymarch_for_nebulae.md` for context. Recover the fog shader pattern from git history at commit `5ad5444` if useful — it's a clip-space fullscreen triangle, ray-AABB slab intersection, fixed-step march with log-encoded density decode.\n\n**Type-based visuals:**\n- H II regions — warm reddish-pink, soft sphere with density falloff\n- Planetary nebulae — greenish-teal, often shell-like; small central point + thin shell\n- Reflection nebulae — bluish, irregular blob biased toward the illuminating star's colour (look up via ATHYG cross-ref)\n- Supernova remnants — filamentary; pink→blue gradient by age\n\n**No photographs.** All visuals procedural, matching the rest of the app's aesthetic.\n\n**Watch out for:**\n- Many PNe have only rough distance estimates; drop entries with no parsed distance.\n- Filter to ATHYG-relevant volume — most distant PNe (\u003e5 kpc) won't render usefully and clutter search.\n- Procedural shape variation per type — without it, every nebula is a blob and the type encoding gets lost.\n\n**Estimate:** 1–2 days for catalogs + ingest + rendering. The volumetric raymarching for the per-nebula effect is ~half the work.\n","status":"open","priority":2,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:20Z","created_by":"Alex Marshall","updated_at":"2026-05-02T18:05:17Z","labels":["ism","phase-3b"],"dependencies":[{"issue_id":"stellata-9hp","depends_on_id":"stellata-2q8","type":"relates-to","created_at":"2026-05-02T19:26:15Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9hp","depends_on_id":"stellata-c7u.5","type":"relates-to","created_at":"2026-05-02T19:26:11Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9hp","depends_on_id":"stellata-io3","type":"relates-to","created_at":"2026-05-02T19:26:13Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.60","title":"Phase 2 — Stage 2 tier 2 supplement: SIMBAD-backed gaia_source_id resolution","description":"Replacement for the deferred regex-based tier 2 (WDS notes prose parsing) that was originally specced in stellata-dch.28 and stripped from PR #100 on review.\n\n# Why deferred (background)\n\n`stellata-dch.28`'s original 5-tier resolution chain included tier 2 = `wds_notes_hip`: parse WDS Notes prose for `Component X is HIP NNN` / `X = HIP NNN` patterns and bind HIPs to component letters. PR #100's review surfaced two problems with the approach:\n\n1. **Volume**: only 10 / 84,916 components actually resolve via tier 2 on current data — all of them obscure secondaries (STF 231 B, TOK 71 D, HJ 3834 E, DUN 116 B, STF 548 B, STF1931 B, STF1999 B, …). The well-known hard cases the bead body cited (α Cen, Sirius, Procyon, Castor) all resolve via other paths (tier 3 athyg_gaia_native or, for Gaia-saturated bright stars, dch.30's HIP2 long-baseline fallback).\n2. **Fragility**: hand-rolled regex over free-form prose. PR #100 review caught an `re.IGNORECASE` over-match where `B component is HIP 96569` bound `\"is\"` as a component letter — invisibly wrong, would have shipped.\n\nThe principled alternative is to query a curated cross-identification service (SIMBAD) rather than re-implementing decades of CDS-team work with regex. We already have refresh-simbad-sample.py in the repo, so the wiring exists.\n\n# Scope\n\nFor every WDS pair that falls through tiers 1, 3, 4, 5, look it up in SIMBAD by `WDS J\u003cwds_id\u003e \u003ccomponent-letter\u003e` and read its resolved Gaia DR3 source_id from the cross-IDs.\n\nImplementation outline:\n- Build the list of unresolved (wds_id, discoverer, component) tuples after `resolve_all_pairs` runs.\n- Batch-query SIMBAD (or use a cached TSV side-file similar to `simbad_sample.tsv`).\n- Parse SIMBAD's response for Gaia DR3 cross-IDs; bind to the component.\n- New `resolve_via` value: `simbad_xid`.\n\n# Dependencies\n\n- `stellata-dch.29` (Gaia astrometry table) — tier 4/5 PM-propagated position match should run first so SIMBAD only handles the residual set.\n- `stellata-dch.30` (HIP2 long-baseline fallback for Gaia-saturated stars) — same reason; SIMBAD is the bottom of the cascade, not the top.\n\nAfter both land, re-measure the residual count. If it's small (\u003c1000), SIMBAD's per-object cost is fine. If it's large, consider VizieR's bulk WDS↔Gaia cross-match tables (Marrese 2020, etc.) as the bulk source and SIMBAD only for the long tail.\n\n# Open question to resolve at start\n\nDoes SIMBAD reliably surface PER-COMPONENT Gaia DR3 source_ids for multi-component WDS systems (η Cas A vs B vs C), or does it resolve at the system level? Spot-check a few hard cases (η Cas, ξ UMa, ζ Cnc) before committing the implementation pattern.\n\n# Smoke\n\n- Run on residual set after dch.29/dch.30 land.\n- Spot-check α Cen B (WDS 14396-6050 B) — SIMBAD should resolve to Proxima/α Cen B's Gaia source_id.\n- Resolution counter `simbad_xid` should be non-zero in the build log.\n\n# Supersedes\n\n- `stellata-dch.59` (primary/secondary synonym anchoring) — same prose-regex approach we're deferring. Closed as superseded.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T12:09:11Z","created_by":"Alex Marshall","updated_at":"2026-05-19T12:09:11Z","labels":["build-binaries","cross-match","phase-2"],"dependencies":[{"issue_id":"stellata-dch.60","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-19T13:09:11Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.60","depends_on_id":"stellata-dch.29","type":"blocks","created_at":"2026-05-19T13:09:22Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.60","depends_on_id":"stellata-dch.30","type":"blocks","created_at":"2026-05-19T13:09:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.192.7","title":"catalog-pure.test.ts: LMC cone boundary tested on Dec axis only; RA-axis (cos(Dec)=0.36) untested","description":"# Finding\n\n`scripts/catalog-pure.test.ts` covers the LMC cone boundary with:\n\n\\`\\`\\`\nconst inDec = LMC_CENTRE_DEC_DEG + (LMC_CONE_HALF_ANGLE_DEG - epsilon);\nconst outDec = LMC_CENTRE_DEC_DEG + (LMC_CONE_HALF_ANGLE_DEG + epsilon);\n\\`\\`\\`\n\nBoth pushes are along Dec at the centre's RA. At LMC declination (−69°), `cos(Dec) ≈ 0.36`, so a 1° shift in RA (hours) corresponds to a much SMALLER angular shift on the sky than the same 1° shift in Dec. The vector-form `angularSeparationDeg` implementation handles this correctly via `cosDecA / cosDecB` weighting, but the test never exercises the RA edge.\n\nWhy this is a real gap rather than nice-to-have:\n- A naive small-angle approximation (which `angularSeparationDeg` deliberately avoids) would over-estimate RA contribution near the LMC's Dec by a factor of ~3 (1/cos(69°) = 2.79). The current implementation is correct, but if anyone \"simplifies\" it later assuming the test surface is sufficient, the regression goes undetected.\n- The LMC field is at high southern latitude where this matters most; the boundary test is at exactly the wrong axis to catch a cos(Dec) bug.\n- `stellata-test-coverage-discipline` rule for boundary tests: test the EDGE in every dimension that affects the predicate. Only Dec axis is tested today.\n\n# Scope (one commit/PR, test-only)\n\nAdd two boundary tests in the existing `applyLmcKinematicOverride` describe block in `scripts/catalog-pure.test.ts`:\n\n1. **RA-axis boundary at LMC's Dec.** Push RA from `LMC_CENTRE_RA_HOURS` by the angular distance equivalent to (LMC_CONE_HALF_ANGLE_DEG − ε) and (+ ε) at the LMC's Dec. Convert the angular separation back to an RA-hours shift via `Δra_hours = (angular_degrees / 15) / cos(Dec_radians)`. Expect inside → non-null, outside → null. Pin the RA shift numerically with `toBeCloseTo` so the cos(Dec) factor is asserted, not just the boundary semantics.\n\n2. **Diagonal boundary** (RA + Dec both shifted, total angular separation = `LMC_CONE_HALF_ANGLE_DEG ± ε`). Confirms the angular distance is great-circle, not Manhattan/box.\n\nIf a helper like `offsetSkyPosition(raHours, decDeg, separationDeg, positionAngleDeg)` would simplify these tests, lift it as a `*-pure.ts` test helper (catalog-pure.test.ts is the only consumer today) or inline the math with a comment.\n\n# Acceptance\n\n- Two new tests in `applyLmcKinematicOverride` describe block: `'boundary: just inside the cone passes on the RA axis (cos(Dec) weighting)'` and `'boundary: diagonal RA+Dec offset at cone edge'`.\n- Tests would fail if `angularSeparationDeg` were \"simplified\" to drop the `cos(Dec)` weighting on the RA delta.\n- `npm test` green; `npm run typecheck` clean.\n\n# Smoke\n\nPure test add — no production change.\n\n# Connects\n\n- `stellata-test-coverage-discipline` — boundary tests need to cover every dimension of the predicate.\n- `stellata-pattern-coverage-across-peers` — when `stellata-dch.55` (SMC kinematic) lands, its own cone test should follow the same pattern (RA + Dec + diagonal). This bead's tests are the template.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:39:14Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:39:14Z","labels":["build-pipeline","catalog","distance","pr-99"],"dependencies":[{"issue_id":"stellata-9mm.192.7","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-19T11:39:14Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.192.7","depends_on_id":"stellata-dch.55","type":"related","created_at":"2026-05-19T11:39:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.192.6","title":"MAX_DIST_PC vs LMC_DISTANCE_PC invariant: 406pc margin silently drops LMC if either shifts","description":"# Finding\n\n`scripts/build-catalog.ts` clamps the catalog at `MAX_DIST_PC = 50_000` (rows above this distance are dropped at `dropped.tooFar++`). `scripts/catalog-pure.ts` sets `LMC_DISTANCE_PC = 49_594`. Margin: 406 pc.\n\nThe LMC kinematic override (dch.8l0 / PR #99) snaps ~54 supergiants to exactly 49 594 pc. After the snap, the row goes through the standard `dist \u003e MAX_DIST_PC` cut. Today it just clears.\n\nFailure shapes this margin can't survive:\n- A future LMC distance refinement (Pietrzyński updates, or a DR4-anchored revision) lands an LMC distance ≥ 50 000 pc → the entire 54-star LMC supergiant population is silently dropped at the cut, with no error or counter to surface it.\n- A `MAX_DIST_PC` reduction (e.g. someone tunes it for performance, or for chart-mode framing) below 49 594 pc silently drops the LMC population.\n- Numerical noise from the override is impossible at LMC (the override hard-snaps to a literal constant), but the same pattern will hit SMC (dch.55 — ~62 kpc, well over MAX_DIST_PC) unless the cut is also relaxed for that population.\n\nThe cut on `MAX_DIST_PC` exists for a reason (rendering bounds + 1/π inversion artifact filter), so the fix isn't \"remove the cut\" — it's \"add a test that pins the invariant `LMC_DISTANCE_PC \u003c MAX_DIST_PC` and fails loudly if either drifts.\" Otherwise the dependency between the two constants is invisible.\n\n# Why this matters now\n\n`stellata-dch.55` (SMC kinematic distance refinement) is queued. SMC distance ≈ 62 kpc, well above `MAX_DIST_PC = 50_000`. That bead WILL collide with this cut unless the relationship is made explicit. The test that pins LMC's invariant should generalise to \"every kinematic-override target distance must be \u003c MAX_DIST_PC, OR MAX_DIST_PC must be raised.\"\n\n# Scope (one commit/PR, test-only)\n\n1. In `scripts/catalog-pure.test.ts` (or a new top-level invariant test file if cleaner), add:\n   - `expect(LMC_DISTANCE_PC).toBeLessThan(MAX_DIST_PC)` — imports MAX_DIST_PC from build-catalog.ts (lift it to module scope if not already exported, or move it into catalog-pure.ts beside the override constants).\n   - Comment on the test naming the failure mode it guards against: \"LMC stars get silently dropped at the \u003e MAX_DIST_PC cut if either constant drifts.\"\n\n2. (If lifting MAX_DIST_PC across the script boundary is awkward, the alternative is to colocate the cut threshold next to the override constants in catalog-pure.ts so the relationship is visible at the point of definition. Picking either factoring is fine; the test is the load-bearing artifact.)\n\n3. Forward-looking note in `docs/build-and-data.md` distance-override section: \"Any kinematic-override target distance must satisfy `dist \u003c MAX_DIST_PC`; the test in catalog-pure.test.ts pins this for the LMC layer.\" This pre-warns the dch.55 SMC bead.\n\n# Acceptance\n\n- New invariant test passes; would fail if either `LMC_DISTANCE_PC` were raised above 50 000 pc, or `MAX_DIST_PC` lowered below 49 594 pc.\n- `npm test` + `npm run typecheck` + `npm run build:catalog` all clean.\n- `grep -n \"MAX_DIST_PC\" scripts/` confirms the constant is reachable from the test (exported, or relocated).\n\n# Smoke\n\nPure cleanup + test add — no user-visible change.\n\n# Connects\n\n- `stellata-test-coverage-discipline` — invariant assertion for \"headline relationship between two constants that the code silently depends on.\"\n- `stellata-dch.55` (SMC kinematic) — the imminent peer that WILL hit this; the test forces the relationship to be confronted at that bead's time, not silently dropped.\n- `stellata-dch.56` (LMC structural-disc fallback distance) — same risk shape.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:38:55Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:38:55Z","labels":["build-pipeline","catalog","distance","pr-99"],"dependencies":[{"issue_id":"stellata-9mm.192.6","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-19T11:38:55Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.192.6","depends_on_id":"stellata-dch.55","type":"related","created_at":"2026-05-19T11:39:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.192.5","title":"catalog-pure.ts LMC parameter citations: vdM\u0026K14 COM/centroid confusion + sexagesimal/decimal mismatch","description":"# Finding\n\nThe LMC kinematic-override parameter block in `scripts/catalog-pure.ts` (around the `LMC_CENTRE_*` / `LMC_PM_*` constants) carries two citation drifts that a reader trusts at face value:\n\n1. **PM values vs the cited paper.** The block uses `LMC_PM_RA_CENTRE = 1.85`, `LMC_PM_DEC_CENTRE = 0.20` mas/yr, and the comment cites van der Marel \u0026 Kallivayalil 2014 (ApJ 781, 121) \"centre of mass bulk proper motion\" as `μ_α* cos δ ≈ +1.91 ± 0.02, μ_δ ≈ +0.23 ± 0.05 mas/yr`. The (1.85, 0.20) values are NOT what vdM\u0026K14 publish for the COM. They look like a centroid-frame value (raw per-star PM mean before correcting for the LMC's internal rotation around its own COM). The centroid-frame value is the RIGHT choice for filtering individual stars on a PM gate, so the code is correct — but the citation is misleading: it says \"centre of mass bulk motion\" when the values are the unrotated centroid.\n\n2. **Sexagesimal/decimal mismatch on the centre coordinate.** The comment reads:\n   `LMC photometric centre: (RA, Dec) ≈ (05h 23m 34s, −69° 45′) = (78.76°, −69.19°).`\n   - `05h 23m 34s` converts to `80.892°`, not `78.76°`.\n   - `−69° 45′` converts to `−69.75°`, not `−69.19°`.\n   The DECIMAL values (78.76°, −69.19°) are what's actually used in code via `LMC_CENTRE_RA_HOURS = 5.25067`, and they match SCIENCE.md. The SEXAGESIMAL form in the code comment is just wrong — it appears to be the more commonly-cited NED/SIMBAD centre, not the centre actually applied.\n\nBoth shapes are `stellata-rename-and-stale-prose-sweep` findings — code comments that make factual claims a future reader would act on, where the claim doesn't survive a paste-compute check.\n\n# Why this matters\n\nConnects to `stellata-data-fidelity-principle`: an astronomer reading the LMC override block has to be able to trust the citation. Right now they would (a) discover the (1.85, 0.20) numbers don't match the vdM\u0026K14 paper they trust, then (b) discover the sexagesimal-to-decimal conversion fails — at which point the credibility of the whole override layer is in question.\n\nAlso catches a real risk for the queued `stellata-dch.55` (SMC kinematic): if SMC parameters get cited the same way, the same drift propagates.\n\n# Scope (one commit/PR, comment-only)\n\nIn `scripts/catalog-pure.ts`, in the `// LMC kinematic parameters. References:` block:\n\n1. Correct the vdM\u0026K14 attribution: state explicitly that (1.85, 0.20) are the LMC's centroid-frame PM (mean of stellar PMs across the LMC field) — not the COM motion. Cite the source the (1.85, 0.20) values came from; if it was vdM\u0026K14 read as the centroid pre-COM-correction, say that. If it was a different paper (Kallivayalil 2013 / Helmi 2018 GaiaDR2 / etc.), cite that instead. The values must match the citation.\n2. Fix the sexagesimal/decimal inconsistency on the centre line. Either:\n   - keep the decimal values (78.76°, −69.19°) and update the sexagesimal to the matching values (~05h 15m 02s, −69° 11′ 24″), OR\n   - keep the sexagesimal (05h 23m 34s, −69° 45′) and update the decimal + the `LMC_CENTRE_RA_HOURS` / `LMC_CENTRE_DEC_DEG` constants accordingly.\n   Pick whichever is the actually-cited source of the LMC centre in the design (likely the decimals, since they match the existing SCIENCE.md text and the tests' fixture data).\n3. SCIENCE.md sweep: re-read the LMC kinematic refinement paragraph and confirm the citations there match the code's corrected citations (currently SCIENCE.md is consistent with the decimals).\n\n# Acceptance\n\n- `grep -n \"vdM\\|van der Marel\\|Kallivayalil\\|Pietrzy\" scripts/catalog-pure.ts SCIENCE.md` — every citation matches a paper that publishes the values used in code.\n- `paste-compute` check passes: every sexagesimal in the LMC block converts to the decimal values quoted alongside it.\n- `npm test` green (no test changes needed — values unchanged unless the fix path is option 2 above).\n- `npm run typecheck` clean.\n\n# Out of scope\n\n- Re-tuning the PM tolerance or centre values themselves — that lives in `stellata-dch.54` (widen Dec PM tolerance).\n- Replacing the centroid-frame gate with a COM-frame gate — that would be a different physics decision.\n\n# Connects\n\n- `stellata-rename-and-stale-prose-sweep` — code-comment factual claims that drift from the cited paper / from the decimal-vs-sexagesimal pair.\n- `stellata-data-fidelity-principle` — every external citation in code must point to a paper that publishes the value used.\n- `stellata-dch.55` (SMC kinematic) — same risk shape on a sibling layer; will need the same citation discipline.\n- `stellata-dch.42` (Phase 6 SCIENCE.md rewrite) — eventual SCIENCE.md sweep; this fix lands earlier (catches a code-level drift the SCIENCE.md sweep wouldn't necessarily reach).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:38:32Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:38:32Z","labels":["build-pipeline","catalog","distance","pr-99"],"dependencies":[{"issue_id":"stellata-9mm.192.5","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-19T11:38:32Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.192.5","depends_on_id":"stellata-dch.55","type":"related","created_at":"2026-05-19T11:39:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.192.4","title":"Override PRs should enumerate row-population partitions in PR description + per-partition build-counts assertions","description":"# Finding\n\nPR #98 (stellata-dch.47, Bailer-Jones distance override) shipped without a `dist_src` eligibility filter — it overrode every AT-HYG row carrying a Gaia DR3 source_id, including rows whose AT-HYG `dist_src` was HIP / GJ / N / OTHER. For ~11+ stars with low-S/N Gaia parallaxes but a canonical non-Gaia distance, this silently regressed their position onto Bailer-Jones's smooth Galactic-density prior (~10–40 kpc at mid-latitudes), placing them outside the disc.\n\nPR #99 (stellata-dch.8l0) added `BJ_ELIGIBLE_DIST_SRCS = {'G_R3', 'G_R2'}` + `isBailerJonesEligible` and re-gated the override. Bug gone, but the **process gap** that let it land in the first place is real and worth a small write-time control.\n\nThe shape — applying a transform that fires on a column-keyed population (here: rows with `gaia` non-empty) without enumerating how the population partitions under *other* columns that should gate the transform (here: `dist_src`) — is exactly [[stellata-pattern-coverage-across-peers]].\n\n# Why this matters going forward\n\nThe dch epic has three more override layers queued — `stellata-dch.55` (SMC kinematic), `stellata-dch.56` (LMC structural-disc fallback), `stellata-dch.58` (OGLE Cepheid cross-match). Each fires on a different population partition. Without an explicit row-population enumeration discipline, each is a fresh opportunity for the same shape of regression: a population we forgot to think about gets silently rerouted onto a prior whose semantics we never validated for that population.\n\n`stellata-dch.57` (Distance-regression QA: SIMBAD + self-consistency build gate, P2) is the **after-the-fact** detector. This bead is the **before-the-fact** preventer.\n\n# Scope (one commit/PR — small)\n\nTwo complementary deliverables, both small:\n\n1. **Authoring discipline — add to the override-PR template.** A short paragraph in `docs/build-and-data.md` (in the section that documents the distance override stack) listing the row-population partitions any new override layer must enumerate in its PR description: AT-HYG `dist_src` × `gaia` × override-source-availability. Each partition must have a stated outcome (overridden / preserved / dropped). The PR description must include the per-partition count from a dry run, **and** the matching assertion must land in `scripts/build-counts.test.ts`.\n\n2. **Per-`dist_src` partition assertion in `build-counts.test.ts`.** Extend `BuildCounts` (and `build-catalog-expected.json`) with at minimum:\n   - `bjOverriddenByDistSrc`: a `Record\u003c'G_R3' | 'G_R2' | 'HIP' | 'GJ' | 'N' | 'OTHER', number\u003e` (zeros for the non-eligible sources).\n   - A vitest assertion that pins each entry. The HIP / GJ / N / OTHER entries must be 0 — this is the assertion that would have caught the dch.47 bug at write time.\n   The same partition shape applies forward to LMC / SMC / Cepheid override layers; pick a structure that generalises.\n\n# Acceptance\n\n- `docs/build-and-data.md` carries the override-PR authoring-discipline paragraph.\n- `BuildCounts` extended; `build-catalog-expected.json` snapshot extended; vitest passes with the HIP/GJ/N/OTHER entries pinned to 0.\n- A retroactive sanity-check: confirm the assertions would have failed against pre-PR-99 catalog state (i.e. the assertion has teeth).\n- [[stellata-pattern-coverage-across-peers]] memory updated with the dch.47/99 case as a representative example (memory grooming, one paragraph).\n\n# Smoke\n\n- `npm test` + `npm run typecheck` + `npm run build:catalog`. No user-visible scene change.\n\n# Connects\n\n- `stellata-dch.57` — related (after-the-fact regression detector; this bead is the write-time complement). Link via `bd dep add` `--type=related`.\n- [[stellata-pattern-coverage-across-peers]] — the underlying authoring rule.\n- [[stellata-test-coverage-discipline]] — extends `build-counts.test.ts` with per-partition headline assertions, the canonical pattern.\n- `stellata-dch.55`, `stellata-dch.56`, `stellata-dch.58` — the imminent override layers that should use the new authoring discipline.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:25:15Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:25:15Z","labels":["build-pipeline","catalog","distance","pr-98","pr-99"],"dependencies":[{"issue_id":"stellata-9mm.192.4","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-19T11:25:14Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.192.4","depends_on_id":"stellata-dch.57","type":"related","created_at":"2026-05-19T11:25:21Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.59","title":"Phase 2 follow-up: WDS notes 'primary/secondary' synonym anchoring in tier 2","description":"Part of the source-ID-anchored catalogue-pipeline rewrite (see parent epic for architecture). Follow-up to stellata-dch.28.\n\n# Why\n\nStage 2 tier-2 (``wds_notes_hip``) currently honours only explicit component-letter context for HIP tokens in WDS Notes prose — patterns ``Component X is HIP NNN`` / ``X is HIP NNN`` / ``HIP NNN = X``. Many real WDS Notes use the synonyms ``primary`` / ``secondary`` / ``companion`` instead — e.g. α Cen's note reads ``Difficult for Hipparcos with secondary (= HIP 71681)``. Today these tokens are dropped (the bare ``HIP 71681`` without an explicit letter anchor is ambiguous).\n\nThe synonym CAN be disambiguated against the WDS pair's ``components`` field: ``AB`` → primary=A, secondary=B; ``AC`` → primary=A, secondary=C; ``Aa,Ab`` → primary=Aa, secondary=Ab. The parser doesn't carry the pair context at note-load time, so the binding has to happen at resolve time (parser returns a synonym → HIP map, resolver picks the right letter from the pair).\n\n# Scope\n\nExtend ``parse_wds_notes`` to ALSO emit a synonym dict alongside the existing letter dict: ``{wds_id: {\"primary\": HIP, \"secondary\": HIP, \"companion\": HIP}}`` for prose like ``primary is HIP NNN``, ``secondary (= HIP NNN)``, ``HIP NNN is the secondary``. Then in ``resolve_component``, after the explicit-letter lookup misses, consult the synonym dict using the pair's ``components`` field to resolve ``primary`` and ``secondary`` to actual letters.\n\nConservative scope:\n- Only handle ``primary``/``secondary`` synonyms (skip ``companion`` — it doesn't disambiguate from secondary in many pairs).\n- Only when the pair has exactly 2 components (binary, not triple+).\n- Match against pairs where ``components`` splits cleanly via ``split_components``.\n\n# Expected coverage uplift\n\nManual sample (α Cen, Sirius, Procyon, Castor) suggests low-hundreds of systems will benefit. Bigger systems are already covered by tier 4 (PM-propagated position match, ``stellata-dch.29``/``.30``) so this isn't critical-path — file as P3.\n\n# Files to touch\n\n- ``scripts/build-binaries.py`` — ``parse_wds_notes`` returns ``(letter_dict, synonym_dict)``; ``resolve_component`` consults synonyms after letter miss.\n- ``scripts/build-binaries.test.py`` — fixture covering primary/secondary synonym patterns and the disambiguation against ``components``.\n\n# Smoke\n\n- ``python3 scripts/build-binaries.test.py``\n- ``npm run build:binaries -- --force`` — wds_notes_hip count should rise by 100+ from current baseline.\n- Spot-check α Cen (WDS 14396-6050): notes synonym ``secondary (= HIP 71681)`` should now resolve component B (in pair AB) and component C (in pair AC) to HIP 71681's gaia source_id.","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:24:15Z","created_by":"Alex Marshall","updated_at":"2026-05-19T12:09:25Z","closed_at":"2026-05-19T12:09:25Z","close_reason":"Superseded by stellata-dch.60 — SIMBAD-backed tier 2 supplement replaces the regex-based prose parsing approach (deferred from PR #100 review).","labels":["build-binaries","cross-match","phase-2"],"dependencies":[{"issue_id":"stellata-dch.59","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-19T11:24:14Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.59","depends_on_id":"stellata-dch.28","type":"related","created_at":"2026-05-19T11:24:18Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.59","depends_on_id":"stellata-dch.60","type":"supersedes","created_at":"2026-05-19T13:09:24Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.192.2","title":"PR 98/99 cleanup: remove unfulfilled DIST_SRC_* scaffolding + dead branch + test tautology in catalog-pure.ts","description":"# Why\n\nPR 98 introduced `DIST_SRC_BAILER_JONES = 'BJ'` (and PR 99 added `DIST_SRC_LMC_KIN = 'LMC_KIN'`) as part of a distance-provenance plan that was never followed through: the override block recomputes `dist/x/y/z/absmag` but never tags which source the new value came from. The Star struct has no `dist_src` field; v4 RECORD_LAYOUT has no slot for it; v6 (dch.33) plans only `gaia_source_id` surfacing; v6→v7 Apsis (dch.49) plans Teff/logg/[M/H]/A0 only. **Distance provenance is not on the dch roadmap.**\n\nThe result is misleading scaffolding:\n- `DIST_SRC_BAILER_JONES` / `DIST_SRC_LMC_KIN` are exported but appear only in build-time `console.log` strings and one self-referential test that asserts `DIST_SRC_BAILER_JONES === 'BJ'`.\n- `applyBailerJonesOverride`'s JSDoc says \"The caller swaps these into the star record and tags `dist_src = 'BJ'`.\" — no such tagging happens.\n- A future reader hunting for where overridden stars are flagged will spend time deriving \"the tagging never landed.\"\n\nTwo additional small cleanups in the same area, in the same commit since they're all one-touch:\n\n- **Dead branch in `parseBailerJonesTsv`**: `if (lines.length === 0) return out;` is unreachable. `''.split(/\\r?\\n/)` returns `['']` (length 1) in Node — confirmed empirically. An empty TSV takes the missing-columns throw path.\n- **Test tautology in `catalog-pure.test.ts`**: the tier-A fixture suite's \"pins the BJ distance and recomputes x/y/z + absmag consistently\" case asserts `expect(out!.absmag).toBeCloseTo(f.mag - 5 * Math.log10(f.bjDist / 10), 10);` — this is the function's defining formula asserted against itself. Catches a mag↔dist swap, doesn't catch the formula being wrong. The adjacent `apparentToAbsoluteMagnitude` test already pins known values (e.g. m=15, d=100 → M=10).\n\n# Scope (one commit/PR)\n\n1. Remove `DIST_SRC_BAILER_JONES` export from `scripts/catalog-pure.ts`.\n2. Remove `DIST_SRC_LMC_KIN` export from `scripts/catalog-pure.ts`.\n3. Update `scripts/build-catalog.ts` log lines (around lines 794 / 801) to inline the literal strings or drop the `dist_src='…'` framing entirely — the log line's job is just human-readable progress.\n4. Remove the JSDoc clause on `applyBailerJonesOverride` claiming the caller tags `dist_src = \"BJ\"`. Same for `applyLmcKinematicOverride`.\n5. Remove the self-referential `expect(DIST_SRC_BAILER_JONES).toBe('BJ')` test in `scripts/catalog-pure.test.ts`.\n6. Remove the dead `if (lines.length === 0) return out;` branch in `parseBailerJonesTsv`.\n7. Remove the tautological `expect(out!.absmag).toBeCloseTo(f.mag - 5 * Math.log10(f.bjDist / 10), 10);` line in the tier-A fixture suite. Leave the `out!.dist` and `sqrt(x²+y²+z²)` assertions as-is (those pin external numbers).\n\n# Acceptance\n\n- `npm test` green (1087+ tests).\n- `npm run typecheck` clean.\n- `npm run build:catalog` runs end-to-end; the build-time log lines still report override coverage in human-readable form.\n- `grep -nE \"DIST_SRC_(BAILER_JONES|LMC_KIN)\" scripts/ src/` returns zero hits.\n- `grep -n \"tags \\`dist_src\" scripts/` returns zero hits.\n\n# Why this isn't deferred\n\nThis is cleanup of misleading code that already shipped. The longer it sits, the higher the chance a future override PR (dch.55 SMC, dch.56 LMC structural-disc, dch.58 OGLE Cepheid) copies the same pattern and adds a third / fourth dead constant. Catching it now is one commit; catching it after three more layers ships is a sweep.\n\nIf distance provenance ever needs to land in the runtime binary, the bead that lands it can re-introduce the constants alongside the binary-format change. Speculative scaffolding before that decision is taken is review-blocking under [[stellata-named-constants-and-dry]].\n\n# Smoke\n\n- `npm test` + `npm run typecheck` + `npm run build:catalog`. No user-visible scene change expected. Pure cleanup.\n\n# Connects\n\n- [[stellata-named-constants-and-dry]] — write-time discipline against unfulfilled scaffolding.\n- [[stellata-rename-and-stale-prose-sweep]] — the unfulfilled docstring is the same shape of drift.\n- [[stellata-test-coverage-discipline]] — the tautological assertion is the \"test that doesn't test anything\" anti-pattern.\n- Sibling DRY refactor bead (filed in same review pass) — shared DistanceOverride interface; lands first, this cleanup follows.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:24:12Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:24:12Z","labels":["build-pipeline","catalog","distance","pr-98","pr-99"],"dependencies":[{"issue_id":"stellata-9mm.192.2","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-19T11:24:11Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.192.2","depends_on_id":"stellata-9mm.192.3","type":"related","created_at":"2026-05-19T11:25:22Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202.9","title":"Small consistency fixes: Bin.mod_K assertion + ORB6 grade fallback log","description":"Two small fixes bundled into one bead (each is ~3 lines).\n\n**D8 — `Bin.mod_K \u003e 0` not asserted.** `refresh-simbad-sample.py:101` — a typo `mod_K=0` in BINS would divide-by-zero in ADQL or hang silently.\n\nFix: add `assert b.mod_K \u003e= 1` in `Bin.__post_init__` or at the top of `fetch_bin_candidates`.\n\n**D9 — ORB6 grade fallback silently swallows malformed grades.** `build-binaries.py:326`: `grade=int(grade_str) if grade_str.isdigit() else 5`. A garbage grade (e.g. `\"X\"` or `\"?\"`) gets the worst grade silently. Downstream Stage 4 will deprioritise this row but won't know it was malformed.\n\nFix: log a count of fallback-grade rows after parsing (`log(f\"ORB6: {n_fallback} rows had non-numeric grade — coerced to 5\")`).\n\n---\nNote: D6 (`_WDS_NOTES_HIP_RE` too broad) was dropped from this bead. The PR #100 review re-scoped Stage 2's tier 2 entirely: the regex-based WDS notes prose parser was removed in favour of a SIMBAD-backed approach tracked in stellata-dch.60. The old `_WDS_NOTES_HIP_RE` no longer exists.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:07:23Z","created_by":"Alex Marshall","updated_at":"2026-05-19T12:09:45Z","labels":["pr-96","pr-97"],"dependencies":[{"issue_id":"stellata-9mm.202.9","depends_on_id":"stellata-9mm.202","type":"parent-child","created_at":"2026-05-19T11:07:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202.8","title":"refresh-bailer-jones: add richer per-batch / coverage report (parity with refresh-gaia-apsis)","description":"`refresh-bailer-jones.py` prints a one-line summary (`matched X/Y = Z%`). `refresh-gaia-apsis.py:report_coverage` prints a 4-line breakdown (rows / phot-only / spec-only / union).\n\nAsymmetric observability across the suite makes triage harder (\"which script tells me what again?\"). A future \"why did this script return only 91% coverage?\" debug session has to learn each script's reporting layer separately.\n\n## Acceptance\n\nEither (a) lift a `report_coverage` helper to `refresh_lib` and have both scripts call it (the only differing axis is which columns count as \"populated\"), or (b) duplicate Apsis's pattern locally in bailer-jones (`{r_med_geo} non-null, {r_med_photogeo} non-null, union`).\n\nOption (a) preferred if bead 9mm.201's refactor lands first; option (b) acceptable as a standalone.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:07:10Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:07:10Z","labels":["pr-92"],"dependencies":[{"issue_id":"stellata-9mm.202.8","depends_on_id":"stellata-9mm.202","type":"parent-child","created_at":"2026-05-19T11:07:10Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202.7","title":"Refresh scripts: row-count slack consistency + coverage-floor rationale comments","description":"Two consistency issues across the refresh-script suite.\n\n**M8 — coverage floors are aspirational, not justified.** `refresh-bailer-jones.py:74` pins `EXPECTED_COVERAGE_MIN = 0.90` (empirical 98.7%). `refresh-simbad-sample.py:123` pins `GAIA_COVERAGE_MIN = 0.95` (empirical 96.7%). `refresh-gaia-apsis.py:111` pins `EXPECTED_UNION_COVERAGE_MIN = 0.80` (empirical 84.8%). The slack is reasonable, but the rationale isn't pinned anywhere — if coverage falls to 91%, is that \"still fine\" or \"investigate\"?\n\n**M9 — row-count slack is inconsistent across \"frozen catalog\" scripts.** `refresh-gaia-hip-xmatch` allows ±0.2% (tightest). Tycho-xmatch ±0.4%. NSS ±0.7%. HIP2 ±1.3%. DR3 is frozen, but VizieR/ESA mirror re-indexes have historically caused 100-row-class drift. The HIP xmatch is 3-7× tighter than its peers without a documented reason.\n\n## Acceptance\n\n1. **For M8:** one-line comment per coverage floor stating the historical low and why the floor sits N% below it. E.g. `EXPECTED_COVERAGE_MIN = 0.90  # empirical 98.7%; floor at 90% absorbs VizieR mirror drift up to 8.7%`.\n2. **For M9:** review all `EXPECTED_ROW_COUNT_{MIN,MAX}` constants across the suite. Adopt a consistent ~1% slack as default unless there's a specific documented reason to go tighter. Each tight bound carries a one-line comment explaining the rationale.\n\nPure-comment change; no behaviour shift.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:07:04Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:07:04Z","labels":["pr-90","pr-91","pr-92","pr-93","pr-94","pr-95"],"dependencies":[{"issue_id":"stellata-9mm.202.7","depends_on_id":"stellata-9mm.202","type":"parent-child","created_at":"2026-05-19T11:07:03Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202.6","title":"refresh_lib: _dtype_matches pandas branch + coerce_masked broader coverage","description":"Two latent gaps in `refresh_lib` discovered in the retrospective on PRs 90-97.\n\n**M6 — `_dtype_matches` doesn't handle pandas Extension dtypes.** The docstring (`refresh_lib.py:168`) claims pandas DataFrame support, but the type-matching logic only branches on numpy `np.dtype` and Python `type`. A pandas `StringDtype()` or `Int64Dtype()` would fall through to `dtype in wants` and likely fail. None of the current callers use pandas, so this is latent.\n\n**M7 — `coerce_masked` coverage is narrow.** `refresh_lib.py:326-344` checks `value is np.ma.masked` and `isinstance(value, np.ma.core.MaskedConstant)`. Astropy can also produce `MaskedArray` scalars with `.mask == True` (depending on column dtype and access pattern). The existing tests pin the cases that currently surface, but a future astropy upgrade might return masked values via a different code path and `coerce_masked` would pass them through as-is.\n\n## Acceptance\n\n1. For M6: either add the pandas Extension-dtype branch to `_dtype_matches` (handling StringDtype / Int64Dtype / Float64Dtype) OR remove the pandas mention from the docstring. Choose based on whether we expect a pandas-DataFrame consumer in the next 6 months.\n2. For M7: add a fallback in `coerce_masked` for \"anything with a `.mask` attribute whose `bool(.mask) == True`\" (best-effort). Add a test exercising a `MaskedArray`-element scalar (not just the `np.ma.masked` constant).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:06:53Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:06:53Z","labels":["pr-88"],"dependencies":[{"issue_id":"stellata-9mm.202.6","depends_on_id":"stellata-9mm.202","type":"parent-child","created_at":"2026-05-19T11:06:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202.5","title":"build-binaries: per-parser cardinality bounds (currently log-only)","description":"The refresh scripts all have row-count bounds. `build-binaries.py`'s parsers do not — each just `log(f\"loaded N rows\")` and continues. If AT-HYG is corrupted with 10M rows (or 100 rows), the script slurps it all into RAM and reports the cardinality without flagging.\n\n## Acceptance\n\nAdd an expected-cardinality bound to each parser in `build-binaries.py`:\n\n- `parse_athyg`: ~310k-320k rows (AT-HYG v3.3 classic-IDs subset)\n- `parse_wds_summ`: ~155k rows\n- `parse_orb6`: ~3,500 rows\n- `parse_gcvs`: ~58k rows\n- `parse_ccdm`: ~5,000 rows (overlap with bead M1)\n- `parse_hip2`: ~118k rows (already row-count-bounded upstream in `refresh-hipparcos2`, but worth pinning here too as defence-in-depth)\n\nRaise `SystemExit` on out-of-band; widths ~10% per side to absorb minor catalogue maintenance.\n\nCoordinate with bead 9mm.201 — `assert_row_count` helper applies equally well here once lifted.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:06:45Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:06:45Z","labels":["pr-97"],"dependencies":[{"issue_id":"stellata-9mm.202.5","depends_on_id":"stellata-9mm.202","type":"parent-child","created_at":"2026-05-19T11:06:44Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202.4","title":"refresh-simbad-sample: validate schema for every bin, not just the first","description":"`refresh-simbad-sample.py:381-382` only schema-validates the first bin's candidates table. The other three bins (each with a different WHERE — including the MOD-K subsample) skip validation.\n\nIf SIMBAD's query planner returns a different schema for the MOD-K branch (unlikely but possible), no check catches it. Pattern matches the bailer-jones one-time check, but the risk is higher here because the bins have **different WHERE clauses**.\n\n## Acceptance\n\nCall `rl.validate_schema(table, BASIC_SCHEMA, label=f\"SIMBAD basic+allfluxes bin {i+1}\")` for every bin, not just `i == 0`. Cost is negligible vs. the multi-minute TAP query per bin.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:06:26Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:06:26Z","labels":["pr-96"],"dependencies":[{"issue_id":"stellata-9mm.202.4","depends_on_id":"stellata-9mm.202","type":"parent-child","created_at":"2026-05-19T11:06:25Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202.3","title":"Batched refresh scripts: lose all progress on mid-run network failure","description":"`refresh-bailer-jones.py` and `refresh-gaia-apsis.py` run ~60-70 batches each (5000 IDs per batch). Apsis takes tens of minutes per the bead notes. A network drop on batch 65 of 70 throws away every preceding batch — back to batch 1.\n\n## Acceptance\n\nA simple incremental TSV write: batch N appended to a `.tmp.batch-N` file, with a final merge step. On re-run, detect existing `.tmp.batch-*` files and resume from the next batch.\n\nAlternative shape: write directly to the final `.tmp` file with one row per batch flushed; the existing atomic-write swap (bead 9mm.196) then takes care of the final commit.\n\n## Notes\n\nLower priority — operationally manageable today via re-run. But: DR4 may double the dataset size and re-running a 1-hour query because batch 65 timed out is a real footgun.\n\nCoordinate with bead 9mm.196 (atomic write) and 9mm.201 (lift `run_in_batches`) so this lands cleanly inside the helper.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:06:22Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:06:22Z","labels":["pr-92","pr-95"],"dependencies":[{"issue_id":"stellata-9mm.202.3","depends_on_id":"stellata-9mm.202","type":"parent-child","created_at":"2026-05-19T11:06:21Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202.2","title":"refresh-simbad-sample: build_output_row assumes v_mag is never null","description":"`refresh-simbad-sample.py:326` does `float(rl.coerce_masked(row[\"v_mag\"]))` with no guard. The `f.\"V\" IS NOT NULL` WHERE clause is the only guarantee.\n\nIf SIMBAD's TAP ever returns a NULL for a row that passed the filter (cold cache, planner quirk, mid-query upstream change), this crashes with `TypeError: float() argument must be a string or...`. The exception would surface (so not silent corruption), but the script would die mid-write after writing the header — at which point sibling bead 9mm.196 (atomic write) is the safety net.\n\n## Acceptance\n\nBelt-and-braces: `if v_mag is None: continue` (or raise an explicit `SystemExit` with the offending oid) before any per-row math.\n\nTest: fixture where one selected row has `v_mag=None` → either the row is skipped with a logged warning OR the script raises with a clear message naming the oid.","status":"open","priority":3,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:06:14Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:06:14Z","labels":["pr-96"],"dependencies":[{"issue_id":"stellata-9mm.202.2","depends_on_id":"stellata-9mm.202","type":"parent-child","created_at":"2026-05-19T11:06:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202.1","title":"build-binaries: CCDM parser sentinel detection is fragile + no row-count bound","description":"`parse_ccdm` (`build-binaries.py:421-424`) waits for a line starting with `------` to flip `in_data`. If VizieR changes the separator to `======` or removes the dashes entirely, `in_data` stays False and the function returns `[]` — no error, no warning.\n\nCombined with no row-count bound (CCDM has ~5,000 rows expected), this could go undetected.\n\n## Acceptance\n\n1. Either (a) detect the data block by a more robust heuristic — e.g. \"first row whose first tab-separated field parses as int\" — or (b) keep the dash sentinel but assert `len(rows) \u003e 0` post-parse with a \"CCDM sentinel detection failed; check VizieR file format\" diagnostic.\n2. Add an expected-row-count bound (~4500-5500 based on current ~5k) and raise on out-of-band.\n3. Test: fixture with a `======` separator → either parses correctly (option a) or raises with the diagnostic (option b).","status":"open","priority":3,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:06:09Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:06:09Z","labels":["pr-97"],"dependencies":[{"issue_id":"stellata-9mm.202.1","depends_on_id":"stellata-9mm.202","type":"parent-child","created_at":"2026-05-19T11:06:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202","title":"Data-pipeline refresh-script robustness follow-ups","description":"Themed sub-epic of `stellata-9mm` collecting medium / low-severity findings from the retrospective on PRs 90-97 (data-pipeline refresh scripts + `build-binaries.py`).\n\nThe high-severity findings (silent-data-corruption bugs + the lift-into-`refresh_lib` DRY refactor) are filed as standalone siblings under `stellata-9mm`. This sub-epic gathers the residual robustness / observability / consistency items so they sit on the backlog as a coherent grooming queue rather than scattering across 9mm at large.\n\n## Children themes (see individual beads for detail)\n\n- CCDM sentinel detection + row-count bound (M1)\n- SIMBAD `v_mag` null-guard (M2)\n- Incremental batched-query checkpointing for long runs (M3)\n- Validate schema for every SIMBAD bin, not just the first (M4)\n- Parser cardinality bounds in `build-binaries` (M5)\n- `refresh_lib` internals: `_dtype_matches` pandas branch + `coerce_masked` broader coverage (M6+M7)\n- Row-count slack consistency + coverage-floor rationale comments (M8+M9)\n- Richer per-pipeline coverage report for bailer-jones (D5)\n- Small consistency fixes: WDS_NOTES_HIP regex, `Bin.mod_K` assertion, ORB6 grade fallback log (D6+D8+D9)\n- Pin Python 3.10+ requirement in `requirements-refresh.txt` (D10)\n\n## Priority\n\nP3 default — code-quality / observability / consistency, not user-visible. Individual children can bump to P2 if one becomes a blocker for in-flight `stellata-dch` work.","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:05:44Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:05:44Z","dependencies":[{"issue_id":"stellata-9mm.202","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-19T11:05:44Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.201","title":"refresh_lib: lift 5 helpers (read_athyg_source_ids, run_in_batches, validate_spot_rows, assert_row_count, write_tsv atomic)","description":"DRY refactor of the refresh-script suite per the retrospective architectural finding (PRs 90-97). Lifts the substantive duplicates into `refresh_lib` so each refresh script becomes a thin orchestration shell with no boilerplate. Sibling to the bugfix beads filed alongside — some helpers land via those (see Cross-references).\n\n## Why helpers, not a framework\n\nIn the retrospective we considered a full framework refactor (config-driven driver / class hierarchy). Rejected because the variation across refresh scripts is **semantic, not parametric** — each script has script-specific quirks (SIMBAD's divergent ADQL dialect; HIP2's misnamed `RArad`/`DErad`; Apsis's union-of-two-pipelines coverage; NSS's Thiele-Innes vs Campbell discriminant; SIMBAD's stratified MOD-then-local-random subsample). The right level of abstraction is helper-functions, not framework. After this lift, each script reads top-to-bottom as: \"here's the ADQL, here's the schema, here's the bounds, here's the spot pin, run it.\" That's the right amount of visible logic.\n\n## Five helpers (acceptance criteria)\n\n**1. `read_athyg_source_ids(path) -\u003e list[int]`** — verbatim duplicate between `refresh-bailer-jones` and `refresh-gaia-apsis`. Lifted to `refresh_lib` with the \"skip empty AND skip '0'\" convention baked in. Both callers replaced.\n\n  → **Lands via sibling bead H4+H5+D1+D7 (AT-HYG ingest consolidation).** Verify here that the helper sits in `refresh_lib` and both scripts import it.\n\n**2. `run_in_batches(client, query_template, ids, batch_size, *, log_label, schema_validate=None) -\u003e list[row]`** — combines:\n\n  - Batched IN-clause splitting (current `refresh-bailer-jones.py:167-181` + `refresh-gaia-apsis.py:307-323`)\n  - Per-batch timing + progress logging (currently copy-pasted as ~10-line blocks across 3 scripts incl. SIMBAD ident batching)\n  - First-batch schema validation hook\n  \n  Returns concatenated row list. Callers replace ~25 lines of orchestration with one helper call.\n\n**3. `validate_spot_rows(rows_by_id, specs, script_name)`** — unifies the spot-check pattern in `refresh-gaia-nss.py:204-244` and `refresh-gaia-apsis.py:197-232`. Per-field dispatch:\n\n  - `(expected, tol)` tuple → numeric ±tolerance match\n  - `None` → cell must be null/masked\n  - `str` → exact-string match\n  \n  Raises `SystemExit` with a delta-list (all mismatches in one failure message) per the current pattern. NSS's string-match branch is the parameterised difference.\n\n**4. `assert_row_count(n, low, high, label)`** — extracts the row-count guard currently copy-pasted across 6+ scripts. Makes the guard unit-testable in `refresh_lib.test.py` (currently a typo like `\u003c low` vs `\u003c= low` would slip through). Each refresh script's `if not (MIN \u003c= n \u003c= MAX): raise SystemExit(...)` collapses to one call.\n\n**5. `write_tsv` atomic + `is_up_to_date` auto-includes `refresh_lib.__file__`**\n\n  → **Lands via sibling P1 bead H1+H2.** Verify here that the behavioural change is in place and tests pass.\n\n## Net effect\n\nEstimated ~120 LoC removed from the refresh scripts. Each script reduces to ~70-90 lines from current ~120-430. The remaining lines are exactly the substance worth reading (ADQL, schema, bounds, spot pins, format quirks).\n\n## Cross-references\n\nSibling bugfix beads under `stellata-9mm`:\n\n- H1+H2 — atomic write + lib mtime tracking (lands helper #5)\n- H3 — xmatch angular_distance default (independent of this refactor)\n- H4+H5+D1+D7 — AT-HYG ingest consolidation (lands helper #1)\n- H6 — spot-checks for 3 scripts (consumes helper #3 after this lands; can land first with inline pattern)\n- H7 — WDS/ORB6 sanity nets (independent of this refactor)\n\nRecommended order: bugfix beads land first; this refactor consolidates afterwards. Helper #3 (`validate_spot_rows`) ideally lands before H6 so the new spot-checks consume the lifted helper.\n\n## Acceptance\n\n- All 5 helpers exist in `refresh_lib` with full docstrings + tests in `refresh_lib.test.py`.\n- All 7 refresh scripts updated to call helpers; line counts approximately as projected above.\n- Re-run `--force` on every refresh script; all TSVs byte-identical to pre-refactor.\n\n## Smoke\n\nForce-rebuild one TSV per script; diff against pre-refactor committed version → byte-identical.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:05:36Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:05:36Z","labels":["pr-88","pr-92","pr-95"],"dependencies":[{"issue_id":"stellata-9mm.201","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-19T11:05:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-mju","title":"[epic] Magellanic Cloud stellar-population overlay — LMC + SMC dedicated catalog ingest","description":"Long-term goal: render the LMC + SMC's actual stellar populations from dedicated extragalactic catalogues, replacing the sparse ~60-star AT-HYG approximation inside the Magellanic Cloud volumes.\n\n# Why this epic exists\n\nThe Stellata catalog is built from AT-HYG, which is a Milky-Way-centric stellar catalog with a hard brightness cut at G ~ 10-12.5. The LMC has billions of stars; AT-HYG sees ~60. The SMC has hundreds of millions; AT-HYG sees ~1-5.\n\nThe result: even with stellata-dch.8l0 (LMC kinematic distance refinement) + stellata-dch.55 (SMC kinematic) + stellata-dch.58 (OGLE Cepheid per-star) + stellata-dch.56 (LMC structural-disc fallback), the rendered LMC is a sparse cluster of ~57 supergiants on (or near) a 2D shell. The actual LMC has a structured disc + bar + 30 Doradus + halo, none of which AT-HYG can ever resolve.\n\nFor the camera-anywhere viewing model (see `stellata-camera-anywhere-perception` memory), this matters: a user warping to the LMC sees ~60 stars where they should see millions.\n\n# Approach (two-stage)\n\n## Stage A: Variable-star population overlay (OGLE-IV)\n\n- **Data**: OGLE-IV LMC + SMC Cepheids, RR Lyrae, LPVs / Miras.\n- **Stars**: ~4.7k LMC + 4.9k SMC Cepheids; 39k LMC + 6k SMC RR Lyrae; 91k LMC + 22k SMC LPVs. Total ~170k Magellanic variables.\n- **Distance**: per-star, ±1-5% via PL/PLZ relation (Cepheids best, RR Lyrae good, LPVs ~10%).\n- **Rendering**: instanced quads sharing the existing star pipeline, separate vertex stream loaded from a new binary (e.g. `public/magellanic-variables.bin`).\n- **Pros**: Real per-star distances, real positions, well-calibrated.\n- **Cons**: Variable-star sub-population only — misses the bulk of LMC/SMC stars (main sequence, RGB).\n\n## Stage B: Full stellar-population overlay (SMASH DR2)\n\n- **Data**: SMASH DR2 (Nidever et al. 2021, ApJ) — ugriz photometry for ~360M sources across LMC + SMC + Bridge + foreground.\n- **Stars**: ~10s of millions in the LMC volume after extracting LMC members via photometric / positional cuts.\n- **Distance**: photometric distance modulus per star via isochrone fitting / TRGB / MS-fitting. Precision ~10-20% per star (limited by reddening + age + metallicity degeneracies).\n- **Rendering**: similar instanced approach but scaled up to millions of stars. May need a tiled / LOD strategy.\n- **Pros**: Full LMC/SMC stellar populations including disc, bar, 30 Dor, tidal arm.\n- **Cons**: Distance per star is mediocre (~5 kpc uncertainty at 50 kpc). May produce a thick smear rather than thin structure for sub-populations. Massive data ingest (10s-100s of millions of rows).\n\n## Interaction with existing AT-HYG LMC subset\n\nTwo options:\n1. **Suppress AT-HYG LMC stars inside the overlay volume** — avoids double-counting. AT-HYG's ~57 supergiants are already in the overlay's catalog under different IDs.\n2. **Keep AT-HYG as the \"labeled bright members\" on top** — search, hover, focus, constellation cross-references all keep working for the named LMC supergiants (HD 268749, HDE numbers, named Cepheids). Overlay is the background population only.\n\nLean toward option 2: keep AT-HYG's labelled stars, render overlay as the diffuse background, give the overlay no search / focus interaction at first.\n\n# Children\n\nTo be filed when this epic is picked up:\n\n- Research / design-gate child: evaluate OGLE-IV vs SMASH vs VMC; pick a Stage A data source; design the per-star binary format; figure out the render-pipeline integration (instanced quads share with stars? separate pass? LOD?).\n- Stage A data ingest: refresh script + binary build for the chosen catalog.\n- Stage A renderer: per-star draws in the LMC/SMC volume.\n- Stage B research / design-gate: when Stage A is solid and the appetite for more depth is real.\n- Stage B data + renderer.\n\n# Why P3 (and an epic, not a task)\n\n- P3: refinement, not regression. The LMC currently renders as ~57 stars on a flat shell, which is a known limitation rather than a bug. dch.55 + dch.56 + dch.58 address per-star precision within that sparse population; this epic addresses the population SIZE.\n- Epic: 4-5 child beads, multi-PR, depends on data ingest + renderer changes + binary format extensions. Won't land in one commit.\n\n# Prerequisites\n\n- dch.8l0 (LMC kinematic) merged — gives us the LMC volume to overlay.\n- dch.55 (SMC kinematic) merged — gives us the SMC volume.\n- dch.58 (OGLE Cepheid cross-match) merged — proves the OGLE ingest plumbing.\n- v6+ binary format (dch.33) merged — needed if we want gaia_source_id on the overlay sources too.\n\nRealistically a 2027+ epic; filing now so the structure exists in the bd graph.\n\n# References\n\n- Soszyński et al. 2015 (OGLE-IV LMC Cepheids), 2016 (RR Lyrae), 2009 (LPVs)\n- Nidever et al. 2017 + 2021 (SMASH, SMASH DR2)\n- Cioni et al. 2011 (VMC)\n- Zaritsky et al. 2004 (MCPS)\n- Bonanos et al. 2009 (LMC luminous + variable stars)","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-19T09:41:08Z","created_by":"Alex Marshall","updated_at":"2026-05-19T09:41:08Z","labels":["build-pipeline","catalog","local-group"],"dependencies":[{"issue_id":"stellata-mju","depends_on_id":"stellata-8l0","type":"related","created_at":"2026-05-19T10:41:16Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-mju","depends_on_id":"stellata-dch.55","type":"related","created_at":"2026-05-19T10:41:17Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-mju","depends_on_id":"stellata-dch.58","type":"related","created_at":"2026-05-19T10:41:17Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.56","title":"Phase 3: LMC structural-disc fallback distance (where OGLE cross-match misses)","description":"Geometric per-star distance fallback for AT-HYG LMC members not caught by the OGLE Cepheid cross-match (filed separately). Lower-priority than OGLE because the structural disc model is geometric (~±2-3 kpc precision) where OGLE PL distances are physical (~±1 kpc per star).\n\n# Goal\n\nAfter dch.8l0 (LMC kinematic shell at flat 49.594 kpc) and OGLE Cepheid cross-match (per-star Cepheid distances for the ~5-15 Cepheids in AT-HYG's LMC subset), the remaining ~40-50 LMC members are B-supergiants with no per-star distance from any survey. Give those a per-star structural distance derived from the LMC's inclined-disc geometry, so the catalog renders the LMC's tilted shell instead of a flat sphere.\n\n# Method (inclined disc model)\n\nvan der Marel \u0026 Cioni 2001 / van der Marel 2002 / Choi et al. 2018 give:\n\n- Inclination: i ≈ 32° (range 24-35° depending on tracer)\n- Line-of-nodes PA: ≈ 122° (range 100-150°)\n- Distance to dynamical centre: d_0 = 49.594 kpc (Pietrzyński 2019 anchor)\n\nFor each (RA, Dec), project into the LMC disc plane, compute the line-of-sight depth offset Δd, set per-star distance = d_0 + Δd.\n\nExisting implementation lives in stellata-38m's LMC wireframe (`src/client/local-group/`). Build-catalog.ts side can either:\n- Duplicate the disc parameters in `catalog-pure.ts` (or `lmc-pure.ts` if we factor out).\n- Or share the local-group source if reasonable (cross-folder import already present elsewhere).\n\n# Priority\n\nP3 because:\n- OGLE cross-match (filed separately) is the precision-preferred approach and lands first.\n- Structural-disc precision (~3 kpc at 50 kpc) is meaningful only when the camera is close to the LMC; not visible from Sol viewpoint.\n- The 40-50 stars without OGLE coverage currently render at the flat anchor — that's a known limitation, not a regression vs pre-dch.8l0 (pre-dch.8l0 they were smeared 5-200 kpc).\n\nBump to P2 if the long-term LMC stellar-population overlay epic gets deferred and the structural-disc fallback becomes the visible LMC shape for an extended period.\n\n# Acceptance\n\n- Each LMC_KIN star not also caught by OGLE ends up at a per-star distance within ±3 kpc of 49.594 kpc, derived from the disc model.\n- The 54-57 LMC stars now form a tilted disc, not a flat shell, when viewed from a position near the LMC.\n- Disc orientation matches the stellata-38m LMC wireframe layer (visual consistency).\n- Tests pin per-star distance at SE/NW/centre fixtures.\n\n# Out of scope\n\n- OGLE Cepheid cross-match (filed separately).\n- Full LMC stellar-population overlay (filed as separate epic).\n- SMC structural disc — SMC has more complex 3D structure; defer until SMC kinematic layer (dch.55) lands.\n\n# References\n\n- van der Marel \u0026 Cioni 2001, AJ 122, 1807 — original inclined-disc model\n- van der Marel 2002, AJ 124, 2639 — refined parameters\n- Choi et al. 2018, ApJ 866, 90 — modern SMASH disc model\n- Pietrzyński et al. 2019, Nature 567, 200 — distance anchor","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T09:29:31Z","created_by":"Alex Marshall","updated_at":"2026-05-19T09:39:45Z","labels":["build-pipeline","catalog","local-group"],"dependencies":[{"issue_id":"stellata-dch.56","depends_on_id":"stellata-8l0","type":"related","created_at":"2026-05-19T10:30:23Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.56","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-19T10:29:30Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.53","title":"End-of-epic deliverable: dch catalogue pipeline writeup + multiples.tsv community dataset publication","description":"# Why this exists\n\nThe dch epic is a top-to-bottom rewrite of Stellata's star-catalogue ingest pipeline: source-ID-anchored cross-matching across AT-HYG / GCVS / CCDM / WDS+ORB6+Gaia, plus Bayesian distance posteriors, kinematic refinements for the LMC, NSS-based binaries, hand-verified known-stars corpus, and so on. By the time the epic completes, every catalogue star will trace its position, distance, magnitude, multiplicity, and (where applicable) orbital elements through a documented chain of authoritative sources rather than a single merge artefact.\n\nThat's worth sharing with the community on two distinct axes:\n\n- **The writeup** — for human readers: someone evaluating browser-based star catalogues, someone hitting the same naive-1/π distance bug in their own pipeline, or someone curious how a single-developer hobby project built up \"best-possible-model-from-current-data\" without compromising on principles.\n- **The dataset** — for data consumers: the eventual `data/multiples.tsv` (produced by Phase 6, stellata-dch.32) is a curated WDS↔Gaia DR3 cross-match table with orbital elements per system. That's a publishable artefact in its own right — there's no equivalent open-data product that pre-cross-matches the WDS catalogue to Gaia DR3 source_ids with per-component letter resolution. Different audience from the writeup (data consumers vs human readers), so deserves its own publication path.\n\nAlex's framing on the writeup: not a journal paper — colloquial \"research paper\" means a public-facing community share. Could be a blog post, a documentation article on stellata.xyz, or a long-form writeup checked into `research/dch-pipeline/` and linked from README. Decide format at writing time. Dataset publication is separate from writeup format and has its own venue decisions (see below).\n\n# Precedent: `research/star-spectral-rendition/`\n\nThe repo's existing `research/` folder follows a stable pattern for end-of-epic writeups, established for `stellata-zsr.1` (star-colour calibration):\n\n- `RECOMMENDATION.md` — long-form prose findings, structured as TL;DR → evidence summary → implementation guidance.\n- Python scripts that produced the evidence (`coverage.py`, `blackbody_color.py`, `compare_sample_stars.py`, `scene_swatches.py`, etc.).\n- TSV input snapshots (`apsis_sample.tsv`) and TXT result tables (`coverage.txt`, `sample_comparison.txt`).\n- PNG visualisations (`sample_swatches.png`, `hr_panels.png`, `scene_swatches.png`, `p3_swatches.png`).\n- `requirements.txt` for repro.\n\nThis bead's writeup deliverable mirrors that shape under `research/dch-pipeline/` (or similar — pick a slug at write time).\n\n# Scope (when this bead runs)\n\nEnd-of-dch-epic, after all phases have shipped:\n\n- Phase 0 prose sweep\n- Phase 1 refresh-script infrastructure (dch.20–26, dch.46, dch.48, dch.50)\n- Phase 2 build-binaries.py (dch.27 + dch.28 shipped Stages 1-2; dch.29 onward for the remaining stages)\n- Phase 3 build-catalog.ts overrides (dch.47 shipped B-J; dch.8l0, dch.33, dch.34, dch.35 outstanding)\n- Phase 4 validation harness (dch.37 known-stars corpus, dch.51 BA-supergiant validation)\n- Layer 5 doc sweep (dch.42)\n\nThe writeup surveys what was actually done — not what was planned — so the bead deliberately sits at the tail. Trying to write it mid-epic invites scope drift and rework when later phases change the story.\n\n# Deliverable shape — writeup\n\n`research/dch-pipeline/RECOMMENDATION.md` (or `STORY.md` / `WRITEUP.md` — pick at write time) with:\n\n1. **TL;DR** — the problem class (position-based cross-matching fails for famous close binaries; naive 1/π distances are catastrophic for low-S/N parallaxes; the LMC doesn't fit a Galactic-density prior; etc.) and the architectural answer (source-ID anchoring + layered distance refinement + kinematic membership rules).\n2. **Evidence per phase** — for each layer, the actual numbers shipped. Example for dch.47: 99.5% B-J override coverage, +15 stars rescued from prior 50-kpc cutoff, HIP 25733 14.3 → 5.5 kpc, HIP 23785 stays within 0.3%.\n3. **Before/after visualisations** — Alex called out at least these explicitly:\n   - Total star volume before vs after B-J correction (a \"big shift\" visible at scene scale).\n   - LMC region before B-J / after B-J alone / after B-J + LMC_KIN (the three-frame story showing why the lockstep ordering matters).\n   - HR-diagram-style scatter of catalog absmag distributions before/after, showing how the catastrophic-inversion tail (the \"−9 mag supergiants at 14 kpc\") collapses.\n   - Possibly: a single-star case study (HIP 25733) showing its position move from \"intergalactic void\" to \"comfortably inside disc\".\n4. **Architectural decisions explained** — why source-ID over position-based; why B-J runs before LMC_KIN; why the 50-kpc cutoff stays in place post-refinement; why we kept naive AT-HYG distances for the 0.5% B-J miss tail (see dch.52).\n5. **What didn't make it** — scoped-out items worth flagging: SMC / Sgr supergiants don't enter the brightness regime yet, Magellanic Stream stars too faint, hand-curated per-star overrides rejected per `stellata-data-fidelity-principle`.\n6. **What's next when Gaia DR4 lands** — broader B-J coverage, SMC kinematics enter scope, NSS catalogue depth improves, prose-parser-deferred WDS-notes work (dch.60) may obviate itself if DR4 / DR5 includes a published visual-pair catalogue.\n\nSupporting Python scripts produce the visualisations from the committed binary + the data/ snapshots. Goal: a curious reader can `python3 research/dch-pipeline/foo.py` and reproduce every figure.\n\n# Deliverable shape — dataset publication\n\n`data/multiples.tsv` (and possibly companion files like the resolved-source-id index) published as a community-citable artifact. Audience: anyone querying for \"give me the WDS↔Gaia DR3 cross-match with orbital elements.\" This is genuinely novel — no equivalent open dataset exists today (SIMBAD has per-object cross-IDs but not the bulk pre-joined table; VizieR has WDS and ORB6 separately but no cross-match to Gaia DR3).\n\nCandidate venues (rank by effort + reach):\n\n- **Zenodo** — lowest friction. Upload TSV + README + LICENSE, get a DOI, link from stellata.xyz / GitHub. Realistic for a hobby project.\n- **GitHub release** — even lower friction. Tag a release on the stellata repo, attach `data/multiples.tsv` as a release asset, link the release URL from README. Works as a stop-gap; no DOI.\n- **VizieR submission** — CDS's astronomical data catalogue. THE canonical venue for community astronomy data. Higher bar (requires VizieR-format documentation, ReadMe in their schema, sometimes FITS) but huge visibility — anyone querying CDS for binary data sees it. Aspirational; pursue if Zenodo lands first and gets traction.\n- **arXiv companion** — paper-style writeup with the dataset linked. Largest effort. Not on the critical path; only consider if the writeup itself goes beyond blog-post scope.\n\nDefault path at write time: **GitHub release for the v1 drop + Zenodo for a citable DOI**. Defer VizieR / arXiv to a follow-up if traction warrants. The Zenodo upload should include:\n- `multiples.tsv` (the data)\n- `multiples-schema.md` (column-by-column description; what each `resolve_via` value means; provenance per column back to AT-HYG / WDS / ORB6 / Gaia / SIMBAD)\n- `multiples-provenance.tsv` (one row per source_id with which catalogue it came from)\n- `LICENSE` (the repo's existing licence inherits)\n- A short `README.md` linking back to the writeup + the live Stellata renderer\n\n# Acceptance criteria\n\n- `research/dch-pipeline/` folder exists with structure mirroring `research/star-spectral-rendition/`.\n- `RECOMMENDATION.md` (or chosen filename) covers all six writeup bullets above.\n- At least 3 PNG visualisations, including the total-star-volume before/after (Alex's specific request).\n- All Python scripts have a `requirements.txt` and run end-to-end against committed `data/` + `public/catalog.bin`.\n- README.md gains a one-line pointer to the writeup. (`docs/` index optional.)\n- `data/multiples.tsv` is attached to a tagged GitHub release with the supporting schema / provenance / README package, AND has a Zenodo DOI minted (or a justified deferral note in the bead's notes field explaining why Zenodo was skipped).\n- Considered: whether any of this material wants to graduate to a stellata.xyz article (or blog post) once the in-repo writeup is settled.\n\n# Connects\n\n[[stellata-data-fidelity-principle]] — the architectural bar the writeup explains.\n[[bead-authoring-scope]] — this is a deliberate single-bead deliverable; the prose IS the artefact, alongside the dataset publication. Not splittable.\n\nParent: stellata-dch (epic).\nRuns after: every dch phase. Deferred via implicit dependency — at write time, file `bd dep add stellata-\u003cnew-bead\u003e stellata-\u003clast-dch-phase\u003e` for whichever bead actually closes the epic.\n\nAuthor: Alex Marshall. AI assistance for evidence compilation + visualisation scripts welcome; the prose voice and architectural narrative are his.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T08:12:44Z","created_by":"Alex Marshall","updated_at":"2026-05-19T15:03:05Z","labels":["catalog","documentation","research"],"dependencies":[{"issue_id":"stellata-dch.53","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-19T09:12:44Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.52","title":"Document B-J override ceiling + optional HIP→DR3 source_id enrichment for ~191 empty-gaia AT-HYG rows","description":"# Finding (filed 2026-05-19 from dch.47 follow-up probing)\n\nThe Bailer-Jones DR3 distance override shipped in dch.47 fires for 310,379 / 312,024 AT-HYG rows with a Gaia source_id (99.5%). The remaining ~1,645 stars + ~1,233 stars without any gaia source_id together account for ~0.9% of the binary's 313,257 records. Probing whether the existing HIP / TYC cross-match tables (`data/gaia_dr3_hip_xmatch.tsv`, `data/gaia_dr3_tyc_xmatch.tsv`) can recover more B-J overrides shows the override is at ceiling for the current Gaia release — but a small source_id enrichment is still worth doing for downstream pipeline value.\n\n# Breakdown of the 0.9% miss\n\n**Population A — AT-HYG rows with empty `gaia` column (2,310 rows in source CSV, ~0.7% of binary).**\n\nOf those:\n- 1,394 have a HIP — looking up `gaia_dr3_hip_xmatch.tsv` recovers a DR3 source_id for **191** of them\n- 2,298 have a Tycho-2 ID — looking up `gaia_dr3_tyc_xmatch.tsv` recovers **0** (the existing TYC xmatch table doesn't carry rows for our specific empty-gaia population; hand-checked, those Tycho-2 IDs simply aren't in the xmatch)\n- **None of the 191 recovered DR3 source_ids are in Bailer-Jones** — so this enrichment unlocks zero new distance overrides\n\n**Population B — AT-HYG rows with a `gaia` source_id but missing from Bailer-Jones (3,913 rows).**\n\nOf those:\n- 2,472 have no HIP, can't probe\n- 1,378 have HIP and AT-HYG's `gaia` agrees with `gaia_dr3_hip_xmatch.tsv` — they ARE DR3 source_ids, B-J just didn't publish a posterior. Almost certainly 2-parameter (position-only) astrometric solutions, or sources excluded by B-J's quality filter\n- 51 have HIP and AT-HYG's `gaia` DIFFERS from the HIP xmatch DR3 source_id (AT-HYG was carrying a DR2 source_id, or the HIP xmatch is wrong, or both candidates are real and AT-HYG picked one) — **0 of the alternative DR3 source_ids are in Bailer-Jones either**\n\n# Conclusion — B-J override is at ceiling\n\nNo HIP-/TYC-backfill chain unlocks any new Bailer-Jones overrides. The 1,645 missing rows lack usable Gaia DR3 parallaxes, full stop. Bailer-Jones 2021 only published posteriors for sources with 5- or 6-parameter astrometric solutions. Gaia DR4 (target launch ~2026) will improve coverage; until then we're at the principled limit.\n\nThis means: **don't re-derive this when the next session wonders.** The finding is the finding. Any further uplift in override coverage waits on Gaia DR4 + a Bailer-Jones DR4 republication.\n\n# Why the enrichment might still be worth doing — beyond B-J\n\nThe 191 newly-resolvable DR3 source_ids (Population A → HIP path) have downstream value across the dch epic:\n\n1. **dch.28 priority chain** — WDS-component → Gaia source_id resolution uses AT-HYG.gaia as one of the join steps. Adding 191 source_ids to AT-HYG would let those 191 stars hit the AT-HYG.gaia path instead of falling through to the more-expensive PM-propagated position match.\n2. **dch.33 v6 binary format** — every star carrying its DR3 source_id is the foundation; +191 stars get one. Tiny dent in the gap, but principled.\n3. **Future Gaia-archive joins** — `gaia_dr3_apsis.tsv` (per-source Teff/logg/[M/H]), `gaia_dr3_nss_two_body.tsv` (Gaia-detected binaries), and any future per-source data are all keyed on DR3 source_id. Pre-resolving the source_id means later joins don't need their own HIP→DR3 step.\n4. **Future B-J refresh** — if Bailer-Jones (or anyone) publishes broader posteriors via a DR4 update, having the source_id already in AT-HYG makes the join trivial.\n\n# Why NOT to also do the 51-row Population-B replacement\n\nThe 51 rows where AT-HYG's `gaia` differs from the HIP xmatch's DR3 source_id are ambiguous:\n- Could be AT-HYG carrying a DR2 source_id (legitimate but stale)\n- Could be the HIP xmatch picking a wrong neighbour (Gaia archives flag uncertain matches via `xm_flag` and `angular_distance`)\n- Could be both candidates real (different solutions, AT-HYG picked the one with usable astrometry)\n\nReplacing AT-HYG.gaia in those rows risks regressing — and gains nothing for B-J (verified zero unlock). Recommend NOT touching Population B. If future work needs broader DR3 source_id coverage there, do an audit first using `xm_flag` + `angular_distance` + a SIMBAD lookup spot-check.\n\n# Scope (if we do this — one commit/PR)\n\nIn `scripts/build-catalog.ts` (or `scripts/build-binaries.py` once that pipeline absorbs the AT-HYG load — currently AT-HYG read lives in build-catalog.ts):\n\n1. Load `gaia_dr3_hip_xmatch.tsv` into `Map\u003chip, dr3_source_id\u003e` at startup. Filter on high-confidence matches only (`number_of_neighbours == 1` AND `angular_distance \u003c 0.5` arcsec — pick the threshold by quick histogram of the 191).\n2. During `readStars`, for AT-HYG rows where `row.gaia` is empty AND `row.hip` is present: set the effective source_id to `hipToDr3.get(row.hip)` if found.\n3. Pass the effective source_id (not just `row.gaia`) into `applyBailerJonesOverride`. The override will still be a miss for these 191, but stats will reflect the enrichment.\n4. Log enrichment count at build time. Add `bjSourceIdEnriched` to `BuildCounts`.\n5. Once dch.33 ships the v6 binary format with `gaia_source_id` per record, the enriched source_id rides into the binary alongside the original.\n\n**No binary format change** — this is a build-time enrichment, not a schema change.\n\n# Acceptance criteria (if implemented)\n\n- Build runs to completion; log line shows ~191 source_ids enriched via HIP xmatch.\n- Override coverage logged separately as `bjOverridden / (bjEligible + bjSourceIdEnriched)` so the +191 doesn't artificially inflate apparent coverage (they're still misses for now).\n- `xm_flag` filter is conservative: high-confidence matches only.\n- Unit test: pure helper that takes (gaiaCol, hip, hipToDr3) → effective source_id. Exercise both branches (gaia present → use as-is; gaia empty + hip in map → enriched; gaia empty + no hip → null).\n\n# Priority + when to act\n\nP3. The 191 enrichment moves 0.06% of the catalog (191 / 313,257). It's principled but small. Sensible to defer until dch.28 (WDS priority chain) is in flight — then the +191 source_ids will demonstrably exercise the AT-HYG.gaia join path in dch.28's resolver, and the value is concrete rather than speculative. Or to bundle into dch.33 (v6 binary format) so the enriched source_id is what gets written to disk.\n\n# Connects\n\n[[stellata-data-fidelity-principle]] — \"best possible model based on current observational data and knowledge\". The 99.5% ceiling under current data IS the best possible model; this bead documents the boundary.\n[[stellata-test-coverage-discipline]] — enrichment count is a tier-2 statistic that belongs in `build-catalog-expected.json`.\n\nSibling: dch.47 (shipped) — Phase 3 distance override that revealed this ceiling.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T07:44:48Z","created_by":"Alex Marshall","updated_at":"2026-05-19T07:44:48Z","labels":["catalog","data-pipeline","distance"],"dependencies":[{"issue_id":"stellata-dch.52","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-19T08:44:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-zsr.4","title":"Tier 2 — re-key star colour LUT from Ballesteros(B-V) to Gaia DR3 Apsis Teff","description":"Re-route the input to the blackbody → sRGB colour LUT (shipped in [pr-89] / `stellata-zsr.3`) so it prefers Gaia DR3 Apsis Teff over the current Ballesteros(B-V) → Teff path. Surfaces the empirical 2.87× coverage uplift from `stellata-zsr.1` (29.3% → 84.8% (Teff, logg) coverage) into rendered chromaticity for the ~85% of stars Apsis has parameters for.\n\n# Origin\n\nFiled as the natural follow-up flagged in two places:\n\n1. PR #89 description: *\"WD-specific Teff routing from the bead description is **not** implemented in this PR. Catalog v4 doesn't store `wd_subclass`; the WDs in the catalog (Sirius B, etc.) render via Ballesteros at their recorded B-V, which still lands in a blue-white range. Tier 2 (Apsis ingest under `stellata-dch`) is the natural home for per-WD T_eff routing — flagging here so it can be split into a follow-up bead if desired.\"*\n2. `stellata-dch.49` description, \"Downstream re-routings (NOT in this bead's scope)\" block: *\"Tier 1 colour LUT (`stellata-zsr.3`) re-keys Teff source from Ballesteros(B-V) → Apsis-direct where available (Apsis preferred; Ballesteros fallback; spectral-class fallback).\"*\n\nBoth notes point at this same re-route. Folds the WD routing into the same change because Apsis carries Teff for WDs natively (Sirius B etc. have Gaia DR3 entries), so the deferred WD path collapses into the Apsis path as a byproduct — no separate `wd_subclass` plumbing required.\n\n# Scope — single PR\n\nRe-keys the LUT input. No LUT regeneration, no shader maths change, no catalog format bump. Apsis fields already in `catalog.bin` via `stellata-dch.49`; this bead consumes them.\n\n# Routing priority (`star.vert.glsl`)\n\nIn strict order, first match wins:\n\n1. Apsis `teff_gspphot` (~85% coverage; primary)\n2. Apsis `teff_gspspec` (smaller, but covers some gspphot gaps)\n3. Ballesteros(B-V) (current Tier 1 path; ~29% of catalog where B-V exists but Apsis doesn't)\n4. Spectral-class T_TABLE (`tempKelvin` in `catalog-pure.ts`, existing)\n5. WD subclass: `50400 / wd_subclass` (existing path in `catalog-pure.ts`)\n6. Solar fallback (B-V = 0.65, current behaviour)\n\nThe LUT itself spans `T ∈ [Ballesteros(-0.4), Ballesteros(+2.0)]`, i.e. ~3300–40000 K. Apsis Teff outside that range clamps to LUT edge (rare — Apsis hot tail tops out near 50000 K for O stars; those clamp blue-white per the existing hot-edge behaviour). White dwarfs with Apsis Teff ~25000–60000 K land near or just above the LUT hot edge — verify the clamp produces blue-white as expected (Sirius B reference: T_eff ≈ 25200 K, well inside the LUT range).\n\n# Dust reddening\n\nUntouched. Apsis Teff is the **intrinsic** parameter (Apsis fits include extinction A0 explicitly). The existing dust-reddening composition in the shader operates on observed chromaticity downstream of the LUT sample and remains observer-position-dependent. Verify Mu Cephei still drifts toward intrinsic peach as the camera warps closer (the case study in `research/star-spectral-rendition/RECOMMENDATION.md` § Mu Cephei).\n\n# Deliverables\n\n- **`src/client/shaders/star.vert.glsl`** — replace the single-source `effectiveCi → LUT` index with a routing helper that picks Teff per the priority above, converts Teff → LUT index, samples. The function name and downstream contract (`ciToColor` output → dust reddening composition) are preserved.\n- **Pure helper** `src/client/shaders/blackbody-lut-pure.ts` (or extend the existing module): `teffToLutIndex(teff: number) =\u003e number` — float in `[0, 1)` for `texture()` sampling. Tested separately from the shader.\n- **Routing helper** `src/client/shaders/star-color-routing-pure.ts` (new) — pure TS function `pickTeffSource(record) =\u003e { teff: number, via: 'apsis-phot' | 'apsis-spec' | 'ballesteros' | 'spectral' | 'wd' | 'solar' }`. Exact same routing the shader applies, mirrored for testability per `[[stellata-test-coverage-discipline]]`. The shader and the test BOTH consume this helper (or its constants) so they cannot drift — per CLAUDE.md § \"Code conventions — DRY overrides the system prompt\" / `[[stellata-named-constants-and-dry]]`.\n- **`star.vert.glsl` attribute additions**: per-instance Apsis Teff (one float, pulled from the v6/v7 buffer that `stellata-dch.49` populates). Null sentinel handling matches the catalog loader's convention.\n- **Counter readout** (debug-panel or build-time log): \"Apsis Teff used for X / 313k stars\" — pin via `expect(...).toBe(N)` per `[[stellata-test-coverage-discipline]]` so the headline coverage figure doesn't decay silently.\n\n# Tests (per `[[stellata-test-coverage-discipline]]`)\n\n- **Routing helper unit test**: each tier exercised with a fixture record. Priority semantics asserted as a separate test (\"Apsis present beats Ballesteros even when B-V also present\").\n- **`teffToLutIndex` unit test**: Teff edges (3300 K, 5778 K, 40000 K) → expected indices (`toBe`, not `toBeCloseTo` for the boundary points).\n- **LUT-sample-via-Teff RGB pin** for 6+ named stars covering each tier: Sirius A (Apsis gspphot, hot), Sol-anchor (Apsis gspphot via solar twin), Mu Cep (Apsis present, cool), Sirius B (WD, Apsis-routed now), a faint Tycho-only star (no Apsis, Ballesteros path), a star with neither (spectral-class path).\n- **Counter test**: \"≥ N records routed via Apsis\" pinned with `toBe` against the build-time count.\n\n# Smoke (per `[[alex-stellata-smoke-tests]]`, manual only)\n\nAfter typecheck + vitest:\n\n- **WD chromaticity (the deferred case from PR #89):** Sirius B, Procyon B, 40 Eridani B. Should land in a blue-white / cyan range matching Apsis Teff (~25000 K), not the Ballesteros B-V proxy.\n- **Apsis cool-end win:** Antares, Betelgeuse, Aldebaran, Mu Cephei. Apsis Teff vs Ballesteros(B-V) Teff will differ for some — check no regression vs the PR #89 chromaticities; if Apsis lands closer to the reference column in `research/star-spectral-rendition/sample_comparison.txt`, that's the win.\n- **Apsis fallback regression check:** a few stars known to have no Apsis (e.g. faint Tycho-only catalogue rows) should look identical to before.\n- **Mu Cephei observer-position-dependent dust:** still drifts toward intrinsic peach as you warp closer.\n- **Chart mode (`m`):** unchanged — chart mode skips the LUT path.\n- **HR diagram qualitative shape:** density along Teff should match panel C continuity in `hr_panels.png`, ideally crisper than the Ballesteros-fed version.\n\n# Confidence (per `[[alex-large-pr-honesty]]` at PR-open time)\n\n- **High:** routing helper + LUT index helper (pure TS, deterministic, per-tier vitest, byte-signature-style pins).\n- **Medium:** shader integration (uniform / attribute wiring is adjacent to the existing `ciToColor` site, but a new per-instance attribute is non-trivial — first low-coverage path to smoke).\n- **Low (needs eyeballs):** actual rendered chromaticity vs expectation. Only the manual smoke list above catches a per-star visual regression. The Mu Cep / Sirius B / Sirius A trio is the load-bearing set.\n\n# Out of scope (filed elsewhere)\n\n- Adding `logg` / `[M/H]` consumers (Phase 5 `stellata-dch.40` mass-ratio refinement; Phase 5 `stellata-dch.41` eclipse photometry limb-darkening; Stefan-Boltzmann radius refinement).\n- `azero_gspphot` cross-check against the Edenhofer dust map — filed separately.\n- Display P3 wide-gamut output (`stellata-zsr.2`, deferred).\n- Pecaut \u0026 Mamajek 2013 extended T_TABLE — Tier 3, deferred.\n\n# Connects\n\n- Depends on `stellata-dch.49` (Apsis fields in catalog binary).\n- Transitively waits on `stellata-dch.33` (v6 format) and `stellata-dch.48` (Apsis TSV ingest).\n- Closes the WD-Teff thread from `stellata-zsr.3` / PR #89.","acceptance_criteria":"- For stars with Apsis Teff present, shader sources Teff from Apsis (gspphot ∪ gspspec), not Ballesteros(B-V).\n- Fallback chain hits each tier in priority order; verified by per-tier vitest spanning Apsis-direct / Ballesteros / spectral-class / WD / solar fallback cases.\n- White dwarfs (Sirius B, Procyon B, 40 Eri B) now render at their Apsis Teff (deferred from PR #89 because catalog v4 didn't carry the necessary input).\n- No regression for Apsis-absent stars — they continue to use the existing Ballesteros / spectral-class / solar path with identical output.\n- Coverage smoke: ≥80% of records take the Apsis path (matches dch.49 acceptance), validated by an instrumented counter in the build script or a runtime debug HUD readout.\n- typecheck + vitest green.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T19:37:28Z","created_by":"Alex Marshall","updated_at":"2026-05-18T19:37:28Z","labels":["apsis","gaia","physics","shader","ux"],"dependencies":[{"issue_id":"stellata-zsr.4","depends_on_id":"stellata-dch.49","type":"blocks","created_at":"2026-05-18T20:37:35Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-zsr.4","depends_on_id":"stellata-zsr","type":"parent-child","created_at":"2026-05-18T20:37:28Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-zsr.4","depends_on_id":"stellata-zsr.3","type":"related","created_at":"2026-05-18T20:37:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.51","title":"Phase 3 — Distance validation harness vs Vaidman et al. 2025 BA-supergiant catalogue","description":"# Why\n\nVaidman et al. 2025 (MDPI Universe 11, 359; DOI 10.3390/universe11110359) publishes a quality-controlled Bayesian recalculation of Gaia DR3/EDR3 distances for 132 Galactic BA-type supergiants — the exact failure-mode population dch.46/dch.47 is designed to rescue (low-S/N parallaxes for luminous distant stars where naive 1/parallax inverts to 10-15 kpc). Their Table A1 lists 119 stars with adopted EDSD_new distances; Table A2 lists 13 low-SNR / high-RUWE stars where they revert to BJ_old (explicitly not recommended for quantitative use).\n\nThe paper independently validates the dch.46 thesis:\n- At high SNR their d_new closely tracks Bailer-Jones d_BJ (differences dominated by ZP shifts + slightly broader posteriors).\n- At intermediate SNR (2.5-5) they see modest ~10-30% offsets at large distances.\n- Below SNR 2.5 the posterior becomes prior-dominated.\n- None of their adopted distances exceed ~6.3 kpc — confirming our outlier-class supergiants belong at 1-4 kpc, not 14 kpc.\n\nThis is the natural reference dataset for validating dch.46+dch.47 AND as a recurring spot-check whenever we change distance source later (DR4 B-J refresh, switch to StarHorse, build-catalog distance-priority change, etc.).\n\n# Scope (one commit/PR)\n\n1. **Reference dataset.** Extract Table A1 (119 EDSD_new rows) + Table A2 (13 BJ_old rows) from the published PDF into `data/distance-validation/vaidman-2025-supergiants.tsv`. Schema: `name`, `d_bj_paper_pc`, `sigma_d_bj_paper_pc`, `ruwe`, `g_mag`, `d_new_pc`, `sigma_d_new_pc`, `snr_tot`, `prior_scale_pc`, `adopted` (one of `EDSD_new` / `BJ_old`). Small (~132 rows) — regular git, no LFS.\n\n2. **Name → Gaia DR3 source_id resolution.** Most rows use HD / BD / HR / named designations (HD 1070, BD+60 51, V755 Cas, theta Aql, eta Leo, epsilon CMa, etc.). Resolve once via SIMBAD TAP (the paper itself used SIMBAD, so we'll match by construction); commit the mapping as an additional `gaia_source_id` column on the TSV. Document the SIMBAD query in `data/distance-validation/README.md` so the mapping is reproducible.\n\n3. **Validation script `scripts/validate-distances.py`.** Reads:\n   - The shipped catalog's per-star distance (via `scripts/verify-catalog.ts` JSON dump, or `data/bailer-jones-dr3.tsv` directly — pick whichever decouples cleanest from in-flight binary schema).\n   - The Vaidman reference TSV.\n   Joins on Gaia source_id. Computes per-star fractional difference vs `d_new_pc` for the EDSD_new subset; aggregates median, 84th-percentile, and top-N largest disagreements. Prints a report keyed for human eyeball + suitable for grep in CI.\n\n4. **Acceptance bar for dch.47 integration.** On the 119-star EDSD_new subset:\n   - median |frac diff| \u003c= 15%\n   - \u003c= 5 stars with |frac diff| \u003e 50%\n   The 13 BJ_old stars in Table A2 are explicitly low-quality — they're a sanity check that our adopted distance equals B-J for them (which it should, since dch.46 ships B-J's r_med_photogeo unchanged). Report-only, no fail bar.\n\n5. **Citation.** Vaidman et al. 2025 added to SCIENCE.md references with DOI, journal, and the role the paper plays in our pipeline (independent validation reference set for the Bailer-Jones distance override). Per [[defer-doc-updates]], the SCIENCE.md edit lands in the same PR as the script.\n\n# Reuse — recurring validation harness\n\nThe script is designed for re-run on every distance-source change:\n- Future B-J refresh from a new Gaia release (DR4 -\u003e r_med_photogeo recomputed).\n- Switching distance source entirely (StarHorse, a B-J successor, or self-derived posteriors).\n- Any change to build-catalog.ts's distance-override priority.\n\nThe 119-star EDSD_new subset is fixed reference data: any new distance source for these stars should agree with d_new within the same bars (or we surface the disagreement and decide explicitly whether it's a regression or an upgrade). Treated as frozen reference data per the same convention as GCVS / dust / Zucker datasets.\n\n# Caveats\n\n- Vaidman et al. use an EDSD prior with L = clip(0.8 * r_BJ50, 300 pc, 2500 pc) — their d_new is partially anchored to d_BJ for the prior scale length, so the comparison is not fully independent at very low SNR. The EDSD_new subset has SNR \u003e= 2.5 where the likelihood dominates the prior; that's where the validation has teeth.\n- Star identification is by name, not Gaia source_id. We're trusting SIMBAD's name -\u003e Gaia DR3 resolution. Spot-check a handful manually against the paper's coordinates.\n- Pulling Table A1/A2 out of a PDF needs care — column alignment in Table A1 (cont. spread across pages 9-11) is the obvious risk; commit the extraction script alongside the TSV.\n\n# Smoke step\n\nAfter running the script:\n- Median |frac diff| value printed; top-5 disagreements named; 119/13 split tallies.\n- Eyeball whether the agreement curve matches the paper's Figure 2 (tight 1:1 at high SNR, modest spread at SNR 2.5-5).\n\n# Connects\n\n[[bead-authoring-scope]] — one script + one reference TSV + one smoke step = one commit.\n[[defer-doc-updates]] — SCIENCE.md citation at commit time, not mid-implementation.\n[[stellata-test-coverage-discipline]] — pure helpers in validate-distances.py (name normalisation, fractional-difference aggregation) get a pytest sibling.\n\nParent: stellata-dch (catalog pipeline epic).\nDepends on: dch.47 (B-J distance override needs to ship in build-catalog before we can validate it end-to-end).","acceptance_criteria":"- data/distance-validation/vaidman-2025-supergiants.tsv committed with 119 EDSD_new + 13 BJ_old rows, schema as described, including gaia_source_id column from SIMBAD resolution.\n- scripts/validate-distances.py runs end-to-end against the post-dch.47 shipped catalog; prints a report with median |frac diff|, 84th-percentile, top-5 disagreements, and 119/13 tallies.\n- On the 119-star EDSD_new subset: median |frac diff| \u003c= 15%; \u003c= 5 stars with |frac diff| \u003e 50%.\n- 13 BJ_old stars: adopted catalog distance equals B-J's r_med_photogeo (report-only).\n- SCIENCE.md references section cites Vaidman et al. 2025 with DOI and role.\n- Pure helpers (name normalisation, fractional-difference aggregation) covered by a pytest sibling.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T17:33:32Z","created_by":"Alex Marshall","updated_at":"2026-05-18T17:33:32Z","metadata":{"paper-doi":"10.3390/universe11110359","paper-github":"https://github.com/nva1dman/gaia_distance_pipeline"},"labels":["catalog","data-pipeline","distance","gaia","phase-3"],"dependencies":[{"issue_id":"stellata-dch.51","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-18T18:33:31Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.51","depends_on_id":"stellata-dch.47","type":"blocks","created_at":"2026-05-18T18:33:37Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ty4.2","title":"Extend MW dust extinction to non-stellar layers (Local Group, future deep-field)","description":"**Goal:** apply the multi-layer dust stack (Edenhofer + Bayestar + analytical slab + arms — see parent **stellata-ty4**) to extragalactic objects, not just MW catalog stars. Currently `dustExtinctionAV` lives only in `star.vert.glsl`; every other layer renders unattenuated.\n\n# Why\n\n`star.vert.glsl` raymarches camera→star through the dust layers and applies A_V to appMag + E(B-V) to colour. No other layer does this:\n- **Local Group wireframes / discs / labels** (`src/client/local-group/`) render at catalog brightness regardless of foreground MW dust — a galaxy behind several kpc of MW disc dust should dim and redden.\n- **Future stellata-0hx deep-field layers** (Local Volume → Virgo → Laniakea → ...) will have the same gap.\n- **Milky Way volumetric layer** already applies its own analytical dust internally; that path is unchanged by this bead.\n\n# Why P3\n\nFuture-proofing for the universe-scale expansion (stellata-0hx is P3, stellata-36y is P2 but operates on a different axis). The current Local Group population is small and mostly wireframe — visual fidelity is dominated by morphology, not foreground extinction. Bumps to P2 once stellata-0hx implementation children start landing and add Mpc-scale objects whose flux matters.\n\n# Approach\n\n1. Lift `dustExtinctionAV` from `star.vert.glsl` into a shared shader include alongside the analytical-slab + Bayestar work in ty4.\n2. Add the include to each consumer's shader path:\n   - `local-group.ts` / `galactic-disc.ts` style wireframes — A_V dims line/fill colour, E(B-V) reddens.\n   - `local-group.ts` billboard discs (for the dwarfs that render as discs not wireframes) — same.\n   - Future deep-field layer shaders — folded in at construction.\n3. Performance: a 48-tap raymarch per extragalactic object is cheap (dozens, not 313k). Could optionally be computed CPU-side at layer-construct time and uploaded as a per-instance attribute (positions are static).\n\n# Open question — slab raymarch density at Mpc scales\n\nThe current `DUST_STEPS = 48` is calibrated for ≤ kpc-scale sightlines (26 pc per step at 1.25 kpc). From M31 the sightline is ~780 kpc and the slab has nonzero density only across the ~600 pc thick MW disc — 48 evenly-spaced steps would put most samples in empty space outside the slab.\n\nOptions to evaluate:\n- Adaptive step count based on sightline length and where it intersects the slab AABB.\n- Analytical integration of the exponential disc along the segment intersecting the disc (closed-form; avoids raymarch entirely for the slab portion).\n- Two-region split: dense steps inside the slab AABB, sparse steps for the empty extragalactic segment (likely zero-density anyway).\n\nThe analytical integration is the cleanest answer if the slab is genuinely just `exp(-(R-R₀)/L_R) × exp(-|z|/h_z)` — it has a closed-form line integral.\n\n# Acceptance\n\n- Local Group galaxies viewed from inside the MW disc dim and redden through the analytical slab.\n- MW catalog stars viewed from extragalactic vantages (e.g. M31) dim through the slab over the ~780 kpc sightline. (74e already covers this for stars; this bead extends it to non-stellar consumers.)\n- No regression in near-Sol fidelity (per-star extinction inside the Edenhofer cube unchanged).\n- A test or debug-panel readout for slab A_V at a known sightline (Sol → M31) matches the analytical integral within a few percent.\n\n# Files\n\n- New `src/client/shaders/dust-extinction.glsl` shared include (lifted from star.vert.glsl).\n- `src/client/shaders/star.vert.glsl` — switch to the shared include.\n- `src/client/local-group/local-group.ts` — add extinction sample to its shader/materials.\n- `src/client/local-group/local-group-loader.ts` — wire dust-texture uniforms.\n- Future deep-field layers — folded in at construction time.\n\n# Related\n\n- **stellata-74e** — the analytical slab implementation; this bead extends its consumers.\n- **stellata-0hx** — extragalactic deep-field; will need this hooked in at layer-add time. Add a dependency-link if/when 0hx children start landing.\n- **stellata-36y** — far-field statistical augmentation; the cumulative-glow layer probably wants the same extinction treatment as a single integrated A_V applied to the HiPS-derived intensity.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-18T11:55:57Z","created_by":"Alex Marshall","updated_at":"2026-05-18T11:55:57Z","labels":["extinction","extragalactic","ism","shader"],"dependencies":[{"issue_id":"stellata-ty4.2","depends_on_id":"stellata-ty4","type":"parent-child","created_at":"2026-05-18T12:55:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.50","title":"Phase 0 — Sweep v1.0 prose from code comments + README (sibling to dch.20)","description":"Sibling cleanup to stellata-dch.20. dch.20 swept v1.0 / v1-era prose from CLAUDE.md, SCIENCE.md, docs/*.md per its explicit scope. A grep audit during dch.20 execution turned up further hits in code comments + README that fall under the same stale-framing pattern but were out of dch.20's stated scope.\n\n# Files + lines (audit run 2026-05-18 after dch.20 landed)\n\n- README.md:378 — \"rendering shelved for v1.0\"\n- src/client/main.ts:91 — \"Cloud layer is shelved for v1.0 (CLAUDE.md)\"\n- src/client/main.ts:149 — \"cloud layer is shelved for v1.0 (CLAUDE.md)\"\n- src/client/main.ts:235 — \"attach call is shelved at v1.0 (CLAUDE.md)\"\n- src/client/stellata.ts:273 — \"Shelved for v1.0\"\n- src/client/stellata.ts:2142, 2209 — \"Cloud layer is shelved for v1.0 (CLAUDE.md): visible=false\"\n- src/client/util/url-state.ts:88 — \"(formerly FLAG_MC_DISABLED — molecular clouds shelved for v1.0)\"\n- src/client/dust/dust-particle-layer.ts:18 — \"Shelved for v1.0\"\n- src/client/solar-system/heliopause.ts:244 — \"only planet-bearing host in v1\"\n- src/client/solar-system/planet-system.ts:4 — \"Sol is the only one populated in v1\"\n- src/client/solar-system/planet-system.ts:214 — 'v1 hardwires \"planets ⇔ Sol\"'\n- src/client/solar-system/time.ts:5 — 'Pinned to \"now\" in v1'\n- src/client/milkyway/milkyway.ts:317 — \"for v1.0 the volumetric layer is hidden entirely in chart mode\"\n- src/client/local-group/local-group.ts:62 — \"shelved for v1.0\"\n- src/client/chart-mode/chart-labels.ts:488 — \"for v1.0 (CLAUDE.md) so this block is dead until re-enabled\"\n\nURL-state-format v1/v2 references in `url-state.test.ts` (lines 40, 837, 1005 — \"32-bit mask in v1\", \"20 in v1. v3\", \"v1 or v2\") are NOT product-v1 — keep as-is. The earlier draft of this bead description also exempted `url-state.ts:88`, but that line is product-v1 prose (\"molecular clouds shelved for v1.0\") sitting adjacent to the URL-flag-bit declarations — NOT a URL-format version reference. Corrected at execution time (proactive-drift-correction).\n\n# Replacement guidance\n\nSame as dch.20:\n- \"shelved for v1.0\" → \"currently shelved\" / \"shelved\" (the status remains current).\n- \"in v1\" referring to product limitations → \"currently\" / drop the version anchor.\n- \"(CLAUDE.md)\" callouts can stay — the section reference is still accurate.\n\n# Acceptance\n\ngrep -rnE 'v1\\\\.0|v1 affords|v1 hardwires|v1 pins|v1 places|in v1\\\\b|for v1\\\\b' --include='*.md' --include='*.ts' --include='*.glsl' --include='*.py' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.venv --exclude-dir=public . — only URL-state-format hits in url-state.test.ts should remain.\n\n# Smoke\n\n- npm run typecheck clean (comment-only changes; defensive verify)\n- npm test green\n\n# Commit\n\nSingle commit. Bundled into PR #92 (per Alex 2026-05-18) alongside dch.46 + dch.20. PR labelled skip-version-bump.\n\n# Connects\n\n[[stellata-rename-and-stale-prose-sweep]] — same prose-follows-behaviour pattern, but here the behaviour change is the stage transition (past v1.0), not a rename.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-18T11:08:59Z","created_by":"Alex Marshall","updated_at":"2026-05-18T17:22:25Z","started_at":"2026-05-18T17:15:17Z","closed_at":"2026-05-18T17:22:25Z","close_reason":"Closed","labels":["cleanup","phase-0","skip-version-bump"],"dependencies":[{"issue_id":"stellata-dch.50","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-18T12:08:59Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.50","depends_on_id":"stellata-dch.20","type":"related","created_at":"2026-05-18T12:09:06Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-r0m.1","title":"Research: webapp scope ceiling + desktop fidelity uplift + technical stack","description":"# Why\n\nFirst research bead under stellata-r0m. Three sub-questions need scoped answers before any desktop-app code can land:\n\n1. **WHERE** does the webapp's WebGL + browser-memory + first-load ceiling sit on representative modest hardware (integrated GPU laptop, mid-tier phone, slow connection)?\n2. **WHAT** does the desktop app add beyond that ceiling — which data layers / fidelity uplifts specifically justify the paid SKU?\n3. **HOW** is the desktop app built — Tauri vs Electron vs native Swift/Metal vs Rust+wgpu — with the shader-codebase-sharing question front and centre?\n\n# Method\n\n**For (1) — webapp ceiling**: empirical, building on stellata-cns.1's perf framework. Pick a representative low-end target (integrated GPU laptop + mid-tier phone). Measure FPS / GPU memory / instance-attribute memory / first-load wire size at increasing star counts (313k classic_ids → 800k m11 → 2M m12 → hypothetical 5M+ Gaia-native depth cuts). Capture the cliff. Include sustained-frame variance, not just steady-state FPS — the chart-mode optimisation work (centroid cache, dirty-tracked SVG) shows variance matters as much as average.\n\n**For (2) — desktop fidelity uplift**: enumerate concrete uplifts that would warrant a paid SKU. Candidates:\n- Full Gaia DR3 magnitude depth (G \u003c 15+, tens of millions of stars).\n- Full Bailer-Jones at native catalog scale (no AT-HYG.gaia subset cap).\n- Edenhofer 3D dust at higher voxel resolution (current build downsamples).\n- Exoplanet catalog (currently P3-blocked behind physical-reality rewrite).\n- Time-series animation (proper motion through epoch range, variable-star light curves).\n- Spectroscopy / HR-diagram side panels.\n- IAU constellation boundaries (deliberately excluded from v1 webapp per CLAUDE.md).\n- Full LG depth (M31 substructure resolved, distant DSOs at native resolution rather than projected).\n- Per-star Apsis-derived metadata exposed in hover (Teff, logg, [M/H], A0).\n\nRank by perceived user value + engineering cost. The bar: each uplift should be obviously beyond what the webapp can reasonably do, not a marginal improvement.\n\n**For (3) — technical stack**: build a feature-matrix decision across four candidates:\n- **Tauri** (Rust core + WebView2 / WebKit frontend) — lightweight, web stack fully reusable, smallest install footprint, no Chromium bundling.\n- **Electron** — heaviest, full Chromium bundled, web stack fully reusable, well-trodden path.\n- **Native Swift / Metal** — macOS-only, no shader reuse without GLSL→MSL translation, best raw perf, App Store-native.\n- **Rust + wgpu** — cross-platform, WGSL shaders share concept with GLSL but require porting, largest engineering scope.\n\nThe Three.js WebGPU backend is production-ready as of 2026, which changes the calculus — running the existing renderer through Tauri-on-WebGPU could be the path of least resistance for sharing the renderer codebase across web and desktop. Worth a focused spike before committing to a stack.\n\n# Acceptance\n\nWritten recommendation in this bead's notes covering all three questions. Should be specific enough that a follow-on child bead can be filed (e.g. \\\"Tauri + WebGPU, ship first as macOS Mac App Store paid SKU, full-Gaia-magnitude-depth + 3D-dust + Apsis-hover the headline\\\"). Each of the three sub-recommendations cites the evidence behind it (perf numbers, fidelity-uplift cost estimates, stack feature-matrix).\n\n# Smoke\n\nDecision-bead — no implementation. The recommendation seeds the technical-stack + scope-split + business-model children of stellata-r0m.\n\n# Connects\n\n- Parent: stellata-r0m.\n- Architectural prerequisite (informational, not blocking research): stellata-dch — the desktop SKU consumes its refinement layers at deeper scale.\n- Informed by: stellata-cns.1 (perf-ceiling baseline) + stellata-cns.2 (row-driver pattern). Both inform what the webapp's ceiling looks like AND how the desktop's deeper ingest extends it.\n- Memory: [[alex-stellata-profile]] (sole-contributor, personal-project framing for the paid SKU).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T10:55:46Z","created_by":"Alex Marshall","updated_at":"2026-05-18T10:55:46Z","labels":["desktop","paid","research"],"dependencies":[{"issue_id":"stellata-r0m.1","depends_on_id":"stellata-r0m","type":"parent-child","created_at":"2026-05-18T11:55:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-r0m","title":"Stellata desktop app — full-fidelity paid expansion (scope split with webapp)","description":"# Why this epic exists\n\nThe webapp's scope is bounded by WebGL performance + browser memory + first-load budget. Even after the deep-tier catalog expansion (stellata-cns), there is a hard ceiling beyond which the webapp cannot reasonably go without degrading the experience on modest hardware (integrated GPUs, mid-tier mobile, slow connections). A desktop app with native GPU access (Metal on macOS, Vulkan / DX12 elsewhere) and no first-load gzip budget can comfortably ingest substantially deeper data — full Gaia DR3 magnitude tiers, full Bailer-Jones at native catalog scale, higher-resolution 3D dust, deeper LG / extragalactic layers, exoplanet catalog, time-series, spectroscopy panels.\n\nThe natural split: **webapp keeps a perf-ceiling-fit dataset, free at stellata.xyz.** **Desktop expands fidelity / depth, paid.**\n\n# Open questions to resolve via children\n\n1. **Webapp scope ceiling** — where does the webapp's instance count + texture / buffer memory ceiling sit on representative modest hardware? Closely related to stellata-cns.1's measurement work. Determines the natural boundary between free and paid.\n2. **Desktop incremental fidelity** — what specifically does desktop add? Candidates: full Gaia magnitude depth (G \u003c 15+), full Bailer-Jones at native catalog scale, 3D extinction at higher voxel resolution, exoplanet catalog (currently P3-blocked), asterism / IAU boundaries, time-series proper motion + variability animation, spectroscopy / HR-diagram panels, full LG depth (M31 substructure, distant DSOs at native resolution), deeper Local Bubble / Radcliffe Wave coverage.\n3. **Technical stack** — Tauri (lightweight, WebView2/WebKit, web stack reusable) vs Electron (heavy, Chromium-bundled, web stack reusable) vs native Swift / Metal (macOS-only, no shader reuse without translation, best raw perf) vs Rust + wgpu (cross-platform, WGSL shaders share concept with GLSL but require porting). Three.js's WebGPU backend changes the calculus — Tauri-on-WebGPU could be the path of least resistance for sharing the renderer codebase across web and desktop.\n4. **Business model** — pricing tier, distribution channel (Mac App Store, Steam, direct), licensing, free-trial / demo strategy, single-purchase vs subscription.\n\n# Sequencing / dependencies\n\n- Architectural prerequisite: **stellata-dch** must ship. The desktop expansion consumes the same source-ID-keyed refinement layers (B-J, Apsis, astrometry, NSS, LMC kinematic) at greater depth. No point splitting webapp vs desktop until dch's pipeline is the canonical ingest path.\n- Informed by **stellata-cns + cns.1 + cns.2**. Those set the webapp's depth ceiling AND the row-driver pattern. Desktop just pushes that pattern deeper.\n- Children: research-first. Decision-bead up front to scope the split before any code lands.\n\n# Out of scope (for now)\n\n- Mobile native (iOS / Android) — separate epic if/when desktop is solid. Same \"free webapp / paid native\" pattern probably applies but mobile-specific.\n- Server-side rendering or cloud streaming — separate question entirely.\n- Per-platform optimisations once the stack is chosen — child epics under whatever stack wins.\n\n# Connects\n\n- [[alex-stellata-profile]] — personal-project context; paid desktop is a sensible scope expansion not a commercialisation pivot.\n- [[stellata-prioritization-framework]] — sits behind closer-to-Sol layers in the queue. P3 / future until those ship.\n- Architectural prerequisite (related): stellata-dch (source-ID-keyed refinement stack at deeper magnitude tiers is exactly what the desktop SKU would consume).\n- Informed by: stellata-cns + stellata-cns.1 + stellata-cns.2 — webapp's depth/budget ceiling AND row-driver pattern feed directly into the split-point definition.","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-18T10:55:09Z","created_by":"Alex Marshall","updated_at":"2026-05-18T10:55:09Z","labels":["desktop","paid","research"],"dependencies":[{"issue_id":"stellata-r0m","depends_on_id":"stellata-cns","type":"related","created_at":"2026-05-18T11:55:48Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-r0m","depends_on_id":"stellata-dch","type":"related","created_at":"2026-05-18T11:55:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-cns.2","title":"Evaluate row-driver choice: Gaia-native vs deeper AT-HYG for the deep-tier fill","description":"# Why\n\nSibling research bead to stellata-cns.1 (m11 vs m12 perf + first-load eval). cns.1 covers WHAT depth + render-budget for the deep-tier expansion; this bead covers WHICH ingest pipeline drives the rows at that depth — AT-HYG reduced_m11/m12 monolithic vs Gaia-native query vs hybrid. Both feed into the same parent (stellata-cns) and inform its implementation design.\n\n# The choice\n\nAfter stellata-dch lands, AT-HYG's value-add narrows by depth tier:\n\n- **At classic_ids (~313k bright stars)** — AT-HYG genuinely earns its keep. It carries the curated brightness cut + HIP/HD/Bayer/Flamsteed/proper-name attribution + V-band Johnson photometry per row. None of those are trivially reproducible: HD/Bayer/Flamsteed/proper-name attribution requires a SIMBAD or Stellarium-style join.\n\n- **At reduced_m11 (~800k) and reduced_m12 (~2M)** — most rows are Tycho-only or Gaia-only. Classical-ID attribution thins out (very few HD/Bayer/Flamsteed entries at apparent mag \u003e 6.5). AT-HYG at that depth is effectively a pre-computed Gaia magnitude cut with the BP/RP→V transformation baked in. Both pieces are reproducible: one Gaia TAP query + the published Riello+ 2021 photometric transformation.\n\nCritically, the stellata-dch architecture is source-ID-keyed end-to-end. Bailer-Jones distances, Apsis Teff/logg/A0, Gaia 5-parameter astrometry (with NSS / HIP2 fallbacks), and the LMC kinematic override all attach via gaia_source_id — irrespective of whether the row originated from AT-HYG or from a direct Gaia query. That makes a HYBRID pipeline architecturally cheap: AT-HYG classic_ids as the \"labeled bright tier\" + Gaia-native for the deep fill, both feeding the same source-ID-keyed refinement stack and the same v6/v7 binary output.\n\n# Options to evaluate\n\n1. **AT-HYG monolithic** — swap classic_ids → reduced_m11 or reduced_m12 (per cns.1's outcome). One ingest path. Loses the classical-ID attribution density advantage at the deep tier (because it's mostly absent there anyway) but keeps a single schema and a single refresh cadence.\n\n2. **Hybrid** — keep AT-HYG classic_ids as the labeled bright tier (313k); add a Gaia-native row driver for stars beyond classic_ids' coverage up to the chosen magnitude floor. Two row drivers feeding one source-ID-keyed refinement stack.\n\n3. **Gaia-native monolithic** — replace AT-HYG entirely with a Gaia query, reconstructing classical IDs from the committed cross-walks (gaia_dr3_hip_xmatch.tsv, gaia_dr3_tyc_xmatch.tsv) + SIMBAD or Stellarium for HD/Bayer/Flamsteed/proper-name. Largest engineering scope; lowest upstream-dependency surface.\n\n# Tradeoffs to weigh\n\n- **Identifier preservation**: HIP/HD/Bayer/Flamsteed/proper-name drives every label, hover, search, and constellation legend. Options 1 and 2 preserve trivially. Option 3 needs reconstruction via SIMBAD/Stellarium join — non-trivial.\n- **V-band photometry**: AT-HYG carries Johnson V. Gaia gives BP/RP/G. Riello+ 2021 publishes the BP/RP→V transformation; star-physics.ts + the colour LUT (zsr) both consume V, so any Gaia-native row driver needs the transformation as a build step.\n- **Storage / commit-data**: reduced_m12 CSV is ~400 MB (LFS), output binary ~80 MB. B-J subset for 2M rows is ~100-160 MB (LFS); Apsis similar. All tractable. Storage starts to bind closer to 30M+ rows (raw Gaia G\u003c12 across the whole sky) — not at the cns scale.\n- **Render budget**: 800k vs 2M instances is addressed in cns.1, independent of row-driver choice.\n- **Refresh cadence**: AT-HYG releases bundle the BP/RP→V transformation + ID attribution. Gaia-native means refreshing those pieces independently — but the dch Phase 1 refresh-script pattern (scripts/refresh_lib.py + per-source refresh scripts) already handles this shape.\n- **dch architectural fit**: all three options fit cleanly because dch's refinement stack is source-ID-keyed. Hybrid and Gaia-native are slightly \"more dch-native\"; AT-HYG-monolithic just uses AT-HYG's gaia field as the source_id link.\n\n# Acceptance\n\nWritten recommendation in this bead's notes for the row-driver strategy stellata-cns implementation should follow. Should pick one of the three options (or a variant) with explicit reasoning. Verify the recommendation against [[stellata-data-fidelity-principle]] (the bar for catalogue choices) and [[stellata-prioritization-framework]] (deep-tier sits at queue position 7).\n\n# Smoke / followup\n\nDecision-bead — no implementation. The recommendation feeds back into stellata-cns's description; the implementation that follows is what's blocked on stellata-dch shipping (the source-ID-keyed refinement stack is the prerequisite that makes hybrid / Gaia-native cheap).\n\n# Connects\n\n- Sibling: stellata-cns.1 — m11 vs m12 perf + first-load eval. Orthogonal axis: cns.1 picks the depth, this bead picks the row driver.\n- Architectural prerequisite: stellata-dch — source-ID-keyed refinement stack (B-J, Apsis, 5p astrometry, NSS, LMC kinematic) is what makes the hybrid option cheap to implement.\n- Memory: [[stellata-data-fidelity-principle]], [[stellata-prioritization-framework]].","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T10:52:28Z","created_by":"Alex Marshall","updated_at":"2026-05-18T10:52:28Z","labels":["catalog","gaia","phase-7","research"],"dependencies":[{"issue_id":"stellata-cns.2","depends_on_id":"stellata-cns","type":"parent-child","created_at":"2026-05-18T11:52:27Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-gxx.6","title":"LG luminosity — debug-panel tuning, URL flag, chart-mode handling","description":"# Scope\n\nWire user/dev surfaces around the LG luminosity layer:\ndebug-panel knobs, URL flag, chart-mode treatment. Blocked by both\nshader beads landing.\n\n# Deliverables\n\n1. **`local-group-tuning.ts` section** in the debug panel — per-\n   object emission-strength multiplier (for visual calibration in\n   the field), global on/off, profile-family selector for spot-\n   testing alternatives. Mirrors milkyway-tuning.ts structure.\n\n2. **URL flag** — `lge=0` (Local Group emission) disables the\n   layer. Add to url-state.ts envelope per docs/url-state.md\n   process. Default-on; serialised only when non-default per the\n   v3 envelope's presence-mask rule.\n\n3. **Chart-mode handling** — decide between:\n   (a) Hide the volumetric layer entirely (milkyway's pre-isobar\n       choice), letting the existing wireframes carry the LG\n       chart-mode aesthetic.\n   (b) Emit an isobar contour pass (milkyway's current choice),\n       drawing surface-brightness contours per object.\n   Default to (a) for v1; the wireframes already read well in chart\n   mode. Re-evaluate after first smoke.\n\n4. **Picker / hover integration.** Confirm the existing\n   `local-group` hover provider keeps working unchanged (it gates on\n   the wireframe's visibility, which is unchanged). Add no new pick\n   logic — the volumetric pass is decorative-emission on top of the\n   wireframe silhouette which already drives picks.\n\n# Smoke\n\nOpen debug panel → toggle LG emission off/on. Try `?lge=0` in URL,\nconfirm layer hides + URL round-trips. Switch to chart mode, confirm\nvolumetric layer hides per choice (a). Confirm hover cards still\nappear on LG objects.\n\n# Connects\n\nParent: [[stellata-gxx]]. Per [[stellata-lo5-hover-conventions]]\nrule 2 — hover gating stays on visibility only, no additional\nfocus-gating introduced by this bead.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T09:32:52Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:32:52Z","labels":["chart-mode","local-group","physics","shader","ui"],"dependencies":[{"issue_id":"stellata-gxx.6","depends_on_id":"stellata-gxx","type":"parent-child","created_at":"2026-05-18T10:32:52Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-gxx.6","depends_on_id":"stellata-gxx.3","type":"blocks","created_at":"2026-05-18T10:33:01Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-gxx.6","depends_on_id":"stellata-gxx.4","type":"blocks","created_at":"2026-05-18T10:33:02Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-gxx.5","title":"LG luminosity — apparent-magnitude calibration test across N viewpoints","description":"# Scope\n\nAutomated vitest verification that the rendered Local Group luminous\nvolumes integrate to the correct apparent V-magnitude from any\ncamera position, within ±0.1 mag tolerance. Blocked by both shader\nbeads landing.\n\n# Deliverables\n\n1. **`src/client/local-group/local-group-emission-calibration.test.ts`**\n   — places a synthetic camera at N viewpoints:\n   - Deep inside MW (e.g. galactic centre)\n   - At the Sun\n   - 30 kpc out of the disc (top of the galaxy)\n   - Near LMC (50 kpc)\n   - Between MW and M31 (400 kpc)\n   - Beyond M33 (1 Mpc)\n\n   For each viewpoint, for each of {LMC, SMC, M31, M33, one bright\n   dSph}, sample the rendered offscreen framebuffer over the object's\n   projected silhouette, integrate flux, convert back to apparent\n   V-mag via the inverse of the shader's m_V conversion, and assert\n   ≤ 0.1 mag deviation from the analytical prediction.\n\n2. **Pinned numbers.** Per [[stellata-test-coverage-discipline]]\n   rule (b) — every catalog m_V value cited in the test asserts with\n   `toBe(...)` against a fixed expected value, not `toBeCloseTo`\n   with a wide tolerance. The ±0.1 mag tolerance applies to the\n   RENDERED-vs-CATALOG comparison, not to the catalog values\n   themselves.\n\n3. **Pure helper.** The flux → m_V inverse lives in\n   `local-group-emission-pure.ts` (lifted from any inline math in\n   the shader-pair beads, per [[stellata-folder-and-controller-conventions]]\n   rule 4 second-use trigger). Test-imported, never redefined.\n\n# Smoke\n\n`npm test` passes. CI catches regressions automatically.\n\n# Connects\n\nParent: [[stellata-gxx]]. This is the GOAL of the epic — without\nthis test, the calibration claim is unverified.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T09:32:40Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:32:40Z","labels":["local-group","physics","shader","test"],"dependencies":[{"issue_id":"stellata-gxx.5","depends_on_id":"stellata-gxx","type":"parent-child","created_at":"2026-05-18T10:32:39Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-gxx.5","depends_on_id":"stellata-gxx.3","type":"blocks","created_at":"2026-05-18T10:33:00Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-gxx.5","depends_on_id":"stellata-gxx.4","type":"blocks","created_at":"2026-05-18T10:33:00Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-gxx.4","title":"LG luminosity — exponential-disc + Sérsic-bulge shader for M31/M33","description":"# Scope\n\nImplement the volumetric exponential-disc + Sérsic-bulge composite\nemission pass for M31 + M33. Blocked by the data-pipeline bead.\nCan land in parallel with the spheroid-shader bead OR share that\nshader's spheroid pass and add the disc-component on top.\n\n# Deliverables\n\n1. **Exponential-disc fragment integration** in\n   `shaders/local-group-emission.frag.glsl` (or sibling\n   `local-group-disc-emission.frag.glsl` if the two profiles diverge\n   enough that one shader is the wrong abstraction). Profile:\n   `density(R,z) = density0 · exp(-R/R_d) · exp(-|z|/h_z)` per\n   milkyway's disc form. Per-instance uniforms: density0_disc, R_d,\n   h_z, plus the spheroid params for the bulge component overlayed\n   on the same mesh.\n\n2. **Per-instance disc proxy mesh.** Flattened ellipsoid sized to\n   ~10 R_d in plane, ~5 h_z in z, oriented per `local-group.json`'s\n   disc orientation (i, PA) for M31 and M33.\n\n3. **Composite emission.** Disc component + bulge Sérsic component\n   add in fragment space. Total per-fragment emission converts to\n   m_V via the shared magnitude-consistency path.\n\n4. **Tests.** Per-instance read-back of disc params + bulge params.\n   Profile integral check: closed-form exponential-disc total flux\n   = 2π · density0 · R_d² for a face-on infinite disc; mesh-truncated\n   integral should match to within the truncation residual.\n\n# Smoke\n\nRender M31 + M33 from:\n- The Sun → confirm M31 reads at ~m_V 3.4 (visible bright extended\n  glow ~3° across, matching naked-eye observation), M33 at ~5.7\n  (perceptible but faint).\n- A camera position halfway between MW and M31 → M31 should fill\n  much more of the field.\n- A camera inside M31's disc → brightness should rise to luminous-\n  interior levels.\n\nDefers tolerance verification to the calibration-test bead.\n\n# Blocks\n\nThe calibration-test bead.\n\n# Connects\n\nParent: [[stellata-gxx]]. If this and the spheroid-shader bead end up\nsharing \u003e50% of their bodies, fold per [[stellata-named-constants-and-dry]]\nrule 2 — the disc-component becomes a parameterised add-on to the\nshared spheroid raymarch.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T09:32:29Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:32:29Z","labels":["local-group","physics","shader"],"dependencies":[{"issue_id":"stellata-gxx.4","depends_on_id":"stellata-gxx","type":"parent-child","created_at":"2026-05-18T10:32:29Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-gxx.4","depends_on_id":"stellata-gxx.2","type":"blocks","created_at":"2026-05-18T10:32:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-gxx.3","title":"LG luminosity — Sérsic-spheroid raymarched shader + per-instance mesh","description":"# Scope\n\nImplement the volumetric Sérsic-spheroid emission pass for LG objects\nof the spheroid profile family: dSphs, M32, NGC 205, Sagittarius dSph,\nLMC, SMC. Blocked by the data-pipeline bead.\n\n# Deliverables\n\n1. **`shaders/local-group-emission.{vert,frag}.glsl`** raymarched\n   Sérsic-spheroid profile in mesh-local frame. Per-instance uniforms:\n   density0, n_sersic, R_eff (parsec), axis ratios (a/b/c), population\n   colour. Camera-inside-vs-outside ray split mirrors milkyway.\n   Magnitude-consistency conversion at the fragment level is shared\n   with milkyway (extract to a `magnitude-consistency.glsl` chunk if\n   needed, otherwise duplicate per [[stellata-named-constants-and-dry]]\n   rule 2's \"extract a builder\" trigger).\n\n2. **`src/client/local-group/local-group-emission.ts`** owns the\n   InstancedBufferGeometry, per-instance uniform block, mesh\n   construction (proxy ellipsoid sized to enclose the profile out to\n   ~10 R_eff). One mesh per object via instancing.\n\n3. **Integration with `local-group.ts`** — the emission layer is a\n   sibling renderer to the wireframe layer (not a replacement). Both\n   read from the same loaded `LocalGroupObject` array. The volumetric\n   layer adds at additive blend; the wireframe sits at low opacity on\n   top.\n\n4. **Tests.** `local-group-emission.test.ts` exercises:\n   - Instance buffer population (read-back of per-instance density0,\n     n, R_eff from a known set of objects).\n   - Dispose path clears all instanced geometry + uniform buffers\n     ([[stellata-consistency-at-the-seam]] rule 1).\n   - Profile integral check via a pure helper that mirrors the GLSL\n     raymarch on CPU for one canonical sample.\n\n# Smoke\n\nRender LMC + SMC + M32 + Sagittarius + 1-2 dSphs from a few viewpoints.\nVisually confirm:\n- Each object has brightness perceptible at its catalog m_V at the\n  Sun.\n- Brightness rises as the camera approaches.\n- Profile shape (centrally peaked for high n; flat-core for low n)\n  reads in the silhouette.\n\nDefers full apparent-magnitude correctness verification to the\ncalibration-test bead.\n\n# Blocks\n\nThe calibration-test bead (must run after BOTH shader beads land).\n\n# Connects\n\nParent: [[stellata-gxx]]. Per\n[[stellata-folder-and-controller-conventions]] rule 1 — files live\nin src/client/local-group/. Per rule 3 — the emission layer is its\nown controller (state + tick + dispose), not bolted onto\nlocal-group.ts.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T09:32:15Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:32:15Z","labels":["local-group","physics","shader"],"dependencies":[{"issue_id":"stellata-gxx.3","depends_on_id":"stellata-gxx","type":"parent-child","created_at":"2026-05-18T10:32:15Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-gxx.3","depends_on_id":"stellata-gxx.2","type":"blocks","created_at":"2026-05-18T10:32:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-gxx.2","title":"LG luminosity — extend build pipeline + overrides for photometry \u0026 profile params","description":"# Scope\n\nPlumb per-object photometric + profile data through the LG build\npipeline so the renderer has what it needs. Blocked by the design-gate\nbead's per-object data plan.\n\n# Deliverables\n\n1. **Extend `scripts/build-local-group.ts`** to surface from LVDB:\n   - `apparent_magnitude_v` (and `_em`, `_ep`)\n   - `rad_sersic`, `n_sersic` (with units → parsec at object distance)\n   - `surface_brightness_rhalf` (mag/arcsec²)\n   - `rhalf_physical` (parsec) as the fallback half-light radius when\n     Sérsic params are absent\n   - `ellipticity`, `position_angle` (already used by wireframe, may\n     need surfacing if not already in the loader output)\n\n2. **Extend `data/local-group/overrides.tsv`** schema with optional\n   columns for hand-curated photometry on standalone rows (M31/M33)\n   and override columns for LVDB rows where the dwarf_all summary is\n   too coarse:\n   - `apparent_magnitude_v_override`\n   - `profile_family` (`sersic` | `exp_disc_sersic_bulge`)\n   - `disc_scale_length_pc` (exp-disc family only)\n   - `bulge_re_pc`, `bulge_n` (exp-disc family only)\n   - `population_color_rgb` (optional, per-object population tint —\n     defaults to a neutral if absent)\n   Populate M31 + M33 photometry: M31 m_V ≈ 3.4, M33 m_V ≈ 5.7,\n   M31 disc R_d ≈ 5.3 kpc, M33 R_d ≈ 1.8 kpc. Cite paper DOIs in\n   `ref_doi` column.\n\n3. **Offline DENSITY0 solver** runs as a build step (in\n   `scripts/build-local-group.ts` or a sibling `solve-densities.ts`):\n   given per-object catalog m_V + distance + profile geometry, solve\n   the per-object density normalisation that makes the analytical\n   profile integral equal the catalog flux. Implementation mirrors\n   the closed-form result in docs/milky-way.md § Magnitude\n   consistency. Result is written into `local-group.json` as a per-\n   object `density0` field consumed by the shader.\n\n4. **Schema update + tests.** `scripts/build-local-group-pure.ts`\n   gains test coverage for the photometry → density0 solver\n   (closed-form check on a known Sérsic profile, vs an exponential\n   disc, vs M31's documented parameters). Per\n   [[stellata-test-coverage-discipline]] rule (b), pin numeric\n   solver outputs with `toBe(N)`.\n\n# Smoke\n\nRegen local-group.json + spot-check 5 entries (LMC, SMC, M31, M33,\none ultra-faint dwarf) — confirm density0 + profile_family + V-mag\nfields are present and within expected ranges.\n\n# Blocks\n\nThe Sérsic-spheroid shader + exponential-disc shader beads.\n\n# Connects\n\nParent: [[stellata-gxx]]. Per [[stellata-data-fidelity-principle]]\nthe solver must work for ANY LVDB row, not a famous-star carve-out.\nPer [[frozen-external-data]] no live fetches.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T09:32:00Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:32:00Z","labels":["local-group","physics","shader"],"dependencies":[{"issue_id":"stellata-gxx.2","depends_on_id":"stellata-gxx","type":"parent-child","created_at":"2026-05-18T10:32:00Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-gxx.2","depends_on_id":"stellata-gxx.1","type":"blocks","created_at":"2026-05-18T10:32:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-3f8.1","title":"Ingest Gaia FLAME stellar ages + masses when DR4 releases","description":"Ingest Gaia FLAME (Final Luminosity Age Mass Estimator) per-star age + mass estimates from `gaiadrN.astrophysical_parameters` for the AT-HYG.gaia source-id subset, so future Stellata work has age as a per-star attribute alongside Teff / logg / [M/H].\n\n# Why deferred to DR4 (not DR3)\n\nFLAME ages in DR3 are produced by isochrone-fitting on Apsis-derived (Teff, L, [M/H]). For MS field stars (the bulk of Stellata's catalogue) the uncertainty is ±2-4 Gyr typical — too noisy to drive any honest user-visible visualisation. Turnoff stars and giants are tighter, but they're a minority of what we render.\n\nDR4 substantially improves FLAME because:\n- Longer astrometric baseline → tighter parallaxes → tighter luminosity.\n- Apsis pipeline upgrades feed cleaner (Teff, logg, [M/H]) inputs to FLAME.\n- Expanded epoch photometry → some asteroseismic enhancement for evolved stars.\n\nPer ESA's published roadmap, DR4 FLAME posteriors are expected to narrow significantly relative to DR3. Not worth a separate one-shot DR3 ingest only to redo it on DR4 release.\n\n# Origin\n\nFiled out of the `stellata-zsr.1` star-colour-calibration research (`research/star-spectral-rendition/RECOMMENDATION.md` + apsis_coverage probe). The Apsis empirical coverage work made it concrete that FLAME columns (age_flame, mass_flame, plus uncertainties) sit in the same `astrophysical_parameters` table as Apsis Teff/logg, so the ingest mechanics are largely shared with the Apsis Teff ingest filed separately under stellata-zsr.\n\n# Scope (revisit at un-defer time)\n\n- New `scripts/refresh-gaia-flame.py` querying `gaiadrN.astrophysical_parameters` for FLAME columns by source_id list. Reuse the shared TAP client (will exist by then — currently in flight as stellata-dch.21).\n- Output: `data/gaia_drN_flame.tsv` (LFS, ~10-15 MB for ~300k rows × ~6 columns).\n- Optional v6+ binary surfacing: add `age` (float16, ±1 Gyr precision) and `mass` (float16) per record. Whether to bake into the binary or load as a side table keyed by source_id is a runtime-vs-bundle-size call at un-defer time.\n- Acceptance: per-star age + uncertainty pinned for AT-HYG.gaia rows where FLAME returns. Coverage stats logged. Smoke on a known-age population (Pleiades members ~110 Myr; Sol ~4.6 Gyr) confirms returned ages are within expected bounds.\n\n# Downstream uses (unlocked by, not required by, this ingest)\n\n- Age-based representational layer (artificial-viz filter under stellata-io3).\n- Mass refinement for the Stefan-Boltzmann physical-radius chain (currently uses spectral-class-only Teff for non-WD stars).\n- Stellar population age maps for Local Group / future galactic-arm visualisation.\n\n# Why NOT under stellata-dch\n\nUser-stated decision (Alex, 2026-05-18): keep this separate from the dch source-ID-anchored pipeline rewrite. dch's scope is astrometry + cross-walks + NSS + Bailer-Jones; FLAME ages are a downstream science-product ingest that rides naturally on AT-HYG refresh timing rather than on the catalogue-architecture rewrite.\n\n# Why parent stellata-3f8\n\nstellata-3f8 (\"Refresh AT-HYG (or replace) when Gaia DR4 releases\") is the umbrella for DR4-gated catalog work. FLAME ingest is naturally a child of that umbrella — when DR4 lands, refresh AT-HYG AND pull FLAME products in the same release cycle.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-18T07:59:18Z","created_by":"Alex Marshall","updated_at":"2026-05-18T07:59:24Z","labels":["catalog","gaia","ingest","physics"],"dependencies":[{"issue_id":"stellata-3f8.1","depends_on_id":"stellata-3f8","type":"parent-child","created_at":"2026-05-18T08:59:18Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3f8.1","depends_on_id":"stellata-zsr.1","type":"related","created_at":"2026-05-18T08:59:22Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-zsr.2","title":"Display P3 wide-gamut output for star colour (zsr.1 Tier 3)","description":"Part of `stellata-zsr.1` research outcome — Tier 3 follow-on.\n\n# Why\n\n`stellata-zsr.1` shipped a Planck + CIE 1931 → sRGB blackbody pipeline\nfor star colours (Tier 1, separate bead). Alex asked whether Display P3\noutput on supporting devices would buy additional fidelity. Empirical\nfinding in the research probe:\n\n- Display P3 and sRGB share the **same blue primary** (DCI-P3 blue is\n  (0.15, 0.06), identical to sRGB blue) — there is NO gamut win at\n  the hot blue end (O/B stars) where I initially expected one.\n- The Planckian locus stays inside both gamuts for all T \u003e ~1700 K.\n  Stellata's catalog floor is ~3000 K (Proxima at 3170 K is the\n  coolest commonly-displayed star). So the per-star LUT sits\n  comfortably inside sRGB.\n- See `research/star-spectral-rendition/p3_swatches.png` (P3-ICC-tagged):\n  only the synthetic 1500 K row shows a perceptible delta. Every\n  actual reference star in our catalog renders identically on P3\n  vs sRGB monitors.\n\nSo Tier 3 is **explicitly NOT a colour-fidelity win for blackbody stars.**\nFile it anyway because:\n\n1. The infrastructure (canvas colour-space negotiation, dual-LUT\n   pattern, gamut-aware shader audit) is a generic prerequisite for\n   any future wide-gamut work in Stellata (nebula emission lines —\n   strong Hα ~ 0.708xy, well outside sRGB; planet auroral colours;\n   chart-mode legend chips with saturated UI palettes).\n2. Documenting \"P3 was investigated and found to be a no-op for the\n   current renderer\" prevents future sessions from re-investigating.\n\n# What to do\n\nBuild the wide-gamut output path so that it's available the day\nnon-Planckian saturated colours appear in the renderer. The work itself:\n\n1. **Device detection** — `window.matchMedia('(color-gamut: p3)').matches`\n   + optional override via `?gamut=p3` URL param for QA.\n2. **Canvas colour-space switch** — request P3 framebuffer:\n     `renderer.getContext().getContextAttributes()` to confirm + set\n     `renderer.outputColorSpace = THREE.DisplayP3ColorSpace` on detect.\n   Fallback to `SRGBColorSpace` otherwise. Requires Three.js r152+\n   (currently shipped in this project per package.json).\n3. **Dual LUT emit** — extend the Tier 1 LUT generator\n   (`scripts/blackbody-lut.ts`) to emit both `blackbody-lut-srgb.bin`\n   and `blackbody-lut-p3.bin`. Single Python helper, two output paths\n   (mirrors `research/star-spectral-rendition/blackbody_color.py`'s\n   `XYZ_TO_LIN_SRGB` + `XYZ_TO_LIN_P3` factoring).\n4. **Texture colour-space audit** — every texture sampled in the\n   star/planet/cloud/dust shaders must carry the correct\n   `texture.colorSpace`. Currently most are sRGB-encoded — moving to\n   a P3 canvas without retagging inputs causes subtle hue shifts.\n   Scope: enumerate every `new THREE.*Texture()` callsite.\n5. **Additive-blend audit** — additive blending must happen in LINEAR\n   space, not gamma-encoded. Current shaders do some math in\n   gamma-encoded space (super-Gaussian intensity is sRGB-encoded then\n   summed). Going wide-gamut without auditing causes hue drift where\n   multiple star glows overlap. Audit `star.frag.glsl`,\n   `planet.frag.glsl`, `cloud.frag.glsl`, `dust-particle.frag.glsl`,\n   `perceptual-disc.glsl`.\n6. **Bloom / post-process** — if any post-processing passes exist,\n   confirm they operate in linear-light and round-trip through the\n   canvas colour space correctly. No bloom shipped today; flag for\n   future.\n7. **Test** — visual smoke on a P3 monitor (Alex has one):\n   `npm run dev`, focus a saturated chart-mode glyph or a future\n   nebula layer, verify the gamut expansion is visible. Compare\n   against sRGB-only viewing (Chrome Incognito on a non-P3 display,\n   or a forced-sRGB DevTools toggle).\n\n# Files likely touched\n\n- `scripts/blackbody-lut.ts` (new in Tier 1) — emit two LUTs.\n- `src/client/star-pipeline.ts` (or wherever Tier 1 lands the LUT\n  load) — runtime branch on detected gamut.\n- `src/client/stellata.ts` — `renderer.outputColorSpace` set on init.\n- `src/client/shaders/*.glsl` — audit additive-blend math + texture\n  decode operations.\n- `SCIENCE.md` — document the P3 path in the colour-rendering section.\n- `docs/rendering.md` — render-stack section gets a \"colour-space\"\n  paragraph naming the choice (P3 on detect, sRGB otherwise).\n\n# Acceptance\n\n- LUT generator emits both `*-srgb.bin` and `*-p3.bin` artefacts;\n  the P3 variant is byte-validated against the\n  `research/star-spectral-rendition/blackbody_color.py`\n  `blackbody_to_displayp3` reference (vitest assertion).\n- Canvas colour space is observed via `renderer.getContext()` on a\n  P3 monitor; observed value is `'display-p3'`.\n- Every texture in the scene has an explicit `colorSpace` field set;\n  no defaults.\n- Manual smoke (Alex): the chart-mode legend chips and (if shipped)\n  any nebula emission renders show visibly more saturated chroma on\n  a P3 monitor vs forced-sRGB. Per-star blackbodies render IDENTICALLY\n  in both — that's the expected no-op outcome.\n\n# Out of scope\n\n- HDR (Display P3 is wide gamut but standard dynamic range; HDR\n  output would be a separate epic).\n- BT.2020 / Rec2100 — wider than P3, no broad device support yet.\n\n# Connects\n\n- `stellata-zsr.1` — research bead that surfaced this work\n- The Tier 1 blackbody LUT bead (filed separately under `stellata-zsr`) — must land first; this\n  bead extends its LUT generator + load path.\n- `stellata-data-fidelity-principle` — \"best possible model based on\n  current observational data\" implies using the best display path\n  available too, not just the best data source.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-17T18:06:04Z","created_by":"Alex Marshall","updated_at":"2026-05-17T18:06:48Z","labels":["research","shader","ux"],"dependencies":[{"issue_id":"stellata-zsr.2","depends_on_id":"stellata-zsr","type":"parent-child","created_at":"2026-05-17T19:06:03Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.45","title":"Gaia-DR3-aware filter for CCDM-flagged optical doubles (Albireo, etc.)","description":"# Problem\n\n`applyDoublesFlag` in `scripts/build-catalog.ts` sets the chart-mode binary wings glyph on AT-HYG rows where the Hipparcos CCDM cross-match (`data/hip_ccdm.tsv`) lists a CCDM ID **and** Hipparcos's `MultFlag ∈ {C, G, O}`. This rule is pre-Gaia: it trusts Hipparcos's own multiplicity confidence call, which was made on positional / photometric evidence in the 1990s without parallax precision below ~mas.\n\nGaia DR2 (2018) and DR3 have since revealed that some CCDM/MultFlag-flagged pairs are **optical doubles**, not gravitationally bound systems. The components sit along nearly the same line of sight from Sol but are at very different distances. The discovered case is Albireo (β¹ + β² Cyg), where both rows have Gaia DR3 distances:\n\n- β¹ Cyg (HIP 95947, AT-HYG `proper=\"Albireo\"`): 111.34 pc\n- β² Cyg (HIP 95951, AT-HYG `proper=\"Albireo B\"`): 122.11 pc\n- 3D separation: **10.77 pc / 35.1 ly** — too distant to be gravitationally bound by orders of magnitude.\n\nCurrent behaviour: chart mode draws the binary wings glyph on Albireo, asserting binarity that current astronomy retracted around 2018. This contradicts the project's data-fidelity principle. Similar misflags likely exist on a small handful of other culturally significant CCDM/MultFlag-confirmed pairs where Gaia later split the distances.\n\n# Proposed fix\n\nAdd a Gaia-aware suppression step to `applyDoublesFlag`:\n\n1. When BOTH components of a CCDM-flagged system have **Gaia-derived distances** available in AT-HYG (`dist_src ∈ {G_R3, BJ, LMC_KIN}` — the `BJ` value is introduced by Bead Z / dch.47 (Bailer-Jones distance override); the `LMC_KIN` value is introduced by Bead AA / stellata-8l0 (LMC kinematic refinement); both extend the dist_src enum). Compute the 3D separation in pc.\n2. If the 3D separation exceeds a \"definitely-not-bound\" threshold (suggested: **1.0 pc**, well above the widest known bound systems at ~1 pc with multi-million-year orbits), suppress the wings glyph for that system — do not OR `0x10` onto the primary's flags.\n3. Preserve current behaviour when either component lacks a Gaia-derived distance (fall back to trusting Hipparcos's MultFlag, as today).\n\nThis is the natural place to fit it into the source-ID-anchored rewrite — Gaia-derived distances are authoritative for the new Layer-1 data set, so the 3D-separation check is a one-line addition once the cross-walk is in place. The predicate accepts ALL Gaia-derived distance values (raw `G_R3`, Bayesian-refined `BJ`, kinematic-refined `LMC_KIN`) so the filter stays correct as new refinement layers are added.\n\n# Acceptance\n\n- Albireo's chart-mode binary wings glyph no longer fires (β¹/β² 3D separation of 10.77 pc exceeds the 1.0 pc cut).\n- True wide binaries within the cut survive — Mizar+Alcor (0.21 pc / 0.68 ly), ε¹+ε² Lyr (0.29 pc / 0.94 ly), α Cen A+B (~0 pc within catalogue precision) all keep their glyphs.\n- Predicate uses `dist_src ∈ {G_R3, BJ, LMC_KIN}` so it stays correct after Beads Z + AA land.\n- The suppressed-glyph set should be auditable: surface in a build-log line or a small artefact so each removal is reviewable against the literature, rather than silent.\n\n# Open question\n\nWhether to *also* introduce a \"this looks like an optical double, not a binary\" UI affordance for the rare suppressed cases. Probably out of scope for this issue — discuss in design before adding.\n\n# Discovered from\n\nPR #78 (README rewrite, \"Things to try\" section). When verifying that Albireo's two AT-HYG rows render as separate stars, noticed they're at very different distances; the modern post-Gaia consensus is that Albireo is an optical pair, not a binary.\n\n# Out of scope\n\n- Generalising to non-CCDM-anchored binary inference (geometric pass, etc.) — separate concern.\n- Building a full \"optical double catalogue\" with affordances — see open question above.\n- Refreshing CCDM itself or replacing MultFlag with Gaia NSS — the broader source-ID rework that parent epic already covers.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-17T12:57:18Z","created_by":"Alex Marshall","updated_at":"2026-05-17T17:08:16Z","dependencies":[{"issue_id":"stellata-dch.45","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-17T13:57:18Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-dch.45","depends_on_id":"stellata-dch.47","type":"related","created_at":"2026-05-17T18:04:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-l5d","title":"Manual-override path for AT-HYG-dropped culturally-important stars (Mu Cep / Garnet Star, etc.)","description":"# Problem\n\nSome culturally-important stars are entirely absent from the model because AT-HYG's upstream row for them lacks the fields the preprocessor requires (`x0`, `y0`, `z0`, `absmag`). The preprocessor drops these rows in `scripts/build-catalog.ts`, so they never appear in `catalog.bin` or `search-index.json`. Searching by HIP, HD, Bayer, or proper name all return nothing — the star isn't there.\n\n# Known case\n\n**μ Cephei (Herschel's Garnet Star).** HIP 107259 / HD 206936. AT-HYG's row carries `bayer=Mu`, `con=Cep`, `proper=Garnet Star`, but every distance / position / magnitude field is empty (`dist_src=N`, indicating \"no distance available\"). Distance to μ Cep is genuinely hard — historically uncertain Hipparcos parallax, and Gaia DR3 saturates on bright cool supergiants — but ground-truth values from the literature exist (~870 pc; absmag around -7).\n\n# Likely other cases\n\nOther bright, well-known cool/red supergiants are exposed to the same Gaia-DR3-saturation failure mode and may be similarly dropped. Worth a one-off audit: count AT-HYG rows that have a `proper` or `bayer` field but no `dist`. A handful of those are likely culturally important and recoverable.\n\n# Proposed fix\n\nMirror the existing `data/local-group/overrides.tsv` pattern: a small whitelist of stars where the maintainer supplies distance + absmag + (optionally) spectral class from primary literature, merged in at preprocessing time after the AT-HYG read but before the drop filter. Each override row cites its source paper / DOI to preserve the data-fidelity principle (SCIENCE.md).\n\nSuggested shape:\n\n- `data/star-overrides.tsv` — columns: `hip`, `dist_pc`, `absmag`, `spect` (optional), `source_doi`, `notes`.\n- `scripts/build-catalog.ts`: load the overrides table after the AT-HYG read; for any HIP in both the AT-HYG and the overrides tables, fill in missing `dist`/`x0`/`y0`/`z0`/`absmag`/`spect` from the override (recomputing Cartesians from the AT-HYG ra/dec + override distance). Rows present in overrides but not in AT-HYG are skipped (not in our scope).\n- SCIENCE.md: new subsection under \"Stellar catalog ingestion\" or \"Modelling decisions deliberately not made\" documenting the override path and listing the entries.\n\n# Acceptance\n\n- μ Cep renders in the model, findable by \"Mu Cep\", \"Garnet Star\", HIP 107259, and HD 206936.\n- Override entries are cited with primary literature DOIs in SCIENCE.md.\n- Override table is small (single-digit entries to start) and grows by explicit maintainer decision, not by sweep.\n\n# Out of scope\n\n- Refreshing AT-HYG when Gaia DR4 lands (separate concern; DR4 may resolve some of these natively).\n- Adding stars that have no AT-HYG row at all.\n- Catalogue-wide reprocessing or filling in absmag for all distance-missing rows (over-aggressive; would pollute the model with bad data).\n\n# Discovered from\n\nCurating the \"Things to try\" section in PR #78 (README rewrite); paired with stellata-tqh (GCVS-search-index gap).\n","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-17T12:03:03Z","created_by":"Alex Marshall","updated_at":"2026-05-17T12:03:03Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-tqh","title":"Add GCVS variable-star designations to search-index.json","description":"# Problem\n\nVariable stars whose primary cultural name is their GCVS designation (R CrB, T Tau, VY CMa, χ Cyg, RR Lyr, SS Cyg, RS Oph, ZZ Cet, etc. — anything from the GCVS letter sequence R, S, T … RR, RS … through ZZ then AA … QZ) are findable in the app only via HIP / HD numeric lookup. Searching by the familiar GCVS name returns nothing.\n\nDiscovered while curating a \"Things to try\" list for the README: tried to point users at VY Canis Majoris by name, found that \"VY CMa\" / \"VY Canis Majoris\" / \"VY\" all return no result because AT-HYG carries no `proper`, `bayer`, or `flam` for the row — only `hip=35793` / `hd=58061`.\n\n# Why this happens\n\nThe GCVS cross-match in `scripts/build-catalog.ts` already attaches `period` and `mag_amplitude` to each AT-HYG row by looking up the row's HIP (then HD) in `crossid.txt` and pulling the matching `gcvs5.txt` row. The GCVS name is the lookup key for that join, but it is currently consumed and discarded — it never gets emitted into `search-index.json`.\n\n# Proposed fix\n\nCarry the GCVS designation through the cross-match into the search index as an additional indexed alias for the AT-HYG row. Likely smallest change:\n\n- `scripts/build-catalog.ts`: when the GCVS cross-match succeeds, attach the GCVS designation (e.g. `\"VY CMa\"`, `\"R CrB\"`) onto the in-memory star record alongside the existing period / amplitude.\n- `scripts/build-catalog.ts` (search-index emit path): include the GCVS name as an additional searchable string for that star, alongside `proper`, `bayer`, `flam`, `hip`, `hd`, `hr`, `gl`.\n- Typeahead: should match the new field automatically if the search layer iterates over all string fields of each row — verify, no shader / renderer changes needed.\n\n# Acceptance\n\n- Searching `VY CMa`, `R CrB`, `T Tau`, `χ Cyg`, `RR Lyr` in the app resolves to the corresponding underlying AT-HYG row.\n- Mixed case / spacing tolerated as elsewhere (`vy cma`, `R Coronae Borealis` if the full GCVS form is stored — TBD whether to expand or keep abbreviated).\n- ~3,700 GCVS-cross-matched stars become findable by their GCVS name.\n\n# Out of scope\n\n- Adding ALL GCVS catalogue entries (only the ones successfully cross-matched into AT-HYG; non-cross-matched variables stay invisible).\n- Resolving GCVS-named stars that don't match any AT-HYG row (those are still unaddressable).\n\n# Discovered from\n\nCurating the \"Things to try\" section in PR #78 (README rewrite).\n","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-17T12:02:35Z","created_by":"Alex Marshall","updated_at":"2026-05-17T12:02:35Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-r8l","title":"Doc migration from memory grooming (blocked on 9mm.194)","description":"Migrate canonical content from 6 bd memories to docs / SCIENCE.md per the durable-principles-in-codebase-not-memory rule established with Alex 2026-05-16. Blocked until 9mm.194 closes — Alex 2026-05-16: \"all PRs are blocked until that epic is done.\" Memory grooming session 2026-05-16 drafted the migrations in worktree `memory-grooming-doc-migration`; worktree exited without committing per [[alex-never-push-to-main]] (bd-only sessions don't open PRs).\n\n# Why deferred\n\nThe grooming-pass session started doc edits in CLAUDE.md (new \"Folder \u0026 module conventions\" section) and in SCIENCE.md (new \"Scope principles\" section). The CLAUDE.md edits collide directly with 9mm.194.2's stated deliverable (\"CLAUDE.md gets 'Folder \u0026 module conventions' section\"). The folder-roster updates that 9mm.194.1 ships also touch CLAUDE.md's repo-layout block. Even non-conflicting doc edits (SCIENCE.md, docs/build-and-data.md, docs/architecture.md, docs/hover.md) are gated by the all-PRs-blocked rule, so the whole set defers.\n\n# The migration plan — mechanical after 194 closes\n\nEach step lifts content from a memory into a permanent home, then bd-forgets the memory. Each is ~one topical commit per [[commit-granularity]]. Do all five in one PR with skip-version-bump label (pure docs).\n\n## 1. docs/hover.md (NEW FILE)\n\nSource: `bd memories stellata-lo5-hover-conventions`.\n\nContent shape:\n- Brief architecture overview: hover-engine.ts (owns pointer listener, 280ms dwell, 14px pick threshold, disambiguator); hover-types.ts (HoverProvider contract: pick + format); hover-pick-disambiguator.ts (camera-distance + prime/fallback tiering); *-hover-provider.ts (one per layer); formatters/*-hover-format.ts (one per layer, pure with vitest coverage).\n- Rule 1 — Spell out unit/quantity terms (Radius, Period, Vmag verbatim).\n- Rule 1a — Line ordering: name → distance+Vmag → Period → Radius (observer-relative first, intrinsic below).\n- Rule 2 — Visibility ⇒ hoverable; no focus-gate on hover. Provider gate mirrors renderer draw predicate. No focus/selection/mode/route state in the gating.\n- Rule 3 — Whole-object hit surface for extended visible objects. Three reusable pick mechanisms: Three.js raycast against mesh (clouds), projected sample-point AABB (heliopause via shared HELIOPAUSE_SAMPLE_POINTS_LOCAL), per-object angular-size disc (LG). Hit-test SVG label bbox when present, pulling element id from shared exported constant.\n- Rule 4 — HTML hover-card typography stays var(--font-mono) even in chart mode. Chart-mode body.monochrome .tooltip CSS swaps bg/color only, not font-family. The boundary is HTML-vs-SVG, not chart-vs-not-chart.\n\nThen add doc-index entry in CLAUDE.md (under docs/overlays.md):\n- \"**docs/hover.md** — hover-label engine, per-layer providers and formatters, and the four UX conventions (spell out units, no focus-gate on hover, whole-object hit surface for extended objects, HTML monospace typography in chart mode). Read when touching anything in src/client/hover/ or adding a hover surface to a new layer.\"\n\nThen: `bd forget stellata-lo5-hover-conventions`.\n\n## 2. SCIENCE.md § Scope principles (NEW section, right after intro before \"## Data sources\")\n\nSources: `bd memories stellata-data-fidelity-principle`, `bd memories stellata-cds-data-sources-and-detail-gradient`, `bd memories stellata-defer-detail-until-zoom-affordance`.\n\nThree subsections under one umbrella \"Scope principles\" heading:\n\na. **Data fidelity — \"best possible model based on current observational data and knowledge\"** — six rules: default to upfront generalised solution not famous-star carve-out + heuristic fallback; prefer official source-ID cross-walks (HIP, Gaia, NSS, SIMBAD) over position-based matching; hand-curated overrides only for singular edge cases or catalog-construction omissions (M31/M33 in LVDB dwarf_all); prefer ship-more-accurate-later for catalog work; all matching must survive catalog upgrades; validation at scale (parallel automated check, not 5-star spot-check).\n\nb. **Detail gradient — highest-density measurable info near Earth, simpler model further out** — per-object near, statistical far. When scoping a layer beyond AT-HYG reach, prefer statistical / aggregate sources (HiPS-derived counts, binned populations) over hand-extending per-object data. Cross-references: bd memory stellata-prioritization-framework (closer-to-Sol layer queue); bd issue stellata-36y (CDS/VizieR ecosystem orientation).\n\nc. **Defer detail until zoom affordance** — billboarded discs sized θ = 2·atan(R/d) floor at pixel-size minimum at any host-relative camera distance more than a few thousand body-radii. Coding detail before the user can perceive it is wasted effort. File per-object detail (textures, banding, haloes, ring systems, exoplanet bodies) under the relevant zoom-in epic (stellata-2f6 for planets, stellata-bk5.3 for exoplanet bodies); ship the layer without it.\n\nThen: `bd forget stellata-data-fidelity-principle stellata-cds-data-sources-and-detail-gradient stellata-defer-detail-until-zoom-affordance`.\n\n(NOTE: SCIENCE.md already references all three by name at lines ~414, 419, 598. After the migration, those references resolve to in-doc anchors instead of memory keys — update them in the same PR.)\n\n## 3. docs/build-and-data.md § Frozen external data (NEW section)\n\nSource: `bd memories frozen-external-data`.\n\nContent: external scientific catalogs in the pipeline are committed under data/ and read from disk at build time. No requests.get / urllib / astroquery / fetch in npm run build or the Python preprocessor. Why: build keeps working when sources go offline / change schemas / move URLs. Existing pattern (AT-HYG, GCVS, Stellarium, Edenhofer, LVDB, Hipparcos CCDM) reflects this. Adding new external data: fetch once, commit raw file under data/ (LFS if \u003e1 MB), document source URL + retrieval date in SCIENCE.md, build scripts read from disk only, fetch helpers named explicitly (scripts/refresh-*.py) and gated from npm run build.\n\nThen: `bd forget frozen-external-data`.\n\n## 4. docs/architecture.md § Authoring patterns — consistency at the seam (NEW section, append at end)\n\nSource: `bd memories stellata-consistency-at-the-seam`.\n\nFour shape subsections:\n\na. **Lifecycle pairing** — every long-lived resource has its teardown wired in the SAME diff that introduces it. bus.on() returns unsub stored on dispose; pool/buffer growth has hard cap or shrinkIfIdle; subarray views across method boundary document lifetime. Example: EventBus had no clear(), cross-session subscriptions leaked.\n\nb. **Sibling symmetry** — sibling functions/helpers/branches (lambertian vs mallama, encode vs decode, v2 vs v3 schema, prime vs fallback picker) must clamp/assert/log identically or document the asymmetry. Example: mallamaPhaseFactor didn't clamp α while lambertianPhaseFactor did.\n\nc. **Sentinel-init for dirty-track** — sentinel initial value MUST fail comparison on first write (NaN, -Infinity, poison-string). Hide/dispose/reset paths reset every numeric sentinel and cached input. Cache keys include every input dimension. Example: pointerEvents = \"\" sentinel matched steady-state, first-frame write was skipped.\n\nd. **Single source of truth for time / camera state / world offset** — code needing wall-clock t reads via Stellata.getT(), never Date.now(). Mid-animation state mutations either make the struct frame-coherent or document an explicit phase invariant. Example: PlanetBodyField.attachHost called Date.now()/1000 instead of getT().\n\nClosing: when to apply. Write-time rules, not review-time. When adding bus.on, find dispose path of same file in same diff. When implementing one of a sibling pair, copy-skim the sibling. When adding a sentinel, write the first-write assertion explicitly.\n\nThen: `bd forget stellata-consistency-at-the-seam`.\n\n## 5. stellata-folder-and-controller-conventions (handled by 194.2)\n\nAlready 9mm.194.2's stated deliverable. After 194.2 ships AND CLAUDE.md \"Folder \u0026 module conventions\" section contains all 5 rules (not just 1-3), `bd forget stellata-folder-and-controller-conventions`. Audit before forgetting: confirm rules 4 (pure helpers extract at second use) and 5 (no multi-paragraph in-code prose) are in the CLAUDE.md section — those are the rules the memory says it carries in full vs CLAUDE.md's short form.\n\n# Acceptance\n\n- Five doc edits land (one PR, five topical commits per [[commit-granularity]]).\n- skip-version-bump label set; release-notes-guard CI passes.\n- Six memories forgotten (1 → 1; 2 → 3; 3 → 1; 4 → 1; 5 → 1 by 194.2; total 7 forgotten across this bead + 194.2).\n- bd memories count drops from 29 → 22 (verify via `bd memories | head -5` or memory count in bd prime header).\n- SCIENCE.md cross-references at lines ~414, 419, 598 updated from memory keys to in-doc anchors.\n\n# Smoke\n\nAfter each `bd forget`, search the target doc for the key phrase from the dropped memory to confirm the concept is reachable. E.g. after forgetting stellata-lo5-hover-conventions, `grep -i 'vmag\\|visibility ⇒ hoverable' docs/hover.md` should hit.\n\n# Connects\n\n- [[stellata-9mm.194]] — hard blocker; 194's CLAUDE.md edits must land first.\n- [[stellata-9mm.194.2]] — handles rule 5 above (folder/controller migration).\n- [[bd-data-grooming-protocol]] — this bead is the carry-over from a memory-grooming pass.\n- [[defer-doc-updates]] — five doc additions ride one coherent PR, not piecemeal.\n- [[alex-never-push-to-main]] — the bd-only-session exit rule that aborted the grooming-session worktree.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T22:01:44Z","created_by":"Alex Marshall","updated_at":"2026-05-16T22:01:44Z","labels":["cleanup","docs","pr-87"],"dependencies":[{"issue_id":"stellata-r8l","depends_on_id":"stellata-9mm.194.2","type":"related","created_at":"2026-05-16T23:01:58Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-r8l","depends_on_id":"stellata-9mm.194.8","type":"blocks","created_at":"2026-05-16T23:01:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.187.7","title":"Hover-engine misc hygiene: unused params + CSS-tied magic numbers","description":"# Findings from PR #72 (stellata-lo5) review — bundle of two nits\n\n## (1) Unused \\`_pixelThreshold\\` in heliopause + cloud Stellata pickers\n\n\\`Stellata.pickHeliopauseHit(clientX, clientY, _pixelThreshold = 14)\\`\nand \\`Stellata.pickCloudHit(clientX, clientY, _pixelThreshold = 14)\\`\naccept a pxThreshold the engine threads through, but neither uses it\n(heliopause runs a silhouette-bbox + label-rect test; cloud\ndelegates to raycast). Underscore prefix marks intentional but\nshadows the engine-contract signature.\n\nEither:\n- Drop the param from the heliopause / cloud public signatures (and\n  matching providers stop forwarding it), or\n- Document why the param is ignored (one-line comment in each method).\n\n## (2) \\`TOOLTIP_MAX_HEIGHT_PX = 96\\` not tied to CSS\n\n\\`hover-engine.ts:29\\` clamps the tooltip's bottom edge to\n\\`window.innerHeight - 96\\`, where 96 is a hand-calibrated worst-case\nheight for \"5 lines + name\". The constant has no programmatic tie to\n\\`styles.css\\` — tweaks to tooltip line-height / padding silently drift\nthe clamp.\n\nFix: derive at runtime via \\`getBoundingClientRect()\\` on the rendered\ntooltip (one frame's worth of measure-then-position) so the clamp\nfollows whatever the CSS produces.\n\n# Why\n\nBoth are minor — neither user-visible at v1.0. Bundled because both\nsit in the same file and the same code-quality theme. P3 unless\neither becomes blocking.\n\n# Acceptance\n\n- One commit per sub-task, or a single combined commit if both stay\n  small.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T21:24:38Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:24:38Z","labels":["hover","nit","pr-72"],"dependencies":[{"issue_id":"stellata-9mm.187.7","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-16T22:24:37Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.187.6","title":"Tighten HoverPayload contract — providers return null on degenerate","description":"# Finding from PR #72 (stellata-lo5) review\n\n`hover-engine.ts` calls `winner.provider.format(winner.hit)` and\nrenders the returned `{ name, lines }` directly. The disambiguator\nguards `winner === null` (no provider hit) but NOT\n`winner.provider.format(...)` returning a degenerate payload.\n\nPlanet, Local Group, cloud, and star formatters currently all emit\n`{ name: '', lines: [] }` on degenerate inputs (\\`!planet\\`, \\`!obj\\`,\n\\`!cloud\\`, etc.). If that path were ever reached, the engine would\nrender an empty card (\\`\u003cdiv class=\"tt-name\"\u003e\u003c/div\u003e\\`) at the cursor.\n\n# Scope\n\nTwo clean options, pick one:\n\n(a) Tighten the contract: `HoverProvider.format` returns\n\\`HoverPayload | null\\`. Engine guards \\`null\\` payload alongside the\nexisting null-winner check. All four formatters change\n\\`return { name: '', lines: [] }\\` to \\`return null\\`.\n\n(b) Have the engine guard \\`!payload.name \u0026\u0026 payload.lines.length === 0\\`\nexplicitly. Formatters keep their current signature.\n\n(a) is the principled fix — null is the right shape for \"no card\nwarranted\". (b) is one line in the engine.\n\n# Why\n\n[[stellata-consistency-at-the-seam]] Rule 1 (lifecycle pairing): the\nprovider's \"valid hit\" contract should match the engine's render\ngate. Today the contract is silently looser on the format side.\n\n# Acceptance\n\n- One of (a) or (b) lands; the empty-card path is provably unreachable.\n- The disambiguator test covers a \"format returns null\" case (or\n  whichever shape the chosen option uses).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T21:24:19Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:24:19Z","labels":["hover","pr-72"],"dependencies":[{"issue_id":"stellata-9mm.187.6","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-16T22:24:19Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.189.2","title":"Export + test phaseFactorFor in planet-body-field","description":"# Finding from PR #72 (stellata-lo5) review\n\n`phaseFactorFor` is a pure helper at file-scope in\n`src/client/planet-body-field.ts:192` but isn't exported, so it can't\nbe unit-tested. It dispatches between Mallama (when a polynomial\nexists) and Lambertian (the default fallback for Pluto + exoplanets)\nmirroring the planet vertex shader's branch.\n\n# Scope\n\n1. Export `phaseFactorFor` (or extract to a `*-pure.ts` sibling).\n2. Add vitest coverage:\n   - Pluto (no Mallama coefs, falls through to Lambertian) — verify\n     the branch by passing `coefs: undefined` and checking against\n     a `lambertianPhaseFactor` direct call.\n   - One body with Mallama coefs (e.g. Venus / Mars) at known α — pin\n     against the direct `mallamaPhaseFactor(coefs, alpha)` value.\n   - Degenerate viewer / host coincidence (lenV or lenHp ≤ 0) returns 1.\n   - α = 0 (viewer aligned with host) returns the same as `peakPhaseFactor`.\n\n# Why\n\n[[stellata-test-coverage-discipline]] (a): pure helpers need to be\ntestable. Theme matches parent 189 (planet pipeline DRY, tests).\n\n# Acceptance\n\n- `phaseFactorFor` is exported (or hoisted to a `phase-function.ts` /\n  `planet-body-field-pure.ts` sibling).\n- A vitest covers at minimum the four cases above.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T21:23:58Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:23:58Z","labels":["planet","pr-72","test"],"dependencies":[{"issue_id":"stellata-9mm.189.2","depends_on_id":"stellata-9mm.189","type":"parent-child","created_at":"2026-05-16T22:23:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.192.1","title":"Unit tests for LocalGroupLayer.pick + Stellata.pickHeliopauseHit + pickCloudHit","description":"# Finding from PR #72 (stellata-lo5) review\n\nThree hover-picker integration paths land in PR #72 with no unit\ncoverage:\n\n- `LocalGroupLayer.pick` — projects each LG object centroid, builds\n  candidates, reduces via `pickFromCandidates`. PR body Medium.\n- `Stellata.pickHeliopauseHit` — silhouette bbox + label rect; two\n  regimes (outside-shell vs inside-shell, near-plane guard).\n- `Stellata.pickCloudHit` — Three.js raycast delegation + visibility\n  gate.\n\n# Scope\n\nFakes a camera, world offset, DOMRect, and (for heliopause) the SVG\nlabel element. Cases (representative):\n\nLG (per the post-review reducer refactor — winner carries\ncameraDistancePc + tier on the candidate):\n- Centroid inside hitRadius → prime hit.\n- Centroid within pxThreshold but outside hitRadius → fallback.\n- `group.visible = false` short-circuits to null.\n- Two overlapping objects: closest cameraDistancePc wins within tier.\n\nHeliopause:\n- Outside-shell: cursor inside silhouette bbox → fallback hit.\n- Inside-shell: at least one sample point behind near plane → bails.\n- Label rect: getBoundingClientRect overlap → fallback hit (even\n  when silhouette test bails on near plane).\n\nCloud:\n- Raycast returns idx → fallback hit with the right cameraDistancePc.\n- `cloudLayer === null` or `group.visible = false` → null.\n\n# Why\n\n[[stellata-test-coverage-discipline]] (c): integration paths.\nTheme matches parent 192 (catalog binary + picker tests; P3 default\nfor non-user-visible refactors).\n\n# Acceptance\n\n- Three vitest files (or one shared) under\n  `src/client/hover/` covering the cases above.\n- Each uses synthetic fakes — no real renderer, no real catalog data.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T21:23:42Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:23:42Z","labels":["hover","pr-72"],"dependencies":[{"issue_id":"stellata-9mm.192.1","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-16T22:23:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.189.1","title":"Unit tests for PlanetBodyField.pick + appMagFor + evalPlanetView","description":"# Finding from PR #72 (stellata-lo5) review\n\n`PlanetBodyField.pick`, `PlanetBodyField.appMagFor`, and the private\n`evalPlanetView` helper (the shared geometry+photometry math, after\nthe post-review DRY pass) all lack unit tests. PR body flagged the\npick path as Medium confidence.\n\n# Scope\n\n- Cover `evalPlanetView(host, planetIdx, camPos)` directly — pin the\n  `(appMag, planetX, planetY, planetZ, dVp)` outputs against\n  known-good inputs (e.g. Mercury, Saturn, Pluto) and validate that\n  appMag matches the `planetApparentMagnitude` ground-truth helper.\n  This is the pure-math shared path; covering it is the highest-value\n  test of the chain.\n- Cover `appMagFor` null branches (host not attached, planetIdx out\n  of range) and one happy path.\n- For `pick`, an integration test with a fake camera + fake bufLocalRel\n  setup that exercises:\n  - prime-tier hit on a planet whose disc contains the cursor\n  - fallback-tier hit on a planet centroid near the cursor but no\n    disc hit\n  - kill condition: appMag \u003e maxAppMag + 0.5 drops candidates\n  - cross-host walk: planets from two attached hosts both eligible\n\n# Why\n\n[[stellata-test-coverage-discipline]] (c): integration paths through\nnew state machinery need read-back assertions. Theme matches parent\n189 (planet pipeline DRY, tests, halo correctness).\n\n# Acceptance\n\n- vitest tests landing for `evalPlanetView` + `appMagFor` + `pick`.\n- `evalPlanetView` is the new shared helper from the lo5 review-fallout\n  commit (\\`hover: share appMag math between planet pick + appMagFor\\`)\n  and is the priority test target — covering it pins the whole math\n  chain that both consumers ride on.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T21:23:24Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:23:24Z","labels":["hover","planet","pr-72"],"dependencies":[{"issue_id":"stellata-9mm.189.1","depends_on_id":"stellata-9mm.189","type":"parent-child","created_at":"2026-05-16T22:23:24Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.187.5","title":"Unit tests for hover-engine FSM (lifecycle, registry, clamps)","description":"# Finding from PR #72 (stellata-lo5) review\n\n`src/client/hover/hover-engine.ts` is integration-only — no unit\ntests. Covers the engine's state machine (idle / pending-timer / shown\n/ dragging), register/unregister deduplication, dispose path\nteardown, and the tooltip placement clamps.\n\n# Scope\n\n- Faking the canvas via `EventTarget` + `vi.useFakeTimers()` is the\n  natural test pattern (vitest's jsdom env provides both).\n- Cases to pin (representative, not exhaustive):\n  1. pointermove → 280 ms timer → providers walked → tooltip shown.\n  2. Drag (pointerdown before timer fires) suppresses the hover.\n  3. pointerleave hides the tooltip and clears the timer.\n  4. register / unregister: same provider not double-added; remove\n     drops it from the walk.\n  5. dispose: clears the timer, removes the four canvas listeners,\n     empties the provider list, hides the tooltip.\n  6. Tooltip placement clamps when cursor approaches right / bottom\n     edge (`Math.min(x + offset, maxLeft)`).\n\n# Why\n\n[[stellata-test-coverage-discipline]] (c): lifecycle FSMs need a\nread-back assertion, not just \"does not throw\". Theme matches the\nparent epic (187 = event-bus \u0026 lifecycle hygiene).\n\n# Acceptance\n\n- vitest file `src/client/hover/hover-engine.test.ts` covering the\n  cases above.\n- Each timer-driven assertion uses fake timers + advanceTimersByTime\n  rather than real waits.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T21:23:05Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:23:05Z","labels":["hover","pr-72"],"dependencies":[{"issue_id":"stellata-9mm.187.5","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-16T22:23:04Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.195","title":"Consolidate physicalRadius floor: Math.max(..., 1e-6|1e-9) magic numbers across 6 sites","description":"Six call sites in `src/client/stellata.ts` floor catalog `physicalRadius[idx]` before multiplying by `R_SUN_PC`, with two different floor values and no shared constant:\n\n- `1e-6` floor: lines 820, 3477, 3665\n- `1e-9` floor: lines 3611, 3640 (pre-existing) plus 2495, 2592 (new in PR #66)\n\nPR #66 lands `MIN_PHYSICAL_RADIUS_R_SUN = 1e-9` for the two NEW sites; this bead is the cleanup of the pre-existing six. Decisions:\n\n1. Settle on one floor value (1e-9 or 1e-6) — needs a check on which sites use the result for \"is this star a valid renderable\" gating (1e-6 ≈ Jupiter radius, 1e-9 ≈ ~0.7 km — different physical floors).\n2. Migrate all sites to the named constant.\n3. Likely consolidate with similar floors in star-geometry / pickers if they exist (look for the pattern around line 820).\n\nWhy P3: cosmetic — no current bug, just inconsistency and a magic-number violation of stellata-named-constants-and-dry rule 1.\n\nSmoke: typecheck + vitest after migration. No user-visible change.\n\nSource: surfaced during PR #66 review (camera-arrival hybrid landing).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T20:53:08Z","created_by":"Alex Marshall","updated_at":"2026-05-16T20:53:08Z","labels":["code-quality","dry","pr-66","stellata"],"dependencies":[{"issue_id":"stellata-9mm.195","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-16T21:53:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9hp.5","title":"Hand-curated famous-SNR overrides (SN 1987A, Cas A)","description":"Sibling to the unified nebulae ingest pipeline (9hp.1). Carries hand-\ncurated overrides for individually-famous SNRs that the MW-centric\npipeline either misses (extragalactic) or would surface with thin /\nmismatched data (very young + well-resolved MW SNRs that warrant\nricher per-object detail than the SIMBAD-TAP filter returns).\n\n# Scope\n\nTwo named seeds, expandable later:\n\n- **SN 1987A** — LMC (~50 kpc). Famously the only naked-eye SN in\n  modern times. **Will NOT be caught by 9hp.1's pipeline** as currently\n  scoped — the epic's data sources filter to MW (~3 kpc cut) and most\n  SNR catalogs (Green's, etc.) are MW-centric. Needs a hand-curated\n  entry with the LMC-anchored position.\n- **Cassiopeia A** — MW (~3.4 kpc, right at the pipeline cut). The\n  youngest known MW SNR (~340 yr), filamentary, well-imaged by Chandra,\n  HST, JWST. May be caught by the unified pipeline but the canonical\n  reference data (Reed 1995 distance, Hwang 2004 morphology) probably\n  isn't what the pipeline ingests. Override to upgrade detail.\n\nFuture additions of similar shape: **Crab Nebula** (M1, also MW, but\nalready a strong-PN-like emitter so already covered by other catalogs),\n**SN 1006**, **Tycho's SNR**, **Kepler's SNR**, **SN 1604**, **N49** (LMC),\n**N132D** (LMC). File as follow-on overrides as needed; this bead is the\ninfrastructure plus the first two seeds.\n\n# Why a separate bead rather than fold into 9hp.1\n\nTwo reasons matching the local-group/overrides.tsv precedent:\n\n1. **Pipeline-data mismatch.** Like the M31/M33 standalones in\n   local-group/overrides.tsv (major spirals excluded from LVDB's\n   dwarf_all table), these famous SNRs sit outside what the unified\n   pipeline catches. Hand-curation is the only path.\n\n2. **Per-object detail.** Famous SNRs warrant well-sourced morphology\n   parameters (shell radius / thickness, asymmetry axes for SN 1987A's\n   triple-ring system) — closer in shape to the local-group/overrides.tsv\n   axes+orient schema than to the auto-pipeline's flat \"blob with\n   distance + size\" record.\n\n# Schema (proposed, draft)\n\n`data/nebulae/snr-overrides.tsv` or similar — one row per famous SNR:\n\n  name | ra_deg | dec_deg | distance_pc | shell_radius_pc | age_yr |\n  shape (sphere|filamentary|three-ring|...) | ref_doi\n\n# Out of scope\n\n- The render pipeline for SNRs in general — that's 9hp.2 (raymarching\n  shader) + 9hp.3 (type-specific visuals). This bead provides the\n  per-object data; 9hp.2/3 consume it alongside the auto-ingested rows.\n\n# Acceptance\n\n- Build artifact contains SN 1987A and Cas A entries with verified\n  position + distance + size.\n- Stellata renders both at the expected sky positions when 9hp's\n  rendering lands.\n- Pattern is documented so future-famous-SNRs can be added without\n  re-thinking the schema.\n\n# Relates to\n\n- 9hp.1 (unified ingest) — produces this bead's \"everything else\" peer.\n  Verify 9hp.1's filter to confirm Cas A is or isn't naturally caught\n  before the override decision is final.\n- local-group/overrides.tsv (the precedent for hand-curated overrides\n  to a catalog-driven layer).\n\n# Status\n\nP3, no urgency. Filed during stellata-lo5.5 conversation when\nSN 1987A surfaced as a notable absence; hand-curation pattern noted.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T19:33:22Z","created_by":"Alex Marshall","updated_at":"2026-05-16T19:33:22Z","labels":["ism","nebulae","phase-3b"],"dependencies":[{"issue_id":"stellata-9hp.5","depends_on_id":"stellata-9hp","type":"parent-child","created_at":"2026-05-16T20:33:21Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9hp.5","depends_on_id":"stellata-9hp.1","type":"related","created_at":"2026-05-16T20:33:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-lo5.9","title":"Hover labels — mobile/touch parity (long-press)","description":"Deferred follow-up: mobile / touch parity for hover labels.\n\n# Why deferred\n\nPer stellata-prioritization-framework, mobile work sits below desktop. v1.0 ships desktop-only hover labels.\n\n# Scope (when picked up)\n\n- Long-press on a pickable surface → show hover card in fixed position above the press location.\n- Tap elsewhere → dismiss.\n- Reuse the same HoverProvider registry + disambiguateHits from the desktop engine.\n- Investigate whether the existing touch-disambiguation in main.ts/stellata.ts needs extending or replacing.\n\n# Acceptance\n\n- iOS Safari + Chrome Android: long-press each class → tooltip appears; tap elsewhere dismisses.\n\nFiled when the design gate lo5.1 closed; sits at P3 until desktop hover work merges.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:41:32Z","created_by":"Alex Marshall","updated_at":"2026-05-16T16:41:34Z","labels":["mobile","picker","ui","ux"],"dependencies":[{"issue_id":"stellata-lo5.9","depends_on_id":"stellata-lo5","type":"parent-child","created_at":"2026-05-16T17:41:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.194.7","title":"Comment density audit — relocate multi-paragraph in-code prose to SCIENCE.md / docs/*.md","description":"Sweep multi-paragraph in-code prose (3+ lines of running prose, not list-of-fields docblocks) in src/client/*.ts. For each block:\n\n- **Invariant or non-obvious constraint** → keep, trim to ≤1 paragraph.\n- **Physics derivation, calibration rationale, tuning history** → relocate to SCIENCE.md or relevant docs/*.md; leave a one-line code-side pointer.\n- **Redundant** (paraphrasing the named identifier or describing 'what' rather than 'why') → delete.\n\n# Why\n\nLong in-code prose costs LLM context tokens on every Read AND rots faster than docs (no tests, no review catches stale comments — see stellata-rename-and-stale-prose-sweep memory). Centralising in SCIENCE.md / docs/*.md makes the prose maintainable (defer-doc-updates rule applies) AND lets future Claude sessions load it on-demand via the CLAUDE.md doc index rather than every time a code file is Read.\n\n# Examples of in-code prose to relocate\n\n- stellata.ts § 'Star size physics' (~13-line docblock with the σ·√(STAR_PHYSICS_FACTOR·Δm) derivation) → SCIENCE.md (likely already there); code-side pointer: '// see SCIENCE.md § Star size physics'.\n- FilterState field docblocks (currently inline per-field 'why' prose) — keep one-line invariants, move history notes ('showMolecularClouds removed — see CLAUDE.md') out.\n- Anywhere stellata-rename-and-stale-prose-sweep flagged stale prose in 9mm retrospective beads — apply the trim.\n\n# Pointer comment shape\n\nConsistent so they are greppable from CLAUDE.md doc references:\n```\n// see SCIENCE.md § Star size physics\n// see docs/galactic-overlay.md § Shaft-start lerp\n```\n\n# Soft ceiling\n\nNo comment block over 12 lines in src/client/*.ts unless documenting a hard invariant a reader MUST grok to understand the next line (e.g. event-bus emit-order ordering, sentinel-init poison values).\n\n# Acceptance\n\n- Grep for multi-paragraph comments → zero unjustified blocks \u003e 12 lines.\n- SCIENCE.md / docs/*.md gain the relocated content under appropriate sections (per defer-doc-updates: doc edits live in this PR, not earlier).\n- Code-side pointers use the consistent shape above.\n\n# Smoke\n\ntypecheck passes (move is pure-prose; no behavioural change).\n\n# Independence\n\nParallelisable with the controller-extraction chain (194.3-.7) — independent file edits, no shared code paths. Can land any time after 194.1 (folder reorg) lands so file paths are stable.\n\n# Connects\n\n- [[alex-pr-review-style]] — DRY of explanatory prose between code + docs.\n- [[stellata-rename-and-stale-prose-sweep]] — same drift class.\n- [[proactive-drift-correction]] — preventative version of the same rule.\n- [[defer-doc-updates]] — doc edits land in this PR.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:41:05Z","created_by":"Alex Marshall","updated_at":"2026-05-17T14:29:04Z","started_at":"2026-05-17T13:28:49Z","closed_at":"2026-05-17T14:29:04Z","close_reason":"merged in PR #82","labels":["cleanup","docs"],"dependencies":[{"issue_id":"stellata-9mm.194.7","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T17:41:04Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.194.7","depends_on_id":"stellata-9mm.194.1","type":"blocks","created_at":"2026-05-16T17:42:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-lo5.7","title":"Cloud hover provider (written, not registered)","description":"Write the cloud hover provider/formatter; do NOT register at v1.0 (clouds are shelved per CLAUDE.md).\n\n# Scope\n\n- `src/client/hover/formatters/cloud-hover-format.ts` — fields: name, distance from Sol via `fmtDist`, size summary (semi-axes).\n- `src/client/hover/formatters/cloud-hover-format.test.ts` — golden output for Taurus + Orion A.\n- A `CloudHoverProvider` class exists but is gated on `FilterState.showMolecularClouds === true`. Since that flag is permanently false at v1.0, the provider does not appear in the engine's registry.\n- Pick path reuses existing `MolecularClouds.raycast` (`molecular-clouds.ts:197–203`) — no change to that file.\n\n# Acceptance\n\n- Formatter unit-tested.\n- Registration toggle works (verify by temporarily flipping the flag in dev; verified behaviour expected: hovering a cloud surfaces the card; flag back to false → no card).\n\n# Why P3\n\nThe cloud layer itself is shelved; this is \"engine-ready\" work, not user-visible at v1.0. Lifts the provider out of \"needs eyeballs\" when clouds un-shelve.\n\nShips as part of lo5 single-PR rollup; close on PR merge. Depends on lo5.2.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T16:40:53Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:59Z","started_at":"2026-05-16T20:18:17Z","closed_at":"2026-05-16T21:38:59Z","close_reason":"merged via PR #72 (commit 12: cloud provider written, auto-registers when clouds un-shelved)","labels":["molecular-clouds","picker","shelved","ui","ux"],"dependencies":[{"issue_id":"stellata-lo5.7","depends_on_id":"stellata-lo5","type":"parent-child","created_at":"2026-05-16T17:40:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.187.4","title":"HudOverlay two-pass alpha sequencing has no integration regression test (ml8 follow-up)","description":"# The gap\n\nPR #70 (stellata-ml8) restructured `HudOverlay.update()` into two passes — commit Sol/GC arrow geometry first, then compute the shared fade alpha from THIS frame's `max(solDrawnLen, gcDrawnLen)`. Removed: the `Stellata._navArrowFadeAlpha` pre-pass alpha that read last-frame's `getDrawnLengths()` (the cause of the toggle-on flash, ml8 symptom 1).\n\n`src/client/arrow-fade.test.ts:91-104` is labelled \"ml8 symptom 1 regression\" but it tests the pure-helper guard:\n\n```ts\nexpect(focusedArrowFadeAlpha('navigate', null, 200, 0, 28)).toBe(1);\n```\n\nThat asserts the `shaftLength=0 → α=1` contract. It does NOT exercise the actual integration bug — `HudOverlay.update()` computing alpha against stale last-frame `getDrawnLengths()`. A future regression that re-introduces a pre-pass alpha computation (or moves the alpha calc back ahead of `updateOne`) would slip past every vitest.\n\nPer `stellata-test-coverage-discipline` rule 5: \"PR test plan flags any path as 'manual smoke'? Promote the cheapest of those (5–10 line vitest) to automated.\" PR #70's confidence section graded the HUD refactor as Medium and explicitly defers to manual smoke.\n\n# What needs to land\n\nAn integration test that exercises the two-pass sequence end-to-end:\n\n1. Mock the SVG element shims `HudOverlay`'s constructor accepts (`SVGCircleElement`, `SVGPathElement` × 4, `SVGTextElement` × 2). The applyFade-side mock infra in `hud-overlay.test.ts:5-12` (`makeFadeEls`) is a starting point but needs extending — `update()` also calls `setAttribute('d', …)`, `setAttribute('x'/'y', …)`, `textContent`, and `style.display`.\n2. Construct a `THREE.PerspectiveCamera` aimed so Sol projects to a known on-screen position. Likely needs `camera.updateMatrixWorld()` + `camera.updateProjectionMatrix()` after positioning.\n3. Call `hideAll()` (simulate toggle-off — sets `solDrawnLen = 0`, `gcDrawnLen = 0`).\n4. Call `update()` with `focusedDiscRadiusPx` large enough to engage the disc-coverage fade (coverage \u003e 0.5 against the expected this-frame shaft length).\n5. Assert `getCurrentFadeAlpha()` reflects the fade computed from this-frame's geometry — NOT 1 (which is what the pre-fix code would return when it read the stale-0 `refLen`).\n\n# Acceptance\n\n- `hud-overlay.test.ts` gains a new `describe` block (suggested name: `HudOverlay two-pass alpha sequencing (ml8)`) covering the integration above.\n- Test fails if `getCurrentFadeAlpha()` is computed against stale `getDrawnLengths()` (i.e., would fail against the pre-#70 code).\n- Test passes against current `main`.\n\n# The wrong claim to replace\n\nThe PR #70 description ([https://github.com/alexmensch/stellata/pull/70](https://github.com/alexmensch/stellata/pull/70)) labels the `shaftLength=0 → α=1` test as the \"regression for symptom 1\". **Wrong** — that's the pure-helper guard, not the integration regression. The integration regression has no automated coverage. Acceptance of this bead corrects that.\n\n# Smoke\n\n`npm test` should grow by ~1 describe block. Test should fail if anyone reverts `HudOverlay.update` to a pre-pass alpha computation (e.g. moves the `focusedArrowFadeAlpha` call ahead of the `updateOne` calls).\n\n# Cost / why P3\n\nIntegration test requires non-trivial mock infrastructure (SVG + camera), ~30-50 lines. Beyond the 5-10 line threshold `stellata-test-coverage-discipline` would normally promote. P3 because (a) the prior pre-pass design has been removed, so the regression vector has no live precursor in the codebase, (b) manual smoke catches it in practice (the symptom is a visible one-frame flash). But the test is the proper regression guard per the discipline rule.\n\n# Related\n\n- `stellata-9mm.131` — broader test-coverage gap across overlays. This bead is the specific HUD-two-pass instance; .131 covers dirty-track sentinels and per-overlay smoke. Linked rather than merged because .131 was already filed for PR #55 fallout.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T15:54:57Z","created_by":"Alex Marshall","updated_at":"2026-05-16T15:54:57Z","labels":["pr-70-followup","test-gap","ui"],"dependencies":[{"issue_id":"stellata-9mm.187.4","depends_on_id":"stellata-9mm.131","type":"related","created_at":"2026-05-16T16:55:01Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.187.4","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-16T16:54:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-0hx.8","title":"CMB last-scattering backdrop — Planck 2018 SMICA skybox (~14 Gpc horizon)","description":"**Goal:** render the Planck 2018 SMICA CMB temperature map as a textured skybox at the observable horizon (comoving distance ~14 Gpc). The natural \"back wall\" of stellata's universe model.\n\n**Tier 6** of stellata-0hx. Optional capstone.\n\n# Why\n\nThe CMB last-scattering surface IS the boundary of the visualisable universe — at z ≈ 1100, no objects exist past it (by definition). It completes the model: everything stellata renders (stars, planets, MW, LG, LV, LSC, Laniakea, cosmic web, BOSS-tier galaxies) floats inside this sphere.\n\n# Scope\n\n## Data\n\n- **Planck 2018 SMICA component-separated CMB map** (HEALPix Nside=512, ~50 MB compressed) from the Planck Legacy Archive. Picked Nside=512 over Nside=2048 (200 MB) — 50 MB fits in initial-load + the screen-pixel resolution doesn't justify finer.\n- Alternative: Commander or NILC maps. SMICA is the most commonly visualised; defer to .1's design-gate.\n- Commit under `data/extragalactic/cmb-planck-smica-nside512.fits` (LFS-tracked).\n- One-off `scripts/refresh-cmb.py` excluded from build per [[frozen-external-data]].\n\n## Pipeline (`scripts/build-cmb.ts`)\n\n- Parse HEALPix FITS → cubemap or equirectangular projection.\n- Output: `public/cmb-skybox.{ktx2,png}` for GPU upload.\n- Apply colour LUT: temperature → colour (Planck's blue-red anomaly scale, or grayscale with overlay).\n\n## Renderer\n\n- New module `src/client/cmb-backdrop.ts`.\n- Rendered as a textured sphere with inward-facing normals, **outside the floating-origin rebase entirely** — drawn last, behind everything, at \"infinity\" (large fixed radius, modulating with the camera-COSMIC regime's far plane).\n- Skybox-style: no depth writes, no depth tests against scene geometry. Always at the back.\n- Toggle in the panel: ON/OFF. Default ON when camera regime is COSMIC.\n\n# Smoke\n\n- Pull camera back to maximum (~10+ Gpc out). CMB temperature pattern visible as a textured sphere around the entire model.\n- Galaxies from .6 + .7 visible inside the CMB sphere.\n- Toggle off → CMB disappears; toggle on → restores.\n- No depth-fighting between CMB sphere and Tier 5/6 galaxies — sphere consistently behind.\n\n# Out of scope\n\n- CMB polarisation overlay (separate fancy viz).\n- Foreground subtraction comparison (SMICA already foreground-cleaned).\n- Cosmic-variance / fluctuation analysis — pure aesthetic backdrop, not an analytical tool.\n\n# Blocks\n\nNothing — this is the capstone.\n\n# Blocked by\n\n.7. Once BOSS-tier objects are rendering past 1 Gpc, the CMB sphere at 14 Gpc is the natural completion.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:55:44Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:55:44Z","labels":["backdrop","cmb","deep-field","extragalactic","galactic","planck","research","visual"],"dependencies":[{"issue_id":"stellata-0hx.8","depends_on_id":"stellata-0hx","type":"parent-child","created_at":"2026-05-16T15:55:43Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.8","depends_on_id":"stellata-0hx.1","type":"blocks","created_at":"2026-05-16T15:56:09Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.8","depends_on_id":"stellata-0hx.2","type":"blocks","created_at":"2026-05-16T15:56:14Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.8","depends_on_id":"stellata-0hx.7","type":"blocks","created_at":"2026-05-16T15:56:18Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-0hx.7","title":"BOSS / eBOSS reach + redshift colouring (z ≲ 0.7, ~3 Gpc / ~8 Gly comoving)","description":"**Goal:** extend reach to z ≲ 0.7 using SDSS BOSS / eBOSS LSS galaxy + quasar catalogues. ~2M point-glow primitives, colour-coded by redshift. Cosmology bake-in for z → comoving distance.\n\n**Tier 5** of stellata-0hx. The cosmological-survey regime.\n\n# Why\n\nPast NED-LVS / SDSS Main, the only catalogues with spectroscopic redshifts are the BAO surveys (BOSS, eBOSS). They sample biased sub-populations (LRGs, ELGs, QSOs) but the redshift is essentially the distance — and the BAO peak at ~150 Mpc emerges as a natural feature of the rendered distribution.\n\n# Scope\n\n## Data\n\n- **SDSS BOSS LSS galaxy catalogue (DR12 + final SDSS-IV DR17)** — 1.5M LRGs + ELGs.\n- **eBOSS quasars** — ~500k quasars to z ~3.5.\n- All commit-once via `scripts/refresh-boss.py` per [[frozen-external-data]]. Output binary parallel to `public/cosmic-web/`.\n\n## Pipeline (`scripts/build-boss.ts`)\n\n- Cosmology bake: Planck 2018 (H0=67.4 km/s/Mpc, Ω_m=0.315, Ω_Λ=0.685). z → comoving distance via numerical integration of `c · ∫dz/H(z)`. Precompute as a lookup table or polynomial fit; bake into binary record.\n- HEALPix Nside=4 chunking continues from .6.\n- De-dup against `public/cosmic-web/`.\n\n## Renderer\n\n- Re-uses `extragalactic-billboards.ts` from .3, fed by chunked binary loader from .6.\n- **Redshift colouring** as primary visual: hue → redshift (e.g. blue→z=0, red→z=0.7). Literalises the z signal.\n- No per-object labels.\n- Camera envelope regime COSMIC (far=8 Gpc, maxDistance=4 Gpc per .2).\n\n## Float32 floor — emulated double-precision\n\nAt ~3 Gpc = 3×10^9 pc, float32 precision is ~300 pc. The far-pass split from .6 isn't sufficient — need emulated double-precision (DSFUN90 / floatfloat) for camera-relative position math.\n\n- CPU-side: compute `(cam.pos - obj.pos)` in float64.\n- Split into hi + lo floats; hand both to GPU.\n- GPU shader: reconstruct via Kahan-like summation in vertex shader.\n- The existing star pipeline already does this trick (floating-origin rebase). Lift the pattern to galaxies.\n\n## Selection-bias documentation\n\nBOSS/eBOSS is heavily biased (LRG = luminous red galaxies; ELG = emission-line galaxies; QSO = active galactic nuclei). The visualisation will look \"spikier\" than reality. Per [[stellata-data-fidelity-principle]], present honestly. Document selection function in `docs/extragalactic-roadmap.md`. Add a debug-panel toggle to colour by population (LRG/ELG/QSO) so the user can SEE the selection bias.\n\n# Smoke\n\n- Pull camera to ~5 Gpc. The redshift-coloured filaments visible; BAO ~150 Mpc feature (if visible at this resolution) as a faint shell-like overdensity around overdense regions.\n- Quasars visible as the highest-z points, blueshift cluster at low z.\n- Distance readout in Gpc (verifies .2 plumbing at extreme scale).\n- Emulated-double smoke: no jitter on per-galaxy positions at 5 Gpc out.\n\n# Out of scope\n\n- DESI DR1+ — eBOSS is the sample at this tier; DESI is much larger and would be a separate refresh bead.\n- Galaxy cluster catalogues (redMaPPer, AMICO) — separate viz.\n- CMB lensing overlay — separate viz.\n\n# Blocks\n\n.8 (CMB backdrop).\n\n# Blocked by\n\n.6 (re-uses chunked loader + hybrid LOD; emulated-double extends the .6 float32 mitigation).","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:55:26Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:55:26Z","labels":["billboards","boss","cosmology","deep-field","eboss","extragalactic","galactic","research","visual"],"dependencies":[{"issue_id":"stellata-0hx.7","depends_on_id":"stellata-0hx","type":"parent-child","created_at":"2026-05-16T15:55:26Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.7","depends_on_id":"stellata-0hx.1","type":"blocks","created_at":"2026-05-16T15:56:08Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.7","depends_on_id":"stellata-0hx.2","type":"blocks","created_at":"2026-05-16T15:56:13Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.7","depends_on_id":"stellata-0hx.6","type":"blocks","created_at":"2026-05-16T15:56:17Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-0hx.6","title":"Pisces-Cetus + Sloan Great Wall + cosmic web — NED-LVS / SDSS Main (~1 Gpc reach)","description":"**Goal:** extend reach to ~1 Gpc / ~3 Gly. ~1.9M galaxies from NED-LVS or ~700k from SDSS Main, rendered as point-glow billboards. The Pisces-Cetus Supercluster Complex (~380 Mpc, core at ~200 Mpc) and Sloan Great Wall (~420 Mpc long at ~300 Mpc distance) are the named structures.\n\n**Tier 4** of stellata-0hx. First tier where the cosmic web (filaments + voids + walls) is the dominant visual.\n\n# Why\n\nThe natural endpoint for \"per-object spectroscopic redshift\" coverage. SDSS Main reaches z=0.25; NED-LVS reaches z=0.2 (1000 Mpc). Past Laniakea, the supercluster watershed picture fragments into the cosmic web.\n\n# Scope\n\n## Data\n\n- **NED-LVS** (Cook et al. 2023, arXiv:2306.06271) — 1.9M objects with distances to 1000 Mpc. 100% complete bright to ~400 Mpc, 70% complete fainter to ~300 Mpc. Includes redshift-independent distances for closest; cz/H0 past ~40 Mpc.\n  - Pro: includes structural data for the bright tail. Inhomogeneous quality but documented per source.\n  - Con: file size ~1+ GB raw FITS.\n- **SDSS Main spectroscopic** — ~700k galaxies to z ≈ 0.25, Northern footprint only (cuts off ~50% of the sky).\n  - Pro: clean uniform selection function.\n  - Con: not all-sky.\n- **Decide at impl time** (gate at design-gate .1 if not already decided): NED-LVS for all-sky + variable quality, OR SDSS Main for clean-but-Northern. Probably NED-LVS.\n\n## Pipeline (`scripts/build-cosmic-web.ts`)\n\n- Parse FITS or CSV → binary format with HEALPix Nside=4 sky-cell chunking (192 cells, ~10k galaxies per cell). Pattern: dust chunk format (`data/dust/chunk_X_Y_Z.bin` + `manifest.json`).\n- Output: `public/cosmic-web/chunk_\u003chealpix\u003e_\u003cdistance-band\u003e.bin` + manifest.\n- De-dup against `public/laniakea.bin`.\n\n## Renderer\n\n- Camera envelope: regime COSMIC kicks in (far=2 Gpc, maxDistance=1 Gpc per .2).\n- **Hybrid LOD**:\n  - Per-galaxy billboards for the nearest ~50k galaxies (within ~300 Mpc of camera). Existing pipeline.\n  - Volumetric density-field representation for everything past that — model the cosmic web as a smooth density volume (analogous to MW disc/bulge volumetric layer). Per-galaxy smoothstep handover avoids LOD pop.\n  - Tradeoff: two render paths to maintain. Pays off in perf + visual quality.\n- **Spatial chunking**: per-frame, only chunks in or near camera frustum upload to GPU. ~20-chunk pinned cache. The dust-loader is the precedent.\n- Labels on ~10 named cosmic-web structures: Pisces-Cetus, Sloan Great Wall, Perseus-Pegasus chain, Boötes Void, CfA Great Wall (Coma Wall), Pavo-Indus, Ophiuchus Supercluster. No per-galaxy labels at this tier.\n\n## Float32 floor mitigation\n\nAt ~1 Gpc = 10^9 pc, float32 precision is ~100 pc. Most galaxies are far enough that camera-relative coords stay within float-safe range, but the closest-to-camera galaxies in a far-zoom view need care. Split rendering into near (parsec-scale, current pipeline) + far (Mpc-scale, this tier) passes — Cesium / Outerra pattern.\n\n# Smoke\n\n- Pull camera back to ~1 Gpc out. Pisces-Cetus filament visible (RA 00h33m, Dec −21°, distance ~200 Mpc).\n- Sloan Great Wall (RA ~12h, Dec −10°, distance ~300 Mpc) visible as a coherent ribbon.\n- Hybrid LOD handover: at very far zoom, the cosmic web is volumetric; at moderate zoom, individual galaxies emerge.\n- Distance readout in Gpc/Gly (verifies .2 plumbing).\n- Floating-origin stable at 1 Gpc out (no jitter on near-side galaxies).\n- Chunk loader fires on camera motion; \"loading deep field…\" indicator on uncached regions.\n\n# Out of scope\n\n- Survey-completeness corrections (we render as-observed, per [[stellata-data-fidelity-principle]] — honest about selection).\n- Cosmological-simulation overlays (Millennium, IllustrisTNG) — separate epic if ever.\n\n# Blocks\n\n.7 (BOSS/eBOSS). Re-uses the chunked binary format + hybrid LOD.\n\n# Blocked by\n\n.5. Re-uses progressive loading from there.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:55:04Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:55:04Z","labels":["billboards","cosmic-web","deep-field","extragalactic","galactic","pisces-cetus","research","sloan-great-wall","visual"],"dependencies":[{"issue_id":"stellata-0hx.6","depends_on_id":"stellata-0hx","type":"parent-child","created_at":"2026-05-16T15:55:04Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.6","depends_on_id":"stellata-0hx.1","type":"blocks","created_at":"2026-05-16T15:56:07Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.6","depends_on_id":"stellata-0hx.2","type":"blocks","created_at":"2026-05-16T15:56:12Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.6","depends_on_id":"stellata-0hx.5","type":"blocks","created_at":"2026-05-16T15:56:16Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-0hx.5","title":"Laniakea + sibling supercluster watersheds — Cosmicflows-4 ingest (~150 Mpc reach)","description":"**Goal:** add the Laniakea supercluster + 5 sibling watersheds (Apus, Hercules, Lepus, Perseus-Pisces, Shapley) as wireframe shells, plus ~56k Cosmicflows-4 galaxies as billboards. ~150 Mpc reach.\n\n**Tier 3** of stellata-0hx. The \"supercluster as gravitational basin\" headline visual.\n\n# Why\n\nThe MW's true cosmographic home is Laniakea (Tully et al. 2014, Dupuy \u0026 Courtois 2023). Sol's position relative to the Laniakea basin attractor and the Great Attractor is the answer to \"where in the universe are we, really\" — far more revealing than the Local Group context alone.\n\n# Scope\n\n## Data\n\n- **Cosmicflows-4** (Tully et al. 2023) — 55,877 galaxy distances + peculiar velocities z ≤ 0.1. EDD (edd.ifa.hawaii.edu). PGC-keyed. Distance is the redshift-independent measurement, not cz/H0. Commit `data/extragalactic/cosmicflows4.csv` (~20 MB).\n- **Dupuy \u0026 Courtois 2023 watersheds** (A\u0026A 678, A176, arXiv:2305.02339) — 6 watershed surfaces as 3D meshes. Fetch from the paper's data repository (or contact authors); commit `data/extragalactic/watersheds/{laniakea,apus,hercules,lepus,perseus-pisces,shapley}.{obj,json}`.\n\n## Pipeline\n\n- `scripts/build-laniakea.ts` — Cosmicflows-4 → binary format (first tier to ditch JSON; mirror `public/catalog.bin` pattern from the star pipeline). De-dup against `public/lsc.json`.\n- Output: `public/laniakea.bin` (~5 MB) + manifest. Watershed meshes pass through as JSON in `public/laniakea-watersheds.json` (small data).\n\n## Renderer\n\n- Camera envelope: regime SUPERCLUSTER (far=300 Mpc, maxDistance=150 Mpc).\n- **6 watershed shells** as `LineSegments` — cheap, ~10k segments per shell × 6 = 60k segments total. Picked LineSegments over translucent surfaces (fill-rate heavy). Reconsider later if wireframe-only reads poorly.\n- ~56k galaxies via `extragalactic-billboards.ts` (from .3). Sub-pixel-floor culling kicks in heavily at this distance.\n- Labels on the 6 watershed basins (Laniakea, Apus, Hercules, Lepus, Perseus-Pisces, Shapley) — anchored to each basin's attractor centroid. Per [[stellata-prioritization-framework]], cosmographic structure labels are the high-value adds here, not per-galaxy.\n- **Progressive loading**: this tier's catalog binary fetches on camera-cross past 30 Mpc (dust-loader precedent). User sees a \"loading deep field…\" indicator on first cross; cached after.\n\n# Smoke\n\n- Pull camera back to ~150 Mpc out from Sol. The 6 watershed shells visible as low-opacity LineSegments; Sol sits inside the Laniakea shell.\n- Galaxies inside Laniakea visibly clump into filaments converging on the Great Attractor.\n- Watershed labels visible at appropriate distance.\n- Progressive-load indicator fires on first zoom past 30 Mpc.\n- Distance readout in Mpc/Mly (verifies .2 plumbing at this scale).\n\n# Out of scope\n\n- Filament-skeleton overlays (DisPerSE-style) — fancier viz, separate bead if ever.\n- BAO feature visualisation — Tier 5.\n- Voids — Tier 4 (Boötes Void at ~250 Mpc is just past this tier).\n\n# Blocks\n\n.6 (Pisces-Cetus). Re-uses progressive loading + chunking pattern (extended to HEALPix Nside=4 in .6).\n\n# Blocked by\n\n.4. The binary format introduced here is the template for .6 / .7.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:54:39Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:54:39Z","labels":["billboards","deep-field","extragalactic","galactic","laniakea","research","supercluster","visual","wireframe"],"dependencies":[{"issue_id":"stellata-0hx.5","depends_on_id":"stellata-0hx","type":"parent-child","created_at":"2026-05-16T15:54:38Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.5","depends_on_id":"stellata-0hx.1","type":"blocks","created_at":"2026-05-16T15:56:06Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.5","depends_on_id":"stellata-0hx.2","type":"blocks","created_at":"2026-05-16T15:56:11Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.5","depends_on_id":"stellata-0hx.4","type":"blocks","created_at":"2026-05-16T15:56:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-0hx.4","title":"LSC + Virgo Cluster tier — EVCC + 2MRS ingest (~30 Mpc reach)","description":"**Goal:** extend the model to the Local Supercluster including the Virgo Cluster (~30 Mpc, ~100 Mly), ~5–10k galaxies, billboards-primary, with wireframe shells for Virgo + Fornax cluster boundaries.\n\n**Tier 2** of stellata-0hx.\n\n# Why\n\nGalaxy density jumps an order of magnitude past Local Volume. Virgo cluster substructure (M87 + ellipticals) is one of the cosmological landmarks the user should see — anchors the MW's location in the Local Supercluster.\n\n# Scope\n\n## Data\n\n- **EVCC (Kim et al. 2014)** — 1589 spectroscopic galaxies in 725 deg² around Virgo, SDSS DR7 footprint. Commit `data/extragalactic/evcc.csv`.\n- **2MRS bright-end** (Huchra et al. 2012, arXiv:1108.0669) — 44,599 all-sky galaxies to Ks ≤ 11.75. Filter to ≤30 Mpc for this tier. Commit `data/extragalactic/2mrs.tsv`.\n- **HyperLEDA** for morphological-type cross-walk where 2MRS doesn't carry it.\n- Distance: cz/H0 with peculiar-velocity correction via Cosmicflows-4 model (loaded from `.5`'s data file, or hand a coarse PV map at this tier).\n\n## Pipeline (`scripts/build-lsc.ts`)\n\n- Merge EVCC + 2MRS + HyperLEDA crosswalk on PGC ID.\n- De-dup against `public/local-volume.json` (within ~12 Mpc).\n- Output: `public/lsc.json` (~5–10k galaxies).\n\n## Renderer\n\n- All-billboard via `src/client/extragalactic-billboards.ts` from .3.\n- **Wireframe overrides** for ~20 famous Virgo + Fornax galaxies (M87, M86, M84, M49, M104 Sombrero, M60, M59, M64).\n- **Cluster boundary shells** for Virgo (centred on M87, ~2 Mpc radius) and Fornax. Coarse wireframe sphere or ellipsoid; one entry per cluster in `lv-overrides.tsv` (re-use the schema).\n- Camera envelope: regime SUPERCLUSTER kicks in at this scale (.2's regime enum).\n- Fade band: `LSC_FADE_BAND` (e.g. 5 Mpc – 50 Mpc).\n\n# Smoke\n\n- Pull camera back to ~30 Mpc, point at Virgo (RA 12h30m, Dec +12°). The Virgo cluster shell appears with M87 anchor visible as a wireframe inside, surrounded by ~thousand billboard galaxies.\n- Distance readout in Mpc (verifies .2 + .3 plumbing).\n- Floating-origin stable; no jitter on stars or LG.\n\n# Out of scope\n\n- Pure ICM / X-ray cluster visualisation (would need ROSAT or Chandra data; separate bead if ever).\n- Galaxy cluster mass / dynamics — same.\n- Coma cluster (~100 Mpc) — Tier 3 (.5).\n\n# Blocks\n\n.5 (Laniakea).\n\n# Blocked by\n\n.3 (re-uses billboard renderer + naming infrastructure).","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:54:17Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:54:17Z","labels":["billboards","deep-field","extragalactic","galactic","local-supercluster","research","virgo","visual"],"dependencies":[{"issue_id":"stellata-0hx.4","depends_on_id":"stellata-0hx","type":"parent-child","created_at":"2026-05-16T15:54:17Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.4","depends_on_id":"stellata-0hx.1","type":"blocks","created_at":"2026-05-16T15:56:05Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.4","depends_on_id":"stellata-0hx.2","type":"blocks","created_at":"2026-05-16T15:56:10Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.4","depends_on_id":"stellata-0hx.3","type":"blocks","created_at":"2026-05-16T15:56:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-0hx.3","title":"Local Volume tier — UNGC ingest + billboard galaxy pipeline (~12 Mpc reach)","description":"**Goal:** extend the model envelope from stellata-1ui's 1.5–2 Mpc out to the Local Volume (~12 Mpc), with per-galaxy wireframes for ~50 best-resolved galaxies and billboarded discs for the long tail (~1000 total).\n\nThis is **Tier 1** in the parent epic (stellata-0hx). First tier where billboards become the primary rendering primitive.\n\n# Why\n\nPer parent epic: Local Volume has the best per-galaxy distance + structural data in the universe (TRGB, Cepheids, SBF; ~5% distance errors). ~50 of the brightest have measured inclination/PA/axial ratio suitable for wireframe primitives (M81, M82, M101, NGC 4258, NGC 5128 / Cen A, Sculptor Group anchors, etc.). The long tail (~900 dwarfs) is per-object measured but only by position + magnitude — billboards.\n\n# Scope\n\n## Data pipeline (`scripts/build-local-volume.ts` — new)\n\n- Source: Updated Nearby Galaxy Catalog (UNGC, Karachentsev et al. 2013, arXiv:1303.5328) text table from sao.ru/lv/lvgdb, OR uncap the existing Pace 2024 LVDB ingest (its `dwarf_all` extends well past 250 kpc). Decide which at impl time based on what UNGC gives that LVDB doesn't.\n- Commit raw under `data/extragalactic/ungc.csv` per [[frozen-external-data]]. LFS if \u003e1 MB.\n- Filter: distance ≤ 12 Mpc.\n- De-dup: drop objects already in `public/local-group.json` (within 2 Mpc) to avoid double-render.\n- Schema parallels `public/local-group.json` (`name, id, center, kind, axes, quat, source, distance`) with two new fields per object: `morph_type` (Hubble T-type), `m_k` (apparent K-band magnitude).\n- Output: `public/local-volume.json` (Tier 1–2 stays JSON; binary format introduced at Tier 3).\n- Pure helpers split into `build-local-volume-pure.ts` with vitest coverage (parallel to `build-local-group-pure.ts`).\n\n## Overrides (`data/extragalactic/lv-overrides.tsv`)\n\nSame schema as `data/local-group/overrides.tsv`. Hand-curated structural rows for the ~50 wireframe-tier galaxies. Cite Cepheid/SBF distance refs + structural-paper refs per object.\n\n## Client runtime\n\n- `src/client/local-volume-loader.ts` — fetch + parse `public/local-volume.json`. Parallel to `local-group-loader.ts`.\n- `src/client/local-volume.ts` — wireframe layer for the ~50 with override rows. Mirrors `local-group.ts` pattern. Re-uses `galactic-fade.ts`'s `LV_FADE_BAND` (from .2).\n- **New module `src/client/extragalactic-billboards.ts`** — instanced quad pipeline modelled on `src/client/shaders/star.{vert,frag}.glsl`. Per-billboard attributes: position (ICRS pc), m_k (apparent K), morph_type (uint8 → colour LUT), distance (for size scaling). Re-uses `perceptual-disc.glsl` for the shading profile. This module is the engine; subsequent tiers (.4–.7) re-feed it.\n- Label gating: re-use `src/client/distance-gated-label.ts` for the ~50 wireframe galaxies. For named billboards (NGC/IC/Messier), feed the label-rank function from .1's spec — limited to ~30 visible at once.\n\n## Smoke (per [[alex-stellata-smoke-tests]])\n\n1. `npm run typecheck` + `npm test` green.\n2. Build script idempotent: re-running `build:catalog` doesn't rewrite `public/local-volume.json` if inputs unchanged (mtime check, mirror `build-local-group.ts`).\n3. Catalogue sanity:\n   - M81 at (RA 09h55m, Dec +69°, 3.63 Mpc)\n   - M101 at (RA 14h03m, Dec +54°, 6.86 Mpc)\n   - Cen A at (RA 13h25m, Dec −43°, 3.8 Mpc)\n   - Sculptor group anchors at expected positions.\n4. Manual browser smoke:\n   - From first-load (Sol focal), pull camera back. At ~5 kpc, MW + LG fade in (existing). At ~500 kpc, LG canonicals (stellata-1ui). At ~3 Mpc, M81 group + Cen A group + Sculptor group appear.\n   - Wireframe vs billboard split: ~50 famous have visible wireframe primitives; long tail is just disc/glow billboards.\n   - Distance readout switches to Mpc past 1 Mpc, Mly past 3 Mly (verifies .2 plumbing).\n   - Labels on M81, M101, Cen A, Sombrero (if in scope), Sculptor + M81 group anchors. No labels on dwarf billboards.\n   - Floating-origin stable at 12 Mpc out — no jitter on the AT-HYG starfield.\n\n# Out of scope\n\n- LSC / Virgo (Tier 2) — separate bead (.4).\n- Naming search-typeahead extension for new galaxies — file as related-link to stellata-l2x.\n- Mobile — Tier 1 is fine on desktop only for now.\n\n# Blocks\n\n.4 (LSC + Virgo). Re-uses this bead's `extragalactic-billboards.ts` renderer.\n\n# Blocked by\n\n.2 (camera envelope + distance formatting).","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:54:00Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:54:00Z","labels":["billboards","deep-field","extragalactic","galactic","local-volume","research","visual","wireframe"],"dependencies":[{"issue_id":"stellata-0hx.3","depends_on_id":"stellata-0hx","type":"parent-child","created_at":"2026-05-16T15:54:00Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.3","depends_on_id":"stellata-0hx.1","type":"blocks","created_at":"2026-05-16T15:56:04Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.3","depends_on_id":"stellata-0hx.2","type":"blocks","created_at":"2026-05-16T15:56:09Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx.3","depends_on_id":"stellata-1ui","type":"blocks","created_at":"2026-05-16T15:56:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-0hx","title":"Extragalactic deep-field — far-field galaxy + cosmic-web model out to the observable horizon","description":"**Goal:** complete the spatial scale of stellata's model from the Local Group (stellata-1ui's ~2 Mpc cap) out to the limits of current observational data — Local Volume → Virgo Supercluster → Laniakea → Pisces-Cetus / Sloan Great Wall → SDSS spectroscopic horizon → CMB last-scattering backdrop.\n\n# Why\n\nStellata's detail-gradient mission: highest-density measurable info near Earth, simpler model further out, per [[stellata-cds-data-sources-and-detail-gradient]]. The far-field beyond LG is currently absent. Real observational catalogues exist with per-galaxy positions + structural data out to ~150 Mpc (Cosmicflows-4), and per-galaxy positions only (redshift-derived) out to ~1 Gpc (NED-LVS, SDSS Main). Past that, individual luminous probes (LRGs, quasars) reach ~3 Gpc. The CMB is the back wall.\n\n# Tier breakdown — what catalogues exist where, and where per-object detail dies\n\n## Tier 1 — Local Volume (≤ ~12 Mpc / ~40 Mly)\n\nPer-galaxy distances + structural data best-in-class.\n\n- **Updated Nearby Galaxy Catalog (UNGC), Karachentsev et al. 2013** (arXiv:1303.5328) — 869 galaxies, CC0-equivalent, sao.ru/lv/lvgdb.\n- **Pace 2024 LVDB** — already ingested under data/local-group/, currently capped at 250 kpc.\n- **Structural data**: ~100–200 of brightest have measured inclination, PA, ellipticity, half-light radius.\n- **SGA-2020** (arXiv:2307.04888) — Sérsic decomposition for ~383k galaxies, DESI footprint only.\n- **Distance accuracy**: ~5% (TRGB, Cepheids, SBF).\n- **Names**: ~100% NGC / IC / UGC / Messier.\n- **Render**: wireframes for ~50 best-resolved (M81, M82, M101, NGC 4258, NGC 5128 / Cen A, Sculptor anchors). Billboarded discs for the long tail.\n\n## Tier 2 — Local Supercluster + Virgo (~30 Mpc / ~100 Mly)\n\nDensity jumps an order. Cluster substructure (Virgo, Fornax, Eridanus) dominates.\n\n- **EVCC** (Kim et al. 2014) — 1589 galaxies in 725 deg², SDSS DR7.\n- **2MRS** (Huchra et al. 2012, arXiv:1108.0669) — 44,599 galaxies all-sky to Ks ≤ 11.75, 97.6% complete.\n- **HyperLEDA** — ~1M galaxies with PA + axial ratio + morphological type.\n- **Distance accuracy**: ±5–10% from cz/H0 after PV correction.\n- **Render**: billboards primary. Wireframes for ~20 famous (M87, M86, M84, M49, M104). Virgo + Fornax cluster boundary shells.\n\n## Tier 3 — Laniakea + sibling superclusters (~150 Mpc / ~500 Mly)\n\nThe supercluster-as-watershed picture.\n\n- **Cosmicflows-4** (Tully et al. 2023) — 55,877 galaxy distances z ≤ 0.1. EDD (edd.ifa.hawaii.edu). PGC-keyed.\n- **Dupuy \u0026 Courtois 2023** (A\u0026A 678, A176, arXiv:2305.02339) — Laniakea + Apus, Hercules, Lepus, Perseus-Pisces, Shapley watersheds as 3D meshes.\n- **2MRS** continues to cover this tier.\n- **Distance accuracy**: ±10–20% PV band, ±5% past it.\n- **Render**: pure billboards for galaxies. ~6 watershed wireframe shells. MW position relative to Laniakea basin centre is the headline.\n\n## Tier 4 — Pisces-Cetus Supercluster Complex + Sloan Great Wall (~200 Mpc – 1 Gpc)\n\nCosmic web becomes dominant visual.\n\n- **NED-LVS** (Cook et al. 2023, arXiv:2306.06271) — 1.9M objects to 1000 Mpc. 100% complete bright to ~400 Mpc; 70% complete fainter to ~300 Mpc.\n- **SDSS Main spectroscopic** — ~700k galaxies to z ≈ 0.25, Northern footprint.\n- **Distance accuracy**: PV-dominated below 100 Mpc, Hubble-dominated above. Past 200 Mpc, z·c/H0 IS the distance to ~1%.\n- **Pisces-Cetus**: ~380 Mpc long, ~160 Mpc wide, core at ~200 Mpc.\n- **Sloan Great Wall**: ~420 Mpc long at ~300 Mpc distance.\n- **Render**: galaxies as point-glow primitives. Labels only on ~10–20 named cosmic-web structures.\n\n## Tier 5 — BOSS / eBOSS reach (z ≲ 0.7, ~2.5 Gpc / ~8 Gly comoving)\n\nThe cosmological-survey regime.\n\n- **SDSS BOSS/eBOSS DR16/DR17** — 1.5M LRGs + ELGs + ~500k quasars.\n- **Distance**: spectroscopic z + ΛCDM → comoving. ~1% accuracy.\n- **Cosmology**: Planck 2018 (H0=67.4, Ω_m=0.315) baked into build pipeline.\n- **Structural data**: essentially none — galaxies are point sources.\n- **Render**: redshift-coloured point glows. Selection bias (LRG/ELG/QSO) documented and accepted per [[stellata-data-fidelity-principle]].\n- **Beautiful**: BAO ~150 Mpc feature emerges from the distribution if power-spectrum-weighted.\n\n## Tier 6 — Beyond z~1 (out to the horizon at z~1100)\n\n- **Quasar samples** — DESI / eBOSS to z~3.5. Sparse + biased.\n- **JWST high-z** — pencil beams only.\n- **CMB last-scattering sphere** — Planck 2018 SMICA HEALPix temperature map, comoving radius ~14 Gpc. The natural back-wall of the visualisable universe.\n\n# Where data quality dies (three different walls)\n\n| Aspect | Where it dies |\n|---|---|\n| Per-galaxy structure (PA, inclination, axial ratio) | ~100 Mpc median, ~500 Mpc for most luminous |\n| Redshift-independent distances | ~150 Mpc (Cosmicflows-4 z\u003c0.1) |\n| Per-galaxy spectroscopic z | ~1 Gpc (SDSS Main), ~3 Gpc (BOSS LRG/ELG) |\n| Sky completeness | 2MRS all-sky to ~150 Mpc; SDSS North-only past that |\n| Individually-named galaxies | ~10k named, mostly \u003c 30 Mpc; PGC numbers past |\n\nNatural endpoint: SDSS-reach universe + CMB backdrop. Past z ≈ 0.5 the picture is fundamentally statistical.\n\n# Architectural changes the proposal asks for\n\n1. **Camera envelope as configurable tier** (not magic literal in stellata.ts). Regime enum: STELLAR / GALACTIC / SUPERCLUSTER / COSMIC. Each sets far/maxDistance/near-plane appropriately. The .2 bead.\n2. **Galaxy billboard pipeline as parallel to star pipeline**. Instanced quads sized by M_K + distance, coloured by morphological type. Shaders extend `perceptual-disc.glsl`. The .3 bead.\n3. **Naming policy**: labels only on named objects. Label-rank function with spatial bucketing, hysteresis, hard cap (~30 visible at once across all tiers combined). Extends `local-group.ts:338–383`.\n\n# Performance considerations\n\nStellata baseline: ~313k instanced-quad stars at 60 fps. Challenge isn't \"can we render\" — it's how float32 precision, label-engine cost, and bundle size scale.\n\n## Cross-cutting\n\n1. **Floating-origin float32 budget** (~7 sig figs):\n   - Tier 1–3 (≤ 200 Mpc): existing rebase sufficient. Precision ~15 pc near 150 Mpc, well below galaxy-position uncertainty (~Mpc).\n   - Tier 4+ (≥ 1 Gpc): split rendering into near (parsec-scale) + far (Mpc-scale) passes. Cesium / Outerra-style.\n   - Tier 5–6: emulated-double-precision on CPU; hand GPU only camera-relative float32. The star pipeline already does this trick — lift it to galaxies.\n\n2. **Camera envelope + log-depth precision**. Far plane growth ~3×10^4×. Log-depth handles it (~1 bit per doubling). Mitigate depth-fighting via reverse-z + dynamic near-plane growth per regime.\n\n3. **Label engine cost**. Today 884 ops/frame (52 LG objects × ~17 ops). Naive Tier 4 → 32M ops/frame. Mitigations: named-only label set (~10k), spatial octree bucketing (~100–500 per-frame candidates), 1-pixel hysteresis, ~30 visible cap.\n\n4. **Initial-load bundle size**. Today ~13 MB catalog + 2 MB search + dust. Tier cumulative ~150–200 MB naive — not viable. **Progressive loading per regime** (dust-loader pattern): Tier 1+2 cold, Tier 3+ fetched on zoom-cross. **Region chunking** Tier 4+ (HEALPix Nside=4 = 192 cells, ~10k galaxies/cell). Frustum-culled GPU upload + small chunk cache.\n\n5. **Build pipeline**. Each tier's `build-*.ts` is one-shot. Per [[frozen-external-data]]: raw fetch is one-off `refresh-*.py` excluded from `npm run build`; commit processed binary. CI build under a minute.\n\n## Per-tier perf table\n\n| Tier | Objects | Wireframes | Billboards | New perf concerns | Tradeoff |\n|---|---|---|---|---|---|\n| 1 LV | ~1000 | ~50 | ~950 | None | None |\n| 2 LSC | ~10k | ~20 + cluster shells | ~10k | Label set growth | Top-N rank extension |\n| 3 Lan | ~56k | 6 watershed shells | ~56k | Binary format; lazy-load | LineSegments shells |\n| 4 PCs | ~1.9M | 10 labels | ~1.9M | Float32 floor; chunking; LOD | Hybrid: billboards near + volumetric far |\n| 5 BOSS | ~2M | None | ~2M | Emulated-double; cosmology bake | LRG/ELG/QSO selection bias |\n| 6 CMB | 1 sphere | None | 1 texture | Skybox outside floating-origin | Nside=512 (50MB) vs Nside=2048 (200MB) — picks 512 |\n\n## Tier-specific tradeoffs called out\n\n- **Tier 3 watersheds**: LineSegments (cheap, ~60k segments total) over translucent surfaces (pretty but fill-rate-heavy).\n- **Tier 4 hybrid LOD**: per-galaxy billboards within ~300 Mpc of camera; volumetric density-field for past that. Per-galaxy smoothstep handover avoids LOD pop. Cost: two render paths to maintain.\n- **Tier 4 chunking**: HEALPix Nside=4 sky cells, dust-loader precedent. Cost: GPU upload churn on fast motion; mitigate with ~20-chunk pinned cache.\n- **Tier 5 selection bias**: per [[stellata-data-fidelity-principle]], present data honestly. Document selection function; debug-panel toggle.\n- **Tier 6 CMB sphere**: textured skybox outside floating-origin. Necessarily occludes anything past it (by definition).\n\n## Deferred from perf scope\n\n- Log-depth additional precision tier past 1 Gpc — wait for actual symptoms.\n- GPU memory ceiling at 1.9M billboards — hardware-dependent; add perf-hud readout in .3.\n- Mobile: likely Tier 1–2 only (per [[stellata-prioritization-framework]] mobile is tier 6).\n\n# Out of scope (deliberate)\n\n- Time-evolution of cosmic web (snapshot only, like MW + LG today).\n- Dark matter halo overlays — separate epic if ever.\n- Per-galaxy stellar population / SED detail (billboards take morphology-bucketed colour).\n- AGN / quasar host distinction (single magnitude-coloured point).\n\n# Children + sequencing\n\n- **.1 Research / design-gate (P2)** — `docs/extragalactic-roadmap.md` with data inventory, cosmology choice, manifest schema. Blocks .2–.8.\n- **.2 Camera envelope + distance formatting refactor (P2)** — regime enum, fmtDistAuto Mpc/Gpc/Gly branches, FadeBand builder. Blocks .3–.8.\n- **.3 Local Volume tier (P3)** — UNGC/LVDB ingest to 12 Mpc, billboard pipeline. Blocks .4.\n- **.4 LSC + Virgo (P3)** — EVCC + 2MRS bright-end, cluster shells. Blocks .5.\n- **.5 Laniakea + watersheds (P3)** — Cosmicflows-4 + Dupuy 2023. Blocks .6.\n- **.6 Pisces-Cetus + Sloan Great Wall (P3)** — NED-LVS / SDSS Main, chunking + LOD. Blocks .7.\n- **.7 BOSS / eBOSS + redshift colouring (P3)** — emulated-double, cosmology bake. Blocks .8.\n- **.8 CMB last-scattering backdrop (P3)** — Planck Nside=512 SMICA skybox.\n\n# References to consult at impl time\n\n- Planning session that filed this epic: /Users/alexm/.claude/plans/i-m-about-to-implement-harmonic-pizza.md\n- Predecessor: stellata-1ui (M31/M33 + 1.5–2 Mpc LG expansion).\n- Sibling far-field epic: stellata-36y (HiPS-derived cumulative glow below AT-HYG floor).","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:52:26Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:52:26Z","labels":["deep-field","extragalactic","galactic","research","visual"],"dependencies":[{"issue_id":"stellata-0hx","depends_on_id":"stellata-1ui","type":"related","created_at":"2026-05-16T15:56:35Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx","depends_on_id":"stellata-36y","type":"related","created_at":"2026-05-16T15:56:20Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx","depends_on_id":"stellata-7de","type":"related","created_at":"2026-05-16T15:56:21Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0hx","depends_on_id":"stellata-l2x","type":"related","created_at":"2026-05-16T15:56:21Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-8ie","title":"Warp / navigate to extended deep-field objects (Local Group + future deep field)","description":"**Status:** decision-pending. Sibling to stellata-l2x (search-catalogue ingest); the two ship the end-to-end \"type M31, press enter, arrive\" UX but the design questions are independent. Filed separately per the navigate-from/to vs search-ingest seam.\n\n# Goal\n\nA working warp-target model for extended Mpc-scale objects. Today's warp lands at a point (a star, a focused planet, a cloud centroid) at a calibrated minDistance. Galaxies aren't points — landing at the centroid of M31 plants the camera *inside* the disc, where the wireframe is invisible by definition (the same MW-disc-inside guard applies). The user needs to park *outside* the disc at a distance where the silhouette reads.\n\n# Open design questions\n\n1. **Park-distance heuristic.** Suggestions:\n   - `3-5 × maxAxis` of the target's local-frame axes.\n   - Solve for a target apparent angular size (e.g. the galaxy fills ~30% of the screen at the user's FOV).\n   - Per-object override in the TSV.\n   - The molecular-cloud warp already solves a near-identical problem — Alex confirmed the reuse direction is the right one in chat; this bead carries the actual application.\n\n2. **Park direction.** Where in the sky does the camera arrive? Suggestions:\n   - On the line of sight from Sol — keeps the \"sky direction matters\" intuition.\n   - Perpendicular to the disc normal (for disc kinds) — gives the canonical \"face-on\" silhouette.\n   - At the same galactic-latitude direction the user was at — preserve user orientation.\n   - User toggle.\n\n3. **MW-disc passage during warp.** A warp from inside the MW disc to M31 crosses the disc surface (and visually the disc fade band) en route. Decisions:\n   - Animate the transit so the user sees the MW disc receding behind them, then M31 approaching?\n   - Cut to the arrival vantage with a fade?\n   - Reuse stellata-2br's log-distance arrival profile across Mpc scales (currently calibrated to ≤ 250 kpc).\n\n4. **Focus model after arrival.** Galaxies aren't stars — there's no orbit ring, no heliopause, no per-star physical body. Does focusing a galaxy:\n   - Surface a custom info panel (the hover/click-pin from stellata-lo5)?\n   - Re-show the wireframe in a chart-style halo?\n   - Just park the camera and let the user inspect?\n\n5. **Departure (warp from a galaxy to somewhere else).** Today warp departure is anchored to the camera's current focus. Galaxy-focus exit semantics:\n   - Click outside in OBSERVE mode unfocuses (same as star) — but star-unfocus closes to the focused star, and \"close to\" a galaxy is ambiguous.\n   - Drop focus on warp-out, same as the cloud-to-star transition handles it.\n\n6. **Outside Local Group (future).** This bead's design needs to forward-compat to deep-field DSOs (`stellata-m40.4`), galaxy groups past 2 Mpc, eventually clusters. The park-distance heuristic shouldn't be hard-coded to 2 Mpc.\n\n# Related work\n\n- stellata-l2x — Search-catalogue ingest for LG. Companion bead; together they ship the end-to-end UX.\n- stellata-2br — Camera-arrival profile (log-distance deceleration). The arrival profile probably needs Mpc-scale validation; file a follow-up bead under 2br if anything breaks at 2 Mpc.\n- stellata-c7u molecular-cloud warp — the existing pattern for \"warp to extended object\". Look here first.\n- stellata-1ui — The 2 Mpc envelope.\n- stellata-m40.4 — Chart-mode glyphs for distant DSOs (Andromeda / Messier on celestial sphere). Different rendering, same navigate-to question.\n- stellata-lo5 — Hover labels' design gate informs the post-arrival focus panel.\n\n# Smoke / acceptance (post-decision)\n\n- Warp from Sol to M31: camera lands outside the M31 disc at a vantage where the inclined disc plate reads as a coherent shape, not edge-on at the centroid.\n- Warp from M31 to M33: arrival vantage shows the M33 disc clearly; the M31 silhouette recedes behind / off-screen.\n- Warp from M31 back to a focused star (e.g. Sirius): transition closes back into the MW with no clipping artifacts at the disc fade band.\n- Chart-mode arrival reads cleanly with the paper aesthetic.\n\n# Out of scope\n\n- Search-catalogue ingest (sibling bead stellata-l2x).\n- Stellar-population resolved rendering inside LG members (out of scope per the original LG epic).","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:18:06Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:18:06Z","labels":["local-group","observe","ux","warp"],"dependencies":[{"issue_id":"stellata-8ie","depends_on_id":"stellata-1ui","type":"related","created_at":"2026-05-16T15:18:17Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8ie","depends_on_id":"stellata-l2x","type":"related","created_at":"2026-05-16T15:18:15Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8ie","depends_on_id":"stellata-lo5","type":"related","created_at":"2026-05-16T15:18:18Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-l2x","title":"Add Local Group objects to the search catalogue / typeahead","description":"**Status:** decision-pending. Several open questions Alex needs to answer before implementation starts. Files separately from the warp / focus-park bead per the navigate-from/to vs search-ingest seam.\n\n# Goal\n\nMake the 123 Local Group objects (LMC, SMC, Sagittarius dSph, classical + ultra-faint dSphs, plus the M31 subgroup + outer-band dIrrs shipped in stellata-1ui) typeable into the existing destination-search typeahead — same UX surface stars use today, so a user can type \"M31\" and warp.\n\n# What exists\n\n- `src/client/search.ts` / `src/client/typeahead.ts` — the destination-search engine.\n- `public/search-index.json` — ~13 MB raw / ~2 MB gzipped index built from the star catalogue, ingested by `scripts/build-catalog.ts`.\n- `public/local-group.json` — the rendered LG catalogue with display name, slug id, ICRS centre, distance per object. Already typeable in principle.\n- The typeahead today is *star-shaped* — entries carry HIP, GJ, Bayer, etc. designations.\n\n# What's missing\n\n- LG entries in `search-index.json` (or a sibling index).\n- A way for the typeahead to disambiguate star vs galaxy results.\n- A warp target resolver for galaxy entries (separate bead — see \"Companion bead\" below).\n\n# Open design questions (Alex decides before any code lands)\n\n1. **Single index vs separate index.** Append LG entries to `search-index.json` as a new entry kind, or ship a `public/lg-search-index.json` alongside? Trade-off: single index is one fetch + one Fuse instance; separate index is cleaner schema but two indexes to merge in UI.\n\n2. **Entry-kind tag.** What discriminator lives on each entry? Options:\n   - `kind: 'star' | 'galaxy' | 'cloud' | ...` enum.\n   - Implicit via designation pattern (M / NGC / IC prefix → galaxy).\n   - The kind drives the warp target resolver and the typeahead row presentation.\n\n3. **What designations to ingest per LG object.**\n   - Primary display name (already in local-group.json).\n   - Catalogue cross-walks — does NGC 205 also typeable as \"M 110\"? \"Messier 110\"? IC 10 / IC 0010? \"Andromeda Galaxy\" → \"M31\"? Alex's call.\n   - LVDB key (slug) as a fuzzy fallback.\n\n4. **Typeahead row presentation.** Today star rows show name + designation cluster + distance. For galaxies — show distance + type (dSph / spiral) + apparent magnitude? Or stay minimal? Likely related to stellata-lo5 (hover labels) — the same \"what info matters for a galaxy\" question.\n\n5. **Constellation typeahead interaction.** Today there's a separate `#con-input` for constellations. Galaxies should sit in destination search, not constellation typeahead. Confirm.\n\n6. **Disambiguation collisions.**\n   - \"Sagittarius\" already routes to the Sagittarius constellation. Now also a dSph (existing) AND a dIrr (new at ~1.2 Mpc). Three results — what's the rank order?\n   - \"M31\" doesn't collide with any star designation today, but \"M 32\" might look like a Messier-cluster shorthand.\n   - Decision: ordering rule on collisions (e.g. brightest / closest / most-clicked / alphabetical).\n\n7. **Index size.** Adding 123 entries × ~50 bytes/entry ≈ 6 KB raw / 1 KB gzipped — negligible. No size pressure either way.\n\n# Companion bead\n\nWarping *to* an extended LG object is a separate concern, filed as a sibling bead (focus-park model for extended objects — see stellata-7l5 once filed). Search ingest doesn't need that to land first, but the typeahead row's \"warp on enter\" hook won't function end-to-end until both beads ship.\n\n# Smoke / acceptance (post-decision)\n\n- Type \"M31\" into `#go-input` → see M31 in typeahead → enter → camera warps near M31 and parks at a sensible distance (sensible = whatever the warp bead defines).\n- Type \"M 32\" / \"M32\" / \"NGC 205\" / \"Sextans\" — each disambiguates correctly per decisions above.\n\n# Related work\n\n- stellata-nxb — Expand search index with alternate designations (V-numbers, GJ, HIP variants, CCDM, etc.). Same pipeline, different content; the kind-discriminator decision in this bead overlaps with theirs.\n- stellata-9hp.4 — Search index integration for RNe illuminators. Same shape — file under a unified scheme if 9hp.4 hasn't started yet.\n- stellata-lo5 — Hover labels' design gate also touches \"what info matters for a galaxy\"; cross-link the field-list outcomes.\n- stellata-1ui — The 2 Mpc envelope this builds on.\n\n# Out of scope\n\n- Warp animation / focus-park model — separate bead.\n- AT-HYG catalogue cross-walks for LG stellar populations (LMC red supergiants etc.) — stellata-8l0 territory.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:17:31Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:17:31Z","labels":["local-group","picker","search","ux"],"dependencies":[{"issue_id":"stellata-l2x","depends_on_id":"stellata-1ui","type":"related","created_at":"2026-05-16T15:18:16Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-l2x","depends_on_id":"stellata-8ie","type":"related","created_at":"2026-05-16T15:18:16Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-l2x","depends_on_id":"stellata-lo5","type":"related","created_at":"2026-05-16T15:18:18Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-7de","title":"LG / extragalactic apparent-magnitude filtering — needs visual-vs-representational decision first","description":"**Status:** research / decision-pending. Not implementable yet — blocked by stellata-8hu (Physical vs Representational rendering — three-mode framework) and entwined with stellata-dbl (Calibration workflow).\n\n# Context\n\nThe Local Group layer (stellata-38m + stellata-1ui) ships objects out to 2 Mpc as **representational wireframes** — LineLoop ellipsoids / discs that ignore apparent magnitude. The wireframes are visible whenever the MW-fade band reveals them, regardless of how dim the actual object is to a human eye. This is correct for the representational use case: if we didn't draw a wireframe at all, the user would never see most of these dwarfs even with eyes glued to the eyepiece — they're too diffuse, too low-surface-brightness, or too distant.\n\nStars sit on the other side of that line: they're rendered with a calibrated apparent-magnitude pipeline (the magnitude slider gates which point-sources draw, calibrated to the perceptual-disc shader). Feeding the Local Group through the same slider conflates two different rendering philosophies.\n\n# What we already have\n\n- **LVDB carries `M_V` (absolute V magnitude)** for every row in the snapshot — column 86 of `data/local-group/lvdb-snapshot.csv`. Apparent magnitude from any vantage is `m_V = M_V + 5·log10(d / 10pc)`, computable at build time once distances are committed.\n- **M31 / M33 standalones** ship without M_V (the overrides.tsv schema doesn't carry it). Well-cited values: M31 M_V ≈ −21.5, M33 M_V ≈ −18.8. Hand-curate when implementation lands.\n- The pure path is already exporting `distance` per object; adding a `appMag` field is a build-script change of ~10 lines.\n\n# Two ways to apply this — both depend on stellata-8hu\n\n## (A) Standard-mode representational filter — independent slider\n\nAdd an LG-specific magnitude slider (or a single \"deep-field\" cutoff knob) that gates wireframe visibility by computed apparent V magnitude at the camera vantage. M31 and the Magellanic Clouds stay visible across the typical range (m_V ≈ 0–5); the long tail of ultra-faints (m_V ≈ 13–17) drops out at moderate settings.\n\nThe key argument for a separate slider: stars and wireframe galaxies compete for different screen real estate. A 17th-mag galaxy spanning a degree is perceptually very different from a 17th-mag point source — the slider semantics shouldn't be identical.\n\n## (B) Pure-Visual blob rendering — calibrated against stars + MW background\n\nReplace (or augment) the wireframe with a low-surface-brightness blob shape whose integrated brightness matches `m_V` at the camera vantage, calibrated against the star-field + MW volumetric background so the relative perceptual weight is right. This is the \"what would I see from here\" view — and the answer is often \"not much\" once you're not inside the disc.\n\nBlocked by stellata-8hu's three-mode framework — Pure Visual exists as a concept there but no layer renders that way today. Also depends on stellata-dbl's calibration procedure (need a benchmark image / procedure to verify M31 blob brightness lines up with the LMC blob brightness at the same magnitude preset).\n\n# Decisions to make at unblock time\n\n- Whether to ship (A), (B), or both. The current 2 Mpc render is the representational baseline; (A) and (B) are two different paths off it.\n- Whether (A)'s slider is independent of the star magnitude slider or unified.\n- Whether (B) replaces wireframes in Pure Visual mode, or both render and the user picks.\n- Blob shape model for (B) — Gaussian falloff vs single-component Sérsic vs n-body-style point cloud. (B) is essentially the planet-body / star-point physics applied to extended objects.\n\n# Data work to do at implementation time\n\n- Build script (stellata-1ui's `build-local-group.ts`) exports `appMagFromSol` per object (computed from M_V + distance). Trivial extension once M_V plumbs through.\n- Add `M_V` column to the override TSV for M31 / M33 (and any other standalone rows). LVDB-merge rows pick up M_V from the LVDB row.\n- Calibration spot-checks against published magnitudes (LMC m_V ≈ 0.9, M31 m_V ≈ 3.4, M33 m_V ≈ 5.7, Sextans dSph m_V ≈ 10.4, …) to verify the M+5log10(d/10) formula lands right.\n\n# Out of scope (separate beads if/when they happen)\n\n- Adding the LG to the star magnitude slider semantics (the unified case under decision point above). Defer that decision to 8hu's design gate.\n- Stellar-population resolved rendering for LMC / SMC / Sgr dSph — already excluded under stellata-cds-data-sources-and-detail-gradient + stellata-38m's out-of-scope list.\n\n# Refs\n\n- stellata-1ui: the 2 Mpc envelope this builds on.\n- stellata-38m: original LG wireframe layer.","status":"deferred","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-16T14:15:24Z","created_by":"Alex Marshall","updated_at":"2026-05-16T14:15:49Z","defer_until":"2027-05-16T15:15:49Z","labels":["local-group","physics","ux","visual"],"dependencies":[{"issue_id":"stellata-7de","depends_on_id":"stellata-1ui","type":"related","created_at":"2026-05-16T15:15:36Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-7de","depends_on_id":"stellata-8hu","type":"related","created_at":"2026-05-16T15:15:35Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-7de","depends_on_id":"stellata-dbl","type":"related","created_at":"2026-05-16T15:15:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.193","title":"Lift apparent-size label-ranking into a reusable engine + audit chart-labels overlap","description":"# Goal\n\nCapture the top-N apparent-size label-ranking pattern introduced in\nstellata-38m as reusable infrastructure so other catalog families\n(future user-option label sets — brightest stars, named stars, nebulae,\nthe M31 group when stellata-1ui lands) can share the same ranking,\nviewport-clipping, and SVG-binding machinery. Audit overlap with the\nexisting chart-mode label engine (`chart-labels.ts`) before deciding\nwhether to lift-and-reuse OR unify the two engines.\n\n# Current state\n\n`src/client/local-group.ts` carries the pattern:\n\n- `LabelCandidate` = `{ id, centerAbs, maxAxis }`\n- `RankingParams` carries camera matrices + viewport + tunables\n  (`topN`, `minPixelSize`, `mwInsideDiscPc`, etc.).\n- `computeVisibleLabels(candidates, params) → Set\u003cid\u003e` runs per frame:\n  1. Inside-MW guard (extra-MW objects suppressed while camera is\n     inside the disc).\n  2. Behind-camera filter via `matrixWorldInverse · centerAbs`.\n  3. Apparent-size floor via `2·atan(maxAxis / camToObj) × pxPerRad`.\n  4. Viewport overlap (centroid ± pxSize/2 vs the screen rect).\n  5. Sort descending by apparent size; take top N.\n- Tunables exposed via the Deep-field debug panel\n  (`local-group-tuning.ts`).\n- SVG labels bound through `createDistanceGatedLabel` with predicate\n  `() =\u003e visibleLabelIds.has(id)`.\n\nThe MW-inside guard is the only LG-specific piece. Everything else is\ngeneric.\n\n# Audit deliverable\n\nRead `src/client/chart-labels.ts` and the chart-mode label flow in\n`docs/chart-mode.md` and write a short audit (in this bead's notes,\nor a new design-gate child) covering:\n\n- Does chart-labels.ts have its own \\\"which to label\\\" picking\n  (magnitude-based? collision-avoidance? top-N?), and how does it\n  relate to the LG pattern?\n- Is there a single shared abstraction that fits both, or are the\n  domains different enough that two engines is correct?\n- What's the right module name + shape for a shared engine?\n\nThen either:\n\n**(a) No meaningful overlap.** Lift the LG ranking — `LabelCandidate`,\n`RankingParams`, `computeVisibleLabels`, and the registration +\nframe-handler scaffolding — to a new `src/client/label-ranking.ts`.\nMove the MW-inside guard to a per-candidate hook so the helper stays\ngeneric. `local-group.ts` becomes a consumer; chart-labels.ts\nunchanged.\n\n**(b) Real overlap.** Unify the two: define a single label-engine\ninterface; reimplement chart-labels.ts and local-group.ts on top of\nit. Larger scope; file as an epic with the two consumers as children.\n\nEither way, leave a follow-up bead for the user-option consumer Alex\nsketched in the stellata-38m PR thread: \\\"show labels on the screen as\na user option\\\" — a settings-panel toggle that opts into labelling for\na configurable set (brightest N stars, named stars, named nebulae,\netc.) reusing whatever shared engine lands here.\n\n# Out of scope\n\n- Any user-facing labelling option (e.g. a 'show labels for named\n  stars' toggle). Gates on the audit conclusion; file separately.\n- The chart-mode rework itself (m40.x) — this bead only touches\n  chart-labels.ts if the audit chooses path (b).\n\n# Acceptance — audit only (this bead)\n\n- Written audit answering the three questions above, stored in this\n  bead's notes (\\`bd update --notes\\`).\n- Decision recorded: path (a), path (b), or 'neither — keep two\n  engines'.\n- If (a) or (b), a follow-up implementation bead filed (P3) with the\n  audit linked.\n\n# References\n\n- stellata-38m PR: introduced the LG ranking pattern.\n- `src/client/local-group.ts`: current implementation.\n- `src/client/chart-labels.ts`, `docs/chart-mode.md`: chart-mode\n  engine to audit against.\n- `src/client/distance-gated-label.ts`: the SVG-binding layer that\n  both engines presumably want to share regardless.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T12:46:03Z","created_by":"Alex Marshall","updated_at":"2026-05-16T12:46:03Z","labels":["chart-mode","local-group","overlays"],"dependencies":[{"issue_id":"stellata-9mm.193","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-16T13:46:03Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.190.1","title":"perf-hud: tear down realMark/realMeasure/realFrame on panel close","description":"# Problem\n\n`perf-hud.ts` flips `installed = true` and rewires the module-level\n`_mark / _measure / _frame` function references to the real\nimplementations on first `buildPerfSection()` call (lines 130-135).\n`dispose()` (lines 245-253) explicitly does NOT reset them — the\nin-file comment justifies this:\n\n\u003e realMark/realMeasure/realFrame stay installed — ring buffers keep\n\u003e filling between toggles so the histogram has data on re-open.\n\nResult: once the user opens the debug panel (or even the Perf section\nspecifically) once in a session, every `perfMark / perfMeasure /\nperfFrame` call site keeps doing real work — Map lookups, ring writes,\nsection GC sweeps — for the rest of the page session, even after the\npanel is closed.\n\n# Call-site cost\n\n`stellata.ts:animate()` calls perf instrumentation ~12× per frame\n(`frame.total`, `controls.update`, `pre-render`, `coreMask`,\n`gpu.render`, `frame.handlers`, each mark + measure pair, plus\n`frame()`). At 60 Hz that's ~720 Map ops/sec, plus the section-GC\nwalk in `realFrame`. Additional call sites in `chart-labels.ts`\n(chart-mode label engine) and elsewhere stack on top.\n\nModest in absolute terms but it's continuous, perpetual work that\nexists ONLY for the HUD — and runs whether or not the HUD is open.\nThe session-wide install runs counter to the \"debug panel sections\nshould be dark when hidden\" invariant Alex specifically asked about.\n\n# Fix\n\nIn `dispose()`, restore the three function references to no-op stubs:\n\n    _mark = () =\u003e {};\n    _measure = () =\u003e {};\n    _frame = () =\u003e {};\n    installed = false;\n\nAnd clear the data structures so a re-open isn't holding pinned\nreferences:\n\n    sections.clear();\n    starts.clear();\n    frameCounter = 0;\n\n# Trade-off\n\nReopening the panel will cold-start the histogram (no recent history\nto show). The original comment cites this as the reason for keeping\nthe install warm. Acceptable cost — the perf HUD is opt-in via the\ndev console, not a live monitoring surface; users who want continuous\nmetrics should keep the panel open.\n\n# Scope clarification — pin/arrow HUDs are NOT in this bead\n\n`pin-debug-hud.ts` and `arrow-fade-debug-hud.ts` also keep their\n`onFrame` body running latch comparisons when the section is\ncollapsed (DOM build is gated, latch updates are not). This is\nINTENTIONAL per the in-file comments — latched extremes are meant\nto span a collapse/expand window. Different design call from the\nperf-hud install leak. Leave alone unless filed separately.\n\n# Acceptance\n\n- `dispose()` resets `_mark / _measure / _frame` to no-op stubs and\n  clears `installed`, `sections`, `starts`, `frameCounter`.\n- After panel close, a quick Chrome devtools sampling profile during\n  a focused-camera-warp shows zero time in `realMark / realMeasure /\n  realFrame / ensureSection` (or those frames no longer appear in\n  the profile at all).\n- Re-opening the panel cold-starts the histogram (empty) and fills\n  it over the next ~RING_SIZE frames as expected.\n- All existing perf-hud vitest tests still pass.","status":"closed","priority":3,"issue_type":"bug","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T11:35:41Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:25Z","started_at":"2026-05-16T11:52:36Z","closed_at":"2026-05-16T21:38:25Z","close_reason":"merged via PR #68 (perf-hud: tear down on debug-panel close)","labels":["debug-panel","perf-hud","pr-68"],"dependencies":[{"issue_id":"stellata-9mm.190.1","depends_on_id":"stellata-9mm.190","type":"parent-child","created_at":"2026-05-16T12:35:41Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.188.4","title":"poi-overlay.ts: add test file covering syncPool / hideEntry / on-screen vs off-screen branch","description":"T-2 finding from the PR #55 review (637bdf9). poi-overlay.ts has no test file. The module ships state-machine behaviour (syncPool's per-POI Entry pool, on-screen/off-screen projection branch, hideEntry sentinel wipe, idempotent show/hide) and a click affordance contract (on-screen label deselects, arrow label aims-at) — none of it is covered.\n\nThe PR #55 review explicitly fixed the hideArrow / hideEntry sentinel wipe (9mm.170). hud-overlay got direct test coverage in the review-response commit (637bdf9) via resetArrowSentinels; poi-overlay's hideEntry equivalent is still untested.\n\nAcceptance — initial coverage:\n- poi-overlay.test.ts created.\n- syncPool: assert createEntry / destroyEntry happen on togglePoi state changes (mocked Stellata.getPois()).\n- hideEntry: assert the sentinel wipe (lastArrowD preserved, numeric sentinels → NaN, string sentinels → '\\0').\n- on-screen / off-screen branching: parametrise the projection so the helper functions can be tested without standing up the full overlay — likely needs a small refactor to extract the on-screen test condition (`projected !== null \u0026\u0026 projected[0] \u003e= ON_SCREEN_PULL_IN_PX \u0026\u0026 ...`) as a pure helper.\n\nCouples with F-4 (the emptyEntryDirtyState() factory introduction).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T11:08:54Z","created_by":"Alex Marshall","updated_at":"2026-05-16T11:08:54Z","labels":["observe","pr-55-followup","ui"],"dependencies":[{"issue_id":"stellata-9mm.188.4","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-16T12:08:54Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.188.4","depends_on_id":"stellata-9mm.188.2","type":"related","created_at":"2026-05-16T12:09:01Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.188.3","title":"poi-overlay vs hud-overlay shaft-length sibling-symmetry: POI keeps 8 px cutoff that HUD removed","description":"F-5 finding from the PR #55 review (637bdf9). Sibling-symmetry violation:\n\n- hud-overlay.ts deliberately removed `MIN_SHAFT_PIXEL_LENGTH = 8` (the cutoff that snapped Sol/GC arrows to invisible at \u003c8 px shaft length). The PR description and the inline comment block at hud-overlay.ts:29-34 explain why: the disc-coverage fade in navigate mode keys on the longest currently-drawn shaft, and a discontinuity at 8 px broke the fade's smooth alpha behaviour. The arrow now renders at any positive shaft length so the fade reads continuously.\n\n- poi-overlay.ts:36 still defines `const MIN_SHAFT_PIXEL_LENGTH = 8;` and uses it at line 349 to hide the POI chevron when the shaft would be shorter than 8 px.\n\nThis is `stellata-consistency-at-the-seam §2` (sibling symmetry): two arrow renderers sharing the same screen-direction cascade + shaft formula, one with a hard cutoff and one without. Question: is the POI cutoff still needed, or is it a leftover from before the fade existed? POI arrows render in OBSERVE mode where the disc-coverage fade is not active (camera is parked at the focal star, no nav-arrow fade alpha), so the cutoff isn't tied to a fade-continuity story. The only justification would be \"an 8 px chevron just looks bad\" — which would apply equally to the (now-rendering) sub-8-px Sol/GC chevrons.\n\nAcceptance:\n- Decide: remove the POI cutoff (match hud behaviour) OR add the cutoff back to hud (matching POI). My read: remove the POI cutoff is the correct call given the architectural direction. The Sol/GC arrows already render at any positive length and the disc-coverage fade story is the more demanding case.\n- Implementation: drop MIN_SHAFT_PIXEL_LENGTH from poi-overlay.ts; replace the gate at line 349 with `if (shaftLengthPx \u003c= 0)` mirroring hud-overlay.ts:388.\n- Smoke: pin a POI in OBSERVE just barely outside the viewport on a high-FOV camera. Should see the chevron continuously shorten as the POI enters the pull-in margin, not snap to invisible at 8 px.\n\nPre-existing — the asymmetry pre-dates PR #55, but the PR touched both files and the review surfaced it.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T11:08:46Z","created_by":"Alex Marshall","updated_at":"2026-05-16T11:08:46Z","labels":["observe","pr-55-followup","ui"],"dependencies":[{"issue_id":"stellata-9mm.188.3","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-16T12:08:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.188.2","title":"DRY hideEntry sentinel-reset against hideArrow's resetArrowSentinels shape","description":"F-4 finding from the PR #55 review (637bdf9). hud-overlay.ts::hideArrow and poi-overlay.ts::hideEntry both do the same \"wipe per-attribute sentinels back to poison-init\" operation on different state shapes. PR #55's review-response commit (637bdf9) extracted resetArrowSentinels(state: ArrowState) as a free function in hud-overlay.ts so the hud side is testable; poi-overlay.ts still inlines the wipe at line 173-187.\n\nPer stellata-consistency-at-the-seam §1 (lifecycle pairing) and the existing `Object.assign(this.solDebug, emptyArrowDebug())` precedent at hud-overlay.ts:444, the cleanest fix is:\n\n1. Define `emptyEntryDirtyState()` in poi-overlay.ts (returns the sentinels-only slice of the Entry interface).\n2. hideEntry becomes: gated `d=''` + `display='none'` writes (as today) then `Object.assign(e, emptyEntryDirtyState())`.\n3. Add a vitest for the wipe (mirrors the new hud-overlay resetArrowSentinels test) — pins the 9mm.170-equivalent regression at the POI call site.\n\nAcceptance:\n- poi-overlay's hideEntry visibly uses an emptyEntryDirtyState() factory.\n- One vitest pins the per-field wipe (lastArrowD preserved, lastRingCx/Cy → NaN, lastOnScreenLabelText → '\\0', etc.).\n- Existing tests still pass; build clean.\n\nConnects to T-2 (poi-overlay.test.ts doesn't exist yet — this bead creates it).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T11:08:32Z","created_by":"Alex Marshall","updated_at":"2026-05-16T11:08:32Z","labels":["pr-55-followup","ui"],"dependencies":[{"issue_id":"stellata-9mm.188.2","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-16T12:08:31Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.188.1","title":"Hoist ARROW_PIXEL_LENGTH + RING_HALO_GAP_PX out of hud/poi overlays","description":"F-3 finding from the PR #55 review (637bdf9). Two calibrated screen-pixel constants are duplicated verbatim between hud-overlay.ts and poi-overlay.ts:\n\n- `ARROW_PIXEL_LENGTH = 110` — hud-overlay.ts:18, poi-overlay.ts:34\n- `RING_HALO_GAP_PX = 4` — hud-overlay.ts:23, poi-overlay.ts:35\n\nBoth arrow renderers consume these to size the shaft + the ring-rim halo gap. Per stellata-named-constants-and-dry rule 1 (\"hoist numeric literals to named consts at first sight of a second usage\"), the duplication is review-blocking. The right home is either arrow-path.ts (the shared arrow util that already exports buildArrowSvgPath, ARROW_HEAD_DEPTH_PX, ARROW_LABEL_OFFSET_PX, ARROW_LABEL_PADDING_PX, screenDirToTarget) — so hud-overlay and poi-overlay re-import from that single canonical location.\n\nAcceptance:\n- arrow-path.ts exports ARROW_PIXEL_LENGTH + RING_HALO_GAP_PX.\n- hud-overlay.ts and poi-overlay.ts import from arrow-path.ts and drop their local consts.\n- Existing tests + typecheck + build all pass.\n\nSmoke: focus a star with Sol off-axis, confirm Sol/GC arrows still attach correctly to the focus ring rim (halo gap visually intact); pin a POI off-screen in OBSERVE, confirm the POI chevron still attaches to the HUD ring rim at the same gap.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-16T11:08:22Z","created_by":"Alex Marshall","updated_at":"2026-05-16T11:08:22Z","labels":["pr-55-followup","ui"],"dependencies":[{"issue_id":"stellata-9mm.188.1","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-16T12:08:22Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-1ui","title":"Expand model volume to 1.5–2 Mpc + Andromeda subgroup","description":"**Goal:** complete the Local Group rendering by expanding the model envelope from 250 kpc (stellata-38m's cap) out to the canonical Local Group boundary (~1.5–2 Mpc), and add wireframes + distance-gated labels for M31, M33, the M31 satellite subgroup, and the outer dwarfs that sit beyond 250 kpc but inside Local Group canonical membership.\n\nThis bead is **deferred** at creation. Depends on stellata-38m. Unblock when the 250 kpc layer is shipped and Alex wants the further-out context.\n\n# Why deferred\n\nPer [[stellata-prioritization-framework]], adding more layers (especially far-field) is tier 5. The 250 kpc cap in stellata-38m is the immediate \"give context for where the MW sits\" payoff; the M31 subgroup is a separate value-add that requires bigger camera + UI work. Splitting keeps the first PR's smoke surface manageable.\n\n# Scope\n\n- **Camera envelope bump:** `src/client/stellata.ts` `PerspectiveCamera` far plane → ~3 Mpc, `TrackballControls.maxDistance` → 1.5 Mpc (or 2 Mpc — decide at unblock time based on whether the IC 10 / WLM band beyond canonical Local Group is worth including).\n- **Data filter extension:** `scripts/build-local-group.ts` distance cutoff → new cap (1.5 or 2 Mpc).\n- **Override rows for the major new objects** in `data/local-group/overrides.tsv`:\n  - **M31 / Andromeda** (~780 kpc): inclined disc, i = 77°, PA = 35°, ~30 kpc disc diameter. Cite Conn et al. 2012 (distance) + relevant PAndAS structural papers (e.g. McConnachie et al. 2018) at impl time.\n  - **M33 / Triangulum** (~840 kpc): inclined disc, i = 54°, ~17 kpc disc. Cite the Cepheid distance reference (Science 308, 1248 / [10.1126/science.1108342]) at impl time.\n  - **NGC 205, M32** (M31 satellites, ~780 kpc): ellipsoids, semi-major ~2.7 / 1.6 kpc.\n  - **IC 10, NGC 6822, Leo I (if it landed outside 250 kpc), Leo A, IC 1613, WLM**: ellipsoids from LVDB rows.\n- **Labels:** per-object `label_threshold_pc`. M31's label probably wants ~200 kpc threshold so it only appears once the user can see the whole M31 disc as a coherent shape. M33 similar.\n- **Distance formatting:** consider extending `src/client/distance-util.ts` (`fmtDistAuto`) to format Mpc explicitly past some threshold. Today's Mpc-scale objects would render in light-years (3.26 Mly per Mpc), which works but is unusual UX-wise.\n- **Chart-mode coordination:** M31 specifically deserves chart-mode treatment via `stellata-m40.4` (distant DSO glyphs) once that bead unblocks. File a `relates-to` link.\n\n# Smoke\n\n- Zoom from first-load all the way out. MW disc + 250 kpc wireframes fade in as before. Past ~500 kpc, M31 + M33 outlines appear; labels appear at their respective thresholds.\n- Sky-position sanity: M31 in Andromeda (RA 00h 42m, Dec +41°); M33 in Triangulum nearby.\n\n# Out of scope\n\n- Anything outside Local Group canonical boundary (~2 Mpc). Sculptor Group, M83 group, etc. — separate decision.\n- Sgr stream + M31 stellar streams — invisible / stellar-scale, not a wireframe primitive.\n\n# Data sources\n\nIn addition to those already cited in stellata-38m:\n- M31 disc geometry: Conn et al. 2012 + McConnachie et al. 2018 (PAndAS). Look up canonical references at impl time.\n- M33 distance: Bonanos et al. 2006, *ApJ* 652, 313 (10.1086/508140) — 840 ± 11 kpc Cepheid distance.\n\n# Reference\n\nPlanning session that filed this bead: /Users/alexm/.claude/plans/let-s-start-plan-mode-nested-bentley.md","status":"closed","priority":3,"issue_type":"feature","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T10:03:16Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:22Z","started_at":"2026-05-16T13:28:19Z","closed_at":"2026-05-16T21:38:22Z","close_reason":"merged via PR #69 (Local Group expansion to 2 Mpc + M31/M33 + Mpc readout)","labels":["galactic","local-group","visual","wireframe"],"dependencies":[{"issue_id":"stellata-1ui","depends_on_id":"stellata-38m","type":"blocks","created_at":"2026-05-16T11:03:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-5gq","title":"Raise camera bounds to 250 kpc for Local Group rendering","description":"**Goal:** small preparatory PR that widens the model envelope so the Local Group wireframe layer (stellata-38m) has room to render. Status quo: camera far plane = 200 kpc, TrackballControls maxDistance = 100 kpc — far too cramped for LMC (50 kpc), SMC (62 kpc), Sgr dSph (26 kpc) to sit comfortably in frame at zoomed-out views.\n\n# Scope\n\n- `src/client/stellata.ts:662–667` — `PerspectiveCamera` far plane `200_000` → `500_000` (pc). Near stays at `1e-10`. Log-depth math is fine across this range; the absolute span matters more than the ratio and we're well inside the precision budget Stellata already relies on for sub-AU near work.\n- `src/client/stellata.ts:681` — `TrackballControls.maxDistance` `100_000` → `250_000`.\n- Sanity-check `src/client/scale-bar.ts` + `src/client/distance-util.ts` (`fmtDistAuto`) handle 200 kpc gracefully. Parsec / light-year formatting scales naturally; no Mpc band needed yet. No change expected — just verify.\n\n# Out of scope\n\n- Any rendering changes. This bead only widens the envelope; the rendering work is stellata-38m.\n- Anything Mpc-scale. The 1.5–2 Mpc expansion to encompass M31/M33/Andromeda subgroup is a separate deferred bead (the new Bead C, filed alongside this one).\n\n# Smoke (manual; per stellata-stellata-smoke-tests do not start a dev server)\n\n1. `npm run typecheck` + `npm test`.\n2. Zoom fully out from first-load. Galactic-disc outline visible at far zoom (already full opacity past 5 kpc from Sol per docs/galactic-overlay.md). Scale bar formats sensibly out to 250 kpc.\n3. Focus + warp to a near star. Confirm no precision regression in the close-up regime (near plane unchanged).\n\n# Versioning\n\nPatch bump per RELEASING.md — visible behaviour change in maxDistance even with no new rendering. PR has a Release notes block.\n\n# Blocks\n\nThis bead blocks stellata-38m (the Local Group wireframe layer needs the larger envelope).\n\n# Reference\n\nPlanning session (this bead's design context): /Users/alexm/.claude/plans/let-s-start-plan-mode-nested-bentley.md","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T10:01:28Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:11Z","started_at":"2026-05-16T10:17:04Z","closed_at":"2026-05-16T21:38:11Z","close_reason":"merged via PR #67 (Local Group wireframes + MW label) — camera bounds raised to 250 kpc for LG rendering","labels":["camera","local-group"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-2br.7","title":"Live debug panel for warp curve tuning","description":"Live tuning surface for the camera-arrival profile, exposed in the unified debug panel (debug.panel()).\n\n# Scope\n\nPer the stellata-2br handoff plan, after the 2br.5 fix lands and the baseline is smoke-validated, expose a debug section that lets Alex iterate live on:\n\n- Curve shape selector — cubic-Hermite (default), quintic smoothstep `6u⁵ − 15u⁴ + 10u³` (sharper decel), asymmetric `f(u^p)` with p \u003e 1 (decel later) or p \u003c 1 (decel earlier), identity-log (`u_eased = u`, sanity-floor case).\n- Per-site duration sliders — `FOCUS_LERP_MS`, `WARP_T_MIN_MS`, `WARP_T_MAX_MS`, `OBSERVE_TRANSITION_MS` (the unfocus path).\n- Live readouts — peak `dec/s` (`1.5·log(dEnd/d0)/durationMs` for cubic-Hermite), current `u` and `u_eased` for the in-flight arrival, `recenteredToDest` flag.\n\n# Why\n\nIf smoke surfaces a divergence between focus-park feel and warp-Fly feel (the two sites use the same helper but the perceptual context differs), iterating via debug HUD is faster than the bake-tune-deploy-test loop.\n\n# Implementation pattern\n\nFollows `src/client/star-tuning.ts` and `src/client/milkyway-tuning.ts` — a section builder that mounts into `debug.panel()`, registers slider/select widgets via the shared helpers, and wires them to setters on `camera-motion.ts` (or to module-level mutable refs the helper reads).\n\nThe `ease` function should become an optional param on `NewArrivalOpts` (defaulting to the current cubic-Hermite) so each call site picks its own — focus-park, warp Fly, and unfocus can diverge if desired.\n\n# Acceptance\n\n- Section visible in debug panel under `window.debug.panel()`.\n- Curve-shape selector changes the in-flight Fly profile within ~1 frame.\n- Per-site duration sliders are independent (focus-park vs warp Fly can have different durations).\n- Once dialled in, bake chosen defaults into the helper / call sites and either gate the HUD behind `window.debug` or remove it.\n\n# Out of scope\n\nPermanent UI exposure (this is a tuning aid). Cloud-arrival shape is already kind-agnostic via the helper; no per-kind toggle needed unless smoke surfaces a divergence.","status":"closed","priority":3,"issue_type":"feature","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-16T09:25:41Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:01:29Z","started_at":"2026-05-16T11:45:31Z","closed_at":"2026-05-16T21:01:29Z","close_reason":"shipped in PR #66 (commit 25e4b8b)","labels":["camera","debug-panel","ux","warp"],"dependencies":[{"issue_id":"stellata-2br.7","depends_on_id":"stellata-2br","type":"parent-child","created_at":"2026-05-16T10:25:40Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.187.3","title":"Add comment explaining the 'this.bus.emit' regex constraint in stellata-events.test.ts","description":"The structural integration test stellata-events.test.ts scans stellata.ts text for /this\\.bus\\.emit\\(\\s*'([a-zA-Z]+)'/g — the regex requires the literal \"this.bus.emit\" prefix. A future refactor that destructures the bus into a local variable (const bus = this.bus; bus.emit(...)) would pass tsc but make the regex silently miss every emit site, so the test would fail \"expected has emit site\" with no obvious cause.\n\n# What to add\n\nA one-paragraph comment above the regex explaining:\n\n- Why the literal \"this.bus.emit\" prefix is required (constrains the search to emit sites inside Stellata; avoids false positives from any other EventBus\u003c...\u003e.emit elsewhere).\n- That a refactor destructuring `bus` from `this` will break the test silently and the regex must be updated in lockstep.\n\n# Acceptance\n\nstellata-events.test.ts has an explanatory comment. The type-side coverage (emit sites are still typed against StellataEventMap via tsc) is mentioned so the reader understands the structural test's complementary role.\n\nWhy P3: test maintainability, not a correctness bug.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T12:52:23Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:52:23Z","dependencies":[{"issue_id":"stellata-9mm.187.3","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T13:52:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.187.2","title":"Wire HMR accept handler so Stellata.dispose() and bus.clear() actually run","description":"PR #51 (stellata-77m) added bus.clear() to Stellata.dispose() and pitches HMR cleanup as the motivating use case. But Stellata.dispose() is never called in production — main.ts has no import.meta.hot accept handler. The dispose path (and bus.clear() with it) is therefore theoretical defense, not currently exercised.\n\n# What to do\n\nAdd an import.meta.hot.accept block in main.ts that:\n1. Calls stellata.dispose() on the existing instance.\n2. Lets Vite re-import and re-bootstrap.\n\n# Acceptance\n\n- Editing src/client/stellata.ts (or any module that re-creates Stellata wiring) under `npm run dev` tears down the old Stellata cleanly and constructs a new one without page reload.\n- Manual verification: open dev tools, watch for \"previous instance disposed\" log line (or similar marker), confirm no listener accumulation across 5 HMR cycles.\n- Coordinate with stellata-9mm.44 (the deferred dispose smoke test): once HMR is wired, the smoke test can also assert clean re-creation.\n\nWhy P3: dev ergonomics, not user-visible. Useful but not blocking.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T12:52:16Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:52:16Z","dependencies":[{"issue_id":"stellata-9mm.187.2","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T13:52:16Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.187.1","title":"Document the bus-subscriber unsubscribe contract in docs/architecture.md","description":"PR #51 (stellata-77m) landed the typed EventBus migration. 15 of the 18 external subscriber files do NOT return an explicit unsubscribe — they rely on Stellata.dispose() calling bus.clear() for teardown. The other 3 (pin-debug-hud, arrow-fade-debug-hud, time-readout) DO return unsubscribers because they have their own mount/unmount lifecycle.\n\nThe contract is correct but undocumented anywhere. The next contributor adding a temporary subscriber will read whichever sibling they happen to look at first.\n\n# What to add\n\nOne paragraph to the event-bus section of docs/architecture.md spelling out the rule:\n\n  \"Subscribers mounted for the lifetime of Stellata may omit the unsub —\n  release happens via bus.clear() inside Stellata.dispose(). Subscribers\n  with their own mount/unmount lifecycle (debug HUDs, modal-scoped\n  overlays, anything that can be torn down without Stellata going away)\n  MUST capture the returned unsubscribe and call it during teardown.\"\n\n# Acceptance\n\ndocs/architecture.md event-bus section explicitly states which subscribers need explicit unsubs vs which don't, and why. No code change needed.\n\nWhy P3: pure documentation; not user-visible.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T12:52:09Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:52:09Z","dependencies":[{"issue_id":"stellata-9mm.187.1","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T13:52:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.192","title":"Stellata core refactors + catalog/picker tests","description":"Themed sub-epic of stellata-9mm collecting the residual core-refactor and catalog/picker test-gap items that don't fit the other themed epics.\n\nScope:\n- Subsystem extracts: StarPipeline class (disc/glow/coreMask resource ownership), DustParticleLayer subsystem.\n- Camera-busy refactor: migrate strict-equivalent sites to isCameraBusy(), audit + document the decision matrix.\n- Catalog/binary test gaps: build-catalog→loader round-trip, SearchEntry wire-format pin, readPipeDelimited unit coverage, HEADER_LAYOUT field-type encoding.\n- Picker/search: Typeahead\u003cT\u003e unit tests (in-progress, stellata-9mm.98), cloud picker asymmetry (deferred), log-depth precision regression test (deferred).\n- Legacy URL-state schema decoder retirement (deferred).\n\nWhy a dedicated epic: heterogeneous but all 'core internals' work — neither overlay nor planet nor event-bus. Grouping prevents them from looking like loose ends of the meta umbrella.\n\nPriority: P3 default — internal refactors; individual children can bump if they block in-flight P1/P2.","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-15T11:52:57Z","created_by":"Alex Marshall","updated_at":"2026-05-15T11:52:57Z","dependencies":[{"issue_id":"stellata-9mm.192","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-15T12:52:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.190","title":"Debug panel \u0026 perf-hud cleanup","description":"Themed sub-epic of stellata-9mm covering follow-ups from the debug-panel consolidation (stellata-n1t).\n\nScope: section builder API split (star/milkyway return HTMLDivElement, perf/pin return {element, dispose, setVisible}) needs unification, diagnostic-readout root styling + reset-link DRY across pin/arrow sections, perf-hud pure-helper extraction + tests (summarize, insertSorted, colourForAvg), debug-panel pure-helper extraction + tests (clampToViewport, rgbToHex, hexToRgb), row-colour dirty-check compares hex against serialized style.color (always mismatches), headline render walks DOM by index instead of captured spans.\n\nWhy a dedicated epic: ~7 internal-tooling findings that don't ship to users but slow down every future debug-panel session if left to drift. None individually justify a PR; bundled they're a focused cleanup pass.\n\nPriority: P3 — internal tooling, not user-visible.","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-15T11:52:45Z","created_by":"Alex Marshall","updated_at":"2026-05-15T11:52:45Z","dependencies":[{"issue_id":"stellata-9mm.190","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-15T12:52:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.186","title":"DRY: diagnostic-readout root styling + reset-link duplicated across pin/arrow sections","description":"Surfaced during PR #65 (stellata-2mi) self-review.\n\n# The duplication\n\n`pin-debug-hud.ts` and `arrow-fade-debug-hud.ts` now build their own root element + reset-link with near-verbatim styling and behaviour:\n\n- Root `style.cssText` — green-on-black ui-monospace readout box, `white-space:pre`, `overflow-x:auto`, `user-select:text`. Arrows adds a `border-left:3px solid #0f0` that the pin version lacks; otherwise identical.\n- `[click to reset latches]` link — same text, same CSS (`margin-top:6px;cursor:pointer;color:#999;user-select:none;`), same `Object.assign(latch, emptyLatch())` handler shape.\n\n# Why it matters\n\nPer `stellata-named-constants-and-dry` rule 2: two functions with mostly-identical structure parameterise their differences and share a builder. The duplication existed in pin first (filed before this issue is the right framing — arrows inherited the shape during the 2mi fold). With two live diagnostic sections now using this template, any third will compound the drift.\n\n# Suggested fix\n\nAdd a `buildDiagnosticReadoutSection(opts)` helper in `debug-panel.ts`:\n\n```ts\ninterface DiagnosticReadoutOpts {\n  borderColor?: string;        // default '#0f0'; arrows uses this for independent-state alert toggling\n  onResetLatches: () =\u003e void;\n}\ninterface DiagnosticReadout {\n  root: HTMLDivElement;\n  body: HTMLDivElement;\n  setBorderColor: (c: string) =\u003e void;  // for arrows' red/green alert\n}\n```\n\nThen both `buildPinSection` and `buildArrowSection` become thin builders that call into it and just own the latch object + per-frame `body.textContent` write + onFrame subscription.\n\n# Acceptance\n\n- `buildDiagnosticReadoutSection` exported from `debug-panel.ts` with the shape above.\n- Both `pin-debug-hud.ts` and `arrow-fade-debug-hud.ts` use it; no inline `style.cssText` for the green-readout chrome or the reset link remains in either file.\n- Arrows' independent-state border toggle keeps working (red on disagreement, green otherwise).\n- All existing vitest + typecheck stays green.\n\n# Smoke\n\n- `npm run typecheck \u0026\u0026 npm test`\n- Manual: open the debug panel, expand both Pin and Arrows sections, confirm both look unchanged and reset-latch link still works on each.\n\n# Files touched\n\n- `src/client/debug-panel.ts` (new helper)\n- `src/client/pin-debug-hud.ts` (consume helper, drop inline chrome)\n- `src/client/arrow-fade-debug-hud.ts` (same)","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T11:39:47Z","created_by":"Alex Marshall","updated_at":"2026-05-15T11:39:47Z","labels":["debug","dry","pr-65","ui"],"dependencies":[{"issue_id":"stellata-9mm.186","depends_on_id":"stellata-9mm.190","type":"parent-child","created_at":"2026-05-15T12:53:50Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.185","title":"Audit + apply sentinel-init poison across overlay visibility tracking","description":"# Context\n\nPR #64 (stellata-8ee) fixed the heliopause label's first-load visible-by-default bug by changing the visibility sentinel from `let visible = false` to `let visible: boolean | null = null`, so the initial `setVisible(false)` call writes through instead of early-returning on the `if (on === visible) return` guard. Same shape is documented under [[stellata-consistency-at-the-seam]] rule 3.\n\nA peer survey during PR #64 review showed **the same anti-pattern in 4-5 sibling overlays**. They are latent in production today only because their onFrame loops typically reach a write-through branch within a frame or two — masking the bug behind browser paint timing. The shape is identical to the heliopause case; a timing shift (or a new bail branch in onFrame) would surface them.\n\n# Peers carrying the same shape\n\n| File | Pattern | SVG container initial state |\n|---|---|---|\n| `src/client/planet-labels.ts:61` | `let groupVisible = false; setGroupVisible(false)` | `\u003cg id=\"planet-labels\"\u003e` no inline `display:none`; children appended at SVG (0,0) default before first onFrame writes positions |\n| `src/client/poi-overlay.ts:147` | `let groupsVisible = false` | three `\u003cg\u003e` (`poi-arrows`, `poi-rings`, `poi-labels`), no inline `display:none` |\n| `src/client/distance-vector-overlay.ts:37` | `let visible = false` | container no inline `display:none` |\n| `src/client/scale-bar.ts:155` | `let zVisible = false` | SVG sub-element |\n| `src/client/constellation-overlay.ts:17` | `let visible = true` | reverse-polarity sentinel — risk is an initial `setVisible(true)` call no-op'ing instead |\n\nDebug-HUD peers (`pin-debug-hud.ts:59`, `perf-hud.ts:55`) carry the same shape but their visibility lifecycle is user-toggled (no auto first-paint), so lower priority — include in the sweep but don't gate on them.\n\n# What to do\n\nFor each peer:\n1. Change the sentinel type to `boolean | null` and initial value to `null`.\n2. Verify the SVG container's effective initial state in `src/client/index.html` and confirm the first init call will land.\n3. For `constellation-overlay.ts` (reverse-polarity), invert the logic: `null` poison still works, but trace the first call's direction.\n4. Add a regression test per peer following the heliopause pattern (stub `document.getElementById`, assert synchronous `display === 'none'` write on init). Co-locate tests; consider a shared helper if more than two peers want the same scaffolding.\n\n# Smoke step\n\nHard-reload `/` with no `?v=` fragment in multiple states:\n- Sol focused (default first-load) — confirm no planet-label / poi / distance-vector / scale-bar-z indicator artefacts paint at SVG (0,0).\n- Switch to OBSERVE mode, then navigate, then back — overlay transitions clean.\n- Theme toggle (light↔dark, normal↔chart) — overlays don't flash.\n\n# Acceptance criteria\n\n- Each listed peer's first-paint contract is pinned by a vitest regression test.\n- Manual smoke confirms no overlay artefacts at SVG defaults on first-load across modes.\n- Grep `let .*[Vv]isible = (false|true)` across `src/client/*.ts` returns zero overlay-layer hits not covered by a poison-sentinel test.\n\n# Why one PR, not five\n\nThe fix is mechanically identical across peers and the regression-test scaffolding wants to be shared (or at minimum, copy-pasted with the same shape). One sweep PR documents the pattern coverage explicitly in the PR description (per [[stellata-pattern-coverage-across-peers]]) and prevents drift between sibling files. Bundles cleanly into one commit + one smoke step per the bead-authoring scope rule.\n\n# Related\n\n- [[stellata-consistency-at-the-seam]] (rule 3, sentinel-init for dirty-track)\n- [[stellata-pattern-coverage-across-peers]]\n- Originating bug: stellata-8ee / PR #64","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-15T11:12:33Z","created_by":"Alex Marshall","updated_at":"2026-05-15T11:12:33Z","labels":["phase-12a","ux"],"dependencies":[{"issue_id":"stellata-9mm.185","depends_on_id":"stellata-8ee","type":"related","created_at":"2026-05-15T12:12:37Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.185","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-ozc","title":"Investigate Gaia DR3 variability catalog as GCVS supplement","description":"Research bead. Output: written decision on whether to add Gaia DR3 variability tables to Stellata's variable-star pipeline (currently GCVS-only).\n\n# Current state\n\nStellata's variable-star data flows from GCVS (~85 k variables) via gcvs5.txt + crossid.txt → scripts/build-catalog.ts → binary catalog v4. GCVS has authoritative classifications, periods, and amplitudes for traditionally-known variables. Many are referenced by classical designations users recognise.\n\n# What Gaia DR3 adds\n\nGaia DR3 has its own variability catalog (~10 M classified variable sources) split across multiple tables:\n- `vari_summary` — top-level classification per source\n- `vari_classifier_result` — per-source classifier output with confidence\n- `vari_rrlyrae`, `vari_cepheid`, `vari_long_period_variable`, `vari_eclipsing_binary`, `vari_short_timescale`, etc. — per-class detailed tables with periods, amplitudes, mode classifications\n\nCompared to GCVS: ~100× more sources, more uniform sampling, recent photometric epochs, but lacks the historical depth and classical-designation cross-references GCVS provides.\n\n# Questions to answer\n\n1. **Coverage gap analysis.** For our 313 k AT-HYG subset, how many stars are GCVS-flagged variable, how many are Gaia-DR3-flagged variable, what's the overlap? Stars Gaia flagged but GCVS missed are the supplement candidates.\n2. **Quality at the seams.** For stars in both catalogs, do period / amplitude estimates agree? Disagreements need a tie-break rule.\n3. **Faint-end policy.** Most Gaia-flagged variables are below AT-HYG's catalog floor entirely. Out of scope for per-object rendering, but could feed a far-field variability statistic (see stellata-36y).\n4. **Render impact.** GCVS already drives Stellata's pulsation. Adding Gaia variables to the AT-HYG-visible set means more stars pulsate — a quality-of-data improvement, not a new feature.\n\n# Acceptance\n\n- Coverage stats: |GCVS ∩ AT-HYG|, |Gaia-var ∩ AT-HYG|, |Gaia-var ∩ AT-HYG \\ GCVS|.\n- Quality check on 20 sample stars in both catalogs.\n- Written recommendation: include / defer / skip. If include, file implementation bead (gcvs + gaia merge in scripts/build-catalog.ts).\n\n# Related\n\n- stellata-fe4 (variable pulsation modelling epic) — modelling-side concern, this bead is data-side. If Gaia DR3 surfaces high-amplitude Miras GCVS missed, they'd be subject to the fe4 R-vs-T amplitude decision.\n- stellata-zsr.1 (Apsis ingest) — same archive; coordinate fetch infrastructure.\n- stellata-36y (far-field epic) — sub-AT-HYG-floor variables could contribute statistically.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T15:32:45Z","created_by":"Alex Marshall","updated_at":"2026-05-12T15:32:45Z","labels":["catalog","ingest","research","variability"],"dependencies":[{"issue_id":"stellata-ozc","depends_on_id":"stellata-36y","type":"related","created_at":"2026-05-12T16:32:57Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-ozc","depends_on_id":"stellata-fe4","type":"related","created_at":"2026-05-12T16:32:55Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-ozc","depends_on_id":"stellata-zsr.1","type":"related","created_at":"2026-05-12T16:32:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-53p","title":"Investigate Gaia DR3 NSS (non-single stars) as binary-orbit source","description":"Research bead. Output: written decision on whether to add Gaia DR3 NSS tables to the multiples pipeline (stellata-dch).\n\n# Why this is separate from existing dch sources\n\nstellata-dch already plans an ingest of:\n- WDS (resolved visual pairs, separation + PA + mags)\n- ORB6 (visual-binary Keplerian orbit fits, ~3 k systems)\n- Gaia DR3 — but only for **parallax + PM matching of WDS pairs**, not for the dedicated NSS tables.\n\nThe Gaia DR3 NSS tables are a **distinct data product**: orbital solutions derived from Gaia astrometry, photometry, and radial velocities. Many NSS systems are NOT in ORB6 — especially astrometric-acceleration binaries and spectroscopic binaries that aren't visually resolvable.\n\n# Tables to evaluate (VizieR / Gaia archive)\n\n- `nss_two_body_orbit` — full Keplerian fits (~169 k systems): P, T_0, e, a, i, ω, Ω. Direct drop-in for our existing Kepler solver.\n- `nss_acceleration_astro` — astrometric-acceleration binaries (~339 k); partial info, not full orbits but useful for \"this star has an unseen companion\" flag.\n- `nss_non_linear_spectro` — spectroscopic-binary orbits from RVS (~33 k).\n- `nss_two_body_orbit_combined` — combined astrometric+spectroscopic (~6 k high-quality systems).\n\n# Questions to answer\n\n1. **Coverage overlap with WDS/ORB6 already planned.** How many NSS systems would be new vs duplicate? Useful metric: count NSS-only systems with Gaia source_id matching an AT-HYG row.\n2. **Orbital element quality.** NSS uncertainties vs ORB6 quality flags — does NSS supplement or supersede ORB6 for the bright stars we care about?\n3. **Render impact.** Stellata's binary visualisation today uses inferred or WDS-derived geometry. NSS gives **physical Keplerian orbits** for thousands of systems we currently approximate. How visible is the difference in scene?\n4. **Pipeline shape.** If included, NSS becomes a fifth source in `scripts/build-binaries.py`. Frozen-external-data: one-shot fetch, committed under data/. File size budget?\n\n# Acceptance\n\n- Coverage statistics filled in.\n- Quality comparison: NSS vs ORB6 on a sample of 20 well-known multiples (α Cen, Sirius, Procyon, Capella, etc.).\n- Written recommendation: include / defer / skip. If include, file an implementation bead as a child of stellata-dch.\n\n# Related\n\n- stellata-dch (multiples pipeline epic) — natural integration point.\n- stellata-zsr.1 — separate Apsis ingest; if both happen, coordinate scripts/refresh-* helpers to share fetch infrastructure.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T15:32:24Z","created_by":"Alex Marshall","updated_at":"2026-05-12T15:32:24Z","labels":["multiples","multiples-pipeline","physics","research"],"dependencies":[{"issue_id":"stellata-53p","depends_on_id":"stellata-dch","type":"related","created_at":"2026-05-12T16:32:53Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-53p","depends_on_id":"stellata-zsr.1","type":"related","created_at":"2026-05-12T16:32:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3f8","title":"Refresh AT-HYG (or replace) when Gaia DR4 releases","description":"Refresh Stellata's per-star catalog against Gaia DR4 once it's released.\n\n# Expected release\n\nGaia DR4 was scheduled by ESA for late 2026 (Q4 2026 target as of my last-known schedule). Verify against https://www.cosmos.esa.int/web/gaia/release before un-deferring — the schedule has slipped multiple times historically and the exact date in this bead is a placeholder.\n\n# Scope\n\n- Wait for an AT-HYG release that incorporates DR4 (David Nash's HYG project upstream typically tracks Gaia releases with a lag of a few months), OR\n- Decide whether to re-derive Stellata's per-star catalog directly from gaia_source instead of via AT-HYG (now that we understand the trade-offs — AT-HYG is mag-limited to classic-IDed stars, leaving Gaia's depth on the floor).\n\n# What DR4 brings vs DR3\n\nPer ESA: full astrometry for all sources (vs DR3's subset), BP/RP and RVS spectra for hundreds of millions more sources, refined astrophysical parameters, longer baseline → tighter parallaxes and proper motions, expanded NSS (non-single stars) tables.\n\n# Why deferred, not closed\n\nExternal-data dependency. Bd-defer keeps it out of 'bd ready' until the data exists. Reopen and re-prioritise when DR4 lands.\n\n# Related\n\n- stellata-36y (far-field HiPS layer) benefits from a DR4-based HiPS map but doesn't strictly depend on it.\n- Any in-flight per-star pipeline changes (catalog format v5, etc.) should be DR4-aware so the refresh is a data swap, not a schema rewrite.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T15:22:18Z","created_by":"Alex Marshall","updated_at":"2026-05-12T15:22:23Z","defer_until":"2026-12-01T00:00:00Z","labels":["catalog","ingest"],"dependencies":[{"issue_id":"stellata-3f8","depends_on_id":"stellata-36y","type":"related","created_at":"2026-05-12T16:22:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-82s","title":"FAQ: how Stellata distinguishes physical binaries from optical line-of-sight pairs","description":"Write a public-facing FAQ entry (sibling of stellata-04y / stellata-e64) walking through the optical-vs-physical binary filter in the multiples pipeline. The maths × multi-source-observational story is genuinely interesting and worth a piece of the public docs — currently the rationale only lives in the parent epic stellata-dch's design block and in build-binaries.py comments.\n\n# Why this is worth documenting\n\nMost \"double stars\" in classical catalogues (WDS) are *optical* — two stars that happen to land in nearly the same line of sight from Earth but sit at very different distances and are unrelated. Filtering those out (so the renderer only animates orbits for genuinely bound systems) is a non-trivial cross-match across three independent observational datasets. The reader gets:\n\n- A nice example of why the same visual phenomenon (two stars at similar sky coords) can have wildly different physical interpretations.\n- A worked example: Betelgeuse's WDS system 05552+0724 lists components A–J. The pipeline injects bound ones (Aa, Ab, Ac, Ad — interferometric companions all at xyz ≈ (3.19, 151.36, 19.68) pc, within 1 AU of Betelgeuse) AND the wide visual ones (D, F, G, H, I, J at similar tangent-plane positions but with sky offsets up to ~4'). Meanwhile unrelated AT-HYG stars in the same sky direction (e.g. HIP 27919 at 223 pc, 0.67° from Betel; HIP 26992 at 309 pc, 2.9° away) crowd the viewport when looking at Betel from Sol — they look \"near\" Betel on screen but sit ~70 pc / ~157 pc behind it in 3D.\n\n# What the FAQ should cover\n\n1. **Data sources** (frozen externals committed under data/):\n   - `wds_summ.txt` — Washington Double Star Catalog: ~150k visually-observed pairs with separation ρ, position angle θ, magnitudes. Catalog is observational; it does NOT distinguish optical from physical.\n   - `orb6_orbits.txt` — Sixth Catalog of Orbits of Visual Binary Stars: ~3k systems where an orbital fit (P, T, e, a, i, ω, Ω) has been published, i.e. confirmed bound.\n   - Gaia DR3 (queried via ADQL, results cached in data/) — parallax + proper motion for both components when both have Gaia solutions. The two independent astrometric measurements per star let us test bound-ness empirically.\n\n2. **The filter chain** (scripts/build-binaries.py):\n   - **WDS notes flag** — when wds_notes.txt explicitly says \"physical\" or \"optical\", trust it.\n   - **Gaia parallax 3σ check** — if both components have Gaia parallaxes and the values differ by \u003e3σ, the two stars are at incompatibly different distances → optical pair, drop.\n   - **Gaia common proper motion** — physically bound stars share Galactic velocity; if PM vectors disagree by more than ~5 mas/yr in either RA or Dec, → optical pair, drop.\n   - **Spectral / magnitude sanity fallback** — when Gaia data is missing for one component, fall back to comparing spectral type + apparent magnitude against the WDS-quoted separation to flag implausible pairings.\n\n3. **The three rendering regimes** (per surviving pair):\n   - **Regime 1 — visually resolved** (default): tangent-plane projection of WDS ρ + θ at the most recent epoch → xyz offset of B from A. No orbital animation; positions are a snapshot.\n   - **Regime 2 — interferometric with ORB6 orbit**: solve Kepler at J2000 using the published elements → project to (ρ, θ) → xyz offset. Live orbital evolution per frame (e.g. α Cen's 79.9-yr period, Algol's 2.87-day eclipse cycle).\n   - **Regime 3 — spectroscopic-only**: a·sin i for separation magnitude, conventional PA=0 (documented limitation). Rare; only emerges when WDS can't resolve and ORB6 can't fit.\n\n4. **Why some \"Betel-X\" labels still get visually crowded by unrelated stars**: even with the filter, the rendered scene from Sol projects stars at different distances onto similar sky directions. The pipeline correctly identifies these AS unrelated (no FLAG_BINARY_PRIMARY links them); the visual crowding is a perspective artefact, not a data error.\n\n# Smoke / acceptance\n\n- New section in the FAQ destination (likely a top-level `docs/faq-multiple-stars.md` page or a sub-section of the eventual public site under stellata-e64's umbrella).\n- Worked Betelgeuse example with the actual numbers (the parent epic and build-binaries.py have them — re-quote rather than re-derive).\n- Cross-references stellata-dch (implementation) and the relevant sections of SCIENCE.md (data-source citations) without duplicating their content.\n\n# Related\n\n- stellata-dch (multiple-star matching epic — the implementation this FAQ documents)\n- stellata-04y (sibling FAQ: catalogue provenance for HIP/HD/HR/Bayer/Flamsteed/Gl/TYC)\n- stellata-e64 (sibling FAQ: rendering-decisions page about size/brightness/perception)","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T12:57:38Z","created_by":"Alex Marshall","updated_at":"2026-05-12T12:57:38Z","labels":["docs","faq"],"dependencies":[{"issue_id":"stellata-82s","depends_on_id":"stellata-04y","type":"related","created_at":"2026-05-12T13:57:47Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-82s","depends_on_id":"stellata-dch","type":"related","created_at":"2026-05-12T13:57:47Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-82s","depends_on_id":"stellata-e64","type":"related","created_at":"2026-05-12T13:57:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-8hu.2","title":"Render multi-star system orbits / connections as representational layer","description":"# Goal\n\nUnder the Physical/Standard/Advanced framework (parent epic\n`stellata-8hu`), add a representational layer that visualises the\n*relationships* in multi-star systems — so a user can see at a glance\nthat Sirius A and Sirius B are bound, even when both are tiny discs\nat the camera's current scale.\n\nTwo natural sub-modes (decide at design time):\n\n1. **Wireframe binary connector.** Simple 2-vertex line between\n   mutually-paired primary↔companion (when `FLAG_BINARY_PRIMARY` is\n   set on one side and `companionIdx` resolves). Cheap; works for all\n   ~14,836 mutual pairs in the catalog.\n2. **ORB6 elliptical orbit projection.** Requires the `binary-orbit-pure.ts`\n   Kepler infrastructure landing in `stellata-dch.9` and the v5\n   orbital-elements table from `stellata-dch.8`. Draws the actual\n   2-body ellipse projected onto the camera plane, anchored on the\n   primary, with the companion sitting at its current J2000+t phase.\n\nLikely v1 = wireframe-only. v2 = pick up orbit ellipses for regime-2\nentries once dch.8 + dch.9 are merged.\n\n# Scope\n\nv1 scope (this bead):\n\n- New module e.g. `src/client/binary-link-layer.ts`. Mirrors\n  `orbit-rings-layer.ts` architecturally — instanced line geometry,\n  toggled by an Advanced-mode switch in the panel.\n- Toggle defaults: off in Pure Visual; on in Standard; user-set in\n  Advanced.\n- Render-order placement per `docs/rendering.md` — line goes UNDER\n  star discs but OVER constellation lines (line is informational\n  about the foreground stars; constellations are background).\n- Style: thin dashed line in the focused star's complementary colour,\n  fade by camera distance like the existing focus ring.\n\n# Smoke\n\n- Default Standard mode: open α Cen, see the dashed line between\n  A and B at the AT-HYG-resolved positions.\n- Sirius: line between A and B; B is a faint distant disc — line\n  helps locate it.\n- Toggle off in Advanced mode — line disappears, stars stay.\n- Pure Visual: line never appears regardless of toggle state.\n\n# Dependencies\n\n- Decision on whether to ship the wireframe v1 standalone or wait\n  for dch.8 + dch.9 to land first and ship orbit-ellipse v2 directly.\n- Coupling with the chart-mode wings glyph (`chart-labels.ts`) —\n  glyph + line should not overlap visually; the glyph is the chart-\n  mode answer to the same question.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-12T10:50:27Z","created_by":"Alex Marshall","updated_at":"2026-05-12T10:50:27Z","labels":["multiples","observe","representational"],"dependencies":[{"issue_id":"stellata-8hu.2","depends_on_id":"stellata-8hu","type":"parent-child","created_at":"2026-05-12T11:50:27Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-dch.11","title":"Investigate AT-HYG-native sub-threshold duplicate at (-8.7310, 1.6110, 5.4470)","description":"# Symptom\n\nClean `npm run build:catalog` after `stellata-dch.6` lands logs exactly one\n`sub-threshold mutual pair below MIN_RENDER_SEPARATION_PC` warning, with\nboth stars HIP-less and at literally identical coordinates:\n\n```\nsub-threshold mutual pair below MIN_RENDER_SEPARATION_PC (0.00e+0 pc):\n  dropping fainter (no HIP, xyz=-8.7310,1.6110,5.4470 absmag=4.71)\n  (kept (no HIP, xyz=-8.7310,1.6110,5.4470 absmag=4.24))\n```\n\n# Why this is worth chasing\n\nThe safety-net in `inferBinaries` is handling it correctly (drops fainter,\nkeeps brighter), but the existence of two AT-HYG rows with zero positional\nseparation and no HIP suggests either:\n\n1. A literal duplicate row in `athyg_33_classic_ids.csv` (data hygiene\n   bug we should ticket upstream / patch around).\n2. A genuine binary that AT-HYG modelled by giving both components the\n   primary's astrometric solution without offset (the same pathology α\n   Cen has, but without the HIPs needed to participate in the multiples\n   pipeline).\n3. A non-Hipparcos data source merged into AT-HYG that fed two rows\n   sharing a Gaia/Tycho-2 solution.\n\nIdentifying which lets us decide whether (a) to file an AT-HYG upstream\nissue, (b) extend `applyMultipleOverridesPure` to cover HIP-less famous\ncompanions via name match, or (c) leave the safety net as the\npermanent fix.\n\n# Scope\n\nRead-only investigation. No code changes expected unless the conclusion\nis \"we should bake in an override\".\n\n# Procedure\n\n1. `awk -F, 'NR\u003e1' data/athyg_33_classic_ids.csv | awk '{ if (sqrt((-8.7310-x0)^2 + (1.6110-y0)^2 + (5.4470-z0)^2) \u003c 0.001) print }'` (adjust column names — `x0,y0,z0` in AT-HYG schema).\n2. Cross-check `proper`, `hip`, `hd`, `hr`, `flam`, `bayer` for both\n   matches. Famous name? GJ designation? Unnamed?\n3. Look up the candidate in SIMBAD / WDS by RA/Dec or HD.\n4. Conclude: is it the same star duplicated, or a real-but-collapsed pair?\n\n# Acceptance\n\nNotes captured on the bead naming the AT-HYG rows + SIMBAD identification\n+ conclusion. If conclusion is \"should be in `multiples-overrides.tsv`\",\nfile the override addition as a separate one-line bead.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T10:50:02Z","created_by":"Alex Marshall","updated_at":"2026-05-12T10:50:02Z","labels":["ingest","investigation"],"dependencies":[{"issue_id":"stellata-dch.11","depends_on_id":"stellata-dch","type":"parent-child","created_at":"2026-05-12T11:50:01Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-bal.9","title":"Verify AI + search crawler access through Cloudflare Workers","description":"src/worker.ts is a thin passthrough to env.ASSETS — no special routing, no security middleware. In principle every crawler should be able to fetch any path. Verify in practice, because Cloudflare's account-level bot management and zone-level WAF can block bots without the Worker code knowing.\n\n# Tasks\n\n1. Test asset paths from a clean network (or via curl --resolve / VPN) with these User-Agents:\n   - Googlebot/2.1\n   - Bingbot/2.0\n   - DuckDuckBot/1.1\n   - ClaudeBot/1.0\n   - GPTBot/1.0\n   - ChatGPT-User/1.0\n   - PerplexityBot/1.0\n   - Google-Extended (Google's AI training UA)\n2. For each, confirm 200 + correct content-type for:\n   - /\n   - /robots.txt\n   - /llms.txt\n   - /sitemap.xml\n   - /og-image.png\n3. Check Cloudflare dashboard:\n   - Security → Bots → 'Verified bots' should be allowed (default)\n   - WAF managed rules → no AI-bot block rules active (Cloudflare offers an opt-in 'block AI bots' toggle — must be OFF)\n   - Page rules / cache rules don't strip user-agent or return 403/451 for bot UAs\n4. If any block surfaces, file follow-up bead.\n\n# Dependencies\n\nBest done after the static assets (robots.txt, llms.txt, sitemap.xml, og-image.png) ship; otherwise there's nothing to verify access to.\n\n# Acceptance\n\n- All test UAs return 200 for all five paths\n- No surprise blocks in Cloudflare console\n- Findings documented in bd remember note (or attached to this bead's notes) for future re-verification post-deploy-changes\n\n# Smoke\n\nRun a small bash script that loops UAs × paths and asserts 200 + reasonable content-type. Capture output in PR description.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:51:30Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:51:30Z","labels":["seo"],"dependencies":[{"issue_id":"stellata-bal.9","depends_on_id":"stellata-bal","type":"parent-child","created_at":"2026-05-12T10:51:29Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bal.9","depends_on_id":"stellata-bal.1","type":"blocks","created_at":"2026-05-12T10:51:40Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bal.9","depends_on_id":"stellata-bal.4","type":"blocks","created_at":"2026-05-12T10:51:41Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bal.9","depends_on_id":"stellata-bal.7","type":"blocks","created_at":"2026-05-12T10:51:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-bal.8","title":"GEO content audit + noscript citability for AI search","description":"Generative Engine Optimization (GEO) is the practice of making content citable by AI search surfaces (Google AI Overviews, ChatGPT search, Perplexity, Claude, You.com). Differs from classic SEO in that the win condition is 'the AI cites your URL when answering related questions', not 'the page ranks #1'.\n\nStellata's / is a near-empty HTML shell that bootstraps a WebGL app. AI crawlers that don't execute JS see nothing substantive — no description of what Stellata is, no answerable passages, no citation hooks. This bead adds a crawler-friendly content layer and audits AI-search reachability.\n\n# Tasks\n\n1. Add a \u003cnoscript\u003e block inside \u003cbody\u003e with a self-contained paragraph describing what Stellata is, what data it visualises (AT-HYG ~313k stars, GCVS variables, Edenhofer dust), what it requires (WebGL2), and a link to the (eventual) /about page or github source. Crawlers that don't run JS render this; users with JS never see it.\n2. Cross-check passages with the llms.txt content so the AI agent gets coherent context whether it fetches /, /llms.txt, or both.\n3. Audit citability — passages should be answer-shaped (declarative, accurate, source-attributed). Reference: https://www.anthropic.com/engineering/multi-agent-research-system (style of citable passages).\n4. Test AI-crawler view of /:\n   - curl --user-agent 'ClaudeBot/1.0' https://stellata.xyz/ → confirm the response contains noscript content\n   - Repeat for GPTBot, PerplexityBot, Googlebot\n5. Test citation behaviour:\n   - Ask ChatGPT search 'what is stellata.xyz?' → confirm accurate answer with citation\n   - Ask Perplexity 'browser-based 3D star catalog viewer' → confirm stellata.xyz appears\n6. Document baseline citation rate in a follow-up note (e.g. bd remember) for future regression-check.\n\n# Dependencies\n\nSurvey-style; ideally lands AFTER robots.txt, llms.txt, and meta-tags beads so the audit reflects the as-shipped state.\n\n# Acceptance\n\n- \u003cnoscript\u003e block renders accurate, self-contained description\n- crawl test with ClaudeBot UA returns the noscript content in the HTML response\n- ChatGPT search returns a coherent answer when asked about stellata.xyz (manual check)","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:51:23Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:51:23Z","labels":["seo"],"dependencies":[{"issue_id":"stellata-bal.8","depends_on_id":"stellata-bal","type":"parent-child","created_at":"2026-05-12T10:51:23Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bal.8","depends_on_id":"stellata-bal.1","type":"blocks","created_at":"2026-05-12T10:51:38Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bal.8","depends_on_id":"stellata-bal.2","type":"blocks","created_at":"2026-05-12T10:51:39Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bal.8","depends_on_id":"stellata-bal.4","type":"blocks","created_at":"2026-05-12T10:51:40Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-bal.7","title":"Add public/sitemap.xml (minimal, root-only)","description":"A sitemap is a small signal but a clean one — robots.txt points to it, search engines and AI crawlers prefer crawling sites with one, and Google Search Console / Bing Webmaster Tools want one for submission.\n\nThis bead ships a minimal sitemap.xml covering only / for now. The deferred stellata-8c1.1 build pipeline will generate a richer multi-page sitemap once /about and /docs/* exist; this bead's output is the placeholder until then.\n\n# Tasks\n\n- Add public/sitemap.xml with one \u003curl\u003e entry:\n  - \u003cloc\u003ehttps://stellata.xyz/\u003c/loc\u003e\n  - \u003clastmod\u003e{ISO-8601 date of last meaningful content change}\u003c/lastmod\u003e\n  - \u003cchangefreq\u003emonthly\u003c/changefreq\u003e\n  - \u003cpriority\u003e1.0\u003c/priority\u003e\n- Reference from robots.txt (Sitemap: https://stellata.xyz/sitemap.xml) — handled by the robots.txt bead\n- Decide on hand-maintained vs. build-time generated. Default: hand-maintained one-line sitemap is fine until 8c1.1 lands. If a build script is preferred, scripts/build-sitemap.ts can read package.json and emit the file — but adds CI complexity for one entry.\n\n# Acceptance\n\n- curl https://stellata.xyz/sitemap.xml returns 200, content-type: application/xml (or text/xml)\n- https://www.xml-sitemaps.com/validate-xml-sitemap.html reports 0 errors\n- Cross-reference from robots.txt resolves\n\n# Supersession\n\nMark as superseded (bd supersede) when stellata-8c1.1 lands its multi-entry sitemap generator.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:51:11Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:51:11Z","labels":["seo"],"dependencies":[{"issue_id":"stellata-bal.7","depends_on_id":"stellata-bal","type":"parent-child","created_at":"2026-05-12T10:51:10Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-bal.6","title":"Generate favicon, apple-touch-icon, web manifest","description":"Stellata has no favicon — the browser tab renders a blank generic icon. Add a full icon set covering:\n- Desktop browser tabs (favicon.ico + favicon-16/32.png)\n- iOS home-screen save (apple-touch-icon.png, 180×180)\n- Android home-screen save + PWA manifest hint (192/512 PNGs + manifest)\n- Theme colour for browser chrome and PWA splash\n\n# Tasks\n\n- Design a single 512×512 source icon (could be a simple star glyph or the Stellata 'S' on a dark backdrop; aesthetics should match the app theme — dark sky, star-disc treatment)\n- Generate the standard set (https://realfavicongenerator.net/ does this from one source):\n  - public/favicon.ico (multi-resolution; 16, 32, 48)\n  - public/favicon-16.png, public/favicon-32.png\n  - public/apple-touch-icon.png (180×180)\n  - public/icon-192.png, public/icon-512.png (PWA)\n- Add public/manifest.webmanifest with name, short_name='Stellata', icons array, theme_color='#000000', background_color='#000000', display='standalone' (or 'browser' if PWA install is undesired), start_url='/'\n- Wire \u003clink\u003e tags in src/client/index.html \u003chead\u003e:\n  - \u003clink rel='icon' href='/favicon.ico'\u003e\n  - \u003clink rel='icon' type='image/png' sizes='32x32' href='/favicon-32.png'\u003e\n  - \u003clink rel='icon' type='image/png' sizes='16x16' href='/favicon-16.png'\u003e\n  - \u003clink rel='apple-touch-icon' sizes='180x180' href='/apple-touch-icon.png'\u003e\n  - \u003clink rel='manifest' href='/manifest.webmanifest'\u003e\n  - \u003cmeta name='theme-color' content='#000000'\u003e (may overlap with meta-tags bead — confirm only one such meta is present)\n\n# Acceptance\n\n- favicon renders in Chrome / Safari / Firefox tab\n- iOS Safari 'Add to Home Screen' shows the apple-touch-icon\n- https://realfavicongenerator.net/favicon_checker shows green for all platforms\n- manifest.webmanifest validates at https://manifest-validator.appspot.com/\n\n# Smoke\n\nOpen https://stellata.xyz/ in a clean browser profile and confirm tab favicon. On iPhone Safari, 'Add to Home Screen' and confirm the icon and name.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:51:03Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:51:03Z","labels":["seo"],"dependencies":[{"issue_id":"stellata-bal.6","depends_on_id":"stellata-bal","type":"parent-child","created_at":"2026-05-12T10:51:03Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-bal.5","title":"Add Schema.org JSON-LD structured data","description":"Structured data (JSON-LD) lets search engines and AI search surfaces understand what Stellata is — software, free-to-use, in-browser, by Alex Marshall, AGPL — and surface richer snippets / knowledge-graph entries / AI citations.\n\n# Tasks\n\nAdd a single \u003cscript type='application/ld+json'\u003e block to src/client/index.html \u003chead\u003e. Use Schema.org WebApplication (correct type for a browser-based app — vs. SoftwareApplication which implies install). Fields:\n\n- @context: https://schema.org\n- @type: WebApplication\n- name: Stellata\n- alternateName: Stellata Star Catalog\n- url: https://stellata.xyz/\n- image: https://stellata.xyz/og-image.png\n- description: (same one-line pitch as meta description)\n- applicationCategory: EducationalApplication (or VisualisationApplication if Schema.org adds it)\n- operatingSystem: Any (WebGL-capable browser)\n- browserRequirements: Requires WebGL2\n- author: { @type: Person, name: 'Alex Marshall' }\n- license: https://www.gnu.org/licenses/agpl-3.0.html\n- offers: { @type: Offer, price: '0', priceCurrency: 'USD' }  // free-to-use signal\n- isAccessibleForFree: true\n- inLanguage: en\n\n(Optional second @type via @graph: CreativeWork referencing the AT-HYG and GCVS catalogues as sources.)\n\n# Acceptance\n\n- https://search.google.com/test/rich-results reports 0 errors, valid WebApplication detected\n- https://validator.schema.org/ reports 0 errors\n- View-source shows one JSON-LD block, well-indented for readability\n- License URL resolves (link-check)\n\n# Smoke\n\nRun https://search.google.com/test/rich-results against https://stellata.xyz/ — should detect WebApplication with name, image, author.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:50:55Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:50:55Z","labels":["seo"],"dependencies":[{"issue_id":"stellata-bal.5","depends_on_id":"stellata-bal","type":"parent-child","created_at":"2026-05-12T10:50:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-bal.4","title":"Add SEO meta tags + OpenGraph + Twitter Card to src/client/index.html","description":"src/client/index.html currently has only charset, viewport, and \u003ctitle\u003e. Add the SEO/social meta-tag set to make the page indexable, shareable, and crawlable.\n\n# Tasks (all in src/client/index.html \u003chead\u003e)\n\nAdd the following:\n- \u003cmeta name='description' content='...'\u003e — one-sentence pitch (≤ 155 chars). Draft: 'Stellata is a browser-based 3D star catalog viewer rendering ~313k real stars from the AT-HYG catalog with GCVS variables, galactic dust, and constellation lines.'\n- \u003clink rel='canonical' href='https://stellata.xyz/'\u003e\n- \u003cmeta name='robots' content='index,follow,max-image-preview:large'\u003e\n- \u003cmeta name='theme-color' content='#000000'\u003e (match dark theme; consider media-query variant for light theme)\n- OpenGraph (Facebook, Slack, LinkedIn, iMessage):\n  - og:title, og:description, og:type=website, og:url, og:image (=/og-image.png, absolute URL), og:image:width=1200, og:image:height=630, og:image:alt, og:site_name=Stellata, og:locale=en_US\n- Twitter Card (X / Twitter, also honoured by some others):\n  - twitter:card=summary_large_image, twitter:title, twitter:description, twitter:image (absolute URL), twitter:image:alt\n- (Optional) \u003cmeta name='author' content='Alex Marshall'\u003e\n\n# Dependencies\n\nBlocked by: og-image bead (the asset path /og-image.png must exist; otherwise the card breaks)\n\n# Acceptance\n\n- View-source on / shows all meta tags above\n- Lighthouse SEO score ≥ 95 (was below 90 before)\n- https://www.opengraph.xyz/url/https%3A%2F%2Fstellata.xyz%2F renders a valid card with title/description/image\n- https://cards-dev.twitter.com/validator renders a valid summary_large_image card\n- Meta description ≤ 155 chars, title ≤ 60 chars (Google SERP truncation)\n\n# Smoke\n\nShare https://stellata.xyz/ in Slack — the unfurl should show title, description, and the OG image.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:50:47Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:50:47Z","labels":["seo"],"dependencies":[{"issue_id":"stellata-bal.4","depends_on_id":"stellata-bal","type":"parent-child","created_at":"2026-05-12T10:50:46Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bal.4","depends_on_id":"stellata-bal.3","type":"blocks","created_at":"2026-05-12T10:51:37Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-bal.3","title":"Create 1200×630 OpenGraph / Twitter Card image asset","description":"Stellata currently has no OG image — share links on Slack, iMessage, Twitter, LinkedIn, Discord all render as text-only previews. Add a 1200×630 PNG (the canonical OG aspect ratio; also satisfies Twitter Card 'summary_large_image') so shared URLs render as visual cards.\n\n# Tasks\n\n- Generate public/og-image.png (1200×630, sRGB, PNG-8 or PNG-24, target \u003c300 KB)\n  - Option A: capture a curated frame from the running app (focused on a recognisable region — e.g. Orion arm, galactic centre direction) and composite the Stellata wordmark on top\n  - Option B: design a graphic in Figma/Affinity that evokes the app's visual language without being a raw screenshot\n  - Default to Option A — the actual render is the headline\n- Optional: public/og-image-square.png (1200×1200) for platforms that prefer square (LinkedIn sometimes; iMessage on iOS uses square in some contexts)\n- The meta-tag wire-up (og:image, twitter:image) lives in the companion 'Meta tags on /' bead — this bead just ships the asset\n\n# Acceptance\n\n- File committed under public/\n- Image visibly renders the Stellata aesthetic (dark sky + stars + wordmark)\n- File size \u003c 300 KB\n- Image passes through Cloudflare Workers ASSETS at https://stellata.xyz/og-image.png with content-type: image/png\n\n# Smoke\n\n- After meta tags wire-up bead lands, run https://www.opengraph.xyz/ or https://cards-dev.twitter.com/validator against https://stellata.xyz/ and confirm the card renders","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:50:39Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:50:39Z","labels":["seo"],"dependencies":[{"issue_id":"stellata-bal.3","depends_on_id":"stellata-bal","type":"parent-child","created_at":"2026-05-12T10:50:38Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-bal.2","title":"Add public/llms.txt for AI agent context","description":"llms.txt is an emerging convention (proposed by Answer.ai, adopted by Cloudflare and others) for giving LLM-powered agents a concise, well-structured summary of a site so they can answer questions or cite it accurately. Unlike robots.txt (which gates crawling), llms.txt is a content hint.\n\n# Spec reference\n\nhttps://llmstxt.org — markdown file at /llms.txt with:\n- H1 site name\n- Blockquote summary\n- Optional sections grouping canonical URLs by purpose\n\n# Tasks\n\n- Add public/llms.txt with:\n  - # Stellata\n  - \u003e one-paragraph description (WebGL star catalog, AT-HYG 313k stars, GCVS variables, browser-based, ecliptic / galactic / extragalactic layers)\n  - ## About — link to https://stellata.xyz/ and the upcoming /about\n  - ## Documentation — placeholder block for future /docs/* (mark as 'coming soon' or omit until 8c1 lands)\n  - ## Source — link to https://github.com/alexmensch/stellata\n  - ## License — note AGPL-3.0-only\n\nKeep the file short (under 200 lines) — agents truncate aggressively.\n\n# Acceptance\n\n- curl https://stellata.xyz/llms.txt returns 200, content-type: text/plain or text/markdown\n- File renders as readable markdown\n- No broken links; all URLs are canonical (https, no trailing slash inconsistency)\n\n# Smoke\n\nAsk Claude / Perplexity to fetch https://stellata.xyz/llms.txt and summarize Stellata — the summary should accurately reflect the project (WebGL, star catalog, ~313k stars, variable stars).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:50:31Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:50:31Z","labels":["seo"],"dependencies":[{"issue_id":"stellata-bal.2","depends_on_id":"stellata-bal","type":"parent-child","created_at":"2026-05-12T10:50:30Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-bal.1","title":"Add public/robots.txt with crawler allowlist + sitemap pointer","description":"Stellata currently has no robots.txt at the apex domain, so crawler behaviour is implicit (allow-all by convention). Add an explicit robots.txt to:\n\n1. Confirm crawl access for classic search engines\n2. Confirm crawl access for AI search crawlers (ClaudeBot, GPTBot, PerplexityBot, ChatGPT-User, Google-Extended, etc.) — opt-in to AI citation\n3. Point to /sitemap.xml once it exists\n\n# Tasks\n\n- Add public/robots.txt\n- Default: User-agent: * Allow: /\n- Add explicit Allow blocks for ClaudeBot, GPTBot, ChatGPT-User, PerplexityBot, Google-Extended, Googlebot, Bingbot, DuckDuckBot, Applebot\n- Sitemap: https://stellata.xyz/sitemap.xml (forward-reference; sitemap.xml ships in a separate bead)\n- Verify Cloudflare Workers ASSETS binding serves /robots.txt as text/plain (the @cloudflare/workers types should infer content-type from extension)\n\n# Acceptance\n\n- curl https://stellata.xyz/robots.txt returns 200, content-type: text/plain\n- robots.txt linter (e.g. validator.w3.org or robotstxt.dev) reports 0 errors\n- File is committed under public/ (matches existing static-asset pattern)","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:50:23Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:50:23Z","labels":["seo"],"dependencies":[{"issue_id":"stellata-bal.1","depends_on_id":"stellata-bal","type":"parent-child","created_at":"2026-05-12T10:50:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-bal","title":"SEO + GEO discoverability for /","description":"Standalone SEO and GEO (Generative Engine Optimization) work for the WebGL app at /. Scoped to ship INDEPENDENTLY of the deferred stellata-8c1 public-site epic — the children here are surface-level meta/asset additions to the existing src/client/index.html and public/ static-asset tree, no new pages required.\n\n# Goal\n\nMake stellata.xyz findable and citable by:\n- Classic search engines (Google, Bing) — Lighthouse SEO ≥ 95\n- AI search surfaces (ChatGPT search, Perplexity, Claude, Google AI Overviews) — accurate brand mention and citable passages\n\n# Why now (independent of 8c1)\n\nstellata-8c1.4 bundles meta tags + sitemap + favicon into the /about + /docs landing, but 8c1 is deferred. Meanwhile the bare / has no description, no OG card, no robots.txt, no llms.txt — every share preview is blank, no AI crawler signals exist, and there is no canonical to anchor against. These are quick wins worth shipping on their own; once 8c1 lands, its sitemap and meta work supersedes the standalone bits filed here.\n\n# Reconciliation with stellata-8c1.4\n\nstellata-8c1.4 is a `related` link target — when 8c1 reopens, audit which children here are already-shipped vs. still-needed and revise 8c1.4's task list to drop the overlap. The standalone work here is forward-compatible with 8c1's eventual sitemap and brand-box Docs button.\n\n# Children\n\n- robots.txt — explicit allow for search + AI crawlers, sitemap pointer\n- llms.txt — emerging convention; concise project summary for AI agents\n- OpenGraph + Twitter Card image — 1200×630 PNG asset, share-link previews\n- Meta tags on / — description, OG, Twitter Card, canonical, robots, theme-color\n- JSON-LD structured data — Schema.org WebApplication/SoftwareApplication\n- Favicon + apple-touch-icon + web-manifest — full icon set\n- Sitemap.xml — minimal one-page sitemap for / (will be superseded by 8c1.1)\n- GEO content audit — citable passages for AI search; noscript fallback\n- Verify AI crawler access through Cloudflare Workers passthrough\n\n# Out of scope\n\n- /about and /docs/* pages (covered by stellata-8c1)\n- Per-docs-page schema and sitemap (stellata-8c1.1)\n- Versioned docs, blog, changelog\n- Multi-language / hreflang (English-only for v1)\n- Paid SEO tooling / GSC submission automation (manual is fine)","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:49:41Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:49:41Z","labels":["seo"],"dependencies":[{"issue_id":"stellata-bal","depends_on_id":"stellata-8c1.4","type":"related","created_at":"2026-05-12T10:51:43Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-6qj","title":"Research: ingest pulsars (ATNF / other) — visual representation and pulsar-planet intersection","description":"# Why\n\nSibling to stellata-d94 (brown dwarfs / WISE). Pulsars are a class of\ncompact-object point sources entirely absent from Stellata today — and\nthey raise representation questions that the brown-dwarf bead doesn't.\nUnlike Y-dwarfs, pulsars are usually invisible optically; they're\ndetected via radio / X-ray / gamma timing. But they're scientifically\nprominent and, crucially, they intersect with the exoplanets epic\n(stellata-bk5) — pulsar planets were the first exoplanets ever\ndetected (PSR B1257+12, 1992), and new ones still appear:\n\nReference example: PSR J2322−2650 — millisecond pulsar at ~230 pc\n(distance from radio timing parallax), with an exoplanet\nPSR J2322−2650 b (https://en.wikipedia.org/wiki/PSR_J2322%E2%88%922650_b)\ndiscovered via pulse-timing variations.\n\n# Scope of this bead (research only)\n\nSurvey the landscape and decide whether (and how) to ingest pulsars in\na v1 pass. Do NOT implement.\n\n## 1. Catalogue survey\n\nPrimary candidate: **ATNF Pulsar Catalogue** (Manchester et al. 2005,\nhttps://www.atnf.csiro.au/research/pulsar/psrcat/) — ~3500 known\npulsars with PSR J-name, position, period, period derivative,\ndispersion-measure distance, characteristic age, etc. Frozen-data\ndownload (per frozen-external-data memory: commit raw catalogue under\ndata/, no live fetch in build).\n\nAlso investigate:\n- EPN Database of Pulsar Profiles — pulse-shape data, not needed for\n  positional rendering.\n- Princeton Pulsar Group catalogues — generally subsumed by ATNF.\n- Fermi LAT Pulsar Catalogue — gamma-ray detections, a useful subset\n  for \"visible to high-energy instruments\" tagging.\n- Recent millisecond-pulsar timing-array catalogues (NANOGrav, EPTA,\n  PPTA) — better positional + distance precision for the MSP subset.\n\n## 2. Distance accuracy — the unique pulsar problem\n\nPulsar distances come from three sources, with very different\naccuracies; the catalogue ingest must carry a distance-method flag:\n\n- **Radio timing parallax** (best, ~5-10% accuracy, only available for\n  a few tens of nearby MSPs including PSR J2322−2650).\n- **Dispersion measure (DM) + Galactic electron density model**\n  (NE2001 — Cordes \u0026 Lazio 2002; or YMW16 — Yao, Manchester \u0026 Wang\n  2017). Typical accuracy 20-40%, can be factor-of-2 wrong in\n  directions where the electron-density model is poorly constrained.\n- **HI absorption distance limits** — bracketing distances from\n  21-cm absorption against known structures. Rare.\n\nThis is fundamentally less accurate than AT-HYG parallax for most\npulsars. Some treatment of distance uncertainty (an uncertainty halo?\na separate visual style?) should be in scope for the design — false\nprecision matters here in a way it doesn't for nearby stars.\n\n## 3. Visual representation — the open questions\n\nPulsars are NOT stars in the conventional sense. Possible treatments:\n\n- **Distinct disc/glow tier with non-stellar colour ramp.** Most\n  pulsars have no meaningful optical counterpart; a fixed neutral or\n  cyan-shifted colour (suggesting X-ray / radio emission) avoids\n  implying false blackbody temperature.\n- **Actual pulse animation.** Pulsars literally pulse — periods range\n  from milliseconds (MSPs) to ~10 s (slow pulsars). At real time these\n  are imperceptibly fast; under time-warp (per stellata-3re's `t`\n  infrastructure) a 1 ms pulsar at 1e6× speed becomes a visible 1 kHz\n  flicker. Possible visual hook for the existing time-warp UX, but\n  needs care — variable-star pulsation in Stellata uses R ∝ √L over\n  weeks-to-years periods, which is a very different regime.\n- **Special glyph in chart mode.** Chart mode already has variable-\n  ring and binary-wing glyphs; a pulsar-specific glyph (e.g. a stylised\n  pulse arc) would fit naturally.\n- **Catalog-flag bit** in catalog.bin, similar to the variable-star\n  bit and the planned exoplanet-host bit. Lazy-load a sidecar\n  pulsars.json with period, P-dot, DM, distance-method, age class.\n\n## 4. Spectral class — pulsars don't have one\n\nStellata's render pipeline assumes a spectral classification (parsed by\n`parseSpectral` in scripts/catalog-pure.ts). Pulsars don't fit that\nschema — they're neutron stars, surface T can be 1e5-1e6 K (X-ray)\nwith no optical counterpart in most cases. The few with optical\ncounterparts (Crab, Vela, Geminga) are very faint. The catalog\ningest needs an \"object class\" field that bypasses the spectral path.\n\nThis is the same architectural question raised in stellata-d94 for\nY-dwarfs (Teff \u003c 500 K, off the bottom of the spectral ramp) and\nlikely future ingest of white dwarfs, magnetars, etc. — converging\ntoward a unified \"object class\" column with class-specific render\npaths. Worth coordinating the design decision across both beads.\n\n## 5. Pulsar-planet intersection with stellata-bk5\n\nThe NASA Exoplanet Archive (stellata-bk5.1's data source) already\ncontains pulsar planets — PSR B1257+12 b/c/d, PSR B1620-26 b\n(circumbinary in M4), PSR J2322−2650 b. If the build pipeline\ningests pulsars as hosts, those exoplanet entries naturally hang off\nthe pulsar host via the same hostname-cross-match logic, with no\nspecial-casing on the exoplanet side.\n\nOpen question: does the orbital-ring + planet-body render path in\nstellata-bk5.3 work visually around a tiny non-stellar host? Pulsar\nplanets are at AU-scale orbits around a ~20 km object; the visual\nscale will be very different from Sol's planet system. May need\ndistinct treatment.\n\n# Out of scope (file as follow-ups if scoping concludes \"yes\")\n\n- Build-script changes for ATNF ingest.\n- Rendering layer for compact-object class.\n- Cross-match logic between ATNF and AT-HYG (mostly disjoint anyway).\n- Pulse-animation shader work.\n\n# Notes\n\n- Closest known pulsars: PSR J0437-4715 (~157 pc), PSR B0950+08\n  (~262 pc), Geminga (~250 pc), PSR J0108-1431 (~210 pc), PSR\n  J2322−2650 (~230 pc). Far more sparse near Sol than AT-HYG stars.\n- Per the prioritization framework, layer-ingest research sits at P3.\n  Coordinate with stellata-d94 — both are \"exotic point-of-light\"\n  research and should converge on a shared object-class architecture\n  if implementation proceeds.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T09:41:21Z","created_by":"Alex Marshall","updated_at":"2026-05-12T09:41:38Z","metadata":{"atnf_url":"https://www.atnf.csiro.au/research/pulsar/psrcat/","wikipedia_url":"https://en.wikipedia.org/wiki/PSR_J2322%E2%88%922650_b","catalog_candidate":"ATNF Pulsar Catalogue (Manchester et al. 2005)"},"labels":["catalog","exoplanets","research"],"dependencies":[{"issue_id":"stellata-6qj","depends_on_id":"stellata-bk5","type":"related","created_at":"2026-05-12T10:41:39Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-6qj","depends_on_id":"stellata-d94","type":"related","created_at":"2026-05-12T10:41:38Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-d94","title":"Research: ingest exotic point-of-light objects (brown dwarfs, sub-stellar) from WISE — and evaluate WISE as exoplanet source","description":"# Why\n\nStellata currently renders only stars from AT-HYG (Hipparcos/Gaia stellar\ncatalog). There's a class of nearby point-of-light objects that are\nabsent from any stellar catalog but are scientifically interesting and\nvisually plausible as additional discs/glows in the local volume —\nnotably cold brown dwarfs.\n\nReference example: WISE 0855−0714 (https://en.wikipedia.org/wiki/WISE_0855%E2%88%920714),\nthe coldest known brown dwarf, at ~2.3 pc from Sol — closer than most\nnamed stars currently rendered. It's invisible to optical catalogs but\nprominent in WISE infrared data.\n\nThis bead is to scope (not implement) two related questions:\n\n1. Should Stellata's model include sub-stellar / exotic local objects\n   (brown dwarfs, white dwarfs not in AT-HYG, etc.), and if so, from\n   which catalog?\n2. Is the WISE / AllWISE / CatWISE catalog family also a good source\n   for exoplanet candidates (direct-imaging companions to nearby\n   stars), feeding the deferred exoplanets epic (stellata-bk5)?\n\n# Scope of this bead (research only)\n\n- Survey WISE catalog products (AllWISE, CatWISE2020, NEOWISE) for\n  coverage of nearby (\u003c~25 pc) sub-stellar objects.\n- Identify which derived/curated catalogs already collate the\n  interesting subset (e.g. NASA's \"Census of L, T, and Y dwarfs\",\n  Kirkpatrick et al. compilations) so we don't have to do photometric\n  selection ourselves.\n- Assess WISE's value for exoplanets — direct-imaging companion lists\n  vs. the NASA Exoplanet Archive already targeted by stellata-bk5.1.\n  Likely complementary, not a replacement.\n- Decide which (if any) objects to ingest in a v1 pass, and the\n  rendering treatment (separate disc/glow tier? merged with stars\n  with an \"object class\" attribute? distinct point-marker layer?).\n- Conform to the frozen-external-data convention: any ingest commits\n  the raw catalog under data/ (LFS if \u003e1 MB), no live fetch in the\n  build pipeline.\n\n# Out of scope (file as follow-ups if scoping concludes \"yes\")\n\n- Build-script changes to actually ingest WISE.\n- Rendering changes to display sub-stellar objects.\n- Cross-matching WISE companions into the existing star catalog as\n  exoplanet hosts (would belong under stellata-bk5).\n\n# Notes\n\n- WISE 0855 is at d ≈ 2.31 pc — closer than 61 Cygni and Sirius.\n  Including it (and the other ~50 known Y-dwarfs within 10 pc) would\n  meaningfully populate the immediate solar neighbourhood.\n- WISE Y-dwarfs are extremely cold (Teff \u003c 500 K); the existing\n  spectral-class → colour mapping would need an extension or a\n  separate colour ramp.\n- Per the prioritization framework, layer-ingest research sits at P3\n  until it's actively in scope; bumping to P2 is appropriate if/when\n  scoping concludes a v1 ingest is small enough to ship as a\n  follow-up to the solar-system / Local Bubble work.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T08:53:15Z","created_by":"Alex Marshall","updated_at":"2026-05-12T08:53:15Z","metadata":{"wikipedia-url":"https://en.wikipedia.org/wiki/WISE_0855%E2%88%920714","catalog-candidate":"WISE / AllWISE / CatWISE"},"labels":["catalog","exoplanets","research"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-zsr.1","title":"Investigate star-colour calibration — current B-V piecewise gradient vs spectral-class Teff vs blackbody → sRGB","description":"The user (Alex) asked whether the star colour shown in the app could be calibrated more precisely. This bead captures the investigation. **It is research-only**: the deliverable is a written conclusion (worth doing / moot). An implementation bead would only be filed if the conclusion is \"yes, worth it.\"\n\n# Current state (as of investigation time)\n\n- **Data available per star (binary catalog v4, 44 B/record):**\n  - `ci` (B-V colour index, float32) — sourced from AT-HYG `ci` column (`scripts/build-catalog.ts:513`). Default fallback when missing: `DEFAULT_CI = 0.65` (solar-like, `build-catalog.ts:48`).\n  - `spectClass` + `lumClass` (uint8 each) — parsed from AT-HYG `spect` string via `parseSpectral` (`scripts/catalog-pure.ts:39`).\n  - **No effective-temperature column in the AT-HYG source CSV.** Confirmed by inspecting `data/athyg_33_classic_ids.csv` header — 34 columns, none named `teff` or similar.\n\n- **What we already derive but don't use for colour:**\n  - `T_TABLE` (`scripts/catalog-pure.ts:101-111`) — main-sequence Teff per spectral class O–M, interpolated by subclass. Currently consumed only by the Stefan-Boltzmann radius pipeline (drives `physRadius`), not by the colour pipeline.\n  - `BC_TABLE` — bolometric corrections, also radius-only today.\n\n- **Current colour function (`src/client/shaders/star.vert.glsl:137-143`):**\n\n  ```glsl\n  vec3 ciToColor(float ciVal) {\n      float t = clamp((ciVal + 0.4) / 2.4, 0.0, 1.0);\n      vec3 hot  = vec3(0.65, 0.78, 1.00);  // CI ≈ -0.4\n      vec3 mid  = vec3(1.00, 0.98, 0.92);  // CI ≈  0.65 (solar anchor)\n      vec3 cool = vec3(1.00, 0.55, 0.35);  // CI ≈  2.0\n      return t \u003c 0.5 ? mix(hot, mid, t*2.0) : mix(mid, cool, (t-0.5)*2.0);\n  }\n  ```\n\n  Three-stop piecewise-linear gradient in CI space. Hand-tuned endpoints; no physical derivation. Dust reddening is applied upstream as `effectiveCi = iCi + absorbAV / R_V` (R_V = 3.1), so the gradient also serves as the reddened-colour ramp.\n\n# Questions to answer\n\n1. **Is there a per-star Teff source we can ingest** (Gaia DR3 `teff_gspphot`, PASTEL spectroscopic catalogue, etc.) that beats spectral-class lookup? If yes — what's the coverage % of our ~313 k AT-HYG classic-IDs subset?\n\n   Frozen-external-data rule (`bd memories frozen-external-data`) applies: any Teff source must be a one-shot fetch committed under `data/`, not a live build dependency.\n\n2. **If we stick with derived Teff:** is `T_TABLE`-based Teff (spectral class + luminosity class) materially better than `ci` for colour, given that B-V already encodes the dominant first-order temperature signal of the photosphere? For stars with a parseable `spect` we have both; the comparison is empirical.\n\n3. **Is `ciToColor`'s three-stop piecewise gradient meaningfully wrong vs a physical blackbody → sRGB function?** Specifically: does CIE-tabulated Planckian locus → sRGB (gamma-corrected, white-point-anchored at D65 or at solar Teff = 5772 K) produce visibly different RGB at the same Teff/CI? If so, where — hot blue end, cool red end, or both?\n\n4. **Is the answer to (3) moot because** (a) the renderer's super-Gaussian profile + glow + bloom dominate perceived chroma, (b) the magnitude range we cap at means most pixels are core-saturated and chroma sits in the wings, or (c) the differences are below the perceivable threshold against the dark background?\n\n5. **Edge case: stars with missing `ci`.** Today we fall back to solar `DEFAULT_CI = 0.65`. If we have spectral class but no `ci`, deriving Teff from the spectral class and synthesising `ci` (via T_eff → B-V relation) would give a more honest colour for those stars than the solar default. How many catalog records are in that \"spect ✓ / ci ✗\" bucket?\n\n# Proposed investigation steps\n\n1. Quantify coverage: count records where `ci ≠ DEFAULT_CI`, where `spect` is parseable, and the intersection / set differences. One-line scan over the binary.\n2. Pick ~20 reference stars across the HR diagram (Vega, Sirius, Sol, Arcturus, Antares, Betelgeuse, an O-type like Mintaka, an M dwarf like Proxima) — compare three colours per star: current `ciToColor`, blackbody→sRGB at Teff(spectral), blackbody→sRGB at Teff(B-V).\n3. If the deltas are visible in-scene (manual smoke per `alex-stellata-smoke-tests`), write up the recommendation: either swap `ciToColor` for a blackbody LUT, add a Teff fallback when `ci` is missing, or ingest an external Teff catalogue.\n4. If the deltas are not visible — close as **moot** with the comparison artefacts pinned in the bead notes.\n\n# Files likely touched (if implementation follows)\n\n- `scripts/catalog-pure.ts` — Teff-from-spectral helper already exists (`T_TABLE`); add Teff→B-V or Teff→sRGB if needed.\n- `scripts/build-catalog.ts` — optionally derive and store synthetic `ci` for stars missing it.\n- `src/client/shaders/star.vert.glsl` — replace or augment `ciToColor`.\n- `SCIENCE.md` — document new colour derivation, cite source (CIE 1931 + Planck, or Mitchell Charity's blackbody table, etc.).\n- `scripts/catalog-pure.test.ts` — unit tests for the new helper.\n\n# Related work\n\n- `stellata-a7d.2.10` (P3, same parent `stellata-zsr`) — applies spectral/Teff correction to **appSize/brightness** (BC_photopic). Different output channel (size, not colour) but shares the per-star Teff infrastructure need. Whatever Teff source/derivation lands first should be reused by both.\n\n# Acceptance\n\n- Coverage stats for `ci` vs `spect` filled in (#1 above).\n- Sample-star colour deltas tabulated for the three candidate functions (#2 above).\n- Written recommendation: either (a) file an implementation bead with a chosen approach, or (b) close as moot with deltas documented as evidence.\n- No code changes in this bead — research-only.","notes":"# Research outcome — 2026-05-17\n\nFull writeup: `research/star-spectral-rendition/RECOMMENDATION.md` (in this PR).\n\n## Headline findings\n\n1. Current `ciToColor` shader is meaningfully wrong at the cool/red end\n   (Antares ΔE=64, Betelgeuse ΔE=61, Aldebaran ΔE=55 vs physical\n   blackbody). Visibly oversaturated orange-red against the additive-glow\n   scene context.\n2. T_TABLE-driven blackbody would REGRESS continuity — only 92,995 stars\n   (29.3% of catalogue) have a known luminosity class, so the giant\n   branch ends up the same colour as the main sequence at the same\n   spectral letter. Panel B in `research/star-spectral-rendition/hr_panels.png`\n   shows the discrete-banding artefact.\n3. B-V-driven blackbody (Ballesteros 2012 + Planck/CIE 1931 → sRGB)\n   gives continuous physical chroma AND honestly discriminates giants\n   from MS via colour. Panel C in the same render.\n4. Empirical Apsis probe (1000 random AT-HYG.gaia source_ids → Gaia DR3\n   archive TAP): (Teff, logg) coverage goes from 29.3% (status quo) to\n   **84.8% with gspphot∪gspspec** — projected ~266,690 stars vs 92,995\n   today. **2.87× improvement.** Among Apsis-resolved stars: 28.5%\n   giants, 30.6% subgiants, 40.9% MS.\n5. Display P3 follow-up probe: **negligible benefit for blackbody star\n   rendering.** DCI-P3 and sRGB share the same blue primary, and the\n   Planckian locus stays inside both gamuts for all T \u003e ~1700 K\n   (Proxima at 3170 K is our coolest displayed star). See\n   `research/star-spectral-rendition/p3_swatches.png` — only the\n   synthetic 1500 K row clips in sRGB. Filed `stellata-zsr.2` for the\n   day Stellata renders non-Planckian saturated colours (nebulae,\n   auroral palettes, chart UI), where the gamut win actually appears.\n\n## Tiered recommendation (decoupled, sequential)\n\n**Tier 1 — Replace `ciToColor` with blackbody→sRGB LUT (ship standalone).**\nSingle shader change + build-time LUT generator. Routing: Ballesteros(B-V)\nwhere `ci` present (96%), T_TABLE where parseable spect only (2%), WD\nformula for white dwarfs, solar fallback for the rest. No catalogue\nschema bump. No `dch` dependency. **Fixes the visible cool-end issue.**\n\n**Tier 2 — Ingest Gaia DR3 Apsis under `stellata-dch` Phase 1.** New\n`scripts/refresh-gaia-apsis.py` reusing `dch.21`'s `refresh_lib.py`\nTAP client. Outputs `data/gaia_dr3_apsis.tsv` (LFS, ~15-25 MB). Surfaces\nper-star Teff into the v6 binary format (rides `dch` Bead M). Tier 1 LUT\nthen keys on Teff directly. **Modest colour-fidelity gain over Tier 1\n(B-V already proxies most logg discrimination), but broader payoff:\nStefan-Boltzmann radius refinement, Edenhofer dust cross-check via\n`azero_gspphot`, metallicity-aware downstream effects.**\n\n**Tier 3 — Deferred.** Pecaut \u0026 Mamajek extended T_TABLE for the\nno-Apsis no-CI bucket; metallicity-aware colour offsets; Display P3\nwide-gamut output (`stellata-zsr.2`, low-value for blackbodies but\nprerequisite for future saturated non-Planckian work).\n\n## `stellata-dch` integration points\n\n- Tier 2 ingest script slots in as a Phase 1 sibling to `dch` Beads B/C/D/E.\n- Apsis Teff carrier rides v6 binary format (`dch` Bead M) — 2 extra bytes\n  per record alongside the planned 8-byte `gaia_source_id`.\n- `dch` Phase 5 (\"spectral / mass refinement\") gets an honest Teff +\n  logg input for stars with Apsis — worth surfacing in Phase 5 scoping.\n\n## Timing\n\n- Tier 1 is shippable today as a single PR. Independent of `dch`.\n- Tier 2 implementation can't start until `dch.21` (`refresh_lib`) lands.\n  Apsis can ingest as TSV-only first (lighter integration); v6 binary\n  surfacing waits for `dch` Bead M.\n- Tier 3 (zsr.2 P3) is filed deferred-ready; revisit when nebula or\n  saturated non-Planckian work appears.\n\n## Artefacts\n\nAll under `research/star-spectral-rendition/` in this PR. Re-runnable via\nthe local venv (`research/star-spectral-rendition/.venv/bin/python \u003cscript\u003e.py`).\nApsis probe deterministic for `RANDOM_SEED=20260517`.\n\n- `coverage.txt`, `coverage.py` — Step 1 catalogue scan\n- `sample_comparison.txt`, `sample_swatches.png`, `compare_sample_stars.py` — Step 2b 20-star three-way comparison\n- `hr_panels.png`, `hr_panels.py` — Step 2c full HR diagram with three colour functions\n- `scene_swatches.png`, `scene_swatches.py` — Step 4 additive-glow-on-black perceptual swatches\n- `apsis_coverage.txt`, `apsis_sample.tsv`, `apsis_sample.py` — Step 3 empirical Apsis coverage probe\n- `p3_swatches.png`, `p3_swatches.py`, `display-p3.icc` — Tier 3 Display P3 demonstration (image is P3-ICC-tagged)\n- `parse_spectral.py`, `test_parse_spectral.py` — Python port of catalog-pure.ts parseSpectral, parity-tested (14/14)\n- `blackbody_color.py` — Planck + CIE 1931 (Wyman 2013) → sRGB AND Display P3\n\n## Status\n\nResearch **complete**. Closing this bead alongside the artefact PR.\nTier 1 (under `stellata-zsr`) and Tier 2 (under `stellata-dch`) to be\nfiled as separate implementation beads on session-close.\nTier 3 P3 follow-on filed as `stellata-zsr.2`.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-12T08:34:48Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:12:29Z","started_at":"2026-05-17T17:22:00Z","closed_at":"2026-05-18T09:12:29Z","close_reason":"Research complete — RECOMMENDATION.md merged in PR #86; Tier 1 filed as stellata-zsr.3, Tier 2 as stellata-dch.48, Tier 3 (Display P3) as stellata-zsr.2","metadata":{"wiki_hr_diagram":"https://en.wikipedia.org/wiki/Hertzsprung–Russell_diagram"},"labels":["physics","research","shader"],"dependencies":[{"issue_id":"stellata-zsr.1","depends_on_id":"stellata-a7d.2.10","type":"related","created_at":"2026-05-12T09:34:52Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-zsr.1","depends_on_id":"stellata-zsr","type":"parent-child","created_at":"2026-05-12T09:34:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2br.4","title":"Cross-host coverage audit — every focusable object uses camera-motion","description":"## Why this exists\n\nPer `stellata-pattern-coverage-across-peers`: when a PR is framed as \"all the Y in this layer use pattern X\", the headline claim is false if even one peer is missed. The point of stellata-2br is unification — this audit confirms it.\n\nLands after the log-distance profile (sibling child 3) so the audit checks against the final behaviour, not the intermediate state.\n\n## Scope\n\n**Park-arrivals only.** Warp Phase 3 (observe→observe position lerp `pEnd → (0,0,0)`) is explicitly out of scope — it is the OBSERVE-mode handover, not a park-arrival, and stays inline by design. Phase 3 already uses cubic-Hermite `u²·(3 − 2u)` (the same shape the helper adopts in 2br.3), so the exclusion is geometric (endpoint at the focal star's local origin → `d_end = 0` is degenerate for log-d), not about easing form. See `docs/camera-arrival.md` § Inventory.\n\n## What needs to be done\n\n1. **Enumerate every park-arrival host** present in the codebase at audit time. From CLAUDE.md and `stellata.ts`:\n   - Stars (`focusStar`)\n   - Clouds (`flyToCloud`)\n   - Any host added since stellata-2br was filed (check `bd ready` + recent merges)\n\n2. **For each host, verify the park-arrival path** routes through `camera-motion.ts`:\n   - `grep` for direct `camera.position.lerp` / `lerpVectors` calls in `src/client/*.ts` at arrival sites — should be zero (allowed elsewhere, e.g. UI fade-ins, warp Phase 3, observe-mode aim).\n   - `grep` for inline `2 * t * t` / `1 - 2 *` piecewise-smoothstep formulas — should be zero at park-arrival sites. Two non-park-arrival matches in `stellata.ts` are expected and deliberate: `getObserveTransitionProgress` (HUD overlay readout) and the `updateObserveTransition` enter/exit fallthrough. Both are observe-mode TIME easing, not park-arrival distance easing, and stay piecewise by design. Warp Phase 3 already uses cubic-Hermite and does not match this grep.\n   - `grep` for `3 * u * u - 2 * u * u * u` / `3·u²−2·u³` — should appear in `camera-motion.ts` (helper) and in `updateWarp`'s Phase 3 branch (already there, unchanged).\n   - Inspect each `focus*` / `flyTo*` method and confirm it builds an `ArrivalState` and ticks via `tickArrival`.\n\n3. **For any non-covered park-arrival host**, file a follow-up child under stellata-2br (do **not** stretch this bead's scope).\n\n4. **PR description must enumerate the host list explicitly** with a tick-box per host. Per `stellata-pattern-coverage-across-peers` this is the canonical defence against false-headline PRs.\n\n## Acceptance\n\n- PR description carries an explicit, per-host checklist of park-arrival hosts.\n- Each host has a one-line \"covered ✓ / follow-up filed: stellata-XXX\" annotation.\n- `grep` audits show zero remaining inline arrival smoothsteps at park-arrival sites.\n- Warp Phase 3 is documented as \"deliberately not routed\" with a link to `docs/camera-arrival.md` § Inventory. Note that Phase 3's `u²·(3 − 2u)` easing isn't itself an outlier — it matches the helper's adopted form — so no special \"different easing\" annotation is needed; the geometry (endpoint at focal-star origin) is the exclusion rationale.\n\n## Smoke (manual)\n\nFor each host in the enumerated list: focus the host, confirm the park-arrival feel is the new log-distance profile (matches Sol/Betelgeuse from child 3).\n\n## Out of scope\n\n- Per-host calibration. If a specific host needs a different profile, file as a follow-up child, don't fix here.\n- Future hosts not yet in the codebase (Local Bubble, Radcliffe Wave, etc.) — they inherit the helper when they land.\n- Warp Phase 3 (out of scope by design).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-12T08:01:53Z","created_by":"Alex Marshall","updated_at":"2026-05-15T14:37:38Z","labels":["camera","ux"],"dependencies":[{"issue_id":"stellata-2br.4","depends_on_id":"stellata-2br","type":"parent-child","created_at":"2026-05-12T09:01:52Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2br.4","depends_on_id":"stellata-2br.3","type":"blocks","created_at":"2026-05-12T09:02:03Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.184","title":"Sweep codebase for astronomical-constant stragglers (shaders, build scripts, tests)","description":"# Problem\n\nstellata-r9q.1 (the focused initial consolidation bead) establishes \\`src/client/astronomy-constants.ts\\` and migrates the two known TS sites (\\`ephemeris.ts\\`, \\`stellata.ts\\`). But astronomical / physical constants are scattered more widely than that — they likely live in:\n\n- Shader source under \\`src/client/shaders/*\\` (GLSL can't \\`import\\` TS — these have to be either hard-coded with a source-of-truth comment + cross-language test, or substituted at build time).\n- Build scripts: \\`scripts/build-catalog.ts\\`, \\`scripts/catalog-pure.ts\\`, \\`scripts/build-clouds.py\\`, \\`scripts/build-dust.py\\`, \\`scripts/verify-catalog.ts\\`.\n- Tests (which may redefine constants — \\`stellata-test-coverage-discipline\\` calls this out explicitly: \\\"Tests: never redefine a constant the production code exports. Import it.\\\").\n- Other client modules: dust loader, ephemeris helpers, planet-system, etc.\n\nPer Alex (2026-05-11): \\\"For the reused 2s value... shared and defined in one place is important for code and unified feeling in the app.\\\" Same principle applies to AU, R_sun, arcsec-per-radian, and other physical constants.\n\n# Scope\n\nAfter stellata-r9q.1 lands the canonical constants file:\n\n1. \\`grep -rn -E \\\"206264|2.2543e-8|1\\\\.49597|6\\\\.957e\\\" --include=\\\"*.ts\\\" --include=\\\"*.tsx\\\" --include=\\\"*.py\\\" --include=\\\"*.glsl\\\" .\\` and similar searches for arcsec/AU/R_sun literals. Triage each hit.\n2. For TS files: replace the literal with an import from \\`astronomy-constants.ts\\`.\n3. For Python build scripts: define a small \\`scripts/astronomy_constants.py\\` mirror with the same values and source comment. Add a cross-language test in vitest that loads both and asserts equality (or at least loads the TS file and asserts the values match documented Python ones).\n4. For GLSL shaders: keep the literal but add a comment naming the canonical TS constant it mirrors. Optionally add a vitest unit test that scrapes the literal from the shader source and asserts it matches the TS constant (defensive against silent drift).\n5. For tests: import the TS constants rather than redefining.\n\n# Out of scope\n\n- Adding new constants that don't currently exist somewhere.\n- Refactoring math that USES the constants (only the literals are touched).\n\n# Smoke\n\n- \\`npm run typecheck\\` green.\n- \\`npm test\\` green (including any new cross-language assertion tests).\n- \\`npm run build:catalog\\` produces a binary numerically equivalent to before (byte-identical if possible).\n- \\`npm run build\\` succeeds.\n\n# Depends on\n\nstellata-r9q.1 (the canonical constants file must exist).","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-11T12:16:56Z","created_by":"Alex Marshall","updated_at":"2026-05-12T07:28:59Z","started_at":"2026-05-11T20:05:57Z","closed_at":"2026-05-12T07:28:59Z","close_reason":"Closed","labels":["pr-61"],"dependencies":[{"issue_id":"stellata-9mm.184","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-11T13:16:56Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.184","depends_on_id":"stellata-r9q.1","type":"blocks","created_at":"2026-05-11T13:27:25Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.21","title":"Apply smooth focus-park to planet focus (when planets become click-focusable)","description":"# Problem\n\nWhen planets become click-focusable in the codebase (currently they are rendered via `PlanetBodyField` but are not a click-focus target), the planet-focus path should route through the generic `parkDistance(...)` + focus-lerp primitives established by stellata-r9q.2 — rather than inventing a planet-specific snap.\n\nPer Alex (2026-05-11): \\\"I want the same approach for future objects: clouds, planets, nebulae, etc. We MUST write code that is reusable.\\\"\n\n# Status: DEFERRED\n\nPlanets aren't a click-focus target in the codebase yet — this bead is filed up-front (per `stellata-pattern-coverage-across-peers`) so the requirement is captured for when that work lands. \\`bd defer\\` immediately after creation; revisit when planet click-focus is being built (likely as part of this epic, `stellata-3re`).\n\n# Scope (sketch, to be refined when picked up)\n\nRoute the planet-focus path through the shared helpers:\n\n- \\`R_pc\\` = planet body radius (in pc, converted from the per-planet \\`radius_earth\\` / \\`radius_jupiter\\` data via planet-radius constants in the astronomy-constants file).\n- \\`dMinFloor\\` = whatever planet manual-zoom floor this epic settles on (a few planet-radii would be a reasonable starting point, but TBD).\n- No \\`extraFloor\\` for planets (no binary-companion analogue, though we may eventually want a host-star or sibling-planet floor).\n\nReuse \\`focusLerpTo(...)\\` from \\`src/client/focus-transition.ts\\` (created by stellata-r9q.2).\n\n# Files (anticipated)\n\n- \\`src/client/planet-system.ts\\`\n- \\`src/client/planet-body-field.ts\\`\n- \\`src/client/stellata.ts\\` — focus dispatch.\n\n# Depends on\n\nstellata-r9q.2 / Child 2 of stellata-r9q (the generic mechanism must exist).\n\n# Related\n\nstellata-r9q (focus-park UX epic) — this bead is the planet-peer counterpart; it lives under stellata-3re because it can only be actioned when planet-focus is being built.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-11T12:16:34Z","created_by":"Alex Marshall","updated_at":"2026-05-11T15:49:05Z","labels":["phase-12a","solar-system","ux"],"dependencies":[{"issue_id":"stellata-3re.21","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-11T13:16:33Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.21","depends_on_id":"stellata-r9q.2","type":"blocks","created_at":"2026-05-11T13:27:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.182","title":"Mask any close-disc star via spatial scan (stellata-rmo follow-up)","description":"Follow-up to stellata-rmo / PR 52. The shipped fix masks the most-recently-focused star + companion (persisted across Esc) plus every vertex of the highlighted constellation. This closes the most common case but leaves a residual gap: a close-disc star that has never been focused and is not a vertex of the highlighted constellation — e.g. drifting past a bright star post-warp without focusing it, or scrolling through a dense field where the camera is near a star that the user never clicked.\n\n# Suggested approach\n\nBuild a coarse spatial index over the catalog at load (grid bin or k-d tree). Each frame, query stars within a bounded radius of the camera and run them through `placeCircle` in `src/client/disc-mask.ts`. `placeCircle` already filters by the 48 px disc threshold, so over-inclusion is cheap.\n\nBin sizing: pick a radius such that the per-frame candidate count stays in the same order as today's ceiling (~64). A few-parsec radius around the camera position should catch every star whose disc clears 48 px under reasonable FOV.\n\n# Smoke step\n\nAfter implementation: pick a path that drifts the camera near a star you never focused (e.g. warp to a target near Procyon, then orbit to drift past a neighbouring star), confirm constellation lines pass behind the never-focused close disc.\n\n# Why deferred\n\nThe shipped option B (persist most-recently-focused) closes the user's exact reported repro at near-zero cost. Option C is the strict superset but requires spatial-index infrastructure that isn't justified by the current bug report. File for future discovery.","status":"deferred","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-11T07:33:58Z","created_by":"Alex Marshall","updated_at":"2026-05-11T07:34:04Z","labels":["ism","solar-system"],"dependencies":[{"issue_id":"stellata-9mm.182","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:38Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-nxb.3","title":"Search index: Tycho-2 (TYC) + Gaia DR3 IDs (already in AT-HYG, currently un-ingested)","description":"**Goal**\n\nIngest the `tyc` and `gaia` columns from AT-HYG (already present in `data/athyg_33_classic_ids.csv` columns 2 and 3, and they'll persist in the m12 upgrade) into the search index so users can find stars by Tycho-2 or Gaia DR3 IDs.\n\nFiled 2026-05-10 after Alex asked whether m12 brings Gaia/TYC — the answer is no, but the columns are already there in classic_ids, untouched by `scripts/build-catalog.ts`. This is a free-data ingest, not blocked on a catalogue refresh.\n\n**What changes**\n\n- `scripts/build-catalog.ts`: parse `row.tyc` (string like `TYC 1234-5678-1`) and `row.gaia` (Gaia DR3 source_id as int64; needs string handling — DataView has no native u64, see url-state.ts approach to 3-byte HIP for prior art).\n- `SearchEntry` (`scripts/catalog-pure.ts`): two new optional fields `tyc` (string) + `gaia` (string — int64 doesn't survive JSON's number-precision constraint cleanly; emit as a decimal string).\n- `src/client/search.ts`: extend the prefix-form direct-lookup map (`TYC ...` / `Gaia ...`) alongside the existing HIP/HD/HR/Gl handling around `bindSearch` ~line 167.\n- `src/client/search.ts:buildStarLabels` fallback ladder: extend to … → HIP → HD → HR → Gl → TYC → Gaia (Gaia at the end because raw 19-digit Gaia IDs are not human-readable; TYC slightly more readable).\n\n**Watch out for**\n\n- Gaia DR3 source IDs are 64-bit integers; JSON's max-safe integer is 2^53 - 1, so emit/store as decimal strings throughout. Internal indices stay numeric.\n- TYC strings have a few variant formats in the wild (`TYC 1234-5678-1`, `1234-5678-1`, etc.). Normalise to a single canonical form for the lookup key (probably space-stripped).\n- search-index.json size grows: ~280k AT-HYG rows × (TYC string + Gaia string) ≈ ~6–10 MB raw before gzip. Worth measuring against the 13 MB raw / 2 MB gzipped current size to see if compression keeps it acceptable.\n\n**Smoke**\n\nAfter build, search box should resolve:\n- `TYC 1234-5678-1` → focuses the star (pick a known TYC entry in classic_ids).\n- `Gaia 5853498713190525696` → focuses Proxima (Proxima's Gaia DR3 source_id).\n- Existing HIP/HD/HR/Gl searches unchanged.\n\nPairs with stellata-04y (designator FAQ) so the doc and the search both add the same catalogue surface simultaneously.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-10T21:52:09Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:53:08Z","labels":["catalog","search"],"dependencies":[{"issue_id":"stellata-nxb.3","depends_on_id":"stellata-nxb","type":"parent-child","created_at":"2026-05-10T22:52:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-04y","title":"Star designator reference / FAQ: catalogue provenance and scope of HIP/HD/HR/Bayer/Flamsteed/Gl/TYC","description":"**Goal**\n\nUser-facing reference content explaining what each star designator is, which catalogue it belongs to, and the rough scope/range of each. Sibling to stellata-e64 (rendering-decisions FAQ) — same audience, different topic. Filed 2026-05-10 after Alex asked the question in a session; the answer was non-trivial and the data wasn't documented anywhere except a flat list in SCIENCE.md.\n\n**Reference content** (use as source material when authoring the page)\n\nDisplay fallback ladder in our codebase (src/client/search.ts:90-119): proper name → Bayer → Flamsteed → HIP → HD → HR → Gl.\n\n- **Proper name** — IAU-approved common names + historical (Sirius, Vega, Betelgeuse). ~450 currently sanctioned by the IAU Working Group on Star Names. AT-HYG `proper` column.\n- **Bayer** — Greek letter + constellation genitive (α CMa = Sirius). Johann Bayer's *Uranometria* (1603), brightness-ranked within each constellation. ~1,500 stars, capped at the Greek alphabet (24) per constellation plus a Latin-letter tail used by Gould.\n- **Flamsteed** — Number + constellation (61 Cyg, 47 UMa). John Flamsteed's *Historia Coelestis Britannica* (1725, posthumous), numbered west-to-east in RA per constellation. ~2,500 entries, naked-eye-ish.\n- **HR** — *Yale Bright Star Catalogue* (Hoffleit \u0026 Jaschek 1991), originally 'Harvard Revised'. 9,110 stars all brighter than V ≈ 6.5 (naked-eye limit). Standard reference for bright stars.\n- **HD** — *Henry Draper Catalogue* (Annie Jump Cannon at Harvard, 1918–1924) + the HDE extension. 359,083 stars total (272,150 in the main HD). Spectral types for everything; goes to about V ≈ 9.\n- **HIP** — *Hipparcos Catalogue* (ESA 1997). 118,218 stars measured astrometrically by the Hipparcos satellite (1989–1993). Parallaxes + proper motions; the basis for almost all modern distance work shy of Gaia. Numbered 1–120404 with gaps. **Real range tops out at 120404** — anything labelled HIP above that in third-party tools is likely the HIC (Hipparcos Input Catalogue, pre-mission, ~400k entries) or a synthetic ID, not a real catalogue HIP.\n- **Gl / GJ** — *Gliese Catalogue of Nearby Stars* (Wilhelm Gliese 1969) + *Preliminary Third Edition* with Hartmut Jahreiß (CNS3, 1991). Stars within ~25 pc; numbered ~1–965 in Gliese plus 1000–4388 in the Jahreiß extension. 'Gl' is the original prefix, 'GJ' the modern combined form — same numbering. Where you'll find Proxima Centauri (GJ 551), Barnard's Star (GJ 699).\n\nRelated catalogues we don't currently display (some already in AT-HYG's CSV but un-ingested — see stellata-nxb):\n- **Tycho-2** (TYC) — Høg et al. 2000. 2.5M stars from the Hipparcos satellite's secondary star-mapper, format `TYC region-num-comp`. AT-HYG's name (Augmented Tycho HYG) is from this.\n- **Gaia** (DR3/DR4) — current state-of-the-art, ~1.8 billion sources.\n- **HIC** — Hipparcos Input Catalogue, pre-mission target list, ~400k entries. Sometimes loosely labelled 'HIP' in older tools.\n- **CCDM** — *Catalog of the Components of Double and Multiple Stars* (Dommanget \u0026 Nys 1994). Used in build-catalog.ts to flag visual doubles via the Hipparcos MultFlag.\n\n**Where it lives** (pick one)\n\n(a) New section in SCIENCE.md (existing canonical home for catalogue/data provenance). Lowest-touch — slots into the existing 'Data sources' content. Not user-facing in-app.\n\n(b) New top-level user doc `docs/star-designators.md`, linked from CLAUDE.md's documentation index AND from a new help-modal tab. Parallel to stellata-e64's (a) option.\n\n(c) Embed in the brand-modal / info-modal as a 'Star names' tab so it surfaces in-app without a separate page.\n\n(a) is the easiest first step; (b)/(c) build on it.\n\n**Smoke**\n\nAfter landing, a reader of SCIENCE.md (or the new doc) should be able to answer: 'What's the difference between HIP and HD?', 'Why does HIP 210432 sound off?', 'Is HR the same as HD?'.\n\nRelated: stellata-nxb (add TYC/Gaia/V-numbers/GJ as searchable designators).","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T21:51:56Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:53:59Z","labels":["docs","faq"],"dependencies":[{"issue_id":"stellata-04y","depends_on_id":"stellata-8c1","type":"parent-child","created_at":"2026-05-10T22:53:56Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-04y","depends_on_id":"stellata-e64","type":"related","created_at":"2026-05-10T22:52:23Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-04y","depends_on_id":"stellata-nxb.3","type":"related","created_at":"2026-05-10T22:52:24Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-dsq","title":"Picker debug overlay: visualise hit-radius + tiers + pickScore in unified debug panel","description":"**Goal**\n\nA debug overlay (toggle `debug.picker()`) that visualises the live pickStar state at the cursor: hit radii for each candidate, tier (prime / fallback), and current pickScore. Pays off for the next ambiguous-click report; would have surfaced the stellata-02d POI gate diagnosis directly (cursor lands on a star, tier circle goes green, but `togglePoi` reject path is then visible too).\n\n**Scope**\n\n- New `src/client/picker-debug-hud.ts` module + section in the unified debug panel (`debug.picker()`), following the `pin-debug-hud.ts` / `arrow-fade-debug-hud.ts` pattern.\n- SVG overlay layer renders into the existing canvas-overlay container.\n- For each star whose projected centre lies within `max(hitRadius, pixelThreshold)` of the cursor:\n  - Circle at projected centre showing `hitRadius` (prime tier, solid) and `pixelThreshold` (fallback tier, dashed/faded).\n  - Colour by pickStar state — green = prime winner, yellow = prime runner-up, blue = fallback hit, grey = fallback runner-up.\n  - Hatched / red fill when togglePoi would reject this candidate (Sol or missing HIP) — surfaces the same gate as stellata-7g2 visually.\n  - Live `pickScore` label next to the centre.\n- Updates on `pointermove` (debounced to RAF).\n- Listens to chart-mode toggle so the SVG layer participates in the chart-mode visibility gate too — most useful in chart mode where 1–2 px discs make overlapping picks ambiguous.\n\n**Why a separate module**\n\n`pin-debug-hud.ts` is currently scoped to the focused-star pin diagnostic; mixing the picker overlay in would blur the scope. New module + new `debug.picker()` registration matches the project convention (one HUD per concern).\n\n**Origin**\n\nFiled during PR #53 follow-up after Alex reported a coincident-star click failure (turned out to be a `togglePoi` gate, not a picker bug — but the overlay would have made that obvious from the page rather than the console).\n\n**Smoke**\n\n1. Open chart mode; `debug.picker()` toggle in the unified panel.\n2. Hover near a crowded patch (Pleiades / Cygnus). Coloured circles appear, scores update on cursor move.\n3. Toggle off → overlay disappears; no SVG noise left in the DOM.\n4. Hover over Sol or another non-HIP star → its circle gets the hatched/reject styling.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T21:39:29Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:39:29Z","labels":["chart-mode","debug","observe","picker"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.20","title":"Store Mallama Mercury c7 in third vec4 attribute to recover full polynomial out to 170°","description":"**Why**\n\nMercury is the only Mallama 2018 fit beyond degree 6. PR #56 capped MERCURY_PHASE.alphaMaxDeg at 87° because the dropped c7 = 6.592e-15 term contributes \u003e0.25 mag past that, and the truncated polynomial would render Mercury wildly wrong (up to ~27 mag too bright at α = 170° if let to run). Past 87° the renderer falls through to anchor-scaled Lambert, which tracks the published 7th-order curve to within ~0.5 mag across 90°–170° — close but not exact.\n\nTo recover Mallama's published 170° validity range with full fidelity, we'd need to store c7 in a third vec4 per-instance attribute. That's the only way to lift αmax for Mercury back to its published 170° without re-introducing the truncation runaway.\n\n**Acceptance**\n\n- New per-instance attribute (e.g. `iPhaseCoefsC` carrying `(c7, _, _, _)` with three reserved slots for future expansion or a different packing).\n- planet-body-field.ts plumbing: allocate / grow / write-static / flush / shift-down, mirroring the existing PhaseA/PhaseB pattern. The bead-9mm.169 read-back test pattern extends naturally.\n- planet.vert.glsl: extend mallamaDV to evaluate degree-7 (one extra Horner step using the new attribute slot).\n- phase-function.ts: extend PhaseCoefficients with c7; update mallamaDV. Update MERCURY_PHASE.c7 = 6.592e-15 and MERCURY_PHASE.alphaMaxDeg = 170. The other planets carry c7 = 0 (Mallama's published fits stop at degree 6 for them).\n- Test: phase-function.test.ts — assert |mallamaPhaseFactor(MERCURY_PHASE, α) − full_7th_order(α)| stays sub-0.01 mag across 0°–170° (the truncation budget collapses once c7 lands).\n\n**Smoke**\n\n- npm test passes including a new tight-bound test on Mercury at high α.\n- Manual: at αmax = 170°, Mercury should render apparent V ≈ +8 (matching reality, lost in solar glare). Prior PR #56 fix already gets this within 0.5 mag via anchor-Lambert; this bead closes the gap to within Mallama's own residual.\n\n**Deferred reasoning**\n\nFiled as P3 — anchor-Lambert at high α is already physically defensible (sub-0.5 mag error, indistinguishable from reality at the brightness levels involved). Worth tackling when the GPU buffer schema gets touched for another reason (e.g. exoplanet phase data under stellata-bk5).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T17:04:46Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:04:46Z","labels":["phase-12a","physics","pr-56","solar-system"],"dependencies":[{"issue_id":"stellata-3re.20","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-10T18:04:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2f6.5","title":"Galactic grid + similar opaque-queue lines render on top of planet body in glow regime","description":"**Symptom**\nGalactic sphere/grid lines (renderOrder -1, opaque queue) are visible 'on top of' Mercury (and presumably any planet in the glow regime). User reported during smoke testing of PR #48 (stellata-3re.19).\n\n**Root cause**\nThe planet core depth mask (`uRenderMode == 2` at renderOrder -4) has a `vPhysRatio \u003c PHYS_RATIO_THRESHOLD discard` gate copied from the disc pass's regime split. At any typical viewing distance for Mercury (Sol-focused at a few AU camera distance), Mercury's vPhysRatio ≈ 0.02 — well below 0.5 — so the core mask never fires.\n\nThat gate is the bug. The disc/glow regime split is meaningful for the *colour* passes (resolved disc vs distant point glow), but the core mask's purpose is different: it's a depth-only occluder for layers that should depth-fail behind the planet's bright body. The bright body exists at any vPhysRatio (the perceptual core where `glow \u003e= uCoreThreshold`) — it just shrinks to a smaller area in the glow regime.\n\nRender-order trap: galactic grid is opaque (renderOrder -1), so it draws BEFORE any of the planet's transparent passes (corrupt at 1.5, restore at 2.5, disc at 3, glow at 4) regardless of renderOrder. Without the core mask firing at -4 in the opaque queue, the galactic grid passes its depth test against background depth, paints colour into the framebuffer at Mercury's screen position, and the planet's later transparent passes can't replace it cleanly:\n- Disc pass uses MaxEquation — keeps the brighter of (grid colour, planet colour). Mercury's night-side colour is dim → grid wins.\n- Glow pass is additive — adds planet glow on top, but the grid colour stays.\n\nSame root cause likely applies to MW (-3, opaque), molecular clouds (-2), galactic disc (-1) bleeding through Mercury at glow regime.\n\n**Suggested approach (one-line shader edit)**\nDrop the `vPhysRatio \u003c PHYS_RATIO_THRESHOLD discard` from `uRenderMode == 2` in `src/client/shaders/planet.frag.glsl`:\n\n```glsl\n// Before:\nif (uRenderMode == 2) {\n  if (vPhysRatio \u003c PHYS_RATIO_THRESHOLD) discard;  // \u003c-- delete this\n  if (vAppMag \u003e uMaxAppMag) discard;\n  if (glow \u003c uCoreThreshold) discard;\n  outColor = vec4(0.0);\n  return;\n}\n```\n\nEffect:\n- Glow regime: core mask now fires at the perceptual core (`glow \u003e= uCoreThreshold`), writes Mercury's actual depth at -4 in opaque queue.\n- Galactic grid / MW / clouds at -1 / -3 / -2 now depth-fail behind Mercury's perceptual core.\n- Halo region of Mercury (where `glow \u003c uCoreThreshold`) untouched — background still peeks through, preserving the soft halo→MW falloff the corrupt+restore pair already preserves for orbit rings.\n- Resolved-disc regime unchanged (vPhysRatio gate was redundant — disc gate at the colour passes already handles regime splits).\n\n**Why this is filed rather than included in PR #48**\nPR #48 was scoped to the orbit-ring fix and grew to include 5 9mm follow-ups, a corrupt+restore depth pair, and a stencil-then-revert iteration. Adding another shader change to a load-bearing depth-write at renderOrder -4 wants its own focused smoke pass — confirm no regression in MW haze through stellar halos, no regression in molecular cloud fall-off near close-range planets, no regression in resolved-disc viewing of bright planets (Jupiter/Saturn at host-orbit-radius). Cleaner as a follow-up.\n\n**Acceptance criteria**\n- At any viewing distance where Mercury renders, galactic grid lines passing through Mercury's screen position do NOT bleed through Mercury's bright body.\n- Same for MW, molecular clouds, galactic disc background lines.\n- Halo region of Mercury (the dim outer perceptual disc) still allows background MW haze to show through (unchanged behaviour, preserved by the gate on `glow \u003e= uCoreThreshold`).\n- No regression in viewing scenarios that the corrupt+restore orbit-ring fix already handles.\n\n**Smoke step**\nFocus Sol with galactic grid + galactic disc enabled. Rotate the camera so Mercury passes in front of a galactic grid line. Confirm the grid line is hidden behind Mercury's bright body. Confirm dim haze (MW background) still shows softly through Mercury's outer halo region.\n\n**Found during smoke testing PR #48 (stellata-3re.19)**","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T15:46:22Z","created_by":"Alex Marshall","updated_at":"2026-05-10T15:46:22Z","dependencies":[{"issue_id":"stellata-2f6.5","depends_on_id":"stellata-2f6","type":"parent-child","created_at":"2026-05-10T16:46:21Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2f6.4","title":"Planet glow visible through host star (corrupt+restore depth bypass)","description":"**Symptom**\nFocused on Sol, Mercury (and presumably any planet) is visible right through the **center** of the Sun — not just the halo. Mercury's glow adds brightness on top of Sol's bright body in the framebuffer.\n\n**Root cause — main mechanism (introduced by PR #48)**\n\nThe corrupt+restore pair landed for stellata-3re.19 (orbit-ring occlusion) actively bypasses Sol's depth occlusion at Mercury's screen position:\n\n1. Sol's core mask at renderOrder -4 (opaque queue) correctly writes Sol's depth at Sol's bright core.\n2. Mercury's CORRUPT pass at renderOrder 1.5 (transparent queue) writes `gl_FragDepth = 0.0` across Mercury's body region. The depth test `0.0 \u003c= framebuffer` (default LessEqualDepth) is **always true** because 0.0 is the smallest possible depth value — so the corrupt unconditionally overwrites Sol's depth at Mercury's screen pixels, regardless of whether Mercury is geometrically behind Sol.\n3. Mercury's RESTORE pass at renderOrder 2.5 has `depthFunc: AlwaysDepth` and writes Mercury's actual depth back over the 0.0.\n4. Now framebuffer depth at Mercury's screen position = Mercury's depth, NOT Sol's depth. Sol's foreground occlusion is gone.\n5. Mercury's disc at renderOrder 3 (MaxEquation) and glow at renderOrder 4 (additive) both pass their depth tests against this restored Mercury depth and paint over the Sol-coloured framebuffer.\n\nThe MaxEquation pass on Mercury's dim night-side mostly loses to Sol's bright body, but Mercury's additive glow adds brightness on top — visible exactly as the user described.\n\n**Root cause — secondary mechanism (pre-existing)**\n\nStar halo region (`glow \u003c uCoreThreshold` in star.frag.glsl) writes `gl_FragDepth = 1.0` (far plane). This is *intentional* — it lets distant stars peek through stellar halos without losing the halo's own visual contribution. But the planet glow at renderOrder 4 has the same depth-test pass behaviour (`planet_depth \u003c= 1.0`), so even without the corrupt+restore bypass, Mercury would be visible through Sol's halo annulus. (Mercury orbits at 0.39 AU which projects inside Sol's halo region in screen space at typical Sol-focused viewing.)\n\n**Why this isn't trivially fixed**\n\n- The corrupt+restore pair is needed for the orbit-ring occlusion this PR shipped — can't just remove it without regressing far-side ring hiding at glow regime.\n- Star halo MUST keep writing far-plane depth — load-bearing for distant-star peek-through, dust, MW haze.\n- Removing additive blending from planet glow would change planet brightness behaviour everywhere.\n\nThe right answer is **host occlusion**: explicitly suppress planet rendering when the planet is geometrically behind its host star. Done at the vertex stage, the planet quad collapses to an off-screen sentinel and none of corrupt/restore/disc/glow fire — Sol stays clean.\n\n**Suggested approach**\n\nCPU-side OR vertex-shader host-occlusion check. Vertex-shader is cleaner (no CPU per-frame allocation):\n\n```glsl\n// In planet.vert.glsl, after computing planetView and hostView:\n// Mercury is occluded by host iff:\n//   1. Mercury is farther from camera than host, AND\n//   2. angular separation (from camera) is less than host's angular radius\nfloat d_vp = length(planetView.xyz);\nfloat d_vh = length(hostView.xyz);\nif (d_vp \u003e d_vh) {\n  vec3 dirToPlanet = planetView.xyz / d_vp;\n  vec3 dirToHost = hostView.xyz / d_vh;\n  float cosAng = dot(dirToPlanet, dirToHost);\n  // host's angular radius (small-angle): atan(R_host / d_vh) ~ R_host / d_vh\n  float hostAngRadius = uHostRadiusPc / d_vh;\n  // cos(small angle) ~ 1 - angle^2/2\n  float cosThreshold = 1.0 - 0.5 * hostAngRadius * hostAngRadius;\n  if (cosAng \u003e cosThreshold) {\n    // Planet behind host's angular disc — collapse to off-screen sentinel\n    gl_Position = vec4(2.0, 2.0, 2.0, 1.0);\n    vAppMag = 0.0;\n    vColor = vec3(0.0);\n    vUv = aCorner;\n    vPhysRatio = 0.0;\n    vSoftness = 0.0;\n    return;\n  }\n}\n```\n\nPlumbing required:\n- Add `hostRadiusPc` to PlanetBodyField.attachHost() signature (currently only `hostAbsmag` is tracked).\n- Pipe through from the call site (Sol = ~696,000 km = ~2.26e-8 pc).\n- Store on AttachedHost, write into a per-instance attribute or shared uniform, expose to planet.vert.glsl.\n\n**Acceptance criteria**\n\n- Mercury (and all planets) at superior conjunction (geometrically behind their host star) does not contribute any colour to the framebuffer at the host star's screen position.\n- Specifically: no additive glow brightening the host star's body or halo where the occluded planet would otherwise project.\n- No regression: planets at inferior conjunction (in front of host) still render with the correct phase function (very dim because the camera-facing side is in shadow).\n- No regression in planets visible against open sky (away from host star).\n\n**Smoke step**\n\nFocus Sol and rotate the camera to put Mercury on the far side of Sol (superior conjunction). Confirm Mercury contributes no visible brightness to Sol's body or halo. Repeat at progressive angles as Mercury moves around its orbit — Mercury should fade out as it enters Sol's angular disc and reappear on the other side.\n\n**Found during smoke testing PR #48 (stellata-3re.19)** — the corrupt+restore pair this PR shipped actively introduces this bypass at the CORE level (not just halo). Pre-PR, Mercury was correctly hidden by Sol's depth at Sol's bright core; only the halo bleed was visible.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T15:45:54Z","created_by":"Alex Marshall","updated_at":"2026-05-10T15:50:49Z","dependencies":[{"issue_id":"stellata-2f6.4","depends_on_id":"stellata-2f6","type":"parent-child","created_at":"2026-05-10T16:45:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2f6.3","title":"Hide planet's own orbit ring across its body when zoomed in (3re.19 follow-up)","description":"**Context**\nFiled as a follow-up after the stellata-3re.19 orbit-ring fix landed (PR #48). The PR's corrupt+restore approach correctly hides:\n- Far-side ring segments behind the planet (depth-test fail vs the 0.0 corrupt write)\n- Other planets' rings behind the focused planet (same mechanism)\n- Other planets' near-side rings in front of the focused planet (correctly drawn)\n\nWhat it does NOT hide:\n- The focused planet's OWN orbit ring at the planet's exact orbital tangent — i.e., the segment of the ring that geometrically passes through the planet body at the planet's current position. This segment lives at depth ≈ planet_depth, which at Mercury-class viewing distances is on the order of 1e-7 in log-depth encoding. In a 16-bit depth buffer (1.5e-5 per step) this rounds to 0, indistinguishable from the corrupt pass's gl_FragDepth = 0.0. The depth-test passes (0 \u003c= 0) and the ring fragment draws over the planet's body.\n\nThis is a small visual artefact — a faint ring line bisecting Mercury at its current orbital position, visible only when looking right at the planet.\n\n**Why defer**\nThe artefact is tolerable at typical Sol-focused viewing where the planet is small on screen. It only becomes visually distracting when the user can fly close enough to fill the viewport with the planet body — exactly the affordance stellata-2f6 is about. The fix needs the camera to actually zoom into the planet first; without that affordance, sinking complexity into hiding the artefact is premature.\n\n**Investigation tried (all on PR #48 smoke testing)**\n- vPhysRatio gate: outer-disc occluder never fired at typical viewing because Mercury is in glow regime (vPhysRatio ≈ 0.02). Removed the gate.\n- gl_FragDepth = 0.0 corrupt across full halo: too generous, broke at body-only (user wanted body-only break).\n- gl_FragDepth = 0.0 corrupt + restore at glow \u003e= uCoreThreshold: works for far-side and other planets' rings; FAILS for the orbit's own tangent due to depth precision (the fundamental issue).\n- Stencil-based masking (NotEqual against bit 1 written by the planet body): worked perceptually for the planet but stencil bleed appeared to mask ALL render lines on Mercury, not just the orbit ring. Reverted (commit 0678241 → 66e5bf4 on PR #48). Diagnosis incomplete — the user's report was 'every single orbit ring and render line appears on top of Mercury' which suggests stencil was being read by far more layers than just the orbit ring (e.g. constellation lines, distance vector chevrons, scale bar, focus ring) or the auto-clear-stencil between frames isn't behaving as assumed when other passes also write to it. Worth a clean re-examination from scratch under the zoom-in epic.\n\n**Approach options for the future fix (no commitment)**\n\n1. **Per-ring screen-space discard.** Replace the orbit ring's LineBasicMaterial with a custom ShaderMaterial. Pass per-ring uniforms: own-planet's projected NDC center + body radius in pixels. Fragment shader discards if within radius. Precision-independent, per-ring scoped (no cross-ring bleed). Requires CPU-side mirror of the planet shader's pxSize calculation per frame.\n\n2. **Stencil with cleaner isolation.** Re-attempt the stencil approach but isolate the bit so only the orbit ring reads it (verify no other layer accidentally tests stencil). Per-ring stencil bits (one bit per planet, capped at 8 with standard stencil) if needed.\n\n3. **Depth bias on the planet body's depth-write.** Push the planet's depth slightly toward camera (gl_FragCoord.z - epsilon). Strict-Less depthFunc on orbit ring would then fail equal cases. Bias would have to scale with camera distance — fiddly but precision-correct.\n\n**Acceptance criteria**\n- When the user is focused on a planet body filling \u003e5% of the viewport, the planet's own orbit ring does not visibly bisect the body from any viewing angle.\n- Other planets' rings depth-test normally against the focused planet's body (in front: visible; behind: hidden).\n- No regression in the existing PR #48 behaviour at distant viewing.\n\n**Smoke step**\nAfter zoom-into-planet works, focus Mercury and rotate the camera. Confirm the orbital tangent at Mercury is hidden across the body for all rotation angles, while other planets' rings respect 3D depth normally.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T15:38:38Z","created_by":"Alex Marshall","updated_at":"2026-05-10T15:38:38Z","dependencies":[{"issue_id":"stellata-2f6.3","depends_on_id":"stellata-2f6","type":"parent-child","created_at":"2026-05-10T16:38:38Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.180","title":"Evaluate whether stars benefit from a planet-style outer-disc occluder pass","description":"**Confidence: Low — speculative**\n\n**Context**\nPR #48 (stellata-3re.19) added a fourth render pass to the planet pipeline — an outer-disc depth occluder at renderOrder 1.5 — so far-side orbit-ring fragments depth-fail behind the planet's full visible disc, not just the bright core. The header comment in `planet-body-field.ts` now explicitly calls out the divergence from the star pipeline (3 passes vs 4) and references this follow-up.\n\nThe star pipeline only has a core depth mask. Halo fragments deliberately push `gl_FragDepth = 1.0` so distant stars peek through additively — that behaviour is core to the look and must NOT change.\n\n**Question**\nAre there other layers that depth-test against a star and would benefit from being occluded by the full halo annulus (not just the bright core)? Candidates worth A/B-testing visually:\n\n- Galactic-grid lines (renderOrder = -1, depthTest: true) passing through the soft halo annulus of a foreground star like Sirius. Background layers already depth-fail behind the core mask; the halo annulus currently lets them through.\n- Distance-vector chevrons (SVG overlay — likely *not* affected since it's CPU-projected DOM, but worth confirming).\n- Constellation stick-figure SVG lines passing close to a foreground star (same DOM caveat).\n- The galactic-disc grid loop bleeding through Aldebaran-class halos.\n\n**Likely outcome**\nProbably no — the halo's far-plane depth write is intentional 'pseudo-transparent' behaviour and any layers we'd want to occlude already use SVG/DOM (immune to depth). But the question deserves a 30-minute investigation and a written 'no, because…' so the divergence is settled, not just deferred.\n\n**Suggested approach**\n1. Open the app focused on Sirius / Aldebaran / Vega.\n2. Enable galactic grid + galactic disc.\n3. Visually confirm whether the grid line passes 'through' the halo annulus or correctly disappears behind it. Same for distance-vector when a star sits between camera and target.\n4. Write the verdict in this issue's notes and close. If the answer is 'yes, port it', file an implementation child.\n\n**Smoke step**\nVisual A/B; no code changes for the investigation itself.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:27:40Z","created_by":"Alex Marshall","updated_at":"2026-05-10T14:27:40Z","dependencies":[{"issue_id":"stellata-9mm.180","depends_on_id":"stellata-9mm.189","type":"parent-child","created_at":"2026-05-15T12:53:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.179","title":"getFocusedPlanetLocalPositions returns a stale Float32Array subarray after growCapacity","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\n`PlanetBodyField.getHostLocalPositions(idx)` (planet-body-field.ts:316-324) returns `this.bufLocalRel.subarray(...)` — a view into the underlying typed array. `Stellata.getFocusedPlanetLocalPositions()` (stellata.ts:1056) re-exports it; `planet-labels.ts:86` calls it on every frame. So far so good.\n\nThe risk: `growCapacity()` (planet-body-field.ts:364-391) **replaces** `this.bufLocalRel` with a fresh `Float32Array(newCap * 3)`. Any caller still holding a subarray reference is now reading a stale, never-updated buffer. Today this isn't a live bug — `planet-labels.ts` re-fetches every frame, and Sol-only attaches stay under the initial capacity of 16. But:\n\n- The contract is silent on lifetime (\"Returns a Float32Array slice…\" — no warning that the view becomes stale).\n- bk5 will attach exoplanet hosts dynamically, triggering grows. If any future caller caches the slice across frames (e.g. a debug HUD that polls less often, or a test that grabs the slice and asserts later), it silently reads stale data.\n- The contract also doesn't forbid mutation — \"overlays must not mutate\" is a JSDoc plea, not a Readonly type. A typed Float32Array doesn't have a Readonly variant in TS, but returning a `.slice()` (copy) would force the immutability AND survive growCapacity at the cost of an allocation per call.\n\n**Location**\n`src/client/planet-body-field.ts:316-324` (`getHostLocalPositions`); `src/client/stellata.ts:1056` (`getFocusedPlanetLocalPositions`); `src/client/planet-labels.ts:86` (consumer that re-fetches each frame, hiding the issue).\n\n**Suggested approach**\nTwo options for Alex:\n\n(a) **Document the contract explicitly** — add a doc comment \"WARNING: the returned view is invalidated by capacity grow / detach. Re-fetch per frame; do not cache across frames.\" Lowest cost, no behaviour change.\n\n(b) **Return a `.slice()` (copy)** — survives capacity grow, costs one allocation per call (9 × 4 bytes for Sol = 36 bytes/frame at Sol-only; bk5 scale grows linearly). Trade per-frame allocation pressure for safety. Probably overkill for the current use site.\n\n**Why**\nThe integration-point flag again: planet-labels and a future debug HUD would both consume this method. The current implementation is correct for the current consumer but makes a footgun for the next one. Alex's call.\n\n**Smoke step**\nFor (a): doc-only change, no behavioural test needed. For (b): unit test that calls `getHostLocalPositions(idx)`, triggers a grow via additional attachHost calls, and asserts the original slice still resolves to its correct values (would fail with subarray, pass with slice).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:24:00Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:14Z","labels":["pr-45"],"dependencies":[{"issue_id":"stellata-9mm.179","depends_on_id":"stellata-9mm.189","type":"parent-child","created_at":"2026-05-15T12:53:44Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.176","title":"PlanetBodyField.attachHost uses Date.now()/1000 directly, bypassing the time.ts abstraction","description":"**Confidence: Medium**\n\n**Finding**\n`PlanetBodyField.attachHost()` (planet-body-field.ts:233) initialises planet positions with `this.writeHostPositions(host, Date.now() / 1000)` — reaching into wall-clock directly. Every other read of the time variable goes through `Stellata.getT()` (which is the contract `time.ts` documents in 3re.1: \"Anything driven by ephemerides … reads the live value via Stellata.getT()\").\n\nConcretely this means: if the time-scrubber epic (stellata-nmu) lands `setT(t)` to scrub time backwards/forwards, the very first frame after attach shows planets at WALL-CLOCK position rather than at the scrubbed `t` — they only snap to scrubbed-`t` on the next `update()` tick. Single-frame visual artefact, but its also the only path in the codebase that reads `Date.now()` in service of an ephemeris value. The same file pulls `t` correctly in `update()` (passed in by the caller from `Stellata.getT()`).\n\n`planet-system.ts:215` has the same smell: `getPlanetOrbitOrientations(Date.now() / 1000)` to evaluate the once-per-attach orbital orientation. Sub-arcmin-per-millennium drift makes this defensible, but its still wall-clock-by-name in a module that imports `tToJDE` from time.ts.\n\n**Location**\n`src/client/planet-body-field.ts:233` (`writeHostPositions(host, Date.now() / 1000)`); `src/client/planet-system.ts:215` (`getPlanetOrbitOrientations(Date.now() / 1000)`).\n\n**Suggested approach**\nEither (a) plumb the current `t` value into `attachHost()` as a parameter so the caller (`Stellata.attachHost` site at stellata.ts:944) supplies `this.getT()`; or (b) accept a `t` resolver in the `PlanetBodyField` constructor — `() =\u003e number` — and call it in attachHost. Option (a) is simpler and matches the `update(camera, t)` signature already in place. The orient case in planet-system.ts has the same fix shape.\n\n**Why**\nThe 3re.1 design note explicitly puts `Stellata.getT()` as the single source of truth for ephemeris time. Wall-clock leakage at `attach` time is a small breach of that contract. At Sol-only scale this is invisible (Sol attaches at startup, nothing scrubs `t`), but the bk5 epic will attach exoplanet hosts dynamically (search-result-driven), at which point any future scrubber will produce a per-attach pop.\n\n**Smoke step**\nWith the time-scrubber out of scope for v1, the smoke is: a `setT(t)` test stub before the attach call shows the first iLocalRel frame is computed at `t` not `Date.now()`. Today: typecheck + test, no visual change.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:22:32Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:49Z","started_at":"2026-05-11T07:56:34Z","closed_at":"2026-05-11T08:46:49Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-45"],"dependencies":[{"issue_id":"stellata-9mm.176","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:22:31Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.175","title":"PlanetBodyField.flushAttributeRange marks all 7 buffers dirty per frame even though only iLocalRel changes","description":"**Confidence: High**\n\n**Finding**\n`PlanetBodyField.flushAttributeRange()` (planet-body-field.ts:561-575) sets `needsUpdate = true` on every per-instance attribute (iLocalRel, iHostLocalPos, iRadiusPc, iColour, iSolidity, iAlbedoP, iHostAbsmag) even though the per-frame `update()` path only ever rewrites iLocalRel. The other six are static — written once in `writeHostStaticAttributes()` at attach, never touched again until detach. The PR comment acknowledges this (\"At Sol-only scale this is a 7-attr × 9-instance upload — fast. bk5 may need range-based updates.\") but doesnt narrow the per-frame flush to the one attribute that actually changed.\n\nA second smell on the same function: its parameters are `_start: number, _count: number` (leading underscore = unused), and the body comment notes three.js doesnt support partial-range updates \"without `updateRanges`\" — but `updateRanges` IS the documented three.js way to do partial uploads. The signature lies about what it does (range-based) and the implementation falls back to whole-buffer flush.\n\n**Location**\n`src/client/planet-body-field.ts:561-575` (`flushAttributeRange`); `src/client/planet-body-field.ts:480-496` (where static attributes get written once); `src/client/planet-body-field.ts:296-313` (`update()` only mutates iLocalRel).\n\n**Suggested approach**\nSplit the flush into two helpers: `flushDynamicAttributes()` for per-frame iLocalRel updates and `flushStaticAttributes()` for attach/detach/recenter. The per-frame call from `update()` shrinks to one `needsUpdate = true`. The recenter case writes iHostLocalPos and would call `flushDynamicAttributes()` extended to include iHostLocalPos, or add a third helper. Bonus: switch to `attribute.updateRanges = [{start, count}]` for actual partial uploads at bk5 scale — three.js r152+ supports it.\n\n**Why**\nAt Sol-only scale (9 instances × 7 floats × 4 bytes = ~252 bytes/frame extra) the cost is negligible, but the architectural argument the bead epic cares about is bk5 scale: 100 hosts × 8 planets each = 800 instances × 6 redundant attributes = ~5 MB of static buffer re-uploaded every single frame. Thats not \"may need range-based updates eventually\" — thats a measurable per-frame cost on hardware with constrained bus bandwidth (mobile). The fix is small enough to land before bk5 lights it up.\n\n**Smoke step**\nAfter refactor: confirm `npm run typecheck \u0026\u0026 npm test` clean. In the browser, panel-tune star/planet sliders and verify visual identity vs main. Add a unit test that mocks needsUpdate setters per attribute and asserts `update()` only flips iLocalRel.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:22:09Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:15Z","labels":["pr-45"],"dependencies":[{"issue_id":"stellata-9mm.175","depends_on_id":"stellata-9mm.189","type":"parent-child","created_at":"2026-05-15T12:53:43Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.174","title":"[PR #56] mallamaPhaseFactor lacks defensive α clamp — asymmetric with lambertianPhaseFactor","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\n`lambertianPhaseFactor` clamps α to [0, π] defensively (and has a test for it: `lambertianPhaseFactor(-1)` returns 1). `mallamaPhaseFactor` does not — it converts `alphaRad` to degrees and drops directly into the polynomial Horner expansion if `aDeg ≤ alphaMaxDeg`. The shader is safe (acos returns [0, π]), and the only TS caller that matters routes through view-space geometry that produces a non-negative α. But the function is exported as a top-level utility next to a defensively-clamped sibling — the asymmetry is a quiet API smell.\n\n**Location**\n`src/client/phase-function.ts` lines 76–94 (`mallamaPhaseFactor`).\n\n**Suggested approach**\nTwo options. (a) Add a leading `const a = Math.max(0, Math.min(Math.PI, alphaRad));` to mirror `lambertianPhaseFactor`, plus a parallel test pinning `mallamaPhaseFactor(MARS_PHASE, -1)` and `mallamaPhaseFactor(MARS_PHASE, 4)` (both should equal Lambert at the clamped α). (b) Document in the docblock that callers must pre-clamp; leave the body untouched. Option (a) is the smaller surprise.\n\n**Why**\nDefensive clamping in one function and not its sibling invites \"I'll just call mallamaPhaseFactor — same shape\" mistakes. Worse, the polynomial path silently gives wildly wrong values at large negative or out-of-range α (Horner happily extrapolates), so a misuse fails-loud-but-not-loud-enough.\n\n**Smoke step**\n`npm test` — existing tests pass with the clamp added because they all pass valid α. The new tests (-1 → Lambert(0), Math.PI+1 → Lambert(π)) cover the clamp.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:14:14Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:04:15Z","started_at":"2026-05-10T16:23:32Z","closed_at":"2026-05-10T17:04:15Z","close_reason":"addressed in PR #56 (merged)","dependencies":[{"issue_id":"stellata-9mm.174","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:14:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.173","title":"[PR #56] Mercury c7 truncation has no quantitative test bound","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\n`MERCURY_PHASE` drops Mallama's published `c7 = +6.592e-15` term to fit a degree-6 polynomial in two vec4 attributes per instance. The source comment says \"sub-0.25 mag for α \u003c 120°, ~2.9 mag at α = 170°\" — but no test pins those bounds. `mallamaPhaseFactor` is exercised at α=30° (returns ΔV in (1.0, 1.3)) and over the full validity sweep `for finite/positive`, but neither asserts the truncation error against the reference 7th-order fit. A future edit that further truncates (drops c6 to free a vec4 slot) would silently widen the error, and a bug in the c5/c6 values could pass current tests as long as ΔV stays positive.\n\n**Location**\n`src/client/phase-function.ts` lines 122–131 (Mercury coefficient block); test file `src/client/phase-function.test.ts` (gap — only a single 30° spot-check, plus the all-curves-finite sweep).\n\n**Suggested approach**\nAdd a test that compares `mallamaPhaseFactor(MERCURY_PHASE, α)` to a reference 7th-order evaluation `(0 + 6.617e-2·α + … + 6.592e-15·α^7)` over a sweep, asserting |ΔV_truncated − ΔV_full| stays under 0.25 mag below 120° and under 3.0 mag at 170°. Cheap test (~10 lines, runs over the validity range) and pins the exact assumption the source comment makes. Surfaces a Mercury-specific tradeoff if Alex ever wants to revisit.\n\n**Why**\nThe truncation is a deliberate storage-vs-accuracy trade. Documenting it in prose is necessary but not sufficient — without a test, the tradeoff slowly erodes as future edits forget the original error budget. This is the only Mallama 2018 fit that's not a faithful copy of the published polynomial; the test would make that explicit.\n\n**Smoke step**\n`npm test` — new test passes against current code. As a sanity step, pad the c6 by 10% and confirm the test catches it.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:13:57Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:04:16Z","started_at":"2026-05-10T16:23:32Z","closed_at":"2026-05-10T17:04:16Z","close_reason":"addressed in PR #56 (merged)","dependencies":[{"issue_id":"stellata-9mm.173","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:13:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.172","title":"[PR #56] cullDistancePc + AttachedHost.brightestReflectance docblocks stale after peakPhaseFactor fold-in","description":"**Confidence: High**\n\n**Finding**\nTwo adjacent doc comments in `planet-body-field.ts` describe the cull-distance machinery as if it still operates on `p · (R/a)²`, but the PR now folds `peakPhaseFactor` into that proxy. Specifically:\n\n1. `AttachedHost.brightestReflectance` (line 126): `/** max over planets of `p · (R / a)²` — the geometry-independent reflectance proxy. Drives cullDistancePc. */` — no mention of the c0 boost.\n2. `cullDistancePc` docblock (lines 95–98): `The actual apparent-mag formula has a φ(α) factor in [0, 1], so the real visibility threshold is at d ≤ d_cull. Using d_cull as the outer bound is conservative...` — the [0, 1] claim no longer holds (Saturn's φ(0) ≈ 1.66). The conservative-bound argument still works because peakPhaseFactor is folded into the input, but the explanation is now misleading.\n\n**Location**\n`src/client/planet-body-field.ts` lines 95–98 (cullDistancePc docblock), line 126 (AttachedHost.brightestReflectance JSDoc).\n\n**Suggested approach**\nUpdate the AttachedHost field comment to `max over planets of p · (R/a)² · peakPhaseFactor(coefs)`. Update the cullDistancePc docblock to acknowledge that φ(α) can exceed 1 for bodies with empirical c0 \u003c 0 (Saturn), and that the cull-distance formula stays conservative because the caller folds peakPhaseFactor into the input proxy. Two small comment edits — one commit.\n\n**Why**\nThese two comments are the canonical explanation for why d_cull is a safe outer bound. Once the underlying assumption changes, leaving the comment stale invites a future contributor to \"simplify\" by removing the peakPhaseFactor multiplication on the grounds that φ ≤ 1.\n\n**Smoke step**\n`npm run typecheck` (no behavioural change). Re-read both comments and confirm a fresh reader can derive the cull bound from the new wording.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:13:36Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:04:16Z","started_at":"2026-05-10T16:23:33Z","closed_at":"2026-05-10T17:04:16Z","close_reason":"addressed in PR #56 (merged)","dependencies":[{"issue_id":"stellata-9mm.172","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:13:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.170","title":"hud-overlay/poi-overlay: hideArrow / hideEntry don't reset numeric sentinels — stale lastLabelX/Y survives show/hide cycle","description":"**Confidence: Medium**\n\n**Finding**\n`hud-overlay.ts` `hideArrow` resets only `lastD` (to '') and `lastLabelDisplay` (to 'none'). It does NOT reset `lastLabelX`, `lastLabelY`, `lastLabelText`, `lastOpacity`, or `lastPointerEvents`. After a hide → show cycle (e.g. user defocuses a star then refocuses a different one), the next `updateOne` call may see new label coordinates within `ATTR_DIRTY_PX` of the old (cached, no-longer-displayed) values and skip the setAttribute. The `label.style.display` IS rewritten (sentinel was 'none'), but the `x`/`y` attributes from the previous focal session may persist on the element if the show-from-hide path lands close enough to the previous coordinates.\n\nSame shape in `poi-overlay.ts` `hideEntry` — sets the four display states but doesn't reset the numeric `lastRingCx`, `lastRingCy`, `lastArrowLabelX`, `lastArrowLabelY`, `lastOnScreenLabelX`, `lastOnScreenLabelY`, or text fields. Re-pinning a previously-pinned star at slightly different geometry could see the same dirty-skip.\n\nVisible impact today is small because the old `x`/`y` were what the element was last rendered at, and the new ones are within 0.05 px (otherwise the guard fails and the write happens). So you get an off-by-half-pixel label position relative to where the rest of the overlay puts it. Probably invisible, but it's the regression class the PR flags as \"stale dirty-track state surviving across show/hide cycles.\"\n\n**Location**\nsrc/client/hud-overlay.ts:469-484 (hideArrow); src/client/poi-overlay.ts:189-194 (hideEntry).\n\n**Suggested approach**\nTwo ways to fix:\n\n1. **Wipe-on-hide**: extend hideArrow / hideEntry to reset every numeric sentinel back to its initial value (`-Infinity`, `-1`, `''`, etc). Mechanically simple, mirrors emptyArrowState() / createEntry() defaults.\n\n2. **Wipe-on-show**: when transitioning from hidden → visible (detected via `lastLabelDisplay === 'none' \u0026\u0026 desired === ''`), reset the numeric sentinels before the per-attribute writes so the writes always land.\n\n(1) is the minimum-disturbance fix; (2) is more localised. Either way: extract the reset logic into a single `emptyArrowState()` reuse path so it stays consistent.\n\n**Why**\nThis is exactly the failure mode the PR description calls out (\"stale dirty-track state surviving across show/hide cycles\"). Worth fixing in the same module rather than relying on the visual smoke checks to catch a half-pixel drift.\n\n**Smoke step**\nFocus star A, sit idle one second, defocus, focus star B at a position whose label resolves to within 0.05 px of star A's label position — the label `x`/`y` setAttribute should fire on the show transition, not be skipped by the stale sentinel. Verifiable via DevTools mutation observer on the label element.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:55Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:40Z","started_at":"2026-05-16T10:02:31Z","closed_at":"2026-05-16T21:38:40Z","close_reason":"merged via PR #55 (hideArrow/hideEntry wipe every per-attribute sentinel)","labels":["pr-55"],"dependencies":[{"issue_id":"stellata-9mm.170","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:33Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.167","title":"hud-overlay applyFade: pointerEvents sentinel '' equals desired-state on first apply — first-frame pointer-events write skipped","description":"**Confidence: Medium**\n\n**Finding**\n`hud-overlay.ts` `emptyArrowState()` initialises `lastPointerEvents = ''`. `applyFade` derives `clickable = alpha \u003e= 0.5 ? '' : 'none'` and writes only if `clickable !== state.lastPointerEvents`.\n\nSteady-state alpha is 1, so `clickable === ''` matches the sentinel — `label.style.pointerEvents` is never explicitly written. This is fine because the inline style starts as `''` (the SVG element default is whatever CSS resolves to, and `style.pointerEvents` is the empty string when no inline rule has been applied). So the actual rendered `pointer-events` resolves to whatever CSS matches the label class.\n\nThe bug surface is when CSS is **not** the source of truth for the desired pointer-events behaviour. The pre-PR code unconditionally wrote `label.style.pointerEvents = clickable` every frame, which guaranteed inline-style precedence. The new code skips that write on the alpha=1 path, so any CSS rule that sets `pointer-events` on a parent or via cascade now wins on the first frame after `emptyArrowState` is reset (e.g. after `hideAll`, which doesn't reset `lastPointerEvents`). If the CSS happens to compute to the same effective value, no-op; if it doesn't, behaviour changes.\n\nThe `distance-vector-overlay.ts` clone of this pattern has the same shape (`lastPointerEvents = ''` sentinel + `distUi.style.pointerEvents = pe`). Same risk.\n\n**Location**\nsrc/client/hud-overlay.ts:66 (sentinel), :579-583 (write guard); src/client/distance-vector-overlay.ts:61 (sentinel), :232-236 (write guard).\n\n**Suggested approach**\nUse a sentinel that does not match either of the possible written values — e.g. `lastPointerEvents = '\\0'` or change the type to `string | null` with `null` initial. Either way, force the first write to land. Same fix in both files.\n\nAlternatively: confirm via inspection that no CSS rule writes `pointer-events` on `.gal-arrow-label` / `#dist-ui` / their ancestors, document that, and pin a test or comment. The pre-PR behaviour was \"inline-style always wins\"; the new behaviour is \"inline-style wins after the first non-empty write.\"\n\n**Why**\nThe PR identifies sentinel-init bugs as the highest-risk regression class for this pass. This is one — admittedly subtle, but the kind that survives all six manual smoke checks listed in the PR plan and shows up as a stray click landing on the Sol label during navigate-mode arrow fade only on a specific browser/CSS path.\n\n**Smoke step**\nFocus a star with Sol off-axis, drag the camera until the navigate-mode disc-coverage fade engages (`alpha \u003c 1`), then drag back. Click on the Sol label location while the fade is at `alpha \u003c 0.5` — the click should NOT slerp the camera to Sol (the label should be unclickable). Then drag back to `alpha = 1` and confirm the click DOES slerp. If the second case fails (click does nothing), the sentinel is suppressing the restore-to-clickable write.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:32Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:37Z","started_at":"2026-05-16T10:02:29Z","closed_at":"2026-05-16T21:38:37Z","close_reason":"merged via PR #55 (applyFade pointer-events sentinel poison-init)","labels":["pr-55"],"dependencies":[{"issue_id":"stellata-9mm.167","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:34Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.165","title":"[PR #54] No test asserting the headline 10-char cam=[0,0,3.7] URL — only \u003c=12 char upper bound","description":"**Confidence: Medium**\n\n**Finding**\nThe PR summary leads with \"the canonical near-Sol orbit URL (?v= for cam=[0,0,3.7]) drops from v2's 22 base64url chars to **10**\". This is the bead's headline win, mentioned in three places (PR title implicit, summary, release notes). The test at url-state.test.ts:134-146 asserts `expect(blob.length).toBeLessThanOrEqual(12)` — i.e. \"≤ 12\", not \"= 10\".\n\nThe encoder's actual byte count for cam=[0,0,3.7] is: 1 ver + 1 mask + 1 sub + 4 z = 7 bytes → ceil(7\\*4/3) = 10 base64url chars. A future change that flips a default constant or adds an outer-mask byte would silently regress the headline win to 11 or 12 chars without tripping the test.\n\n**Location**\nsrc/client/url-state.test.ts:134-146.\n\n**Suggested approach**\nTighten to `expect(blob.length).toBe(10)`. Add a parallel assertion that `blobBytes(blob) === 7` for the exact wire-byte count, mirroring the LEB128 block's exact-equality style at lines 836-857.\n\n**Why**\nThe looser `toBeLessThanOrEqual` only catches a regression that *grows* past 12 chars. The PR is sold on a specific number; the test should pin that number.\n\n**Smoke step**\n`npm test` — the assertion is a regression guard with the exact win.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:29Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:35Z","started_at":"2026-05-10T17:50:45Z","closed_at":"2026-05-10T18:17:35Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.165","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:12:29Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.164","title":"WarpState.pEnd is mutated mid-animation; frame ownership of pStart/A/dirBack/dir0 is implicit","description":"**Confidence: Medium**\n\n**Finding**\nPR #50 mutates `state.pEnd` in-place at phase-3 start to convert it from the source-local frame to the destination-local frame. The other position-typed fields on `WarpState` — `A`, `pStart`, `dir0`, `dirBack` — are NOT shifted; they are still in the source-local frame for the rest of the warp. Today this is benign because `A` and `pStart` are only read in phases 1 and 2 (before the recentre runs) and `dir0` / `dirBack` are unit vectors so frame-invariant. There's no comment, no assert, and no type-level signal warning a future reader that 'after phase 3 first frame, only `pEnd` and unit vectors are valid; positional fields are stale'. A future feature that reads `state.A` or `state.pStart` during phase 3 / finishWarp would land subtly off by exactly the recenter delta.\n\n**Location**\nsrc/client/stellata.ts:338-384 (WarpState interface) and src/client/stellata.ts:3679-3681 (the in-place mutation)\n\n**Suggested approach**\nTwo options. (1) Shift the positional fields too in the same block — three more triples of `-= dx/dy/dz`. Cheap, makes the whole struct frame-coherent, eliminates the gotcha. (2) Document the invariant on the WarpState interface and on the mutation site: a comment block calling out which fields are valid in which frame after phase 3. Option (1) is more robust; option (2) is cheaper and acknowledges that today's readers don't need them.\n\n**Why**\nThe PR's correctness rests on no other code reading `state.pStart` / `state.A` after phase 3 begins. That's a non-local invariant — anyone touching updateWarp or finishWarp later has to know it without a signpost. Either fix it or sign-post it.\n\n**Smoke step**\n1. Apply chosen option.\n2. Existing manual smoke from the PR test plan still passes (observe→observe long warp, no jitter; skip works in all phases).","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:28Z","created_by":"Alex Marshall","updated_at":"2026-05-10T20:02:07Z","started_at":"2026-05-10T18:24:57Z","closed_at":"2026-05-10T20:02:07Z","close_reason":"merged in PR #50 (commit 783d396, v1.6.1)","labels":["pr-50"],"dependencies":[{"issue_id":"stellata-9mm.164","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:12:28Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.163","title":"EventBus.EmitArgs uses 'extends void' which traps if a payload type ever includes void","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nThe bus's varargs typing is `type EmitArgs\u003cM, K\u003e = M[K] extends void ? [name: K] : [name: K, payload: M[K]]`. This is correct for the current map (only `'state'` and `'frame'` use `void`), but `extends void` is a subtle tool: any union that *includes* `void` (e.g. a future event `pendingChange: void | { id: number }`) would match the no-payload branch and the compiler would refuse the payload. More common conventions for 'absent payload' are sentinel object types (`undefined`) or a separate map for void events.\n\n**Location**\n`src/client/event-bus.ts:7-9`\n\n**Suggested approach**\nEither (a) add a one-line comment explaining the `extends void` choice plus a guardrail comment 'do not introduce `void | T` payloads to the map — use `T | undefined` and emit explicit undefined.' Or (b) switch the convention to `undefined`-payload events (`state: undefined; frame: undefined`) with a slightly different conditional type. (a) is the cheaper docs-only fix; (b) is closer to how a TypeScript-first codebase would idiomatically express it.\n\n**Why**\nThis is exactly the kind of typing-soundness footgun the migration was supposed to retire (a typo in an event name is now a compile error — this is the next concentric ring of correctness). Documenting it is enough; rewriting the conditional is judgment.\n\n**Smoke step**\nNone for option (a) — comment-only. For option (b), `npm run typecheck` + `npm test` + spot-check the existing `bus.emit('frame')` call sites still compile.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:27Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:17Z","labels":["pr-51"],"dependencies":[{"issue_id":"stellata-9mm.163","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:20Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.161","title":"[PR #54] currentStateOf cam-elision still uses approx() while vec3FieldV3 uses strict ===; encoder layer split","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nThere are now **two** cam-elision predicates running on each URL write, with different epsilons:\n- `currentStateOf` (lines 938-941, 942-944, 945-947) decides whether to populate `view.cam / view.tgt / view.up` based on `!approx(c.x, camDefault[0]) || ...` (epsilon = 1e-3).\n- `vec3FieldV3.isPresent` (line 257-262) re-checks the same view with strict `===` and may then declare the field absent in the presence mask.\n\nIf `currentStateOf` populates view.cam with a value 5e-4 from default (so `approx` is true → would be omitted) — wait, the `approx` is `||` so any divergence keeps it. The interaction is: `currentStateOf` may populate view.cam even when all components are within EPS of default; vec3FieldV3 then re-checks at strict equality and may still emit a sub-mask + 4 bytes for components that are 5e-4 away from default. This is fine for floating-origin sub-µpc (`!== 0`) but means `currentStateOf`'s population check is partly redundant with the field's isPresent and `worldOffset === non-Sol` triggers force-emit at line 939, which is a third predicate.\n\n**Location**\nsrc/client/url-state.ts:927-947 (currentStateOf); 257-262 (vec3FieldV3.isPresent).\n\n**Suggested approach**\nTwo layers of cam-elision are not obviously wrong (the outer `approx` cheaply skips the small-noise case before the strict `!==` runs), but the comment block at 904-921 describes the contract verbally and the predicates aren't unified. Consider: collapse to one predicate (let vec3FieldV3.isPresent be the only gate; populate view.cam unconditionally as the raw float and let isPresent decide). Or document the layered design explicitly. Alex's call on which layer should own omission.\n\n**Why**\nThe PR comment at 226-233 explicitly motivates strict equality for sub-µpc float-origin cam values. `currentStateOf` running `approx` first means a cam at 5e-4 pc from default would be `approx`-equal to default, populated, then strict-emitted — but a cam at 1e-6 pc (the floating-origin case) would also be approx-equal-to-default, populated, then strict-emitted with a sub-mask + 4 bytes. So both paths produce the right wire; the question is whether the redundant approx layer earns its keep or just confuses.\n\n**Smoke step**\n`npm test` — floating-origin sub-µpc round-trip tests at lines 96-118 still pass.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:14Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:36Z","started_at":"2026-05-10T17:50:44Z","closed_at":"2026-05-10T18:17:36Z","close_reason":"Resolved via documentation in PR #54: added a comment to currentStateOf explaining the two-layer (outer 1e-3 approx + inner strict ===) elision. Inner strict equality is load-bearing for floating-origin sub-µpc cam (a7d.2.11), so layers can't be collapsed. File a new bead if the actual predicate-collapse is wanted later.","dependencies":[{"issue_id":"stellata-9mm.161","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:12:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.158","title":"scale-bar.ts uses whole-frame signature dirty-track; new pass uses per-attribute — divergent strategies in same overlay layer","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\n`scale-bar.ts` uses a coarse-grained dirty-track strategy: it builds a single per-frame signature string (`sig = 'nav|...'`) at the top of `onFrame` and bails out if it matches `lastSig`. When the signature differs, it redraws everything (every line, every text node) unconditionally. This is more aggressive than the per-attribute pattern the PR adopts: a stationary camera in NAVIGATE-mode redraws zero attributes (signature matches), and a moving camera redraws all attributes (the inner setLine helper has no dirty guards).\n\nThe newly-touched overlays (constellation, disc-mask, distance-vector, hud, poi) use per-attribute dirty-tracking instead — they always run the full per-frame computation, then skip individual setAttribute calls when the values match. This is *strictly worse than* scale-bar's approach when the signature happens to match (more JS work) but *better* when one attribute changed and the others didn't (no early-exit blocks the partial reuse).\n\nEither strategy is defensible; having both in the overlay layer is not. After this PR there's no guidance for the next overlay's author on which to choose.\n\n**Location**\nsrc/client/scale-bar.ts:93, :111-113, :167-169, :471-476 (whole-frame signature, no per-attribute guards). vs. the five-overlay per-attribute pattern in this PR.\n\n**Suggested approach**\nTwo reasonable resolutions:\n\n1. **Codify the choice**: add a one-paragraph note to docs/overlays.md (or docs/performance.md) on when to use signature-vs-per-attribute, with examples. Signature wins for overlays where every attribute is keyed off the same small set of inputs (scale-bar: bar pixel count + label string drive everything). Per-attribute wins for overlays where independent attributes can change at different rates (hud-overlay: ring radius + arrow path + label all independent).\n\n2. **Migrate scale-bar to per-attribute**: ~30 setLine + 6 setAttribute callsites; mechanical. Removes the divergence at the cost of some redraw cost on the moving-camera path (which is already cheap relative to scale-bar's geometry math).\n\n(1) preserves scale-bar's signature optimisation and is cheaper; (2) is the consistency answer.\n\n**Why**\nEngineering manager review priority: \"Architectural fit\" — having two ways to dirty-track in the same overlay layer makes the next overlay author guess. A documented choice closes that. (Per-attribute is what the PR scaled to five files; signature might still be the better fit for scale-bar; documenting which-when avoids re-debating it next overlay.)\n\n**Smoke step**\nEither an updated docs/overlays.md section that the next reviewer can point at, or a scale-bar diff that shows zero behavioural change for the no-camera-movement case.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:06Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:18Z","labels":["pr-55"],"dependencies":[{"issue_id":"stellata-9mm.158","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:30Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.157","title":"[PR #48] buildMaterials' four ShaderMaterial constructors duplicate boilerplate","description":"**Confidence: Medium**\n\n**Finding**\n`PlanetBodyField.buildMaterials` (lines 437-500) now constructs four `THREE.ShaderMaterial` instances. They all share `glslVersion: THREE.GLSL3`, `vertexShader: planetVert`, `fragmentShader: planetFrag`, and `uniforms: { ...sharedPlanetUniforms, uRenderMode: { value: N } }`. Three of them set `transparent: true`. The differences are: blending (default vs `AdditiveBlending` vs `CustomBlending` via `applyDiscBlendDefaults`), depthWrite, depthTest, colorWrite — i.e. the per-pass behaviour. The shared scaffolding repeats four times. Adding a fifth pass (e.g. for chart-mode flat planets, or for planet labels' depth-occlusion) would multiply the boilerplate further.\n\n**Location**\nsrc/client/planet-body-field.ts:437-500\n\n**Suggested approach**\nExtract a small factory:\n\n```ts\nfunction makePlanetMat(\n  mode: number,\n  shared: object,\n  per: Partial\u003cTHREE.ShaderMaterialParameters\u003e,\n): THREE.ShaderMaterial {\n  return new THREE.ShaderMaterial({\n    glslVersion: THREE.GLSL3,\n    vertexShader: planetVert,\n    fragmentShader: planetFrag,\n    transparent: true,\n    uniforms: { ...shared, uRenderMode: { value: mode } },\n    ...per,\n  });\n}\n```\n\nEach pass becomes one line of intent: `makePlanetMat(3, sharedPlanetUniforms, { depthWrite: true, colorWrite: false })`. The disc-pass `applyDiscBlendDefaults` call stays separate (it's a multi-field mutation that would lose semantic clarity if folded in).\n\n**Why**\nDRY for a four-pass shape with shared scaffolding; bk5 / chart-mode might add a fifth, and the factory keeps the per-pass behaviour visible at a glance.\n\n**Smoke step**\n`npm test src/client/planet-body-field.test.ts` (renderOrder test still passes); `npm run typecheck`.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:04Z","created_by":"Alex Marshall","updated_at":"2026-05-10T16:12:37Z","started_at":"2026-05-10T14:24:56Z","closed_at":"2026-05-10T16:12:37Z","close_reason":"Shipped in PR #48 (stellata 1.4.5).","dependencies":[{"issue_id":"stellata-9mm.157","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:12:04Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.156","title":"EventBus tests don't cover mid-emit registration ordering","description":"**Confidence: Medium**\n\n**Finding**\n`event-bus.test.ts` has six cases: delivery, no-payload, unsubscribe, dedupe, no-subscribers, mid-emit unsubscribe. The seventh corner case — a handler registering a *new* handler mid-emit — is not pinned. JS `Set` iteration semantics include items added after iteration starts, so the new handler will fire in the same emit round (in insertion order, after all currently-iterating items). This is the implementation-defined behaviour; nothing in the bus code or test suite documents which way it goes, so a future refactor (e.g. snapshotting the Set into an array before iterating, which we'd want anyway if emit became re-entrant under flag) would silently flip the contract.\n\n**Location**\n`src/client/event-bus.test.ts` (test suite — gap)\n\n**Suggested approach**\nAdd a single `it('runs handlers added mid-emit in the same round', ...)` (or `'defers handlers added mid-emit until the next emit'` if Alex wants the opposite contract). Pinning either direction makes the contract intentional. Cheap test, cheap insurance.\n\n**Why**\nThe mid-emit unsubscribe test already exists, so the suite is half-aware of mutation-during-iteration. Adding the registration sibling completes the corner-case coverage and prevents a refactor from breaking re-entrant emit users. Same intent as PR #51's other test additions: pin the spec.\n\n**Smoke step**\n`npm test` passes the new case.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:01Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:15:14Z","started_at":"2026-05-15T12:05:29Z","closed_at":"2026-05-15T12:15:14Z","close_reason":"Folded into PR #51 (commits 00f39b0, 11d300a, 48e850f)","labels":["pr-51"],"dependencies":[{"issue_id":"stellata-9mm.156","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:19Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.154","title":"[PR #56] aOff and bOff are identical in writeHostStaticAttributes — collapse","description":"**Confidence: High**\n\n**Finding**\n`writeHostStaticAttributes` declares two named offsets `aOff` and `bOff` whose initialisers are byte-for-byte identical (`baseVec4 + i * 4`). The naming suggests they index into different layouts within the same instance slot, but they don't — `bufPhaseA` and `bufPhaseB` are separate Float32Arrays that both happen to be vec4-shaped, so the per-slot offset is the same. Reader has to compute the equality by hand to be sure no off-by-one is hiding.\n\n**Location**\n`src/client/planet-body-field.ts` lines 522–531 (the write-block inside `writeHostStaticAttributes`).\n\n**Suggested approach**\nCollapse the two locals into a single `const off = baseVec4 + i * 4` (or just `const off = (baseScalar + i) * 4` for parity with the other offsets) and use it for both `bufPhaseA` and `bufPhaseB` writes. Drops the cosmetic distinction that hints at a layout that doesn't exist. Optionally, the `pc ? pc.cN : 0` repetition (8 lines) could collapse via a tiny helper or a default-zero `PhaseCoefficients` constant in `phase-function.ts` — but the offset collapse is the higher-signal win.\n\n**Why**\nDRY/elegance — repeated identical computations with different names are a low-grade reader trap. They're the kind of thing a future edit accidentally desyncs (someone adjusts `aOff` for a re-pack and forgets `bOff`). One name forecloses that possibility.\n\n**Smoke step**\n`npm test` — the existing 14 lifecycle tests in `planet-body-field.test.ts` exercise attach / detach / grow / recenter / setMaxAppMag and indirectly exercise this write path; pass means the collapse didn't change behaviour.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:54Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:04:17Z","started_at":"2026-05-10T16:23:33Z","closed_at":"2026-05-10T17:04:17Z","close_reason":"addressed in PR #56 (merged)","dependencies":[{"issue_id":"stellata-9mm.154","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.152","title":"[PR #54] applyDecodedView uses defaultCamForMode in worldOffset branch but spreads OBSERVE_CAM_LOCAL elsewhere — pick one","description":"**Confidence: Medium**\n\n**Finding**\nInside `applyDecodedView` (lines 1069-1095) two adjacent branches express the same idea differently:\n- Line 1071: `const camDefault = defaultCamForMode(view.mode); stellata.camera.position.set(camDefault[0], camDefault[1], camDefault[2]);`\n- Line 1093: `stellata.camera.position.set(...OBSERVE_CAM_LOCAL);` — only fires when `willEnterObserve`, so functionally identical to `defaultCamForMode('observe')` but written via a constant spread.\n\nBoth expressions ultimately resolve to the same vector when mode=observe. The mixed style is small but inconsistent: one site looks the value up via the helper, the other hard-codes the const. A future change to OBSERVE_CAM_LOCAL would touch one site; a change to defaultCamForMode would touch the other; a maintainer reading either branch can't tell at a glance whether the two are coupled.\n\n**Location**\nsrc/client/url-state.ts:1069-1095 (applyDecodedView, worldOffset + willEnterObserve branches).\n\n**Suggested approach**\nUse `defaultCamForMode` consistently for both call sites, or extract a tiny helper `setCameraToDefault(stellata, mode)` and invoke it from both. The cam-default invariant is now defined in one place (`defaultCamForMode`) and there is no value in the OBSERVE_CAM_LOCAL spread except as a way to write `[0,0,0]` symbolically — but `defaultCamForMode('observe')` already returns it.\n\n**Why**\nClosed bead 9mm.20/9mm.24's whole point was to give the cam-omission invariant a single name; the leftover constant spread at line 1093 is the last place that doesn't follow the rule.\n\n**Smoke step**\n`npm test` — the mode-aware cam-fix-up tests at url-state.test.ts:155-178 still pass.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:52Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:34Z","started_at":"2026-05-10T17:50:44Z","closed_at":"2026-05-10T18:17:34Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.152","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:51Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.151","title":"[PR #48] Star and planet pipelines now diverge — star has 3 passes, planet has 4","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nBefore PR #48 the planet pipeline mirrored the star pipeline 1:1: core depth-mask + disc + glow, same uRenderMode 2/1/0, same gates, same gl_FragDepth = 1.0 halo trick. The PR header comment in `planet-body-field.ts` previously said \"three materials share the geometry (same pattern as the star pipeline)\". After the PR, planets have an outer-disc occluder (uRenderMode == 3) that stars do not — divergence with no equivalent in `star.frag.glsl`. There is no doc capturing whether the divergence is deliberate (planets need to occlude orbit rings; stars don't have orbit rings to occlude) or whether the same fix should be ported to stars (e.g. stars vs galactic-disc gridlines, stars vs distance-vector chevrons).\n\n**Location**\nsrc/client/shaders/planet.frag.glsl:62-75 (new uRenderMode == 3 branch); src/client/planet-body-field.ts:25-34 (header comment); compare with src/client/shaders/star.frag.glsl:116-127.\n\n**Suggested approach**\nDecide whether the divergence is permanent or transitional. If permanent: add a sentence to the planet-body-field.ts header comment explicitly calling out \"unlike stars, planets carry a fourth pass because orbit rings need depth-occlusion across the full disc, not just the bright core.\" If transitional: file a follow-up to evaluate whether the star pipeline benefits from the same trick (do any layers depth-test against a star's halo annulus and want to be occluded? — galactic grid lines passing through Sirius, distance-vector chevrons through a foreground star, etc.).\n\n**Why**\nThe 3re epic explicitly chose the star pattern for planets (\"three-pass parity with stars\" in docs/solar-system.md). Breaking that parity without explanation degrades the cross-pipeline mental model that makes future shader edits cheap.\n\n**Smoke step**\nEither a comment-only update (`grep -A 5 'Three materials' src/client/planet-body-field.ts` confirms updated wording) or — if porting the trick — manual A/B against galactic gridlines passing through Sirius's halo.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:48Z","created_by":"Alex Marshall","updated_at":"2026-05-10T16:12:38Z","started_at":"2026-05-10T14:24:57Z","closed_at":"2026-05-10T16:12:38Z","close_reason":"Shipped in PR #48 (stellata 1.4.5).","dependencies":[{"issue_id":"stellata-9mm.151","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.150","title":"StellataEventMap doesn't document fine-grained vs catch-all 'state' relationship","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nThe `StellataEventMap` declaration (stellata.ts:294) has a one-line comment 'subscribers register via `Stellata.on(name, fn)` and the compiler enforces the payload type per event. `state` and `frame` are no-payload events.' What's *not* captured: every fine-grained mutation (`focus`, `vector`, `vectorCloud`, `cloudFocus`, `pois`, `filter`, `cameraMode`) emits both its specific event AND `state`, while `warp` and `planetSystem` emit only their specific event (warp emits state via `fireStateChange` after, planetSystem does not). `docs/architecture.md` documents this for `state` ('fires on any discrete state mutation') but the rule is split across the two locations and the asymmetry isn't explicit anywhere.\n\n**Location**\n- `src/client/stellata.ts:291-306` — StellataEventMap declaration\n- `docs/architecture.md` §'Event bus on Stellata'\n\n**Suggested approach**\nEither: (a) extend the StellataEventMap doc-comment with a one-paragraph 'who emits what alongside state' table and the rationale (state is the URL-sync trigger, frame is the render-tick fanout, fine-grained events are for UI subscribers that want only their slice). Or: (b) introduce a typed wrapper at the Stellata level (e.g. `fireStateChange(emit?: keyof Map)`) that makes the pairing explicit at the call sites. (a) is cheaper; (b) is more invasive but ironclad.\n\n**Why**\nThe 'every subscriber wired across the codebase' integration surface is the thing the PR tests don't cover (per Alex's large-PR-honesty framing). The map is now the canonical spec for that surface — anything implicit about its semantics should be moved into the type or the comment so future-Claude doesn't have to grep `fireStateChange` to discover the contract.\n\n**Smoke step**\nDoc-only change: re-read `docs/architecture.md` after the patch and confirm the relationship between fine-grained events and `'state'` is unambiguous from the doc alone.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:47Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:19Z","labels":["pr-51"],"dependencies":[{"issue_id":"stellata-9mm.150","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:21Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.147","title":"disc-mask scope narrower than stellata-rmo's repro: companions of constellation vertices still leak when their disc renders","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nstellata-rmo's repro reads: 'Focus a constellation member star where a stick-figure line passes behind it … unfocus the star … same line segments now render on top of the disc.' PR #52 fixes that exact case for the *vertex* itself (every vertex of the highlighted constellation gets a cutout). It does NOT fix the analogous case where a *binary companion of* a vertex is what the camera is parked near: e.g. focus Mizar (vertex), unfocus, camera still close → Mizar gets masked (it's a vertex), but if the user instead clicks Alcor (Mizar's companion, may or may not itself be a vertex depending on Stellarium's asterism for Ursa Major) and unfocuses, only Alcor's relationship to its own `companion` field would mask it — and `companion` is directed (per scripts/catalog-pure.ts:420 'A's nearest may not be B's nearest'). Anything close enough to render a disc but not a vertex and not focused will still paint lines on top.\n\n**Location**\nsrc/client/disc-mask.ts:104-114 (constellation iteration). Compare scripts/catalog-pure.ts:420-440 (directedness of `companion`). The PR's bug-target stellata-rmo describes the broader class.\n\n**Suggested approach**\nTwo options. (1) Document scope: add a sentence to the file's top comment naming what's *not* covered ('A close star that is neither focal, focal's companion, nor a vertex of the highlighted constellation will not be masked. In practice such a star is rare — the user has to deliberately park near a non-vertex, non-companion star.'). (2) Extend coverage: scan a small window of the catalog by squared distance to the camera each frame (top-K nearest, K=8 or so) and run them through tryPlace. K nearest neighbours of the camera is the architecturally correct generalisation; bounded scan keeps it cheap. Option (1) is a one-line doc change; option (2) is more code but closes the rmo class of bugs entirely.\n\n**Why**\nThe PR title and body claim to fix stellata-rmo, but the fix is keyed to constellation-highlighted iteration. Scope-narrowing fixes are common and fine — but they should be documented so a future reader doesn't read the title and assume the broader bug is closed. Filing low-confidence because Alex may have deliberately scoped to vertices given the lines-not-rendering-without-highlight observation (no constellation highlight ⇒ no lines ⇒ nothing to paint over a disc, so the scope IS the bug class in practice).\n\n**Smoke step**\nFor (1): doc-only. For (2): focus Mizar, unfocus, manually drag camera near a known non-vertex non-companion close star (e.g. one of the ~313k catalog members not in a Stellarium asterism), confirm its disc gets masked.","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:37Z","created_by":"Alex Marshall","updated_at":"2026-05-11T07:44:36Z","started_at":"2026-05-10T22:06:07Z","closed_at":"2026-05-11T07:44:36Z","close_reason":"Closed","labels":["pr-52"],"dependencies":[{"issue_id":"stellata-9mm.147","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:37Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.147","depends_on_id":"stellata-rmo","type":"related","created_at":"2026-05-10T15:11:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.145","title":"pickStar two-tier prime-vs-fallback priority untested at integration level","description":"**Confidence: High**\n\n**Finding**\n`pickScore` is unit-covered with three regression tests (Double Double, Alula coincident pair, sub-pixel mag bias). What's NOT tested is the surrounding two-tier control flow in `pickStar`: the contract that 'prime hits always beat fallback hits' (line 2804: `return discIdx !== -1 ? discIdx : fbIdx`), and that the prime tier uses `hitRadius = max(pxSize/2, MIN_DISC_HIT_RADIUS_PX)` while the fallback uses `pixelThreshold`. PR description acknowledges this gap explicitly: 'The user-visible effect (clicking the right star on the canvas) needs manual smoke since pickStar lives in the Three.js scene path.' The integration is exactly where the original au3/xec bugs lived — a unit-tested pure helper plus an unverified integration is the same shape as 'tested the lemma, deployed the theorem'.\n\n**Location**\nsrc/client/stellata.ts:2720-2805 (`pickStar`)\nsrc/client/star-geometry.test.ts:163-194 (existing pickScore tests, scope-bounded to the helper)\n\n**Suggested approach**\nExtract the inner-loop pick reducer to a pure function in `star-geometry.ts`: input is an iterable of `{idx, pxDist, hitRadius, appMag, inFilter}` plus the fallback `pixelThreshold`; output is the winning idx or -1. Then `pickStar` becomes 'project + filter + collect candidates' followed by 'pickFromCandidates(candidates, pixelThreshold)'. The reducer is then unit-testable with synthetic candidate arrays covering: prime-only, fallback-only, mixed prime-and-fallback (prime wins), tied scores in prime tier (same as Alula), prime hit just inside hitRadius vs fallback hit just under pixelThreshold (prime wins regardless of score). No Three.js scene needed.\n\n**Why**\nThis is the same DRY pattern that PR #53 already applied to `pickScore` (lifting the score formula out of the inline body). The inner reducer is one level up: the same lift makes the priority semantics testable.\n\n**Smoke step**\nAdd a test `describe('pickFromCandidates')` with 5+ scenarios. All must pass without rendering a Three.js scene. Manual smoke per the PR description still required for the projection layer (camera projection + viewport math), but the priority logic itself is pinned.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:35Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:59:09Z","started_at":"2026-05-10T20:09:17Z","closed_at":"2026-05-10T21:59:09Z","close_reason":"Shipped in PR #53 (merge commit 63da7a6, v1.6.2)","labels":["pr-53"],"dependencies":[{"issue_id":"stellata-9mm.145","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:34Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.144","title":"[PR #54] No round-trip test for the bit-21 t field (Float64); decoders handle it but encoder gate is untested","description":"**Confidence: Medium**\n\n**Finding**\nBit 21 (`t`, scrubber-pinned wall-clock) appears in `FIELDS_V2` (lines 593-598) and `FIELDS_V3` (lines 672-677) as `fixed(8)` Float64 LE. `currentStateOf` gates emission on `!isLive(tNow)` (line 953). The PR test plan and code comments both note this is wired but never emitted in v1 — emission flips on with stellata-nmu.\n\nThere is no test that `encodeBlob({ t: 1234567890.123 })` round-trips through `decodeBlob` and produces the same value. The bit-21 path is genuinely dead code today (encoder never emits) but the decoder will run on stellata-nmu's first emission, and a regression in the float64 LE write/read pair would manifest only at that point.\n\nA v3 blob built by hand with bit 21 set (analogous to the v1/v2 backward-compat tests) would also catch a future change that resizes the field.\n\n**Location**\nsrc/client/url-state.ts:672-677 (FIELDS_V3 t spec); src/client/url-state.test.ts (no test).\n\n**Suggested approach**\nAdd a test under the v3 round-trip block: `encodeBlob({ t: 1234567890.123 })` → decode → assert `view.t === 1234567890.123` exactly (Float64 round-trips losslessly). Also assert that `encodeBlob({ t: undefined })` does not occupy bit 21 (presence guard).\n\n**Why**\nThe bit-21 t-field's purpose is in cross-PR territory — it ships in this PR's wire format but is activated by a different epic (stellata-nmu). Test coverage now is cheap insurance against a regression that would otherwise surface as a wrong wall-clock during share-link load.\n\n**Smoke step**\n`npm test` runs the new t-field round-trip test.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:32Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:34Z","started_at":"2026-05-10T17:50:43Z","closed_at":"2026-05-10T18:17:34Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.144","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:31Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.141","title":"[PR #48] orbit-rings-layer.ts renderOrder ladder comment omits heliopause, dust, clouds","description":"**Confidence: Medium**\n\n**Finding**\nThe renderOrder ladder comment in `orbit-rings-layer.ts` constructor (lines 240-248) is touched by PR #48 to add the new 1.5 outer-disc layer. It still omits other layers that sit at relevant points on the ladder:\n- molecular clouds at -2 (`molecular-clouds.ts:71`)\n- heliopause at 1 (`heliopause.ts:97`) — same value as star glow\n- dust particles at 2 (`stellata.ts:1742`) — same value as orbit rings\n\nThe new ladder reads as if star glow (1) is the only thing at 1 and orbit rings (2) is the only thing at 2.\n\n**Location**\nsrc/client/orbit-rings-layer.ts:240-248\n\n**Suggested approach**\nSingle-edit comment update — add the missing entries:\n\n```\n//   -4   star + planet core depth masks\n//   -3   Milky Way / galactic disc\n//   -2   molecular clouds\n//   -1   galactic disc grid\n//    0   star discs\n//    1   star glow, heliopause shell\n//    1.5 planet bodies — outer-disc depth occluder\n//    2   orbit rings (this layer), dust particles\n//    3   planet bodies — disc pass\n//    4   planet bodies — glow pass\n```\n\nEither fix all of them or pick one canonical location for the full ladder (e.g. `docs/rendering.md`) and stop duplicating partial copies.\n\n**Why**\nThis file's ladder is one of the few \"complete\" inline references for the renderOrder hierarchy and is touched by every PR that adds a layer. Letting it drift means each new contributor learns half the layout from one file's comment.\n\n**Smoke step**\nAfter edit: `grep -rn 'renderOrder ' src/client/ | grep -v '\\.geometry\\.\\|frustumCulled' | sort` should agree with the updated ladder comment.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:27Z","created_by":"Alex Marshall","updated_at":"2026-05-10T16:12:38Z","started_at":"2026-05-10T14:24:58Z","closed_at":"2026-05-10T16:12:38Z","close_reason":"Shipped in PR #48 (stellata 1.4.5).","dependencies":[{"issue_id":"stellata-9mm.141","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.140","title":"Heliopause geometry block duplicated verbatim in SCIENCE.md and docs/solar-system.md","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nThe heliopause-construction recipe (122 / 115 / 200 AU + apex direction + \"unit sphere → scale (115,115,161) → offset 39 AU toward antiapex → rotate so +Z lands on antiapex\" → result \"+122 / -200 along apex\") appears nearly verbatim in two places authored by this PR:\n\n- SCIENCE.md \"Heliopause boundary\" section (under \"## Solar system\")\n- docs/solar-system.md \"Heliopause boundary\" section\n\nBoth are added in PR #38, ~10 lines of identical numerical / construction prose. The two-doc convention (CLAUDE.md says SCIENCE.md = data sources + physics, docs/\u003ctopic\u003e.md = code-level constraint surface) is sound, but the construction recipe — which is implementation detail (sphere scale, offset, rotation) — sits more naturally in docs/solar-system.md while SCIENCE.md should hold only the citation-bearing measurements (122/115/200 AU + apex direction).\n\n**Location**\n- SCIENCE.md (PR #38 add) \"Heliopause boundary\" subsection\n- docs/solar-system.md \"Heliopause boundary\" section\n\n**Suggested approach**\nTrim SCIENCE.md to the four cited measurements + a one-line \"Construction details: see `docs/solar-system.md` § Heliopause boundary.\" docs/solar-system.md keeps the full construction recipe. Single source of truth for the construction details means the next physical-asymmetry tweak edits one file, not two.\n\n**Why**\nFirst edit will drift the two copies — a numerical change at one site that doesn't propagate to the other turns the \"ground truth\" SCIENCE.md into a stale reference. Same DRY argument as the cross-doc citations elsewhere in the project.\n\n**Smoke step**\nDiff the two paragraphs verbatim; confirm SCIENCE.md after the trim still cites all four sources (Voyager 1, Voyager 2, IBEX, Frisch \u0026 Slavin 2013); confirm docs/solar-system.md still carries the construction recipe.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:26Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:48Z","started_at":"2026-05-11T07:53:51Z","closed_at":"2026-05-11T08:46:48Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-38"],"dependencies":[{"issue_id":"stellata-9mm.140","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:25Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.139","title":"projectWithNearClip module-scope scratches rely on un-enforced single-caller-per-frame contract","description":"**Confidence: Low — needs Alex's input** (judgment call on encapsulation vs. perf)\n\n**Finding**\n`projectWithNearClip` in `distance-vector-overlay.ts` was changed from `.clone()` per call (three Vector3 allocations) to module-scope scratches `projVa` / `projVb` / `projEnd`. The PR description justifies this as \"called at most once per frame from this module's onFrame handler — safe.\" The function is, however, `export`ed.\n\nThe contract is correct today: the only caller is the local `onFrame` handler, and the returned arrays `pA`, `pB` are fresh `[number, number]` so post-return reads are safe even if the scratch is later clobbered. But the contract is unenforced — a future caller (a sibling overlay, a test that drives both `projectWithNearClip` and another module-scoped projection consumer in the same tick, or a refactor that pulls the function into a shared util) would silently return stale data. The two callsites in the test file already exist and would be fine; the risk is purely future-callers.\n\n**Location**\nsrc/client/distance-vector-overlay.ts:25-27 (scratch declarations), :240-294 (the function body).\n\n**Suggested approach**\nTwo reasonable resolutions, your call:\n\n1. **Document the contract louder**: rename the scratches to `onFrameProjVa` etc. (or move them inside the closure scope if export isn't actually needed downstream beyond tests), and add a top-of-function assert in dev builds: `if (projectingFlag) throw; projectingFlag = true; ... finally { projectingFlag = false; }`.\n\n2. **Pass scratches in**: add an optional `scratch?: { va, vb, end }` parameter that defaults to a fresh allocation when omitted, and have the `onFrame` handler create one stable scratch object for itself. Equivalent perf, no module-level state, callable from any context.\n\nOption (2) is the principled fix; option (1) is the minimum-disturbance fix.\n\n**Why**\nModule-scope scratches in an exported function are a footgun whenever another module is one refactor away from depending on it. The current `@__PURE__` annotations also imply the values are immutable per call — they aren't. Today's behaviour is correct; the bead is about the next person who reuses the helper.\n\n**Smoke step**\nRe-run the existing `distance-vector-overlay.test.ts` suite (18 tests) — no behaviour change. After option (2), confirm two-call test that drives the function back-to-back with different inputs returns the expected results for both calls.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:17Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:22Z","labels":["pr-55"],"dependencies":[{"issue_id":"stellata-9mm.139","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:37Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.138","title":"docs/chart-mode.md 'Picking under chart mode' missing pickScore third bullet","description":"**Confidence: High**\n\n**Finding**\n`docs/chart-mode.md` § 'Picking under chart mode' enumerates the two pre-existing pickStar fixes (variable bright-extreme filter, MIN_DISC_HIT_RADIUS_PX floor) but doesn't mention the screen-pixel proximity tiebreak the PR introduces. Chart mode is the regime where overlapping small discs are most common (small ~1–2 px discs across a crowded sky), so it's the most user-visible context for the new pickScore behaviour and the natural place to document it. The doc is the canonical reference for 'how does the picker behave in chart mode?', and a future reader chasing a 'wrong star picked' report under chart mode lands here first.\n\n**Location**\ndocs/chart-mode.md:186-200 (the 'Picking under chart mode' section)\n\n**Suggested approach**\nAdd a third bullet describing the screen-pixel proximity tiebreak: 'pickScore tiebreak. Within the prime tier (cursor inside a rendered disc), the cursor's pixel distance to each candidate's projected centre wins (pickScore = pxDist + appMag·0.05). Visually-resolved pairs whose hitboxes overlap stay independently clickable — the Double Double (ε¹/ε² Lyr) is the canonical case. Sub-pixel mag bias tiebreaks coincident catalog companions sharing x/y/z, e.g. Alula Australis A/B (Gl 423A/B at identical coordinates).' Cross-link from `star-geometry.ts`'s pickScore comment.\n\n**Why**\nThe existing two bullets already follow this format. Adding the third keeps the chart-mode picking section the single source of truth for picker behaviour quirks. Without this, the only place to learn about the new behaviour is the inline comment in pickStar.\n\n**Smoke step**\nAfter the edit, reading docs/chart-mode.md § Picking should fully describe the picker contract for chart mode — variable filter, hit-radius floor, and pickScore tiebreak.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:12Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:59:09Z","started_at":"2026-05-10T20:09:17Z","closed_at":"2026-05-10T21:59:09Z","close_reason":"Shipped in PR #53 (merge commit 63da7a6, v1.6.2)","labels":["pr-53"],"dependencies":[{"issue_id":"stellata-9mm.138","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:11Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.137","title":"[PR #48] Four-pass renderOrder test sorts before assert — wouldn't catch a disc↔outer swap","description":"**Confidence: Medium**\n\n**Finding**\nThe new test in `planet-body-field.test.ts` (lines 187-200) asserts:\n\n```ts\nconst orders = f.group.children\n  .map((m) =\u003e m.renderOrder)\n  .sort((a, b) =\u003e a - b);\nexpect(orders).toEqual([-4, 1.5, 3, 4]);\n```\n\nBecause it sorts before comparing, the test is satisfied as long as the *set* of renderOrder values is `{-4, 1.5, 3, 4}` regardless of which mesh holds which value. A future refactor that swaps disc↔outer (e.g. moving outer to renderOrder 3 and disc to 1.5) would still pass. The PR's own description gives the contract: outer-disc must be 1.5 (between star glow at 1 and orbit rings at 2), disc at 3, glow at 4, core at -4. The test should pin those bindings, not just the value set.\n\n**Location**\nsrc/client/planet-body-field.test.ts:187-200\n\n**Suggested approach**\nEither expose typed accessors (`f.meshOuter.renderOrder` etc.) and assert each individually, or reorganise the assertion to map mesh-name → renderOrder via the mesh's `name` property (set names in `buildMaterials`). A single-line change adding `mesh.name = 'outer' | 'disc' | 'glow' | 'core'` would let the test do `expect(f.group.children.find(m =\u003e m.name === 'outer')!.renderOrder).toBe(1.5)` without exposing private fields.\n\n**Why**\nPR description says \"a future reorder fails CI rather than silently regressing the bug,\" but the sort defeats that. The test currently only catches deletion or addition of a renderOrder value — not the bug it claims to prevent.\n\n**Smoke step**\n`npm test src/client/planet-body-field.test.ts` still passes after the assertion is tightened; mutate one renderOrder in source and confirm the test fails.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:10Z","created_by":"Alex Marshall","updated_at":"2026-05-10T16:12:38Z","started_at":"2026-05-10T14:24:58Z","closed_at":"2026-05-10T16:12:38Z","close_reason":"Shipped in PR #48 (stellata 1.4.5).","dependencies":[{"issue_id":"stellata-9mm.137","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:10Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.136","title":"disc-mask.ts: cutout pool grows unbounded; comment says 'we don't bother with a hard cap'","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\n`ensureCircles(n)` allocates new `\u003ccircle\u003e` SVG nodes whenever `n` exceeds the pool size, and the pool is never shrunk. The new comment is explicit: 'Pool of \u003ccircle\u003es, grown on demand and never shrunk … we don't bother with a hard cap.' Today the upper bound is the largest constellation's vertex count + 2, which is small (Stellarium's longest-asterism constellations are around 30–40 vertices). But the bound is structural-data-dependent, not enforced. A future change that adopts a denser asterism source, or a bug that accidentally feeds the wrong list (e.g. all-constellations chart-mode iteration leaks into the disc-mask path), would cause the pool to grow to thousands of nodes silently.\n\n**Location**\nsrc/client/disc-mask.ts:30-43 (`ensureCircles` and the surrounding comment)\n\n**Suggested approach**\nAdd a soft cap (e.g. `MAX_MASK_CIRCLES = 64`) and either silently truncate beyond it or assert in dev. The truncate option mirrors the original code's bounded-pool philosophy without re-introducing a fixed-4 limit; the assert option turns a future regression into a loud failure on next run. Either is one extra branch in `ensureCircles`. Keep the comment explaining that today's expected ceiling is ~40.\n\n**Why**\nDefensive cap costs nothing at runtime and bounds the worst case if data shape ever shifts. The current comment ('we don't bother') reads as deliberate, but isn't actually a tradeoff — there's no perf or complexity cost to a soft cap. Filing low-confidence because this may simply be a stylistic preference.\n\n**Smoke step**\nManual PR smoke unchanged; `npm run typecheck` clean. Optional: temporarily set the cap to 4 to confirm truncation is silent (no console errors) and that visual regression is contained to over-cap stars not getting cutouts.","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:09Z","created_by":"Alex Marshall","updated_at":"2026-05-11T07:44:36Z","started_at":"2026-05-10T22:06:08Z","closed_at":"2026-05-11T07:44:36Z","close_reason":"Closed","labels":["pr-52"],"dependencies":[{"issue_id":"stellata-9mm.136","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:09Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.134","title":"Welcome vs About modal use different headline framings (\"physics-based\" vs \"observational\")","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nThe Welcome modal opens with:\n\u003e Stellata is a **physics-based, empirical 3D model** of our galaxy that simulates what it would be like to actually *be there*.\n\nThe About modal opens with:\n\u003e Stellata is an **observational 3D model** of our local part of the galaxy at every scale we've measured it.\n\nThe README opens with \"An observational 3D model…\" and the HTML title says \"Observational 3D Model of our Galaxy\". Three of four intro surfaces lead with \"observational\"; only the welcome modal leads with \"physics-based, empirical\". The two phrases are not exact synonyms — \"physics-based\" implies *modelling*, \"observational\" implies *catalogue ingestion* — and the rest of the PR is explicit that Stellata is the latter (\"Theoretical predictions and conjectured structures are excluded\").\n\n**Location**\n- src/client/index.html line 120 (welcome modal)\n- src/client/index.html line 150 (about modal)\n- README.md line 3\n\n**Suggested approach**\nRewrite the Welcome modal opening to lead with the same \"observational\" framing the About modal, README, and HTML title use. Drop \"physics-based\" — the empirical-model claim is the differentiator, not the physics machinery underneath it. A consistent tagline across the four surfaces also reads as more deliberate brand voice.\n\n**Why**\nThe PR's stated goal is to align every intro surface around \"the empirical-model principle\". Welcome modal is the only outlier and it's the *first* surface a new visitor sees, so the inconsistency is the most visible one.\n\n**Smoke step**\nRead all four surfaces (welcome modal, about modal, README first sentence, browser tab title) and confirm the noun phrase Stellata uses to describe itself is the same across all four.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:07Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:24Z","labels":["pr-38"],"dependencies":[{"issue_id":"stellata-9mm.134","depends_on_id":"stellata-9mm.191","type":"parent-child","created_at":"2026-05-15T12:53:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.133","title":"phase-3 recenter recomputes the worldOffset delta that recenterOrigin already has","description":"**Confidence: High**\n\n**Finding**\nThe phase-3 observe-warp recenter in `updateWarp` reads `catalog.positions` for the destination star and subtracts `worldOffset` to compute `(dx, dy, dz)` so `state.pEnd` can be shifted into the new frame. `recenterOrigin` (called transitively via `recenterFocusToStar`) computes the same three deltas internally on its first three lines (`newOrigin.x - this.worldOffset.x` etc.) before it shifts camera/target/instances. The PR open-codes a duplicate of that arithmetic immediately above the `recenterFocusToStar` call, then throws the deltas away after using them on `pEnd`.\n\n**Location**\nsrc/client/stellata.ts:3666-3681\n\n**Suggested approach**\nEither (a) snapshot `worldOffset` before the call and compute the delta after `recenterFocusToStar` returns by subtracting old from new, or (b) have `recenterOrigin` (or a `recenterOriginDelta` variant) return the `Vector3` delta it computed so the caller can apply it to other state in the same frame without re-deriving. Option (b) is the cleaner contract — the function's whole job is to shift things by a delta — and would also serve any future caller that needs to migrate auxiliary state across a recentre. One commit; non-breaking since today's only callers ignore the return value.\n\n**Why**\nThe two derivations have to stay numerically identical for the lerp `pEnd → B` to land cleanly on B at `f=1`. Today they're computed twice in the same precision so they happen to match, but a future change to `recenterOrigin`'s rounding (e.g. a snap-to-float32 pass) would silently desync `pEnd` from the post-shift frame. Single-source-of-truth makes that class of bug impossible.\n\n**Smoke step**\n1. Observe→observe warp Sol → Betelgeuse: post-arrival slerp ends with the destination disc rock-solid at NDC centre.\n2. `debug.pin()` HUD: phase-3 last frame shows |camera.position| ≪ source-frame B.\n3. `npm test` clean.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:05Z","created_by":"Alex Marshall","updated_at":"2026-05-10T20:02:06Z","started_at":"2026-05-10T18:24:55Z","closed_at":"2026-05-10T20:02:06Z","close_reason":"merged in PR #50 (commit 783d396, v1.6.1)","labels":["pr-50"],"dependencies":[{"issue_id":"stellata-9mm.133","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:05Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.130","title":"docs/ux-tweaks.md MIN_DISC_HIT_RADIUS_PX warning describes removed mechanism","description":"**Confidence: High**\n\n**Finding**\n`docs/ux-tweaks.md` line 88 documents the trade-off for raising `MIN_DISC_HIT_RADIUS_PX` as: 'Raise for easier hover at the cost of foreground stars stealing picks from neighbours.' That mechanism — closest-to-camera tiebreak between overlapping prime-tier hits — no longer exists after PR #53; the prime tier now scores by pixel distance from cursor to disc centre. The new failure mode for raising `MIN_DISC_HIT_RADIUS_PX` is different: a neighbour star whose centre projects nearer the cursor than the visually-targeted star can win, because hit radii now overlap more often. The doc warning misleads anyone tuning the constant.\n\n**Location**\ndocs/ux-tweaks.md:85-88\n\n**Suggested approach**\nRewrite the trade-off bullet to reference the new pickScore tiebreak. Suggested text: 'Raise for easier hover at the cost of more cases where a neighbour disc whose centre is marginally closer to the cursor wins over the visually-targeted star (pickScore tiebreak).'\n\n**Why**\nDoc drift compounds: the comment is the only place a future reader tuning chart-mode hover sees the trade-off explained; the inline pickStar comment doesn't surface here. Update is cheap and prevents a wrong mental model.\n\n**Smoke step**\nAfter the edit, the doc bullet should be self-consistent with the pickScore comment in star-geometry.ts. Reading both should yield the same picker-tiebreak description.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:54Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:59:08Z","started_at":"2026-05-10T20:09:16Z","closed_at":"2026-05-10T21:59:08Z","close_reason":"Shipped in PR #53 (merge commit 63da7a6, v1.6.2)","labels":["pr-53"],"dependencies":[{"issue_id":"stellata-9mm.130","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.129","title":"[PR #48] heliopause.ts comment doesn't mention new outer-disc layer between rings and shell","description":"**Confidence: High**\n\n**Finding**\n`heliopause.ts` lines 94-95 read: \"Behind planet bodies (renderOrder=3) and above stars (0) — sits alongside orbit rings (2) at renderOrder=1, since both are dim chrome for the same layer.\" PR #48 introduces a new planet outer-disc occluder at renderOrder=1.5, sitting between the heliopause shell (1) and the orbit rings (2) — the heliopause is no longer \"alongside orbit rings\" without an interposed depth-only planet pass. The comment doesn't reflect that.\n\n**Location**\nsrc/client/heliopause.ts:94-97\n\n**Suggested approach**\nOne-line comment update inside the same constructor preamble. Either acknowledge the 1.5 outer-disc occluder explicitly (\"Behind planet bodies (3, 4) and the planet outer-disc occluder (1.5)\") or rewrite the comment to enumerate the post-PR ladder.\n\n**Why**\nThe whole point of PR #48 documenting renderOrder ladders inline is that a future reorder fails. Leaving the heliopause comment stale undermines that — the next contributor reading the heliopause file will believe rings are the next neighbour above.\n\n**Smoke step**\n`grep -n renderOrder src/client/heliopause.ts` shows the updated comment matches the actual ladder; `npm run typecheck` (no behavioural change).","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:53Z","created_by":"Alex Marshall","updated_at":"2026-05-10T16:12:39Z","started_at":"2026-05-10T14:24:59Z","closed_at":"2026-05-10T16:12:39Z","close_reason":"Shipped in PR #48 (stellata 1.4.5).","dependencies":[{"issue_id":"stellata-9mm.129","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.128","title":"disc-mask.ts: observe-mode 'no other disc reaches threshold' assumption hardened without verification","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nThe pre-PR observe-mode early-return triggered when the focal star was hidden (`focus === null || observe`); the rationale was 'we only mask the focal pair, and they're hidden in observe.' The PR widens cutout sources to every constellation vertex but keeps the same blanket observe-mode skip, justifying it with: 'Other stars are always far away from a camera parked at a focal star, so they don't reach the disc threshold either.' That assumption isn't verified in code or doc — it's an empirical claim about the camera-position invariant during observe mode.\n\n**Location**\nsrc/client/disc-mask.ts:75-82 (the new observe early-return + comment)\ndocs/overlays.md:84-89 (corresponding doc update making the same claim)\n\n**Suggested approach**\nEither (a) add a smoke note to docs/overlays.md naming the invariant explicitly: 'OBSERVE camera sits at the focal star's local position, so any other catalog star is at least `min(catalog gap)` away — well beyond DISC_THRESHOLD_PX at any reasonable FOV.' Cite where in stellata.ts that camera-position invariant is enforced. Or (b) drop the early-return for the constellation-vertex pass only, keeping the focal-pair skip — i.e. let the iteration run with `focus = null` and trust the per-circle threshold check to no-op cheaply when no disc is actually rendering. Option (b) is more defensive at the cost of a few dozen `renderedSizePx` calls per frame in observe.\n\n**Why**\nHardening an undocumented assumption to cover more cases is the kind of code that silently regresses when an unrelated change (camera-position offset in observe, FOV widen, or a new \"cinematic\" observe submode) violates the invariant. The original branch was load-bearing only for the focal pair; the new branch is load-bearing for every constellation vertex.\n\n**Smoke step**\nEither doc-only (a) or repro: highlight Orion, focus Betelgeuse, enter OBSERVE, then nudge the OBSERVE camera (drag controller) so it points toward another bright Orion vertex (Bellatrix / Saiph) and verify no disc crosses 48 px (or, if it does, that the lines paint through it intentionally). If (b) is chosen: run the full PR test plan + verify there's no observable cost in the perf-hud disc-mask tick row in observe mode.","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:51Z","created_by":"Alex Marshall","updated_at":"2026-05-11T07:44:35Z","started_at":"2026-05-10T22:06:08Z","closed_at":"2026-05-11T07:44:35Z","close_reason":"Closed","labels":["pr-52"],"dependencies":[{"issue_id":"stellata-9mm.128","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:50Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.125","title":"recenterFocusToStar duplicates the focus-recenter core inside setFocus","description":"**Confidence: High**\n\n**Finding**\nPR #50 factors a new private `recenterFocusToStar(newIdx)` out of `swapObserveAnchor` so phase-3 of an observe→observe warp can recentre the floating origin onto the destination star ahead of `finishWarp`. The new helper does the focus-recenter core (recenterOrigin to star, focusedStar = idx, controls.minDistance = minOrbitDistForStar(idx), onFocusHandlers fan-out, refreshPlanetSystem). The same five lines also live inline in `setFocus` (the `if (idx !== null)` recentre branch + `focusedStar = idx` above + the minDistance / onFocusHandlers / refreshPlanetSystem fan-out below). The PR adds the helper but doesn't route `setFocus` through it, so the duplication persists.\n\n**Location**\nsrc/client/stellata.ts:1410-1460 (setFocus) and src/client/stellata.ts:2466-2475 (recenterFocusToStar)\n\n**Suggested approach**\nRefactor `setFocus` so its focus-on-star branch delegates to `recenterFocusToStar`. `setFocus`'s extra responsibilities — cloud-clear, observe-cleanup, post-recenter target/camera snap, the unfocus minDistance clamp, and the trailing fireStateChange — stay inline; the shared core moves into the helper. One commit. Keep both call sites' behaviour identical (the target snap is unique to setFocus and stays under setFocus's roof; recenterFocusToStar's caller in updateWarp doesn't need it because phase-3 doesn't re-anchor controls.target the same way).\n\n**Why**\nDRY. The PR comment on `recenterFocusToStar` already calls out that navigate's setFocus(destIdx) at finishWarp does the equivalent — codifying that equivalence in code, not just prose, prevents drift if either site grows a new responsibility.\n\n**Smoke step**\n1. Focus Sol from no-focus, focus a far star (e.g. Betelgeuse), unfocus — focus state, focus-ring overlay, panel and URL update unchanged.\n2. Observe→observe warp arrives without jitter.\n3. `npm test` and `npm run typecheck` clean.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:47Z","created_by":"Alex Marshall","updated_at":"2026-05-10T20:02:05Z","started_at":"2026-05-10T18:24:54Z","closed_at":"2026-05-10T20:02:05Z","close_reason":"merged in PR #50 (commit 783d396, v1.6.1)","labels":["pr-50"],"dependencies":[{"issue_id":"stellata-9mm.125","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.124","title":"[PR #54] vec3FieldV3.encodeBytes is called twice per field per encodeBlob — recomputes sub-mask each time","description":"**Confidence: Medium**\n\n**Finding**\n`encodeBlob` walks `FIELDS_V3` twice per call: once at line 717-719 to size the buffer (`total += f.encodeBytes(view)`) and again at line 725-729 to step `off` after writing (`off += f.encodeBytes(view)`). For `vec3FieldV3.encodeBytes` (lines 240-248) this means three component comparisons against `componentDefaults(v)` are executed twice for every present vec3, and `componentDefaults` itself runs `defaultCamForMode(v.mode)` twice for cam. The path also runs after the encode body has already done the same comparison at line 263-269.\n\nThree identical comparisons per cam encode (size pass, write pass, sub-mask construction) × per-write debounced URL = noticeable on warp animations or rapid-camera-move, even if not catastrophic.\n\n**Location**\nsrc/client/url-state.ts:713-732 (encodeBlob); 240-275 (vec3FieldV3.encodeBytes + encode).\n\n**Suggested approach**\nHave `encode` return the byte count it wrote, so the second encodeBytes call disappears: `off += f.encode(view, dv, off)`. The buffer-sizing pass either accepts the extra computation (it is unavoidable to size before allocating) or computes presence + bytes together and stashes per-field byte counts in a small array reused by the write pass. Either way the cam comparison runs at most twice (sizing + writing), not three times, and the `f.key === 'cam'` lookup happens on a fast path.\n\n**Why**\nMinor perf, but the comment on `startUrlSync` (lines 1196-1198) explicitly motivates a no-allocation steady-state path. Halving the work inside encodeBlob is consistent with that goal.\n\n**Smoke step**\n`npm test` green; no observable behavioural change. Optional: a microbenchmark counting comparator calls.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:45Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:33Z","started_at":"2026-05-10T17:50:42Z","closed_at":"2026-05-10T18:17:33Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.124","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.123","title":"Outer-disc near-z write defeats disc-pass halo gl_FragDepth = 1.0 trick","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nThe disc pass's halo branch in `planet.frag.glsl` (line 78) writes `gl_FragDepth = 1.0` so halo fragments don't occlude later passes — same trick the star pipeline uses. PR #48 inserts a depth-only outer-mask pass at renderOrder 1.5 that writes the planet's actual near-z across the *full* visible disc, including the halo annulus. With the default `LESS` depth test, the disc pass halo fragment then has `gl_FragDepth = 1.0 \u003c near-z = false` and gets discarded — no MaxEquation blend with the already-drawn MW/clouds/stars. The PR description and the new `docs/solar-system.md` paragraph both claim \"the perceptual halo→soft background falloff still works through the disc pass's MaxEquation blending,\" but the depth math contradicts that claim. The unchecked manual smoke item (\"a bright distant star or MW patch behind a focused planet's halo: the soft halo→background falloff should look the same as before\") is the only validation Alex flagged.\n\n**Location**\nsrc/client/shaders/planet.frag.glsl:78 vs new uRenderMode == 3 branch at lines 62-75; docs/solar-system.md:106-117\n\n**Suggested approach**\nEyeball the smoke step against a Mercury or Venus disc framed against a bright MW patch — compare a build with the outer mask vs without, with disc-pass halo rendered both ways. If the halo annulus is sub-pixel for v1's planets and the regression is invisible, document the deliberate halo loss in the doc paragraph instead of claiming preservation. If visible, alternatives: keep the outer mask gated on `glow \u003e= uCoreThreshold` (only writes near-z where the disc pass would too — same as core mask but at the disc-extent radius), or apply `depthFunc: LessEqualDepth` to the rings so a 1.5/3 tie passes, or use a stencil approach.\n\n**Why**\nStar pipeline halo→soft-MW interplay was specifically called out as a load-bearing visual in `docs/rendering.md` (line 48-50: \"Halo fragments push gl_FragDepth = 1.0 so they paint dim haze without occluding the later glow pass\"). Adopting that pattern for planets in 3re.16 then breaking it in 3re.19 is an architectural-fit regression worth flagging even if the visual delta is small for v1's small planet discs.\n\n**Smoke step**\nManual: focus Sol, fly inside Saturn-orbit range with Saturn framed against a bright Milky Way bulge patch; check whether the halo annulus around Saturn's disc still softens the MW colour (compare A/B with and without the new mat outer mesh).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:40Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:27Z","labels":["pr-48"],"dependencies":[{"issue_id":"stellata-9mm.123","depends_on_id":"stellata-9mm.189","type":"parent-child","created_at":"2026-05-15T12:53:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.122","title":"pickStar: depth-occlusion semantics dropped without explicit contract note","description":"**Confidence: Medium**\n\n**Finding**\nThe old prime-tier tiebreak was `closest-to-camera wins (foreground occludes)` — a depth-ordering rule that mirrored the GPU's render order. The PR replaces it with `pickScore = pxDist + appMag·0.05`, which is dominated by screen-pixel proximity. With non-coincident overlapping discs, this means: a faint background star can win over a bright foreground star whose disc the cursor is inside, when the background star's projected centre is even ~1 px closer to the cursor (the mag bias is only 0.05 px / mag, so a 4-mag-brighter foreground is only 0.2 px of bias). The Double Double / Alula fixes are net wins, but the contract change — 'visible proximity beats depth ordering, always' — is not called out anywhere user-facing or in the helper docstring. `pickScore`'s comment mentions the Double Double rationale but doesn't acknowledge that depth-occlusion is now deliberately ignored, so a reader debugging a 'why did the picker pick the background star?' report has no signpost.\n\n**Location**\nsrc/client/star-geometry.ts:73-90 (`pickScore` docstring)\nsrc/client/stellata.ts:2752-2762 (the prime-tier comment block in `pickStar`)\n\n**Suggested approach**\nExtend the `pickScore` docstring to spell out the trade-off explicitly: 'this rule deliberately ignores camera-distance / depth ordering; if a faint background star projects closer to the cursor than a bright foreground star, the background wins. The au3/xec failure mode (overlapping disc hitboxes leaving one component unclickable) was deemed worse than the rare inverse case, where a foreground bright disc is visually obvious but the cursor lands marginally closer to a background star's centre.' Same paragraph in `pickStar`'s tier-1 comment.\n\n**Why**\nFuture maintainers will hit a 'why did the picker resolve to the wrong star?' report and need to know whether the answer is 'bug' or 'intentional contract'. Right now the only way to learn the contract is to git-blame the commit that removed `discBestCamDist`.\n\n**Smoke step**\nN/A (doc-only). After the docstring update, a reader of either file should be able to answer 'does pickStar prefer foreground over background when both contain the cursor?' without reading the diff.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:39Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:59:08Z","started_at":"2026-05-10T20:09:15Z","closed_at":"2026-05-10T21:59:08Z","close_reason":"Shipped in PR #53 (merge commit 63da7a6, v1.6.2)","labels":["pr-53"],"dependencies":[{"issue_id":"stellata-9mm.122","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:38Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.120","title":"disc-mask.ts dirty-track guard inverted (\u003c ATTR_DIRTY_PX vs \u003e=) vs rest of pass","description":"**Confidence: High**\n\n**Finding**\n`disc-mask.ts` writes the `setCx/setCy/setR` guards as `if (Math.abs(value - s.last*) \u003c ATTR_DIRTY_PX) return;` (write executes on the fall-through). Every other overlay in this PR (and chart-labels.ts) writes the guard as `if (Math.abs(... - last) \u003e= ATTR_DIRTY_PX) { write; }` — early-write form. Same end behaviour, but the inverted phrasing is a review-time hazard: a reader scanning the diff for \"\u003e= ATTR_DIRTY_PX\" will miss the disc-mask sites, and a future patch that flips the comparison or the decimal point will be a one-character bug that's invisible to grep.\n\n**Location**\nsrc/client/disc-mask.ts:50-64\n\n**Suggested approach**\nRewrite the three helpers in the same early-write form as the other four overlays:\n\n```ts\nconst setCx = (s: Slot, value: number) =\u003e {\n  if (Math.abs(value - s.lastCx) \u003e= ATTR_DIRTY_PX) {\n    s.el.setAttribute('cx', value.toFixed(1));\n    s.lastCx = value;\n  }\n};\n```\n\n(or factor through the shared util in the sibling DRY bead — whichever lands first).\n\n**Why**\nConsistency across the overlay layer matters more than micro-readability of the early-return form. The guard direction is the kind of detail the next reviewer should be able to confirm-by-inspection without hunting for the inverted phrasing.\n\n**Smoke step**\nDiff the post-rewrite file against pre-rewrite output for a frozen camera position — exact same setAttribute call sequence.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:33Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:32Z","started_at":"2026-05-16T10:02:23Z","closed_at":"2026-05-16T21:38:32Z","close_reason":"merged via PR #55 (disc-mask routed through canonical setNumAttr — inverted-guard form gone)","labels":["pr-55"],"dependencies":[{"issue_id":"stellata-9mm.120","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.119","title":"perf marker name 'onFrame.total' lags renamed event 'frame'","description":"**Confidence: High**\n\n**Finding**\nThe `animate()` loop wraps the bus emit with `perfMark('onFrame.total')` / `perfMeasure('onFrame.total')`, but the event itself was renamed from `onFrame` (the old method) to `'frame'` (the bus event). The perf section legend in `docs/performance.md` was updated to call this 'the full `'frame'` emit loop' but kept the literal section name `onFrame.total` because that's what `stellata.ts` still passes. So the user-facing label and the new event name disagree.\n\n**Location**\n- `src/client/stellata.ts:3465` — `perfMark('onFrame.total');`\n- `src/client/stellata.ts:3467` — `perfMeasure('onFrame.total');`\n- `docs/performance.md:46` — table entry still labelled `onFrame.total`\n\n**Suggested approach**\nRename the marker to `frame.handlers` (or `frame.emit`) to match the event nomenclature, and update the row in the perf-table. This is the section that measures the `bus.emit('frame')` cost specifically, distinct from `frame.total` (the whole-tick wrapper).\n\n**Why**\nNaming consistency across the eleven event types is the value proposition of the migration; leaving `onFrame.total` as the perf label leaks the old vocabulary into runtime telemetry. Future readers grepping for `'frame'` won't find the perf section; future readers grepping for `onFrame` get a false hit on a renamed concept.\n\n**Smoke step**\n`debug.panel()` opens; the Perf section row reads with the new name; `npm run typecheck` and `npm test` green.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:31Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:15:12Z","started_at":"2026-05-15T12:05:27Z","closed_at":"2026-05-15T12:15:12Z","close_reason":"Folded into PR #51 (commits 00f39b0, 11d300a, 48e850f)","labels":["pr-51"],"dependencies":[{"issue_id":"stellata-9mm.119","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:22Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.116","title":"ATTR_DIRTY_PX duplicated 5× across overlays + chart-labels — extract shared util","description":"**Confidence: Medium** (the PR description preempts this — needs Alex's call on whether the threshold has reached extraction)\n\n**Finding**\n`const ATTR_DIRTY_PX = 0.05` plus the comment \"Half a .toFixed(1) step ... Mirrors chart-labels.ts.\" is duplicated verbatim across five module files. Each file also reinvents an ad-hoc helper shape around it — `disc-mask.ts` uses a `Slot` object + `setCx/setCy/setR` closures, `distance-vector-overlay.ts` mixes `setLineD/setLineBgD` closures with inline `if (Math.abs(...) \u003e= ATTR_DIRTY_PX)` blocks, `hud-overlay.ts` is purely inline, `poi-overlay.ts` mixes string-equality helpers with inline numeric guards, and `constellation-overlay.ts` has only a string-equality `setD`. Six callsites if focus-ring-overlay is added (see sibling bead).\n\nThe PR description acknowledges this and argues \"three lines of constant beats premature abstraction.\" That argument was reasonable when the count was 1 (chart-labels alone). It now stands at 5, with 6 imminent. The \"mirrors chart-labels.ts\" comment becomes self-falsifying the moment any one tuning diverges.\n\n**Location**\nsrc/client/chart-labels.ts:138, src/client/disc-mask.ts:20, src/client/distance-vector-overlay.ts:21, src/client/hud-overlay.ts:42, src/client/poi-overlay.ts:49 (constant). Helper-shape divergence: src/client/disc-mask.ts:50-64, src/client/distance-vector-overlay.ts:69-78 + 197-215, src/client/hud-overlay.ts:256-267 + 414-440, src/client/poi-overlay.ts:147-166 + 297-326, src/client/chart-labels.ts:557-564 + 642-657 + 703-715.\n\n**Suggested approach**\nExtract `src/client/dirty-attr.ts` exporting `ATTR_DIRTY_PX = 0.05` and a small toolkit:\n\n```ts\nexport function setNumAttr(el: Element, name: string, value: number, last: { v: number }, decimals = 1): void;\nexport function setStrAttr(el: Element, name: string, value: string, last: { v: string }): void;\nexport function setStyle(el: ElementCSSInlineStyle, prop: string, value: string, last: { v: string }): void;\n```\n\n(or whatever shape ends up reading cleanly — a state-bag object per element is one option, the boxed-`{ v }` cell is another). Migrate the five overlays to use it. The shape question is the bead's main risk; happy to file a follow-up that prototypes one shape and lets reviewers pick.\n\n**Why**\nFive copies of the same 3-line pattern + comment is past the point where DRY pays off — the cost of one shared module beats the cost of: (a) future drift between thresholds, (b) review burden when each new overlay reinvents a slightly different helper shape, (c) the next-overlay reviewer having to re-derive whether `\u003e= ATTR_DIRTY_PX` or `\u003c ATTR_DIRTY_PX` is the correct gate (disc-mask uses the inverted form — see sibling bead). The PR's stated rationale (\"three lines of constant beats premature abstraction\") does not survive the actual five-file body count.\n\n**Smoke step**\nAfter migration: `npm run typecheck`, `npm test`, and a manual stationary-camera idle in NAVIGATE + OBSERVE to confirm no regression in the dirty-skip behaviour.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:18Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:30Z","started_at":"2026-05-16T10:02:21Z","closed_at":"2026-05-16T21:38:30Z","close_reason":"merged via PR #55 (ATTR_DIRTY_PX hoisted to src/client/dirty-attr.ts)","labels":["pr-55"],"dependencies":[{"issue_id":"stellata-9mm.116","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:31Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.115","title":"Cloud picker still 'closest-to-camera'; stars now pixel-proximity (asymmetry)","description":"**Confidence: Medium**\n\n**Finding**\nPR #53 fixed the same au3/xec bug class for stars by switching the prime tier from camera-distance to screen-pixel proximity (`pickScore`). The cloud picker (`Stellata.pickCloud` → `MolecularClouds.raycast`) still relies on Three.js `raycaster.intersectObjects`, which sorts hits by ray-distance from the camera and picks the first — i.e. the same 'closest-to-camera wins' rule au3/xec just rejected for stars. If the user clicks where two cloud ellipsoids overlap on screen, the foreground cloud always wins even when the cursor is visually plainly inside the background cloud's silhouette. Clouds are shelved (`FilterState.showMolecularClouds = false`), so this isn't a live UX bug today, but the picking-model asymmetry is now documented in code (one path screen-pixel-driven, the other camera-distance-driven) and will need reconciling when clouds re-enable.\n\n**Location**\nsrc/client/molecular-clouds.ts:197-203 (`raycast`)\nsrc/client/stellata.ts:1627-1636 (`pickCloud`)\nCompare: src/client/stellata.ts:2752-2804 (`pickStar` two-tier with pickScore)\n\n**Suggested approach**\nWhen the cloud layer is un-shelved, mirror `pickStar`'s contract: project each cloud centroid (or silhouette nearest-point) to screen and tiebreak overlapping cloud hits by pixel-distance from the cursor, with an analogous size-or-brightness mag bias (cloud volume / opacity). The simplest version reuses Three.js's intersectObjects to gate hit-vs-miss but resorts the hit list by projected-centroid-pixel-distance before picking the first. Alternatively, replace the mesh raycast entirely with an analytic ellipsoid screen-projection — `molecular-clouds.ts` already has `scratchDirLocal`/`scratchU`/`scratchV` projection scratch space for the silhouette overlay, so the math is partially in place.\n\n**Why**\nThe whole point of au3 (Double Double) was that depth-tiebreak makes overlapping targets unclickable. Clouds are visually large and frequently overlap (Taurus / Perseus on the same line of sight). The same UX failure will reappear the moment clouds are re-enabled.\n\n**Smoke step**\nAfter re-enabling clouds: position camera so two clouds overlap (e.g. Perseus in front of California). Click each visible silhouette near its centre — both must be selectable independently. Today (under the current raycast logic) only the foreground is reachable.\n","notes":"Deferred 2026-05-10 during PR #53 work. Tied to molecular-cloud un-shelve work; fix lands when FilterState.showMolecularClouds re-enables. Cloud picker still uses Three.js raycaster.intersectObjects (closest-to-camera), an asymmetry vs the new star pixel-proximity rule but inert while clouds are hidden.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:17Z","created_by":"Alex Marshall","updated_at":"2026-05-10T20:18:13Z","labels":["pr-53"],"dependencies":[{"issue_id":"stellata-9mm.115","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-15T12:54:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.113","title":"Stale on*Change names in stellata.ts comments after event-bus migration","description":"**Confidence: High**\n\n**Finding**\nFive comments inside `stellata.ts` still refer to the pre-migration getter names (`onFocusChange`, `onWarpChange`, `onCameraModeChange`) even though those methods no longer exist on the class. The comments are correct in intent but talk about an API surface that was deleted in this PR.\n\n**Location**\n- `src/client/stellata.ts:1794` — \"search box empties via onFocusChange → syncFocusUI\"\n- `src/client/stellata.ts:1814` — \"search box clears via the onFocusChange handler\"\n- `src/client/stellata.ts:2190` — \"overlays listening to onWarpChange are expected to hide themselves\"\n- `src/client/stellata.ts:2409` — \"setFocus would do, triggering an onCameraModeChange flicker\"\n- `src/client/stellata.ts:2441` — \"emit an onCameraModeChange event, briefly flickering UI\"\n\n**Suggested approach**\nMechanical s/onFooChange/the 'foo' event/ (or 'foo' bus emit) at each site. The PR already did this in `docs/architecture.md` and `docs/camera-modes.md` for the same nomenclature; these stragglers in `stellata.ts` were missed. Single commit, one file.\n\n**Why**\nComments documenting cross-cutting state-flow are the first thing future-Claude reads — they should reference real API names. Otherwise readers grep for `onFocusChange` and find nothing, then have to reverse-engineer which event the comment means.\n\n**Smoke step**\n`grep -nE 'on(Focus|Vector|Filter|CameraMode|Cloud|Frame|State|Pois|Warp|PlanetSystem)Change' src/client/stellata.ts` returns no matches; `npm run typecheck`.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:14Z","created_by":"Alex Marshall","updated_at":"2026-05-15T12:15:12Z","started_at":"2026-05-15T12:05:27Z","closed_at":"2026-05-15T12:15:12Z","close_reason":"Folded into PR #51 (commits 00f39b0, 11d300a, 48e850f)","labels":["pr-51"],"dependencies":[{"issue_id":"stellata-9mm.113","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.109","title":"[PR #54] vec3FieldV3 sub-mask: high 5 bits silently ignored on decode (no defensive cap)","description":"**Confidence: Medium**\n\n**Finding**\n`vec3FieldV3` (lines 249-256, 276-285) treats the sub-mask byte as 3 component bits + 5 reserved/unused bits, but the decoder only inspects `sub \u0026 1`, `sub \u0026 2`, `sub \u0026 4` and silently ignores bits 3-7. A hand-edited or corrupt URL with `sub = 0xFF` decodes as if only the low 3 bits were set — mostly fine, but the file already takes a defensive stance elsewhere (POI count cap at decode time, fov clamp at encode time, varint length cap of 5 bytes). Reserved sub-mask bits being silently dropped is inconsistent with that stance.\n\nAdjacent: the comment at lines 222-224 says `low 3 bits = which components diverge from default` but never names the high 5 bits as reserved — future maintainers might think they're available for repurpose, breaking the v3 wire format.\n\n**Location**\nsrc/client/url-state.ts:222-256 (vec3FieldV3 comment + decodeBytes/decode); also affects every vec3 in v3.\n\n**Suggested approach**\nTwo options. (a) Spell the reservation in the comment: `bits 3-7 reserved (must be 0); decoder ignores for forward-compat with future per-component encodings`. Optional: assert in dev that `sub \u0026 0xF8 === 0` to catch hand-edits early. (b) If forward-compat for the high bits matters (e.g. a future per-component f64 escape), document the layout now so future v4 doesn't have to bump the schema version unnecessarily.\n\n**Why**\nThe wire format's bit-budget should be readable from the code without inferring it from the encoder. A test that hand-builds a v3 blob with `sub = 0xFF` and asserts it still decodes the low 3 bits correctly would also pin the silent-drop behaviour in code rather than prose.\n\n**Smoke step**\nAdd a test that hand-builds a v3 blob with cam sub-mask 0xF7 (only x set among low 3, plus all reserved bits) and asserts cam decodes as `[x_value, 0_default_y, 0_default_z]`. `npm test` green.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:09:55Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:32Z","started_at":"2026-05-10T17:50:41Z","closed_at":"2026-05-10T18:17:32Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.109","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:09:55Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.107","title":"disc-mask.ts: no unit-test coverage for dedup / pool-growth / threshold branching","description":"**Confidence: High**\n\n**Finding**\nPR #52 substantially extends `disc-mask.ts` per-frame logic: a `seen` Set deduplicates indices across focus + companion + every constellation-vertex iteration, an `ensureCircles` pool grower replaces a fixed-size pool, the observe-mode early-return now clears only the prior-frame slice, and `renderedSizePx` is now invoked per vertex of the highlighted constellation (vs at most 2 stars). None of this branching has unit coverage — there are no test files for `disc-mask` anywhere in the repo (`find . -name 'disc-mask*'` returns only worktree copies of the source). The PR's own test plan is a manual smoke checklist (Orion + Betelgeuse), so a regression in dedup, pool growth, or the lastUsed-tail clear would only surface visually on next manual smoke.\n\n**Location**\nsrc/client/disc-mask.ts (whole module, currently uncovered)\n\n**Suggested approach**\nExtract a pure helper that, given a focused-star idx (nullable), companion lookup fn, highlightCon idx, constellation polyline list, and a `renderedSizePx(idx) \u003e threshold` predicate, returns the ordered list of indices to mask + a Set of seen indices. Cover: focus-null + highlight-set, focus-set + companion = -1, focus is a vertex of the highlighted constellation (dedup), polyline shares an endpoint with another polyline (dedup across polylines), all vertices below threshold (used = 0), conIdx out of bounds. Keep the DOM-touching half (`ensureCircles`, `placeCircle`) inside the factory — purity isn't required there, only for the index-selection pipeline.\n\n**Why**\nThe dedup pipeline replaces an explicit, hand-verifiable focus + companion sequence with a Set-based fan-in across an arbitrary-length data structure. That's exactly the shape of code that can silently drop or double-count stars when the constellation data shape changes (e.g. polylines stored differently, or a vertex appears in three polylines). Pinning the index-selection contract via vitest matches the project's pattern for newly-introduced branching logic (see `scripts/catalog-pure.test.ts`, `src/client/perceptual-magnitude.ts` + test).\n\n**Smoke step**\n`npm test` covers new disc-mask.test.ts; `npm run typecheck` clean; the manual PR test plan still passes (Orion / Betelgeuse focus + unfocus + zoom-out).","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:09:47Z","created_by":"Alex Marshall","updated_at":"2026-05-11T07:44:34Z","started_at":"2026-05-10T22:06:09Z","closed_at":"2026-05-11T07:44:34Z","close_reason":"Closed","labels":["pr-52"],"dependencies":[{"issue_id":"stellata-9mm.107","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:09:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.106","title":"[PR #54] varintLen and writeVarint duplicate the same shift-loop; encodeBlob recomputes mask twice","description":"**Confidence: High**\n\n**Finding**\n`varintLen` (lines 371-379) and `writeVarint` (lines 343-354) implement the same `do { x \u003e\u003e\u003e= 7; n++; } while (x !== 0)` loop. `writeVarint` already returns the byte count it wrote — so the byte count is computable in both directions, but `encodeBlob` calls `varintLen` to size the buffer, allocates, then calls `writeVarint` and trusts (silently) that the length matches.\n\nAdjacent issue: `writeUrl` (line 1123) calls `computePresence` to decide between '' and `?v=`, then `encodeBlob` (called by writeUrl) immediately calls `computePresence` again at line 714. Two full FIELDS_V3 walks with isPresent() per-field on every URL write.\n\n**Location**\nsrc/client/url-state.ts:343-354 (writeVarint), 371-379 (varintLen), 705-732 (computePresence + encodeBlob), 1123-1131 (writeUrl).\n\n**Suggested approach**\nEither drop `varintLen` and pre-write the varint to a 5-byte scratch `Uint8Array`, then concat with payload — or accept the duplication but unit-test that `varintLen(m) === writeVarint(_, _, m)` for masks 0, 1\u003c\u003c6, 1\u003c\u003c7, 1\u003c\u003c13, 1\u003c\u003c14, 1\u003c\u003c20, 1\u003c\u003c21, 0xffffffff so a future change to either function trips a test instead of producing a malformed blob. For computePresence: have encodeBlob return both blob and mask, and writeUrl use mask !== 0 from that single call.\n\n**Why**\nDuplicate length math is the kind of thing that drifts without notice — a bug in either function produces an off-by-one buffer over/underread that the existing round-trip tests would catch only because the version+mask path happens to be dead-fixed. Direct unit coverage of the helpers (or removing the duplication) makes that intent load-bearing.\n\n**Smoke step**\nAdd a unit test asserting `varintLen(m)` equals the byte count returned by `writeVarint` for the masks listed above; `npm test` still green.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:09:35Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:32Z","started_at":"2026-05-10T17:50:40Z","closed_at":"2026-05-10T18:17:32Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.106","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:09:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.105","title":"[PR #54] vec3FieldV3 hard-codes cam-mode special case via key-string check; decodeV3 mirrors it","description":"**Confidence: High**\n\n**Finding**\n`vec3FieldV3` (line 234) is a generic helper for cam/tgt/up/worldOffset, but inside it `componentDefaults` branches on `key === 'cam'` to return mode-aware defaults (line 237). The other three keys take the static `def` path. Symmetrically, `decodeV3` (line 792) re-introduces the same special case: `if (f.key === 'cam') camSub = dv.getUint8(off);` to capture the sub-mask for the post-pass cam fix-up.\n\nThe same per-key-string check appears in both encode and decode, in two different files-of-the-mind, and renaming the cam key (or adding a second mode-dependent vec3 like a future observe-mode tgt or up) requires editing both sites.\n\n**Location**\nsrc/client/url-state.ts:234-286 (vec3FieldV3 with embedded `key === 'cam'` branch); 780-805 (decodeV3 with embedded `f.key === 'cam'` branch).\n\n**Suggested approach**\nPass `componentDefaults` as a callback parameter to `vec3FieldV3(bit, key, getDefault)` instead of branching on key. The cam call site supplies `v =\u003e defaultCamForMode(v.mode)` and tgt/up/worldOffset supply `() =\u003e def`. `decodeV3`'s post-pass can be replaced by storing the sub-mask on the field spec or by exposing a `postDecode(view)` hook so cam's mode-dependent fix-up lives next to its encoder rather than as scattered `f.key === 'cam'` checks.\n\n**Why**\nThe cam-default-mode-aware behaviour is a single conceptual rule (closed bead 9mm.20 already extracted the const `OBSERVE_CAM_LOCAL` / `defaultCamForMode`), but the code that *uses* it still looks like three coincidences: vec3FieldV3 inlines a key check, decodeV3 inlines another key check, and applyDecodedView inlines a third call to defaultCamForMode (line 1071). Pushing the rule into the field spec localises it.\n\n**Smoke step**\n`npm test` — observe-mode cam fix-up tests (lines 155-178 of url-state.test.ts) still pass and `?v=` still round-trips a focused observe pose at \u003c 12 chars.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:09:17Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:31Z","started_at":"2026-05-10T17:50:40Z","closed_at":"2026-05-10T18:17:31Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.105","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:09:17Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.104","title":"[PR #54] FIELDS_V2 / FIELDS_V3 differ only in the four vec3 entries — duplicate ~70 lines","description":"**Confidence: High**\n\n**Finding**\n`FIELDS_V2` (lines 515-599) and `FIELDS_V3` (lines 607-678) are byte-for-byte identical for bits 3-19 and bit 21 — that is 16 of 20 field specs duplicated verbatim, including the inline preset/con/flags/focusCleared/pois/t entries with their full encode/decode bodies. The only differences are bits 0/1/2/20 swapping `vec3Field` for `vec3FieldV3`. A field-shape change in one (say, repurposing the 23-bit ID space, fixing a bug in pois encode) silently has to be applied twice or the two tables drift.\n\n**Location**\nsrc/client/url-state.ts:515-678\n\n**Suggested approach**\nBuild the two tables from a single shared base. One option: `buildFields(vec3Spec: (bit, key) =\u003e FieldSpec)` returning the full 20-entry list parameterised on the vec3 helper. `FIELDS_V2 = buildFields(vec3Field)` and `FIELDS_V3 = buildFields(vec3FieldV3)`. The frozen-decoder concern is preserved because v2's vec3Field stays unchanged; the dedup is on the 16 shared entries. FIELDS_V1 stays separate (different scalar shape) per the existing comment, but the v2/v3 split is a copy.\n\n**Why**\nThe file's own comment block at the top (`Adding a field… freeze the old one verbatim so its decoder stays correct`) only argues for *not retiring* old tables — it doesn't argue for textual duplication. A shared builder keeps v2 frozen *by construction* while eliminating ~70 lines of code that would otherwise need to be edited in lockstep for any non-vec3 field change.\n\n**Smoke step**\n`npm test` — all 503 v3 round-trip + v1/v2 backward-compat tests should still pass.\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:08:55Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:31Z","started_at":"2026-05-10T17:50:39Z","closed_at":"2026-05-10T18:17:31Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-9mm.104","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:08:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.103","title":"MAGIC and BINARY_VERSION coupling not enforced (currently 'HYG4'/4)","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\n`MAGIC = 'HYG4'` and `BINARY_VERSION = 4` are currently in lockstep — the trailing digit of MAGIC matches the version. The PR's instructions to future contributors (catalog-pure.ts:215, docs/build-and-data.md) say 'bump BINARY_VERSION + MAGIC' as if they're a coordinated pair, but nothing enforces the convention. A future PR could bump version to 5 while leaving MAGIC at 'HYG4', and only the runtime version check (`Bad magic` is fine, `Unsupported catalog version: 5 (expected 4)` is the only catch — but only against an old catalog.bin file).\n\nThe risk is small (any divergence is loudly caught at next `npm run build:catalog` + dev load), but the convention is a load-bearing one and would benefit from a one-line test.\n\n**Location**\nscripts/catalog-pure.ts:261-262\n\n**Suggested approach**\nAdd to the `binary-format constants` describe block: `expect(MAGIC.endsWith(String(BINARY_VERSION))).toBe(true)`. One line, pins the documented convention. If the convention isn't worth keeping (e.g. v10 makes the digit-suffix scheme awkward), drop it from prose and do nothing.\n\n**Why**\nThe PR's own description says 'bump BINARY_VERSION + MAGIC in catalog-pure.ts' — i.e. they are in sync by convention. Tests should pin convention if it's load-bearing.\n\n**Smoke step**\n`npm test` after adding the assertion. Bump only one of MAGIC/BINARY_VERSION and verify the test catches it.","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:49Z","created_by":"Alex Marshall","updated_at":"2026-05-12T11:40:16Z","closed_at":"2026-05-12T11:40:16Z","close_reason":"v5 scaffolding landed in 24c34cb; coupling assertion folded in","labels":["pr-42"],"dependencies":[{"issue_id":"stellata-9mm.103","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.101","title":"startUrlSync lastCam seed/update duplicates the same 9-line snapshot pattern","description":"**Confidence: Medium**\n\n**Finding**\n`startUrlSync` writes the same 3-vec3 (camera.position, controls.target, camera.up) into the lastCam Float64Array in three places: the new initial seed (lines 933-939), the per-frame change-detector body (970-972), and (implicit) every comparison block at 963-967. The seed and the update bodies are textually identical. A small extracted helper would document the layout and prevent index-mismatch drift.\n\n**Location**\n- src/client/url-state.ts:933-939 (new initial-seed block — added in PR #39)\n- src/client/url-state.ts:963-972 (existing per-frame compare + write)\n\n**Suggested approach**\nExtract `function snapshotCam(stellata: Stellata, out: Float64Array)` (or method on a tiny class) that writes the 9 components with a single line per axis triple, e.g. via three Vector3.toArray() calls into out at offsets 0/3/6. Call once for the seed and once per-frame after the change check fires. Optional: a parallel `function camChangedSinceSnapshot(...)` for the comparison block, but that's a bigger refactor — keep this bead scoped to the snapshot helper.\n\n**Why**\nThe change-detector layout (`[0..2] camera.position, [3..5] controls.target, [6..8] camera.up`) is now embedded in two places. Adding a fourth axis (e.g. fov, future zoom) means editing two parallel sections that must stay aligned by index. A helper centralises the layout and makes additions one-line.\n\n**Smoke step**\n`npm test`: existing url-state tests cover the per-frame writer; manual smoke is bare-URL load → URL stays empty until first interaction (the contract this seed protects).","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:45Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:47Z","started_at":"2026-05-11T07:56:31Z","closed_at":"2026-05-11T08:46:47Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-39"],"dependencies":[{"issue_id":"stellata-9mm.101","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.100","title":"perf-hud colour ramp uses unnamed thresholds (4 ms, 0.7×budget, 2× cap)","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\n`colourForAvg` uses a literal `4` (ms) threshold for the green→amber transition; the histogram bar ramp uses `MS_PER_FRAME_60 * 0.7 ≈ 11.67` for the same green→amber transition; the visible-height cap uses `MS_PER_FRAME_60 * 2`. Three magic numbers, two of them encoding similar visual semantics (\"approaching the budget\") at different scales, neither named.\n\n**Location**\nsrc/client/perf-hud.ts:262-264 (`colourForAvg`: `avg \u003e 4`), :317 (`const cap = MS_PER_FRAME_60 * 2`), :333-335 (histogram colour ramp: `MS_PER_FRAME_60 * 0.7`)\n\n**Suggested approach**\nHoist named constants near the existing `MS_PER_FRAME_60` block. Suggested:\n- `AVG_AMBER_MS = 4` — \"row colour amber threshold (avg ms)\"\n- `HISTO_AMBER_RATIO = 0.7` — \"histogram bar amber threshold as fraction of MS_PER_FRAME_60\"\n- `HISTO_HEIGHT_CAP_MULT = 2` — \"histogram bar visible-height cap as multiple of MS_PER_FRAME_60\"\n\nOptionally collapse the row + bar colour ramps to a shared helper that takes an amber threshold and a red threshold parameter, since both ramps are 3-stop linear (green / amber / red).\n\n**Why**\nDRY + readability. The colour-ramp logic is duplicated almost verbatim between row and histogram; the only difference is the amber threshold (one absolute, one ratio). A reader has to triangulate three sites to understand the calibration.\n\n**Smoke step**\n`npm run typecheck \u0026\u0026 npm test`. Manual: `debug.panel()`, induce sustained \\\u003e16.7 ms frames (toggle a heavy filter), confirm bars and rows transition through amber → red as before.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:44Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:47Z","started_at":"2026-05-11T07:56:32Z","closed_at":"2026-05-11T08:46:47Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-41"],"dependencies":[{"issue_id":"stellata-9mm.100","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:43Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.97","title":"modal-dismiss: querySelectorAll once at bind time — late-added dismiss buttons aren'\\''t wired","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\n`bindModalDismissal` runs `modal.querySelectorAll\u003cHTMLElement\u003e(\"[data-modal-dismiss]\")` once at bind time and attaches click handlers. If a modal's contents are later mutated (e.g. dynamic close button injected into a content area), the new dismiss button won't be wired. The pre-refactor code had the same behavior, but consolidating into a helper is the right time to decide on the contract.\n\n**Location**\nsrc/client/modal-dismiss.ts:36-38\n\n**Suggested approach**\nTwo options:\n1. Leave as-is and document \"modal contents must not change after bind\" on `bindModalDismissal`.\n2. Switch to a single delegated click listener on `modal`: `modal.addEventListener(\"click\", e =\u003e { if ((e.target as Element).closest(\"[data-modal-dismiss]\")) close(); })`. Late-added dismiss buttons just work, and the per-button listener overhead disappears.\n\nOption 2 is the single-line change that future-proofs the helper.\n\n**Location**\nsrc/client/modal-dismiss.ts:36-38\n\n**Why**\nToday every modal's dismiss buttons are static, so the bug doesn't exist in production. But the helper is going to be the modal pattern going forward; pinning the contract now (or making it robust) saves a debugging session later.\n\n**Smoke step**\nAfter applying #2, open about modal, run `document.getElementById(\"about-modal\").querySelector(\".modal-card\").insertAdjacentHTML(\"beforeend\", \"\u003cbutton data-modal-dismiss\u003eextra\u003c/button\u003e\")`, click the new button, confirm modal dismisses.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:36Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:34Z","labels":["pr-40"],"dependencies":[{"issue_id":"stellata-9mm.97","depends_on_id":"stellata-9mm.191","type":"parent-child","created_at":"2026-05-15T12:53:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.95","title":"readPipeDelimited's existsSync check duplicates main()'s outer gate","description":"**Confidence: Medium**\n\n**Finding**\n`readPipeDelimited` checks `if (!existsSync(path)) return` internally, but `main()` already gates the only two call sites (`parseGcvsMain()` and `parseGcvsCrossref()`) behind `if (existsSync(SRC_GCVS) \u0026\u0026 existsSync(SRC_GCVS_XREF))` (build-catalog.ts:682). The inner check fires three times per build (twice in the helper + once in the outer gate) for the same files; under normal flow it's dead defensive code. `parseHipCcdm` is structured the opposite way — it self-gates (line 323) and `main()` also gates (line 699), but that's at least symmetric across both GCVS-style paths.\n\n**Location**\nscripts/build-catalog.ts:226-233 (helper), 682 (outer gate around the two GCVS parsers)\n\n**Suggested approach**\nEither: (a) drop the `existsSync` from `readPipeDelimited` and rely on the outer gate (simplest; matches the helper's stated 'pure pipe-delimited reader' purpose); or (b) drop the outer `existsSync` pair in main() and rely on the helper's per-file no-op (lets `parseGcvsMain`/`parseGcvsCrossref` be safely callable in any context). (a) is preferred because the outer gate also wraps the log-line / timing block, so `main` would still need to know whether the data files were present to print 'GCVS files not found; skipping…'. Keep the outer gate, drop the inner one.\n\n**Why**\nThe PR's framing is dedup, but the dedup leaves two 'is the file there?' checks side-by-side. One is the system-of-record (the outer gate decides whether to even enter the GCVS branch and emits the user-facing log message); the other is a no-op fallback. Tightening the inner helper to assume its caller already checked makes the contract clearer and removes a read of the filesystem on a build hot path (~hundreds of thousands of stars × N file stats is negligible, but the principle is cleaner).\n\n**Smoke step**\n`npm run build:catalog` reports the same record counts (313242 records, 11316 Hip + 13926 HD GCVS xrefs, 3677 variables matched) before and after the change. `npm test` unaffected (helper still pure inside).","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:33Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:46Z","started_at":"2026-05-11T07:56:30Z","closed_at":"2026-05-11T08:46:46Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-49"],"dependencies":[{"issue_id":"stellata-9mm.95","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.94","title":"Section builder API split: star/milkyway return HTMLDivElement, perf/pin return {element, dispose, setVisible} — unify","description":"**Confidence: High**\n\n**Finding**\nThe four section builders wired into `debug.ts togglePanel` have two distinct shapes:\n\n- `buildStarSection(stellata): HTMLDivElement` and `buildMilkywaySection(layer): HTMLDivElement` both build the `makeCollapsibleSection` themselves and return the wrapped section ready to append.\n- `buildPerfSection(): {element, dispose, setVisible}` and `buildPinSection(stellata): {element, dispose, setVisible}` return a builder triple, and `togglePanel` does the `makeCollapsibleSection` wrapping at the call site, manually wires `onCollapseChange → setVisible`, AND manually calls `setVisible(!isCollapsed())` once after wiring (because `onCollapseChange` doesn't fire on init).\n\nThe result is ~25 lines of duplicated boilerplate in `togglePanel` per dispose-needing section, and adding a fifth section means replicating the pattern again. The asymmetry leaks into the docstring at debug.ts:24 (\"either a section element or a {element, dispose, setVisible} triple\") which is itself a design smell.\n\n**Location**\nsrc/client/debug.ts:55-94 (togglePanel), src/client/star-tuning.ts:18-22 + src/client/milkyway-tuning.ts:31-35 (build their own section), src/client/perf-hud.ts:117 + src/client/pin-debug-hud.ts:57 (return a triple).\n\n**Suggested approach**\nPick one shape. Two options:\n\n1. *All sections return a triple.* Push `makeCollapsibleSection` into `togglePanel` (or a single-line helper `mountSection({title, storageKey, build})`) for every section. Star/milkyway gain a no-op `dispose` and a stub `setVisible` (or `setVisible` becomes optional). `togglePanel` reduces to a four-element array iteration.\n\n2. *All sections handle their own collapsing.* Star/milkyway already do. Perf/pin take a `storageKey` and `makeCollapsibleSection` themselves, returning `{section, dispose}` or just `HTMLDivElement` plus a separate dispose registration callback. `togglePanel` collects disposers via a `disposers: (() =\u003e void)[]` array.\n\nOption 2 keeps the current return-shape of star/milkyway but loses the \"each section shape mirrors its needs\" intentional split. Option 1 unifies and is more iteration-friendly but slightly more ceremony for the sliders-only sections. I lean (1) — easier extension and lets the dispose/visibility lifecycle be a first-class capability.\n\n**Why**\nPer CLAUDE.md priority (Architectural fit). The unification is the explicit purpose of this PR (\"Unify debug panel\"); having two builder shapes inside the unified panel undercuts that goal and creates the exact \"copy this paragraph\" tax the next dev will hit when they add a section. Also tightens `togglePanel` so it stops being a per-section transcription.\n\n**Smoke step**\n`npm run typecheck`, `npm test`. Open panel, confirm all four sections render + collapse + persist. Close + reopen, confirm sessionStorage state survives. Reload, confirm collapse state resets.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:32Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:35Z","labels":["pr-44"],"dependencies":[{"issue_id":"stellata-9mm.94","depends_on_id":"stellata-9mm.190","type":"parent-child","created_at":"2026-05-15T12:53:49Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.93","title":"AU_PC redefined locally in first-load.test.ts and heliopause.test.ts instead of imported from ephemeris","description":"**Confidence: High**\n\n**Finding**\nTwo test files redefine the AU_PC constant inline (`const AU_PC = 1 / 206264.80624709636;`) rather than importing the canonical export from ephemeris.ts. If the canonical value were ever refined (e.g. to a higher-precision IAU figure), the test constants would silently diverge from production code and tests could pass against a wrong reference value.\n\n**Location**\n- src/client/first-load.test.ts:5 (new in PR #39)\n- src/client/heliopause.test.ts:5 (pre-existing; now joined by a second copy)\n- canonical source: src/client/ephemeris.ts:23 (re-exported at ephemeris.ts:324, also re-exported from orbit-rings-layer.ts:35)\n\n**Suggested approach**\nReplace the local `const AU_PC = ...` in both test files with `import { AU_PC } from './ephemeris';`. One line of import, one line of redefinition removed each. Apply to both files in a single bead since they are the same fix and currently the *only* sites of redefinition.\n\n**Why**\nTests verifying physical-unit conversions should reference the same constant the production code uses; otherwise a typo or precision update in ephemeris.ts could silently desynchronise the test reference. This is also the project's existing convention — see orbit-rings-layer.ts:35 where AU_PC is even re-exported for downstream callers, exactly to avoid each module redefining its own copy.\n\n**Smoke step**\n`grep -rn 'const AU_PC' src/` returns zero matches outside ephemeris.ts; `npm test` still green.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:29Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:45Z","started_at":"2026-05-11T07:56:29Z","closed_at":"2026-05-11T08:46:45Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-39"],"dependencies":[{"issue_id":"stellata-9mm.93","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:28Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.92","title":"No regression test for the name-table layout (zero-sentinel + length-prefixed UTF-8)","description":"**Confidence: Medium**\n\n**Finding**\nThe catalog binary's name-table format is described in catalog-pure.ts:255-259 (\"Two zero bytes of padding so name offset 0 reads as the 'no name' sentinel, followed by length-prefixed UTF-8 strings: uint16 byteLen, then byteLen bytes\"). The PR added pin tests for `HEADER_LAYOUT` and `RECORD_LAYOUT` but not for this third layout component. `catalog-loader.test.ts` exercises the table indirectly (the 'name table' describe block, lines 295-356) but each test instantiates names through the synthetic `buildCatalog` helper, which itself is a parallel implementation of the writer's chunk-builder logic in build-catalog.ts:711-732 — drift between the two would not be caught.\n\n**Location**\nscripts/catalog-pure.ts:255-259 (spec); scripts/build-catalog.ts:711-732 (writer); src/client/catalog-loader.ts:145-169 (reader); src/client/catalog-loader.test.ts:36-100 (synthetic builder)\n\n**Suggested approach**\nAfter the round-trip helper in 9mm.80 lands, extend it to cover the name-table portion: writer emits the canonical encoding, reader resolves names by offset, and a property-style test fuzzes name lists (including UTF-8 multi-byte chars, empty list, very long names near the 65535 cap). Alternatively, add direct constants for the name-table contract: `NAME_TABLE_PADDING = 2`, `NAME_LENGTH_PREFIX_BYTES = 2`, and a focused unit test that pins them.\n\n**Why**\nName-table layout is part of the 'binary format' the PR centralised but only two of the three components (header, record) got the regression block. The third one stays pinned only by the prose comment.\n\n**Smoke step**\n`npm test` after the change. Deliberately omit the 2-byte zero-sentinel padding from the writer and verify the new test fails.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:27Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:45Z","started_at":"2026-05-11T08:02:10Z","closed_at":"2026-05-11T08:46:45Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-42"],"dependencies":[{"issue_id":"stellata-9mm.92","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:27Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.90","title":"No-reverse-sync caveat duplicated across star-tuning.ts and milkyway-tuning.ts","description":"**Confidence: High**\n\n**Finding**\nPR #47 added a near-identical \"No reverse sync\" header comment to both src/client/star-tuning.ts (lines 13-16) and src/client/milkyway-tuning.ts (lines 14-17). The wording is essentially the same — both warn that makeSlider's `initial` is a snapshot at build time and external writes will not move the thumb. Ironically a cleanup PR introduced a small DRY violation: any third tuning section will copy the same caveat again.\n\n**Location**\nsrc/client/star-tuning.ts:13-16\nsrc/client/milkyway-tuning.ts:14-17\n(Pattern lives in src/client/debug-panel.ts:302-344 — makeSlider / SliderOpts.initial.)\n\n**Suggested approach**\nHoist the caveat onto makeSlider (or SliderOpts.initial) as a JSDoc, where the snapshot semantics actually live. The per-section header comments can collapse to one line (\"No reverse sync — see makeSlider.\"). A future tuning section then inherits the contract via the JSDoc instead of copy-pasting.\n\n**Why**\nThe \"no reverse sync\" property is a property of makeSlider, not of the star-disc or Milky Way sections. Locating the caveat at its source is the standard fix for \"same comment in multiple consumers\" — it survives Cmd-click, IDE hover, and grep. The current state will only get worse: PR #47 itself acknowledges \"presets\" as a hypothetical future writer, and adding presets is the kind of follow-up that will spawn more tuning sections each carrying their own copy of the same comment.\n\n**Smoke step**\nNone needed — pure comment relocation. npm run typecheck clean.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:23Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:44Z","started_at":"2026-05-11T07:53:53Z","closed_at":"2026-05-11T08:46:44Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-47"],"dependencies":[{"issue_id":"stellata-9mm.90","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.89","title":"modal-dismiss: per-modal keydown listener — multiple-open modals all close on one ESC","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nThe new `bindModalDismissal` adds a fresh `keydown` listener every time a modal opens, and the listener only checks `if (!modal.hidden) close()`. If two modals using this helper are open simultaneously (e.g. some future flow opens credits while about is still open), an ESC keypress fires both listeners and closes both. The previous brand-modal had a SINGLE shared listener that walked both modals — same problem under the same hypothetical, but the failure mode was visible at one site. With the refactor it's now a contract of `bindModalDismissal` itself.\n\n**Location**\nsrc/client/modal-dismiss.ts:23-25\n\n**Suggested approach**\nTwo reasonable fixes; pick whichever fits the design intent:\n1. Document the helper as \"single-modal-at-a-time\" and add a runtime warn if a second `open()` is called while another bound modal is still showing.\n2. Make the helper aware of an \"active modal stack\" so ESC only closes the top one (last-opened wins).\n\nToday every caller (info / about / credits / help) opens exactly one modal at a time so this is theoretical. Worth filing because the consolidated helper now silently bakes this behavior into every future modal user.\n\n**Why**\nThe previous code had the same coupling but a future maintainer adding a fifth modal had to look at brand-modal.ts's shared-listener pattern and reason about it. Now it's buried in a generic helper; the gotcha is invisible.\n\n**Smoke step**\nOpen about-modal then credits-modal via dev-console (`document.getElementById(credits-modal).hidden = false`); press ESC and confirm both close together. If that's acceptable, document it; if not, implement #2.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:22Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:39Z","labels":["pr-40"],"dependencies":[{"issue_id":"stellata-9mm.89","depends_on_id":"stellata-9mm.191","type":"parent-child","created_at":"2026-05-15T12:53:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.88","title":"docs/build-and-data.md: search-index section doesn't reference SearchEntry","description":"**Confidence: Medium**\n\n**Finding**\nThe PR makes `SearchEntry` in scripts/catalog-pure.ts the single source of truth for the search-index.json wire format and adds in-file comments framing it as the shared writer↔reader contract. `docs/build-and-data.md` has a 'Search index (`public/search-index.json`)' section (lines 69-89) that still describes the format procedurally — 'Short keys (`i/p/b/f/hip/hd/hr/gl/c/s`) to keep wire size down' — without pointing to where the canonical TypeScript definition lives. Future maintainers reading the doc won't know there's a single interface to update.\n\n**Location**\ndocs/build-and-data.md:69-89 (Search index section)\n\n**Suggested approach**\nAdd a one-line pointer in the search-index section: 'Field shape pinned in `scripts/catalog-pure.ts` as the `SearchEntry` interface — the writer (build-catalog.ts) and the reader (src/client/search.ts) both import it; drift = compile error.' Same convention this doc already uses for the binary record layout ('Layout consistency is pinned by the binary-format constants block in scripts/catalog-pure.test.ts').\n\n**Why**\nDiscoverability. The file-format paragraph in docs/build-and-data.md is the natural place a new contributor goes when extending the search index (e.g. adding alternate designations per stellata-nxb). Without the pointer, they'll re-introduce drift by editing one side without the other.\n\n**Smoke step**\nVisual review of the rendered markdown; the pointer should sit alongside the existing 'pinned by ... .test.ts' note for the binary layout.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:14Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:44Z","started_at":"2026-05-11T07:53:51Z","closed_at":"2026-05-11T08:46:44Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-49"],"dependencies":[{"issue_id":"stellata-9mm.88","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.86","title":"Stale 'Orion-framed' / 'Orion highlighted' comments document a dropped draft","description":"**Confidence: High**\n\n**Finding**\nTwo comments still describe an earlier draft of the first-load view in which Orion was highlighted. The shipped feature parks at the galactic centre with NO constellation highlight (FIRST_LOAD_VIEW.con is undefined and the test at first-load.test.ts:32 explicitly asserts this). The stale comments contradict the code and the docs.\n\n**Location**\n- src/client/main.ts:154-155 — \"…canonical first-load view (Sol focus, parked at 5 AU framing Orion, HUD on, Orion highlighted) — stellata-vjm.\"\n- CLAUDE.md:162 — \"5 AU Orion-framed park via first-load.ts\"\n\n**Suggested approach**\nReplace both with the galactic-centre framing language used in the PR body, README, first-load.ts module header, and docs/solar-system.md (\"5 AU Sol park aimed at the galactic centre, HUD on, no constellation highlight\").\n\n**Why**\nComments documenting load-bearing behaviour need to match the code; future maintainers reading main.ts will look at these to decide whether to preserve Orion-framing on changes that touch the first-load path. CLAUDE.md is the always-loaded entry point for Claude-Code sessions and a wrong description there propagates into every downstream session.\n\n**Smoke step**\ngrep -n 'Orion' CLAUDE.md src/client/main.ts and confirm zero hits in the first-load region; visual smoke (load /) still shows galactic-centre framing.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:12Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:43Z","started_at":"2026-05-11T07:53:52Z","closed_at":"2026-05-11T08:46:43Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-39"],"dependencies":[{"issue_id":"stellata-9mm.86","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.85","title":"HEADER_LAYOUT shape loses field-type info — only inline comments capture uint32 vs ASCII","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\n`HEADER_LAYOUT` and `RECORD_LAYOUT` are typed as `{name: number}` (offset only). Field-type information (`uint8`, `uint16`, `uint32`, `float32`, `ASCII4`) lives only in `// uint32`-style comments next to each entry. Consequence: writers and readers must remember which `view.setXxx` / `view.getXxx` to call per field, the test must mirror the type info into a parallel `sizes` table (see 9mm.71), and an automated reader/writer pair (see 9mm.80) can't be expressed without re-stating the type info a third time.\n\n**Location**\nscripts/catalog-pure.ts:267-293 (HEADER_LAYOUT, RECORD_LAYOUT)\n\n**Suggested approach**\nPromote each layout entry to `{ offset: number; kind: 'u8' | 'u16' | 'u32' | 'f32' | 'ascii4' }`. Provide tiny `readField(view, base, spec)` / `writeField(view, base, spec, value)` helpers that switch on `kind`. Writer and reader then read/write by name — `writeField(view, off, RECORD_LAYOUT.physRadius, s.physicalRadius)` — and the per-call `setFloat32` boilerplate vanishes. Trade-off: a small per-call switch overhead in two hot loops (writer is one-shot at build, but reader runs 313k×). Worth measuring before adopting.\n\nThe 'maybe unnecessary scaffolding' angle: the current shape is concise and the PR's regression test pins offsets. Promoting kind into the const may be over-engineering for a 4-month-old format with one writer and one reader.\n\n**Why**\nFiles this as 'needs input' because it is fundamentally a tradeoff between elegance (single source for offset+type) and reading-loop cost. Alex should decide whether the reader-loop cost is acceptable. A middle path: keep the offset-only consts, but export a `RECORD_FIELD_KINDS` const next to them so the test (and any future round-trip helper) imports it instead of re-stating sizes.\n\n**Smoke step**\nIf pursuing the kind-aware shape: `npm test` plus a perf-hud measurement of `parseBinary` cold-load time, comparing before/after on the full ~313k catalog (target: no measurable regression beyond noise).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:08Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:40Z","labels":["pr-42"],"dependencies":[{"issue_id":"stellata-9mm.85","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-15T12:54:09Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.84","title":"Test gap: extract perf-hud pure helpers (summarize, insertSorted, colourForAvg) for vitest coverage","description":"**Confidence: High**\n\n**Finding**\n`perf-hud.ts` has three pure helpers used per-tick by `renderPanel()` that have non-trivial behaviour and zero test coverage: `summarize()` (avg+max over a partial-fill ring buffer — must respect `s.count`, not `s.ring.length`), `insertSorted()` (partial top-N sort: keeping `MAX_TABLE_ROWS=8` descending-by-avg with bounded splice), and `colourForAvg()` (threshold step at the 60Hz frame budget and the 4ms warning).\n\nThe current `perf-hud.test.ts` only covers the \"no-op API stays callable\" contract. The three helpers above are exactly the kind of \"obvious-looking but easy to off-by-one\" code that benefits most from a 10-line test file. Specifically: insertSorted with arr.length === MAX_TABLE_ROWS and a value smaller than every element should be a no-op, not a splice that walks past the cap; summarize with count=0 must return zeros (currently does), with count\u003cRING_SIZE must average over count not RING_SIZE (currently does — but a regression here would silently halve early-frame averages).\n\n**Location**\nsrc/client/perf-hud.ts:248-258 (summarize), :262-264 (colourForAvg), :358-366 (insertSorted), :260 (fmtMs).\n\n**Suggested approach**\nExtract the four helpers into `perf-hud-pure.ts` and re-import them from `perf-hud.ts`. Add `perf-hud-pure.test.ts` covering: summarize over a partial-fill ring (count=3, ring partially populated); insertSorted top-3 with ascending and descending input streams; insertSorted no-op when below smallest existing avg at cap; colourForAvg at 4.0/4.0001/16.6/16.7 boundary samples; fmtMs(9.99) vs fmtMs(10.0) format flip.\n\n**Why**\nPer CLAUDE.md priority (Tests). The previous PR #41 (stellata-r48) optimised this exact path — preserving its correctness through future refactors needs lock-in via tests. The functions are textbook-pure; cost of extraction is trivial.\n\n**Smoke step**\n`npm test` shows new module covered. Run a session, expand Perf section, confirm headline + table + histogram still update at ~5Hz with same numbers as pre-refactor (eyeball comparison).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:07Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:41Z","labels":["pr-44"],"dependencies":[{"issue_id":"stellata-9mm.84","depends_on_id":"stellata-9mm.190","type":"parent-child","created_at":"2026-05-15T12:53:50Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.83","title":"perf-hud test only covers no-op contract; insertion-sort + dirty-track + section-GC untested","description":"**Confidence: Medium**\n\n**Finding**\nPR #41 added `src/client/perf-hud.test.ts` with two cases, both covering only the \"no-op until installed\" API contract (`mark/measure/frame` don't throw before install). The actual code-quality value of the PR — single-pass insertion sort, per-row / per-bar dirty-tracking, the panel rebuild→mutate switch — has zero regression coverage. Pre-existing logic that's also uncovered: section garbage-collection after `RING_SIZE` silent frames (commented at perf-hud.ts:90-95 as critical for chart.* dropoff after exit) and `summarize` (sum/avg/max).\n\n**Location**\nsrc/client/perf-hud.test.ts (entire file), src/client/perf-hud.ts:90-95 (GC), :248-258 (summarize), :355-365 (insertSorted)\n\n**Suggested approach**\nAdd tests against the pure helpers without DOM: export `insertSorted` and `summarize` (or a test-only entrypoint). For `insertSorted`: descending-by-avg ordering, fixed cap behaviour (9th input drops the lowest), tie-breaking semantics, empty-input handling. For `summarize`: count=0 returns {0,0}, ring-buffer wraparound. For section GC: install, fire `mark/measure` for two labels, fire `RING_SIZE+1` frames without re-marking one, assert that label is gone (would require exposing `sectionsForTest()` or similar). Skip the DOM-render path tests if the cost of jsdom setup outweighs the value — but the dirty-track contract is the core deliverable so probably worth a smoke that confirms repeated render() calls with stable inputs don't emit redundant style writes (Proxy on style or count setter calls).\n\n**Why**\nTest-coverage gap matches the priority of the change. The PR's own description claims \"+2 for perf-hud no-op API contract\" — accurate, but the no-op contract was the lowest-risk piece. The new sort + dirty-track is the part that could regress silently (already does in part — see colour-comparison bead).\n\n**Smoke step**\n`npm test` shows new green cases for insertion-sort, summarize, and section GC. CI parallel runs unaffected.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:06Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:42Z","labels":["pr-41"],"dependencies":[{"issue_id":"stellata-9mm.83","depends_on_id":"stellata-9mm.190","type":"parent-child","created_at":"2026-05-15T12:53:51Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.82","title":"chart-labels.ts projectVec comment misleading: shared scratch is module-level not 'fresh'","description":"**Confidence: High**\n\n**Finding**\nThe new `projectVec` in chart-labels.ts has a comment claiming \"The overlay-project scratch is fresh on every call, so the shared module-level scratch costs no allocation.\" That contradicts itself: overlay-project.ts uses a module-level `scratch` Vector3 that is reused across ALL callers (chart-labels, hud, poi, focus-ring, disc-mask, constellation), not \"fresh\". The intended meaning was probably \"the shared scratch is owned per-call (no caller-supplied scratch needed)\" but as written it reads like an incorrect denial of the sharing.\n\n**Location**\nsrc/client/chart-labels.ts:744-749\n\n**Suggested approach**\nRewrite to: \"// projectToScreen owns its own module-level scratch; passing `p` as input is safe because projectToScreen does scratch.copy(p).\" That accurately describes the contract callers depend on (input is preserved, no per-call allocation).\n\n**Why**\nFuture maintainers reading this comment may believe the scratch is per-call and try to interleave projectToScreen calls with concurrent reads of an unrelated Vector3, which is fine — but they may also write back to the scratch assuming exclusivity, which is wrong. Accurate comments are the contract.\n\n**Smoke step**\nNone — comment-only change.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:05Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:43Z","started_at":"2026-05-11T07:53:53Z","closed_at":"2026-05-11T08:46:43Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-40"],"dependencies":[{"issue_id":"stellata-9mm.82","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:05Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.81","title":"Test gap: SearchEntry wire-format keys not pinned","description":"**Confidence: High**\n\n**Finding**\nThe PR promotes `SearchEntry` to `scripts/catalog-pure.ts` as the shared writer↔reader contract for `search-index.json` (~13 MB raw across hundreds of thousands of entries) and explicitly frames it the same way the binary record layout is framed: 'drift here ships a broken index'. The binary record layout has a regression-pinning test (the `catalog-pure / binary-format constants` describe block in catalog-pure.test.ts: header offsets non-overlap, record offsets non-overlap, fields cover RECORD_SIZE, flag bits are distinct powers of two). The new `SearchEntry` interface gets no equivalent pin.\n\n**Location**\nscripts/catalog-pure.ts:295-313 (interface); scripts/catalog-pure.test.ts (no covering describe block)\n\n**Suggested approach**\nAdd a small `describe('catalog-pure / search-index wire contract', ...)` block that asserts the field-name set and required-vs-optional shape — for example: build a sample `SearchEntry` exercising every optional field, JSON.stringify it, and check the key set against an explicit literal (`['i','p','b','f','c','s','hip','hd','hr','gl']`). Bonus: assert that the writer-side optional-field omission convention round-trips (i.e. no `undefined` keys end up in the JSON). The test should fail loudly if anyone renames a key or drops one without a coordinated reader change.\n\n**Why**\nThe whole motivation in the PR description — 'drift is now a typecheck failure rather than a broken index at runtime' — only holds for typecheck-time changes within the same TypeScript build. A runtime change (e.g. some future build script writing `{ index: ... }` instead of `{ i: ... }`) sails past tsc but breaks every consumer. Mirroring the binary-format pinning closes that gap.\n\n**Smoke step**\nNew test case fails when a key in the interface is renamed; passes against current code. `npm test` still reports 485+ passing tests (no regression in existing suite).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:00Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:43Z","labels":["pr-49"],"dependencies":[{"issue_id":"stellata-9mm.81","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-15T12:54:07Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.80","title":"No round-trip test from build-catalog writer to catalog-loader parser","description":"**Confidence: High**\n\n**Finding**\nThe PR moves writer + reader onto shared `RECORD_LAYOUT` constants — but there is no test that actually runs the writer's record-emit logic and feeds it to `parseBinary`. `catalog-loader.test.ts` has its own synthetic `buildCatalog()` that mirrors the writer (lines 36–100); `scripts/catalog-pure.test.ts` doesn't write binaries at all. So the writer side of the layout sharing is exercised only at build time (verified manually via the catalog.bin md5 in the PR description) — not under `npm test`.\n\nIf a future change to `build-catalog.ts` introduces a writer-only bug — e.g. a stray `view.setUint16` where `setUint8` was intended, or an off-by-one in the variable-fields branch — vitest catches nothing because the writer is in a side-effecting CLI script, not a tested function. The next deploy would silently ship a corrupt binary.\n\n**Location**\nscripts/build-catalog.ts:749-783 (record write loop); src/client/catalog-loader.ts:124-143 (record read loop)\n\n**Suggested approach**\nExtract the per-record write and the per-record read into pure functions in catalog-pure.ts:\n- `writeRecord(view: DataView, off: number, record: BinaryRecord): void`\n- `readRecord(view: DataView, off: number): BinaryRecord`\n\nThe current writer/reader keep their loop structure but call those helpers. Add a vitest property-style test that fuzzes records, writes via `writeRecord`, reads via `readRecord`, and asserts equality (with quantisation tolerance for ampUnits/period). Bonus: `writeRecord` and `readRecord` are the natural seam where the layout const becomes load-bearing — drift in either direction (writer OR reader) gets caught.\n\n**Why**\nThe PR's correctness argument is 'md5 matches main', i.e. byte-for-byte identical to the un-refactored writer. That's a one-shot fact about today, not a regression guarantee for tomorrow's changes to either side. A round-trip property test pins the contract for the lifetime of v4.\n\n**Smoke step**\n`npm test`; deliberately swap two field assignments in build-catalog.ts (e.g. `physRadius` ↔ `ci`) and verify the round-trip test catches it.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:04:48Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:44Z","labels":["pr-42"],"dependencies":[{"issue_id":"stellata-9mm.80","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-15T12:54:07Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.79","title":"Test gap: extract debug-panel.ts pure helpers (clampToViewport, rgbToHex, hexToRgb) to a tested module","description":"**Confidence: High**\n\n**Finding**\n`debug-panel.ts` has three small pure functions that are easy to break and impossible to unit-test in their current form: `clampToViewport`, `rgbToHex`, and `hexToRgb`. None are exported. The colour round-trips are particularly bug-prone (off-by-one rounding at channel boundaries; NaN propagation when hex is malformed). `clampToViewport` is the only thing keeping a saved position from being unreachable on a smaller window — also worth pinning.\n\nAdditionally, `loadPosition` uses `typeof === 'number'` which lets NaN pass through; a once-saved bad position would clamp to the corner forever. `Number.isFinite` is safer.\n\n**Location**\nsrc/client/debug-panel.ts:23-53 (clampToViewport, loadPosition), src/client/debug-panel.ts:371-383 (rgbToHex/hexToRgb).\n\n**Suggested approach**\nExtract the four helpers into `debug-panel-pure.ts` (or a small `color-util.ts` for the colour pair if they get reused) and add a vitest `debug-panel-pure.test.ts`. Cover: clamp at all four edges + below-margin + viewport-smaller-than-panel; rgb→hex→rgb round-trip preserves ~3-decimal precision; hex with shorthand or bad input fails predictably; loadPosition rejects NaN-shaped JSON. Keep `makeDebugPanel`/`makeSlider`/`makeColor` as the DOM-coupled exports they already are.\n\n**Why**\nPer CLAUDE.md priority (Tests). Pure functions with no test coverage are exactly the easy wins — once extracted they cost ~30 lines of vitest and prevent the low-frequency-but-confusing \"my saved panel position is gone after a window resize\" class of bug from regressing silently.\n\n**Smoke step**\n`npm test` passes with new file present; coverage report shows new module at \u003e90%; manually drag panel near viewport edge, resize window smaller, reopen, confirm panel re-clamps in.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:04:47Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:44Z","labels":["pr-44"],"dependencies":[{"issue_id":"stellata-9mm.79","depends_on_id":"stellata-9mm.190","type":"parent-child","created_at":"2026-05-15T12:53:52Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.77","title":"Test gap: readPipeDelimited has no unit coverage","description":"**Confidence: High**\n\n**Finding**\nThe PR extracts `function* readPipeDelimited(path: string): Iterable\u003cstring[]\u003e` to dedupe the existsSync→readFileSync→split-lines→split-on-`|` boilerplate previously duplicated in `parseGcvsMain` and `parseGcvsCrossref`. The helper has zero unit tests despite being the new shared seam between the two GCVS parsers. The pure parts of its behaviour (skip empty/whitespace lines, split on `|`, trim each cell, missing-file → empty iteration) are exactly the kind of input-shape contract that the rest of catalog-pure.test.ts pins down with vitest cases (cf. `normalizeGcvsName`, `parseGcvsNumber`).\n\n**Location**\nscripts/build-catalog.ts:226-233 (helper definition); scripts/catalog-pure.test.ts (no covering describe block)\n\n**Suggested approach**\nMove the trim/split/skip-empty logic into a pure helper in `scripts/catalog-pure.ts` (e.g. `splitPipeDelimited(text: string): string[][]` or a generator that takes a string), keep the file-IO shell (`existsSync` + `readFileSync`) in build-catalog.ts wrapping it, and add a vitest describe block covering: blank lines skipped, whitespace-only lines skipped, trailing whitespace inside cells trimmed, CRLF line endings, empty input, single-line input. This mirrors the existing pure/impure split where build-catalog hosts file IO and catalog-pure hosts the deterministic transforms with tests.\n\n**Why**\nThe pipe-reader is the new single point of failure for both GCVS parses. A regression in cell-trimming or line-skipping would silently corrupt the variability cross-match (period/amplitude=0 for affected stars). Pin the contract with tests before any future caller adds further format quirks.\n\n**Smoke step**\n`npm test` passes new describe block; `npm run build:catalog` still reports the same 11316 Hip + 13926 HD GCVS xrefs and 3677 variables matched as the PR's test plan.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:04:43Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:45Z","labels":["pr-49"],"dependencies":[{"issue_id":"stellata-9mm.77","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-15T12:54:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.76","title":"Reserved flag bits documented in comment but no test asserts they stay reserved","description":"**Confidence: Medium**\n\n**Finding**\ncatalog-pure.ts:323-324 lists `0x08, 0x20, 0x40, 0x80` as 'reserved bits available without a binary-version bump.' This is prose. If a future commit adds `FLAG_FOO = 0x08` and forgets to update that comment, no automated check catches the staleness. The new `distinct powers of two` test would still pass (0x08 is single-bit and distinct from the others).\n\n**Location**\nscripts/catalog-pure.ts:322-328\n\n**Suggested approach**\nAfter consolidating flags into a registry (see sibling bead 9mm.74), add a complementary assertion: `expect(usedFlagBits \u0026 RESERVED_FLAG_BITS).toBe(0)` where `RESERVED_FLAG_BITS = 0x08 | 0x20 | 0x40 | 0x80` is exported as a named const next to the `FLAGS` registry. Forces any reservation change to be a deliberate edit at one site rather than a silent drift between code and comment.\n\n**Why**\nThe 'available bits' comment is a load-bearing piece of project knowledge — the PR description and `docs/build-and-data.md` both reference it as the path to add functionality without a binary version bump. If it drifts, future Claude / Alex sessions will believe a bit is free when it's already used.\n\n**Smoke step**\n`npm test` after the change. Add a fake `FLAG_FOO = 0x08` and verify the reservation test fails.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:04:28Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:43Z","started_at":"2026-05-11T08:02:12Z","closed_at":"2026-05-11T08:46:43Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-42"],"dependencies":[{"issue_id":"stellata-9mm.76","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:04:28Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.75","title":"Headline render walks DOM by index instead of using captured span refs","description":"**Confidence: High**\n\n**Finding**\n`renderPanel()` updates the headline via DOM index gymnastics: `headlineEl.childNodes[0].nodeValue = ...`, `headlineEl.children[0].firstChild!.nodeValue = ...`, `headlineEl.children[1].firstChild!.nodeValue = ...`. This mixes `.children` (element-only, indexes `lowSpan` and `gpuSpan`) with `.childNodes` (all nodes, includes text nodes). The build path already created typed locals `lowSpan` and `gpuSpan` but they're discarded — only `headlineEl` is retained. A future change that adds another text node or reorders the headline shape silently scrambles the writes (the `!` suppresses the type warning).\n\n**Location**\nsrc/client/perf-hud.ts:149-156 (build creates `lowSpan`, `gpuSpan` as locals), :277-281 (renderPanel walks by index)\n\n**Suggested approach**\nLift `lowSpan` and `gpuSpan` to module-level refs (typed `HTMLSpanElement | null`) alongside `headlineEl`. Set them in build, null them in dispose, write to them by name in render. Better still, also keep a ref to the leading text node (currently `childNodes[0]`) — `document.createTextNode('')` returns a Text and TS will type it. Then `renderPanel` becomes three named writes against typed nodes with no `!` and no `.children` / `.childNodes` interleave.\n\n**Why**\nSame elegance issue as the row pool — the build path already chose to keep typed handles for the rows (`{ line, label, values }`), so the headline should follow suit. Removes the stale comment \"(3 children: textNode, lowSpan, textNode, gpuSpan)\" which under-counts (4 children).\n\n**Smoke step**\n`npm run typecheck \u0026\u0026 npm test`. Manual: `debug.panel()`, confirm \"FPS NN low NN gpu N.NNms\" still renders and updates.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:04:27Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:46Z","labels":["pr-41"],"dependencies":[{"issue_id":"stellata-9mm.75","depends_on_id":"stellata-9mm.190","type":"parent-child","created_at":"2026-05-15T12:53:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.74","title":"Flag-bit regression test hand-lists FLAG_* — adding bit needs test edit","description":"**Confidence: High**\n\n**Finding**\nThe new test `flag bits are distinct powers of two` (catalog-pure.test.ts:677–684) hardcodes `[FLAG_HAS_NAME, FLAG_IS_SOL, FLAG_HAS_BAYER, FLAG_BINARY_PRIMARY]`. Adding a 5th flag (FLAG_PLANET_HOST, FLAG_HAS_EXOPLANET, etc.) silently gains zero coverage — the new bit isn't in the array, so distinctness/single-bit-ness isn't checked. There's no central `FLAGS = { hasName: 0x01, ... } as const` that the test could iterate.\n\n**Location**\nscripts/catalog-pure.ts:325-328 (FLAG_* exports as separate consts); catalog-pure.test.ts:677-684 (hand-listed array)\n\n**Suggested approach**\nGroup the flags into a single `FLAGS = { ... } as const` object (or an exported `FLAG_REGISTRY` array) and re-export the individual `FLAG_HAS_NAME` etc. as named aliases for callsite readability. The test then walks `Object.values(FLAGS)` so any new flag is automatically pinned to be a single bit and distinct from the others. Bonus: the registry is the natural place to assert against the 'reserved bits' comment (`0x08, 0x20, 0x40, 0x80` should not appear as values).\n\n**Why**\nThe PR's thesis is 'adding a bit means adding a name here, not sprinkling another magic number' — same logic should apply to the test. Right now adding a flag means TWO edits (the new const + the test array) and skipping the second one passes CI silently.\n\n**Smoke step**\n`npm test` — add a deliberate `FLAG_FAKE = 0x10` colliding with FLAG_BINARY_PRIMARY and verify the iteration-based test catches it.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:04:16Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:42Z","started_at":"2026-05-11T08:02:12Z","closed_at":"2026-05-11T08:46:42Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-42"],"dependencies":[{"issue_id":"stellata-9mm.74","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:04:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.73","title":"Drop dead rowsContainer / histoBarsContainer module refs in perf-hud","description":"**Confidence: High**\n\n**Finding**\nPR #41 introduced module-level `rowsContainer` and `histoBarsContainer` nullable refs but they're only ever read in the null-guard at the top of `renderPanel` (`if (!panelEl || !headlineEl || !rowsContainer || !histoBarsContainer) return;`). They're never dereferenced thereafter — the per-tick writes go through `rowPool[i].line` and `histoBars[i]` directly. The container element refs add bookkeeping noise, an extra null-init/null-out path in dispose, and tempt readers into thinking they participate in render.\n\n**Location**\nsrc/client/perf-hud.ts:49 (`let rowsContainer`), :51 (`let histoBarsContainer`), :194 (assignment), :221 (assignment), :237-238 (nulled in dispose), :267 (only read site)\n\n**Suggested approach**\nDrop both let-bindings. Build still appends `rowsParent` / `histo` to `div` directly (already does); no other site needs the container handles. Remove from the dispose null-out and from the renderPanel null-guard. The `panelEl` and `headlineEl` guards are sufficient — if the panel exists, its child containers exist.\n\n**Why**\nDRY-ish — the guard duplicates the `panelEl` non-null check in two redundant ways. Removing the dead refs makes the section's lifecycle (build → render → dispose) clearer: only `panelEl`, `headlineEl`, `rowPool[]`, `histoBars[]` matter.\n\n**Smoke step**\n`npm run typecheck \u0026\u0026 npm test`. Manual: `debug.panel()` in dev console, expand Perf section, confirm histogram + rows still render and update; collapse + re-expand still works.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:04:09Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:42Z","started_at":"2026-05-11T07:56:31Z","closed_at":"2026-05-11T08:46:42Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-41"],"dependencies":[{"issue_id":"stellata-9mm.73","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:04:09Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.72","title":"No test pins inline byte-plan comment to RECORD_LAYOUT consts","description":"**Confidence: Medium**\n\n**Finding**\n`scripts/catalog-pure.ts` lines 219–253 contain a 35-line ASCII-art comment block describing every header + record byte (\"32 spectClass uint8\", \"36 ampUnits uint8 GCVS amplitude in 0.05 mag units\", etc.) right above the `HEADER_LAYOUT` / `RECORD_LAYOUT` consts. The comment is the human-readable spec, the consts are the machine-readable spec. There's no automated check that they agree. If `RECORD_LAYOUT.hip` moves to byte 36 but the comment still says `40..43 hip`, future readers get a wrong story — and the regression-prevention block in the test file can't catch it because it doesn't read the comment.\n\n**Location**\nscripts/catalog-pure.ts:219-253 (block comment); catalog-pure.test.ts:620 (block where pin would go)\n\n**Suggested approach**\nEither (a) drop the redundant comment and rely on `RECORD_LAYOUT` + per-field inline comments as the spec, or (b) add a regression-prevention test that does a regex/parse pass over the block comment, extracts `\u003clo\u003e..\u003chi\u003e \u003cname\u003e` triples, and asserts each matches `RECORD_LAYOUT[name]` and the implied size. Option (a) is cleaner — the inline `// uint8 (×0.05 mag)` annotations next to each layout entry already convey the same information.\n\n**Why**\nThe PR rightly centralised offsets but left a parallel English description of those offsets that can drift independently. Either delete the duplication or pin it.\n\n**Smoke step**\n`npm test` after the change. If pinning is chosen, deliberately edit one line of the comment to wrong byte range and verify the new test fails.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:04:00Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:41Z","started_at":"2026-05-11T08:02:11Z","closed_at":"2026-05-11T08:46:41Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-42"],"dependencies":[{"issue_id":"stellata-9mm.72","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:03:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.71","title":"Test sizes table parallels RECORD_LAYOUT — drift not caught","description":"**Confidence: High**\n\n**Finding**\nThe new `binary-format constants` regression block in `scripts/catalog-pure.test.ts` validates non-overlap by walking `RECORD_LAYOUT` entries against a hand-coded `sizes: Record\u003cstring, number\u003e` map (4 for floats, 1 for uint8, 2 for uint16, 4 for uint32). That map is a second copy of per-field type info that already lives in inline comments next to `RECORD_LAYOUT` (`// float32`, `// uint8`, etc.) — not in a machine-readable form. If a field is added to `RECORD_LAYOUT` and the test `sizes` map is forgotten, `expect(sizes[name]).toBeDefined()` fails — loud, fine. But if the wrong size is supplied (e.g. a new `uint16` mistakenly added as 4 in the test), the non-overlap check passes against an inconsistent type, which is a parallel-implementation footgun. Same shape duplicated across the header test (line 624–626) and record test (line 645–650).\n\n**Location**\nscripts/catalog-pure.test.ts:624-650\n\n**Suggested approach**\nPromote the layout shape from `{name: offset}` to `{name: {offset, kind}}` where `kind` is one of `'u8' | 'u16' | 'u32' | 'f32' | 'ascii4'` (or a numeric byte size). Keep `RECORD_LAYOUT.x` etc. accessor aliases (or a derived const) so writer/reader sites read unchanged. The test then walks the same shape to derive sizes — single source of truth for both layout and pin-test. Alternatively, expose a `recordFieldSize(name)` helper from catalog-pure that the test imports.\n\n**Why**\nThe PR's whole thesis is 'no inline byte literals across writer/reader/verify' — but the regression test reintroduces the duplication on the type-size axis. Promoting kind into the layout const is a one-edit change for both directions (add field, get test coverage automatically).\n\n**Smoke step**\n`npm test` — existing 65 tests stay green; intentionally introduce a 2-byte field with size 4 in the test map and verify the non-overlap test now flags the type mismatch.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:03:43Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:41Z","started_at":"2026-05-11T08:02:10Z","closed_at":"2026-05-11T08:46:41Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-42"],"dependencies":[{"issue_id":"stellata-9mm.71","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:03:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.70","title":"Extract DustParticleLayer subsystem (particleMesh + material + dispose + attach helpers)","description":"Sibling refactor to stellata-9mm.43 (StarPipeline extraction). Independent — different file, different concerns, no shared blockers.\n\n# Why\n\nThe dust-particle layer in stellata.ts owns its own resource cluster:\n- `particleMesh: THREE.Mesh | null`\n- `particleMaterial: THREE.ShaderMaterial | null`\n- `disposeParticles({ removeFromScene })` helper\n- `attachDustParticles(...)` constructor\n- `setParticleStrength(v)` runtime knob\n- Dust uniforms wired into the material at attach time\n\nThese were made individually disposable in PR #30 (stellata-9mm.35 disposeParticles helper, stellata-9mm.38 DustField.dispose), but the encapsulation itself is still inlined in Stellata. Same architectural smell as the disc/glow/coreMask cluster covered by 9mm.43 — a sub-system without a dispose() contract of its own.\n\nNote: the dust-particle layer is **shelved for v1.0** per CLAUDE.md (rendering disabled by default; uParticleStrength = 0). Decoupling it now keeps the re-enable path simple and prevents the shelved code from accumulating tangle as Stellata.ts evolves.\n\n# Scope\n\n- New `src/client/dust-particle-layer.ts` class owning `{ mesh, material, scene-attach, dispose }` plus the existing setParticleStrength / setParticleSize debug knobs.\n- Stellata holds it as one optional field; the existing inline particleMesh / particleMaterial / disposeParticles / attachDustParticles / setParticleStrength move into the class.\n- DustField (the per-star dust raymarch CPU helper) stays where it is — that's a separate subsystem already covered by stellata-9mm.38.\n\n# Smoke\n\n- typecheck + vitest pass.\n- Manual: temporarily set uParticleStrength \u003e 0 in the debug HUD (window.debug...), confirm particle layer attaches and renders. Toggle back to 0; confirm it disappears. Reload page; confirm shelved-default (strength=0) state still works.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T09:58:11Z","created_by":"Alex Marshall","updated_at":"2026-05-17T15:55:25Z","started_at":"2026-05-17T14:29:12Z","closed_at":"2026-05-17T15:55:25Z","close_reason":"merged in PR #83","dependencies":[{"issue_id":"stellata-9mm.70","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T17:42:38Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.70","depends_on_id":"stellata-9mm.194.1","type":"blocks","created_at":"2026-05-16T17:43:44Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.69","title":"Migrate strict-equivalent camera-busy sites in stellata.ts to isCameraBusy()","description":"Follow-up implementation bead for the audit landed in stellata-9mm.59. Depends on that bead's per-site annotations + decision matrix.\n\n# Scope\n\nFor each site flagged `// equivalent: isCameraBusy()` by the audit:\n- Replace the inline `(this.warpState || this.aimState || ...)` predicate with `this.isCameraBusy()`.\n- Drop the site's local annotation now that the call is self-documenting.\n\nSites flagged `// narrower: \u003creason\u003e` are left as-is. The audit's reason annotation stays on those sites permanently.\n\nRisk: per the audit, each surviving narrower site has a deliberate reason. The migration only touches strict-equivalent sites — but a strict-equivalent flag in the audit is itself a judgment call, so a per-site smoke pass is required (don't trust the audit blindly).\n\n# Smoke\n\nPer-site browser smoke for each migrated entry point. The user-visible interactions to exercise depend on which sites the audit identifies; expected coverage:\n- warp interrupt during aim (start a warp, then click another star — should not steal focus mid-warp)\n- aim interrupt during warp finish (click during the warp's slowdown phase)\n- observe enter/exit transition click handling (click during the navigate→observe lerp and reverse)\n- pickStar / pickCloud / aimAt / setCameraMode / focusStar / onPointerUp — whichever the audit migrates.\n\n# Acceptance\n\n- All sites flagged 'equivalent' in stellata-9mm.59 now call `isCameraBusy()`.\n- typecheck + vitest pass.\n- Browser smoke for every migrated site.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T09:45:44Z","created_by":"Alex Marshall","updated_at":"2026-05-10T09:45:44Z","dependencies":[{"issue_id":"stellata-9mm.69","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-15T12:54:05Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.69","depends_on_id":"stellata-9mm.59","type":"blocks","created_at":"2026-05-10T10:45:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.68","title":"Retire legacy URL-state schema decoders","description":"The URL-state encoder in src/client/url-state.ts ships a frozen FieldSpec table and decoder for every prior schema version, plus dispatch in decodeBlob. Each version costs maintenance overhead (~80–100 lines of code, plus regression tests) to keep legacy share URLs and bookmarks decoding correctly.\n\nEventually those legacy URLs will have aged out of circulation enough that the cost stops being worth it. At that point we should drop the older FieldSpec tables, their decoders, the version dispatch branches in decodeBlob, the SCHEMA_VERSION_V\u003cn\u003e constants for retired versions, and the corresponding backward-compat test blocks in url-state.test.ts.\n\nNot urgent — gated on circulation timeline. The auto-upgrade rewrite path in applyFromUrl means every load of an old URL silently rewrites the address bar to the current schema, so the legacy decoders are mostly serving cold paths (someone opening a bookmark for the first time in months/years).","design":"At pickup time, decide:\n\n1. Which version(s) to retire. Look at the gap between the oldest still-supported version and the current SCHEMA_VERSION. A reasonable heuristic: only keep N-1 (one version back) so the auto-upgrade rewrite still has a fallback for users who haven't loaded the site since the most recent schema bump.\n\n2. Whether to drop the corresponding backward-compat test block. Keeping the manually-built blob tests for retired versions has no cost beyond the test file size, but it also serves no purpose once the decoder is gone.\n\n3. Any URL-state-adjacent constants or tag-bit aliases that exist only for retired versions (e.g. FOCUS_HIP_TAG vs FOCUS_HIP_TAG_V2 — the V2 alias has continued existence even though v2's id space matches v3's).\n\nTouch points:\n- src/client/url-state.ts — FIELDS_V\u003cn\u003e, decodeV\u003cn\u003e, SCHEMA_VERSION_V\u003cn\u003e, dispatch in decodeBlob\n- src/client/url-state.test.ts — \"v\u003cn\u003e backward compatibility\" describe block\n- docs/architecture.md — the \"wire formats coexist\" paragraph","acceptance_criteria":"- Retired version's FIELDS table, decoder function, and SCHEMA_VERSION constant are removed from url-state.ts.\n- decodeBlob's dispatch no longer references the retired version; throws \"Unsupported view version\" for it as it would for any unknown.\n- Backward-compat test block for the retired version is removed from url-state.test.ts.\n- docs/architecture.md \"wire formats coexist\" paragraph updated to reflect remaining supported versions.\n- npm test + npm run typecheck still pass.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T09:25:58Z","created_by":"Alex Marshall","updated_at":"2026-05-10T09:26:04Z","dependencies":[{"issue_id":"stellata-9mm.68","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-15T12:54:10Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-c7u.5.2","title":"MC: HII emission overlay near hot embedded stars","description":"**Goal:** faint emission/scattering term near embedded stars hot enough to produce visible HII regions — a red/pink tinge fading with distance from the host. Threshold from **stellata-c7u.1**. Spec context in parent **stellata-c7u.5**.\n\n### Scope\n- Threshold: typically embedded stars ≥ early B (per c7u.1 research).\n- Per qualifying embedded star: a faint emission term in the cloud-rendering layer that adds (not modulates) a pink/red contribution near the star, fading with `r/r_HII`.\n- Cloud-type variation: quiescent dark cloud (no/few hot embeddings) renders unchanged; active SF region (multiple hot embeddings) acquires the emission term.\n- Reflection nebulosity remains explicitly **deferred** to **stellata-9hp** which covers RNe as discrete blob objects.\n\n### Files\n- `src/client/shaders/cloud.frag.glsl` (or successor shader landed by `stellata-c7u.6`) — emission contribution.\n- Possibly `src/client/molecular-clouds.ts` — uniform plumbing for the per-cavity emission descriptor.\n\n### Acceptance / visual gate\n- Orion A around the Trapezium reads with subtle red/pink emission overlay.\n- Quiescent clouds (Taurus, Chamaeleon) are unaffected.\n- Emission never competes with the physical extinction signal — it stays clearly secondary, matching the c7u-epic constraint that physical signals dominate.\n\n### Depends on\n**stellata-c7u.5.1** (cavity descriptors define where HII regions live).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:10:16Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:10:16Z","labels":["ism","molecular-clouds","physics"],"dependencies":[{"issue_id":"stellata-c7u.5.2","depends_on_id":"stellata-c7u.5","type":"parent-child","created_at":"2026-05-10T09:10:16Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.5.2","depends_on_id":"stellata-c7u.5.1","type":"blocks","created_at":"2026-05-10T09:10:52Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-c7u.5.1","title":"MC: embedded young-star cavities + density modulation","description":"**Goal:** model cavity-carving by embedded O/B + YSO stars inside Zucker clouds. Density modulation only — no emission yet. Spec context in parent **stellata-c7u.5**.\n\n### Cross-match (build time)\n- Identify O/B stars and known YSOs whose 3D positions fall inside any Zucker cloud bbox.\n- Use ATHYG spectral types for O/B identification.\n- Per cloud, list embedded hot stars: position, spectral type, luminosity proxy.\n- Output: per-cloud cavity descriptor in the dust manifest or a new cloud JSON field.\n\n### Density modulation\n- For each embedded hot star, reduce local density inside a radius scaled by stellar luminosity (Strömgren-sphere-style; constants from **stellata-c7u.1**).\n- Soft falloff at the cavity edge — `density × (1 − cavity_strength × falloff(r/r_cav))`.\n- Bake into the same chunked 3D-texture pipeline that **stellata-c7u.2** populates with Zucker densities (single source of truth, no double-counting).\n\n### Acceptance / visual gate\n- Orion A (multiple O stars) renders visibly differently from quiescent dark cores like Taurus — Orion's density feels \"hollowed\".\n- Per-star extinction `star.vert.glsl` automatically picks up the modulation from the existing voxel-volume raymarch (no shader change required).\n- Cavity edges read as soft, not stepped.\n\n### Files\n- `scripts/build-clouds.py` — emit cavity descriptors per cloud.\n- `scripts/build-dust.py` — apply cavity modulation when overlaying Zucker into the voxel volume.\n\n### Out of scope\n- HII emission overlay — sibling **stellata-c7u.5.2**.\n- Reflection nebulosity — deferred to **stellata-9hp** (already covers RNe as discrete blob objects).\n\n### Depends on\n**stellata-c7u.2** (data pipeline) + **stellata-c7u.4** (substructure landscape into which cavities are carved).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:10:15Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:10:15Z","labels":["ism","molecular-clouds","physics"],"dependencies":[{"issue_id":"stellata-c7u.5.1","depends_on_id":"stellata-c7u.5","type":"parent-child","created_at":"2026-05-10T09:10:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-nxb.2","title":"Search index: GJ / Gliese + CCDM letters + Greek-letter abbreviations","description":"**Goal:** broader coverage of alternate-designation classes after V-numbers land in **stellata-nxb.1**. Spec context in parent **stellata-nxb**.\n\n### Scope (in priority order, each independently shippable)\n- **GJ / Gliese** — common in nearby-stars literature. Source: AT-HYG `gl` field is already in the binary; just thread it into `search-index.json` if not already.\n- **Multi-component CCDM letters** (Alpha Centauri C, A, B). Coordinate with the binary/multiple-star work in **stellata-dch** — the override-list output there may inform CCDM-letter resolution.\n- **Greek-letter abbreviations** (Alf Cen C alongside Alpha Centauri C) if not already handled by nxb.1's audit.\n\n### Out of scope (per parent)\nLFT, LHS, LPM, LTT, NLTT — esoteric proper-motion catalogues; low search value.\n\n### Files\n- `scripts/build-catalog.ts` / `scripts/catalog-pure.ts` — extend search-index entry generation.\n- Tests in `scripts/catalog-pure.test.ts`.\n\n### Acceptance\n- \"GJ 551\" and \"Gliese 551\" both resolve to Proxima Centauri.\n- \"Alpha Centauri C\" and \"Alf Cen C\" both resolve.\n- Multi-component CCDM letters surface the right star.\n- No regression on typeahead behaviour.\n\n### Depends on\n**stellata-nxb.1** (audit + V-numbers establish the pattern).\n### Related\n**stellata-dch.3** (CCDM-letter overrides may share pipeline).","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:09:21Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:09:21Z","dependencies":[{"issue_id":"stellata-nxb.2","depends_on_id":"stellata-nxb","type":"parent-child","created_at":"2026-05-10T09:09:20Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-nxb.2","depends_on_id":"stellata-nxb.1","type":"blocks","created_at":"2026-05-10T09:10:51Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-fe4.2","title":"Variable pulsation: implement chosen fix path (a/b/c)","description":"**Goal:** implement the option (a / b / c) chosen by sibling **stellata-fe4.1**. Spec context in parent **stellata-fe4**.\n\n### Implementation depends on chosen option\n- **(a)** Cap-multiplier table — touch `scripts/catalog-pure.ts` (per-type multiplier) + `src/client/shaders/star.vert.glsl` (apply cap to `radiusFactor`). One-line per type.\n- **(b)** R+T amplitude split — extend the variable-star uniform pipeline with a per-frame T-shift uniform; touch `star.vert.glsl` + `star.frag.glsl` (colour/temperature output). Per-type amplitudes live in `scripts/catalog-pure.ts` and SCIENCE.md.\n- **(c)** No code change; close this issue with rationale, leave a brief note in SCIENCE.md / CLAUDE.md.\n\n### Doc updates\n- SCIENCE.md — variable-pulsation section: cite the interferometric source (Lacour 2009 / Woodruff 2009 / Wittkowski 2016) and document the chosen amplitude split.\n- CLAUDE.md — update or remove the \"Things deliberately kept out\" bullet about variable-star T-swing.\n\n### Acceptance\n- For (a)/(b): Mira full-amplitude swing on screen reads as believable against the published interferometric gap.\n- For (c): bullet in CLAUDE.md updated with reasoning + interferometric citation.\n- No regression on Cepheid / RR Lyrae / eclipsing pulsation behaviour (unless option (b) intentionally re-tunes them).\n\n### Depends on\n**stellata-fe4.1** (research conclusion drives the implementation).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:08:09Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:08:09Z","dependencies":[{"issue_id":"stellata-fe4.2","depends_on_id":"stellata-fe4","type":"parent-child","created_at":"2026-05-10T09:08:09Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-fe4.2","depends_on_id":"stellata-fe4.1","type":"blocks","created_at":"2026-05-10T09:10:49Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-fe4.1","title":"Variable pulsation: quantify Mira R-swing vs interferometry (research)","description":"**Goal:** decide whether the constant-T `R ∝ √L` pulsation model is physically defensible at high amplitude. Spec context in parent **stellata-fe4**.\n\n### Method\n- For Mira (the type-defining example) and a few other high-amplitude AAVSO archetypes — chi Cyg, R Hya, R Leo — compute the on-screen angular-diameter ratio between minimum and maximum light under the current `R ∝ √L` assumption used in `src/client/shaders/star.{vert,frag}.glsl` (`R_eff = R · radiusFactor`).\n- Compare to **interferometric measurements** of the actual physical radius swing:\n  - Lacour 2009 (CHARA Mira disc).\n  - Woodruff 2009 (VLTI Mira).\n  - Wittkowski 2016 (VLTI Mira-class long-period variables).\n  - Other CHARA/VLTI/IOTA Mira papers from the ~2008–2020 window.\n- Quantify the gap: ratio of modelled R-swing to measured R-swing per archetype.\n\n### Decision among options\n- **(a)** Cap R-swing per variable type (simple multiplier table). Brightness amplitude on V-band then under-served. Cheapest.\n- **(b)** Split amplitude: per-type fixed fraction of luminosity change goes to R, rest encoded as colour/temperature shift in the disc + glow shader (the spectral colour uniform path is already there; extending to per-frame T-shift is contained).\n- **(c)** Keep current model, document limitation in SCIENCE.md as a known stylistic choice.\n\n### Decide\n- Whether option choice differs by GCVS variable type (Mira, Cepheid, RR Lyrae, eclipsing). Cost of (b) goes up if type-specific.\n- Whether the fix is worth the complexity given that variable pulsation is an aesthetic-correctness concern, not a critical-path bug.\n\n### Deliverable\nQuantified discrepancy + chosen option (a/b/c) recorded in this issue's notes. Drives sibling **stellata-fe4.2** (implementation) — if (c), close fe4.2 with the rationale and update the CLAUDE.md \"kept out\" bullet to cite this research.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:08:08Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:08:08Z","dependencies":[{"issue_id":"stellata-fe4.1","depends_on_id":"stellata-fe4","type":"parent-child","created_at":"2026-05-10T09:08:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.9.2","title":"Warp arrival projection drift / pin-snap mitigation","description":"**Goal:** address Effect B from parent **stellata-a7d.2.9** — float32 projection drift then pin snap visible at warp arrival. Only ship this if the drift is still perceptible after the flyby fix in sibling **a7d.2.9.1**.\n\n### Effect to address\nDuring warp the floating origin sits on the source star, so the destination's worldPos magnitude is ~|AB| (e.g. ~265 pc for Sol→Rigel). GPU float32 precision at that magnitude is ~|AB|·1e-7 ≈ 2.6×10⁻⁵ pc — comparable to endOffset (~3.4×10⁻⁵ pc for Rigel). The projected centre can drift a noticeable fraction of a screen in final approach. The pin (`uPinFocusToCenter`) does NOT engage during warp, so the drift is visible. At `finishWarp`, `recenterOrigin` moves origin to the dest → projection precision becomes essentially exact → pin engages → star snaps to mathematical NDC position.\n\n### Mitigation\n- Generalise `uPinFocusToCenter` to non-origin pin targets.\n- Pin the destination star to its `lookAt`-projected position during the final phase of warp (requires passing target NDC or world position as a uniform).\n\n### Files\n- `src/client/stellata.ts` — `updateWarp` final-phase pin engagement.\n- `src/client/shaders/star.vert.glsl` — generalised pin uniform.\n\n### Acceptance\n- Warp arrival has B's disc grow at perceptibly uniform angular rate; no pop at parking.\n- No regression on short warps where the prior arrival already felt fine.\n\n### Depends on\n**stellata-a7d.2.9.1** (only do this if the flyby fix doesn't already eliminate the drift visually).","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:07:02Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:07:02Z","dependencies":[{"issue_id":"stellata-a7d.2.9.2","depends_on_id":"stellata-a7d.2.9","type":"parent-child","created_at":"2026-05-10T09:07:02Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-a7d.2.9.2","depends_on_id":"stellata-a7d.2.9.1","type":"blocks","created_at":"2026-05-10T09:10:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.9.1","title":"Warp flyby curve: arc fillet (or clothoid) at source-side pop","description":"**Goal:** replace warp's in-line departure with a curve that takes the camera *off* the A↔B line so the user perceives a flyby of the source star A, not a teleport through it. Spec context in parent **stellata-a7d.2.9**.\n\n### Approach\n- Smooth join between two line segments at A:\n  - Start tangent: camera's current direction (or current radial-from-A — depends on launch pose).\n  - End tangent: +forward along A→B.\n- Implement as **arc fillet** for C¹ continuity at A. If centripetal jerk at the waypoint is perceptible, upgrade to **clothoid** (Euler spiral, linearly varying curvature) for C² continuity.\n- Arc-length parametrise the warp position-time profile so the angular size of A grows smoothly as the camera curves past — no perceived teleport.\n\n### Reference: failed prior attempt preserved on archive branch\n`archive/a7d.2.9-inline-angular-ease` (pushed to origin). Single commit on top of a7d.2 worktree HEAD at archival.\n```\ngit fetch origin\ngit log archive/a7d.2.9-inline-angular-ease -1\ngit diff archive/a7d.2.9-inline-angular-ease~1 archive/a7d.2.9-inline-angular-ease -- src/client/stellata.ts\n```\nDon't reuse the in-line angular-ease shape — it treats A as a point on the warp axis the camera passes through, which is the wrong mental model.\n\n### Files\n- `src/client/stellata.ts` — `startWarp`, `updateWarp`, `finishWarp`.\n\n### Acceptance\n- Warp start reads as a continuous flyby of the source star — no perceived teleport across A.\n- A's apparent size grows smoothly as the camera curves past.\n- C¹ continuity at the waypoint(s); C² preferred if centripetal jerk is otherwise perceptible.\n- No regression on short warps (Sol → α Cen) where the existing profile already feels fine.\n\n### Out of scope\n- Effect B: float32 projection drift / pin-snap on arrival — sibling **stellata-a7d.2.9.2**. Re-evaluate after this lands; the flyby reframe may already eliminate it.\n- Destination-side ease — keep the existing 1/d_B-linear angular-ease arrival.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:07:01Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:07:01Z","dependencies":[{"issue_id":"stellata-a7d.2.9.1","depends_on_id":"stellata-a7d.2.9","type":"parent-child","created_at":"2026-05-10T09:07:01Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-8r1.3","title":"Wander: runtime state in stellata.ts (G key + updateWander + recenter)","description":"**Goal:** runtime wander mode integrated into the existing stellata.ts state machine. Spec in parent **stellata-8r1**.\n\n### Scope\n- Add `'wander'` mode alongside warp. Same disable pattern: `controls.enabled = false`, `body.classList.toggle('wandering', true)`, `pointerup` short-circuited, URL writer skips camera-hash updates while active.\n- Entry: keyboard shortcut `G` (verify free; W is warp; Esc/Space skip warp). Exit: any `pointerdown`, `Esc`, or `G` again.\n- Focus interaction: activating wander **unfocuses** any current focal star (`setFocus(null)`), recentring origin to Sol — clean absolute frame for the duration.\n- Particle state: `_wanderActive`, `_wanderVelocity`, `_wanderDistanceSinceRecenter`.\n- Per-frame `updateWander(dt)` per parent's algorithm (sample, accel, integrate, clamp, step, recenter every ~1 pc).\n- Initial kick: field-scaled with floor — `v0 = max(KICK_FLOOR, KICK_TIME × |g| × GRAVITY_SCALE)`. Defensive guard for degenerate `target == position` direction.\n\n### Floating-origin contract — CRITICAL\nAfter unfocus on entry `worldOffset = (0,0,0)`. Once `recenterOrigin` fires, **all gradient samples must be at `camera.position + worldOffset`**, never `camera.position` alone. Same for lookahead and any read-back. Call this out in code comments around every sampling site.\n\n### Tuning levers (define as named constants at top of wander section)\n- `WANDER_SOFTENING_PC = 2.0` — **build-baked**, must match `build-gravity.ts`. Comment.\n- `WANDER_MASS_EXPONENT = 0.5` — **build-baked**, must match.\n- `WANDER_GRAVITY_SCALE = 0.1` — runtime\n- `WANDER_VELOCITY_DAMPING = 0.99` per-frame at 60 fps; applied as `pow(damping, dt × 60)` for frame-rate independence\n- `WANDER_MAX_SPEED_PC_PER_S = 20.0`\n- `WANDER_SIM_TIME_SCALE = 5.0`\n- `WANDER_LOOKAHEAD_PC = 2.0`\n- `WANDER_KICK_TIME_S = 2.0`\n- `WANDER_KICK_FLOOR_PC_PER_S = 1.0`\n\n### Files\n- `src/client/stellata.ts` — wander state + per-frame update + key handler + exit handling.\n- `src/client/main.ts` — pass loader to `Stellata`.\n- `src/client/styles.css` — optional `body.wandering` style (cursor change / subtle vignette).\n- `CLAUDE.md` (post-landing): extend floating-origin section with the absolute-frame sample contract; add a \"Wander mode\" section.\n\n### Acceptance / smoke\n- G enters wander; pointerdown / Esc / G exits.\n- URL writer does not serialise intermediate poses while wandering.\n- Camera drifts plausibly through dense regions; voids still produce motion via the kick floor.\n- No visible jitter at recentre boundaries.\n\n### Depends on\n**stellata-8r1.2** (loader).","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:05:13Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:05:20Z","labels":["phase-10"],"dependencies":[{"issue_id":"stellata-8r1.3","depends_on_id":"stellata-8r1","type":"parent-child","created_at":"2026-05-10T09:05:13Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8r1.3","depends_on_id":"stellata-8r1.2","type":"blocks","created_at":"2026-05-10T09:10:47Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-8r1.2","title":"Wander: src/client/gravity-loader.ts (parse + trilinear sampleGradient)","description":"**Goal:** runtime loader for `public/gravity.bin` per the spec in parent **stellata-8r1**.\n\n### Scope\n- New `src/client/gravity-loader.ts`, mirroring `catalog-loader.ts` shape.\n- Fetch `gravity.bin`, validate magic (\"GRV1\") + version. Hard fail on mismatch.\n- Expose `gridN`, `halfExtent`, `softening`, `massExponent`, `gradients: Float32Array` (length `N³ × 3`).\n- Export `sampleGradient(absolutePos: THREE.Vector3): THREE.Vector3` — trilinear interpolation.\n  - Return zero vector for positions outside the bounding sphere.\n  - Index clamping: `floor + 1` must not read past `N − 1`. Test fractional voxels at the cube edge.\n- Wire parallel-load in `main.ts` alongside `catalog.bin` and `search-index.json`.\n\n### Tests (vitest)\n- Synthetic GRV1 file fixture: validate header parsing.\n- Sample at grid-aligned positions returns the stored gradient.\n- Sample at a position halfway between two voxels returns the average.\n- Sample at the corner voxel does not throw or read past the array end.\n- Sample at a position outside the bounding sphere returns zero.\n\n### Depends on\n**stellata-8r1.1** (binary format + file must exist).\n\n### Out of scope\nRuntime wander state machine; that's the next sibling.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:05:12Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:05:20Z","labels":["phase-10"],"dependencies":[{"issue_id":"stellata-8r1.2","depends_on_id":"stellata-8r1","type":"parent-child","created_at":"2026-05-10T09:05:11Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8r1.2","depends_on_id":"stellata-8r1.1","type":"blocks","created_at":"2026-05-10T09:10:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-8r1.1","title":"Wander: scripts/build-gravity.ts precompute (gravity.bin + isUpToDate)","description":"**Goal:** offline precompute that emits `public/gravity.bin` per the spec in parent **stellata-8r1**.\n\n### Scope\n- New `scripts/build-gravity.ts`. Reads star positions from `catalog.bin` (inline a small reader; no shared module).\n- Bounding volume: cube of side `2R` centred at origin where `R = max distance from Sol`. Voxel side ≈ `2R / 128` (128³ grid → ~25 MB).\n- Mass proxy: `luminosity = 10^((4.74 − absmag)/2.5)`; `mass = max(0, luminosity^MASS_EXPONENT)`. Default `MASS_EXPONENT = 0.5`.\n- Gradient computation per voxel per star with `SOFTENING²` regularisation; vectors NOT normalised.\n- Run in `worker_threads`, chunked one Z-slice per task. Target \u003c5 min on a modern laptop. Optional pre-cull beyond ~500 pc for speed; measure before adding.\n\n### Output binary format (32-byte header + N³ × 12 bytes)\n- `0–3`: ASCII \"GRV1\"\n- `4–7`: uint32 grid dimension N (128)\n- `8–11`: float32 grid half-extent in parsecs (= R)\n- `12–15`: float32 softening\n- `16–19`: float32 mass exponent\n- `20–31`: reserved (zeros)\n- Body: float32 x, y, z gradient per voxel, index `x + y*N + z*N*N` (x varies fastest).\n\n### Build wiring\n- Add `build:gravity` to `package.json`, chained after `build:catalog` in both `dev` and `build`.\n- Use the same `isUpToDate` pattern as `build-catalog.ts`, gated on `catalog.bin` mtime + script mtime + build-baked constants.\n- Add `/public/gravity.bin` to `.gitignore`.\n\n### Build-baked constants (must match runtime sampler)\n- `WANDER_SOFTENING_PC = 2.0`\n- `WANDER_MASS_EXPONENT = 0.5`\n- `GRID_DIM = 128`\n\n### Acceptance\n- `npm run build:gravity` produces `public/gravity.bin`.\n- Re-run is idempotent (no-op when up to date).\n- Header round-trips: magic = \"GRV1\", dimension = 128, half-extent ≈ catalog max-distance.\n- Spot-check: a voxel within ~5 pc of Sol has nonzero gradient pointing roughly toward the local stellar density centre.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T08:04:35Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:05:20Z","labels":["phase-10"],"dependencies":[{"issue_id":"stellata-8r1.1","depends_on_id":"stellata-8r1","type":"parent-child","created_at":"2026-05-10T09:04:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-3re.18","title":"Per-planet empirical phase curves (Mallama 2018) replacing Lambertian default","description":"**Goal:** swap the v1 Lambertian phase function `φ(α) = (sin α + (π−α)·cos α)/π` for per-planet empirical phase curves. Mallama et al. 2018 publishes V-band phase coefficients for each major planet, including Saturn's ring contribution and Venus's phase asymmetry.\n\n# Why\n\nLambertian over-darkens at large phase angle for atmospheric planets — Venus is brighter at large α than Lambert predicts because of forward scattering through its atmosphere. Saturn at all phases benefits from including the ring system contribution to total brightness. For Mercury and Mars, Lambert is an adequate approximation, but the empirical curves are still more correct.\n\n# Source\n\nMallama, Krobusek, Pavlov 2018 — \"Comprehensive wide-band magnitudes and albedos for the planets, with applications to exo-planets and Planet Nine,\" *Icarus* 282 (2017) 19–33. DOI 10.1016/j.icarus.2016.09.023.\n\nPer-planet empirical fit forms vary; coefficients are tabulated for each planet. Implementation likely adds `phaseFunctionCoefficients` to `Planet` and a per-planet `phaseFunction(α)` method, defaulting to Lambert for hosts that don't supply coefficients (which means: every exoplanet under stellata-bk5).\n\n# Files likely touched\n\n- `src/client/planet-system.ts` — add coefficients to relevant SOL_PLANETS entries; default Lambert when absent.\n- `src/client/shaders/planet.vert.glsl` (or the perceptual-disc chunk) — branch on a per-instance flag, or use a series form that collapses to Lambert when coefficients are zero.\n\n# Priority\n\nP3. Visual refinement, not a correctness gate. Lands after the v1 Lambert phase function (stellata-3re.17) ships.","notes":"PR: https://github.com/alexmensch/stellata/pull/56 (open, awaiting manual smoke + merge). Bead stays in_progress until merge — close with bd close after the PR lands.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T19:25:44Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:18:39Z","started_at":"2026-05-10T09:18:54Z","closed_at":"2026-05-10T18:18:39Z","close_reason":"PR #56 merged 2026-05-10 (commit bc39f0c on main)","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.18","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-08T20:25:43Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.18","depends_on_id":"stellata-3re.17","type":"related","created_at":"2026-05-08T20:25:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-0pz","title":"Document and rescue silently-dropped famous stars (KNOWN_MISSING_HIPS pattern)","description":"**Goal:** rescue famous stars silently dropped by the AT-HYG → catalog-binary build, and document the rescue pattern for future cases.\n\n# The trap\n\n`scripts/build-catalog.ts` drops any AT-HYG row with empty `x0/y0/z0/plx` columns *without warning*. AT-HYG v3.3 has several famous stars in this state — the row exists but the 3D position fields are blank, so the star never reaches the renderer or the search index.\n\n`KNOWN_MISSING_HIPS` in `scripts/build-catalog.ts` is **not** a rescue list. It's only consulted when a Stellarium constellation-line endpoint can't be resolved — to suppress the warning. Arbitrary missing famous stars don't surface there; the build still succeeds cleanly.\n\n# Known cases\n\n- HIP 5165  — alpha Phe / Ankaa (in `KNOWN_MISSING_HIPS`)\n- HIP 89341 — mu Sgr / Polis (in `KNOWN_MISSING_HIPS`)\n- HIP 107259 — mu Cep / Herschel's Garnet Star (NOT in the list — currently silently dropped, action item below)\n\n# Investigation pattern (when a famous star is reported missing)\n\n1. Grep `data/athyg_33_classic_ids.csv` for the HIP. Empty `x/y/z` cells → AT-HYG lacks parallax for that star.\n2. Don't conflate \"missing from render\" with \"missing from search\" — the same CSV feeds both.\n3. Rescue options:\n   - Patch a synthetic row at build time using a hand-measured parallax (Gaia DR3, Hipparcos re-reduction).\n   - Switch data sources for that subset of stars.\n4. Document the rescue source + retrieval date in SCIENCE.md per the `frozen-external-data` convention.\n\n# Tasks\n\n- Rescue HIP 107259 (mu Cep / Herschel's Garnet Star).\n- Audit AT-HYG for other named stars with empty x/y/z columns; either add to `KNOWN_MISSING_HIPS` (if it's an expected-missing constellation-line endpoint) or rescue (if it's a user-facing famous star).\n- Optional: surface a build-time warning when an AT-HYG row with a non-empty `bayer`/`flamsteed`/`proper` field is dropped due to empty x/y/z, so future cases don't go silent.\n\n# Out of scope\n\n- Re-pulling AT-HYG itself. Per `frozen-external-data` the source CSV is committed and refresh is a manual one-shot, not a build dependency.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-08T18:34:49Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:34:49Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.17","title":"Phase-angle planet illumination (φ(α)) — proper reflected-light geometry","description":"**Goal:** replace the v1 full-illumination assumption (`φ = 1`) in planet apparent-magnitude with a proper phase function. A planet's brightness should depend on its phase angle relative to the viewer.\n\n# Why\n\nCurrently, Jupiter on the far side of the Sun from camera = full phase = bright; Jupiter between camera and Sun = ~zero illuminated face = ~zero brightness. The v1 formula treats both the same. Visually wrong, especially obvious from inside the solar system or from a nearby star where geometry matters.\n\n# Implementation sketch\n\nPhase angle `α = ∠(viewer–planet–host)`.\n\nFor a Lambertian sphere:\n```\nφ(α) = (sin α + (π − α) · cos α) / π\n```\n\nFor more realistic planetary phase curves, use the empirical `H, G` magnitude system or per-planet phase coefficients (Mallama et al. 2018 give published phase curves for all major planets — including Saturn's ring contribution, Venus's phase asymmetry).\n\nSubstitute into the apparent-magnitude formula from the parent v1 bead:\n```\nm_planet = m_host − 2.5 · log10( p · φ(α) · (R / d_host_planet)² · (R / d_viewer_planet)² )\n```\n\n# Affects\n\n- Apparent-magnitude visibility cutoff for planets (parent bead, in spirit) — phase function tightens / loosens visibility geometrically.\n- chart-mode planets (stellata-m40.3) — chart-mode rendering should also respect phase brightness so symbology / disc-size / opacity matches what's visible.\n\n# Files likely touched\n\n- `src/client/planet-system.ts` — add `phaseFunction(α)` per planet (default Lambert; override per-planet for empirical).\n- Wherever apparent magnitude is computed (the new planet pipeline) — pipe in viewer position to compute `α`.\n\n# Priority\n\nP3. Visual refinement, not a correctness gate. Ship after the v1 apparent-magnitude work lands.","design":"# Scope split\n\nLambertian φ(α) = (sin α + (π−α)·cos α)/π lands in the planet pipeline\nas part of the 3re.15 + 3re.16 + 3re.17 PR. This bead closes when\nLambert ships.\n\nPer-planet empirical phase curves (Mallama 2018, including Saturn's\nring contribution + Venus's phase asymmetry + atmosphere-aware\nEarth/Titan) are filed as a separate P3 follow-up — see the related-\nlinked bead.\n\n# Phase angle derivation in the planet vert shader\n\nα = ∠(viewer→planet, host→planet). With iHostLocalPos and iLocalRel\ngiving us host- and planet-positions in the floating-origin local\nframe, and viewer = camera position (also local frame):\n\n  vec3 vph = cameraPos − planetWorldPos;       // viewer → planet\n  vec3 hph = iHostLocalPos − planetWorldPos;   // host → planet\n  // (planetWorldPos = iHostLocalPos + iLocalRel)\n  float cosA = dot(normalize(vph), normalize(hph));\n  float alpha = acos(clamp(cosA, -1.0, 1.0));\n  float phi = (sin(alpha) + (PI - alpha) * cos(alpha)) / PI;\n\nMultiply brightness by φ → 2.5·log10(φ) added to m_planet inside the\nplanet-side appMag calculation.\n\n# Cross-cutting architecture\n\nSee parent stellata-3re --design for the planet pipeline architecture\nand shared perceptual-disc abstraction.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-08T18:08:33Z","created_by":"Alex Marshall","updated_at":"2026-05-09T09:03:59Z","started_at":"2026-05-08T19:37:21Z","closed_at":"2026-05-09T09:03:59Z","close_reason":"Landed in PR #45 (v1.4.0): planet apparent-magnitude pipeline + Lambertian phase + global PlanetBodyField architecture.","labels":["phase-12a","solar-system"],"dependencies":[{"issue_id":"stellata-3re.17","depends_on_id":"stellata-3re","type":"parent-child","created_at":"2026-05-08T19:08:33Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-0it","title":"Evaluate WebGPU migration (renderer + shader port)","description":"## Goal\n\nDecide if/when Stellata should migrate from `THREE.WebGLRenderer` (WebGL2 + GLSL ES 3.00) to `THREE.WebGPURenderer` (WebGPU + WGSL/TSL). Capture context here so the decision is informed when a triggering need surfaces — not a P2 active scope.\n\n## What WebGPU unlocks\n\n- **Compute shaders** with shared memory, atomics, storage buffers. Many Stellata problems become trivial: visible-star count = one compute dispatch + atomic counter (~10 lines), GPU-driven culling for big catalogs, particle physics, on-GPU dust resampling.\n- **Storage buffers (SSBO equivalent)** — could simplify the dust raymarch attribute juggling and search-index uploads.\n- **Indirect draws + GPU-driven culling** — pull the per-frame `hasStarWithin` / `pickStar` / `countVisibleStars` family entirely off the CPU.\n- **Better state model** — pipeline objects + bind groups instead of WebGL's global state machine. Far fewer footguns.\n- **Lower CPU-side draw overhead.** Stellata is GPU-bound so the gain is modest, but real for per-frame overlay + label code.\n\n## Browser support (2026-05)\n\n- Chrome / Edge: stable since v113 (May 2023). Fully shipped.\n- Safari (macOS + iOS): stable since 17.4 (March 2024). iOS 17.4+ is now the majority.\n- Firefox: shipped to release in v141 (mid-2025).\n\nEffectively generally available on evergreen browsers; long tail (older mobile, locked-down work browsers) is still WebGL2-only. Not a blocker for a hobby project audience.\n\n## Three.js story\n\n`WebGPURenderer` is no longer \"examples-only\" — first-class renderer. Shaders are authored in **TSL (Three Shading Language)**, a JS DSL that compiles to WGSL or GLSL — write once, run on either renderer. Rough edges to verify before committing:\n\n- **Logarithmic depth buffer** (Stellata uses this; see `stellata-a7d.1`) — historically a sore spot on WebGPURenderer. Status as of early 2026 is \"supported but verify\" — needs a smoke pass before migration.\n- **Custom GLSL3 shaders** — keep working unchanged on WebGLRenderer; on WebGPURenderer must port to TSL or WGSL. **This is the bulk of the migration work.** Stellata surface area: `star.vert/frag.glsl`, `dust-particle.vert/frag.glsl`, `cloud.vert/frag.glsl`, `milkyway.vert/frag.glsl`, plus any non-trivial chunks in `galactic-overlay.ts`.\n- **InstancedBufferAttribute, 3D textures, MRT, Line2** — all supported.\n\n## Migration cost estimate (eyeballed)\n\n- ~1 week: port the four shader pairs to TSL + verify each pass renders identically.\n- ~3 days: cross-browser smoke (Chrome/Safari/Firefox × Mac/iOS/Win — driver differences matter on WebGPU).\n- ~2 days: perf comparison and tuning (log depth, dust raymarch step count, etc.).\n- ~1 day: build/Vite config + `RawShaderMaterial` consumer migration.\n\nCall it ~2 weeks elapsed for someone who's done a Three.js WebGPU port before; more for a first-time port.\n\n## When this becomes worth doing\n\n- **A critical mass of compute-shader-shaped problems** stacks up. Visible-count alone is not enough; need 3+ to justify the migration cost.\n- **stellata-cns lands and 313k → 1M+ stars stresses the WebGL2 pipeline.** That's the natural moment to bundle a WebGPU port with the perf rework — see stellata-cns.1 perf research; if m12 doesn't run on integrated GPUs / mobile, WebGPU + GPU-driven culling is one of the alternatives to \"fall back to m11.\"\n- **GPU-bound features land that compute would simplify**: nebulae raymarch on compute, particle physics, exoplanet orbital integration GPU-side, etc.\n\n## When it's NOT worth doing\n\n- Stellata is feature-complete-ish and stable — migration for migration's sake is a 2-week tax with no user-visible win and a few imperceptible regressions.\n- The visible-count alone (stellata-9mm.67) is the smallest possible reason to migrate.\n\n## Triggering signals (when you see one of these, revisit this bead)\n\n1. m12 catalog upgrade (stellata-cns) hits a perf wall on integrated GPUs / mobile.\n2. Any GPU-bound feature surfaces where compute + atomics is the obvious tool (and CPU-mirroring or texture-RT-readback is the WebGL2 workaround).\n3. Three.js drops `WebGLRenderer` (not anytime soon, but track).\n4. Stellata picks up multi-platform deployment goals where WebGPU's lower CPU overhead matters.\n\n## How to revisit\n\n1. Re-validate browser-support and Three.js WebGPU feature-completeness numbers (this bead's data ages fast).\n2. Spike: port `star.vert/frag.glsl` to TSL on a throwaway branch, verify on Chrome/Safari/Firefox, confirm log-depth works.\n3. If the spike succeeds, scope the full port as an epic with one shader pair per task.\n\n## History\n\nFiled 2026-05-08 from the stellata-9mm.67 review session, where the visible-count CPU mirror was shelved because dust extinction can't be cheaply applied on CPU and WebGL2 has no cheap GPU-side counter. WebGPU's atomic-counter compute shader would solve that in ~10 lines. See stellata-9mm.67 notes for the full WebGL2 GPU-counting alternative (counting RT + reduction + async fence readback — viable but ~100 lines of plumbing).","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-08T09:12:18Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:22:14Z","dependencies":[{"issue_id":"stellata-0it","depends_on_id":"stellata-9mm.67","type":"related","created_at":"2026-05-08T10:12:23Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0it","depends_on_id":"stellata-cns","type":"related","created_at":"2026-05-08T10:12:25Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-0it","depends_on_id":"stellata-cns.1","type":"related","created_at":"2026-05-08T10:12:24Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2f6.1.3","title":"Major moons: rendering integration (parent-relative bodies + visibility heuristic)","description":"**Goal:** render the major moons as parent-relative billboarded bodies in the focused planet's local frame. Mirrors `star-system.ts`'s per-planet body mesh, scaled down to N=1–7 per parent.\n\n### Render layer\n\nLikely a new `MoonSystem` class alongside `StarSystem`, or an extension of `StarSystem` that handles per-planet child bodies. Decision deferred until 2f6's focal-frame design lands — the natural shape depends on whether planet focus is a sub-state of star focus or a separate type.\n\nIf a separate class:\n\n```typescript\nclass MoonSystem {\n  setMoons(moons: readonly Moon[], parentPole: ParentPole): void;\n  // Per-frame: pull moon positions from the resolver (2f6.1.2),\n  // rotate by the parent-equatorial-to-ICRS quaternion, write to\n  // iPosition InstancedBufferAttribute, set needsUpdate.\n  update(t: number): void;\n  setVisible(on: boolean): void;\n  setMonochrome(on: boolean): void;\n}\n```\n\n### Visibility heuristic\n\nMajor moons orbit at sub-AU scales — Earth's Moon at ~0.0026 AU, Triton at ~0.0024 AU, Iapetus at ~0.024 AU. At any zoom outside the planet-zoom-affordance from 2f6, every moon projects sub-pixel and is invisible.\n\nRecommended gate: render moons only when the camera is within some multiple of the parent planet's heliocentric distance from the parent (e.g. closer than ~0.5 AU to the parent). Cheaper than a per-moon angular-separation heuristic and avoids the \"dot cluster on top of the planet disc\" failure mode at far framings.\n\nA second per-moon visibility test (à la `ringVisibility` in `star-system.ts`) suppresses moons whose pixel separation from the parent disc collapses below threshold. Galilean moons are well-separated visually at typical Jupiter-zoom; rocky moons (Phobos, Deimos) and Saturn's inner moons (Mimas, Enceladus) need closer zoom to pull off the parent.\n\n### Layered visibility\n\nSame gate as the rest of the solar-system layer:\n- Visible only when the focused star has planets AND a planet is the focal sub-object (per 2f6 focus model).\n- Hidden in chart mode.\n- Hidden during warp (consistent with planet labels, distinct from planet bodies which we now keep visible per `30eccc7` — moons at warp scale are unambiguous noise).\n\n### Body shading\n\nReuse `shaders/planet.{vert,frag}.glsl`. The fragment shader's `solidity` knob already differentiates rocky / icy / gas-giant edges; for moons:\n- `rocky` (Phobos, Deimos): 1.0 (hard edge).\n- `icy` (most outer moons — Europa, Enceladus, Titan, etc.): 0.7 or so.\n- `irregular` (very small captured asteroids — Phobos, Deimos do double-duty here): 1.0 with the caveat that 3D shape rendering is a 2f6 follow-up.\n\nEarth's Moon phase is the only moon whose phase is visually obvious; defer to 2f6's general planet-phase work — same shading model applies uniformly.\n\n### Tests\n\n- `MoonSystem` toggles visibility correctly via `setVisible`, `setMonochrome`.\n- `MoonSystem.update(t)` calls the resolver and re-uploads the iPosition attribute.\n- The visibility-by-camera-distance gate works at threshold edges.\n\n### Dependencies\n\n- Blocks on `stellata-2f6.1.2` (resolver) for the position pipeline.\n- Blocks on `stellata-2f6` itself for the focal-frame design (where `MoonSystem` mounts in the scene graph and how its host position is set per frame).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-08T08:11:34Z","created_by":"Alex Marshall","updated_at":"2026-05-08T08:11:34Z","dependencies":[{"issue_id":"stellata-2f6.1.3","depends_on_id":"stellata-2f6.1","type":"parent-child","created_at":"2026-05-08T09:11:33Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2f6.1.3","depends_on_id":"stellata-2f6.1.2","type":"blocks","created_at":"2026-05-08T09:11:43Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2f6.1.2","title":"Major moons: per-parent positionsAt(t, out) resolver (Kepler, parent-relative)","description":"**Goal:** compute moon positions in the parent planet's local frame at any wall-clock `t`. Mirrors the `PlanetSystem.positionsAt(t, out)` shape introduced in stellata-3re.3.\n\n### API\n\nFor each major-moon-bearing parent:\n\n```typescript\n// Returns moon positions in the parent planet's equatorial frame (xy\n// planar, z perpendicular). The renderer then applies the parent\n// planet's pole-orientation quaternion to land them in ICRS, mirroring\n// the per-host-plane dispatch from stellata-3re.8 / star-system.ts.\nfunction moonPositionsAt(parent: PlanetName, t: number, out: Float32Array): void;\n```\n\n`out` length = 3 × (count of moons for that parent), in the order the data-ingest bead (`stellata-2f6.1.1`) defines.\n\n### Implementation per moon\n\n1. `T = (tToJDE(t) - J2000_JD) / 36525` — centuries since J2000 (same convention as planet-positions resolver).\n2. `M = M₀ + Ṁ·T + n·(t - t₀)` — mean anomaly, where `n = 360°/period`.\n3. Solve Kepler's equation `M = E − e·sin(E)` (lift the solver from `ephemeris.ts:solveKepler`).\n4. In-plane cartesian: `x = a·(cos E − e)`, `y = a·√(1 − e²)·sin E`, `z = 0` (parent equator).\n5. Rotate by ω (in-plane), then by I (about x'), then by Ω (about parent z) → parent-equatorial cartesian.\n6. km → parsecs (or stay in km until rendering — pick once, document once).\n\n### Caching\n\nMoon orbital periods range from ~7 hours (Io's parent-relative motion at planet-zoom can be visible) to ~16 days (Titan) to ~360 days (Iapetus). The minute-bucket cache from 3re.3 (planet positions) is too coarse here — moons at planet-zoom complete an orbit in days, and a smooth scrubber (stellata-nmu) wants per-frame motion.\n\nRecommendation: cache per ~1 second (or even per-frame for the focused parent). Tune empirically once 2f6 affordances exist.\n\n### Why parent-equatorial frame, not direct ICRS\n\nEach parent planet's equator is tilted relative to the ecliptic by a different amount (Earth ~23.4°, Mars ~25°, Jupiter ~3°, Saturn ~27°, Uranus ~98°, Neptune ~28°). Moons orbit close to the parent equator (Laplace plane for outer giants), not the ecliptic. Returning positions in the parent-equatorial frame and letting the renderer rotate to ICRS via the parent's pole quaternion (data from `stellata-2f6.1.1`) is the natural decomposition.\n\nThis mirrors the per-host orbital-plane dispatch already in `star-system.ts:orbitalPlaneNormalFor` — Sol's planets get the ecliptic, exoplanets get the galactic plane (3re.8), parent planets' moons get the parent's equator. Same shape.\n\n### Tests\n\n- Per-moon: distance from parent stays in `[a(1-e), a(1+e)]` over ±3000 years.\n- Per-moon: position varies measurably over a fraction of one orbital period (e.g. quarter-period sample displaces by ~√2·a).\n- Per-moon: position returns to start after one full period.\n- Triton specifically: orbit is retrograde — moon-cross-velocity dotted with parent's spin axis is negative.\n\n### Dependencies\n\n- Blocks on `stellata-2f6.1.1` (data ingest — supplies the elements + parent poles).\n- Reuses `solveKepler` from `ephemeris.ts`.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-08T08:11:04Z","created_by":"Alex Marshall","updated_at":"2026-05-08T08:11:04Z","dependencies":[{"issue_id":"stellata-2f6.1.2","depends_on_id":"stellata-2f6.1","type":"parent-child","created_at":"2026-05-08T09:11:04Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2f6.1.2","depends_on_id":"stellata-2f6.1.1","type":"blocks","created_at":"2026-05-08T09:11:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-2f6.1.1","title":"Major moons: data ingest (per-planet IAU pole + per-moon orbital elements)","description":"**Goal:** assemble the static catalog the moon-position resolver and renderer need. Snapshot at build time per the `frozen-external-data` rule.\n\n### Per-planet data (parent reference frame)\n\nMost moons orbit close to their parent planet's equatorial plane (or Laplace plane for outer moons). The parent's equatorial plane is defined by the IAU rotational pole — RA, Dec of the planet's north pole in ICRS, plus a per-planet rotation rate W₀ + W·d. For our visualisation purposes only the pole matters (we don't need to track surface-fixed reference longitude).\n\nSource: IAU/IAG Working Group on Cartographic Coordinates and Rotational Elements, latest published recommendation. Bundled in the JPL ephemeris file headers; also tabulated in NASA fact sheets.\n\nPer-planet additions:\n- `poleRaDeg`, `poleDecDeg` — north pole orientation in ICRS J2000.\n- (Optional) per-century pole drift, if any planet's pole moves enough to matter at our zoom — Mars/Earth precess slowly, all are sub-arcsec/century, almost certainly noise at our scale; skip unless trivial.\n\n### Per-moon data\n\nFor each major moon (~20 total per `stellata-2f6.1`):\n- `name` (display string)\n- `parentPlanet` (enum: 'earth' | 'mars' | … | 'neptune')\n- `radiusKm` (mean radius for billboard sizing; same convention as `Planet.radiusKm`)\n- `colour` (representative single-tone RGB; same convention as `Planet.colour`)\n- `type` ('rocky' | 'icy' | 'irregular' — irregular for Phobos / Deimos)\n- **Orbital elements at J2000 in the parent's equatorial frame:**\n  - `semiMajorAxisKm` — keep in km (not AU) since moon orbits are sub-AU\n  - `eccentricity`\n  - `inclinationDeg` (relative to parent equator; Triton ≈ 157° = retrograde)\n  - `longAscNodeDeg` (Ω)\n  - `argPeriDeg` (ω)\n  - `meanAnomalyDeg` at J2000 (M₀)\n  - `periodDays` (or mean motion n in deg/day) — derivable from a + parent GM but precomputing avoids the lookup\n- (Optional) per-century secular rates for Ω, ω, M₀ where the moon's orbit precesses fast enough to matter (most major moons: yes for Ω, ω; small for M₀ if a, e are roughly constant)\n\n### Sources\n\nJPL Solar System Dynamics maintains canonical tables:\n- Per-planet IAU pole: https://ssd.jpl.nasa.gov/?planet_pos\n- Per-moon mean elements: https://ssd.jpl.nasa.gov/sats/elem/\n- Per-moon physical properties (mass, radius, colour estimates): https://ssd.jpl.nasa.gov/sats/phys_par/\n\nThe build-time approach decision (bundle CSV + parse, or hand-curate, or generate from astronomia) lives in the **related** bead `stellata-2f6.2`. The output schema this bead specifies is independent of the source.\n\n### Output\n\nA new module under `src/client/`, e.g. `moon-system.ts`, exporting:\n\n```typescript\nexport interface Moon {\n  readonly name: string;\n  readonly parentPlanet: PlanetName;\n  readonly radiusKm: number;\n  readonly colour: readonly [number, number, number];\n  readonly type: 'rocky' | 'icy' | 'irregular';\n  readonly elements: KeplerElements;  // J2000 osculating, parent-equatorial frame\n}\n\nexport const MAJOR_MOONS: readonly Moon[] = [...];\n\n// Per-planet pole orientation in ICRS — drives the parent-equatorial frame.\nexport interface ParentPole {\n  readonly raDeg: number;\n  readonly decDeg: number;\n}\nexport const PLANET_POLES: Record\u003cPlanetName, ParentPole\u003e = {...};\n```\n\n### Test coverage\n\n- Each moon has positive radius / period / a.\n- Eccentricities in [0, 1).\n- Inclinations in [0, 180] degrees (Triton confirms the retrograde range works).\n- Names match the `stellata-2f6.1` scope (no minor moons accidentally included).\n- Element tables cross-check published values (e.g. Earth's Moon a ≈ 384,400 km; Io a ≈ 421,800 km).\n\n### Related\n\n- `stellata-2f6.1` (parent epic).\n- `stellata-2f6.2` (astronomia build-time investigation — informs the source-of-data choice).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-08T08:10:35Z","created_by":"Alex Marshall","updated_at":"2026-05-08T08:10:35Z","dependencies":[{"issue_id":"stellata-2f6.1.1","depends_on_id":"stellata-2f6.1","type":"parent-child","created_at":"2026-05-08T09:10:34Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2f6.1.1","depends_on_id":"stellata-2f6.2","type":"related","created_at":"2026-05-08T09:11:41Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-2f6.2","title":"Investigate astronomia as build-time ephemeris-table source","description":"**Goal:** evaluate whether to use the [astronomia](https://www.npmjs.com/package/astronomia) npm package at *build time* to generate small runtime ephemeris tables, particularly for the moon ephemerides needed by the major-moons epic (sibling stellata-2f6.1).\n\n### Backstory\n\nWe rejected astronomia at runtime three times now (planet positions in stellata-3re.3, etc.) because shipping the full VSOP87 / ELP coefficient set is ~500 KB gzipped — way past what billboarded planet/moon discs actually need. The astronomia tarball is 18.7 MB unpacked, with per-body coefficient files in the hundreds-of-KB range.\n\nBut astronomia is well-tested, semi-actively maintained, and bundles solid Meeus implementations of:\n- VSOP87D for planets (we replaced with JPL Standish 1992 → no value to redo from astronomia)\n- Abridged ELP for the Moon (~70 KB; Meeus chapter 47)\n- Galilean satellites (jupitermoons.js, ~10 KB; Meeus E2x3 truncation)\n- Major Saturnian moons (saturnmoons.js, ~10 KB; Meeus chapter 45)\n- Lacks: Uranian moons (we'd need GUST86 from elsewhere), Neptunian moons\n\n**The angle worth pursuing:** instead of bundling astronomia at runtime *or* hand-rolling each formulation, run astronomia in the build pipeline and emit smaller domain-specific tables.\n\n### Investigation tasks\n\n1. **Tree-shake test.** Import only `astronomia/jupitermoons` and `astronomia/saturnmoons` and `astronomia/moonposition` in a build target. Measure resulting bundle size. Astronomia's package.json may not expose tree-shaking-friendly entry points; if so, copy the relevant files directly.\n\n2. **Build-time table-generator pattern.** Sketch a `scripts/build-ephemeris.ts` that:\n   - imports needed astronomia modules\n   - samples positions / elements at chosen epochs (J2000 ± centuries, fine-grained around now)\n   - fits truncated polynomials per element per body (e.g. 5th-order over ±3000 years)\n   - emits TypeScript const tables to `src/client/ephemeris-data/` or a build artifact like `public/ephemeris.bin`\n   - covers: Earth's Moon, the four Galileans, the seven major Saturnians\n\n3. **Compare with manual JPL CSV ingest.** JPL's [satellite-elements table](https://ssd.jpl.nasa.gov/sats/elem/) publishes mean elements + secular rates directly — no astronomia needed. The build-pipeline angle is only valuable if astronomia gives us *better* coverage / accuracy than the JPL CSV at the same effort. Need to assess this per-system.\n\n4. **Output format choice.** Consider TypeScript const tables (typecheck-friendly, compiled into bundle), JSON file (lazy-loaded, smaller bundle), or a binary format mirroring `catalog.bin` (smallest, opaque). The pattern from `scripts/build-catalog.ts` is the natural reference.\n\n### What this is NOT\n\n- **Not** about runtime astronomia. Runtime use is rejected.\n- **Not** about replacing JPL Standish for planets — that work is done; astronomia and Standish use the same upstream VSOP series, so re-deriving from astronomia would reinvent the wheel.\n\n### Considered and rejected: fork / upstream a lightweight astronomia\n\nIdea was floated to PR a \"lite\" build target into astronomia, or maintain a stellata fork. Rejected as out of scope for this project:\n\n- astronomia's tarball is ~18 MB raw because it bundles *all* the coefficient sets — a \"lite\" build is at odds with the package's positioning as a complete reference.\n- The actual code we need is small enough to write or generate ourselves.\n- Forking commits us to maintenance of a JS library we'd otherwise just consume.\n- astronomia release cadence is sparse (8+ months since last release at time of writing); upstream PR may sit indefinitely.\n\nIf anything emerges from the build-time investigation that *would* benefit the broader community, an upstream PR could be a follow-up; but the investigation should not block on that path being viable.\n\n### Output of this bead\n\nA short recommendation: which moon systems benefit from build-time astronomia (vs manual JPL CSV vs hand-rolled Meeus formulas), what the build pipeline looks like, and whether stellata-2f6.1's data-ingest child should adopt this approach or skip it.\n\n### Related\n\n- `stellata-2f6.1` (major moons epic) — primary consumer of the recommendation.\n- `stellata-1gh` (deep-time accuracy research) — orthogonal but related: the same investigation could inform whether astronomia helps the planet ephemerides at deep-time edges.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-08T08:10:02Z","created_by":"Alex Marshall","updated_at":"2026-05-08T08:10:02Z","dependencies":[{"issue_id":"stellata-2f6.2","depends_on_id":"stellata-1gh","type":"related","created_at":"2026-05-08T09:11:44Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2f6.2","depends_on_id":"stellata-2f6","type":"parent-child","created_at":"2026-05-08T09:10:02Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2f6.1","title":"(EPIC) Major moons of Sol's planets","description":"**Goal:** render the major moons of Sol's planets as planet-relative bodies once the user can zoom into a planet (gated on stellata-2f6).\n\n**Scope: major moons only.** Approximately 20 named, roughly-spherical satellites:\n\n- **Earth (1):** Moon\n- **Mars (2):** Phobos, Deimos (both small captured asteroids; included for completeness since Mars only has these two)\n- **Jupiter (4):** Io, Europa, Ganymede, Callisto (the Galileans)\n- **Saturn (~7):** Mimas, Enceladus, Tethys, Dione, Rhea, Titan, Iapetus (skip Hyperion — irregular shape, less major)\n- **Uranus (5):** Miranda, Ariel, Umbriel, Titania, Oberon\n- **Neptune (1):** Triton (retrograde orbit, i ≈ 157°)\n\n### Deliberately excluded\n\n- **All minor / irregular moons (~270+).** Saturn jumped from 83 → 146 in 2023 from the Subaru survey; the vast majority are sub-5km outer irregulars with provisional designations like `S/2003 J 2`. No visual value at any zoom we'd render at; mostly potato-shaped and unnamed; continually being rediscovered. Snapshot-and-freeze a constantly-changing catalog of meaningless-at-disc-scale rocks is poor ROI.\n- **Pluto's moon system.** Filed as a separate epic if/when Pluto becomes a focal body.\n- **Asteroid moons** (e.g. Dactyl orbiting Ida) and **comet structures**. Out of scope.\n- **Saturn / Jupiter / Uranus / Neptune ring systems.** Sister bead — same gating epic (stellata-2f6) but very different render approach. To be filed separately once moons take shape.\n\n### Why gated on stellata-2f6 (planet-as-object)\n\nAt any current zoom, planet bodies floor-clamp to ~2 px discs. Moons that orbit at sub-AU distances would project sub-pixel and be invisible. Implementing moon rendering before planet-zoom affordance lands violates the new `stellata-defer-detail-until-zoom-affordance` rule. Once 2f6 lets the user fly within hundreds of thousands of km of a planet, moons become first-class visual elements.\n\n### Why gated, also, on the time scrubber feeling right\n\nPlanet positions move sub-arcsec/sec at our zoom — invisible, cached for 60s in 3re.3. Moons at planet-zoom move *visibly*: Earth's Moon completes an orbit in 27.3 days; at planet-zoom, that's degrees per minute of perceptible sweep. The minute-bucket cache from 3re.3 needs to drop way down for moons under any \"play time\" affordance the scrubber introduces (stellata-nmu).\n\n### Children (to file)\n\n1. **Data ingest.** Per-planet IAU pole orientation (RA, Dec of north pole) + per-moon J2000 osculating Keplerian elements + secular rates where available. JPL satellite-elem table is the canonical source. See related bead on the astronomia build-time investigation.\n2. **Per-parent moon-position resolver.** Same shape as `PlanetSystem.positionsAt(t, out)` from 3re.3, but operating in the parent planet's equatorial frame. Returns moon positions relative to the parent planet's centre.\n3. **Rendering integration.** Extend the body-mesh pattern from star-system.ts to support per-planet child bodies. Per-moon visibility heuristic similar to per-planet orbit rings (3re.7).\n4. **(Coupled to 2f6 phase work)** Earth's Moon phase rendering. Moon phase is the most visually obvious property of any moon; once 2f6 introduces planet phase shading, Earth's Moon inherits it.\n\n### Other considerations the children pick up\n\n- **Non-equatorial orbits.** Most moons orbit close to the parent's equatorial plane (Laplace plane for outer moons). Parent equatorial plane ≠ ecliptic. Need each planet's IAU pole. Triton is the famous exception — retrograde at i ≈ 157°.\n- **Tidal locking.** Most major moons are tidally locked to their parent. If 2f6 introduces 3D body meshes (vs current billboards), moons inherit the same shading model; tidal lock means the same face always toward parent.\n- **Co-orbital pairs.** Saturn's Janus and Epimetheus literally swap orbits every 4 years; Tethys has Trojan companions at L4 / L5 (Telesto, Calypso). Independent Kepler around the parent gives them all the right (independent) positions; no special handling needed.\n- **Earth-Moon barycentre.** Our JPL Standish table (3re.3) computes EM Bary, not Earth itself. Earth's centre lies ~4700 km off-barycentre — sub-pixel at planet-disc scale, visible at Earth-zoom. The Earth-Moon split would naturally fall out of moon ephemerides if we add them.\n- **Searchability and focusability.** Should \"Europa\" in the search box jump to it? Should moons be focal objects? Belongs to 2f6's \"what's a focal object\" rework, not here.\n\n### Out of scope for v1 of this epic\n\n- Phase / illumination shading other than Earth's Moon (the rest defer to 2f6's general planet-phase work — apply to all bodies uniformly when it lands).\n- Moon-on-moon eclipses, transits, mutual events.\n- Saturn ring shadow on Saturn's moons.\n- Moon-of-moon (none currently confirmed in our system anyway).","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-08T08:09:13Z","created_by":"Alex Marshall","updated_at":"2026-05-08T08:09:13Z","dependencies":[{"issue_id":"stellata-2f6.1","depends_on_id":"stellata-1gh","type":"related","created_at":"2026-05-08T09:11:44Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2f6.1","depends_on_id":"stellata-2f6","type":"parent-child","created_at":"2026-05-08T09:09:13Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2f6.1","depends_on_id":"stellata-nmu","type":"related","created_at":"2026-05-08T09:11:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-1gh","title":"Research: planet position accuracy beyond ±3000 years from J2000","description":"Beyond the validity window of JPL Standish 1992 (used by stellata-3re.3), planet positions become unreliable. JPL Standish 1992 with cubic Jupiter–Neptune corrections is good to ~arcminute over ±3000 years; outside that window the linear+cubic element model breaks down (mean-motion resonances and longer-period perturbations dominate).\n\nIf the time-scrubber epic (stellata-nmu) wants to allow scrubbing further than ±3000 years from J2000:\n\n### Options to investigate\n\n1. **Cap the scrubber range at ±3000 years.** Simplest. Documented in the scrubber UX as 'historical-era only'. No code changes here.\n2. **Switch to VSOP87 (full series).** ~1.5 MB raw / ~500 KB gzipped via the astronomia npm package. Designed validity ±4000 years. Even VSOP87 is undefined beyond that window.\n3. **Truncated VSOP87 inline.** Pick the largest-amplitude terms per planet from the VSOP87 series, ship a trimmed coefficient table inline. ~50 KB code. Lets us tune the size/accuracy tradeoff.\n4. **Numerical integration.** Symplectic integrator (e.g. WHFast) seeded from JPL DE4xx initial conditions. Good for arbitrary epochs but blows the bundle and adds CPU cost.\n\n### Decision points\n\n- What is the maximum scrubbable  range the scrubber should expose?\n- What is acceptable accuracy at the scrubber's far edges? (E.g. arc-minute vs degree-scale).\n- Bundle size vs accuracy tradeoff for stellata's overall page-weight budget.\n\nThis task is research-only — produces a recommendation that informs the scrubber's range constraints (stellata-nmu) and any future planet-position upgrade.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-07T11:43:10Z","created_by":"Alex Marshall","updated_at":"2026-05-07T11:43:10Z","dependencies":[{"issue_id":"stellata-1gh","depends_on_id":"stellata-3re.3","type":"related","created_at":"2026-05-07T12:43:29Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-1gh","depends_on_id":"stellata-nmu","type":"parent-child","created_at":"2026-05-08T11:22:07Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2f6","title":"Planet-as-object: select, focus, and zoom into individual planets","description":"**Goal:** treat planet bodies as selectable, focusable objects so the camera can fly arbitrarily close to any of them and reveal per-planet visual detail.\n\n**Why now:** the current solar-system layer (stellata-3re) renders planets as physically-sized billboarded discs. At any reasonable host-relative camera distance the bodies project below the pixel-size floor and clamp — Jupiter and Mercury read as identical 2-px dots. A full set of per-planet refinements (texture maps, atmospheric haloes / scattering, banding, ring systems, moons, axial tilt, day-side / night-side shading) all depend on the user being able to get close enough to a single body to see them. Without that affordance, those details are wasted code.\n\n**Scope (open — refine before scheduling):**\n\n1. Planet picking. The existing star-pickStar pipeline returns a star catalog index; this needs an analogue for planet bodies: cursor → planet identifier (host star + planet index in PlanetSystem). Hover tooltip + click select.\n\n2. Planet focus. setFocus(starIdx) currently snaps the floating origin to the focal star and clamps controls.minDistance to the per-star physical floor. For planets, the focal frame becomes (host star, planet index); the floating origin recenters on the planet's local position; controls.minDistance drops below the planet's physical radius (e.g. ~5e3 km for Earth orbit, ~2e4 km for Jupiter). Need to think through the implications for the floating-origin precision invariants.\n\n3. Camera framing. Arrival pose, default orbit radius for a focused planet (analogue of the per-star d_min(R) from stellata-a7d.2), warp-from-host-to-planet animation.\n\n4. URL state. Today the URL encodes a focused star index; planet focus needs an additional sub-identifier.\n\n5. UI plumbing. Search-by-planet-name, planet entries in the focus search box, breadcrumb indicator (\"focused on Earth orbiting Sol\"), Esc step-back semantics (planet → host star → unfocused).\n\n6. What it unblocks (separate beads filed once this epic clears the design gate):\n   - **stellata-3re.10**: texture research → texture rendering, since you can now actually see them.\n   - **Atmosphere / banding / scattering**: per-body shader work, deferred from stellata-3re.4 because the bodies currently floor-clamp to a 2-px disc.\n   - **Ring systems** (Saturn, faint rings of the other giants), **moons**, **axial tilt and rotation**, **day/night phase shading**.\n   - **Exoplanet planet inspection** (ties to stellata-bk5.3): the same selection/zoom affordance carries to focused-host exoplanet bodies.\n\n**Design gate (suggested first child):** decide whether planet focus is a refinement of the existing star-focus state (a sub-index added to focusedStar) or a separate orthogonal focus type (focusedPlanet alongside focusedStar / focusedCloud). Implications for setFocus, URL encoding, observe mode, and the Stellata.getFocusedPlanetSystem() event ripple from this choice.\n\n**Defer:**\n- Anything exoplanet-specific (covered under stellata-bk5).\n- Solar system aside-from-planets — heliopause is in 3re.5, deep-space probes in stellata-ywn.\n\nLABELS: phase-future, solar-system, ui, design-gate","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-07T11:05:07Z","created_by":"Alex Marshall","updated_at":"2026-05-07T11:05:07Z","dependencies":[{"issue_id":"stellata-2f6","depends_on_id":"stellata-3re.10","type":"relates-to","created_at":"2026-05-07T12:05:35Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2f6","depends_on_id":"stellata-3re.4","type":"relates-to","created_at":"2026-05-07T12:05:35Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2f6","depends_on_id":"stellata-bk5.3","type":"relates-to","created_at":"2026-05-07T12:05:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.66","title":"Unit tests for v1.1.4 hot-path helpers (peakAmplitudeFactor, minDistForStar, pickStar, getWarpInfo, url-state EPS)","description":"PR #35 (v1.1.4) shipped six bd-tracked changes — two bug fixes (warp no-op for co-located stars, variable peak overshoot) and four perf wins (dust prefilter, url-state EPS, pickStar binary-search window, hot-path scratch Vector3s). All verification was manual smoke tests; no new unit tests were added.\n\nCoverage gaps worth filling so future refactors of these helpers have a regression net:\n\n- peakAmplitudeFactor(idx): `pow(10, amp/10)` for a variable, `1` for non-variables (no period or no amplitude). Pure helper, easy to test with a hand-rolled catalog stub.\n- minDistForStar / minOrbitDistForStar: that Reff = R · peakAmplitudeFactor is what gets fed into distAtFillFraction. distAtFillFraction itself is already covered by star-geometry.test.ts; the wrapper is not.\n- Warp no-op branch in startWarp: distPc \u003c 1e-6 takes the setFocus / setFocusedCloud path (no warp animation). Could be covered by a Stellata harness test or extracted to a pure helper.\n- pickStar windowed-scan: behavioural test that the windowed scan returns the same picks as the (now-removed) full-catalog scan over a representative slider range. Would catch off-by-one in sortedDistRange.\n- url-state startUrlSync EPS comparison: that the new component-wise epsilon comparator schedules a write iff any of the 9 components changes by ≥ 1e-3.\n\nAcceptance: vitest module(s) covering at least the first three above. The pickStar one is harder (needs camera + catalog harness) and may be a follow-up.\n\nSource PR: #35.\n\nFiled during the post-merge review cleanup that extracted sortedDistRange / destLocalPositionInto helpers — those helpers are also untested and could be in scope here.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-07T09:58:57Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:15Z","started_at":"2026-05-08T18:25:56Z","closed_at":"2026-05-08T18:54:15Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.66","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-07T10:58:57Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-fe4","title":"Revisit variable-star pulsation: radius-only vs radius+temperature, validate Mira amplitude realism","description":"Revisit whether modeling variability via radius change alone is physically defensible. Today Stellata uses the constant-T assumption `R ∝ √L` (so brightness amplitude lands entirely on R), which is explicitly listed in CLAUDE.md under 'Things deliberately kept out':\n\n\u003e Temperature-swing component of variable-star brightness change. We use `R ∝ √L` (constant-T assumption); real pulsating variables split the brightness change between R and T swings. Modelling T changes per variable type is more complexity than the visualisation warrants.\n\nThe concern reopening this: for high-amplitude pulsators (Mira itself being the type-defining example), full-amplitude variability under the constant-T model implies an angular-diameter swing that may be extreme relative to what real stellar physics produces. Real Miras split the change between R-swing and T-swing (and the T-swing dominates V-band amplitude more than people often assume).\n\n# What needs investigating\n\n1. **Quantify the discrepancy.** For Mira (and a few other high-amplitude AAVSO archetypes — chi Cyg, R Hya, R Leo) compute the on-screen angular-diameter ratio between minimum and maximum light under the current `R ∝ √L` assumption, and compare to interferometric measurements of the actual physical radius swing (CHARA / VLTI / IOTA Mira-disc papers — Lacour 2009, Woodruff 2009, Wittkowski 2016, etc.). If the modeled R-swing is far larger than the real R-swing, the visual is not 'physical reality with perceptual exaggeration only where the eye demands it' — it's just wrong.\n\n2. **Decide on a fix path.** Three plausible options, in increasing complexity:\n   - (a) Cap the R-swing per variable type (simple multiplier table), accept that brightness amplitude is then under-served on V-band.\n   - (b) Split the amplitude: a fixed per-type fraction of luminosity change goes to R, the rest is encoded as a colour/temperature shift in the disc + glow shader (we already pass spectral colour via uniforms — extending that to per-frame T-shift is a contained change to the variable-star uniform path).\n   - (c) Keep the current model, document the limitation in SCIENCE.md as a known stylistic choice, do nothing in code.\n\n3. **Decide if the answer differs by GCVS variable type** (Mira-types, Cepheids, RR Lyrae, eclipsing binaries are all already in the build). The cost of (b) goes up if it has to be type-specific; (a) scales fine.\n\n# Pointers\n\n- Variable-star data flows from GCVS via scripts/catalog-pure.ts → binary v4 → vert/frag uniforms.\n- Pulsation lives in src/client/shaders/star.{vert,frag}.glsl (`R_eff = R · radiusFactor` plus glow scaling).\n- Documented choices: SCIENCE.md, CLAUDE.md ('Things deliberately kept out' bullet quoted above), docs/rendering.md (variable pulsation section).\n- Loosely related: stellata-a7d (physical-scale rewrite — this bead is downstream of that landing) and stellata-a7d.2.10 (V-band → perceived brightness correction, the static-spectral analogue of what (b) above does dynamically).\n\n# Acceptance\n\n- Mira's full-amplitude angular-diameter swing in the renderer is verified against published interferometric measurements; gap quantified.\n- Decision recorded: implement (a), (b), (c), or 'something else' — and either the CLAUDE.md 'kept out' bullet is updated/removed, or the bead is closed with rationale to leave it.\n- If implementing (b): the temperature-swing path lands in the variable-star shader uniform pipeline with per-type amplitudes documented in SCIENCE.md.","notes":"# Visual reference — Wikipedia Cepheid pulsation animation\n\nAlex flagged this animation as a possibly useful visual reference for the option-(b) split (R-swing + T-swing in phase) the epic is considering. NOT a physics-validation source — useful only for getting an eye on what coupled R+T pulsation looks like rendered.\n\n**URL:** https://commons.wikimedia.org/wiki/File:Cepheid_animation_5_rend_1.gif\n**Author:** Merikanto, 2023-01-09, CC BY-SA 4.0\n**Rendering stack:** POV-Ray 3.7, 100 frames, 500×500, 5 s loop\n\n## Animation parameters (verified against the file page metadata)\n\n- **Radius:** sphere scale oscillates 0.8 ↔ 1.2 — ±20% (40% peak-to-peak)\n- **Temperature:** blackbody 5500 K ↔ 6800 K, synced in phase with radius\n- **Surface:** procedural granite + wrinkles overlay (purely aesthetic)\n- **Brightness:** ambient lighting varies with pulsation phase\n\n## How physics-grounded is it?\n\n- **Radius amplitude is exaggerated.** Real classical Cepheids vary by ~5–10% in radius (e.g. δ Cephei itself ≈ 7%), not 40% peak-to-peak. RR Lyrae and W Vir sit in similar ranges. The animation's ±20% radius is well above observed values.\n- **Temperature range is plausible.** 5500–6800 K spans a believable classical-Cepheid range across phase (typical F-G excursions). The qualitative direction — hotter at minimum radius / near maximum light — matches the standard κ-mechanism phase relation.\n- **The R+T phase coupling is the right qualitative model** — and is precisely what option (b) in this epic proposes for the renderer.\n\n## Why keep this on the bead\n\nWhen fe4.1 lands on a decision and fe4.2 starts on option (b), this is a quick reference for what a published procedural rendering of the coupled R+T animation looks like — useful for calibration intuition. The actual physics inputs still come from the interferometric citations already listed in fe4.1 (Lacour 2009, Woodruff 2009, Wittkowski 2016) and the standard Cepheid photometric amplitude tables. Treat the Wikipedia animation as visual mood-board, not a parameter source.","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:59:05Z","created_by":"Alex Marshall","updated_at":"2026-05-18T09:20:20Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-nmu.3","title":"Decision: time control widget design (slider, jump-to-date, FF/RW, format)","description":"**Goal:** decide what the scrubber actually looks like before building it.\n\n### Design surface to settle\n\n1. **Continuous slider vs jump-to-date input vs both.** A slider gives the \"scrub the universe\" feel; a date input lets the user be precise. Both is probably right, with the slider as the primary affordance and the date input as a click-to-edit detail.\n2. **Slider range.** `±10 yr`? `±10,000 yr`? Or two-tier: a log-scale that maps a single-track slider over `±100 Myr`?\n3. **Auto-play.** Forward/back buttons that march `t` at adjustable speed (e.g. 1 day/sec, 1 yr/sec, 1000 yr/sec). Pause/resume.\n4. **Keyboard nudges.** Arrow keys = fine step; shift = coarse; alt/option = jump? Use the existing keyboard-shortcuts.ts dispatch.\n5. **Time format on the readout.** Inherits from stellata-3re.H — both this widget and the static readout should agree on the format. Leading recommendation in 3re.H is UTC Zulu (`2026-05-07 18:23Z`).\n6. **Where it lives on the screen.** `.ui-bottom` next to the scale bar, or a dedicated affordance further from the scale bar so they do not visually compete?\n7. **Live mode.** \"Live\" means `t = Date.now() / 1000`, refreshed each frame. The widget needs a clear \"Live\" state when scrubbed back to now; should the slider snap to \"Live\" when within tolerance, or does the user have to click a button?\n8. **URL parameter.** `t=\u003cunix\u003e` only when scrubbed away from \"Live\"; absence in URL = live. Already specified in stellata-3re.1; confirm here that the widget honours it.\n\n### Out of scope\n\n- The actual implementation of the widget — that becomes its own task once the design lands.\n\nLABELS: time-scrubber, design-decision, ui","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:51:32Z","created_by":"Alex Marshall","updated_at":"2026-05-07T08:51:32Z","dependencies":[{"issue_id":"stellata-nmu.3","depends_on_id":"stellata-nmu","type":"parent-child","created_at":"2026-05-07T09:51:31Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-nmu.2","title":"Decision: hide-vs-keep policy for non-time-aware objects at extreme t","description":"**Goal:** decide what happens to scene objects without time data when `t` is far from now.\n\n### The problem\n\nSome objects (molecular clouds, dust, distant catalogue stars without proper-motion data) cannot be correctly positioned at non-current epochs. Two extreme behaviours:\n\n- **Always show.** Render them at their J2000 positions regardless of `t`. Easy. Misleading at million-year timescales (the local dust cloud morphology at t = -10 Myr is nothing like today's).\n- **Always hide.** Render only objects that have time data. Honest. Empty / sparse for `t != now` since most layers have no time info.\n\n### Options between the extremes\n\n- **Soft fade.** Each layer carries a \"validity timescale\" — within it, render normally; outside it, fade to invisibility (linear / log fade window).\n- **Hard cutoff.** Past a single global `|t - now|` threshold (say 10,000 years), hide every layer that does not have time info.\n- **Banner.** Always show everything, but display a banner (\"at this scale only the solar system is correctly positioned\") when `|t - now|` is large.\n\n### Recommendation (tentative)\n\nSoft fade per layer:\n- ATHYG stars without proper-motion data fade above ~10,000 yr offset.\n- Molecular clouds + dust fade above ~100,000 yr offset.\n- The solar-system layer is always valid.\n\nCombined with the audit from the sibling task, this gives a coherent story: the user can sweep millennia and the scene gracefully degrades to \"only what we know\" at the extremes.\n\n### Open question\n\nWhere do constellation lines fit? They are derived from star positions, so they trail whatever ATHYG does. If half the stars in Orion fade out, the stick-figure breaks — probably the right behaviour (the constellation literally changes on long-enough timescales).\n\nLABELS: time-scrubber, design-decision","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:51:19Z","created_by":"Alex Marshall","updated_at":"2026-05-07T08:51:19Z","dependencies":[{"issue_id":"stellata-nmu.2","depends_on_id":"stellata-nmu","type":"parent-child","created_at":"2026-05-07T09:51:19Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-nmu.1","title":"Audit: catalogue object motion data + decide what moves with t","description":"**Goal:** answer the central design question for the time scrubber — when `t` moves, which objects in the scene move with it?\n\n### Audit\n\nFor each layer / dataset, document the proportion of records carrying time-relevant motion data:\n\n| Layer | Field(s) needed | Question to answer |\n|---|---|---|\n| ATHYG stars | `pmra`, `pmdec`, `rv` | What % have all three? Just proper motion (pmra/pmdec)? Just radial velocity? |\n| GCVS variables | (already animated via uTime) | Period of pulsation already drives variable-star size. Does scrubbing `t` correspond to phase shifts? |\n| Zucker molecular clouds | (none expected) | Confirm: clouds are snapshot-only. |\n| Edenhofer dust | (none expected) | Confirm: dust map is current-epoch. |\n| Stellarium constellation lines | (N/A) | Constellations are derived from star positions; they move iff stars move. |\n\n### Decision options\n\n- **All-or-nothing:** every object that *can* move, moves; everything else freezes in place. Pro: consistent. Con: clouds in their J2000 positions at t = 1500 CE is misleading; at t = 0.1 Myr it is wrong.\n- **Honest motion:** move only objects with motion data; visually fade out (or hide) objects without it as `|t - now|` exceeds their reasonable lifetime. Pro: honest. Con: complicated UX.\n- **Solar-system-only:** the time scrubber only affects planets / probes; everything else is frozen. Pro: simplest. Con: limits the scrubber's narrative power (no \"see Polaris drift\").\n\n### Recommendation (tentative)\n\nBranch on time scale:\n- `|t - now| \u003c ~10,000 yr`: all stars with proper-motion data move; everything else stays in place (clouds / dust / constellations look the same on this timescale anyway).\n- `|t - now| \u003e ~10,000 yr`: only solar-system objects move; stars freeze and a banner explains.\n\nThis connects to the \"hide-vs-keep\" sibling task — they share the same time-scale logic.\n\n### Deliverable\n\nA documented decision in this issue's closing note + the audit numbers (so future-you can revisit if the catalogue refresh changes the data coverage).\n\nLABELS: time-scrubber, design-decision","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:51:09Z","created_by":"Alex Marshall","updated_at":"2026-05-07T08:51:09Z","dependencies":[{"issue_id":"stellata-nmu.1","depends_on_id":"stellata-nmu","type":"parent-child","created_at":"2026-05-07T09:51:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-bk5.6","title":"Decision: planet type indication (gas giant vs rocky vs unknown)","description":"**Goal:** decide how planet type is communicated visually. Lands before stellata-bk5.3 (rendering).\n\n### What we have\n\nNASA `pscomppars` does not carry a single canonical \"type\" column. We can derive type heuristically from radius and mass:\n\n- **Rocky:** `pl_rade \u003c ~1.6` (the \"radius valley\" boundary from Fulton 2017)\n- **Mini-Neptune / sub-Neptune:** `1.6 \u003c pl_rade \u003c ~4`\n- **Neptune-like ice giant:** `4 \u003c pl_rade \u003c ~10`\n- **Gas giant:** `pl_rade \u003e ~10` (or `pl_radj \u003e ~0.8`)\n- **Unknown:** radius is null\n\nMass can disambiguate (ρ = M/V) but radius alone is sufficient for visual buckets.\n\n### What to decide\n\n(a) **Skip type entirely.** Render every planet as a uniform disc. Loses information.\n(b) **Colour-code by type.** Rocky = warm grey/brown, sub-Neptune = pale blue-green, Neptune = deep blue, gas giant = banded yellow. Quick to ship, schematic. Matches the same approach used for Sol planets in 3re.E.\n(c) **Per-planet representative colour.** When NASA carries colour info (rare — mostly atmospheric albedo for transiting planets), use it; otherwise fall back to the type-bucket colour. Marginal improvement over (b).\n\n### Unknown type (null radius)\n\nUse a neutral grey + the \"unknown\" indicator from bk5.3's null-radius treatment. Same visual; the indicator does the explaining.\n\n### Recommendation (tentative)\n\nOption (b) — type-bucketed colour from radius, derived once in the build script and stored as a categorical enum (`rocky | sub_neptune | neptune | gas_giant | unknown`) on each planet record. Renderer maps enum → colour. Cheap, informative, consistent with how Sol's planets get representative colours in 3re.E.\n\n### Format the renderer consumes\n\n`PlanetSystem.planets[i].type: \"rocky\" | \"sub_neptune\" | \"neptune\" | \"gas_giant\" | \"unknown\"` — derived in build script from `pl_rade` (with `pl_radj * 11.2` as fallback). Single enum, single colour table.\n\nLABELS: exoplanets, phase-11, design-decision","notes":"Created 2026-05-07 — completes the trio of bk5 design decisions Alex flagged. Together with the orbital-fallback and size-representation decisions, bk5.3 (rendering) inherits a fully-specified data contract.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:50:52Z","created_by":"Alex Marshall","updated_at":"2026-05-07T08:56:51Z","labels":["phase-11"],"dependencies":[{"issue_id":"stellata-bk5.6","depends_on_id":"stellata-bk5","type":"parent-child","created_at":"2026-05-07T09:50:52Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-bk5.5","title":"Decision: planet size representation format (absolute vs relative-to-host)","description":"**Goal:** decide how exoplanet sizes are represented in the renderer when radius is known. Lands before stellata-bk5.3 (rendering).\n\n### The problem\n\nNASA `pscomppars` carries radius as either `pl_rade` (Earth radii) or `pl_radj` (Jupiter radii), with one or both populated depending on the source. Stellata's star renderer (post stellata-a7d.2) sizes discs by true angular diameter `θ = 2·atan(R/d)` using `R` in parsecs. To plug planets into the same pipeline we need `R` in parsecs — straightforward unit conversion either way.\n\n### What to decide\n\nThe user-visible question is whether the planet's on-screen size is anchored to:\n\n(a) **Absolute physical scale.** A 1 R⊕ planet always renders at the same angular diameter from a given camera distance, regardless of its host's size. This is what the angular-diameter pipeline naturally produces. Real-world result: most planets at typical viewing distances are sub-pixel; only gas giants resolve.\n\n(b) **Relative-to-host visual scale.** Multiply planet radius by some perceptual factor (or render at `min(physicalRadius, hostRadius × 0.05)` to keep the visual ratio readable). Loses physical accuracy but the planet:host visual ratio is informative.\n\n(c) **Hybrid.** Physical when zoomed close (planet resolves geometrically), exaggerated minimum when zoomed far.\n\n### Recommendation (tentative)\n\nOption (c) — same `max(appSize, physSize)` pattern stars already use. For planets, `appSize` would be a small floor (e.g. 2 px) so they remain visible at typical exoplanet-viewing zoom levels even when the true angular size is sub-pixel. The physical-disc threshold takes over at close approach.\n\n### Format the renderer consumes\n\n`PlanetSystem.planets[i].radius_pc: number | null` — a single canonical field, derived in the build script from whichever NASA column is populated. Build script handles unit conversion (`R⊕ = 4.2588e-5 AU`, `R♃ = 4.7790e-4 AU`, `1 AU = 4.8481e-6 pc`) so the runtime never sees the source-column ambiguity.\n\n### Null radius\n\nAlready covered in bk5.3 — render at default mid-range size with a visual indicator. Reaffirm here so the contract is consistent.\n\nLABELS: exoplanets, phase-11, design-decision","notes":"Created 2026-05-07 — Alex flagged that the size-representation format was not pinned down in the existing bk5 description.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:50:40Z","created_by":"Alex Marshall","updated_at":"2026-05-07T08:56:51Z","labels":["phase-11"],"dependencies":[{"issue_id":"stellata-bk5.5","depends_on_id":"stellata-bk5","type":"parent-child","created_at":"2026-05-07T09:50:39Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-bk5.4","title":"Decision: orbital fallback when pl_orbsmax is null","description":"**Goal:** decide what to do when NASA Exoplanet Archive has no semi-major axis for a confirmed planet. Lands before stellata-bk5.3 (rendering) so the renderer inherits a settled rule.\n\n### The problem\n\nThe NASA `pscomppars` table has `pl_orbsmax` (semi-major axis in AU) populated for the majority of confirmed planets but not all. Some entries carry only `pl_orbper` (period) or `pl_bmasse` (mass) without orbit geometry. The renderer cannot draw an orbit ring without a radius.\n\n### Options\n\n1. **Compute from period via Kepler's third law.** When `pl_orbper` is present and `st_mass` is approximately known (or default to the host's `st_lum`-implied mass), `a = (G·M·P²/4π²)^(1/3)`. Pros: physically grounded. Cons: requires guessing host mass when missing; minor planets get smeared.\n2. **Fixed default.** Show every null-orbit planet at a fixed visual radius (e.g. 1 AU) with a clear visual indicator. Pros: simple. Cons: misleading.\n3. **Hide null-orbit planets.** Skip rendering entirely. Pros: honest. Cons: loses entries.\n4. **Synthetic spacing.** When N planets exist around a host with no orbit data, space them at the geometric mean of the system's known orbits. Cons: invented physics.\n\n### Recommendation (tentative)\n\nOption 1 (Kepler's third law) when `pl_orbper` is present, falling back to option 3 (hide) otherwise. Surface a separate metric in the build log: how many planets get orbit, how many get period→orbit conversion, how many are dropped.\n\n### Out of scope\n\n- The null-radius case (already covered in bk5.3 — different problem, different fallback).\n- Eccentricity null — already handled (default to 0 / circular).\n\nLABELS: exoplanets, phase-11, design-decision","notes":"Created 2026-05-07 — flagged by Alex as an open decision the existing bk5 description did not address.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:50:24Z","created_by":"Alex Marshall","updated_at":"2026-05-07T08:56:51Z","labels":["phase-11"],"dependencies":[{"issue_id":"stellata-bk5.4","depends_on_id":"stellata-bk5","type":"parent-child","created_at":"2026-05-07T09:50:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-nmu","title":"Time scrubber: catalogue motion, hide policy, and control widget","description":"**Status:** Standalone, low priority. Deferred until the v1 solar-system layer (stellata-3re) and exoplanets (stellata-bk5) ship.\n\n**Goal:** build a user-facing time scrubber on top of the wall-clock `t` variable from stellata-3re.1. The v1 solar-system layer only displays `t = now` — no scrubbing. This epic covers the design + UX work to let the user move time forward and back.\n\nThe complexity is not the scrubber UI itself (small widget). It is the three open questions about what time means at scene scale:\n\n### Question 1: catalogue motion (proper motion + velocity)\n\nWhen `t` moves significantly, should ATHYG stars move along their proper-motion / radial-velocity vectors? The visual coherence question — at t = 1500 CE, do nearby stars sit where the scrubber would put them, or do they freeze at their J2000 positions?\n\nTo answer:\n- Audit what fraction of ATHYG carries proper-motion (`pmra`, `pmdec`) and radial-velocity (`rv`) data.\n- Audit what fraction of GCVS variables, Zucker molecular clouds, and Edenhofer dust have any time information.\n- Decide whether to (a) move only objects with velocity data, (b) move all stars and freeze everything else, (c) freeze everything outside the solar system.\n\n### Question 2: hide-vs-keep policy at extreme epochs\n\nOver centuries: probably fine to leave non-time-aware objects in place (clouds, dust, distant stars look the same).\nOver millions of years: the Milky Way has rotated, the local dust morphology has changed completely, the molecular clouds may not have existed. Showing them in place is misleading.\n\nWhere is the cutoff? Do we cap the scrubber range? Do we fade objects out as `|t - now|` grows past their reasonable lifetime? Do we show a banner (\"at this scale, only the solar system is correctly positioned\")?\n\n### Question 3: control widget design\n\nWhat does the scrubber look like? Open shape questions:\n- Continuous slider, or jump-to-date input, or both?\n- Forward/back nudges (±1d, ±1y, ±1000y keys)?\n- Auto-play (FF/RW with adjustable speed)?\n- What time format does the readout use — UTC/Zulu, ISO 8601, JD, plain English? (This decision is shared with stellata-3re.H and should land there first.)\n- Where on the screen does it live — `.ui-bottom` next to scale bar, or a dedicated affordance?\n\n### Files likely touched (when implementation begins)\n\n- New: `src/client/time-scrubber.ts`\n- Touch: `src/client/stellata.ts` (extend `t` setter to fan out to catalogue rendering when motion is enabled), `url-state.ts` (add `t=` URL param when scrubbed away from now), `index.html`/`styles.css`.\n\n### Out of scope\n\n- The `t` variable itself (lives in stellata-3re.1).\n- The \"live\" / \"now\" time readout when no scrubbing is happening (lives in stellata-3re.H).","notes":"Spun out of stellata-3re during a 2026-05-07 session: solar-system v1 only needed t=now display, not a scrubber. The deferred design questions deserve their own epic so future work can sequence the audits / decisions before implementation.\n\nChildren include the original 3re.2 (time-scrubber UI in .ui-bottom), reparented from stellata-3re.","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:48:20Z","created_by":"Alex Marshall","updated_at":"2026-05-07T08:48:20Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-57b","title":"Log zero-result search queries to surface catalogue gaps","description":"Capture and persist a record of search queries that return zero results, so we have signal for which alternate designations / catalogue entries are worth supporting.\n\nUser-stated category: defer. This is much more involved than the related catalogue-expansion work and should sit below it on the queue.\n\nWhy: today we have no telemetry on what users *try* to search for. If someone types 'V645' and gets nothing, we'll never know. A zero-result log would feed prioritisation of stellata-nxb (alternate designations) and any future catalogue additions.\n\nDesign considerations to think through before implementing:\n\n- **Privacy / scope.** Stellata is a static Cloudflare Workers site with no backend currently. Logging implies either (a) a small Worker endpoint with a KV/D1 store, or (b) a third-party analytics SDK. Either way, what we're logging is just-typed text — needs care around accidentally capturing PII (low risk, but think it through).\n- **Frequency / debounce.** The typeahead fires on every keystroke. Don't log per-keystroke. Options: log only on final blur or Enter; log only after N seconds of no further keystrokes; log only queries of length \u003e=4; sample. Probably some combination.\n- **Junk filtering.** Random keymashing, single characters, extremely long strings — exclude.\n- **Signal threshold.** Need traffic to make this useful. Stellata has very low traffic right now, so the practical value is months-out.\n\nTasks (when un-deferred):\n1. Decide infrastructure — Worker endpoint + Cloudflare D1/KV vs. minimal third-party analytics (Plausible, etc. — both already privacy-minded).\n2. Wire client-side debounce + filter logic into search.ts.\n3. Build a simple admin view (or just SQL queries) to surface frequent zero-result terms.\n4. Feed findings back into stellata-nxb.","status":"deferred","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-07T08:38:36Z","created_by":"Alex Marshall","updated_at":"2026-05-07T08:38:46Z","dependencies":[{"issue_id":"stellata-57b","depends_on_id":"stellata-nxb","type":"related","created_at":"2026-05-07T09:38:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.65","title":"typeahead-util scrollRowIntoView: offsetTop semantics depend on offsetParent","description":"scrollRowIntoView in typeahead-util.ts uses row.offsetTop, which is relative to the row's offsetParent — not necessarily the list element. If the dropdown's CSS ever changes the list's position/transform such that it stops being the offsetParent of its \u003cli\u003es, the scroll math would silently drift (rows would scroll to wrong offsets). The unit tests use a stub that hardcodes offsetTop, so they wouldn't catch this.\n\nFix options: (1) use row.offsetTop - list.offsetTop (works when list is also positioned), (2) use getBoundingClientRect deltas (more robust but layout-thrashes), (3) document the offsetParent assumption in the helper.\n\nSurfaced in the PR #30 review (epic 9mm). Nit-level — confirm via the existing manual smoke (arrow-nav through long search dropdown).","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-06T22:56:39Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:13Z","started_at":"2026-05-08T18:25:51Z","closed_at":"2026-05-08T18:54:13Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.65","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T23:56:38Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.64","title":"Stellata.attachDust: dispose previous DustField on re-attach","description":"Stellata.attachDust(d) stores this.dust = d so dispose() can release the ~128 MiB Data3DTexture. Today attachDust is called exactly once, but the contract isn't enforced — a second call with a different DustField would leak the previous one (no dispose).\n\nFix: in attachDust, if this.dust is non-null and not equal to the incoming dust, call this.dust.dispose() before reassigning. Or document the single-call contract explicitly and assert in non-prod builds.\n\nSurfaced in the PR #30 review (epic 9mm). Nit-level — no observable bug today.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-06T22:56:35Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:13Z","started_at":"2026-05-08T18:25:50Z","closed_at":"2026-05-08T18:54:13Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.64","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T23:56:34Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.63","title":"DRY: angularToPx (viewport.y / fovYRad) divide duplicated at 4 sites in stellata.ts","description":"Surfaced in PR #22 review of commit 32077d7 (stellata-a7d.3 — distance vector lands on cloud silhouette).\n\nThe pixel-per-radian conversion `viewport.y / Math.max(fovYRad, 1e-9)` is now computed inline at four sites in src/client/stellata.ts:\n\n1. `renderedSizePx` (line ~2550) — star disc diameter.\n2. `renderedCloudSizePx` (line ~2613) — cloud silhouette diameter (added by this commit).\n3. `renderedDiscPxAtPeak` (line ~2780) — variable-star peak disc, used by arrow-fade.\n4. (older) — verify with `grep -n 'viewport.y / Math.max(fovYRad'` for the canonical count.\n\nEach site reads the same two uniforms (`uViewport`, `uFovYRad`) and applies the same 1e-9 floor on fovYRad. A single `Stellata.angularToPx()` accessor would unify the lookup and the floor.\n\nSibling to:\n- stellata-9mm.52 (broader physical-size formula triplication — that bead's `physSizePx(R, d, viewport_y, fovYRad)` helper would consume an internal angularToPx, but the divide is also used from contexts where the full physSize formula isn't appropriate, so worth a separate accessor).\n- stellata-9mm.57 (1e-30 distance clamp duplicated at 5 sites — same DRY pattern).\n\nAcceptance:\n- New private/internal accessor `Stellata.angularToPx(): number` returns `viewport.y / Math.max(fovYRad, 1e-9)`.\n- All four call sites use the accessor.\n- No behaviour change; existing tests pass.","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T22:05:18Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:28Z","closed_at":"2026-05-06T22:35:28Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.63","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T23:05:17Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.62","title":"Prolate-cloud silhouette: largest-axis approximation overshoots end-on view","description":"Surfaced in PR #22 review of commit 32077d7 (stellata-a7d.3 — distance vector lands on cloud silhouette).\n\n`renderedCloudSizePx(cloud, dCamPc, angularToPx)` in src/client/molecular-clouds.ts approximates a cloud's projected silhouette by picking the largest semi-axis as the radius. This matches `cloudViewingDistancePc`'s convention and guarantees the chevron tip never lands inside the visible silhouette — but for strongly prolate clouds viewed end-on it can overshoot substantially.\n\nExample: cloud axes [10, 1, 1] viewed along the long axis. True silhouette is a circle of radius 1 pc; helper returns the diameter that 10 pc would project to — a 10× overshoot. The distance-vector chevron tip then floats noticeably off the rendered cloud edge.\n\nZucker 2021 catalog clouds are not all spheres; several are elongated enough for this to be visible at certain viewing angles.\n\nFix sketch: rotate the camera direction into cloud-local space using `cloud.quat`, then compute the silhouette ellipse semi-axes from the two axes perpendicular to that direction. Take the larger of those two as the silhouette radius (still conservative for non-axis-aligned views, but tight for axis-aligned ones). Pure math; no Three.js dependency in the helper.\n\nTest: extend molecular-clouds.test.ts with a prolate-end-on case asserting the silhouette matches the perpendicular axis, not the longitudinal one.\n\nAcceptance:\n- `renderedCloudSizePx` accepts a viewing direction (or camera-relative position) and produces silhouette diameter using the projected ellipse, not max axis.\n- Existing tests still pass (sphere → unchanged; max-axis aligned-view → unchanged).\n- New test covers prolate-end-on case.","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T22:05:08Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:28Z","closed_at":"2026-05-06T22:35:28Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.62","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T23:05:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.61","title":"Test gap: worldOffset URL round-trip only covers pc-scale anchors; no kpc/Mpc test guards Float32 cam/tgt precision claim","description":"Surfaced in PR #22 (stellata-a7d.2) review.\n\nsrc/client/url-state.test.ts:59 round-trips worldOffset with the pc-scale value `[51.6, 257, -37.7]`. The encoder's design comment at url-state.ts:135 explicitly justifies the float32 choice via \"Float32 ULP at megaparsec absolute scale is ~10⁻² pc — invisible in any view because the user-visible pose is the cam/tgt offset *within* the local frame, and that's encoded at full Float32 precision relative to the anchor.\"\n\nThat precision claim isn't tested. The current test asserts the worldOffset itself round-trips at 3 decimal places, but doesn't pin the *cam/tgt* fidelity at large worldOffset magnitudes — which is the actual operational concern (user warps to galactic-centre vicinity, cam/tgt are tiny local-frame numbers, share URL, recipient lands in the same pose).\n\nFix: add a test that:\n1. Sets worldOffset to a kpc-scale value (e.g. `[8500, 0, 0]` — galactic-centre direction) AND a Mpc-scale value (`[1e6, 0, 0]`).\n2. Encodes a small local-frame cam/tgt at sub-pc precision (e.g. `cam = [0, 0, 5e-7]`, `tgt = [0, 0, 0]` for a close-approach pose).\n3. Round-trips through encodeBlob → decodeBlob.\n4. Asserts the decoded cam/tgt match the input within float32 ULP relative to the worldOffset magnitude — i.e. the *combination* (cam + worldOffset reconstructed in absolute space) lands within ~10⁻² pc at Mpc anchor.\n\nThis guards the architectural claim that worldOffset + small cam is the right way to handle large-coordinate poses.\n","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:47:10Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:27Z","closed_at":"2026-05-06T22:35:27Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.61","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:47:10Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.59","title":"Audit camera-busy guard sites in stellata.ts: document the decision matrix (no behavioural change)","description":"Surfaced in PR #22 (stellata-a7d.2) review. Re-scoped from the original implementation bead (split 2026-05-10): this bead is the audit-only deliverable; the migration follows in a child bead.\n\n`Stellata` exposes two near-but-not-equivalent activity predicates:\n- `isObserveTransitionActive()` — observe enter/exit, excludes 'unfocus' kind.\n- `isCameraTransitionActive()` — warp || any observeTransition (incl. unfocus).\n- `isCameraBusy()` — warp || aim || isObserveTransitionActive (added in PR #30 batch 2).\n\nThe manual `if (this.warpState || this.aimState) return;` (and variants) appears in 6+ places in src/client/stellata.ts:\n- 1075, 1079 (setCameraMode — already migrated cleanly in PR #30 batch 2)\n- 1267-ish (setFocus's observe-cleanup branch — checks via cameraMode)\n- 1880, 1977 (focusStar / startWarp)\n- 2267 (aimAt)\n- 2774 (onPointerUp)\n\nEach site has its own subset of (warp, aim, observeTransition, cameraMode==='observe', focusedStar !== null) checks, with no consistent ordering and no shared explanation.\n\n# Scope (audit-only)\n\nOutput is a comment-only PR. No code-path changes:\n\n1. Comment block above the `isCameraBusy()` definition documenting the decision matrix: which combinations of (warp, aim, observeTransition, cameraMode==='observe', focusedStar !== null) gate which classes of UI action.\n2. At each of the 6+ sites, an inline annotation flagging the predicate as either:\n   - `// equivalent: isCameraBusy()` — strict-equivalent, queued for migration\n   - `// narrower: \u003creason\u003e` — deliberately divergent, with the reason captured (e.g. 'cosmetic cloud picking suppressed during warp only — aim-while-pointing should still update the cursor cloud').\n3. The site list at the top of this bead's description converted into a structured table inside the comment block (or a section in docs/architecture.md if it's better placed there).\n\nThe output of this bead is read by the implementation child to drive the migration without re-litigating each site.\n\n# Smoke\n\n- typecheck + vitest pass (no behavioural change, so neither should drift).\n- Manual diff: confirm only comment lines changed; no expression / control-flow edits.","notes":"Re-opened 2026-05-06 after partial completion in PR #30 batch 2. The isCameraBusy() helper and documented decision matrix were added on Stellata; one site (setCameraMode lines 1124-1128) was migrated cleanly (the pre-migration check was provably equivalent to isCameraBusy). The other 5+ sites remain on their original narrower predicates because each picks its own subset deliberately (e.g. cosmetic cloud picking is suppressed during warp only, not during aim) — those still need a per-site semantic audit before migration. Continue from here when the audit is scheduled.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:46:32Z","created_by":"Alex Marshall","updated_at":"2026-05-10T09:45:30Z","dependencies":[{"issue_id":"stellata-9mm.59","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-15T12:54:06Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.57","title":"DRY: 1e-30 distance clamp magic number repeated in 5 sites within stellata.ts","description":"Surfaced in PR #22 (stellata-a7d.2) review.\n\nThe literal `1e-30` is used as a close-distance floor in five sites in src/client/stellata.ts:\n- line 2221 (aimAtConstellation, projection guard)\n- line 2447 (pickStar, dCam guard for log10)\n- line 2545 (renderedSizePx, dCam guard)\n- line 2753 (renderedDiscPxAtPeak, dCam guard)\n- line 3003 (shouldEnableCoreMask, tan(halfAngle) guard)\n\nThe comment at line 2543 explains the choice: \"keeps log10(dCam) finite without clamping atan(R/d) at close approach.\" That reasoning is invisible at the other four sites.\n\nFix: name it once at the top of stellata.ts, e.g. `const DCAM_LOG_FLOOR_PC = 1e-30;` with the explanatory comment. Replace the five literals.\n\nThe unrelated `1e-9` floor used as the physicalRadius minimum in minOrbitDistForStar/minDistForStar (lines 2701, 2724) is a different magic number with different rationale — leave separate, but consider a parallel `MIN_PHYSICAL_RADIUS_R_SUN` if that ever gets a third site.\n","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:46:08Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:26Z","closed_at":"2026-05-06T22:35:26Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.57","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:46:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.56","title":"DRY: binary-companion floor block duplicated between minOrbitDistForStar and minDistForStar","description":"Surfaced in PR #22 (stellata-a7d.2) review.\n\n`Stellata.minOrbitDistForStar` (src/client/stellata.ts:2699-2712) and `Stellata.minDistForStar` (src/client/stellata.ts:2722-2737) end with an identical companion-floor block:\n\n```\nconst comp = this.catalog.companion[idx];\nif (comp \u003c 0) return base;\nconst p = this.catalog.positions;\nconst dx = p[comp * 3] - p[idx * 3];\nconst dy = p[comp * 3 + 1] - p[idx * 3 + 1];\nconst dz = p[comp * 3 + 2] - p[idx * 3 + 2];\nconst sep = Math.sqrt(dx * dx + dy * dy + dz * dz);\nreturn Math.max(\u003cbase value\u003e, sep * BINARY_MIN_DIST_FACTOR);\n```\n\nTrivial extraction:\n```\nprivate binaryFloorPc(idx: number): number {\n  const comp = this.catalog.companion[idx];\n  if (comp \u003c 0) return 0;\n  ...sep...\n  return sep * BINARY_MIN_DIST_FACTOR;\n}\n```\nThen both callers become `return Math.max(base, this.binaryFloorPc(idx));`.\n\nBonus: makes it cheaper to add a third caller (e.g. observe-mode park if that ever differentiates) without re-typing the block.\n","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:45:57Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:26Z","closed_at":"2026-05-06T22:35:26Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.56","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:45:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.55","title":"DRY: pin-engagement guard (target.lengthSq() \u003c 1e-12) duplicated across isPinEngaged, animate, and pin-debug-hud","description":"Surfaced in PR #22 (stellata-a7d.2) review.\n\nThe focused-star pin guard `controls.target.lengthSq() \u003c 1e-12` is reproduced in three places:\n\n1. `Stellata.isPinEngaged()` — src/client/stellata.ts:956 (canonical)\n2. `Stellata.animate()` — src/client/stellata.ts:3089 (per-frame, picks uPinFocusToCenter value)\n3. `pin-debug-hud.ts:102` — `const guard = t.lengthSq() \u003c 1e-12;`\n\nThe threshold (`1e-12`) is the same magic number in all three; the surrounding focus / mode / animation predicates are also duplicated between (1) and (2).\n\nWorse: pin-debug-hud already calls `stellata.isPinEngaged()` on line 103 to populate `pinNow`. The `guard` variable on line 102 is then a redundant standalone calc that just exists to display the predicate result. Trivially removable.\n\nFix:\n- Have `animate()` call `isPinEngaged()` instead of re-implementing the predicate.\n- Drop the `guard` local in pin-debug-hud.ts; display `pinNow` (and optionally surface the threshold constant) without re-deriving the bool.\n\nStretch: extract the threshold itself to a named constant `PIN_ENGAGE_THRESHOLD_SQ_PC = 1e-12` so the magic 1e-12 isn't a literal at three sites. Comment-rot risk if the threshold ever moves.\n","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:45:43Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:25Z","closed_at":"2026-05-06T22:35:25Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.55","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:45:43Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.54","title":"DRY: SHAFT_START=28 hardcoded in computeDiscCoverageAlpha shadows HudOverlay.getShaftStartPx()","description":"Surfaced in PR #22 (stellata-a7d.2) review.\n\n`Stellata.computeDiscCoverageAlpha` at src/client/stellata.ts:2656 declares:\n```\nconst SHAFT_START = 28;  // FOCUS_RING_RADIUS_PX + RING_HALO_GAP_PX\n```\nduplicating the live value already exposed by `HudOverlay.getShaftStartPx()` (src/client/hud-overlay.ts:243) and reconstructed independently as `FOCUS_RING_RADIUS_PX (24) + RING_HALO_GAP_PX (4)` in computeShaftStartRadius (hud-overlay.ts:441). `distance-vector-overlay.ts:14` also uses `SOURCE_OFFSET_PX = 28` for the same geometric reason.\n\nThe arrow-fade debug HUD already consumes the right getter (`stellata.hud.getShaftStartPx()` at arrow-fade-debug-hud.ts:81). `computeDiscCoverageAlpha` should do the same.\n\nFix: replace the local `const SHAFT_START = 28` with `this.hudOverlay.getShaftStartPx()` so the alpha calc tracks whatever shaft start the HUD actually used last frame (which is mode-aware — focus ring rim in nav, HUD ring rim in observe).\n\nCaveat: `getShaftStartPx()` is 0 immediately after construction before the first `update()` runs. `computeDiscCoverageAlpha` is gated to navigate mode and a focused star, so by the time it runs HudOverlay has already updated at least once. Worth a defensive fallback to `FOCUS_RING_RADIUS_PX + RING_HALO_GAP_PX` if 0 — but ideally export those two constants from hud-overlay so they're the single source.\n\n`distance-vector-overlay.SOURCE_OFFSET_PX` should also share the same constant since it has the same geometric meaning (start the shaft past the focus-ring rim).\n","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:45:35Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:25Z","closed_at":"2026-05-06T22:35:25Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.54","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:45:34Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.53","title":"DRY: ZOOM_FLOOR_FRACTION (0.9) and VAR_TROUGH_FLOOR_FRACTION (0.2) duplicated as bare numbers in star.vert.glsl","description":"Surfaced in PR #22 (stellata-a7d.2) review.\n\nTwo close-approach fraction constants are defined as named consts in src/client/stellata.ts and then re-typed as bare numeric literals in src/client/shaders/star.vert.glsl:\n\n| Name                            | TS site                | GLSL site                  |\n|---------------------------------|------------------------|----------------------------|\n| `ZOOM_FLOOR_FRACTION = 0.9`     | stellata.ts:189        | star.vert.glsl:233 (`0.9 * min(uViewport.x, uViewport.y)`) |\n| `VAR_TROUGH_FLOOR_FRACTION = 0.2` | stellata.ts:2568      | star.vert.glsl:235 (`const float VAR_TROUGH_FLOOR_FRACTION = 0.2;`) |\n\nThe TS comment at line 188 explicitly says: \"Both the vertex shader (variability headroom) and minOrbitDistForStar use this constant; keep them in sync.\" That's the canonical smell — should be a uniform.\n\nFix: pass either a single precomputed `uMaxPhysSizePx` uniform (= `ZOOM_FLOOR_FRACTION * min(viewport.x, viewport.y)`, recomputed on resize) or two scalar uniforms `uMaxPhysFrac` / `uVarTroughFrac`. Either way the shader reads from the uniform instead of a bare literal, and the TS const becomes the single source of truth.\n\n`renderedSizePx` already consumes `ZOOM_FLOOR_FRACTION` directly (stellata.ts:2553), so it would just stay using the const. The shader is the only consumer that loses the literal.\n","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:45:24Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:25Z","closed_at":"2026-05-06T22:35:25Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.53","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:45:24Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.52","title":"DRY: physical-size formula triplicated across TS + GLSL (renderedSizePx, renderedDiscPxAtPeak, star.vert.glsl)","description":"Surfaced in PR #22 (stellata-a7d.2) review.\n\nThe angular-diameter-to-pixels formula `2·atan(R·radiusFactor / dCam) · viewport.y / fovYRad` now lives in three places:\n\n1. `renderedSizePx` in src/client/stellata.ts:2534-2592 (full app/phys blend with variability headroom).\n2. `renderedDiscPxAtPeak` in src/client/stellata.ts:2745-2762 (physSize-only with peak amplitude factor — used by arrow-fade only).\n3. `star.vert.glsl` line 332 (the actual shader physSize, plus the headroom calc at lines 228-243).\n\nThe variability headroom block (`maxUpLog10` / `maxDownLog10` / `ampLimitMag` / `ampEff`) is reproduced word-for-word between TS:2569-2576 and GLSL:236-243.\n\nRisk: any future tweak to the size math (e.g. switching to a smoothstep blend at the pxSize boundary, or revisiting the trough floor) has to be applied in three locations, with no single source of truth and no test guarding the equivalence.\n\nFix: extract a single `physSizePx(R_pc, dCam_pc, viewport_y, fovYRad)` pure function in TS and a parallel GLSL routine inside star.vert.glsl. Have `renderedSizePx` and `renderedDiscPxAtPeak` both call it. Same for the variability headroom — one helper, two call sites.\n\nOut of scope for this bead: TS↔GLSL sharing of the actual function body (would require a build-time include or a constants module emitting GLSL). The two `physSizePx` definitions can stay separate, but with paired comments + a regression test asserting they produce the same numeric output for representative inputs (R=Sol, R=Betelgeuse at typical d/FOV).\n","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-06T21:45:16Z","created_by":"Alex Marshall","updated_at":"2026-05-06T22:35:24Z","closed_at":"2026-05-06T22:35:24Z","close_reason":"Addressed in PR #30 batch 2 (9mm.51 surgery follow-up). 9mm.59 helper added + one-site migration; remaining sites left as documented per the bead's audit-first warning.","dependencies":[{"issue_id":"stellata-9mm.52","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:45:16Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-zsr","title":"(EPIC) Visual refinements — perceptual polish on top of the physical model","description":"Aesthetic / perceptual polish that sits on top of the a7d physical-scale rewrite. Each child is a refinement, not a fix — visual quality wins that aren't on the critical path for shipping the physical model itself.\n\nInitial children (moved out of a7d.2 once that epic finished its core scope):\n\n- stellata-a7d.2.9 — Cinematic flyby of source star with arc/clothoid fillet (smooth warp transition).\n- stellata-a7d.2.10 — Spectral/temperature correction to appSize (V-band → human-eye perceived brightness via BC_photopic).\n\nBoth are P3: small visual deltas, no critical-path dependency. Add new perceptual / aesthetic refinements here as they surface (e.g. limb darkening, atmospheric dispersion at the disc edge, etc.).","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-06T19:34:02Z","created_by":"Alex Marshall","updated_at":"2026-05-06T19:34:02Z","dependencies":[{"issue_id":"stellata-zsr","depends_on_id":"stellata-8hu","type":"related","created_at":"2026-05-08T19:09:03Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.50","title":"docs/rendering.md doesn't describe the depth-encoding pipeline (logarithmic depth + per-pass overrides)","description":"docs/rendering.md is the canonical reference for the star pipeline — three passes, super-Gaussian profile, depth-mask vs disc vs glow, manual gl_FragDepth writes. After PR #7 it has nothing to say about how depth is actually encoded:\n\n- The `logarithmicDepthBuffer: true` setting and its prerequisite role for stellata-a7d.2 are not mentioned.\n- The interaction between manual `gl_FragDepth` writes and the `\u003clogdepthbuf_fragment\u003e` chunk in star.frag.glsl (the default-then-overwrite-then-conditional-halo pattern at lines 81/82/155) isn't documented.\n- The fact that all four custom shader pairs (star/cloud/dust-particle/milkyway) require log-depth chunks isn't called out, so a future shader added without them would silently mis-sort against the rest of the scene.\n\nWhy it matters: rendering.md is in the always-loaded CLAUDE.md doc index — future Claude sessions read it before touching shaders. Without mention of the log-depth pipeline, a tweak to `gl_FragDepth` writes (e.g. for the future per-star physical-size override, or the molecular-cloud physical-shading work) could land without realising the chunk system exists.\n\nSuggested direction: short subsection under existing 'Three passes' coverage — half a page — that documents (a) log-depth is on and why, (b) every custom ShaderMaterial needs the four `\u003clogdepthbuf_*\u003e` chunks, (c) manual `gl_FragDepth` writes must be threaded around the chunk include with the existing star.frag.glsl pattern (or use `= 1.0` for far-plane signalling, which is encoding-independent).","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:15:26Z","created_by":"Alex Marshall","updated_at":"2026-05-08T11:13:14Z","started_at":"2026-05-06T06:27:18Z","closed_at":"2026-05-06T23:06:46Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.50","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:15:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.49","title":"No precision regression test for log-depth ordering across the new dynamic range","description":"PR #7 expanded the camera frustum from `(near=1e-3, far=2e5)` to `(near=1e-10, far=2e5)` — a dynamic range of 15 orders of magnitude — and is relying on `logarithmicDepthBuffer: true` to keep front-back ordering correct across that range. There is no automated test that exercises this.\n\n`npm test` (244 tests) has nothing depth-related: search.test, controls.test, distance-util.test, etc. all run pure JS. The PR's verification was visual ('default view renders identically to main') which catches gross regressions but won't catch precision drift at the extremes — a star at d ≈ 5e-8 pc (the a7d.2 target d_min) sorting incorrectly against a 200 kpc galactic-disc fragment, for example.\n\nWhy it matters: this is a prerequisite PR for stellata-a7d.2, where d_min drops to ~5e-8 pc. The whole point of enabling log-depth was to keep z-fighting away from the close-range disc rendering. A future change that re-disables log-depth, accidentally drops one of the eight chunk includes, or restores a per-shader `gl_FragDepth = gl_FragCoord.z` write would silently regress that — and no CI signal would catch it. Visual verification in dev mode is the only line of defence.\n\nSuggested direction: a vitest test using a headless WebGL2 context (or three.js's existing test harness) that:\n1. Renders two unit quads at known distances spanning [1e-8, 2e5] pc.\n2. Reads back depth via a render target with `THREE.DepthTexture`.\n3. Asserts pairwise depth ordering across at least 4-5 distance buckets.\n\nIf a full GL test is too heavy, a JS-side approximation: assert that for the chosen near/far/log-depth-buffer settings, the float32 `gl_FragCoord.z` produced by the matrix chain is monotonic across a sampled distance grid. Cheaper but only catches CPU-side regressions (e.g. someone resetting near to 1e-3).","notes":"Deferred from PR #30 (2026-05-06 batch). Log-depth precision is a GPU concern — verifying it requires either rendering across the actual dynamic range (1e-10 to 3e4 pc) and reading depth-buffer samples, or a unit test of the log-depth chunk's varying math. The chunk is three.js stock code so it's not the unit being tested; what's worth pinning is end-to-end z-order at extreme distances. Pick up alongside 9mm.44 since both want a real WebGL2 renderer in tests. The depth-encoding section added to docs/rendering.md (this batch) documents the invariant in prose.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:15:10Z","created_by":"Alex Marshall","updated_at":"2026-05-10T09:44:48Z","dependencies":[{"issue_id":"stellata-9mm.49","depends_on_id":"stellata-9mm.192","type":"parent-child","created_at":"2026-05-15T12:54:11Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.47","title":"Dead-code default 'gl_FragDepth = gl_FragCoord.z' in star.frag.glsl after log-depth chunk added","description":"src/client/shaders/star.frag.glsl line 81 sets `gl_FragDepth = gl_FragCoord.z;` and the very next line includes `\u003clogdepthbuf_fragment\u003e`, which under `logarithmicDepthBuffer: true` (now hardcoded in stellata.ts:473) always overwrites that value with the log-encoded depth from `vFragDepth`. The `gl_FragCoord.z` default only matters when log-depth is OFF — a state the renderer can never enter because the option isn't user-facing.\n\nThe PR added a comment (lines 79-80) explaining the chunk overrides this — which is honest but documents that the line doesn't do anything useful. The halo override at line 155 (`if (glow \u003c uCoreThreshold) gl_FragDepth = 1.0;`) is the only non-redundant `gl_FragDepth` write remaining, and it's correct because 1.0 is the far plane in any depth encoding (already noted in PR description).\n\nWhy it matters: low — pure cleanup. No correctness issue. Worth noting because every reader of the shader has to mentally trace why a write-then-overwrite pattern is intentional.\n\nSuggested direction: delete the line and the now-unnecessary 'logdepthbuf chunk overrides' explanation comment, rely solely on the `\u003clogdepthbuf_fragment\u003e` chunk for the default depth write, and keep the halo `gl_FragDepth = 1.0` override. Optionally collapse the comment block to a one-liner about the halo override.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:14:31Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:49Z","started_at":"2026-05-05T22:51:51Z","closed_at":"2026-05-06T23:06:49Z","close_reason":"Closed as 'dead-code removed' in PR #30; PR #33 restores the line as a defensive default after the GLSL-spec analysis (undefined depth in any path that doesn't write gl_FragDepth when another path does). Net result: removal then restore with documented rationale.","dependencies":[{"issue_id":"stellata-9mm.47","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:14:31Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.46","title":"Overlay near-plane clip threshold piggybacks on camera.near — semantically overloaded after log-depth switch","description":"Seven overlays read `camera.near` directly as the 'behind-camera / on-near-plane' clip threshold for screen-space projection:\n\n- src/client/distance-vector-overlay.ts:163 — `const threshold = -camera.near;` (used twice for source + destination clipping)\n- src/client/focus-ring-overlay.ts:66 — `if (v.z \u003e -camera.near) ...`\n- src/client/poi-overlay.ts:405 — `if (v.z \u003e= -camera.near) return null;`\n- src/client/disc-mask.ts:53 — `if (v.z \u003e -camera.near) return false;`\n- src/client/hud-overlay.ts:412 — `if (v.z \u003e= -camera.near) return null;`\n- src/client/constellation-overlay.ts:58 — `if (v.z \u003e -camera.near) return null;`\n- src/client/chart-labels.ts:748 — `if (projVec.z \u003e= -camera.near) return null;`\n\nPR #7 dropped `camera.near` from `0.001` pc (~3 mm view-space) to `1e-10` pc to give logarithmic depth buffer headroom for stellata-a7d.2's per-star physical sizing. That collapses the overlay clip threshold to effectively zero — overlays now keep rendering for objects in view-space `z ∈ (-1e-10, 0)`, which previously got clipped at `-0.001`. `camera.near` is now serving two unrelated purposes: GPU log-depth precision floor, and CPU overlay near-plane clipping. The two concepts no longer share a sensible value.\n\nWhy it matters: at the current orbit floor (DEFAULT_MIN_DIST_PC = 5e-3 pc) practical behavior is unchanged because the focused star's view-z (~-5e-3) clears both thresholds. But the implicit coupling is fragile — distance-vector-overlay.ts:178 also sets `endView.z = threshold - 1e-4` to nudge the clipped vertex behind the near plane; with threshold ≈ 0 that nudge becomes `z ≈ -1e-4` (in view-space pc), which differs from the prior `z ≈ -1.001e-3` by a factor of 10.\n\nSuggested direction: introduce a named constant — e.g. `OVERLAY_NEAR_CLIP_PC = 1e-3` — exported from a shared module, and replace the seven `camera.near` reads with it. Decouples overlay clipping from log-depth precision. The PR description should ideally have called this out as a touched-but-not-tested interaction.","notes":"Reopened 2026-05-06 after PR #30 rebase onto PR #22. PR #22 changes the orbit floor and overlays still read camera.near. The OVERLAY_NEAR_CLIP_PC decoupling may no longer be needed; re-evaluate the threshold against the new orbit floor and float32 cancellation regime.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:14:17Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:13Z","started_at":"2026-05-05T22:51:50Z","closed_at":"2026-05-08T18:54:13Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.46","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:14:17Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.45","title":"Stale doc: camera-modes.md still cites camera.near = 0.001","description":"`docs/camera-modes.md` lines 11-12 still say `camera.near = 0.001`, but PR #7 (stellata-a7d.1) dropped the value to `1e-10` (stellata.ts:487). The 'strictly less than controls.minDistance' invariant still holds, but the cited value is wrong, and the doc never mentions that the new value is only safe because `logarithmicDepthBuffer: true` was simultaneously enabled on the WebGLRenderer (stellata.ts:473). A future maintainer reading the doc would conclude the near plane is at perceptual scale (1 mm), not at log-depth precision floor.\n\nWhy it matters: docs/camera-modes.md is in the always-loaded index in CLAUDE.md as the canonical reference for the near-plane / minDistance invariant. Out-of-date numbers there mislead future Claude sessions and humans alike.\n\nSuggested direction: update the cited value to `1e-10`, add one sentence noting the log-depth buffer is what allows the aggressive near plane (it's the prerequisite for the per-star physical-size rewrite in a7d.2). Same paragraph could also reference docs/rendering.md once that gains a log-depth section (sibling finding).","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:13:58Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:45Z","started_at":"2026-05-06T06:27:18Z","closed_at":"2026-05-06T23:06:45Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.45","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:13:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.44","title":"Add a smoke test for Stellata.dispose() — instantiate, dispose, instantiate again, no errors","description":"The c2l audit landed without any test coverage at the dispose() entrypoint, so the next time a new layer is added (and somebody forgets to plug it into dispose()) the regression has to be caught by manual review again — same path that left coreMaskMaterial / particleMesh / clouds / galactic{Disc,Grid} unreleased before this PR.\n\nLeak detection per se is hard to unit-test (no easy way to assert a Three.js Material was disposed without monkey-patching), but the cheap, useful guard is a smoke test:\n\n1. Instantiate Stellata with a tiny synthetic catalog (the test suite already builds these — see catalog-loader.test.ts).\n2. Call dispose().\n3. Instantiate a second Stellata against the same canvas / catalog.\n4. Call dispose() again.\n5. Assert no throws and (optionally) that disposed === true.\n\nA tighter version: spy on each owned resource's dispose() via vi.spyOn before teardown, assert each was called exactly once. That catches future regressions where a new layer ships without dispose() integration.\n\nDirection: new src/client/stellata.dispose.test.ts. Reuse the in-memory catalog fixture from catalog-loader.test.ts. Skip if WebGL isn't available in the vitest environment (jsdom may need a stub renderer).","notes":"Deferred from PR #30 (2026-05-06 batch). Stellata's constructor instantiates THREE.WebGLRenderer which needs a real WebGL2 context. Running this in vitest requires either (a) headless-gl or a similar GL polyfill + jsdom, or (b) a Stellata test seam that lets a lightweight fake renderer flow through. Both are bigger than a unit test. Note that PR #30 already verified each owner's dispose() in isolation: DustField.dispose (9mm.38), HudOverlay.dispose (9mm.42), molecular-clouds.test.ts construction, MolecularClouds smoke. The remaining gap is the *integration* — does Stellata.dispose() walk all owners. Pick up when adding the first integration-test infrastructure.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:12:55Z","created_by":"Alex Marshall","updated_at":"2026-05-10T09:44:47Z","dependencies":[{"issue_id":"stellata-9mm.44","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:25Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.43","title":"Extract StarPipeline class for the disc/glow/coreMask trio (resource ownership + dispose)","description":"Stellata.dispose() (src/client/stellata.ts) is currently a flat list mixing two patterns:\n\n(a) High-level subsystem dispose calls — controls.dispose(), clouds.dispose(), galacticDisc.dispose(), galacticGrid.dispose(), milkyway.dispose(), renderer.dispose().\n\n(b) Hand-rolled cross-resource cleanup for the star pipeline — geometry.dispose(), material.dispose(), glowMaterial.dispose(), coreMaskMaterial.dispose() — plus a comment explaining the 'one geometry, three materials' invariant to future readers. That comment is the smell: the explanation belongs inside a class whose dispose() encapsulates it.\n\nThe same architectural fit applies in the rest of the file — MolecularClouds, MilkyWay, GalacticDisc, GalacticGrid each own their resources behind a dispose() contract. The star pipeline (the densest resource cluster in the app) is the one that doesn't.\n\n# Scope (re-scoped 2026-05-10)\n\nResource ownership + dispose only. Per-frame uniform-write migration is **out of scope** — Stellata can keep mutating `this.starPipeline.discMaterial.uniforms.X.value` from outside. The encapsulation win here is the dispose contract + the resource-ownership invariant, not access discipline. If the leaky-access feeling becomes acute after this lands, file a follow-up at that point with concrete examples.\n\nDustParticleLayer extraction is filed as a **separate sibling bead** (independent — different code, different concerns, dust layer is shelved per CLAUDE.md).\n\nDirection:\n- New `src/client/star-pipeline.ts` class owning `{ geometry, discMaterial, glowMaterial, coreMaskMaterial, discMesh, glowMesh, coreMaskMesh }`.\n- Constructor takes the inputs Stellata currently builds (uniforms map, vertex/fragment shader sources, scene reference, render-order constants) and produces an attached, ready-to-render pipeline.\n- `dispose()` walks all owned resources in the right order (the chart-mode blend swap on `glowMaterial` is just material mutation — nothing extra to dispose).\n- Stellata holds it as one field (`private starPipeline: StarPipeline`); the existing fields (discMesh, glowMesh, coreMaskMesh, geometry, material, glowMaterial, coreMaskMaterial) are removed in favour of accessor properties on StarPipeline OR direct `this.starPipeline.X` reads at the call sites.\n- `Stellata.dispose()` collapses the star-pipeline block to a single `this.starPipeline.dispose()` call, with the invariant comment moving inside the class.\n\n# Smoke\n\nVisual diff against main: open the app, focus a few stars (Sirius, Polaris, α Cen), toggle chart mode, confirm rendering identical at all magnitude / FOV settings. Particle layer (off by default) is unaffected since it lives separately.\n\n- typecheck + vitest pass.\n- Browser smoke against named stars to confirm disc/glow/coreMask passes still composite identically.","notes":"Deferred from PR #30 (2026-05-06 batch). Wrapping the star pipeline (geometry + disc + glow + core-mask + particle materials/meshes + their dust uniforms) in a StarPipeline subsystem is a substantial restructuring of stellata.ts. PR #30 made each of those resources individually disposable (9mm.35 disposeParticles helper, 9mm.38 DustField.dispose, 9mm.1 applyDiscBlendDefaults helper) which establishes most of the seams a StarPipeline would expose, but the encapsulation itself was scoped out. Worth doing alongside 9mm.44 (dispose smoke test) since the subsystem is what would make that test cleaner.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:12:42Z","created_by":"Alex Marshall","updated_at":"2026-05-17T16:09:18Z","started_at":"2026-05-17T15:57:50Z","closed_at":"2026-05-17T16:09:18Z","close_reason":"Extracted StarPipeline class for the disc/glow/coreMask trio in PR #84 — completes the integration-shell decomposition epic (9mm.194 now 11/11).","dependencies":[{"issue_id":"stellata-9mm.43","depends_on_id":"stellata-9mm.194","type":"parent-child","created_at":"2026-05-16T17:42:39Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.43","depends_on_id":"stellata-9mm.194.1","type":"blocks","created_at":"2026-05-16T17:43:44Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.41","title":"url-state.ts has duplicate import lines from ./distance-util (stellata-6oy)","description":"url-state.ts:3-4 — PR #23 added `import { setUnit } from './distance-util';` on line 3, leaving the existing `import { getUnit, onUnitChange } from './distance-util';` on line 4. Two separate import statements from the same module on adjacent lines.\n\nTrivial style nit, but it's the kind of thing eslint/prettier-organize-imports would catch automatically. Either consolidate to `import { setUnit, getUnit, onUnitChange } from './distance-util';` or run an import-organiser pass over the file.\n\nSuggested direction: collapse to one line; consider adding a lint rule (eslint-plugin-import's no-duplicates) so the next merge can't re-introduce the split.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:12:16Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:44Z","started_at":"2026-05-05T22:51:50Z","closed_at":"2026-05-06T23:06:44Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.41","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:12:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.37","title":"inferBinaries returns redundant primaries field equal to mutualPairs by construction","description":"`inferBinaries` returns `{ pairs, mutualPairs, primaries }` (catalog-pure.ts:244-246), but the implementation guarantees `primaries === mutualPairs` by construction (line 315: `return { pairs, mutualPairs, primaries: mutualPairs }`). The build-script log line at build-catalog.ts:678 prints both as if they were independent stats, which is misleading:\n\n  `28 companion assignments, 14 mutual pairs (14 primaries) in Xms`\n\nThe parens-aside makes \"primaries\" look like a separately-derived statistic when it's a tautology with mutualPairs.\n\nDirection: drop the `primaries` field from the return type (it's literally `mutualPairs`) and update the log line to either omit the redundant count or rename it to make the equivalence obvious. Pre-PR the field made sense because primaries was tracked via a Set; after the PR's mutual-only restructure it isn't. Minor cleanup.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:56Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:43Z","started_at":"2026-05-05T22:53:22Z","closed_at":"2026-05-06T23:06:43Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.37","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.36","title":"dust-loader particle vs chunk size-mismatch handling is inconsistent (stellata-adr)","description":"PR #23 added file-size validation for particles.bin in loadDustParticles (dust-loader.ts:80-86). It console.warn()s and returns null on mismatch.\n\nBut the existing chunk path at dust-loader.ts:231-233 throws an Error on the same kind of failure (`if (buf.byteLength !== chunk.bytes) throw new Error(...)`). Same root cause (file-size invariant violated → can't safely parse), opposite reaction.\n\nWhy it matters: code review can't tell at a glance whether a corrupt voxel file should fail-fast or warn-and-skip. The two paths diverged because they were touched by different fixes; the codebase now has two conventions for 'bad file size' depending on which file. The chunk's caller catches the throw and continues with other chunks (line 216-220), so the user-visible behaviour is similar — but mechanism differs.\n\nAlso: catalog-loader.ts:94-97 throws on bad magic / version; dust-loader.ts:64-71 console.warn()s on bad magic / version and returns null. Same convention split.\n\nSuggested direction: Pick one. Either (a) loadDustParticles throws like fetchAndUpload does and the caller (main.ts:113-115) traps; or (b) fetchAndUpload returns null on size mismatch like loadDustParticles does. Whichever, document it in a one-line comment so future code-quality gates aren't a coin flip.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:46Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:43Z","started_at":"2026-05-06T06:24:07Z","closed_at":"2026-05-06T23:06:43Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.36","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.35","title":"Dispose particleMesh via a single helper instead of duplicating cleanup in attachDustParticles + dispose()","description":"PR #14 added particle-mesh disposal to Stellata.dispose() (src/client/stellata.ts:2931-2934) verbatim alongside the identical block already inside attachDustParticles (src/client/stellata.ts:1247-1251).\n\nBoth copies do:\n  this.particleMesh.geometry.dispose();\n  this.particleMaterial?.dispose();\n\n(The attachDustParticles copy also scene.remove()s the mesh first.) Two parallel cleanup paths for the same owned resource is the classic DRY trap the audit was meant to surface.\n\nWhy it matters: when the particle layer eventually gains a third resource (texture atlas, secondary buffer, etc.), the change has to be made in two places or the dispose path silently leaks again — exactly the failure mode this PR was fixing.\n\nDirection: extract a private disposeParticles() that handles geometry+material (and conditionally scene.remove()), and call it from both sites. Same pattern as the controls.dispose() / milkyway.dispose() encapsulation already in use.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:44Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:42Z","started_at":"2026-05-05T22:53:22Z","closed_at":"2026-05-06T23:06:42Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.35","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:44Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.32","title":"Replace synthetic OVERRIDE-N CCDM keys with a typed group source","description":"In `parseHipCcdm` (build-catalog.ts:316-318), curated visual doubles are stuffed into the same `Map\u003cstring, number[]\u003e` as real CCDM systems by minting synthetic keys like `OVERRIDE-0`, `OVERRIDE-1`. This is a type-system smell — sentinel-string keys in a typed map for what's really a discriminated union.\n\nIssues:\n- Compile-time the override keys are indistinguishable from real CCDM IDs. If the upstream CCDM file ever contains a literal `OVERRIDE-0` (theoretical but not impossible), the override silently merges with the file entry.\n- The `scanned` / `viaOverride` / `kept` log accounting in `parseHipCcdm` is awkward partly because the override path injects entries before the file scan and then is filtered out during the scan.\n- Reads less elegantly than just keeping override systems in their own list.\n\nDirection: either (a) tag groups with an explicit source: `type CcdmGroup = { source: 'ccdm', ccdmId: string, hips: number[] } | { source: 'override', hips: number[], reason: string }` and return `CcdmGroup[]` (or two parallel arrays), or (b) keep `parseHipCcdm` returning only file-derived groups and have `applyDoublesFlag` consume both the parser output and `KNOWN_VISUAL_DOUBLES` directly, unioning at flag time.\n\nEither way drops the magic prefix and makes the override path self-evident.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:29Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:42Z","started_at":"2026-05-06T06:27:17Z","closed_at":"2026-05-06T23:06:42Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.32","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:29Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.31","title":"Add : void return types to applyTheme / registerThemeStellata for parity with bindChartMode","description":"PR #8 added `: void` to `bindChartMode` in chart-mode.ts:33 but theme-toggle.ts (touched in the same PR) still has two zero-return public exports without explicit annotations:\n\n- src/client/theme-toggle.ts:14 — `export function registerThemeStellata(stellata: Stellata) {`\n- src/client/theme-toggle.ts:18 — `export function applyTheme(mode: ThemeMode) {`\n\nBoth clearly return nothing. Since PR #8 touched this file and applied the void-return convention to one bind*-style export, the same convention belongs on these two as a consistency sweep within the file the PR already opened.\n\nWhy it matters: small DRY/consistency miss inside a file actively edited by the cleanup PR — exactly the gap the trivial-cleanup PR was meant to close. Note the wider codebase (~25 other exported bind*/create*/setX functions) also lacks return annotations; that wider sweep is out of scope here and is a separate stylistic call.\n\nSuggested direction: append `: void` to both signatures.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:25Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:41Z","started_at":"2026-05-05T22:51:48Z","closed_at":"2026-05-06T23:06:41Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.31","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:25Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.30","title":"applyDecodedView's controls.update() trigger condition is a 4-way OR that should be a single dirty flag","description":"src/client/url-state.ts:724 — `if (hasCam || hasTgt || view.up || willEnterObserve) stellata.controls.update();`\\n\\nPR #1 grew this from a 3-way OR (hasCam||hasTgt||view.up) to 4 by appending willEnterObserve. The pattern is: \"if I touched anything camera/controls cared about above, update.\" That's a textbook dirty-flag, not a chain that should grow with each new branch.\\n\\nWhy it matters: small but every future cause of \"controls needs an update after I touched something\" extends the OR. The test for \"did I mutate something?\" is the same boolean for all four branches; spelling it as four boolean expressions is mild jank and invites missing a fifth.\\n\\nSuggested direction: at the top of the camera-touching block, declare `let controlsDirty = false;` and set it inside each branch that mutates camera.position / controls.target / camera.up, including the observe pre-snap. Then `if (controlsDirty) stellata.controls.update();` reads as the intent.\\n\\nLow priority — purely a tidiness call. Mention only because PR #1 is exactly where the OR grew.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:23Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:41Z","started_at":"2026-05-05T22:53:21Z","closed_at":"2026-05-06T23:06:41Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.30","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.26","title":"Apply ?? null/255 cleanup to search.ts:303 missed by PR #8","description":"PR #8 (stellata-y84 trivial cleanups) replaced `idx !== undefined ? idx : null` with `idx ?? null` in src/client/molecular-clouds.ts:184 but missed the same nit pattern in src/client/search.ts:303 — `const conIdx = entry.c !== undefined ? entry.c : 255`. Note the same file already uses the modern form at line 218 (`const conIdx = entry.c ?? 255`), so this is an internal consistency fix as well. Two further nearby instances at search.ts:399 and search.ts:407 use `idx !== undefined ? [...] : []` and cannot be reduced to `??` (they branch shape, not value); leave those as-is or rewrite as `idx === undefined ? [] : [...]` for consistency.\n\nWhy it matters: trivial-cleanup PRs should sweep parallel sites. The PR claimed to have reviewed all 54 source files; this miss in a file that already uses the preferred form shows the sweep was incomplete.\n\nSuggested direction: 1-line change at search.ts:303 to `const conIdx = entry.c ?? 255;`.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:11:00Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:40Z","started_at":"2026-05-05T22:51:47Z","closed_at":"2026-05-06T23:06:40Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.26","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:11:00Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.24","title":"Magic [0,0,0] literal in url-state — name it (OBSERVE_CAM_LOCAL or reuse DEFAULT_TGT)","description":"src/client/url-state.ts:614 and 722 both spell the observe-mode camera default as a fresh `[0, 0, 0]` literal:\\n\\n- Line 614: `const camDefault = mode === 'observe' ? [0, 0, 0] : DEFAULT_CAM;`\\n- Line 722: `stellata.camera.position.set(0, 0, 0);`\\n\\nMeanwhile lines 39-41 already define `DEFAULT_CAM`, `DEFAULT_TGT`, `DEFAULT_UP` as named tuples. The new value is conceptually distinct from `DEFAULT_TGT` (different semantic meaning — camera *at* focal star, not target *at* origin) so reusing `DEFAULT_TGT` would obscure intent, but a fresh-name constant (e.g. `OBSERVE_CAM_LOCAL = [0, 0, 0] as const`) would tie the two call sites together symbolically and make grep find both.\\n\\nWhy it matters: small but exactly the kind of duplicate magic value the file otherwise avoids — every other vector default in this module is named.\\n\\nSuggested direction: add a named const at the top alongside `DEFAULT_CAM` / `DEFAULT_TGT` / `DEFAULT_UP` and use it in both the encoder branch and the decoder snap-back. Mostly relevant only if the broader generalisation in the sibling bead doesn't subsume it.","notes":"Reopened 2026-05-06 after PR #30 rebase onto PR #22. Coupled to 9mm.20 (OBSERVE_CAM_LOCAL); reverted with that bead. Re-evaluate the magic [0,0,0] in the worldOffset frame.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:51Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:12Z","started_at":"2026-05-05T22:51:46Z","closed_at":"2026-05-08T18:54:12Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.24","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:51Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.23","title":"renderResultsDom / renderDom rebuilds set hoverIdx=0 but don't scroll the active row into view","description":"In src/client/search.ts:88 (SearchBox.render) and src/client/constellation-typeahead.ts:88 (renderQuery), every query-change rebuilds the dropdown and assigns hoverIdx = 0 (or -1 if empty). The render path sets `li.className = 'active'` on row 0 but never calls scrollIntoView on it, while applyHoverClass — invoked from arrow-nav — does scroll.\n\nResult: arrow-nav and rebuild produce inconsistent scroll behaviour. In practice rebuild also resets resultsEl.scrollTop to 0 because innerHTML='' tears down the scroll position, so today the active row at index 0 is always visible after a rebuild. But the moment a future change either (a) adds incremental rendering instead of full innerHTML rebuild, or (b) preserves scroll across rebuilds, this asymmetry will surface as 'highlight is on a row above the visible window'.\n\nSuggested direction: consolidate the scroll-active-into-view call into a single helper used by both paths, or have renderDom/renderResultsDom call setHover(0) as the final step instead of pre-baking class='active' into the inner loop. Folds naturally into stellata-xbj when the unification happens.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:46Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:39Z","started_at":"2026-05-06T06:30:13Z","closed_at":"2026-05-06T23:06:39Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.23","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.22","title":"Update docs/camera-modes.md OBSERVE drag section to mention pointercancel teardown","description":"docs/camera-modes.md lines 196-242 give a thorough walkthrough of the OBSERVE-mode drag mechanism: pointer-down captures dGrabbed, pointer-move rotates, pointer-up promotes momentum on a tight gap. There is no mention of pointercancel.\n\nPR #19 added a real branch to the lifecycle (pointer-cancel: tear down without flick) but the corresponding doc paragraph wasn't touched. Stellata's docs are generally exhaustive — see docs/camera-modes.md for proof — so this is a small drift but worth catching while reviewing.\n\nWhy it matters: future readers (including future Claude sessions) rely on the docs to understand the camera state machine. An undocumented branch is exactly how the next regression sneaks in.\n\nSuggested direction: one short bullet in docs/camera-modes.md OBSERVE section noting that pointercancel mirrors pointer-up but skips momentum promotion, and why (OS-preempted touch is not a deliberate release).","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:40Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:39Z","started_at":"2026-05-06T06:27:16Z","closed_at":"2026-05-06T23:06:39Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.22","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:39Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.20","title":"Encoder/decoder cam-omission invariant in observe mode lives only in prose","description":"PR #1 split the cam=[0,0,0]-in-observe optimisation across two files-of-the-mind that never reference each other in code:\\n\\n- Encoder side: src/client/url-state.ts:614 — `const camDefault = mode === 'observe' ? [0, 0, 0] : DEFAULT_CAM;`\\n- Decoder side: src/client/url-state.ts:720-723 — `const willEnterObserve = view.mode === 'observe' \u0026\u0026 stellata.getFocusedStar() !== null; if (willEnterObserve \u0026\u0026 !hasCam) stellata.camera.position.set(0, 0, 0);`\\n\\nThe linkage is documented only in comments at 608-613 and 715-719. If a future change renames the observe enum, adds a third mode, or shifts the floating-origin invariant (camera no longer parks at focal-star local origin), exactly one of these two sites will be touched and the other will silently disagree — receivers will land at the wrong quaternion or the URL will pick up 16 chars of A's again.\\n\\nWhy it matters: the cam=[0,0,0] sentinel is implicit, not a wire-format constant. There's no guard that catches drift.\\n\\nSuggested direction: extract a single `OBSERVE_CAM_LOCAL = [0, 0, 0]` const (or a named helper `observeModeCamDefault(view)`) referenced from both sites, OR fold the snap-back into a single `afterDecode(view, stellata)` step so the encoder/decoder duality is implementation detail of one helper. Bonus: a unit test that round-trips a full observe-mode `DecodedView` through encode→apply→capture and asserts the camera quaternion.","notes":"Reopened 2026-05-06 after PR #30 rebase onto PR #22. PR #22 added a worldOffset URL field that reframes the cam-default check entirely: when the frame isn't Sol-anchored, cam is always emitted regardless of value. Re-evaluate the magic [0,0,0] under #22's worldOffset frame.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:36Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:12Z","started_at":"2026-05-05T22:51:45Z","closed_at":"2026-05-08T18:54:12Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.20","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.17","title":"Generalise per-field default-omission in url-state encoder via FieldSpec.encodeIfDefault","description":"src/client/url-state.ts:605-623 — `currentStateOf` decides whether to emit cam/tgt/up by spelling out three near-identical `!approx(c.x, D[0]) || !approx(c.y, D[1]) || !approx(c.z, D[2])` branches inline. PR #1 made the cam branch a 4th brute-force special case (`mode === 'observe' ? [0,0,0] : DEFAULT_CAM`) rather than extending the FieldSpec abstraction. This is the exact failure shape the file's own header comment warns about — every new \"omit when default\" rule lives outside the field table.\\n\\nThe FieldSpec abstraction (lines 126-140, 146-160) already exposes `isPresent` but it only checks `v[key] !== undefined` — default-equality vs canonical defaults is hand-rolled per-field at the call site, in a different file region (~30 lines below the FieldSpec definitions).\\n\\nWhy it matters: any future \"omit X when at default\" rule (e.g. up=[0,1,0] check at 621-623, or future POI/cloud defaults) gets cut-and-pasted again, and a mode-aware variant like cam-in-observe will silently disagree with itself if the encoder default and the decoder snap-back drift apart.\\n\\nSuggested direction: lift `encodeIfDefault` (or a per-spec `getDefault(state)` callback) into FieldSpec so `currentStateOf` does `if (!matchesDefault(spec, view, ctx)) view[key] = ...` in one loop. Mode-awareness becomes a context arg, not a new branch in encoder prose.","notes":"Investigated 2026-05-08 in batch 3. Decision: not worth the structural cost. FieldSpec.isPresent already serves as the per-field omission gate; currentStateOf populates view fields conditionally, so 'absent in view' = 'omit'. The conditions in currentStateOf are heterogeneous (compare-to-constant, override-flag presence, mode-aware defaults, derived state) — a single declarative encodeIfDefault on FieldSpec would not actually simplify them. The 9mm.20/9mm.24 cleanups (defaultCamForMode + OBSERVE_CAM_LOCAL) addressed the worst case (mode-aware default duplicated across encoder/decoder) and that's the meaningful win in this area. Closing as wontfix-this-iteration.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:22Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:53:49Z","started_at":"2026-05-08T18:25:48Z","closed_at":"2026-05-08T18:53:49Z","close_reason":"Investigated and decided wontfix — the proposed encodeIfDefault generalisation would not actually simplify currentStateOf's heterogeneous default predicates. The mode-aware-default subcase (the worst offender) is fixed by 9mm.20/9mm.24 instead.","dependencies":[{"issue_id":"stellata-9mm.17","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:21Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.16","title":"typeahead-util.ts comment cross-references stellata-9kz instead of stellata-xbj for the unify follow-up","description":"src/client/typeahead-util.ts:4 says:\n\n  // The two are otherwise parallel implementations of the same UI primitive\n  // — see stellata-9kz for a follow-up to unify them behind a single\n  // `Typeahead\u003cT\u003e` abstraction.\n\nBut stellata-9kz is the cap-design issue (\"scroll-to-find vs type-to-narrow\") — not unification. The unify follow-up actually lives at stellata-xbj (\"Unify SearchBox and constellation typeahead behind a Typeahead\u003cT\u003e abstraction\"), which was created the same day PR #29 merged and is what xbj's description explicitly traces to PR #29.\n\nMinor but real: a future maintainer following the comment lands on the wrong issue. One-character fix.\n\nSuggested direction: change 9kz → xbj in the comment. Optionally also add a separate sentence pointing to 9kz for the cap-policy debate.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:19Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:37Z","started_at":"2026-05-05T22:51:42Z","closed_at":"2026-05-06T23:06:37Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.16","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:18Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.15","title":"Adopt setPointerCapture in ObserveControls instead of routing through window","description":"src/client/observe-controls.ts currently registers pointermove/pointerup/pointercancel on the global window object so the drag survives the cursor leaving the canvas (lines 109-112, 119-121). PR #19 added pointercancel into this same window-routed pattern.\n\nThe canonical Pointer Events idiom is canvas.setPointerCapture(e.pointerId) on pointerdown, then attach pointermove/pointerup/pointercancel directly to the canvas. The browser then routes all subsequent events for that pointerId to the captured element (regardless of where the cursor is) and automatically fires lostpointercapture when the OS preempts the gesture — i.e. the exact stuck-drag scenario PR #19 was patching becomes a built-in lifecycle event.\n\nBenefits:\n- One listener-set on the canvas instead of split between canvas (pointerdown/wheel) and window (move/up/cancel)\n- lostpointercapture is a single event covering pointercancel + window-blur + tab-hide + cursor-leave-page-area, which is broader than the pointercancel coverage PR #19 added\n- No need for the activePointerId === pointerId guards inside each handler — capture filters at the source\n\nWhy it matters: PR #19 was a patch on top of a non-canonical pattern. Migrating to setPointerCapture would have made that patch unnecessary and would also cover the window-blur-mid-drag gap that pointercancel doesn't always fire for.\n\nSuggested direction: capture the pointer in onPointerDown, release it (or rely on auto-release) in onPointerUp/onPointerCancel, listen for lostpointercapture as the unified teardown signal, move all listeners to canvas.","notes":"Deferred from PR #30 (2026-05-06 batch). This is an architectural migration from window-routed pointer events to setPointerCapture, not a small refactor. Worth its own PR with a careful manual smoke pass on iOS Safari + Chrome + Firefox since the pointer-capture lifecycle differs across browsers. PR #30 took the narrower fix (9mm.18: window blur + visibilitychange listeners through cancelDrag) which covers the immediate stuck-drag failure mode this bead's lostpointercapture would also have caught. Pick up when there's a dedicated input-handling pass — that PR could also fold in 9mm.12 (ObserveControls drag tests) since both touch the same module.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:15Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:14Z","started_at":"2026-05-08T18:25:51Z","closed_at":"2026-05-08T18:54:14Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.15","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.13","title":"Constellation-typeahead test hardcodes the cap value instead of importing TYPEAHEAD_MAX_RESULTS","description":"src/client/constellation-typeahead.test.ts:54 and :101 assert `out` length equals literal `10`. The constant lives in src/client/typeahead-util.ts and is re-exported through the binder, so the test could import it and stay coupled. Today, bumping TYPEAHEAD_MAX_RESULTS to (say) 12 in typeahead-util.ts produces a green typecheck but a red test, and the failure message will read 'expected 12 to equal 10' with no obvious link to the constant — the test descriptions even reference the constant by name (\"caps total length at TYPEAHEAD_MAX_RESULTS\") while the assertion uses the literal.\n\nPR #29 perpetuated this smell when it changed the cap from 30 to 10 — both the value and a search-and-replace on the description string had to be updated in lockstep. The fix is mechanical: import the constant in the test and assert against it.\n\nThis same smell applied before PR #29 (was hardcoded 30), but the PR is the natural place to call it out because it just changed the value and the test descriptions in lockstep.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:08Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:37Z","started_at":"2026-05-05T22:51:42Z","closed_at":"2026-05-06T23:06:37Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.13","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.12","title":"Add vitest coverage for ObserveControls drag lifecycle (down/move/up/cancel)","description":"src/client/observe-controls.ts has no vitest coverage. PR #19 added a pointercancel handler whose contract is 'mirrors pointerup but does not promote momentum' — a contract that is trivially testable with synthetic PointerEvents against the class instance, but instead the PR's test plan acknowledges only manual iOS Safari testing is feasible.\n\nThe class is small and side-effect-light: it takes a canvas, a camera, and getFov/setFov closures. An offscreen canvas + a fresh THREE.PerspectiveCamera in jsdom would let the suite assert:\n- pointerdown sets dragging/activePointerId\n- pointermove rotates camera.quaternion\n- pointerup followed quickly promotes momentum (momentumSpeed \u003e 0); slow release does not\n- pointercancel clears state and never promotes momentum (regression test for PR #19)\n- pointerup/pointermove with the wrong pointerId is ignored\n- disable() clears all state and detaches listeners\n\nWhy it matters: pointer-event sequencing bugs hit exactly the platforms hardest to manually test (iOS phone-call interrupt, system-gesture preemption). A unit test for pointercancel-no-momentum is the cheapest possible regression net.\n\nSuggested direction: src/client/observe-controls.test.ts under vitest, dispatching new PointerEvent('pointer*', { pointerId, clientX, clientY }) on the canvas and window as appropriate.","notes":"Deferred from PR #30 (2026-05-06 batch). Vitest runs in environment:'node' and the project has no jsdom/happy-dom dep. The test would need either (a) adding jsdom as a devDependency + per-file environment override, or (b) hand-rolled stubs for window / document / HTMLCanvasElement and PointerEvent — feasible but heavyweight enough to deserve a focused PR. Note that 9mm.39 (this batch) added pure-linalg coverage for the camera.up alignment, so the riskiest 9mm.18-related logic is already pinned; what's still uncovered is the pointer-event sequencing itself.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:10:01Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:15Z","started_at":"2026-05-08T18:25:57Z","closed_at":"2026-05-08T18:54:15Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.12","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:10:00Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.11","title":"Test gap: no regression test guards disc-pass blend equation across material lifetimes","description":"PR #25 had to update two parallel sites for the AddEquation -\u003e MaxEquation switch (constructor + chart-mode swap-back). There is no automated check that the disc material's blendEquation matches the documented intent at any point in its lifecycle, so a future drift between the two locations would only be caught by manual visual QA against close binaries.\n\nThe vitest suite already covers pure helpers (parseSpectral, physicalRadius, GCVS parsers). A small DOM-light assertion test in src/client/ that:\n\n1. Constructs a Stellata (or extracts the material-setup logic to a small testable helper).\n2. Asserts material.blendEquation === THREE.MaxEquation, blendSrc === OneFactor, blendDst === OneFactor, blending === CustomBlending.\n3. Toggles setMonochrome(true) then setMonochrome(false) and re-asserts the same four properties on the disc material.\n\n...would catch this regression class. Pair naturally with stellata-9mm.1 (extract a helper) — the helper is what the test asserts on.\n\nSuggested direction: extract applyDiscBlendDefaults(material) per stellata-9mm.1 and write a unit test that calls it then verifies the four properties; toggle test verifies setMonochrome(false) re-applies them.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:59Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:36Z","started_at":"2026-05-06T06:24:06Z","closed_at":"2026-05-06T23:06:36Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.11","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.10","title":"Hoist the 'active' CSS class literal out of the four typeahead call sites","description":"The string literal 'active' is now hard-coded in four places across three files after PR #29:\n- src/client/typeahead-util.ts:25  classList.remove('active')\n- src/client/typeahead-util.ts:29  classList.add('active')\n- src/client/search.ts:100         li.className = i === this.hoverIdx ? 'active' : ''\n- src/client/constellation-typeahead.ts:73  li.className = i === hoverIdx ? 'active' : ''\n\nThis is the first DRY violation introduced by the new shared module — typeahead-util.ts owns the toggle but the two binders still rebuild rows with the same literal in their own renderDom paths. A typo in any of the four would silently break the highlight on either arrow-nav or rebuild without a test failing.\n\nWhy it matters: PR #29 created the perfect place to centralise this (typeahead-util.ts) but only moved two of the four occurrences. Either export an HOVER_CLASS constant from typeahead-util.ts and consume it in both render fns, or factor the per-row `\u003cli\u003e` creation into a helper that owns the class. Note that styles.css couples '.search-results li.active' (line 263) and 'body.monochrome .search-results li.active' (line 949) to the same string; CSS will need the same name. Folded into stellata-xbj's unify work, but worth flagging separately so it isn't lost if xbj is deferred.\n\nSuggested direction: `export const TYPEAHEAD_ACTIVE_CLASS = 'active'` in typeahead-util.ts, replace all four sites.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:56Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:36Z","started_at":"2026-05-05T22:51:41Z","closed_at":"2026-05-06T23:06:36Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.10","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:55Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.9","title":"Hide viewSpaceScreenDir scratch vector inside arrow-path.ts","description":"src/client/arrow-path.ts:33-44 — viewSpaceScreenDir takes a caller-supplied scratch Vector3, but every other Three.js helper module in src/client owns its scratch state internally as closure-private state (focus-ring-overlay.ts:8 'const v = new THREE.Vector3()', disc-mask.ts:36, distance-vector-overlay.ts:30-31). Pushing scratch out as an API parameter leaks an implementation detail to two call sites that currently have to remember to pass an unaliased Vector3, and it asymmetrically singles out this helper from buildArrowSvgPath next door (which is allocation-free by virtue of returning a string).\n\nCompounding minor: transformDirection already normalises the 3D vector internally, so the explicit Math.hypot + division renormalises the projected 2D pair a second time. Functionally fine, just slightly wasteful and obscures that the input is already on the unit sphere.\n\nSuggested direction: declare 'const scratchVS = new THREE.Vector3()' at module scope inside arrow-path.ts and drop the third parameter. Two call sites (hud-overlay.ts:277, poi-overlay.ts:325) lose a thread-through arg. If shared scratch across helpers is desirable, move all overlay scratch vectors into one module-level pool.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:54Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:36Z","started_at":"2026-05-06T06:34:26Z","closed_at":"2026-05-06T23:06:36Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.9","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.7","title":"Architectural audit: revisit halo-transparency mechanism (gl_FragDepth=1.0) under MaxEquation","description":"The disc fragment shader's 'halo transparency' rule (src/client/shaders/star.frag.glsl line 155, 'if (glow \u003c uCoreThreshold) gl_FragDepth = 1.0;') was designed for premultiplied-alpha blending: a dim halo fragment painted dim colour with low alpha, and pushing depth to the far plane let the later additive-glow pass deposit distant stars on top through that haze.\n\nUnder MaxEquation, the framebuffer ends up with max(haloRGB, dst) — i.e. a saturated halo always wins, a dim halo over a brighter background gets clipped to zero contribution. The depth=1.0 trick still allows depth-test pass for later glows, but the value of the halo painting at all is qualitatively different: a halo over the Milky Way no longer adds, it gates by max. PR #25 explicitly calls this an intended tradeoff ('faint halos against bright backgrounds wash out'), but the asymmetry between disc halo and glow pass (additive) over the same background may now produce a hard discontinuity at vPhysRatio=0.5 that wasn't there before.\n\nNot asking to revert — the artifact fix is the right call. Just want a short investigation: render a single mid-distance star against the MW band, sweep vPhysRatio across the 0.5 split, and confirm there's no visible step. If there is, the unified super-Gaussian + plateau profile (docs/rendering.md) may need a corresponding tweak to the pass-split smoothstep so the two passes meet smoothly under the new blend regime.\n\nSuggested direction: targeted visual A/B at the vPhysRatio=0.5 boundary; if a step appears, explore narrowing the discard fringe or softening the pass-split criterion at the boundary.","notes":"Deferred from PR #X (2026-05-08 batch 3). Re-affirmed: requires visual A/B at vPhysRatio=0.5 boundary with the renderer running, not a code-only task. Pick up when there's a session for visual QA against ESO eso0932a, or when a user reports a hard-edge artefact at the disc/glow split.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:47Z","created_by":"Alex Marshall","updated_at":"2026-05-10T09:58:19Z","started_at":"2026-05-08T18:25:55Z","closed_at":"2026-05-10T09:58:19Z","close_reason":"Visual A/B implicitly performed through normal usage since PR #25 (2026-05-03) and the defensive depth-write hardening in PR #33 (2026-05-06). No vPhysRatio=0.5 discontinuity observed against the MW band. logarithmicDepthBuffer + defensive gl_FragDepth write both verified still in place (stellata.ts:633, star.frag.glsl:77).","dependencies":[{"issue_id":"stellata-9mm.7","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:46Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.5","title":"Unify duplicate projectToScreen helpers in hud-overlay + poi-overlay","description":"src/client/hud-overlay.ts (lines 405-415) and src/client/poi-overlay.ts (lines 398-408) contain byte-identical private projectToScreen(point, camera, w, h) implementations: clone the input, applyMatrix4(matrixWorldInverse), bail when v.z \u003e= -near, applyMatrix4(projectionMatrix), NDC → CSS pixels. focus-ring-overlay.ts (lines 62-74) and the inline conversions in chart-labels.ts / disc-mask.ts / constellation-overlay.ts implement the same pattern by hand.\n\nPR #27 introduced a new shared helper (viewSpaceScreenDir) into arrow-path.ts to deduplicate the third-tier path but left the other two tiers' shared projection helper duplicated. Natural place to continue: lift projectToScreen into arrow-path.ts (or a new screen-projection module if arrow-path feels mismatched) and import from both overlays.\n\nBonus: the inline .clone() per call allocates one Vector3 per point per frame in a hot path — unifying lets us thread a scratch vector through one place rather than five.\n\nWhy it matters: ratifies and amplifies the duplication this PR almost addressed.","notes":"Reopened 2026-05-06 after PR #30 rebase onto PR #22. PR #22 keeps projectToScreen local to hud-overlay.ts and uses a different cascade in poi-overlay.ts. Hoist to arrow-path.ts is reverted; consolidation should follow #22's structure if pursued.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:41Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:11Z","started_at":"2026-05-05T22:51:40Z","closed_at":"2026-05-08T18:54:11Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.5","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:41Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.4","title":"Extract drag-teardown helper in observe-controls (3 copies of the same 4-field reset)","description":"In src/client/observe-controls.ts the same drag-state reset (dragging=false; activePointerId=null; momentumSpeed=0; lastRotAngle=0) appears in three places:\n\n- onPointerCancel lines 199-202 (added by PR #19)\n- onPointerUp lines 173-174 + the no-flick branch lines 187-188\n- disable() lines 123-126\n\nPR #19 introduced the third copy. The class already factors momentum constants and pixelToWorldDir, but the state-reset is open-coded. A private endDrag(promoteMomentum: boolean) helper called from all three sites would collapse the parallel writes and make the 'cancel = up minus the flick' contract explicit.\n\nWhy it matters: invariant drift. If a future field joins the drag state (e.g. lastMoveTimeMs needs reset, or a new pointer-capture handle is added), three call sites must be updated. The bug this PR fixed was caused by exactly this kind of partial coverage on a different controller.\n\nSuggested direction: introduce private endDrag(promoteMomentum: boolean) on ObserveControls; call from onPointerUp/onPointerCancel/disable. No behaviour change.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:34Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:35Z","started_at":"2026-05-05T22:51:39Z","closed_at":"2026-05-06T23:06:35Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.4","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:34Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.3","title":"Extract shared 3-tier screen-dir cascade from hud-overlay + poi-overlay","description":"PR #27 added a third-tier viewSpaceScreenDir fallback identically in both src/client/hud-overlay.ts (lines 248-289) and src/client/poi-overlay.ts (lines 294-334). The full 3-tier cascade (aux-step → target-projection → view-space) is now structurally identical in both files apart from variable names and indentation: same magic-number pixel thresholds (slen \u003e= 1, tlen \u003e= 1), same control flow, same comments verbatim. Future tweaks — adding a fourth tier, changing the threshold, swapping the comment — must be made in two places.\n\nWhy it matters: this is the bug-class that produced stellata-dy2 in the first place — divergent direction logic. Cementing the duplication for the fix doubles the surface area for the next divergence.\n\nSuggested direction: lift a screenDirFromCascade(origin, dir, camera, anchorScreen, targetWorld, auxStepW, w, h) helper into src/client/arrow-path.ts (alongside viewSpaceScreenDir and buildArrowSvgPath). Returns [sux, suy] | null. Both call sites collapse to a single call.","notes":"Reopened 2026-05-06 after PR #30 rebase onto PR #22. The 3-tier cascade was redesigned by PR #22 to a 2-tier (target-projection + view-space, dropping the aux-step tier) with per-axis FOV scaling. screenDirFromCascade is no longer the right shape. Re-evaluate consolidation against #22's design.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:29Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:11Z","started_at":"2026-05-05T22:51:39Z","closed_at":"2026-05-08T18:54:11Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.3","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:29Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.3","depends_on_id":"stellata-9mm.5","type":"related","created_at":"2026-05-08T19:22:16Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.3","depends_on_id":"stellata-akk","type":"related","created_at":"2026-05-08T19:22:19Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9mm.3","depends_on_id":"stellata-vjj","type":"related","created_at":"2026-05-08T19:22:22Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.2","title":"Comment-rot: 'premultiplied-alpha' / 'opaque-over' descriptions of disc pass are stale post-MaxEquation","description":"PR #25 changed the disc-pass blend from premul-alpha additive to per-channel max, but several authoritative comments still describe the old behaviour. New readers will be misled about what the pass actually does:\n\n- src/client/shaders/star.frag.glsl line 9: '1 = disc pass (premul-alpha, close-range resolved stars)'.\n- src/client/shaders/star.frag.glsl lines 139-140: 'Disc pass — only disc-dominated stars. Premultiplied-alpha blending; depth handling below decides whether each fragment occludes the background.'\n- src/client/stellata.ts line 1569: 'disc pass is opaque-over in colour mode, multiply in chart mode'.\n- docs/rendering.md line 43: 'Premultiplied-alpha blend + depthTest + depthWrite.'\n- docs/rendering.md lines 198-202 ('Halo transparency'): describes the halo as painting 'dim alpha-blended colour' — under MaxEquation, alpha is independently max'd and the dim halo's RGB only paints if it beats whatever's already in the framebuffer.\n\nSuggested direction: replace each with a one-line description of MaxEquation semantics, ideally pointing back to the canonical comment in stellata.ts so this rationale lives in one place.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T22:09:28Z","created_by":"Alex Marshall","updated_at":"2026-05-06T23:06:34Z","started_at":"2026-05-05T22:53:21Z","closed_at":"2026-05-06T23:06:34Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-9mm.2","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T23:09:27Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm","title":"Code quality — retrospective review fallout","description":"Umbrella for code-quality follow-ups surfaced by retrospective code reviews of merged PRs. Children are individually-actionable refactors / DRY / abstraction tasks. None are user-visible; collectively they reduce drift between parallel implementations and improve the codebase's bus factor.\n\nDefaults to P3 per the prioritization framework (code-quality refactors that aren't user-visible). Bump individual children to P2 only if one becomes a blocker for in-flight work.","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-05T21:56:24Z","created_by":"Alex Marshall","updated_at":"2026-05-05T21:56:24Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-xbj","title":"Unify SearchBox and constellation typeahead behind a Typeahead\u003cT\u003e abstraction","description":"`src/client/search.ts` (the `SearchBox` class) and `src/client/constellation-typeahead.ts` (a closure-bag binding) are two implementations of the same UI primitive. Both maintain their own `hoverIdx`, render results into `\u003cli\u003e\u003cspan/\u003e\u003cspan class=\"sub\"/\u003e\u003c/li\u003e`, attach a `mousedown` per-row listener, defer blur by 140ms, dispatch arrow/Enter/Escape keys, and reverse-sync display name on external state change.\n\nPR #29 cracked open the seal between them by adding a shared `typeahead-util.ts` (TYPEAHEAD_MAX_RESULTS, applyHoverClass) and a shared `dom-util.ts` (escapeHtml). The next step is unifying the binding itself.\n\n# Why this matters\n\nDRY: the two files have parallel keydown handlers, parallel blur timers, parallel render loops. Bug fixes have to be applied twice. PR #29 is exactly that pattern — a P1 surfaced in the focus typeahead, fixed in both for parity.\n\n# Suggested shape\n\n```ts\nclass Typeahead\u003cT\u003e {\n  constructor(opts: {\n    input: HTMLInputElement;\n    resultsEl: HTMLUListElement;\n    runQuery: (q: string) =\u003e T[];\n    renderRow: (item: T, hover: boolean) =\u003e HTMLElement; // or innerHTML string\n    onSelect: (item: T) =\u003e void;\n    onClear?: () =\u003e void;\n    initialQueryOnFocus?: 'value' | 'always-fresh';      // const cap diff\n    pinNoneOnEmpty?: boolean;                            // constellation-only\n  }) { ... }\n\n  setName(name: string): void;\n  // arrow-nav, blur-defer, escape, mousedown picking all internal\n}\n```\n\nThen `bindSearch` instantiates two of these (focus + to) and `bindConstellationTypeahead` instantiates one. The `SearchBox` class disappears.\n\n# Out of scope for #29 (which is a P1 bug fix). This is the cleanup.","notes":"Deferred from PR #30 (2026-05-06 batch). Unifying SearchBox (search.ts) and the constellation typeahead (constellation-typeahead.ts) behind a single Typeahead\u003cT\u003e abstraction is a multi-file restructure of the picker UX. PR #30 closed several of its prerequisites: TYPEAHEAD_MAX_RESULTS + TYPEAHEAD_ACTIVE_CLASS hoisted (9mm.10, 9mm.13), applyHoverClass scroll consolidated (9mm.19, 9mm.23), tests added for the shared helpers (9mm.6). The two binders are now structurally close enough that the abstraction is a smaller diff than it was, but still its own PR — picking it up should also re-evaluate stellata-xbj's children once they're filed.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-05T21:54:16Z","created_by":"Alex Marshall","updated_at":"2026-05-08T19:43:35Z","started_at":"2026-05-08T19:33:52Z","closed_at":"2026-05-08T19:43:35Z","close_reason":"Shipped via PR #43 — Typeahead\u003cT\u003e abstraction unifies SearchBox and constellation-typeahead binding; net -163 lines, no behavioural change.","dependencies":[{"issue_id":"stellata-xbj","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-05T22:56:29Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9kz","title":"Typeahead result-cap design: scroll-to-find vs type-to-narrow","description":"The Focus / To star search box and the constellation typeahead share a TYPEAHEAD_MAX_RESULTS = 10 cap (defined in src/client/search.ts, imported by src/client/constellation-typeahead.ts). 10 was chosen so the wraparound point matches what the dropdown's 320px max-height + ~30px row height actually shows on screen — the user shouldn't arrow-nav past invisible rows.\n\nOpen question: is type-to-narrow the right model, or should we let users scroll the dropdown to find rare matches?\n\n# Today's behaviour\n- Star search: Fuse.js fuzzy search, capped to 10 results after dedup. Distinguishes overlap by score.\n- Constellation: substring filter on (name + code), capped to 10. With 88 constellations and a sparse query, useful matches can fall off the end.\n\n# Pros of current cap\n- Visible == navigable. Arrow-down wrap matches the visible window.\n- Forces the user to be more specific. Encourages typing 'ori' over scrolling Andromeda → Orion.\n- Mobile-friendly (smaller drop-down works for thumb taps).\n\n# Pros of allowing more results\n- Discovery: a user who types a partial substring may want to see 'oh there's also X'.\n- 88 constellation names is small enough to scroll through if you don't remember the exact spelling.\n- Star search is fuzzy — sometimes the right answer ranks 11th if multiple stars share label fragments.\n\n# Possible directions if we revisit\n1. Raise the cap (e.g. 30) and rely on scrollIntoView to keep the active row visible. Wraparound at 30 is jarring but workable.\n2. Keep the cap but show a 'type to see more…' hint when results would have overflowed.\n3. Two-stage: cap to 10 by default, expand to full list on Tab or extra ArrowDown past the last visible row.\n4. Make the cap dynamic to viewport height (CSS variable, JS-derived).\n\n# Filed as part of stellata-eya\nThe cap was 12 (search) and 30 (constellation) before; the in-place arrow-nav fix exposed the inconsistency between visible-count and wraparound-count. Set both to 10 as the simplest unified behaviour while filing this for follow-up.\n\n# Acceptance\nDecide on a rule and either keep the current cap or land one of the alternatives above.","notes":"Constant lives at src/client/search.ts:TYPEAHEAD_MAX_RESULTS. Comment there points back to this issue.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-05T21:42:57Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:40Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-4ph","title":"Decide: snap vs animated transition when clicking a new star to focus","description":"# Question\n\nCurrently, clicking a star (when focus is already on a different star or nothing) snaps focus instantly: setFocus runs, the floating origin recenters, the camera teleports to the new orbit position. It's fast but visually jarring — the world cuts to a new view with no continuity.\n\nThe OBSERVE-mode swap-anchor flow (`swapObserveAnchor`) handles a related case more smoothly: it preserves the camera quaternion and lerps the local frame between anchors. Could navigate-mode focus changes use a similar idea? Or should it animate as a flythrough — like a mini-warp from the current pose to the new orbit?\n\nPossible approaches:\n\n- **(A) Keep the snap.** Clicking is decisive and immediate; users who want a flythrough have the warp button. Cheapest. Status quo.\n- **(B) Lerp camera position over OBSERVE_TRANSITION_MS-style duration**, recentering origin once at start. Smooth visual continuity, no precision cost (recentre happens once).\n- **(C) Auto-warp.** Reuse the warp state machine. Always ends with the camera in the standard orbit pose, with the same arrival animation as a search-select.\n- **(D) Hybrid.** Snap if the click is on the currently focused star (no-op anyway) or if the new star is within some local-frame distance; animate otherwise.\n\nNot part of a7d.2 — file as a future UX decision. The current snap behaviour isn't broken, just potentially improvable.\n\n# Where to look\n\n- src/client/stellata.ts `focusStar` (~1735) — the search-select / explicit focus path; sets up an aim animation toward the new orbit position.\n- src/client/stellata.ts `onClick` / click handler (~2676) — the click-to-focus dispatch.\n- src/client/stellata.ts `swapObserveAnchor` (~2021) — the related observe-mode pattern.\n- docs/camera-modes.md — warp animation 3-phase state machine (the relevant prior art for option C).\n\n# Branch\n\nStandalone UX bead; no dependency on the a7d epic. Implement on a fresh worktree when the user signals they want to take it on.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-05T18:13:08Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:40Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3c8","title":"Decide: should Sol/GC arrows retire on unfocus, or focus ring persist?","description":"# Observation\n\nWhen the HUD is on and the user unfocuses from a star in front of them, the focus ring disappears (expected — no focus) but the Sol/GC arrows stay drawn. The arrows are mode-bound (navigate), not focus-bound; the ring is focus-bound. The asymmetry feels odd from a UX standpoint — either both should persist or both should retire on unfocus.\n\nThis was bundled into a7d.2.11's bug report alongside the float32 unfocus jump, but split out here because it's a UX decision rather than a numerical-precision fix.\n\n# Options\n\nA. Hide arrows on unfocus until camera moves, matching the ring's disappearance.\nB. Keep ring drawn on the (former) focal star until camera moves significantly away — symmetric with arrows persisting.\nC. Leave as-is; the asymmetry isn't actually wrong (arrows are about navigation context, ring is about focus context).\n\n# Where to look\n\n- src/client/hud-overlay.ts — Sol/GC arrow visibility logic.\n- src/client/focus-ring-overlay.ts — focus ring visibility (driven by getFocusedStar).\n- bd memory stellata-a7d2-pin-mechanism for context on why this came up.\n\n# Branch\n\nIf pursued, a small UX-only commit on its own branch — no code shared with a7d.2.11.","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-05T17:42:48Z","created_by":"Alex Marshall","updated_at":"2026-05-05T18:18:48Z","closed_at":"2026-05-05T18:18:48Z","close_reason":"Won't fix — intended behaviour. Focus ring is bound to focus state (correctly disappears on unfocus), Sol/GC arrows are bound to navigate mode (correctly persist). The asymmetry is by design, not a UX inconsistency.","dependencies":[{"issue_id":"stellata-3c8","depends_on_id":"stellata-a7d.2.11","type":"related","created_at":"2026-05-05T18:42:54Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-n1t","title":"Consolidate debug panels into one, with bigger sliders, collapsible sections, and drag-to-move","description":"The dev tooling has accreted three separate floating panels — debug.panel() (star tuning + milkyway tuning), debug.perf() (FPS + per-section frame timing), and debug.pin() (pin diagnostic HUD) — each with its own toggle, position, and styling. Cross-referencing them during calibration is awkward (especially when investigating a star-rendering issue that also involves perf characteristics).\n\nConsolidate. Acceptance criteria:\n\n1. Single 'debug panel' surfaces all three: tuning controls (star + milkyway), perf HUD readouts, pin HUD readouts. One toggle (debug.panel() or window.debug, pick one canonical entry point — keep the others as console aliases for muscle memory if useful).\n\n2. Each section is collapsible (click section header to fold/unfold). Collapse state persists across panel re-toggle within the page session (sessionStorage is fine; not localStorage — calibration state should reset on reload).\n\n3. Sliders are bigger. Today the tuning-panel sliders are ~tiny — hard to grab and to make fine adjustments. Aim for at least ~250 px wide and tall enough to click reliably without missing.\n\n4. The whole panel is draggable within the viewport — grab a header bar and move it. Position persists across re-toggle within the session. Don't fight the user-select:text on readouts (the pin HUD body is selectable for copy-paste; that has to keep working — only the drag-handle area should grab pointer events).\n\nFiles:\n- src/client/debug.ts — entry points (window.debug.* registration)\n- src/client/debug-panel.ts — generic chrome (slider/colour/section helpers)\n- src/client/star-tuning.ts, milkyway-tuning.ts — tuning sections\n- src/client/perf-hud.ts — perf HUD\n- src/client/pin-debug-hud.ts — pin diagnostic HUD\n\nOut of scope: reworking the actual readouts/knobs each section exposes — that's pure UX polish for the hosting frame, not the contents.","notes":"PR #44 open: https://github.com/alexmensch/stellata/pull/44. Branch worktree-stellata-n1t-debug-panel-unify. Will close after merge.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-04T08:06:28Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:20:29Z","started_at":"2026-05-08T20:01:01Z","closed_at":"2026-05-10T08:20:29Z","close_reason":"PR #44 merged 2026-05-09 (commit 2d86a34): unified debug panel with drag + collapsible sections shipped","dependencies":[{"issue_id":"stellata-n1t","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:19:18Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.10","title":"Spectral/temperature correction to appSize (V-band → human-eye perceived brightness)","description":"The appSize formula uses appMag = absmag + DM where absmag is V-band (Johnson V, centred near 550 nm). The user raised: shouldn't perceived brightness depend on the star's spectral class / temperature, since the human eye is more sensitive to some wavelengths than others and stars have different spectral distributions?\n\n# Status of V-band as a proxy\n\nV-band IS approximately photopic-weighted — Johnson V peaks at ~550 nm, the human photopic luminosity function peaks at 555 nm with similar bandwidth. So the catalog's V-band absmag ALREADY encodes the eye's spectral sensitivity to first order. This is the right primary quantity to use; refinements are small.\n\n# What V-band misses\n\nFor very hot or very cool stars there's a residual mismatch:\n- **Hot blue stars (O, B, hot A)**: peak emission in UV, V-band underestimates perceived brightness slightly because the eye picks up the deep-blue tail of their spectrum.\n- **Cool red stars (M dwarfs and especially M giants/supergiants like Betelgeuse, Antares, Mira)**: peak emission in NIR. V-band underestimates because the eye still responds to the long red tail (extending past V-band's red cutoff toward photopic's red tail).\n\nThe standard astrophysical correction is the **bolometric correction (BC)**, but BC is for total power (bolometric magnitude), which is too aggressive for a visual model — we want the human eye's integrated response, not all wavelengths. A 'photopic correction' table (or a polynomial in T_eff or B-V colour) would be the right tool.\n\n# What BC_photopic touches — and what it does NOT\n\nThree rendering axes, only one of which BC_photopic touches:\n\n| Axis | Driver | BC_photopic role |\n| --- | --- | --- |\n| **Chromaticity (RGB ratio)** | Blackbody → sRGB LUT keyed on Teff (`stellata-zsr.3` / PR #89; refined by `stellata-zsr.4` once Apsis Teff is wired) | None — LUT is a chromaticity transform; BC is a luminance quantity, so chromaticity is BC-free by construction. |\n| **Disc angular size (close-range pass)** | `R_physical / d` from Stefan-Boltzmann + V-band absmag + Teff | None — disc size stays geometric. V-band absmag is already a reasonable photopic proxy for the radius derivation; full bolometric is \"too aggressive\" for the visual model. |\n| **Luminance (point-source glow + disc per-pixel intensity)** | `appMag` in the glow pass; intrinsic surface brightness in the disc pass | This is the axis BC_photopic corrects. Cool M giants get a small luminance bump; hot O/B stars get a smaller one. |\n\nSo this bead's correction lands on appMag (which drives the perceptual glow) and — verified at execution time — on the disc pass's per-pixel intensity if and only if it currently wires V-band absmag into surface brightness. Disc angular size doesn't change. Disc colour doesn't shift.\n\n# Proposed approach\n\n1. **Teff source: reuse zsr.4's routing helper.** `pickTeffSource(record) → { teff, via }` from `stellata-zsr.4` already returns a single canonical per-star Teff (priority: Apsis gspphot → Apsis gspspec → Ballesteros(B-V) → spectral-class T_TABLE → WD subclass → solar). Consume this helper directly — no separate Teff path for BC_photopic.\n2. **BC_photopic(T_eff) polynomial fit.** Lives in `scripts/catalog-pure.ts` as a pure helper. Small everywhere (max ~0.3 mag in the M-supergiant tail, ~0.2 mag at the O end), zero near solar T_eff (Sol is the calibration anchor).\n3. **Apply in shader**: `appMag_perceived = absmag + DM + BC_photopic(T_eff)` — BC is generally negative for cool stars (perceived brighter than V suggests) and slightly negative for hot ones. Replaces the current `appMag` everywhere the glow pass reads it. Audit the disc pass at execution time — if it reads `appMag`-derived intensity, apply there too (not chromaticity, not size).\n\nThe \"store BC per star in catalog.bin\" branch that the earlier version of this description considered is no longer needed: PR #89 already brought Teff into the shader at render time, `stellata-dch.49` will surface Apsis Teff per record (~85% coverage), and `stellata-zsr.4` provides the canonical Teff-picking helper. Computing `BC_photopic(teff)` in-shader on top of those is ~3 lines plus the polynomial.\n\n# Why this matters more after a7d.2.7's soft-saturation lands\n\nOnce appSize stops saturating at very bright magnitudes, every mag of difference DOES translate to a perceived size difference. So small spectral corrections become VISIBLY relevant where they're currently absorbed by the saturation.\n\n# Why this is P3 (not P2)\n\nV-band is already first-order correct. The visual difference from a 0.2 mag BC correction is small — the star-exaggeration slider amplifies it but doesn't make the absence of BC feel broken. This is a refinement, not a fix; can land later in the zsr epic without blocking anything.\n\n# Files likely touched\n\n- `scripts/catalog-pure.ts` — BC_photopic(Teff) polynomial as a pure helper, exported.\n- `scripts/catalog-pure.test.ts` — pin BC at reference points (Sol = 0; M0V; M2 supergiant; B0V; O5V) with `toBe` per [[stellata-test-coverage-discipline]].\n- `src/client/shaders/star.vert.glsl` — apply `BC_photopic(teff)` to appMag before the brightness compute. Reuse the Teff that `stellata-zsr.4`'s routing helper picks; do not re-derive.\n- `src/client/shaders/blackbody-lut-pure.ts` (or sibling) — if a GLSL BC helper is added, mirror it as a pure TS function for tests.\n- SCIENCE.md — document the BC source / methodology at commit time per [[defer-doc-updates]].\n\n# References to track down\n\n- Bessell, Castelli, Plez (1998) bolometric corrections.\n- Wikipedia 'Bolometric correction' for the basic shape.\n- Photopic luminosity function (CIE 1924 / 1931).\n\n# Acceptance\n\n- BC_photopic(Teff) applied to appMag in the navigate-mode brightness compute, consuming the Teff that `stellata-zsr.4`'s routing helper returns.\n- If the disc pass also reads V-band-derived intensity, BC applied there too (verified at execution time); disc angular size and disc chromaticity unchanged.\n- Sol unchanged (BC ≈ 0 by calibration).\n- M giants visibly brighter than V-band suggested; B stars subtly brighter.\n- SCIENCE.md updated with the source / formula.\n- Test for BC_photopic at known reference points (Sol = 0, M0V ≈ -0.1, etc.) pinned with `toBe`.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-04T07:28:13Z","created_by":"Alex Marshall","updated_at":"2026-05-18T19:46:50Z","dependencies":[{"issue_id":"stellata-a7d.2.10","depends_on_id":"stellata-zsr","type":"parent-child","created_at":"2026-05-06T20:34:10Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-a7d.2.10","depends_on_id":"stellata-zsr.4","type":"related","created_at":"2026-05-18T20:46:55Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-e64","title":"Rendering-decisions FAQ / page: physical model behind size, brightness, perception","description":"Create a user-facing reference page (or FAQ) that explains the rendering decisions in Stellata's physical model, in the same level of detail as the a7d.2.7 investigation summary. Aimed at users (not just maintainers) who reasonably ask things like:\n\n- 'Why don't stars appear very different in size at relatively similar distances?'\n- 'Why does the disc start appearing only when I get close?'\n- 'What's the relationship between brightness and apparent size?'\n- 'Is what I see physically accurate, or stylised?'\n- 'What does the star-exaggeration slider actually do?'\n\n# Suggested structure\n\n1. **What you're seeing.** The three rendering passes (core depth-mask, opaque disc for close range, additive point-glow for distant). Brief diagram or photo pair (close vs distant of the same star).\n2. **Two visible-size sources.** `physSize` (true angular diameter 2·atan(R/d) projected to pixels) and `appSize` (Gaussian-PSF model perceived radius ∝ √brightness-above-threshold). Stars take `max(appSize, physSize)` — so the dominant cue depends on distance.\n3. **The PSF model.** Why brightness affects perceived size at all (point source + atmospheric/instrumental PSF + eye threshold). Why the curve is √Δm and not linear. Saturation behaviour and the user-controllable K (star-exaggeration) knob.\n4. **Why distant stars don't differ in apparent size much.** At 100s of pc all stars are far below the angular-resolution limit; size is brightness-driven; the brightness-to-size formula intentionally compresses (no one star should fill the screen).\n5. **What the close-range disc shows.** When physical disc emerges; the variability pulse modulating disc radius for known variables; binary companion handling.\n6. **What's NOT modelled.** Bolometric vs V-band corrections, atmospheric reddening (other than dust extinction), proper-motion through time, T-component of variable brightness change.\n7. **Chart mode caveat.** Chart mode uses a separate flat-disc sizing (NOT the PSF model) — it's stylised on purpose, like a sky atlas. Don't compare chart-mode visuals to navigate-mode physical fidelity.\n\n# Where it lives\n\nTwo reasonable options:\n\n(a) New top-level user doc at `docs/rendering-decisions.md` (or similar), linked from CLAUDE.md's documentation index AND from a new help-modal tab. Existing `docs/rendering.md` is maintainer-focused; this would be the user-facing companion.\n\n(b) Embed as a section in an existing user-facing artefact — README, info-modal, brand-modal — so it surfaces from a click without a separate page.\n\nUser's framing 'FAQ or a dedicated page' suggests (a) is acceptable. Worth checking whether the brand-modal / info-modal could grow a 'How rendering works' tab to surface it in-app.\n\n# Source material\n\nThe level of detail in the a7d.2.7 investigation summary (the closing notes there) is the target. Pull from:\n- docs/rendering.md (maintainer-facing — has the formulas + design intent)\n- SCIENCE.md (physics references and citations)\n- stellata-a7d.2.7 closing notes (specifically: PSF model rationale, why distant stars converge in size)\n- stellata-a7d.2.10 outcomes (spectral correction details, when written)\n\n# Acceptance\n\n- Page exists and is reachable from at least one in-app entry point.\n- Covers the seven sections above (or a clean reorganisation of them).\n- Plain-English explanations alongside any formulas — not maintainer-doc-style.\n- Updated when behaviour changes (e.g. when a7d.2.7's saturation soften lands, the page reflects it).","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-04T07:27:51Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:38Z","dependencies":[{"issue_id":"stellata-e64","depends_on_id":"stellata-8c1","type":"parent-child","created_at":"2026-05-10T22:53:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.9","title":"Smooth warp transition: cinematic flyby of source star with arc/clothoid fillet","description":"# Smooth warp transition: cinematic flyby of source star with arc/clothoid fillet\n\nThe warp animation has two readability problems that read as \"pops\":\n\n1. **Source-side pop.** The reorient lerps the camera to put A between\n   pStart and B, and *then* starts the bulk acceleration — the close\n   approach to A is essentially over in the first frame or two of the\n   bulk because position-time is symmetric while angular-size-vs-distance\n   goes as 2·atan(R/d). The user perceives A as instantly disappearing\n   instead of being a flyby.\n\n2. **Destination-side pop.** Mirror problem at arrival. With a symmetric\n   accelerate/decelerate position-time profile, angular size grows\n   nonlinearly in the last few endOffsets and B \"pops into existence\"\n   instead of smoothly arriving.\n\n# Reframing — fly-by, not in-line deceleration\n\nThe user mental model is a **cinematic flyby of the source star**:\nstarting from whatever orientation the camera has at warp-start, smoothly\ntransition into the A→B warp path so the camera \"swings past\" A as it\naccelerates. This is a smooth join between two line segments:\n\n- Start tangent: the camera's current direction (or current radial-from-A\n  vector — depends on launch pose).\n- End tangent: +forward along A→B.\n\nThe term-of-art for joining two line segments at a waypoint with C¹\n(tangent) continuity is an **arc fillet**. If we also want C² continuity\n(curvature continuity, no centripetal jerk step) — the right choice if\nwe're simulating actually flying along the curve in space — that's a\n**clothoid transition** (Euler spiral, linearly varying curvature along\narc length). Either gets us off the A↔B line so the camera literally\n*flies past* A instead of through it.\n\n# Failed prior attempt — preserved on archive branch\n\nA first attempt was made in May 2026 implementing an **in-line angular\nease**: extended the reorient with a 1/mag-linear tail (camera moves\ndown the A→B axis past A) and replaced the bulk's symmetric ease with\nbulk + 1/d_B-linear arrival. Approach discarded because it still treats\nA as a point on the warp axis the camera passes through — wrong mental\nmodel. The \"reorient + departure tail + bulk + arrival\" partition itself\nwas conceptually useful; the right reframe is to replace the in-line\ndeparture with a fillet/clothoid that curves *off* the A↔B line.\n\nThe work is preserved on branch `archive/a7d.2.9-inline-angular-ease`\n(pushed to origin). Single commit on top of the a7d.2 worktree HEAD at\nthe time of archival. To inspect:\n\n```\ngit fetch origin\ngit log archive/a7d.2.9-inline-angular-ease -1\ngit diff archive/a7d.2.9-inline-angular-ease~1 archive/a7d.2.9-inline-angular-ease -- src/client/stellata.ts\n```\n\nOr check it out and run the warp from the dev server to feel why it\ndoesn't deliver the cinematic-flyby effect the user wants.\n\n# Effect B — float32 projection drift then pin snap (separate concern)\n\nDuring warp the floating origin sits on the source star, so the\ndestination's worldPos magnitude is ~|AB| (e.g. ~265 pc for Sol→Rigel).\nGPU float32 precision at that magnitude is ~|AB|·1e-7 ≈ 2.6×10⁻⁵ pc —\ncomparable to endOffset (~3.4×10⁻⁵ pc for Rigel). The projected centre\ncan drift by a noticeable fraction of a screen in the final approach.\nThe pin (uPinFocusToCenter) does NOT engage during warp, so the drift\nis visible. At finishWarp, recenterOrigin moves the origin to the dest\n→ projection precision becomes essentially exact → pin engages → star\nsnaps to its mathematically perfect NDC position.\n\nMay or may not still be visible after the flyby fix. Mitigations if\nneeded: generalise uPinFocusToCenter to non-origin pin targets and pin\nthe dest to its lookAt-projected position during the final phase of\nwarp (requires passing target NDC or world position as a uniform).\n\n# Investigation order\n\n1. Implement the flyby curve (arc fillet first; upgrade to clothoid if\n   the centripetal jump is perceptible). Arc length parametrisation +\n   angular ease at arrival.\n2. Re-evaluate the destination pop. The flyby reframes the source side\n   only; arrival can still keep the 1/d_B-linear angular ease design.\n3. If recenter/pin-snap is still visible, address Effect B with the\n   simplest sufficient mechanism.\n\n# Files\n\n- src/client/stellata.ts — startWarp, updateWarp, finishWarp.\n- src/client/shaders/star.vert.glsl — only if Effect B needs a\n  generalised pin.\n\n# Acceptance\n\n- Warp start reads as a continuous flyby of the source star — no\n  perceived teleport across A, A's apparent size grows smoothly as the\n  camera curves past.\n- Warp arrival has B's disc grow at a perceptibly uniform angular rate;\n  no pop at parking.\n- C¹ continuity at the waypoint(s); C² preferred to avoid centripetal\n  jerk if simulating physical flight.\n- No regression on short warps (Sol → α Cen) where the existing profile\n  already feels fine.\n\n# Why descoped from a7d epic\n\na7d is about replacing the **physical geometry** of star rendering —\ndisc formula, orbit floor, and parking distance based on true angular\ngeometry. Warp camera animation is **visual/UX polish** built on top of\nthose parking distances; the transition's smoothness is independent of\nwhether the underlying physical model is right. Nice-to-have, not\non the a7d critical path.\n","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-03T19:38:52Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:03:52Z","started_at":"2026-05-05T19:05:49Z","dependencies":[{"issue_id":"stellata-a7d.2.9","depends_on_id":"stellata-zsr","type":"parent-child","created_at":"2026-05-06T20:34:07Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-a7d.2.5","title":"Refresh docs at end of a7d.2 PR","description":"User explicitly deferred docs updates until they accept the feature is ready to merge. Per the global defer-doc-updates memory: don't edit docs/, CLAUDE.md, README.md, or SCIENCE.md during implementation; do it as the FINAL step before requesting merge.\n\n# Already-committed docs that may need a refresh\n\nBranch worktree-a7d.2-physical-disc-geometry has commit 1be9c0c \"docs: refresh rendering.md + camera-modes.md for angular geometry\" applied during the original implementation pass. These predate:\n- commit 5035edf (uPinFocusToCenter pin + GLOBAL_MIN_DIST_PC bump)\n- any work for stellata-a7d.2.1 / .2 / .3 / .4\n\nSo they're STALE relative to the final shape. Don't re-edit them mid-flight. Audit at PR-ready time.\n\n# Process\n\n1. After all sub-issues (.1, .2, .3, .4) are closed and the user signals \"feature is ready to merge\":\n2. `git diff main..HEAD -- src/ scripts/ public/` — full final code diff.\n3. Open docs/rendering.md, docs/camera-modes.md, CLAUDE.md, SCIENCE.md.\n4. Update only what's now stale: orbit-floor formula, parking-distance formula, GLOBAL_MIN_DIST_PC value, uPinFocusToCenter mechanism, focus-ring/scale-bar UX changes, focusStar() default, warp pStart formula.\n5. Don't add unsolicited new content; trim what no longer applies.\n\n# Acceptance\n\nDocs accurately describe merged code. User signs off before merge.","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-03T18:55:45Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:28:47Z","closed_at":"2026-05-06T21:28:47Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-a7d.2.5","depends_on_id":"stellata-a7d.2","type":"parent-child","created_at":"2026-05-03T19:55:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-jf4","title":"Hover tooltip distance: from-Sol vs from-camera decision","description":"The hover tooltip on a star (and cloud) currently always reports distance-from-Sol, but that label is ill-defined when the camera isn't at Sol — particularly inside OBSERVE mode where the user is conceptually \"standing on\" a focused star.\n\n# Current behaviour\n\nsrc/client/main.ts:223-228 (describeStarDetailed):\n\n```ts\nconst p = catalog.positions;\nconst dist = Math.sqrt(p[idx*3]**2 + p[idx*3+1]**2 + p[idx*3+2]**2);\nconst ctx = [con, fmtDist(dist)].filter(Boolean).join(' · ');\n```\n\n`catalog.positions` is absolute ICRS Sol-centered pc, so the tooltip distance is always Sol-distance no matter where the camera is.\n\nsrc/client/main.ts:201 — cloud hover uses `c.distanceFromSol` for the same reason.\n\n# Decision options\n\nA. **Always Sol-distance (status quo)** — simplest, consistent with the bottom-left scale bar's reference point. Reads weirdly when you're parked on Capella in observe and a tooltip says \"Procyon · 11.4 pc\" — pc *from where?*\n\nB. **Always camera-distance** — \"how far away is this from where I am\". Right answer in OBSERVE; useful in navigate too when scouting around (\"the cluster I'm looking at is 50pc, this star is 12pc closer\"). Loses the absolute-scale anchor that some users may rely on.\n\nC. **Camera-distance in observe, Sol-distance in navigate** — least surprising per mode, but doubles the mental model and makes the tooltip's meaning state-dependent.\n\nD. **Both** — show Sol-distance always, plus camera-distance when the camera is non-trivially far from Sol. Cluttered but most informative.\n\nE. **Distance from focused star** when one is focused, otherwise from Sol — matches the framing the user is currently in (\"how far from the thing I'm looking at\"). The focused star is the intuitive reference frame for orbit-mode exploration; falls back gracefully on no-focus.\n\n# What to consider before deciding\n\n- Camera-relative distance ties into floating-origin math but is cheap (Vector3 distance from camera.position to local star pos).\n- Cloud hover: same decision applies. Whatever label scheme is picked needs to extend to `describeCloud` consistently.\n- The \"Distance · X\" line in cloud tooltip is currently labelled identically to star tooltips' implicit Sol-distance — a label change (e.g. \"Distance from Sol · X\" vs \"Distance · X\") is a separate clarity question.\n- This may interact with the bottom-left scale bar (currently a Sol-relative reference) — if we shift the tooltip to camera-relative, do we want a similar shift elsewhere?\n\n# Deliverable\n\nA written decision attached to this issue (which option, with rationale), then a follow-up implementation task.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-03T17:35:59Z","created_by":"Alex Marshall","updated_at":"2026-05-03T17:35:59Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-h1j","title":"Investigate luminosity-class differentiation in star rendering (red dwarf vs red giant)","description":"Audit how well the current star renderer reflects the physics of different luminosity classes. Concrete observation: Betelgeuse (M1-2 Ia-ab red supergiant, ~10^5 Lsun, R ~ 700-1000 Rsun) and Barnard's Star (M4 V red dwarf, ~0.0035 Lsun, R ~ 0.2 Rsun) do not look as different as they should — a red supergiant and a red dwarf are ~7-8 orders of magnitude apart in luminosity and ~3-4 orders in radius, so they should read as visually distinct on screen. May be partly an apparent-magnitude artefact (Barnard's is 9.5 ly away, Betelgeuse is ~550 ly), but the suspicion is that the disc-radius / glow profile is collapsing the giants and dwarfs toward similar visual sizes once the camera is close to either.\n\nWhat to investigate:\n- How physicalRadius() in scripts/catalog-pure.ts handles luminosity class — does it actually pull dwarfs down and giants up, or does absmag dominate and class get washed out?\n- The disc/glow super-Gaussian profile (docs/rendering.md): does the plateau-vs-Gaussian morph differentiate I/II/III from IV/V at typical camera distances, or does it converge?\n- Spectral-class colour: are M dwarfs and M giants colour-coded differently? They have similar surface temperatures but very different radii — colour alone won't distinguish them.\n- What known stars to A/B compare for ground truth: Betelgeuse vs Barnard's Star, Antares vs Proxima, Aldebaran vs 61 Cygni, Rigel (B8 Ia) vs Sirius (A1 V). Note their canonical L/R values and check what Stellata renders.\n\nDecide whether the visual gap actually reflects physics or whether the renderer needs a luminosity-class-aware adjustment. This is exploratory — the outcome may be 'it's correct, the dynamic range just looks compressed' or 'we need a class-dependent size/brightness multiplier'.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-03T11:19:06Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:22:08Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-kdq","title":"dust-loader: re-upload chunks on WebGL context loss","description":"Carved out from stellata-adr (which originally bundled buffer-size validation + context-loss handling). Buffer validation landed; context-loss handling is the remaining piece.\n\n# What's missing\n\nsrc/client/dust-loader.ts:226-258 — uploadChunk uses the (undocumented) Three.js renderer.properties.get(tex).__webglTexture path to push voxel data with gl.texSubImage3D. There is no listener for WebGL context loss / restore. On context loss, the GPU texture is destroyed; on restore Three.js reallocates a fresh zero-filled texture, but our uploaded chunks are gone and we never re-upload them, leaving permanent black holes in the dust grid until full page reload.\n\n# Fix options\n\nTwo designs, neither obviously the right call:\n\na) Cache the per-chunk Uint8Array buffers and re-upload on 'webglcontextrestored'. Memory cost: ~128 MiB resident (one byte per voxel × 64 chunks × 2 MiB).\n\nb) On 'webglcontextrestored', re-fetch all chunks via the existing fetchAndUpload pipeline. No memory cost, but re-downloads ~128 MiB and re-fills progressively (same UX as initial load).\n\nOption (b) is probably right: dust is decorative, and context loss is rare enough that an extra fetch is fine. The browser disk cache should also serve the chunks back without hitting the network.\n\n# Acceptance\n\n- canvas listens for webglcontextlost (preventDefault) + webglcontextrestored\n- on restore, all previously-loaded chunks repopulate the texture\n- particles.bin is also re-uploaded (or its mesh re-attached) — relevant for stellata.attachDustParticles consumers\n- repro by triggering loseContext via the WEBGL_lose_context extension in dev console","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-03T10:41:06Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:39Z","dependencies":[{"issue_id":"stellata-kdq","depends_on_id":"stellata-adr","type":"discovered-from","created_at":"2026-05-03T11:41:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-drh","title":"Toliman at min distance: dark halo ring around disc — likely companion overlap","description":"Visual artifact observed when zooming to minimum distance on Toliman (α Cen B, HIP 71681) in navigate mode.\n\n# What you see\n\nA single bright disc fills the lower-left of the viewport (per user screenshot). Just outside the bright core is a clearly-visible darker gray ring/halo, then the falloff resumes normally. The halo reads as a separate object's outline rendered behind/inside the focused star's disc.\n\n# What's happening (working theory)\n\nα Cen is a tight system: A (Rigil Kentaurus) and B (Toliman) sit ~24 AU apart on average — within the 0.005 pc geometric-binary radius. At minimum-distance focus on Toliman, the camera ends up inside or extremely close to the *other* component's render volume, so we're rendering the companion's disc geometry on top of (or coincident with) the focused star.\n\nThe dark ring is consistent with the *companion's* core depth-mask being written under the focused star's outer falloff: the depth mask occludes the additive glow contribution from the other star, leaving a dark band where one star's mask sits inside the other's halo.\n\n# Likely resolves under stellata-a7d\n\nstellata-a7d (Physical-scale rewrite of star sizing \u0026 close approach) replaces the close-approach pipeline with true angular geometry. Under that model, two stars 24 AU apart at any reasonable zoom are spatially resolved — they won't share screen pixels at min-distance focus the way they do today. So this bug should fall out for free once a7d lands.\n\n# Verify before fixing\n\n- Confirm the dark ring is the companion's core mask (toggle the companion off / hide its disc and see if the halo disappears).\n- If a7d is months out and this is still annoying, a smaller fix is to push the focused star's disc-pass forward in depth or skip rendering the companion when it's within the focused star's screen extent.\n\n# Repro\n\nFocus 'Toliman' in navigate mode, manual-zoom all the way in until the disc fills ~½ the viewport's smaller dimension. The dark halo ring is visible just outside the bright core.","status":"closed","priority":3,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-03T08:35:26Z","created_by":"Alex Marshall","updated_at":"2026-05-04T09:07:28Z","closed_at":"2026-05-04T09:07:28Z","dependencies":[{"issue_id":"stellata-drh","depends_on_id":"stellata-7gs","type":"supersedes","created_at":"2026-05-04T10:07:27Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-drh","depends_on_id":"stellata-a7d","type":"related","created_at":"2026-05-03T09:35:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-1et","title":"Touch gestures: pinch-to-zoom + multi-touch behaviour on iPad / touch devices","description":"The mobile epic body already flags 'no pinch-to-zoom, two-finger roll is desktop-Safari-only by design' as a known gap. Even on iPad-sized viewports — which 47c.3 may decide is the only fully-supported mobile target — the current camera doesn't support pinch-to-zoom, so 'works on iPad' is currently aspirational.\n\nWHAT'S THERE TODAY (src/client/stellata.ts + observe-controls.ts):\n- TrackballControls (Three.js) drives the navigate camera. Its built-in touch handling is single-finger orbit + (we believe) two-finger pan. Pinch-to-zoom is NOT a TrackballControls feature.\n- observe-controls.ts handles the OBSERVE camera (drag yaw + pitch, wheel FOV). No touch pinch.\n- Two-finger roll exists ONLY on desktop Safari (gesturestart/change events) per docs/camera-modes.md.\n- touch-action: none is set on #scene to prevent default browser gestures on the canvas.\n\nWHAT NEEDS WORK:\n1. Verify on iPad: do single-finger orbit + double-tap-zoom + the pan gesture actually work via TrackballControls' touch path, or are they broken too?\n2. Pinch-to-zoom (navigate mode):\n   - Map pinch span change to camera distance (matches scroll-wheel behaviour).\n   - Implement via TouchEvent (iOS-friendly) since gesture* events are Safari-desktop-only.\n3. Pinch-to-FOV (observe mode):\n   - Same gesture, mapped to camera.fov instead of distance.\n4. Decide what the two-finger drag does:\n   - On desktop today: nothing in canvas (mouse drag = orbit, wheel = zoom).\n   - On touch: Three.js TrackballControls treats two-finger drag as pan. We may want to keep that, or override to roll, or disable.\n5. Single-finger drag in OBSERVE mode:\n   - Currently observe-controls.ts uses pointerdown/move; should already work on touch since pointer events fire for touch. Verify.\n6. Confirm the touch-action: none on #scene doesn't kill the gesture handlers we add.\n\nCRITICAL FILES:\n- src/client/stellata.ts — TrackballControls instance, existing onTouchStart/Move/End (currently used only for two-finger roll detection).\n- src/client/observe-controls.ts — OBSERVE camera; pointer-event-based.\n- node_modules/three/examples/jsm/controls/TrackballControls.js — to understand existing touch behaviour before extending.\n\nOUT OF SCOPE: changing the desktop control scheme; replacing TrackballControls wholesale (the epic explicitly excludes that).","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:46:09Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:37Z","dependencies":[{"issue_id":"stellata-1et","depends_on_id":"stellata-47c","type":"parent-child","created_at":"2026-05-02T20:47:17Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-qsg","title":"Decision: minimum viewport policy + mobile gating UX","description":"Decide how Stellata behaves on viewports too small for the desktop-tuned layout. The current settings panel + topbar overlap the canvas badly on phone-width devices, and full-bleed is impractical in browser iOS Safari (see 47c.2 notes). Several distinct UX paths are possible — pick one before sinking effort into per-control mobile redesigns.\n\nOPTIONS to evaluate (these are the design space; deliverable is a chosen direction + threshold + copy):\n\nA. Hard block below threshold\n   - At \u003cX width, replace the app with a simple gate: 'Stellata is built for tablet and desktop. Open this on a larger screen, or Add to Home Screen for an immersive full-screen experience on this device.'\n   - Pros: zero effort tuning per phone breakpoint; one consistent fallback.\n   - Cons: refuses users; may feel aggressive.\n\nB. Soft warning modal (dismissable)\n   - First-launch detect: small viewport → show modal explaining caveats + offering 'Add to Home Screen' instructions, dismissable to continue with degraded UX.\n   - Pros: respects user agency.\n   - Cons: more code, easy to dismiss-and-forget.\n\nC. Banner/toast (always visible)\n   - Persistent thin banner: 'Best on tablet/desktop · Add to Home Screen for full-screen'.\n   - Pros: low-effort.\n   - Cons: takes vertical space; nags.\n\nD. Just polish best we can, no gating\n   - Spend the effort making the panel collapsible, controls touch-friendly, modals reflow.\n   - Pros: most users get a usable experience.\n   - Cons: a lot of work, never quite as good as desktop, harder to QA.\n\nE. Hybrid: D for tablet (≥768 width), A or B for phone (\u003c480 or \u003c600 width)\n\nTHRESHOLD QUESTION: where's the cutoff?\n- iPad mini (portrait) is ~768 width × 1024 height — likely OK with desktop layout.\n- iPhone Pro Max (portrait) is ~430 width × 932 height — settings panel covers ~65% of width.\n- iPhone SE (portrait) is ~375 width — even worse.\n- Landscape phones (e.g. 932 × 430) have ample width but very short height.\n\nRecommend deciding on width-based gating in portrait; treat landscape phone as 'works but tight'.\n\nADD-TO-HOME-SCREEN COPY:\n- If we recommend AtHS in the gating UI, draft the instructions inline. Apple's icon for the share sheet has changed across iOS versions; either show a generic step-by-step or a screenshot.\n\nINPUTS to this decision (gather before deciding):\n- Walk through panel + modals on real iPhone (covered by 47c.1).\n- Render perf at iPhone resolution: how bad is it really? (Also 47c.1.)\n- Telemetry: do we have any data on phone visits today? (Probably not; treat as unknown.)\n\nDELIVERABLE: a written decision (which option + threshold + AtHS copy) attached to this issue, then file follow-up issues under 47c that *implement* that decision.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:45:48Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:37Z","dependencies":[{"issue_id":"stellata-qsg","depends_on_id":"stellata-47c","type":"parent-child","created_at":"2026-05-02T20:47:16Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-nqb","title":"Roll-up: small comment/WHY/dead-code cleanups from code review","description":"Surfaced in code review (stellata-y84). Roll-up of small findings worth fixing in one cleanup pass; each is 1-3 lines and low-risk.\n\n**Comment / WHY gaps**\n- src/client/shaders/star.vert.glsl:201-216 — magic constant `VAR_TROUGH_FLOOR_FRACTION = 0.2` has no comment. Add a one-line WHY (\"perceptual floor below which a variable reads as 'disappeared' rather than 'dimmed'\").\n- scripts/catalog-pure.ts:34 — comment says \"C/S/W (cool carbon)\" but code also matches N and R; extend to \"C/S/W/N/R\".\n- scripts/catalog-pure.ts:113-123 — `interpolate` fallthrough is unreachable in practice; comment as such or assert.\n- src/client/galactic-coords.ts:36 — note that `galZ` ends up parallel-to-but-not-equal-NGP after re-orthogonalisation.\n- src/client/catalog-loader.ts:130 — note \"byte 37: padding\" after the amplitude read.\n- src/client/catalog-loader.ts:144 — extend the existing nameOffset=0 sentinel comment with \"(allows nameOffset=0 to mean 'no name')\".\n\n**Small dead/redundant**\n- src/client/main.ts:74 — declare `window.stellata` in src/client/globals.d.ts instead of `as unknown as` cast.\n- src/client/url-state.ts:142-144 — `fixed(n)` returns two functions both ignoring args; signatures are misleading.\n- src/client/url-state.ts:32 + src/client/stellata.ts:73 — `ALL_SPECT_MASK` defined twice; export from one and import in the other.\n- src/client/warp-button.ts:11 — `as unknown as SVGGElement` is a type lie; type as `Element` (no cast needed for `addEventListener`).\n- src/client/observe-controls.ts:62-65 — fields could be `readonly`.\n\n**Shader micro-wins**\n- src/client/shaders/cloud.frag.glsl:29 — replace 4-component dot product with `abs(normalize(vNormalView).z)`.\n- src/client/shaders/star.vert.glsl:203-217, 291-294 — `baseSize0`, `logSpan0`, `logRatio0`, `sizeAtRef0` are computed twice (variability branch and non-variable branch); compute once outside.\n\n**Galactic disc**\n- src/client/galactic-disc.ts:131-156 — `makeRing` allocates a fresh `THREE.LineBasicMaterial` per ring (6 rings, all visually identical). Use one shared material; opacity update writes once.\n\n**Star tuning panel**\n- src/client/star-tuning.ts:13 + src/client/milkyway-tuning.ts:97 — closure-captured initial values become stale if `setStarRenderParams` / `setReddeningRGB` is called from elsewhere (URL state, future presets). Either document the no-reverse-sync, or read live state on each `onChange`.\n\n**Style**\n- src/client/molecular-clouds.ts:48-50 — `meshIndex: Map\u003cstring, number\u003e` keyed by `mesh.uuid`. Store `mesh.userData.cloudIdx = i` at construction; raycast reads `hits[0].object.userData.cloudIdx`. Drops the Map.\n- src/client/poi-overlay.ts:373 — `catalog: { hip: Uint32Array }` structural-typed param hides dependency; use `Catalog`.\n- src/client/keyboard-shortcuts.ts:99-112 — `cTapTimer` non-null sentinel for \"first tap pending\" reads better as `pendingFirstCTap` boolean alongside the timer.\n\nSeverity: LOW/MEDIUM (cleanup pass; safe single-line fixes).\n","notes":"PR #47 open: https://github.com/alexmensch/stellata/pull/47. Branch worktree-stellata-nqb-finish. Will close after merge.\n\nFinal batch (PR #47, 2026-05-10): closing out remaining nqb roll-up items.\n\nDone this PR:\n* catalog-pure.ts T_TABLE row-7 comment: 'C/S/W' → 'C/S/W/N/R' to match the case statement at line 34.\n* catalog-pure.ts interpolate(): added a comment that the post-loop fallthrough is unreachable for in-range subclass keys (0-9 inclusive across all tables), noting it's a defensive clamp.\n* cloud.frag.glsl: replaced abs(dot(normalize(vNormalView), vec3(0,0,1))) with abs(normalize(vNormalView).z) — mathematically identical, one fewer op.\n* poi-overlay.ts: imported Catalog and replaced the structural { hip: Uint32Array } param on labelFor() with the named type.\n* star-tuning.ts + milkyway-tuning.ts: added a header comment documenting the no-reverse-sync (sliders snapshot getters at build time, won't update if URL state / presets write the params later).\n\nSkipped as stale or cosmetic-only:\n* VAR_TROUGH_FLOOR_FRACTION WHY comment — already documented in stellata.ts:212-216 since 9mm.53 lifted the constants out of GLSL.\n* main.ts:74 globals.d.ts cast — already done in earlier work; no cast remains.\n* url-state.ts fixed(n) signature lie — current signatures use _-prefixed unused params, the idiomatic 'I'm ignoring this' marker.\n* star.vert.glsl baseSize0/logSpan0/logRatio0/sizeAtRef0 dedupe — bead's named locals don't appear in current code; the variability vs non-variability split was refactored.\n* keyboard-shortcuts.ts cTapTimer rename to pendingFirstCTap — current code uses the timer handle's null/non-null state as the single source of truth, which is cleaner than a parallel boolean.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:35:08Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:32:15Z","started_at":"2026-05-08T18:25:55Z","closed_at":"2026-05-10T08:32:15Z","close_reason":"Merged in PR #47 — comments/WHY gaps, type narrows, DRY star.vert.glsl, cloud.frag micro-win, star-tuning no-reverse-sync caveat all shipped.","dependencies":[{"issue_id":"stellata-nqb","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:19:18Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-nqb","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:35:07Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-t6l","title":"Remove dead showMolecularClouds plumbing post-shelving","description":"Surfaced in code review (stellata-y84).\n\n`FilterState.showMolecularClouds` was removed from the user UI when the molecular-cloud overlay was shelved for v1.0 (CLAUDE.md), but the field remains threaded through:\n\n- src/client/stellata.ts:55, 334 — interface and DEFAULT_FILTER (always false)\n- src/client/stellata.ts:1180-1189 — `pickCloud` checks the flag (always-false branch)\n- src/client/stellata.ts:2693, 2739 — `clouds.update(worldOffset, false)` is always called with literal false\n- src/client/main.ts:286 — `pickStar`-then-`pickCloud` chain runs through dead path\n\nFix: One of:\n- (a) Remove the flag and the threading through. URL bit 2 stays reserved with a one-line comment.\n- (b) Keep the flag but mark with a single comment \"dead until clouds layer is re-enabled\" and remove the conditional pluming so `clouds.update()` takes no arg.\n\nEither way, drop the `pickCloud` invocation from main.ts since it's currently unreachable.\n\nSeverity: MEDIUM (cleanup; eliminates real dead code paths).\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:35:07Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:14Z","started_at":"2026-05-08T18:25:52Z","closed_at":"2026-05-08T18:54:14Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-t6l","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:19:21Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-t6l","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:35:06Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-075","title":"build-catalog: type CSV rows + SearchEntry + dedupe GCVS pipe-reader","description":"Surfaced in code review (stellata-y84).\n\nscripts/build-catalog.ts uses untyped Records throughout:\n\n1. **CSV row access** — `csv-parse` runs with `cast: false` and rows accessed as `row.x0`, `row.absmag`, `row.spect` etc. Every column is implicitly `any`. A typo'd column name yields `undefined` and silently corrupts the binary.\n\n   Fix: Declare `interface AthygRow { x0: string; y0: string; z0: string; absmag: string; ci: string; spect: string; con: string; ... }` and cast the parser stream once.\n\n2. **Search-index entries** (line 758-769) — built as `Record\u003cstring, string | number\u003e` with stringly-keyed shorthand fields (`p`, `b`, `f`, `c`, `s`, `i`, `hip`, `hd`, `hr`, `gl`). The keys are a public wire contract with src/client/search.ts; one typo here ships a broken index.\n\n   Fix: `interface SearchEntry { i: number; p?: string; b?: string; f?: number; c?: number; s?: string; hip?: number; hd?: number; hr?: number; gl?: string; }`. Build with that type to catch drift between writer and reader.\n\n3. **Related**: scripts/build-catalog.ts:184-214 (`parseGcvsMain`) and 216-256 (`parseGcvsCrossref`) both implement: existsSync → readFileSync → split lines → trim-skip → split on `|`. Extract `function* readPipeDelimited(path)` yielding trimmed-field arrays.\n\nSeverity: MEDIUM (type-safety + DRY in build path).\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:35:06Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:43:41Z","started_at":"2026-05-10T08:32:39Z","closed_at":"2026-05-10T08:43:41Z","close_reason":"Merged in PR #49 (78d4d67)","dependencies":[{"issue_id":"stellata-075","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:19:16Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-075","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:35:06Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-v2j","title":"Catalog binary format: extract shared RECORD_LAYOUT constants + flag-bit registry","description":"Surfaced in code review (stellata-y84).\n\nThe binary catalog format constants (`RECORD_SIZE = 44`, `HEADER_SIZE = 32`, `NO_COMPANION = 0xffffffff`) are duplicated between scripts/build-catalog.ts and scripts/verify-catalog.ts:11. The byte-offset map exists only as scattered comments at scripts/build-catalog.ts:708-714 and 727; e.g. `setUint8(off + 36, ampUnits)`, `setUint16(off + 38, periodUnits)` are bare numeric literals.\n\nA single change to the layout requires touching two files plus the runtime reader (src/client/catalog-loader.ts). Drift between writer and reader silently ships corrupt data.\n\nFix:\n- Extract `const RECORD_LAYOUT = { x: 0, y: 4, z: 8, absmag: 12, ci: 16, physRadius: 20, companion: 24, nameOffset: 28, spectClass: 32, lumClass: 33, conIndex: 34, flags: 35, ampUnits: 36, period: 38, hip: 40, SIZE: 44 }` to a shared module (e.g. `scripts/catalog-pure.ts` or new `scripts/binary-format.ts`).\n- Import from build-catalog.ts, verify-catalog.ts, and src/client/catalog-loader.ts.\n- Use the named offsets in DataView reads/writes.\n\nBonus: a single canonical record-layout block-comment listing every byte (used and reserved) so adding flag bit `0x08` later doesn't collide silently. Currently flag bits `0x08`, `0x20`, `0x40`, `0x80` are unused with no documented reservation.\n\nSeverity: MEDIUM (no current bug, but it's the most realistic vector for a future corrupt-binary regression).\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:35:05Z","created_by":"Alex Marshall","updated_at":"2026-05-08T19:27:18Z","started_at":"2026-05-08T19:21:04Z","closed_at":"2026-05-08T19:27:18Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-v2j","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:19:22Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-v2j","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:35:04Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-77m","title":"Stellata event-bus consolidation: replace eight on*Handlers arrays with typed bus","description":"Surfaced in code review (stellata-y84).\n\nsrc/client/stellata.ts has eight `on*Handlers` arrays and matching `on*Change` getters / `fire*Change` firers (focus, cloud focus, frame, camera mode, pois, vector, filter, ...). ~150 lines of mechanical boilerplate.\n\nFix: Replace with a single typed `EventBus\u003cEventMap\u003e` with `on(name, handler)` / `emit(name, payload)`. Concrete payload types follow through to subscribers in url-state.ts, controls.ts, etc. Eliminates duplication and tightens types at every callsite.\n\nOptional sub-improvement: dedupe handlers on register (Set instead of Array) — currently nothing prevents double-registration.\n\nSeverity: MEDIUM (DRY — biggest single readability win in stellata.ts).\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:34:02Z","created_by":"Alex Marshall","updated_at":"2026-05-15T13:14:37Z","started_at":"2026-05-10T08:44:11Z","closed_at":"2026-05-15T13:14:37Z","close_reason":"Landed in PR #51 (merged commit e5977e6, 2026-05-15)","dependencies":[{"issue_id":"stellata-77m","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:13Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-77m","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:34:01Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-wk2","title":"Unify SearchBox and constellation-typeahead; share escapeHtml","description":"Surfaced in code review (stellata-y84).\n\nTwo related cleanups around search and the typeahead:\n\n1. **`escapeHtml` duplicated** — defined verbatim in search.ts:557-563 and constellation-typeahead.ts:143-149. Move to a shared util and import.\n\n2. **Two parallel implementations of the same UI** — `SearchBox` class in search.ts and standalone `bindConstellationTypeahead` in constellation-typeahead.ts are nearly identical: same input/results/active-row pattern, same listeners, same race-deferred blur, same render markup (`\u003cspan class=\"sub\"\u003e`). The typeahead is essentially a `SearchBox` with a different filter function.\n\nFix: Have `bindConstellationTypeahead` instantiate `SearchBox` with a constellation-specific filter and item renderer. Removes ~150 LOC and a parallel implementation. (This is removing duplication, not adding abstraction.)\n\nSeverity: MEDIUM (DRY + reduces drift risk).\n","status":"closed","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:34:01Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:15Z","closed_at":"2026-05-06T21:19:15Z","close_reason":"duplicate of stellata-xbj (Unify SearchBox and constellation typeahead behind a Typeahead\u003cT\u003e abstraction)","dependencies":[{"issue_id":"stellata-wk2","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:34:01Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-vjj","title":"Arrow shaft + screen-direction helpers shared by hud-overlay and poi-overlay","description":"Surfaced in code review (stellata-y84).\n\nsrc/client/hud-overlay.ts (Sol/GC arrows) and src/client/poi-overlay.ts (observe-mode pinned-star arrows) implement near-identical algorithms:\n\n- aux-step calculation: `auxStepW = (ARROW_PIXEL_LENGTH * dist) / focalPx` with `focalPx = h / (2 * tan(fov*π/360))`\n- screen-direction derivation from world-space dir + camera (with target-projection fallback)\n- shaft-shrink + `buildArrowSvgPath` + label-clamp block (~80 lines duplicated almost verbatim between hud-overlay.ts:283-318 and poi-overlay.ts:328-352)\n\nThe shared utility module `arrow-path.ts` already exists for the SVG path builder; the screen-direction and aux-step math are the natural next thing to live there.\n\nFix: Extract:\n- `screenDirectionFromWorldDir(camera, origin, dir, target, w, h)` → arrow-path.ts\n- `renderArrowAndLabel(path, bg|null, label, ...)` for the shaft+label block\n\nMigrate both overlays to use the helpers.\n\nSeverity: MEDIUM (DRY — bug-fix risk: changes today require touching two places).\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:34:00Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:12Z","started_at":"2026-05-08T18:25:46Z","closed_at":"2026-05-08T18:54:12Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-vjj","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:19:23Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-vjj","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:34:00Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-r48","title":"perf-hud: cache histogram spans, drop unused Stellata arg, single-pass row build","description":"Surfaced in code review (stellata-y84).\n\nsrc/client/perf-hud.ts has two issues:\n\n1. **Per-tick array thrash** (line 147): `Array.from(sections.keys()).filter(...).map(...).sort(...).slice(0,8)` allocates 4+ arrays at 5Hz. Single-pass build would suffice.\n\n2. **innerHTML rebuild** (line 180): `panelEl.innerHTML = ...` reparses the entire HUD HTML, including 60 histogram spans, 5 times per second when the HUD is open.\n\n3. **Unused parameter**: `installPerfHud(stellata: Stellata)` (line 81) doesn't use the parameter; it can be dropped along with the import at line 1 and the call site in debug.ts:64.\n\nFix: Build histogram bars as cached span elements once; update only `style.height` and `style.background` per tick. Build the rows array in one loop. Drop the unused Stellata argument.\n\nSeverity: MEDIUM (gated behind `debug.perf()` so impact is bounded).\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:33:59Z","created_by":"Alex Marshall","updated_at":"2026-05-08T19:09:24Z","started_at":"2026-05-08T19:06:00Z","closed_at":"2026-05-08T19:09:24Z","close_reason":"PR #41","dependencies":[{"issue_id":"stellata-r48","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:19:20Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-r48","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:33:58Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-snv","title":"Modal dismissal helper across info-modal/brand-modal/help-modal","description":"Surfaced in code review (stellata-y84).\n\nThree modal files implement essentially the same dismissal lifecycle (ESC, backdrop click, close button, keydown listener add/remove) with subtle drift:\n\n- **info-modal.ts** uses ID-targeted listeners (`#info-backdrop`, `#info-close`).\n- **brand-modal.ts** uses `[data-modal-dismiss]` selector AND uniquely couples two modals via cross-checks of `.hidden` (line 17): when closing one modal, it inspects the other to decide whether to detach the keydown listener. Adding a third modal breaks this.\n- **help-modal.ts** uses `[data-modal-dismiss]` like brand.\n\nFix: Extract `bindModalDismiss(modal: HTMLElement, onDismiss: () =\u003e void)` that wires ESC, `[data-modal-dismiss]` clicks, and per-open keydown lifecycle (closure-scoped, so each open's listener is disposed on close). Migrate all three modals. Tag `#info-backdrop` and `#info-close` with `data-modal-dismiss` so they unify with the others.\n\nNote: info-modal still owns its localStorage opt-in — keep that local; only the dismissal mechanics are shared.\n\nSeverity: MEDIUM (DRY win + the brand-modal cross-check is an actual fragility).\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:33:59Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:54:14Z","started_at":"2026-05-08T18:25:53Z","closed_at":"2026-05-08T18:54:14Z","close_reason":"Closed","dependencies":[{"issue_id":"stellata-snv","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:19:21Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-snv","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:33:59Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-akk","title":"SVG overlays: dirty-track attribute writes + scratch-vector projection helpers","description":"Surfaced in code review (stellata-y84).\n\nSeveral SVG overlays unconditionally rewrite attributes / textContent every animate tick. The dirty-tracking pattern from chart-labels.ts (compare proposed value to lastWritten, skip identical) is missing across:\n\n- **hud-overlay.ts:308-318** — `path.setAttribute('d', d)`, `bg.setAttribute('d', d)`, `label.setAttribute('x'/'y', ...)`, `label.textContent = ...` per frame for both Sol and GC arrows.\n- **poi-overlay.ts:88-92, 73-86, 191-204, 328-352, 362** — text and class writes every frame for every POI.\n- **disc-mask.ts:46-61** — focused star + companion `cx/cy/r` writes every frame.\n- **distance-vector-overlay.ts** — same kind of per-frame attribute writes; plus `getComputedTextLength()` at line 139 forces SVG layout every frame even when the destination label is unchanged.\n- **constellation-overlay.ts:50-72** — segment string `setAttribute('d', ...)` per frame for every constellation in chart mode.\n\nAlso: **projection helpers in poi-overlay.ts:391, hud-overlay.ts:400, distance-vector-overlay.ts:161-179** all `.clone()` Vector3 inputs per call. Per frame × per POI/arrow this is the biggest non-obvious allocation cluster outside stellata.ts itself.\n\nFix: Adopt `lastD`, `lastText`, `lastX`, `lastY`, `lastR` fields per element; gate writes on change. Replace `.clone()` projection helpers with module-scope scratch `Vector3`s (poi-overlay already has the pattern for `tmpStarLocal` / `tmpDir` etc). Cache `getComputedTextLength` keyed on text content.\n\nSeverity: HIGH (multiple files, but each small; together they're the steady-state per-frame DOM cost).\n","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T19:33:08Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:42Z","started_at":"2026-05-10T09:28:07Z","closed_at":"2026-05-16T21:38:42Z","close_reason":"merged via PR #55 (SVG overlays dirty-track pass + scratch-vector projection helpers)","labels":["perf"],"dependencies":[{"issue_id":"stellata-akk","depends_on_id":"stellata-9mm.188","type":"parent-child","created_at":"2026-05-15T12:53:28Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-akk","depends_on_id":"stellata-y84","type":"discovered-from","created_at":"2026-05-02T20:33:08Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-m40.4","title":"Chart-mode: distant DSO glyphs (galaxies, globular clusters)","description":"**Goal:** when chart mode is active, render the projected distant DSOs from `stellata-2q8` as type-coded chart glyphs at their RA/Dec — mirroring the symbology used by nebulae in `stellata-m40.2`.\n\n**Scope:**\n- Galaxies — traditional ellipse outline (oriented to angular major axis if known)\n- Globular clusters — circle with crosshair (Sky Atlas 2000.0 convention)\n- Other extragalactic types — fall back to galaxy ellipse or generic outline\n\n**Constraints (from the m40 epic):**\n- Chart mode is monochrome — line style and shape carry the type encoding\n- Outlines should read at the same visual weight as constellation lines\n\n**Implementation:**\n- Extend `chart-mode.ts` / `chart-labels.ts` to register a DSO-overlay path\n- Use the same projected-at-infinity celestial-sphere positions from `stellata-2q8`\n- Symbology lookup from `stellata-m40.1`'s design reference\n\n**Visibility:** active when chart mode is engaged. Picks up the same magnitude-floor filter that drives `stellata-2q8` in non-chart mode.\n\n**Depends on:** `stellata-m40.1` (symbology design) + `stellata-2q8` (DSO data + base rendering).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:24:40Z","created_by":"Alex Marshall","updated_at":"2026-05-02T18:24:40Z","labels":["chart-mode","dso","visual"],"dependencies":[{"issue_id":"stellata-m40.4","depends_on_id":"stellata-2q8","type":"blocks","created_at":"2026-05-02T19:24:41Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-m40.4","depends_on_id":"stellata-m40","type":"parent-child","created_at":"2026-05-02T19:24:39Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-m40.4","depends_on_id":"stellata-m40.1","type":"blocks","created_at":"2026-05-02T19:24:41Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-2q8","title":"Projected distant DSOs on celestial sphere (Andromeda, Messier objects, etc.)","description":"**Goal:** include the catalogued naked-eye / brighter-than-floor objects that exist *beyond* the model's spatial volume — Andromeda (M31), Triangulum (M33), distant globular clusters, other Messier galaxies — projected onto the celestial sphere at effective infinity. Filter visibility by the existing magnitude UI control.\n\n**Why:** completes the perceptual model. A user looking at the sky should see Andromeda where Andromeda actually is, even though it's at ~780 kpc — well outside the model's volume. Computationally near-free since each object is a single point with no real 3D position to maintain.\n\n**Scope:**\n- **In:** extragalactic objects + distant globular clusters + any catalogued DSO that's *beyond the model's spatial volume* but brighter than the model's magnitude floor (see `stellata-cns` — the chosen m11 or m12 catalog cutoff defines the floor for all object categories).\n- **Out:** objects already rendered in the model in 3D — M42 Orion Nebula (local SF region, comes via `stellata-9hp`), local star clusters, anything within the catalog's spatial range. Avoid double-rendering.\n- **Wavelength:** human-visible spectrum only. No IR/radio sources unless their visible-band counterparts are visible.\n\n**Magnitude semantics:** lower number = brighter. Magnitude *floor* = dimmest cutoff (highest number we render). 'Brighter than the floor' means apparent magnitude ≤ floor.\n\n**Implementation:**\n- Catalog ingest at build time. Candidates: OpenNGC (well-curated, commonly available), the Messier catalogue as a starting subset, or SIMBAD TAP for unified type-filtered fetch.\n- Per-object data: name, RA/Dec, apparent magnitude, type (galaxy / globular cluster / etc.), angular size if known.\n- Render: projected at fixed large distance (effectively infinity) on a celestial-sphere overlay. Direction = RA/Dec. Each object is a small glyph/disc + optional label. Type-coded visuals can match `stellata-9hp`'s nebula symbology where overlap exists (galaxies vs PNe vs globulars).\n- Visibility filter: rides the existing magnitude control; objects with apparent mag \u003e current display limit are hidden.\n\n**Files likely touched:**\n- New `scripts/build-distant-dsos.{py|ts}` for catalog ingest → `public/distant-dsos.json`\n- New `src/client/distant-dsos.ts` rendering layer\n- Wired into the magnitude-filter pipeline in `stellata.ts` / `controls.ts`\n- Search index (`search-index.json`) gains entries with `kind: 'dso'` discriminator (parallels the cloud / nebula patterns)\n\n**Out of scope:**\n- Chart-mode treatment — handled separately via `stellata-m40` (decide there whether each type gets a chart symbol).\n- True 3D positioning of these objects. They live on the celestial sphere; any user 'travel toward' them is a no-op (they recede with the camera).\n\n**Relates to:** `stellata-cns.1` for the magnitude-floor decision that determines the catalog's apparent-mag cutoff.\n\n**No hard dependencies — can be implemented before `cns.1` resolves; just parameterise the floor and let the magnitude UI filter.**","status":"deferred","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:22:30Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:38Z","labels":["catalog","projection","visual"],"dependencies":[{"issue_id":"stellata-2q8","depends_on_id":"stellata-9hp","type":"relates-to","created_at":"2026-05-02T19:26:15Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2q8","depends_on_id":"stellata-cns.1","type":"relates-to","created_at":"2026-05-02T19:22:34Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-2q8","depends_on_id":"stellata-m40","type":"relates-to","created_at":"2026-05-02T19:24:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-38m","title":"Wireframe outlines for nearby galactic structures (Magellanic Clouds, etc.)","description":"**Goal:** add wireframe outlines for visible Local Group members within 250 kpc of the Milky Way, plus distance-gated labels for both the MW (newly added — currently no label) and each labelled Local Group object. The MW label fixes a longstanding inconsistency with the heliopause's labelled apex. Together these give the user spatial context for *where* the Milky Way sits when fully zoomed out.\n\nRefines [[stellata-data-fidelity-principle]]: rendered geometry is representational, but positions / distances / structural parameters come from peer-reviewed sources committed under `data/` per [[frozen-external-data]] — no live fetches.\n\n# Blocked by\n\nstellata-5gq (camera envelope bump to 500 kpc far plane / 250 kpc maxDistance). The 100 kpc max maxDistance today is too cramped for the layer to be useful.\n\n# Visibility (no UI, no URL flag — explicit user decision)\n\nThe MW disc has no toggle today; this layer inherits that model. Each object's wireframe opacity tracks the SAME distance-from-Sol fade curve `galactic-disc.ts` uses today (`smoothstep(FADE_INNER_PC=500, FADE_OUTER_PC=5000, |camera-Sol|)`). Past 5 kpc from Sol, MW + Local Group outlines are at full base opacity; close to Sol they're invisible (same as the existing MW disc).\n\nPer [[stellata-named-constants-and-dry]], `FADE_INNER_PC` / `FADE_OUTER_PC` hoist to a shared module (e.g. `src/client/galactic-fade.ts`) since they now have two call sites. Update `galactic-disc.ts` to import the hoisted constants in the same PR.\n\n# Objects rendered (within 250 kpc; distance filter is the build-script's source of truth, not hand-curated)\n\n- **Sagittarius dSph** (~26.5 kpc) — triaxial ellipsoid, the closest dwarf, tidally disrupting.\n- **LMC** (49.59 kpc — Pietrzyński 2019, Nature 567, 200) — inclined disc + thickness rings. i ≈ 32°, PA_nodes ≈ 135°, scale length 4.5 kpc, scale height 1 kpc (van der Marel \u0026 Kallivayalil 2014, ApJ 781, 121; confirmed Ripepi et al. 2022, MNRAS 512, 563).\n- **SMC** (62.44 kpc — Graczyk 2020, ApJ 904, 13) — triaxial ellipsoid with axes 1 : 1.33 : 1.61 (Subramanian \u0026 Subramaniam 2012, ApJ 744, 128). Line-of-sight elongated ~12 kpc.\n- **Draco, Ursa Minor, Sculptor, Sextans, Carina, Fornax, Leo II, Leo I** — classical dSphs, oblate ellipsoids driven by LVDB's per-row ellipticity + half-light radius.\n- Ultra-faint dwarfs that LVDB lists within 250 kpc — render whatever the filter catches. Geometry is essentially free (LineLoop with ~64 segments × ~3 rings × N is trivial); they're useful when zoomed in close to a satellite even if invisible at coarse zoom.\n\n# Data pipeline (per [[frozen-external-data]])\n\n- `data/local-group/lvdb-snapshot.csv` — committed snapshot of the [Pace et al. 2024 Local Volume Database](https://github.com/apace7/local_volume_database) `dwarf_all` table. **CC0 licence**, peer-reviewed (Open Journal of Astrophysics, arXiv:2411.07424). LFS-track if \u003e 1 MB; uncompressed likely well under.\n- `data/local-group/overrides.tsv` — hand-curated structural detail + label policy for objects where LVDB's summary row is too coarse for meaningful 3D rendering. Schema:\n  `name, kind (disc|ellipsoid), inclination_deg, pa_deg, a_pc, b_pc, c_pc, label_threshold_pc, ref_doi`\n  Three core rows initially: LMC (disc), SMC (triaxial), Sgr dSph (3D axis allocation since LVDB's projected ellipticity doesn't capture line-of-sight extent). Other dSphs use LVDB-only and need no override row.\n- `scripts/build-local-group.ts` — reads CSV + overrides → filters by distance ≤ 250 kpc → merges per-object structural detail → writes `public/local-group.json`. Idempotent (mtime check, mirror `scripts/build-clouds.py`). Schema parallels `RawCloud` from `cloud-loader.ts:24–32` plus a `kind: 'disc' | 'ellipsoid'` discriminator and a `label_threshold_pc?: number` field.\n\n# Client rendering (mirrors `galactic-disc.ts` LineLoop pattern)\n\n- `src/client/local-group-loader.ts` — fetch + parse `public/local-group.json`. Shape parallel to `cloud-loader.ts`.\n- `src/client/local-group.ts` — new layer module. Per object:\n  - Position via RA/Dec/d_helio_pc → ICRS heliocentric XYZ (build script does the conversion so the runtime sees ready-to-use ICRS XYZ; no `GAL_TO_ICRS` rotation since LVDB + the LMC/SMC structural papers are sky-frame).\n  - **Discs** (LMC only at 250 kpc cap): one midplane LineLoop ring + a thickness pair offset ±c along the disc normal, all rotated by `(inclination, PA_nodes)`.\n  - **Ellipsoids** (everything else): three orthogonal LineLoop meridian rings on the principal axes, ~64 segments each.\n  - Floating-origin rebase per frame: `group.position.copy(worldOffset).negate()` (same pattern as `molecular-clouds.ts:96`).\n  - Material: `LineBasicMaterial`, base opacity comparable to `DARK_BASE_OPACITY` in `galactic-disc.ts`. Per-frame opacity = `smoothstep(FADE_INNER_PC, FADE_OUTER_PC, |camera-Sol|) × baseOpacity`.\n\n# Distance-gated label helper\n\nHeliopause's per-frame projection pattern at `src/client/heliopause.ts:217–315` is the template. Extract a generic helper:\n\n```ts\n// src/client/distance-gated-label.ts\ncreateDistanceGatedLabel(stellata, {\n  elementId: string,             // SVG \u003ctext id=\"...\"\u003e in markup\n  samplePointsLocal: Vector3[],  // silhouette samples for support-point search\n  labelDir: { x, y },            // screen-space direction (e.g. bottom-right)\n  visible: () =\u003e boolean,        // per-frame gating predicate\n  lerp: number,                  // screen-position smoothing\n});\n```\n\nPer [[stellata-named-constants-and-dry]] second-usage rule, the helper IS the extraction: heliopause's label call site moves to it (predicate stays `stellata.anyOrbitRingVisible()`), MW + Local Group label call sites use it (predicate becomes `() =\u003e cameraToObject.length() \u003e object.labelThresholdPc`). If predicate divergence makes the heliopause refactor noisy, leave heliopause alone and document the asymmetry in a comment.\n\n# Per-object label policy\n\n- **Milky Way**: `label_threshold_pc` ~10 kpc — label appears once camera is outside the disc.\n- **LMC**: ~30 kpc — visible once you can see the whole inclined disc.\n- **SMC**: ~30 kpc.\n- **Sagittarius dSph**: ~10 kpc (the closest, smaller threshold OK).\n- Classical dSphs: ~20 kpc default unless overridden; ultra-faints unlabelled.\n\nSVG markup additions in `src/client/index.html`:\n- `\u003ctext id=\"mw-label\"\u003eMilky Way\u003c/text\u003e` in the same SVG layer that hosts `heliopause-label`.\n- One `\u003ctext id=\"lg-\u003cslug\u003e-label\"\u003e` per labelled Local Group object, emitted dynamically at layer construction (mirror `constellation-overlay.ts`).\n\n# Acceptance\n\n- `npm run typecheck` + `npm test` green.\n- Build script idempotent: re-running the build doesn't re-rewrite `public/local-group.json` if inputs unchanged.\n- Manual smoke (per [[alex-stellata-smoke-tests]] — alex tests in his own browser, do not start a dev server):\n  1. Zoom out from first-load. Past ~500 pc from Sol, MW disc fades in (existing behaviour). Past ~10 kpc from galactic centre, \"Milky Way\" label appears anchored to the galactic-centre projection, lerping into place.\n  2. Continue zooming to 50+ kpc. LMC + SMC + Sgr dSph + classical dSph outlines fade in on the same curve. Each labelled object's label appears once camera is past its `label_threshold_pc`.\n  3. Sky-position sanity check: LMC + SMC sit south-east of the galactic centre as in photographs; Sgr dSph sits ~6 kpc below the galactic plane.\n- Reload at zoomed-out view (URL captures camera via existing v3 envelope, not via new flags) → outlines + labels restore to the same state.\n\n# Test gaps to add (per [[alex-large-pr-honesty]])\n\n- `build-local-group.ts`: distance filter, override-merge precedence, idempotency.\n- `local-group-loader.ts`: parse round-trip on generated JSON.\n- `distance-gated-label.ts`: support-point selection, near-plane-clip bail-out, lerp-snap on first show.\n- Renderer + label engine are \"low (needs eyeballs)\" — flag the manual smoke list explicitly in the PR description per [[alex-large-pr-honesty]].\n\n# Files likely touched\n\n- New: `data/local-group/lvdb-snapshot.csv`, `data/local-group/overrides.tsv`, `scripts/build-local-group.ts`, `src/client/local-group.ts`, `src/client/local-group-loader.ts`, `src/client/distance-gated-label.ts`, `src/client/galactic-fade.ts`.\n- Edited: `src/client/index.html` (SVG label slots), `src/client/galactic-disc.ts` (import hoisted fade constants), `src/client/heliopause.ts` (move to shared helper), `src/client/stellata.ts` (instantiate layer), `package.json` (build script wire-up if needed).\n\n# Out of scope\n\n- M31, M33, NGC 205, M32, IC 10, NGC 6822, Leo A, IC 1613, WLM — **separate deferred bead** (the new Bead C filed alongside this one; needs the 1.5–2 Mpc envelope).\n- Magellanic Stream — invisible HI gas, user-confirmed exclusion.\n- M31 / M33 / dSph chart-mode glyphs — `stellata-m40.4`.\n- Galactic-disc fade-curve rework — current curve (full opacity past 5 kpc from Sol) is correct for this work.\n- Local Group objects as focus targets — no plan now; revisit visibility rules if/when that changes.\n- Star-catalogue extension to LMC/SMC/Sgr stellar populations — AT-HYG depth doesn't reach LMC/SMC reliably; Sgr dSph red giants marginal (see [[stellata-cds-data-sources-and-detail-gradient]]). File a separate follow-up if relevant.\n\n# Data sources (committed under `data/local-group/`)\n\n- Pace et al. 2024, *Local Volume Database*, Open Journal of Astrophysics (arXiv:2411.07424). CC0. https://github.com/apace7/local_volume_database\n- Pietrzyński et al. 2019, *Nature* 567, 200 (10.1038/s41586-019-0999-4) — LMC distance.\n- van der Marel \u0026 Kallivayalil 2014, *ApJ* 781, 121 (10.1088/0004-637X/781/2/121) — LMC structure.\n- Ripepi et al. 2022, *MNRAS* 512, 563 (10.1093/mnras/stac495) — LMC Cepheid 3D map.\n- Graczyk et al. 2020, *ApJ* 904, 13 (10.3847/1538-4357/abbb2b) — SMC distance.\n- Subramanian \u0026 Subramaniam 2012, *ApJ* 744, 128 (10.1088/0004-637X/744/2/128) — SMC structure.\n\nDOIs go in `overrides.tsv` `ref_doi` column AND in code comments at the call sites that consume per-object structural data, per [[stellata-named-constants-and-dry]].\n\n# Documentation deferral (per [[defer-doc-updates]])\n\nDon't touch CLAUDE.md / SCIENCE.md / docs/ during implementation. At commit time, audit the diff: SCIENCE.md gains a Local Group data-source section; CLAUDE.md module roster gains the new files + `data/local-group/`. Possibly a new `docs/local-group.md` if the layer earns its own doc — decide at commit time.\n\n# Reference\n\nPlanning session (this bead's full design context): /Users/alexm/.claude/plans/let-s-start-plan-mode-nested-bentley.md","status":"closed","priority":3,"issue_type":"feature","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:17:09Z","created_by":"Alex Marshall","updated_at":"2026-05-16T21:38:17Z","started_at":"2026-05-16T10:18:43Z","closed_at":"2026-05-16T21:38:17Z","close_reason":"merged via PR #67 (Local Group wireframes + MW label)","labels":["galactic","local-group","visual","wireframe"],"dependencies":[{"issue_id":"stellata-38m","depends_on_id":"stellata-5gq","type":"blocks","created_at":"2026-05-16T11:03:22Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-38m","depends_on_id":"stellata-m40","type":"relates-to","created_at":"2026-05-02T19:26:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-m40.3","title":"Chart-mode: planets of the focused host star at current t","description":"**Goal:** render the planets of the focused host star in chart mode at their angular positions for the current `t`, using the symbology + labeling rules resolved in `stellata-m40.1`. Currently this means Sol's planets only, since Sol is the only host with a planet table; the implementation must be host-generic so that any focal host with a planet table picks this up automatically when `stellata-bk5` (exoplanets) ships.\n\n**Implementation:**\n- In chart mode, the focused host's planet positions (currently from `stellata-3re.3` VSOP87 for Sol) project to angular sky coordinates\n- Render per `m40.1`'s resolved symbology (glyph vs. magnitude-disc; non-Sol depiction convention)\n- Labeling flows through the existing named-star path in `chart-labels.ts` (same dirty-track / collision contract)\n- Time-aware: when the user scrubs `t`, planet positions update\n\n**Files likely touched:**\n- `src/client/chart-mode.ts`\n- `src/client/chart-labels.ts`\n- `planet-system.ts` / `planet-body-field.ts` may grow a chart-mode branch (or a sibling reader consumes the same per-host table)\n\n**Visibility:** active when chart mode is engaged AND the focal star has a planet system. Outside that, planets don't render in chart mode. Filter mechanism (focal-host gate vs. apparent-mag self-filter) is decided in `m40.1`.\n\n**Acceptance:**\n- Sol focused + chart mode → planets render at correct angular positions for `t`, labeled per the named-star convention from `m40.1`\n- No Sol-specific assumptions in the rendering path: when `bk5` ships planet tables for other hosts, focusing one of those hosts in chart mode produces planets without further code change\n- Symbology matches `m40.1`'s output\n\n**Depends on:** `stellata-m40.1` (symbology + open questions resolved), `stellata-3re.4` (Sol planet bodies; closed), `stellata-3re.3` (positions API; closed), `stellata-3re.16` (host-generic apparent-mag visibility cutoff; closed).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:14:25Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:13:27Z","labels":["chart-mode","solar-system","visual"],"dependencies":[{"issue_id":"stellata-m40.3","depends_on_id":"stellata-3re.16","type":"blocks","created_at":"2026-05-08T19:08:52Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-m40.3","depends_on_id":"stellata-3re.4","type":"blocks","created_at":"2026-05-02T19:14:33Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-m40.3","depends_on_id":"stellata-m40","type":"parent-child","created_at":"2026-05-02T19:14:25Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-m40.3","depends_on_id":"stellata-m40.1","type":"blocks","created_at":"2026-05-02T19:14:32Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-m40.2","title":"Chart-mode: nebulae outlines per type symbology","description":"**Goal:** render catalogued nebulae from `stellata-9hp` as chart-mode outlines using the per-type symbology defined in `stellata-m40.1`.\n\n**Implementation:**\n- Extend `chart-mode.ts` to register a nebulae-overlay path active in chart mode\n- Per-nebula glyph in `chart-labels.ts` (or a new `chart-nebulae.ts` if the existing label engine isn't a clean fit)\n- Type-specific symbology from `m40.1`: PNe symbol, H II outline, RNe outline, SNR outline\n- Picking + label coordinates with existing chart-mode label engine\n\n**Files likely touched:**\n- `src/client/chart-mode.ts` — orchestrator hook for the nebulae layer\n- `src/client/chart-labels.ts` — glyph integration (or new module)\n- Possibly `src/client/shaders/nebula.frag.glsl` (from `stellata-9hp.2`) gets a chart-mode branch like the cloud shader's `uChartIsobar` path\n\n**Visual gate:** a region rich in catalogued nebulae (e.g. Orion's belt + sword) reads as a star atlas — outlines indicate nebula presence and approximate extent without dominating the page.\n\n**Depends on:** `stellata-m40.1` (symbology design) + `stellata-9hp` epic shipping (nebulae data + base rendering).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:14:24Z","created_by":"Alex Marshall","updated_at":"2026-05-04T08:14:01Z","labels":["chart-mode","nebulae","visual"],"dependencies":[{"issue_id":"stellata-m40.2","depends_on_id":"stellata-9hp.3","type":"blocks","created_at":"2026-05-02T19:14:32Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-m40.2","depends_on_id":"stellata-m40","type":"parent-child","created_at":"2026-05-02T19:14:24Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-m40.2","depends_on_id":"stellata-m40.1","type":"blocks","created_at":"2026-05-02T19:14:31Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-m40.1","title":"Design: chart-mode visual language for catalogued structures (Sky Atlas 2000.0 reference)","description":"**Goal:** produce the design reference that drives `m40`'s child rendering tasks. Output is a written section added to `docs/chart-mode.md` covering per-structure-type chart symbology.\n\n**Reference:** Sky Atlas 2000.0 conventions (user has a scanned reference page available on request). Other classical atlases (Norton's, Uranometria) are secondary references if useful.\n\n**Per-type symbology to define:**\n- **Nebulae** — by type (PNe, RNe, H II, SNR), each has traditional chart symbol conventions:\n  - PNe: small green-tinged circle with cross or shell-style outline\n  - H II / diffuse: dashed or solid outline approximating the angular extent\n  - RNe: outline biased toward illuminator, often dashed\n  - SNR: filamentary or shell outline\n- **Planets of the focused host star** — labelled symbol or filled disc at current angular position, sized appropriately vs. chart star labels. See open design questions below — this section drives `m40.3` and is host-generic (Sol now, exoplanets when `stellata-bk5` ships).\n\n**Open design questions for chart-mode planets** (resolve here, then `m40.3` implements):\n\n1. **Filter mechanism.** Two candidate framings: (a) gate on the focal-host system — planets render only for the star the camera is observing-from; (b) gate on apparent magnitude — planets outside the focal host fall below the visibility cutoff anyway (per `stellata-3re.16`'s host-generic apparent-mag math) and self-filter. (a) and (b) likely produce identical visible output; pick the one whose framing matches how chart-mode planets are *intended* to read (a focal-system affordance vs. a sky-photometry artefact).\n2. **Labeling.** Treat each planet like a named star, so it flows through the existing named-star path in `chart-labels.ts` (same font, same collision rules, same dirty-track contract). Confirm or specify divergences.\n3. **Glyph vs. magnitude-disc.** Two options: (a) special chart symbol per planet (e.g. ♀ ♂ ♃ ♄ for Sol); (b) same disc-glyph as a star, sized by the planet's apparent magnitude through the existing star-disc pipeline. (a) gives traditional-atlas legibility; (b) is host-generic and works for any focal star.\n4. **Non-Sol depiction.** The traditional ♀ ♂ ♃ ♄ symbols are Sol-specific — they don't exist for exoplanets. The chart-mode convention must work for any focal host. Candidate options: (a) generic filled-disc glyph + designation, used uniformly including for Sol; (b) Sol gets the traditional symbols as an explicit override, exoplanets get the generic glyph; (c) astronomical host-letter-index labelling (b/c/d/…) for exoplanets, named labels for Sol's planets. Decision interacts with question 3 — if (3b) wins, depiction is just the disc + label, no glyph alphabet to design. Decision goes in `docs/chart-mode.md`.\n\n**Constraints:**\n- Chart mode is monochrome (existing convention) — colour can't differentiate types; line style and shape carry the encoding\n- Outlines should read at the same visual weight as constellation lines, neither dominating nor disappearing\n- Coordinate with the per-frame label engine in `chart-labels.ts` so symbols don't fight existing star/constellation labels for space\n\n**Output:** new subsection in `docs/chart-mode.md` with reference symbology table + opacity/weight values + resolved answers to the four open planet questions above. Drives `m40.2` and `m40.3` implementation.\n\n**No code in this phase.**","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:14:23Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:13:19Z","labels":["chart-mode","research","visual"],"dependencies":[{"issue_id":"stellata-m40.1","depends_on_id":"stellata-m40","type":"parent-child","created_at":"2026-05-02T19:14:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"stellata-m40","title":"Chart-mode rendering of catalogued structures (nebulae, planets, etc.)","description":"**Goal:** as new structures land in the model (nebulae, solar-system planets, etc.), they need a chart-mode representation when they're visually relevant in the real sky. Traditional star atlases (Sky Atlas 2000.0 is the canonical reference) render bright/catalogued objects as light outlines so observers can use the chart against the actual sky.\n\n**Per-structure visibility decision** (made up-front so each structure's chart-mode treatment is clear):\n\n| Structure | Visually relevant? | Chart-mode? |\n|---|---|---|\n| Stars (existing) | yes | ✓ shipped |\n| Constellation lines (existing) | reference | ✓ shipped |\n| Galactic disc (existing) | yes (band of MW) | ✓ shipped (isobar) |\n| **Nebulae (PNe, RNe, H II, SNR)** — `stellata-9hp` | **YES** | **add** |\n| **Planets of focused host star** — `stellata-3re.4` (Sol), `stellata-bk5` (exoplanets) | **YES** — host-relative (Mercury through Saturn naked-eye from Sol; exoplanet bodies are naked-eye from their own host) | **add** (`m40.3`, host-generic — Sol now, exoplanets pick up the same path when `bk5` ships) |\n| Molecular clouds — `stellata-c7u` | no (extinction is the signal, dark patches at most) | skip |\n| Local Bubble — `stellata-dk3` | no (3D cavity, invisible) | skip |\n| Radcliffe Wave — `stellata-n2x` | no (3D structure) | skip |\n| Heliopause / scale rings — `stellata-3re.5` | no | skip |\n| Deep-space probes — `stellata-ywn` | no | skip |\n\n**Children:**\n- `.1` Design language — chart-mode visual conventions per structure type (line weights, fills, opacity, label conventions). User has a scanned Sky Atlas 2000.0 reference page available on request. Includes the four open planet design questions (filter / labeling / glyph-vs-disc / non-Sol depiction) that gate `.3`. Output: written reference in `docs/chart-mode.md` covering the per-type symbology rules.\n- `.2` Nebulae chart-mode rendering (depends on `stellata-9hp` shipping + `.1` design)\n- `.3` Planets-of-focused-host-star chart-mode rendering, host-generic (depends on `stellata-3re.4` shipping + `.1` design; auto-extends to exoplanets when `bk5` ships)\n\n**Future:** any new structure added to the model needs a chart-mode decision — visible? render. invisible? skip. Add a child here when the call is 'render'.\n\n**Out of scope:**\n- Any change to existing chart-mode shipped behaviour (stars, constellation lines, galactic disc isobar)\n- The constellation-name label-size tweak (filed separately as its own task)","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:13:52Z","created_by":"Alex Marshall","updated_at":"2026-05-10T17:13:35Z","labels":["chart-mode","visual"],"dependencies":[{"issue_id":"stellata-m40","depends_on_id":"stellata-2q8","type":"relates-to","created_at":"2026-05-02T19:24:42Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-m40","depends_on_id":"stellata-38m","type":"relates-to","created_at":"2026-05-02T19:26:14Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-hpo","title":"Chart-mode: enlarge constellation name labels + increase transparency","description":"**Goal:** in chart mode, constellation name labels currently render small enough that individual letters can be occluded by adjacent star labels. Increase size by ~50% and increase transparency to compensate (so the larger labels don't dominate).\n\n**Tweak target:**\n- Size: ~50% larger\n- Opacity: more transparent than today (since the larger size will compensate for legibility)\n\n**Files likely touched:**\n- `src/client/chart-labels.ts` — per-frame label engine + glyphs; constellation-name rendering lives here\n- `src/client/chart-mode.ts` — orchestrator if any size/opacity uniforms live there\n- Possibly `src/client/styles.css` if labels are SVG/HTML-based\n\n**Verification:** load chart mode, check that overlapping constellation-name + star-label cases now read clearly without the constellation name dominating sparse regions of the sky.","status":"closed","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:13:39Z","created_by":"Alex Marshall","updated_at":"2026-05-17T18:30:53Z","started_at":"2026-05-17T18:22:10Z","closed_at":"2026-05-17T18:30:53Z","close_reason":"Closed","labels":["chart-mode","polish","visual"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-3re.2","title":"Time-scrubber UI in .ui-bottom","description":"**Goal:** the user-facing time scrubber widget. Lives in `.ui-bottom`. Final implementation task in the time-scrubber epic — depends on the three design decisions (motion audit, hide policy, widget design) being settled first.\n\n### Brief\n\nDiscreet, unobtrusive. Visible only when the solar-system layer is active (focused star has planets per stellata-3re.6).\n\n### Likely shape (refined by stellata-nmu.3)\n\n- Short text readout showing current `t` (matches the format chosen in stellata-3re.11 — likely UTC Zulu).\n- Click-to-edit affordance for jump-to-date.\n- Forward / back nudges (e.g. ±1 day, ±1 year keys via keyboard-shortcuts.ts dispatch).\n- Optional auto-play (FF/RW) per stellata-nmu.3.\n- \"Live\" state when scrubbed back to now.\n\n### Files likely touched\n\n- `src/client/index.html`, `styles.css` for the chip.\n- `src/client/stellata.ts` for visibility wiring + `t` setter integration.\n- New module `src/client/time-scrubber.ts`.\n\n### Depends on\n\n- `stellata-3re.1` for the `t` state + setter.\n- `stellata-3re.11` for the time-format decision (the static readout settles the format; the scrubber inherits it).\n- `stellata-nmu.1`, `stellata-nmu.2`, `stellata-nmu.3` for the design questions about catalogue motion, hide policy, and widget shape.\n","notes":"Reparented from stellata-3re to stellata-nmu on 2026-05-07. v1 solar-system layer ships only a static time readout (stellata-3re.11); this scrubber is deferred to the time-scrubber epic.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:07:32Z","created_by":"Alex Marshall","updated_at":"2026-05-08T18:22:10Z","labels":["solar-system","ui"],"dependencies":[{"issue_id":"stellata-3re.2","depends_on_id":"stellata-3re.1","type":"blocks","created_at":"2026-05-02T19:07:59Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-3re.2","depends_on_id":"stellata-nmu","type":"parent-child","created_at":"2026-05-07T09:54:29Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-bk5.3","title":"Rendering: orbiting planets, orbit rings, null-radius indicator","description":"**Goal:** when the user focuses a flagged host star, render its known exoplanets as orbiting bodies — orbital rings sized to `semi_major_axis_au`, planet discs sized to `radius_earth` / `radius_jupiter` where known.\n\n**Visual decisions deferred to implementer** (see parent `stellata-bk5` for context):\n- Orbit ring style (solid / dashed / fade-with-distance)\n- Planet disc shading (flat / lit / temperature-tinted via `st_teff`)\n- Null-radius indicator (an unknown-radius planet still has a known orbit; show the orbit ring + a default-size marker?)\n\n**Leverage `stellata-a7d`:** disc sizing should use the same physical-angular-diameter formula `θ = 2·atan(R/d)` from the rewrite, not the legacy log-mapped formula. Planets at correct angular size at the camera distances expected when focused on a host star.\n\n**Files touched:** likely a new `src/client/exoplanets.ts` rendering layer + shader pair, plus integration into `stellata.ts` focus path.\n\n**Depends on:** `stellata-bk5.2` for data, and **strongly benefits from `stellata-a7d.2` having landed** so disc sizing is physically consistent.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:06:42Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:39Z","labels":["exoplanets","phase-11","visual"],"dependencies":[{"issue_id":"stellata-bk5.3","depends_on_id":"stellata-3re.7","type":"relates-to","created_at":"2026-05-07T09:55:53Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bk5.3","depends_on_id":"stellata-3re.8","type":"relates-to","created_at":"2026-05-07T09:55:50Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bk5.3","depends_on_id":"stellata-3re.9","type":"relates-to","created_at":"2026-05-07T09:55:55Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bk5.3","depends_on_id":"stellata-a7d.2","type":"blocks","created_at":"2026-05-02T19:08:02Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bk5.3","depends_on_id":"stellata-bk5","type":"parent-child","created_at":"2026-05-02T19:06:42Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bk5.3","depends_on_id":"stellata-bk5.2","type":"blocks","created_at":"2026-05-02T19:06:48Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bk5.3","depends_on_id":"stellata-bk5.4","type":"blocks","created_at":"2026-05-07T09:55:24Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bk5.3","depends_on_id":"stellata-bk5.5","type":"blocks","created_at":"2026-05-07T09:55:26Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bk5.3","depends_on_id":"stellata-bk5.6","type":"blocks","created_at":"2026-05-07T09:55:29Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":5,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-bk5.2","title":"Client loader: lazy-load exoplanets.json on first focus of a flagged host star","description":"**Goal:** when the user focuses a star whose 'has-exoplanets' flag bit is set, fetch `public/exoplanets.json` (once, cached for the session) and surface its `ExoplanetSystem` for the focused host.\n\n**Implementation:**\n- New `src/client/exoplanet-loader.ts` mirroring the structure of `catalog-loader.ts`\n- Singleton fetch promise — first call kicks off the load, subsequent calls reuse the result\n- API: `getExoplanetsForStar(idx: number): ExoplanetSystem | null`\n- Wired into `stellata.ts` focus path: when a flagged star is focused, kick the fetch and (on resolve) update the rendering layer\n\n**Out of scope:** any UI affordance is part of `stellata-bk5.3`. This issue ends at `getExoplanetsForStar()` returning data.\n\n**Depends on:** `stellata-bk5.1` for the binary flag bit + json file.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:06:41Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:39Z","labels":["client","exoplanets","phase-11"],"dependencies":[{"issue_id":"stellata-bk5.2","depends_on_id":"stellata-bk5","type":"parent-child","created_at":"2026-05-02T19:06:41Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bk5.2","depends_on_id":"stellata-bk5.1","type":"blocks","created_at":"2026-05-02T19:06:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-bk5.1","title":"Build pipeline: NASA Exoplanet Archive ingest, cross-match, catalog flag bit + lazy json","description":"**Goal:** ingest the NASA Exoplanet Archive `pscomppars` table at build time, cross-match host stars to ATHYG, set a 'has-exoplanets' flag bit per star in `catalog.bin`, and emit `public/exoplanets.json` keyed by host-star catalog index for lazy loading.\n\n**Data fetch** (one-off, committed via Git LFS — see parent `stellata-bk5` for the exact curl URL and Git LFS commands):\n- `data/exoplanets_nasa.csv` from the TAP API\n- Fields: `pl_name, hostname, hip_name, hd_name, ra, dec, sy_dist, pl_rade, pl_radj, pl_orbsmax, pl_orbper, pl_eccen, pl_bmasse, sy_pnum, st_teff, st_rad, st_lum`\n\n**Parser:** new `parseExoplanetData(csvPath)` alongside the GCVS parsers. Returns `Map\u003chostname, ExoplanetSystem\u003e` with planets sorted by `semi_major_axis_au` ascending (nulls last). Schema in parent `stellata-bk5`. Don't filter null-radius planets — renderer handles that.\n\n**Cross-match — HIP + HD + Gliese.** Build symmetric reverse maps (`exoByHip`, `exoByHd`, `exoByGl`) after the existing post-sort `hipToIndex` map. Resolve hostname → ATHYG catalog index; record on each matched star a 'has-exoplanets' bit + an offset into the lazy json.\n\n**Outputs:**\n- `catalog.bin` gains a flag bit per star (likely fits in an existing reserved bit; check the binary v4 layout)\n- `public/exoplanets.json` keyed by catalog index → `ExoplanetSystem`. Lazy-loaded by the client on first focus of a star with the flag set.\n\n**Files touched:** `scripts/build-catalog.ts`, `scripts/catalog-pure.ts` (new parser there per existing pattern), tests in `scripts/catalog-pure.test.ts`.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:06:40Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:38Z","labels":["build-pipeline","catalog","exoplanets","phase-11"],"dependencies":[{"issue_id":"stellata-bk5.1","depends_on_id":"stellata-bk5","type":"parent-child","created_at":"2026-05-02T19:06:40Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-bk5.1","depends_on_id":"stellata-d94","type":"related","created_at":"2026-05-12T09:53:20Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9hp.4","title":"Search index integration + RNe illuminator cross-match to ATHYG","description":"**Goal:** named nebulae appear in the search bar like stars and clouds; reflection nebulae's illuminating stars are resolved to ATHYG indices for colour-biasing in `stellata-9hp.3`.\n\n**Search index:**\n- Extend `search-index.json` to include nebula entries with `kind: 'nebula'` discriminator (existing pattern: stars use `kind: 'star'`, clouds use `kind: 'cloud'`)\n- Common-name + type-prefix entries (e.g. 'Orion Nebula', 'Ring Nebula', 'M42')\n- Focus search box dispatches to `flyToNebula` (analogous to `flyToCloud` from the cloud overlay)\n- `stellata.ts` gains `flyToNebula(nebulaIdx)` mirroring `flyToCloud`\n\n**RNe illuminator cross-match:**\n- Magakian RNe entries name an illuminating star (HD/HIP). Resolve at build time via the same hipToIndex / hd → index maps used by `build-catalog.ts`\n- Emit `illuminator_idx: number | null` per RNe entry in `nebulae.json`\n- Renderer (in `stellata-9hp.3`) uses the illuminator's colour to bias the bluish reflection tint\n\n**Depends on:** `stellata-9hp.1` for the nebulae data file.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:06:05Z","created_by":"Alex Marshall","updated_at":"2026-05-04T08:13:54Z","labels":["ism","nebulae","phase-3b","search"],"dependencies":[{"issue_id":"stellata-9hp.4","depends_on_id":"stellata-9hp","type":"parent-child","created_at":"2026-05-02T19:06:04Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9hp.4","depends_on_id":"stellata-9hp.1","type":"blocks","created_at":"2026-05-02T19:06:11Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9hp.3","title":"Type-specific visual treatments (H II, PNe, RNe, SNR)","description":"**Goal:** distinguishable visuals per nebula type so the type encoding reads at a glance. All visuals procedural — **no photographs**, matching the rest of the app's aesthetic.\n\n**Per-type treatment** (full notes in parent `stellata-9hp`):\n- **H II regions** — warm reddish-pink, soft sphere with density falloff\n- **Planetary nebulae** — greenish-teal, often shell-like; small central point + thin shell\n- **Reflection nebulae** — bluish, irregular blob biased toward the illuminating star's colour (look up via ATHYG cross-ref from `stellata-9hp.4`)\n- **Supernova remnants** — filamentary; pink → blue gradient by age\n\n**Approach:** type-specific density profile + colour mapping passed as uniforms to the raymarch shader from `stellata-9hp.2`. Procedural shape variation per type is essential — without it, every nebula renders as a blob and the type encoding is lost.\n\n**Depends on:** `stellata-9hp.2` (raymarch shader) and `stellata-9hp.4` (RNe illuminator cross-match for the bluish-toward-illuminator bias).","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:06:03Z","created_by":"Alex Marshall","updated_at":"2026-05-04T08:13:52Z","labels":["ism","nebulae","phase-3b","visual"],"dependencies":[{"issue_id":"stellata-9hp.3","depends_on_id":"stellata-9hp","type":"parent-child","created_at":"2026-05-02T19:06:03Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9hp.3","depends_on_id":"stellata-9hp.2","type":"blocks","created_at":"2026-05-02T19:06:11Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9hp.3","depends_on_id":"stellata-9hp.4","type":"blocks","created_at":"2026-05-02T19:06:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-9hp.2","title":"Per-nebula raymarching shader (small AABB volumetrics)","description":"**Goal:** the volumetric rendering primitive for nebulae. Each nebula occupies a small AABB (typically 1–50 pc), so per-nebula raymarching with 16–32 samples is dense in voxel-space and produces smooth volumetrics without the aliasing that killed the all-sky fog approach.\n\n**Reference:** recover the fog shader pattern from git history at commit `5ad5444` — clip-space fullscreen triangle, ray-AABB slab intersection, fixed-step march with log-encoded density decode. See `memory/feedback_raymarch_for_nebulae.md` for context on why the all-sky version failed and per-nebula AABBs work.\n\n**Implementation:**\n- New shader pair `src/client/shaders/nebula.{vert,frag}.glsl`\n- Per-nebula uniform set: AABB centre + extents, density profile parameters, type-coded colour (set by `stellata-9hp.3`)\n- Render-order placement coordinated with stars / clouds / Milky Way\n\n**Out of scope:** type-specific procedural shape variation lives in `stellata-9hp.3`. This issue delivers the raymarch primitive with a single neutral density profile.\n\n**Depends on:** `stellata-9hp.1` (data) for AABBs to render against.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:06:02Z","created_by":"Alex Marshall","updated_at":"2026-05-04T08:13:50Z","labels":["ism","nebulae","phase-3b","shader"],"dependencies":[{"issue_id":"stellata-9hp.2","depends_on_id":"stellata-9hp","type":"parent-child","created_at":"2026-05-02T19:06:02Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-9hp.2","depends_on_id":"stellata-9hp.1","type":"blocks","created_at":"2026-05-02T19:06:10Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-cns.1","title":"Evaluate perf + first-load tradeoffs: reduced_m12 (~2M stars) vs reduced_m11 (~800k)","description":"**Goal:** decide which ATHYG reduced subset the catalog upgrade in `stellata-cns` should target. The parent issue currently specifies `reduced_m12` (~2M stars, ~80 MB binary, ~20 MB gzipped). `reduced_m11` (~800k stars) is the smaller alternative.\n\n**Tradeoffs to measure:**\n- **First-load size**: m12 ~20 MB gzipped vs m11 estimated ~8 MB. Significant for slow connections and mobile.\n- **Render perf**: 2M instanced quads at 60 fps on integrated GPUs / mobile chips vs ~800k. Three.js instance-attribute memory grows linearly. Two-pass core/glow split helps overdraw, but instance count is the dominant cost.\n- **Scientific completeness**: m12 reaches further into the kpc range; m11 truncates at brighter apparent mag and leaves more gaps at intermediate distances (50 pc – kpc).\n- **Build pipeline cost**: m12 CSV is ~400 MB (LFS); m11 is smaller. Build time + CI implications.\n\n**Method:**\n- Pull both ATHYG subsets and run `scripts/build-catalog.ts` against each\n- Measure: binary size, gzipped wire size, instance-attribute memory, FPS on a representative low-end target (integrated GPU, ideally a mid-tier phone)\n- Compare scientific feel: are intermediate-distance gaps in m11 visually obvious during travel?\n\n**Output:** a recommendation in this issue's notes for which subset `stellata-cns` should target. Update `cns` accordingly when decided.\n\n**Watch out for:**\n- m11 ⊂ m12 ⊂ `reduced_m13.5` etc. — all are subsets of full ATHYG; differences are purely the apparent-mag cutoff.\n- Mobile perf may be the deciding factor — if m12 doesn't run on phones, the answer is m11 regardless of completeness.","notes":"2026-05-02 — note: this decision sets the **model's magnitude floor**, not just the star catalog cutoff. Whichever cutoff (m11 or m12) wins, it becomes the dimnest threshold for all object categories — variable stars, nebulae, projected distant DSOs (`stellata-38m` and the new projected-DSOs task), and any future object types. Factor this into the decision: m11 (~mag 11 floor) gives a cleaner, brighter sky; m12 (~mag 12 floor) gives a deeper sky but more visual density and more catalog work for every object type that has to respect the floor.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T18:00:26Z","created_by":"Alex Marshall","updated_at":"2026-05-04T08:14:06Z","labels":["catalog","phase-7","research"],"dependencies":[{"issue_id":"stellata-cns.1","depends_on_id":"stellata-2q8","type":"relates-to","created_at":"2026-05-02T19:22:34Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-cns.1","depends_on_id":"stellata-cns","type":"parent-child","created_at":"2026-05-02T19:00:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-io3.2","title":"Dataset boundary overlay: visualise where high-fidelity data ends (Edenhofer dust volume, etc.)","description":"**Frame:** part of the artificial-visualisation cluster — like the dust particle layer and MC silhouettes, this is an opt-in overlay that surfaces something you can't directly see in the rendered scene. Specifically *not* a \"you're outside the realistic model\" status chip: that framing implies degraded mode and breaks immersion. Going farther from the high-fidelity volume is just part of the experience; the underlying dataset boundary becomes visible only if the user opts in.\n\n**What to visualise (initial scope):**\n- The **Edenhofer dust voxel cube** (~1.25 kpc AABB at world origin) — the high-fidelity 3D dust volume that drives per-star extinction inside it. Outside, an analytical disc/scale-height profile takes over (see `stellata-74e`). Render as a faint translucent sphere or box mesh, off by default.\n\n**Future extensions (not blockers for this issue):**\n- ATHYG catalog completeness boundary (e.g. apparent-magnitude horizon as a faint shell)\n- Zucker cloud coverage region\n- Per-star dust extinction validity envelope\n- Other dataset envelopes added later\n\n**Implementation sketch:**\n- Faint translucent mesh at world origin (size = Edenhofer AABB). Premultiplied alpha; very low opacity. Sits in the same toggle group as the rest of the artificial-viz overlays.\n- No status chip, no banner, no \"degraded\" framing in the UI.\n\n**Files likely touched:**\n- `src/client/stellata.ts` — boundary mesh + group registration\n- `src/client/controls.ts` — toggle in the artificial-viz panel section (once that section's UX is designed under `stellata-io3`)\n- `src/client/url-state.ts` — toggle persistence\n- `src/client/index.html`, `src/client/styles.css` — minimal\n\n**Out of scope:**\n- Any text indication that the user is in / out of a high-fidelity region — explicitly rejected during scoping.\n- Changes to extinction physics — that's `stellata-74e`'s scope.\n\n**Estimate:** ~2 hours once the artificial-viz toggle group exists.\n\n**History:** extracted from `stellata-74e` Part 2 (\"realism indicator + boundary mesh\") on 2026-05-02. The original framing as a status chip was rejected; reframed as an artificial visualisation overlay alongside dust-particle viz and MC silhouettes.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:57:42Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:39Z","labels":["artificial","ism","visualisation"],"dependencies":[{"issue_id":"stellata-io3.2","depends_on_id":"stellata-74e","type":"relates-to","created_at":"2026-05-02T19:26:13Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-io3.2","depends_on_id":"stellata-io3","type":"parent-child","created_at":"2026-05-02T18:57:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-io3.1","title":"Re-purpose shelved warm-glow MC overlay as part of artificial-viz toggle group","description":"**Goal:** the currently-shelved `cloud.frag.glsl` warm-glow MC overlay (decorative, not physical) is a candidate inhabitant of the artificial-visualisation layer. Decide its role *after* Epic A (`stellata-c7u`) lands physical MC rendering.\n\n**Current state:** `FilterState.showMolecularClouds = false`, panel toggle removed, URL flag bit 2 reserved. Render path, shaders, and data are preserved.\n\n**Question this issue captures:**\n- After Epic A's physical MC rendering ships, do we *also* want the legacy artificial overlay as a separate user-toggleable layer? (Cases for: explicit \"show me the cloud locations\" mode for users who want to find clouds, similar in spirit to the existing molecular-cloud focus / vector / warp UX. Cases against: A.6's subtle stylised presence may already cover this need.)\n- If yes: re-frame the warm-glow renderer as the artificial-viz cloud layer, distinct from Epic A's physical pass. Toggle lives in the artificial-viz group from `stellata-io3`.\n- If no: delete the shelved code path entirely.\n\n**Decision deferred** until Epic A's A.6 lands, so the team can evaluate whether subtle stylised presence is sufficient. This issue is a placeholder for that decision.\n\n**Depends on Epic A's A.6 (`stellata-c7u.6`) for the assessment to make sense — relates_to dependency rather than blocks since neither outcome is required for the epic to ship.**","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:39:30Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:39Z","labels":["artificial","ism","molecular-clouds","visualisation"],"dependencies":[{"issue_id":"stellata-io3.1","depends_on_id":"stellata-c7u.6","type":"relates-to","created_at":"2026-05-02T18:39:36Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-io3.1","depends_on_id":"stellata-io3","type":"parent-child","created_at":"2026-05-02T18:39:30Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-io3","title":"Artificial visualisation layer: structures invisible/near-invisible to the naked eye","description":"**Tracking epic — no decisions yet, just a grouping.**\n\n**Frame:** some structures in the catalogue are essentially invisible (interstellar dust outside dense clouds), nearly invisible (most molecular clouds, against background stars), or only partially visible (faint nebulae, distant SF regions). Showing them at all is a *deliberate non-physical choice* — a visualisation, not a recreation. They belong together as a UX cluster: there should be a clear conceptual line between \"what you'd actually see\" and \"what we're overlaying to help you see structure.\"\n\n**Possible UX shape (deferred):**\n- A single \"artificial overlay\" toggle group in the settings panel — e.g. dust visualisation, MC silhouettes, faint nebulae overlays — clearly labelled as visualisation, not as physical truth\n- Each layer can be independently toggled within the group\n- Off by default? On by default? — design decision later\n- Possibly a single master switch with detail toggles underneath, or a mode-style switch (physical / visualisation / chart-mode)\n\n**What this epic does NOT do today:** make any decisions. It tracks the related issues so a future design pass can treat them as one coherent UX cluster.\n\n**Children (work issues that fit this frame):**\n- `stellata-zq3` — Revisit the dust visualisation layer (the always-artificial dust particle splat layer)\n- (filed under this epic: a child to evaluate re-purposing the currently-shelved warm-glow MC silhouette as part of the artificial-viz toggle group, separate from Epic A's *physical* MC rendering)\n\n**Related but not (yet) reparented:**\n- `stellata-9hp` — Nebulae catalogs (PN, RNe, H II, SNR). Many catalogue nebulae are faint enough to be effectively invisible without aperture; conceptually fits this frame, but it's a self-contained feature with its own scope. Revisit grouping if a unified \"artificial overlay\" UX emerges.\n- `stellata-c7u` (Physical MC rendering epic) — the *physical* counterpart to the artificial MC overlay child below. Both can coexist; they answer different questions (\"what would I see?\" vs \"where is the structure?\").\n\n**No hard dependencies; epic is purely organisational.**","status":"deferred","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:39:06Z","created_by":"Alex Marshall","updated_at":"2026-05-08T11:13:13Z","labels":["artificial","ism","visualisation"],"dependencies":[{"issue_id":"stellata-io3","depends_on_id":"stellata-9hp","type":"relates-to","created_at":"2026-05-02T19:26:13Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-io3","depends_on_id":"stellata-c7u","type":"relates-to","created_at":"2026-05-02T19:26:12Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-c7u.7","title":"A.7 Inside-the-cloud immersion + ambient response tuning","description":"**Goal:** confirm that flying into a cloud reads as physics. The expected experience (per scoping discussion):\n- Stars whose line of sight traverses cloud material in front of the camera dim and redden proportional to remaining-depth.\n- Peripheral stars (off-axis, sightline doesn't traverse cloud) are unaffected.\n- Ambient gets very dark — there's almost no scattered light inside an MC in the visible.\n- Where embedded young stars carve cavities (A.5), local density drops and visibility recovers near them.\n\n**This phase is mostly tuning + verification once A.2–A.6 have landed:**\n- Verify per-star extinction integration handles the camera-inside-cloud case correctly (the raymarch starts inside the medium; integration must work from camera→star regardless of which side of the envelope the camera is on).\n- Tune ambient / fog so darkness is conveyed without losing the sky entirely (some fraction of distant starfield should remain visible — the sky outside the cloud should not be pitch-black if the cloud is finite).\n- Test all known approachable clouds: Taurus, Orion A, Orion B, Perseus, Ophiuchus, Chamaeleon, Lupus, Cepheus, Pipe, Corona Australis. Record any cloud-specific calibration tweaks needed.\n\n**Files likely touched:**\n- `src/client/stellata.ts` — ambient / fog adjustments per camera position relative to cloud\n- `src/client/shaders/star.vert.glsl` — verify camera-inside-medium raymarch correctness\n- Possibly `docs/molecular-clouds.md` for the cloud-by-cloud notes\n\n**Visual gate:** flying into Taurus produces a dimming/reddening experience that feels physical — not a hard fog wall, not unchanged starfield.\n\n**Depends on A.2–A.6.**","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:38:10Z","created_by":"Alex Marshall","updated_at":"2026-05-02T17:53:31Z","labels":["ism","molecular-clouds","visual"],"dependencies":[{"issue_id":"stellata-c7u.7","depends_on_id":"stellata-c7u","type":"parent-child","created_at":"2026-05-02T18:38:10Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.7","depends_on_id":"stellata-c7u.5","type":"blocks","created_at":"2026-05-02T18:38:27Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.7","depends_on_id":"stellata-c7u.6","type":"blocks","created_at":"2026-05-02T18:38:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-c7u.6","title":"A.6 Subtle stylised 'presence' pass for empty-sky cloud silhouette","description":"**Goal:** physical extinction is the dominant signal, but on directions where there are no bright background stars the cloud would be invisible. A *very* subtle stylised pass conveys the cloud's shape without competing with the physical extinction signal.\n\n**Constraints:**\n- Must read as decorative, not as the cloud's body. Users should never mistake the stylised hint for the actual luminous emission of the cloud (MCs barely emit in visible).\n- Easy to under-do; easy to over-do. Tune so the stylisation is invisible against starfields and only emerges over starless directions.\n- Should respect mode: chart mode already has its own isobar contour treatment (`uChartIsobar` path in `cloud.frag.glsl`); coordinate so the stylised presence is dark-mode-only or differs between modes.\n\n**Possible approaches (decision in A.1):**\n- Re-purpose the existing shelved `cloud.frag.glsl` warm-glow as the stylised pass, but with opacity ~0.05–0.1 instead of decorative levels, possibly modulated by absence of background stars\n- Faint fresnel-style edge glow keyed to silhouette direction\n- Substructure noise (from A.4) reading through as faint texture rather than smooth shading\n\n**Files likely touched:**\n- `src/client/shaders/cloud.frag.glsl` — possibly retained, possibly rewritten\n- `src/client/molecular-clouds.ts` — re-enables the layer (`FilterState.showMolecularClouds` default flip)\n- `src/client/controls.ts` — re-add the panel toggle if we want one (decision in A.1)\n- `src/client/url-state.ts` — URL flag bit 2 was reserved; re-encode or leave reserved per A.1 decision\n\n**Visual gate:** flying past Taurus against a starfield reads as physical (stars dim and redden behind it). Flying past Taurus against a star-poor direction reads as a *very faint* hint of cloud shape, never as a luminous blob.\n\n**Depends on A.3, A.4 ideally landed first so the physical signal exists before the decorative pass is layered on top.**","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:37:59Z","created_by":"Alex Marshall","updated_at":"2026-05-02T17:53:30Z","labels":["ism","molecular-clouds","shader","visual"],"dependencies":[{"issue_id":"stellata-c7u.6","depends_on_id":"stellata-c7u","type":"parent-child","created_at":"2026-05-02T18:37:58Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.6","depends_on_id":"stellata-c7u.3","type":"blocks","created_at":"2026-05-02T18:38:25Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.6","depends_on_id":"stellata-c7u.4","type":"blocks","created_at":"2026-05-02T18:38:25Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.6","depends_on_id":"stellata-io3.1","type":"relates-to","created_at":"2026-05-02T18:39:36Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-c7u.5","title":"A.5 Embedded young-star cavities + cloud-type variations (HII, reflection nebulosity)","description":"**Goal:** model the cloud-shaping effect of embedded young stars. O/B stars carve density cavities via stellar winds + ionising radiation (Strömgren spheres, HII regions), and hot enough hosts produce visible emission. This is what gives an active SF region (Orion A) a different inside-the-cloud experience from a quiescent dark cloud (Taurus dark cores).\n\n**Cross-match (build time):**\n- Identify O/B stars and known YSOs whose 3D positions fall inside any Zucker cloud bbox\n- Use ATHYG spectral types for O/B identification\n- For each cloud, list embedded hot stars with: position, spectral type, luminosity proxy\n- Output: per-cloud cavity descriptor in the dust manifest or cloud JSON\n\n**Density modulation:**\n- For each embedded hot star, reduce local density inside a radius scaled by stellar luminosity (Strömgren-sphere-style; constants from A.1)\n- Soft falloff at the cavity edge — `density × (1 − cavity_strength × falloff(r/r_cav))`\n\n**Cloud-type variation:**\n- Quiescent dark cloud (no/few hot embeddings): high peak density, sharp envelope\n- Active SF region (multiple hot embeddings): cavities, lower mean density, optionally an emission overlay\n- HII region: when embedded star is hot enough (≥ early B), add a faint emission/scattering term — a red/pink tinge near the star, fading with distance. Threshold from A.1.\n\n**Reflection nebulosity (optional within scope):** B-type stars in dusty regions backscatter blue light. If included, decide whether it's worth modelling here or deferred to the the nebulae catalog work Nebulae epic (`stellata-9hp`) which already covers reflection nebulae as discrete blob objects. Likely defer.\n\n**Visual gate:** Orion A (multiple O stars) should render visibly differently from Taurus dark-cloud cores (quiescent) — Orion's density should feel \"hollowed\", and the area around hot embedded stars should be perceptibly less dim.\n\n**Depends on A.2 (data pipeline) and A.4 (substructure landscape into which cavities are carved).**","status":"open","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:37:44Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:03:55Z","labels":["ism","molecular-clouds","physics"],"dependencies":[{"issue_id":"stellata-c7u.5","depends_on_id":"stellata-9hp","type":"relates-to","created_at":"2026-05-02T19:26:11Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.5","depends_on_id":"stellata-c7u","type":"parent-child","created_at":"2026-05-02T18:37:43Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.5","depends_on_id":"stellata-c7u.4","type":"blocks","created_at":"2026-05-02T18:38:24Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-c7u.4","title":"A.4 Procedural substructure: 3D noise inside cloud envelope for filamentary density","description":"**Goal:** real MCs are filamentary and clumpy with a log-normal/power-law density PDF, not smooth ellipsoids. Substructure makes the cloud feel real — both visually (where the subtle stylised pass A.6 reads filaments) and physically (extinction varies along sightlines through the cloud, not just by depth).\n\n**Approach:** procedural 3D noise (fBm and/or Worley/Voronoi) shaped by the ellipsoid envelope from Zucker. Octaves and amplitudes calibrated in A.1. The noise modulates density inside the envelope; outside the envelope density goes to zero smoothly.\n\n**Where the noise is sampled — design decision in A.1:**\n- (a) Bake into the dust voxel grid in `build-dust.py` — single source of truth, but voxel resolution caps detail.\n- (b) Evaluate per-fragment in `star.vert.glsl` raymarch — finer detail, but more shader cost per star and harder to integrate.\n- (c) Hybrid: voxel-scale noise baked in, sub-voxel detail evaluated in shader for the close-camera case.\n\n**Implementation depends on (a/b/c):**\n- (a) → `scripts/build-dust.py`\n- (b) → `src/client/shaders/star.vert.glsl` plus a noise function include\n- (c) → both\n\n**Visual gate:** a sightline grazing the edge of Taurus should show patchy/filamentary extinction (not a smooth ellipsoid darkening), and the subtle stylised presence pass (A.6) should read structured, not blob-like.\n\n**Depends on A.2.**","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:37:27Z","created_by":"Alex Marshall","updated_at":"2026-05-02T17:53:29Z","labels":["ism","molecular-clouds","shader"],"dependencies":[{"issue_id":"stellata-c7u.4","depends_on_id":"stellata-c7u","type":"parent-child","created_at":"2026-05-02T18:37:27Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.4","depends_on_id":"stellata-c7u.2","type":"blocks","created_at":"2026-05-02T18:38:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-c7u.3","title":"A.3 Wavelength-selective extinction: redden background stars passing through dust","description":"**Goal:** the per-star extinction layer in `star.vert.glsl` should redden background stars, not just dim them. This is the dominant *visible* signature of an MC against rich star fields and is what makes a dust lane look like a dust lane.\n\n**Investigate first:** does the current per-star extinction path apply a single grey magnitude penalty, or does it already separate B/R? Confirm during research and adjust scope accordingly. (A.1 should answer this; record findings here.)\n\n**Model:** standard interstellar reddening with RV = A_V / E(B−V) ≈ 3.1 (Cardelli-Clayton-Mathis 1989). Compute E(B−V) per star from the integrated optical depth, then apply A_B = A_V × (RV+1)/RV and A_R proportionally. Use existing per-star B and V (or B-V) channels in the catalog binary.\n\n**Implementation:**\n- Touch: `src/client/shaders/star.vert.glsl`, possibly `star.frag.glsl`\n- Add a reddening factor in the colour pipeline so the displayed star colour shifts toward red proportional to extinction depth, on top of the existing dimming\n- Verify chart-mode and observer mode both pick up the reddening (or one of them is intentionally exempt — decide in A.1)\n\n**Visual gate:** an O/B star (intrinsically blue) sitting behind ~10 mag of MC dust should render distinctly red, not just dim blue.\n\n**Depends on A.2** for the calibrated dust density. Doesn't depend on A.4 (substructure) or A.5 (cavities) — those refine the density field A.3 reads from.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:37:12Z","created_by":"Alex Marshall","updated_at":"2026-05-02T17:53:29Z","labels":["extinction","ism","molecular-clouds","shader"],"dependencies":[{"issue_id":"stellata-c7u.3","depends_on_id":"stellata-c7u","type":"parent-child","created_at":"2026-05-02T18:37:12Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.3","depends_on_id":"stellata-c7u.2","type":"blocks","created_at":"2026-05-02T18:38:23Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-c7u.2","title":"A.2 Build pipeline: bake Zucker MC density into Edenhofer dust voxel volume","description":"**Goal:** make the per-star extinction raymarch in `star.vert.glsl` the single source of truth for absorption, with Zucker MCs supplying higher-fidelity local density inside the Edenhofer volume. No double-counting between the two layers.\n\n**Touches:**\n- `scripts/build-dust.py` — at the end of the existing dust-volume bake, overlay Zucker MC contributions per voxel. Each cloud's density profile (centroid, semi-axes, density model from A.1) replaces or augments the underlying Edenhofer value where they overlap.\n- `data/dust/manifest.json` — bump version; record that Zucker has been merged in.\n- `scripts/build-clouds.py` — likely needs to emit the per-cloud parameters consumed by `build-dust.py` (peak density, profile shape, etc.) in addition to today's bbox/centroid.\n\n**Decision points (resolved in A.1, applied here):**\n- Replace vs additive blend in overlap voxels\n- Whether procedural substructure (A.4) is baked into the voxel field at build time, or applied per-fragment in the shader at runtime, or both\n- Calibration: ensure that flying through Taurus produces reasonable Av (~5–15+ mag) rather than under- or over-extinction\n\n**Sanity gate:** `scripts/verify-catalog.ts`-equivalent check for the dust manifest — pick a cloud (e.g. Taurus), compute a sightline through its centre, confirm Av matches the literature value within a factor of ~2.\n\n**Out of scope:** the shader-side reddening (A.3) and substructure noise (A.4) are downstream. This phase produces the data they consume.","status":"open","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:37:01Z","created_by":"Alex Marshall","updated_at":"2026-05-02T17:53:28Z","labels":["build-pipeline","ism","molecular-clouds"],"dependencies":[{"issue_id":"stellata-c7u.2","depends_on_id":"stellata-c7u","type":"parent-child","created_at":"2026-05-02T18:37:00Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-c7u.2","depends_on_id":"stellata-c7u.1","type":"blocks","created_at":"2026-05-02T18:38:22Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"stellata-47c.2","title":"Mobile Safari chrome polish + Add-to-Home-Screen PWA path","description":"Goal: a deliberately-mobile-aware chrome layout in iOS Safari, plus a polished Add-to-Home-Screen (PWA standalone) path for users who want full-bleed.\n\nWHAT WAS LEARNED (from PR #6, abandoned):\n- Browser iOS Safari does NOT let arbitrary content paint behind the URL bar while the URL bar is at full size, regardless of `viewport-fit=cover` + `100lvh` + safe-area-inset. The chrome stays opaque.\n- The 'modern site' behaviour the user remembers (URL bar shrinks; canvas extends behind) only kicks in for *scrollable* pages — iOS minimises the URL bar after a user scroll. Stellata is fixed-viewport (overflow: hidden) so there's no scroll, so the URL bar never minimises, so full-bleed never engages.\n- Workarounds (artificially-scrollable body, programmatic scroll-to-1px) are fragile across iOS versions and fight our gesture-capturing canvas.\n- Standalone PWA (Add to Home Screen) has none of these limits — no URL bar at all, full-bleed by default.\n- We did achieve correct sizing of the GL buffer relative to the layout viewport via documentElement.clientWidth/clientHeight (vs window.innerWidth/innerHeight). That code is in PR #6's history if we want to recover it, but the user opted to revert and start fresh under a re-scoped issue.\n\nNEW SCOPE:\n1. Polish what we can in browser Safari without chasing impossible full-bleed:\n   - Confirm + fix scale-bar / settings panel positioning so they sit cleanly above the URL bar (use `100vh - 100dvh` / safe-area-inset-bottom thoughtfully, not sum).\n   - Add the meta tags that improve the experience: `viewport-fit=cover`, `apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style=black-translucent`, `theme-color`.\n   - Avoid the `100% / inner*` buffer-vs-CSS stretch: size the GL buffer to the layout viewport (documentElement.clientWidth/clientHeight) so stars don't squash on iOS.\n2. Make Add-to-Home-Screen produce a clean standalone experience:\n   - Manifest.json with name, short_name, theme_color, background_color, icons (192/512 PNG), display: standalone.\n   - Apple touch icon link in \u003chead\u003e.\n   - First-launch behaviour in standalone mode (no URL bar; full-bleed by default).\n\nDEPENDS ON 47c.3 (design-decision) — the gating-vs-recommend question changes how aggressive this work is. If we hard-block phone viewports, this issue shrinks to 'just AtHS polish'. If we soft-warn, this is the scaffolding for a deliberately mobile-aware chrome layout.\n\nOUT OF SCOPE: touch gestures (split into 47c.4); any layout work for the settings panel itself (covered by the existing panel-rework design issue 1yu).","notes":"Implementation landed on claude/add-mobile-and-ux-issues:\n- index.html: viewport-fit=cover, theme-color, apple-mobile-web-app-capable, status-bar-style=black-translucent\n- styles.css: html/body height -\u003e 100dvh; #scene + .overlay sized to 100dvw/100dvh so the CSS rect tracks the iOS visual viewport (matching the GL buffer that's already sized to window.innerHeight); modal max-height + kb-modal margin migrated to dvh; the four edge-anchored chrome containers (.ui-top, .ui-top-left, .ui-bottom, .warp-btn) wrapped with max(\u003cn\u003epx, env(safe-area-inset-*)) / calc(\u003cn\u003epx + env(safe-area-inset-*)) so they clear the notch / Dynamic Island / home indicator without changing desktop spacing.\n- Three.js resize path unchanged: window.resize already fires on iOS URL-bar transitions and feeds window.innerWidth/innerHeight into renderer.setSize / camera.aspect.\n- npm run typecheck and npm test (244 tests) pass; npm run build succeeds.\n\nNOT yet verified on real iPhone hardware — leaving in_progress until that verification is done. Device checklist from the issue body still applies: real iPhone Safari portrait + landscape, Add to home screen launch, and Android Chrome regression check.","status":"deferred","priority":3,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:40:00Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:37Z","started_at":"2026-05-02T18:39:07Z","dependencies":[{"issue_id":"stellata-47c.2","depends_on_id":"stellata-47c","type":"parent-child","created_at":"2026-05-02T17:39:59Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-47c.2","depends_on_id":"stellata-qsg","type":"blocks","created_at":"2026-05-02T20:46:35Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-47c.1","title":"Audit mobile UX and produce subtask breakdown under the mobile epic","description":"Investigative subtask whose deliverable is the rest of the mobile epic's subtask list. Walk through every documented interaction on a real iPhone and a real Android device (or emulators if hardware is unavailable) and document what's broken, missing, or degraded.\n\nCoverage checklist (with the relevant source for each):\n- Camera controls — src/client/stellata.ts (TrackballControls), src/client/observe-controls.ts (look-around + FOV).\n- Settings panel — src/client/controls.ts, src/client/panel-layout.ts, src/client/styles.css.\n- Modals — src/client/info-modal.ts, brand-modal.ts, help-modal.ts, the goModal/conModal plumbing in keyboard-shortcuts.ts, and constellation-typeahead.ts.\n- Search inputs — src/client/search.ts, src/client/constellation-typeahead.ts.\n- Top-bar \u0026 affordances — src/client/warp-button.ts, mode-toggle.ts, brand-box markup in src/client/index.html.\n- Overlays — every *-overlay.ts and disc-mask.ts, plus scale-bar.ts.\n- Render perf baseline — src/client/main.ts + the perf hooks noted in docs/performance.md (debug.perf()); record FPS numbers in the issue notes.\n\nDeliverable: file the follow-up subtasks under the parent epic, with rough priorities. Then close this issue.\n\nOut of scope: actually fixing anything. This issue is investigation only.\n","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:39:44Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:37Z","dependencies":[{"issue_id":"stellata-47c.1","depends_on_id":"stellata-47c","type":"parent-child","created_at":"2026-05-02T17:39:43Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-47c","title":"Mobile experience: make Stellata workable on phones / small touch devices","description":"Stellata today is built and tuned for desktop — TrackballControls with mouse + wheel, a fixed right-side settings panel, modals sized for desktop viewports, and a render budget tuned for discrete GPUs. The result on phones is degraded and in places unusable: gestures don't map cleanly (no pinch-to-zoom, two-finger roll is desktop-Safari-only by design), the panel covers the canvas, modals overflow, and performance has not been characterised on mobile chips.\n\nGoal: make Stellata a deliberate, usable mobile experience without compromising the desktop product. Concrete subtasks are deferred to the audit issue below — touch gestures, responsive panel layout, modal sizing, mobile-perf characterisation, and any catalog/dust downscaling decisions are the likely categories but should be confirmed by the audit before being broken out.\n\nOut of scope (for now): a separate mobile build, native app packaging, replacing TrackballControls wholesale on desktop.\n\nCritical files (for orientation when subtasks are scoped):\n- src/client/controls.ts — right-side settings panel layout/widgets.\n- src/client/stellata.ts — TrackballControls wiring + camera state machine.\n- src/client/observe-controls.ts — drag/yaw/pitch + wheel FOV (desktop-tuned).\n- src/client/styles.css, src/client/index.html — viewport meta, layout containers.\n- docs/camera-modes.md, docs/ui-and-controls.md — current desktop assumptions (two-finger roll = Safari-only, panel reverse-sync, etc.).\n\nThis epic closes only when its subtasks are all closed.\n","status":"deferred","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:39:28Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:37Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-p69","title":"Further compress the view URL parameter","description":"The `?v=` blob (schema v2, in `src/client/url-state.ts`) pays a fixed 12-byte cost per vec3 (`cam`, `tgt`, `up`) whenever **any single component** diverges from its default. A camera at `[0, 0, 3.7]` — the common case for a near-Sol orbit on the z-axis — burns 8 bytes encoding two zero-valued floats.\n\nWorked example: `https://stellata.xyz/?v=AgEAAAAAAAAAAAAAPtRsQA` decodes to:\n\n- byte 0: `0x02` — version 2\n- bytes 1–3: `0x000001` — presence mask, only bit 0 (`cam`) set\n- bytes 4–15: cam float32 LE × 3 = `[0.0, 0.0, ~3.7004]`\n\n→ 16 bytes, 22 base64url chars. 8 of those bytes (≈11 chars) are zero floats.\n\nTwo compression opportunities:\n\n1. **Per-component vec3 encoding.** Only emit floats for components that diverge from the per-component default. The defaults already vary per vec3 (`cam=[0,0,30]` navigate / `[0,0,0]` observe; `tgt=[0,0,0]`; `up=[0,1,0]`), so a per-component compare is the natural extension of the existing whole-vec3 default check at `url-state.ts:614-623`.\n\n2. **Varint presence mask.** The 24-bit mask is often near-zero in its high bytes (the example has `0x00 0x00` for bytes 2–3). LEB128 (7 bits payload + continuation bit) collapses single-bit masks to 1 byte instead of 3.\n\nThe exponent-notation idea from the original sketch (encode `1.24` in fewer bytes than full float32) is harder to land cleanly without losing round-trip exactness. float32 is already a binary-exponent format; shrinking to float16 trades ~3 sig-figs of precision and breaks at galactic distances. Keeping it as out-of-scope for the first pass — possibly worth revisiting just for the `up` vector, which is always normalised and ≤1.0 per component.\n\nHeadline impact for the example URL: 22 chars → ~12 chars (≈45% shorter), with no precision loss.\n","design":"# Schema v3 (in `src/client/url-state.ts`)\n\nBump `SCHEMA_VERSION` from 2 to 3. Keep `FIELDS_V1`/`decodeV1` and `FIELDS_V2`/`decodeV2` frozen verbatim — same pattern already in use. Add `FIELDS_V3` and `decodeV3`. The auto-upgrade rewrite at `applyFromUrl:778` already triggers when `decoded.version !== SCHEMA_VERSION`, so legacy URLs in the address bar will be silently rewritten to v3 on next load.\n\n## Change 1 — per-component vec3 encoding (primary win)\n\nReplace `vec3Field` for v3 with a variant that emits a 1-byte sub-mask prefix:\n\n```\n[1 byte: lower 3 bits = which components diverge from default]\n[for each set bit: 4-byte f32 LE]\n```\n\nPer-vec3 byte cost (vs. v2's flat 12 bytes whenever the field is present):\n\n| diverging components | v3 bytes  | v2 bytes | Δ  |\n|----------------------|-----------|----------|-----|\n| 0 (field omitted)    | 0         | 0        | 0   |\n| 1                    | 1 + 4 = 5 | 12       | −7  |\n| 2                    | 1 + 8 = 9 | 12       | −3  |\n| 3                    | 1 + 12 = 13 | 12     | +1  |\n\n`isPresent` returns true iff any component diverges from the per-component default — same trigger as today, just per-component. Components with bit clear fill in from the per-component default at decode time.\n\n### Wrinkle: `cam` default depends on mode\n\nThe encoder already handles this at `currentStateOf:614` (`camDefault = mode === 'observe' ? [0,0,0] : DEFAULT_CAM`). The decoder doesn't know mode at the point it decodes `cam`, because mode comes from the `flags` field (bit 13). Two options:\n\n- **(a) Two-pass decode.** Decode all fields, then post-fix `cam` defaults using the now-known mode. Cheap (one extra branch after the field loop) but couples decode order.\n- **(b) Always treat cam default as `[0,0,30]` on the wire.** Simpler, but sacrifices the savings on observe-mode shares where `cam.z=0`.\n\n**Recommend (a).** The post-decode fix is a single `if (mask \u0026 cam_bit \u0026\u0026 view.cam) { ... }` check after the loop, reading `view.mode` from the already-decoded `flags`. No structural change to the field-loop machinery.\n\n### Defaults table\n\nHoist a per-key default table at module scope so encoder and decoder share one source of truth:\n\n```ts\nconst VEC3_DEFAULTS_NAVIGATE: Record\u003c'cam'|'tgt'|'up', readonly [number,number,number]\u003e = {\n  cam: [0, 0, 30], tgt: [0, 0, 0], up: [0, 1, 0],\n};\nconst VEC3_DEFAULTS_OBSERVE_CAM: readonly [number, number, number] = [0, 0, 0];\n```\n\n## Change 2 — varint presence mask (secondary win, separate commit)\n\nReplace the fixed 3-byte mask with LEB128:\n\n- 7 bits payload + 1 continuation bit per byte\n- Mask `0x000001` → 1 byte (saves 2)\n- Mask `0x000800` → 2 bytes (saves 1)\n- Mask `0x080001` → 3 bytes (no change)\n\n`encodeBlob` writes the varint after the version byte; `decodeV3` reads the varint and tracks the resulting offset before stepping through fields. No changes to per-field encode/decode contracts.\n\nLand as a follow-up commit on top of Change 1 so the v3 round-trip suite already exists when the mask format moves.\n\n## Test plan\n\n`src/client/url-state.test.ts` already has byte-length assertions and round-trip suites. Extend with:\n\n- v3 round-trip block — copy the pattern from the existing vec3 / scalar / focus / POI suites, but assert tighter byte budgets (e.g. `cam=[0,0,3.7]` blob ≤ 9 bytes, ≤ 12 base64url chars).\n- v2-decode regression block — clone the v1-decode regression at `url-state.test.ts:421+`. Encode a known view via the v2 path (need a tiny test helper that runs `FIELDS_V2`-flavoured encode), feed to `decodeBlob`, assert the same `view` falls out and `version === 2`.\n- Component-default fill tests — `roundtrip({ cam: [0, 0, 3.7] })` returns `cam: [0, 0, 3.7]` (decoder fills the omitted x/y from default). `roundtrip({ tgt: [0, 0, 0] })` omits the field entirely. Mode-aware cam default: encode with `mode: 'observe'` and `cam: [0, 0, 0]` → field omitted; with navigate and `cam: [0, 0, 0]` → cam present (because navigate default is `[0, 0, 30]`).\n- Update the empty-blob length assertion at `url-state.test.ts:18` if Change 2 lands (varint mask shrinks empty to 2 bytes / 3 chars).\n\n## Out of scope\n\n- float16 / varint-mantissa float encoding for cam/tgt. Addressed in description — defer.\n- float16 for `up` only (always normalised, ≤1.0). Tempting; revisit once Change 1 is landed and we have measurements.\n- Compressing other fields (`spect` is a 9-bit mask, can't shrink; `dmin`/`dmax` u16 already maps to slider step count, u8 would lose granularity).\n\n## Files touched\n\n- `src/client/url-state.ts` — primary\n- `src/client/url-state.test.ts` — regression coverage\n","acceptance_criteria":"- Existing v1 and v2 URLs continue to decode unchanged (regression suite enforces this).\n- `?v=` blob for `cam = [0, 0, 3.7]` with no other state drops from 22 base64url chars to ≤14 chars (and ≤12 chars after Change 2).\n- Default state still produces no `?v=` query param at all.\n- Fully-populated vec3s (every component non-default) grow by at most 1 byte per vec3 — accepted trade.\n- Round-trip exactness preserved for every v3 field (no precision loss vs. v2 within the schema's already-quantised scalar grid).\n- Vitest suite extended with v3 round-trips and a v2-decode regression block matching the existing v1-regression coverage at `url-state.test.ts:421+`.\n- Address bar auto-rewrites legacy URLs to v3 on load (existing `applyFromUrl` upgrade pattern, no new code needed).\n- Mode-dependent `cam` default (`[0,0,30]` navigate / `[0,0,0]` observe) handled correctly on encode and decode; covered by tests.","notes":"PR #54 opened (worktree-stellata-p69-url-compress). Two-commit roll-up: per-component vec3 sub-mask + LEB128 presence mask. Headline cam=[0,0,3.7] URL drops 22 chars → 10. v1 + v2 decoders frozen; auto-upgrade rewrite handles legacy URLs. Leave in_progress until PR merges.","status":"closed","priority":3,"issue_type":"feature","assignee":"Alex Marshall","created_at":"2026-05-02T16:18:09Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:17:31Z","started_at":"2026-05-10T09:05:32Z","closed_at":"2026-05-10T18:17:31Z","close_reason":"Shipped in PR #54","dependencies":[{"issue_id":"stellata-p69","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-06T22:19:19Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-8c1.5","title":"Update CLAUDE.md + add docs/site.md (final commit)","description":"Updates internal-developer documentation to reflect the new /about + /docs site. Lands LAST per project memory 'defer-doc-updates' — don't edit CLAUDE.md, README.md, anything under docs/, or SCIENCE.md while feature work is in flight.\n\nTasks:\n- CLAUDE.md repo-layout section — add the new content/, src/client/about.html, src/client/site*, src/client/_docs-template.html, src/client/docs/, scripts/build-docs.ts, scripts/vite-plugin-docs.ts, public/robots.txt, sitemap.xml, favicon.svg, public/images/.\n- CLAUDE.md 'Local commands' section — add 'npm run build:docs'.\n- CLAUDE.md 'Documentation index' section — add a new 'docs/site.md' bullet pointing readers there for the public-site build pipeline.\n- Create docs/site.md — internal-developer doc covering: markdown-it pipeline, build-docs.ts behaviour, vite-plugin-docs hot-reload mechanics, multi-entry rollup config, where /about and /docs/* HTML files are sourced, sitemap regeneration, and how to add a new docs page.\n- README.md — short note about /about and /docs being separate from the app (one paragraph max).\n\nVerify before close:\n- CLAUDE.md still under the 200-line system-reminder budget when truncated (the file is loaded in full per CLAUDE.md convention, but the index summary line should stay tight)\n- docs/site.md is concise and references existing files rather than duplicating their content\n\nOnce this closes, parent stellata-8c1 can be closed too.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:02:02Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:38Z","dependencies":[{"issue_id":"stellata-8c1.5","depends_on_id":"stellata-8c1","type":"parent-child","created_at":"2026-05-02T17:02:01Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8c1.5","depends_on_id":"stellata-8c1.2","type":"blocks","created_at":"2026-05-02T17:02:02Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8c1.5","depends_on_id":"stellata-8c1.3","type":"blocks","created_at":"2026-05-02T17:02:02Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8c1.5","depends_on_id":"stellata-8c1.4","type":"blocks","created_at":"2026-05-02T17:02:02Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-8c1.4","title":"/ SEO additions + brand-box Docs button + About-modal link","description":"Wires cross-page navigation between / and the new /about + /docs URLs, and adds basic SEO to the existing /. Depends on stellata-8c1.1 (the new URLs must exist before we link to them, even as skeletons).\n\nTasks (all in src/client/index.html unless noted):\n- Add per-page meta to / — \u003ctitle\u003e stays 'Stellata — Interactive Star Catalog', plus \u003cmeta name='description'\u003e, \u003clink rel='canonical'\u003e, OG tags (og:title, og:description, og:image, og:url, og:type=website), Twitter Card equivalents, favicon link.\n- Add a \u003cnoscript\u003e block describing what Stellata is + link to /about (crawlable, only shown to crawlers / no-JS users — acceptable since the app needs WebGL anyway).\n- Insert a 'Docs' button between #brand-about and #brand-credits (index.html:218-225). It is a plain \u003ca href='/docs'\u003e, not a modal — so brand-modal.ts likely needs no edit. Match existing .link-btn styling.\n- Add a 'Read more →' link inside the About modal (index.html:131-167) pointing to /about. Existing modal copy stays.\n- Credits modal stays unchanged; /docs/the-data expands on it.\n- Confirm public/sitemap.xml (generated by build-docs.ts in the pipeline issue) lists / with priority 1.0 and recent lastmod.\n\nVerify before close:\n- 'View source' on / shows meta description, OG tags, noscript block, favicon link\n- Brand box on / shows About · Docs · Credits in that order\n- Clicking Docs goes to /docs; clicking About opens modal which now has a 'Read more →' link to /about\n- Lighthouse SEO score on / ≥ 95 (was below before — main goal of this issue)\n- /sitemap.xml lists / + /about + every /docs/* URL","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:01:51Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:38Z","dependencies":[{"issue_id":"stellata-8c1.4","depends_on_id":"stellata-8c1","type":"parent-child","created_at":"2026-05-02T17:01:51Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8c1.4","depends_on_id":"stellata-8c1.1","type":"blocks","created_at":"2026-05-02T17:01:51Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-8c1.3","title":"/docs/* markdown content for all 7 pages","description":"Drafts user-facing markdown for every /docs/* page. Depends on stellata-8c1.1 (pipeline must exist first).\n\nCRITICAL: Drafted content must be sourced from CURRENT code/data, not from memory or assumption. There is a project memory entry about famous stars that don't appear in the catalog (μ Cep et al). The limitations.md page deliberately mirrors that.\n\nPages (write all 7 in this single issue):\n- content/docs/index.md — TOC + intro\n- content/docs/navigate-mode.md — TrackballControls, click states, focus ring, distance vector, panel filters, magnitude presets, warp button. Source: docs/architecture.md, docs/camera-modes.md, src/client/controls.ts, warp-button.ts.\n- content/docs/observe-mode.md — drag yaw/pitch, wheel FOV, two-finger roll (Safari-only desktop), pinned POIs. Source: docs/camera-modes.md, src/client/observe-controls.ts, mode-toggle.ts.\n- content/docs/keyboard-shortcuts.md — mirror of help-modal content + the shortcuts dispatched in keyboard-shortcuts.ts. Source: src/client/help-modal.ts, keyboard-shortcuts.ts.\n- content/docs/the-data.md — AT-HYG v3.3, GCVS, Hipparcos CCDM, Stellarium constellation lines, Edenhofer dust, Zucker molecular clouds (note: shelved). Link to SCIENCE.md for full citations. Source: SCIENCE.md, docs/build-and-data.md.\n- content/docs/the-physics.md — magnitudes \u0026 physical radius, spectral class → temperature → colour, super-Gaussian intensity profile, variable pulsation (R ∝ √L assumption), dust extinction, Milky Way volumetric model, magnitude consistency conversion. Source: docs/rendering.md, docs/milky-way.md, SCIENCE.md.\n- content/docs/limitations.md — proper motion not animated, missing stars (μ Cep et al), all-sky dust raymarch deferred, irregular variables skipped, temperature swing in variables not modelled, rectangular block artifact past mag ~9, molecular clouds shelved for v1. Source: CLAUDE.md 'Things deliberately kept out' + 'Temporarily shelved' sections, project memory.\n\nEach markdown file gets frontmatter: title, description (used for \u003ctitle\u003e, meta description, OG tags via the docs template).\n\nPublic docs should be friendlier and shorter than internal docs/* — not a 1:1 copy. Cite SCIENCE.md and the GitHub repo for deep-dive readers.\n\nVerify before close:\n- /docs index TOC renders, sidebar shows all 7 pages\n- Each /docs/\u003cpage\u003e renders end-to-end, anchor links work, sidebar highlights current page\n- No factual claims contradicted by current src/ or data/ — spot-check the limitations page against project memory and CLAUDE.md\n- Lighthouse SEO score on /docs ≥ 95","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:01:34Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:38Z","dependencies":[{"issue_id":"stellata-8c1.3","depends_on_id":"stellata-04y","type":"blocks","created_at":"2026-05-10T22:54:52Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8c1.3","depends_on_id":"stellata-8c1","type":"parent-child","created_at":"2026-05-02T17:01:33Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8c1.3","depends_on_id":"stellata-8c1.1","type":"blocks","created_at":"2026-05-02T17:01:33Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8c1.3","depends_on_id":"stellata-e64","type":"blocks","created_at":"2026-05-10T22:54:53Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-8c1.2","title":"/about content + screenshots + site.css","description":"Fills out the /about landing page with real content and design-led styling. Depends on stellata-8c1.1 (build pipeline must exist first so /about even renders).\n\nSections (from original plan):\n1. Hero — name + one-sentence positioning + screenshot of the app + 'Open the explorer →' CTA linking to /\n2. What it is — 2–3 short paragraphs (what you're looking at, coordinate origin, scale)\n3. What you can see — iconographic feature grid (313k stars, variable pulsation, Milky Way volumetric, dust extinction, constellation lines, Hipparcos doubles, two camera modes)\n4. Try this — suggested destinations as deep links into / with URL state (Sirius for binarity, Polaris for variability, Antares for size, etc.)\n5. The data — short summary + link to /docs/the-data\n6. About / built by — Alex, GitHub link, AGPL note\n\nDrafting sources (read these — don't write from memory):\n- CLAUDE.md, SCIENCE.md, docs/architecture.md, docs/rendering.md\n- src/client/index.html:131-176 (existing About + Credits modal copy — this is the seed)\n- src/client/brand-modal.ts\n\nTasks:\n- Write src/client/about.html with the full hand-written sections above + per-page meta tags (title, description, canonical, OG, Twitter Card).\n- Capture screenshots → public/images/about/. Alex captures these manually; in implementation, leave \u003cimg\u003e tags pointing at expected paths and call out the missing assets in the PR description.\n- Expand src/client/site.css with shared header/footer + /about-specific hero + feature-grid styles. Reuse CSS custom properties from styles.css (--bg, --fg, --accent, font stacks).\n- Implement minimal mobile nav toggle in src/client/site/site.ts.\n\nVerify before close:\n- /about renders end-to-end in dev with all sections\n- 'Open the explorer →' returns to /\n- Lighthouse SEO score on /about ≥ 95\n- No CSS leakage between site.css and styles.css (the app at / still renders correctly)","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:01:11Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:37Z","dependencies":[{"issue_id":"stellata-8c1.2","depends_on_id":"stellata-8c1","type":"parent-child","created_at":"2026-05-02T17:01:10Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-8c1.2","depends_on_id":"stellata-8c1.1","type":"blocks","created_at":"2026-05-02T17:01:10Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"stellata-8c1.1","title":"Build pipeline + multi-entry Vite + skeleton pages","description":"Lays the plumbing for the /about and /docs work. Goal: by the end of this issue, /about and /docs/index render as empty/skeleton pages and the build pipeline regenerates docs HTML on .md change in dev.\n\nScope:\n- Add markdown-it + @types/markdown-it + markdown-it-anchor + gray-matter as devDependencies (use Node 22 fs.globSync rather than the glob package; verify node --version ≥ 22 first).\n- Create scripts/build-docs.ts — reads content/docs/*.md, applies gray-matter frontmatter, renders body via markdown-it (+ anchor plugin), wraps with src/client/_docs-template.html, writes to src/client/docs/\u003cslug\u003e.html. Also writes public/sitemap.xml from the same file list.\n- Create scripts/vite-plugin-docs.ts — buildStart runs build-docs.ts once; configureServer chokidar-watches content/docs/**/*.md and the template, regenerates on change, sends Vite HMR full-reload signal.\n- Update vite.config.ts to use a multi-entry rollupOptions.input via fs.globSync('src/client/**/*.html', { ignore: '**/_*.html' }) and register vite-plugin-docs.\n- Create src/client/_docs-template.html with placeholders ({{TITLE}}, {{DESCRIPTION}}, {{CONTENT}}, {{SIDEBAR}}, {{CANONICAL}}).\n- Create src/client/about.html as a minimal skeleton (title + h1 only — content lands in the about issue).\n- Create src/client/site.css (minimal; full styling lands in the about issue).\n- Create src/client/site/site.ts (empty stub for mobile nav toggle / smooth-scroll).\n- Create content/docs/index.md as a skeleton (frontmatter + a heading; full content lands in docs-content issue).\n- Add public/robots.txt (allow all + sitemap link) and public/favicon.svg (small SVG mark).\n- Update package.json: add 'build:docs': 'tsx scripts/build-docs.ts' and wire it into 'build' (after build:dust-sync, before build:client) and 'dev' (run once before vite, plugin handles incremental).\n- Update .gitignore: add src/client/docs/ (generated).\n\nVerify before close:\n- npm run typecheck passes\n- npm test still passes (244 tests, no behavioural change to app code)\n- npm run build:docs alone produces src/client/docs/index.html\n- npm run build produces dist/about.html and dist/docs/index.html with hashed asset refs\n- npm run dev: editing content/docs/index.md triggers a Vite reload\n- curl-test deployed preview that /docs/index serves at both /docs/index and /docs/index/\n\nThis issue blocks: about, docs-content, seo-nav.","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:00:49Z","created_by":"Alex Marshall","updated_at":"2026-05-06T21:19:37Z","dependencies":[{"issue_id":"stellata-8c1.1","depends_on_id":"stellata-8c1","type":"parent-child","created_at":"2026-05-02T17:00:49Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"stellata-8c1","title":"Public site (/about + /docs)","description":"The public-facing supporting site for the Stellata WebGL app. Adds a design-led /about landing page for first-time visitors arriving from search/social, plus a /docs/* documentation site (5–8 pages) covering both camera modes, the science behind the render, and the model's limitations. The app stays at / so direct traffic still lands in the WebGL explorer.\n\nFull design lives in:\n/Users/alexm/.claude/plans/i-want-to-create-refactored-feather.md (original plan)\n/Users/alexm/.claude/plans/resume-planning-of-users-alexm-claude-pl-bright-origami.md (resumed plan with beads breakdown)\n\nURL structure:\n- / — existing WebGL app (unchanged)\n- /about — single hand-written design-led landing page\n- /docs — docs index / TOC\n- /docs/navigate-mode, /docs/observe-mode, /docs/keyboard-shortcuts, /docs/the-data, /docs/the-physics, /docs/limitations\n\nBuild pipeline: markdown-it + a tiny build-docs.ts generator + a vite-plugin-docs hot-reload wrapper, with multi-entry rollup input via fs.globSync. /about is a hand-written HTML file. Docs pages are generated from content/docs/*.md into src/client/docs/*.html (gitignored), then run through Vite's normal asset-hashing pipeline.\n\nThis feature closes only after all child tasks are closed and the verification block from the original plan passes:\n- **stellata-8c1.1** — build pipeline + multi-entry Vite + skeleton pages\n- **stellata-8c1.2** — /about content + screenshots + site.css\n- **stellata-8c1.3** — /docs/* markdown content for all 7 pages (consumes the two source-content beads below)\n- **stellata-8c1.4** — / SEO additions + brand-box Docs button + About-modal link\n- **stellata-8c1.5** — update CLAUDE.md + add docs/site.md (final commit)\n- **stellata-04y** — source content for /docs/the-data — star designator reference (HIP / HD / HR / Bayer / Flamsteed / Gl / TYC catalogue provenance and scope). Blocks 8c1.3.\n- **stellata-e64** — source content for /docs/the-physics — rendering-decisions FAQ (PSF model, two-size sources, distance behaviour, what's not modelled). Blocks 8c1.3.\n\nOut of scope: docs search, versioned docs, multi-language, blog/changelog page, replacing the in-app first-visit modal.","status":"deferred","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-02T16:00:01Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:55:25Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-zxy","title":"Rectangular block artifact in default star render","description":"**Status:** pre-existing rendering artifact, unrelated to ISM work, debug after ISM lands.\n\nRectangular block artifact in the default star render. Pre-dates the dust work, parking until ISM is done.\n\nSee `memory/project_rectangular_blocks_todo.md` for any prior investigation notes.\n","notes":"Related to stellata-a7d (physical-scale rewrite). The rewrite touches star.vert.glsl heavily; investigate this artifact in the rewrite branch to see whether it persists, is fixed incidentally, or worsens — but do not block the rewrite on this bug.\nRe-scoped 2026-05-02: bug is a dataset artifact in the current classic_ids subset, not a renderer bug. Will likely disappear (or shift) when the catalog upgrade in stellata-cns lands. Revisit during/after that upgrade rather than fixing standalone.","status":"open","priority":3,"issue_type":"bug","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:38Z","created_by":"Alex Marshall","updated_at":"2026-05-02T17:53:26Z","labels":["rendering"],"dependencies":[{"issue_id":"stellata-zxy","depends_on_id":"stellata-a7d","type":"relates-to","created_at":"2026-05-02T18:14:21Z","created_by":"Alex Marshall","metadata":"{}"},{"issue_id":"stellata-zxy","depends_on_id":"stellata-cns","type":"parent-child","created_at":"2026-05-02T18:53:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-bk5","title":"Exoplanets around host stars","description":"**Status:** Standalone. No phase dependencies — could ship anytime.\n\n**Goal:** when the user zooms into a host star with confirmed exoplanets, render them as orbiting bodies around the host with realistic (where known) sizes and orbital geometry. Star-centric UX: no search-by-planet-name, no planet labels in the open field — exoplanets are a nice \"you see them when you visit\" reward.\n\nThree parts: (1) build pipeline ingests NASA Exoplanet Archive data and emits a flag in `catalog.bin` + a lazy-load `exoplanets.json`, (2) client loader caches the lazy-load file on first focus of any host star, (3) rendering layer draws planets + orbital rings around the focused host. This phase fully nails Parts 1 and 2; Part 3 is described enough to drive the implementation but specific visual choices (orbit ring style, planet disc shading, null-radius indicator) are deferred to whoever picks this up.\n\n### Part 1: Data ingest in `scripts/build-catalog.ts`\n\n**Source.** NASA Exoplanet Archive `pscomppars` table (planetary system composite parameters — canonical \"best values per planet\" view). One-off download, committed via Git LFS:\n\n```bash\ncurl -o data/exoplanets_nasa.csv \\\n  \"https://exoplanetarchive.ipac.caltech.edu/TAP/sync?query=SELECT+pl_name,hostname,hip_name,hd_name,ra,dec,sy_dist,pl_rade,pl_radj,pl_orbsmax,pl_orbper,pl_eccen,pl_bmasse,sy_pnum,st_teff,st_rad,st_lum\u0026FORMAT=csv\u0026TABLE=pscomppars\"\ngit lfs track \"data/exoplanets_nasa.csv\"\ngit add .gitattributes data/exoplanets_nasa.csv\n```\n\n**Parser.** New `parseExoplanetData(csvPath)` alongside the GCVS parsers. Returns `Map\u003chostname, ExoplanetSystem\u003e` with planets already sorted by `semi_major_axis_au` ascending (nulls last).\n\n```typescript\ninterface ExoplanetSystem {\n  hostname: string;\n  hip: number | null;       // parsed from \"HIP 12345\"\n  hd:  number | null;       // parsed from \"HD 12345\"\n  gl:  string | null;       // Gliese designation if hostname is one\n  ra: number | null; dec: number | null; dist_pc: number | null;\n  star_teff: number | null; star_rad: number | null; star_lum: number | null;\n  n_planets: number | null;\n  planets: ExoplanetRecord[];\n}\n\ninterface ExoplanetRecord {\n  name: string;\n  radius_earth: number | null;       // null = unknown — render with default + indicator\n  radius_jupiter: number | null;\n  mass_earth: number | null;\n  semi_major_axis_au: number | null;\n  period_days: number | null;\n  eccentricity: number | null;       // null in NASA data when poorly constrained;\n                                     // renderer should default to 0 (circular)\n}\n```\n\nEmpty CSV cells → `null`. Don't filter null-radius planets — rendering layer handles that case.\n\n**Cross-match — HIP + HD + Gliese.** After the existing post-sort `hipToIndex` map is built, build the symmetric reverse maps:\n\n```typescript\nconst exoByHip = new Map\u003cnumber, ExoplanetSystem\u003e();\nconst exoByHd  = new Map\u003cnumber, ExoplanetSystem\u003e();\nconst exoByGl  = new Map\u003cstring, ExoplanetSystem\u003e();   // normalised \"Gl 551\" form\n```\n\nThen walk the sorted catalog, matching HIP first, HD second, Gliese third (the catalog has a `gl` field; AT-HYG carries it). Result: `exoplanetsByCatalogIdx: Map\u003crecordIdx, ExoplanetSystem\u003e`. Adding Gliese over the original HIP+HD-only plan catches a few high-profile M-dwarf hosts (Proxima = `Gl 551`, Lacaille 9352 = `Gl 887`, etc.).\n\n**Expected match rate is ~30–50%** of NASA's ~5,800 systems. Bright-star hosts (Tau Ceti, 51 Peg, Upsilon And, Proxima, Trappist-1) match cleanly via HIP/HD/Gliese; faint Kepler/TESS hosts (Kepler-N, TOI-N, KOI-N) typically don't carry any classical designation, so they're invisible to this cross-match. the `reduced_m12` catalog upgrade does **not** lift this — the limitation is the designation space, not catalog magnitude depth. Document in the build log and mention in `CLAUDE.md` after landing so future Claude sessions don't chase this as a bug.\n\n**Catalog v3 → v4.**\n- Bump the `version` uint32 in the header from `3` to `4`.\n- Set flag bit 3 (`0x08`, \"hasPlanet\") on host stars during the record-writing loop. Existing bit 4 (`0x10`, isBinaryPrimary) unchanged.\n- 40-byte stride is preserved — no struct field changes.\n- Reader hard-requires v4 (no transitional v3 acceptance). General principle: regenerate from source whenever upstream data changes; binaries / indices are cheap.\n\n**Emit `public/exoplanets.json`.** After all catalog records are written, emit a record-index-keyed JSON of the per-system data:\n\n```typescript\nconst out: Record\u003cstring, {\n  hostname: string;\n  dist_pc: number | null;\n  star_teff: number | null;\n  star_rad: number | null;\n  star_lum: number | null;\n  n_planets: number | null;\n  planets: ExoplanetRecord[];\n}\u003e = {};\nfor (const [idx, system] of exoplanetsByCatalogIdx) {\n  out[idx] = { hostname: system.hostname, dist_pc: system.dist_pc,\n               star_teff: system.star_teff, star_rad: system.star_rad,\n               star_lum: system.star_lum, n_planets: system.n_planets,\n               planets: system.planets };\n}\nfs.writeFileSync('public/exoplanets.json', JSON.stringify(out));\n```\n\nAdd `public/exoplanets.json` to `.gitignore` alongside the other generated files. Add `data/exoplanets_nasa.csv` to the `isUpToDate` inputs so a NASA refresh triggers a rebuild.\n\nExpected size: ~5,800 planets × ~200 bytes ≈ 1.2 MB raw, ~300–500 KB gzipped. Negligible — Vite's static-asset gzip on Cloudflare handles the wire compression. Eager-load on first need is fine.\n\n### Part 2: Client loader in `src/client/exoplanet-loader.ts`\n\nNew module. Single in-memory cache, single inflight-promise dedupe:\n\n```typescript\ninterface ExoplanetData { /* matches the JSON shape above */ }\n\nlet cache: Record\u003cstring, ExoplanetData\u003e | null = null;\nlet fetchPromise: Promise\u003cvoid\u003e | null = null;\n\nasync function ensureLoaded(): Promise\u003cvoid\u003e {\n  if (cache !== null) return;\n  if (fetchPromise) return fetchPromise;\n  fetchPromise = fetch('/exoplanets.json')\n    .then(r =\u003e r.json())\n    .then(data =\u003e { cache = data; });\n  return fetchPromise;\n}\n\nexport async function getExoplanetData(catalogIdx: number)\n  : Promise\u003cExoplanetData | null\u003e {\n  await ensureLoaded();\n  return cache?.[catalogIdx] ?? null;\n}\n```\n\nAlso expose `hasPlanet` on the per-record data exposed by `catalog-loader.ts` (parse from flag bit 3).\n\n### Part 3: Trigger + render wiring in `stellata.ts`\n\n**Trigger.** Lazy-load fires on `setFocus(idx)` when the focused star has `hasPlanet === true`. One fetch ever (cache is permanent for the session); subsequent focuses on planet-bearing stars are cache hits. Stars without planets never trigger the fetch.\n\n```typescript\nif (focusedStar.hasPlanet) {\n  getExoplanetData(focusedStarIdx).then(data =\u003e {\n    if (data) this.events.emit('exoplanetData', { idx: focusedStarIdx, data });\n  });\n}\n```\n\nAdd `onExoplanetData(handler)` to the existing event bus alongside `onFocusChange` etc.\n\n**Render.** Out-of-scope for this phase doc to nail down — defer the visual choices to whoever implements. Constraints to honour:\n- Orbital rings drawn around the focused host using `semi_major_axis_au` for radius. Default `eccentricity = 0` (circle) when null. Rings should render in the local frame so floating-origin works.\n- Planet body rendered with size driven by `radius_earth` (or `radius_jupiter` when present), scaled relative to the host star's `physicalRadius` so the visual ratio is roughly correct.\n- **Null-radius planets are still rendered** — at a default mid-range size, with a visual indicator (dashed ring? `?` overlay? reduced opacity? — TBD). Do not filter out null-radius planets during preprocessing or load.\n- Planets are not searchable and not labelled in the open field. Visible only when their host is focused. A side-panel or on-hover detail readout for an individual planet's stats is fair game.\n\n### Files\n\n- New: `data/exoplanets_nasa.csv` (LFS), `src/client/exoplanet-loader.ts`, whatever rendering modules Part 3 ends up needing.\n- Touch: `scripts/build-catalog.ts` (parser + cross-match + flag + JSON emit + `isUpToDate`), `src/client/catalog-loader.ts` (hard-require v4, expose `hasPlanet`), `stellata.ts` (focus trigger + render integration), `main.ts` (no parallel load needed — loader is lazy), `.gitignore` (`/public/exoplanets.json`), `.gitattributes` (LFS pattern).\n- After landing, update `CLAUDE.md`:\n  - Binary catalog format section — document flag bit 3 and v4 bump.\n  - Add a new \"Exoplanets\" section explaining cross-match strategy (HIP/HD/Gliese), expected match rate (~30–50%), and the Kepler/TESS-host gap that the cross-match deliberately doesn't chase.\n\n### Watch out for\n\n- `pscomppars` schema can drift if NASA changes column names. Build script should fail loud (not silent-null) if any expected column is missing from the CSV header.\n- Multiple stars sharing the same HD number (composite designations, rare but real). First-write-wins on the reverse maps is fine but worth a comment so the behaviour is explicit.\n- `sy_dist` (NASA) and the catalog's parallax-derived distance can differ slightly. For display (\"distance to system: X pc\") use `sy_dist`; for any 3D-positioning math use the catalog's position (more authoritative inside the local frame and consistent with everything else).\n- Rendering must respect the floating-origin frame — orbital rings drawn at the host's local-frame position, not absolute. Same contract as constellation lines / focus ring.\n- v3 → v4 hard requirement: anyone with a stale `catalog.bin` will get a load error. The build pipeline regenerates automatically via `isUpToDate`, but mention in the changelog/commit so others know to `rm public/catalog.bin` if they hit a magic-byte mismatch.\n\n### Estimate\n\n1 day for Parts 1 + 2 (data plumbing is mechanical, follows the GCVS pattern). Part 3 (rendering) is a separate sitting — probably half a day to a full day depending on how fancy the planet/ring visuals get and how the null-radius indicator lands.\n\n### Deliberately excluded\n\n- Search-by-planet-name. Star-centric UX only — planets are a visit-and-discover reward, not a search target. Avoids confusing planet names against star names in the dropdown.\n- Planet labels visible from the open stellata. Visible only when host is focused.\n- Cross-match against TIC / KIC / Gaia DR3 IDs to capture Kepler/TESS hosts. The catalog doesn't carry those designations, and adding a parallel ID space is beyond this phase's scope. Re-evaluate if a future catalog upgrade brings them in.\n- Apsidal motion / orbital phase animation. Static rings + static planet positions on the rings (or at periastron, or distributed by mean anomaly — a render-time choice).\n","notes":"Now blocked by stellata-a7d (physical-scale rewrite). Part 3 (rendering) requires the host star to render at correct angular size so planet-relative-to-host visual ratios work. Until the rewrite lands, host stars at close range are sized by a log-mapped pixel formula that doesn't preserve R_planet/R_star ratios — drawing planets against that base would produce inconsistent scale.","status":"deferred","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:34Z","created_by":"Alex Marshall","updated_at":"2026-05-08T11:12:28Z","labels":["phase-11"],"dependencies":[{"issue_id":"stellata-bk5","depends_on_id":"stellata-3re.6","type":"relates-to","created_at":"2026-05-07T09:55:48Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-8r1","title":"Wander mode (autonomous gravity-field camera)","description":"**Status:** Standalone exploration feature. No dependencies on other phases — can ship any time the catalog format is stable.\n\n**Goal:** the camera behaves as a particle moving through a precomputed gravitational field derived from the star catalog. Hit `G`, the camera drifts autonomously through the local stellar neighbourhood, following gradients of stellar mass density. Click / `Esc` / `G` again to exit, leaving the camera wherever it ended up. Naturalistic exploration without steering — dense clusters pull the camera in, ridges deflect it.\n\nTwo parts: (1) an offline precompute script that builds a `gravity.bin` asset, (2) runtime integration in `stellata.ts` following the warp pattern.\n\n### Part 1: `scripts/build-gravity.ts`\n\n**Bounding volume.** Read star positions from `catalog.bin` (inline a small reader, same approach `verify-catalog.ts` uses — no need to factor a shared module). Compute max distance from Sol across all stars, call this `R`. Grid is a cube of side `2R` centred at the origin, voxel side ≈ `2R / 128`. Voxel centres outside the bounding sphere of radius `R` store a zero gradient — no extrapolation past the catalog.\n\n**Grid.** 128³ voxels → ~25 MB output (3 × float32 per voxel). Coarse relative to inter-star spacing near Sol (~1 pc), but that's desirable — wander should feel like ridges and basins of density, not orbital snap-ins on individual stars.\n\n**Mass proxy.**\n```\nluminosity = 10 ^ ((4.74 − absmag) / 2.5)\nmass       = max(0, luminosity ^ MASS_EXPONENT)\n```\nDefault `MASS_EXPONENT = 0.5`. The physical mass-luminosity relation gives ~0.286, but 0.5 keeps dim stars from being completely swamped — a tuning lever, not a physics value. Document the physical reference in the comment.\n\n**Gradient computation.**\n```\nfor each voxel center p:\n  for each star i:\n    r_vec   = star_position − p\n    dist_sq = dot(r_vec, r_vec) + SOFTENING²\n    g      += mass_i × r_vec / (dist_sq × sqrt(dist_sq))\n```\nVectors are NOT normalised — magnitude encodes field strength.\n\n**Performance.** 128³ × 313k ≈ 660 G ops. Run in Node `worker_threads`, chunked one Z-slice per task, target \u003c5 min on a modern laptop. Optional optimisation: per-voxel cull stars beyond e.g. 500 pc (at softening 2 pc, a star at 500 pc contributes ~1/250000 the weight of one at 1 pc). Worth measuring before adding — could shave an order of magnitude.\n\n**Output `public/gravity.bin`.**\n```\nHeader (32 bytes):\n  0–3    ASCII \"GRV1\"\n  4–7    uint32 grid dimension N (128)\n  8–11   float32 grid half-extent in parsecs (= R)\n  12–15  float32 softening value used (informational)\n  16–19  float32 mass exponent used (informational)\n  20–31  reserved (zeros)\n\nBody:\n  N³ records × 12 bytes — float32 x, y, z gradient\n  Index order: x + y*N + z*N*N (x varies fastest)\n```\n\nAdd to `.gitignore` alongside `catalog.bin`. Add `build:gravity` to `package.json`, chained after `build:catalog` in both `dev` and `build` scripts (depends on `catalog.bin` existing). Use the same `isUpToDate` pattern as `build-catalog.ts`, gated on `catalog.bin` mtime + the script's own mtime + the build-baked constants.\n\n### Part 2: `src/client/gravity-loader.ts`\n\nFollowing `catalog-loader.ts` structure:\n- Fetch `gravity.bin`, validate magic + version.\n- Expose `gridN`, `halfExtent`, `softening`, `massExponent`, `gradients: Float32Array` (length `N³ × 3`).\n- Export `sampleGradient(absolutePos: THREE.Vector3): THREE.Vector3` with trilinear interpolation. Return zero for positions outside the bounding sphere.\n\nLoad in parallel with `catalog.bin` and `search-index.json` in `main.ts`.\n\n### Part 3: Wander state in `stellata.ts`\n\n**State machine.** Add `'wander'` mode alongside warp. Same disable pattern: `controls.enabled = false`, `body.classList.toggle('wandering', true)`, `pointerup` short-circuited, URL writer skips camera-hash updates while active.\n\nEntry: keyboard shortcut `G` (verified free; `W` is warp; `Esc` / `Space` skip warp). Exit: any `pointerdown`, `Esc`, or `G` again.\n\n**Focus interaction (option (a)).** Activating wander **unfocuses** any current focal star (calls `setFocus(null)`), which also recentres the floating origin to Sol. The wander particle then operates in a clean absolute frame for the duration of the mode.\n\n**Coordinate-frame correctness — CRITICAL.** The gradient field is in absolute Sol-centred coordinates, matching `catalog.positions`. After unfocus on entry, `worldOffset = (0, 0, 0)` and `camera.position` is in absolute space, so initial sampling is direct. But once the wander loop calls `recenterOrigin(...)` for precision (see below), `worldOffset` becomes non-zero and `camera.position` shifts toward zero. From that point all gradient samples must be at `camera.position + worldOffset`, never at `camera.position` alone. Same for the lookahead and any read-back. This is the single most common way for the field to silently drift — call it out in code comments around the sampling.\n\n**Particle state.**\n```typescript\nprivate _wanderActive = false\nprivate _wanderVelocity = new THREE.Vector3()\nprivate _wanderDistanceSinceRecenter = 0\n```\n\n**Per-frame update `updateWander(dt)`.**\n1. `samplePos = camera.position + worldOffset`. If `velocity.lengthSq() \u003e ε²`, `samplePos += normalize(velocity) × WANDER_LOOKAHEAD_PC`.\n2. `accel = sampleGradient(samplePos) × WANDER_GRAVITY_SCALE`.\n3. `velocity += accel × dt × WANDER_SIM_TIME_SCALE`.\n4. `velocity *= pow(WANDER_VELOCITY_DAMPING, dt × 60)` — anchors the \"0.99 per frame\" lever to 60 fps so feel is frame-rate independent.\n5. Clamp `|velocity| ≤ WANDER_MAX_SPEED_PC_PER_S`.\n6. `step = velocity × dt × WANDER_SIM_TIME_SCALE`. `camera.position += step`. `controls.target += step` (keep target moving with camera so orbit orientation is preserved on exit).\n7. `_wanderDistanceSinceRecenter += step.length()`. If it crosses ~1 pc, call `recenterOrigin(camera.position + worldOffset)` and reset the counter. Keeps float32 precision good when wandering far from Sol.\n\n**Initial kick (entry).** Field-scaled with a floor — guarantees motion even in a void, and scales naturally with `GRAVITY_SCALE` without a separate retune.\n```\ng         = sampleGradient(camera.position + worldOffset)\nv0        = max(WANDER_KICK_FLOOR_PC_PER_S,\n                WANDER_KICK_TIME_S × |g| × WANDER_GRAVITY_SCALE)\ndirection = normalize(controls.target − camera.position)\n            (fallback: +Z if degenerate, never normalise zero)\n_wanderVelocity = direction × v0\n```\n\n**Tuning levers.** Define as named constants at the top of the wander section. Comments must call out runtime vs build-baked:\n- `WANDER_SOFTENING_PC = 2.0` — **build-baked**; must match `build-gravity.ts`. Changing requires regenerating `gravity.bin`.\n- `WANDER_MASS_EXPONENT = 0.5` — **build-baked**; same constraint.\n- `WANDER_GRAVITY_SCALE = 0.1` — runtime.\n- `WANDER_VELOCITY_DAMPING = 0.99` — runtime; interpreted as per-frame at 60 fps, applied as `pow(damping, dt × 60)`.\n- `WANDER_MAX_SPEED_PC_PER_S = 20.0` — runtime.\n- `WANDER_SIM_TIME_SCALE = 5.0` — runtime.\n- `WANDER_LOOKAHEAD_PC = 2.0` — runtime.\n- `WANDER_KICK_TIME_S = 2.0` — runtime; \"free-fall seconds\" worth of initial kick velocity.\n- `WANDER_KICK_FLOOR_PC_PER_S = 1.0` — runtime; absolute floor so voids still produce motion.\n\n### Files\n\n- New: `scripts/build-gravity.ts`, `src/client/gravity-loader.ts`.\n- Touch: `stellata.ts` (mode + state + `updateWander` + key handler), `main.ts` (parallel load, pass loader to `Stellata`), `package.json` (script + chain), `.gitignore` (`/public/gravity.bin`), `styles.css` (optional `body.wandering` style — e.g. cursor change or subtle vignette to signal \"you are not driving\").\n- After landing, update `CLAUDE.md`: extend the floating-origin section with the \"sample in absolute frame\" contract and add a new \"Wander mode\" section.\n\n### Watch out for\n\n- Floating-origin sampling — covered above; single biggest landmine.\n- Trilinear interpolation must clamp grid indices: a fractional voxel near the cube edge can land at `N − 1 + ε`; `floor` gives `N − 1` but `floor + 1` would read past the end.\n- Unfocus-on-entry is intentional (option (a)). Don't be tempted to \"preserve focus for the panel\" — focus drives `worldOffset` and decoupling them is messy.\n- Kick direction normalises `target − position`; defensively guard the degenerate case (target == position) with a fixed fallback vector so we never normalise zero.\n- URL-sync `onFrame` skip-while-warping needs to extend to wander — we don't want intermediate poses serialised.\n- 25 MB `gravity.bin` is a meaningful download; serve gzipped (Vite default for `public/` handles this) and load eagerly initially. Revisit lazy-load only if startup latency complaints surface.\n\n### Estimate\n\n1–1.5 days. Most of it is build-script worker setup plus tuning the nine levers to a feel that's neither stuck-on-Sol nor slingshot-out-of-the-catalog. The `stellata.ts` integration is small and follows the warp pattern.\n\n### Deliberately excluded\n\n- UI controls for the tuning levers — constants in source only.\n- A wander button (keyboard shortcut only).\n- Apparent-magnitude or visibility input to steering — field is mass × position only.\n- Any changes to dust / nebula rendering.\n","status":"deferred","priority":3,"issue_type":"epic","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:31Z","created_by":"Alex Marshall","updated_at":"2026-05-10T08:03:50Z","labels":["phase-10"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-cns","title":"Deep-tier star catalog expansion (50 pc – kpc fill)","description":"# Goal\n\nPopulate the resolved-discrete star layer at intermediate travel distances (50 pc – ~kpc from Sol), where `classic_ids` runs out of entries because faint distant stars rarely have classical designations.\n\nComplements the Milky Way volumetric layer, which fills the diffuse glow at distance — this work fills the point sources at intermediate range. Together they make travel away from Sol feel populated at every scale.\n\n# Open design questions — research children up front\n\nTwo orthogonal axes need decisions before implementation lands:\n\n- **Depth + perf budget** — stellata-cns.1 evaluates AT-HYG `reduced_m11` (~800k) vs `reduced_m12` (~2M) on first-load, render-budget, and scientific completeness. Whichever wins becomes the model's magnitude floor (see cns.1's NOTES — the decision applies to ALL object categories, not just stars).\n- **Row-driver** — stellata-cns.2 evaluates AT-HYG monolithic vs Gaia-native monolithic vs hybrid (AT-HYG classic_ids as labeled bright tier + Gaia-native for deep fill). Architecturally interesting because stellata-dch's refinement stack is source-ID-keyed end-to-end and applies uniformly regardless of row-driver upstream.\n\n# Architectural prerequisite — stellata-dch\n\nImplementation of this epic is contingent on stellata-dch shipping. The dch refinement layers (Bailer-Jones distances, Gaia 5p / NSS / HIP2 astrometry, Apsis Teff / logg / [M/H] / A0, LMC kinematic override) all attach via gaia_source_id. Without dch in place, ANY deep-tier expansion would still ingest catastrophically-inverted 1/π distances for the low-S/N parallax population that dominates faint stars — exactly the regime where the upgrade is supposed to add value. Linked via `bd dep add stellata-cns stellata-dch --type=related`.\n\n# What changes (row-driver dependent — depends on cns.2 outcome)\n\n- **Data source**: TBD per cns.2 — either `data/athyg_33_reduced_m12.csv` (or _m11), or a Gaia-native row driver, or a hybrid carrying both. AT-HYG source repository: https://codeberg.org/astronexus/athyg.\n- `scripts/build-catalog.ts` reads the chosen row driver(s). Same source-ID-keyed dch refinement stack applies regardless. Same v6/v7 binary format.\n- Output `public/catalog.bin` grows substantially (exact figure depends on cns.1 outcome + row-driver choice).\n- `search-index.json` content unchanged at the deep tier — only stars with classical identifiers go in the search index. The added stars (~hundreds of thousands or low millions) contribute to rendering only.\n- GCVS variability cross-match picks up more variables (more HIP/HD entries present in the bigger catalog).\n- Geometric binary inference runs over more stars; the `BINARY_MAX_SEP_PC = 0.005 pc` threshold keeps the count visually relevant.\n\n# Watch out for\n\n- Initial catalog download grows from ~3 MB gzipped (classic_ids) to ~8 MB (m11) or ~20 MB (m12). Significant for first-load on slow connections. Worth measuring whether progressive load (render available chunks while the rest streams) is needed.\n- Instance count at the chosen depth needs profiling on modest hardware (integrated GPUs, mobile). The two-pass split handles overdraw efficiently, but instance-attribute memory grows linearly. If there's a perf cliff, fall back to a shallower cut.\n- `classic_ids` ⊂ `reduced_m12` in practice (stars with classical IDs are almost all brighter than mag 12 from Earth), but verify iconic targets (Proxima, Barnard's Star, Sol-itself) survive the switch regardless of row-driver choice.\n- Constellation index assignment per star uses the AT-HYG `con` column unchanged when AT-HYG is the row driver; a Gaia-native row driver needs an explicit RA/Dec → constellation lookup step.\n\n# Connects\n\n- Research children: stellata-cns.1 (depth + perf), stellata-cns.2 (row-driver).\n- Architectural prerequisite: stellata-dch (source-ID-keyed refinement stack).\n- Memory: [[stellata-data-fidelity-principle]] (bar for catalogue choices), [[stellata-prioritization-framework]] (deep-tier sits at queue position 7).","notes":"2026-05-02 — design principle decided: the apparent-magnitude cutoff of the chosen catalog subset (m11 or m12) becomes the **model's magnitude floor** — the dimmest threshold for *all* objects in the model, not just stars. Anything dimmer than this floor is excluded from the model regardless of category (variable stars, nebulae, projected distant DSOs, etc.). This makes the magnitude UI control coherent across all object types and gives a clean contract: 'show me everything brighter than mag X' applies uniformly. The decision in stellata-cns.1 (m11 vs m12) therefore sets the model's floor, not just the star catalog cutoff.","status":"open","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:28Z","created_by":"Alex Marshall","updated_at":"2026-05-18T10:54:44Z","labels":["catalog","phase-7"],"dependencies":[{"issue_id":"stellata-cns","depends_on_id":"stellata-dch","type":"related","created_at":"2026-05-18T11:54:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-n2x","title":"Radcliffe Wave spine","description":"**Goal:** glowing polyline showing the ~3 kpc oscillating gas wave that passes near the Sun. Famous nearby structure, narratively striking, very small data.\n\n**Data sources:**\n- **Alves et al. 2020** (Nature, original discovery).\n- **Konietzka et al. 2024** — oscillation phase data.\n- Catherine Zucker's GitHub / interactive gallery again is the practical entry point for the spine point list (~100 points).\n\n**Implementation sketch:**\n- Read ~100 (x, y, z) spine points from source. Tiny — kilobytes.\n- Render as a `Line2` (Three.js fat line helper) with emissive shader, optionally colour-coded by oscillation phase from Konietzka 2024.\n- Label \"Radcliffe Wave\" at midpoint of the spine.\n- UI toggle.\n- Bonus: when toggled on, highlight catalogued molecular clouds that are part of the wave (cross-ref against the molecular-cloud overlay's cloud catalog).\n\n**Watch out for:**\n- Coordinate frame conversion (Galactic → ICRS).\n- The wave extends across ~3 kpc, well past the 1.25 kpc Edenhofer volume; the spine itself is fine to render outside the dust grid, it just won't have associated dust at its extreme ends.\n\n**Estimate:** 2–4 hours.\n","status":"deferred","priority":3,"issue_type":"feature","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:24Z","created_by":"Alex Marshall","updated_at":"2026-05-08T11:12:29Z","labels":["ism","phase-4b"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-zq3","title":"Revisit dust visualisation layer","description":"**Status:** infrastructure complete, default off, needs render tuning.\n\n**The fundamental tension:** real interstellar dust is *dark*. It absorbs starlight; it doesn't emit. The current particle implementation renders dust as additive warm-brown glows, which is visually pretty but inverts physical reality. A user looking at the additive fog sees \"glowing dust everywhere\" when in reality those regions are *darker* than empty space.\n\nThe per-star extinction layer (shipped) is the physically honest representation — stars behind dust dim and redden. That's what an observer at Sol would actually see.\n\n**Open questions to settle before re-enabling particles:**\n\n1. **What's the role of the visualisation layer?** Two possible framings:\n   - **(a)** \"Show me where the dust is\" — a non-physical overlay, clearly marked as visualisation, similar to how astronomical maps overlay contour lines. Additive glow is fine here, just label it as such.\n   - **(b)** \"Make the dust visible against starlight\" — physically motivated darkening using `MultiplyBlending` so dust patches occlude stars rather than emitting light. This is closer to truth but only visible where there are bright background stars.\n2. **Particle splat tuning** — current values (`MIN_PX = 30`, `MAX_PX = 80`, brightness range 0.04–0.15) are a starting point; needs side-by-side testing to find the splat-size / brightness combination where dense regions feel like fog and diffuse regions stay below the perceptual floor. The goal is \"smooth from overlap, never individual specks.\"\n3. **Importance sampling tradeoff** — current density-weighted sampling over-represents dense cores; sqrt or log weighting gives more diffuse-region representation but flattens the cores. A two-tier approach (80% density-weighted + 20% uniform-where-d\u003ethreshold) might be the right balance.\n4. **UX entry point** — currently console-only via `stellata.setParticleStrength(x)`. Promoting to a user-facing toggle needs UI design (probably a checkbox under \"Display Settings\" with a short label like \"Show interstellar dust\").\n\n**Files to touch:**\n- `src/client/shaders/dust-particle.vert.glsl` — splat sizing, brightness\n- `src/client/shaders/dust-particle.frag.glsl` — falloff curve, blend mode\n- `scripts/build-dust.py` `sample_particles()` — importance-sampling\n- `src/client/stellata.ts` `attachDustParticles()` — blend mode if changing\n\n**What NOT to change:** the per-star extinction layer is shipped and working. Don't tune it during particle work; it's the physical baseline.\n\n**Reference for the abandoned fullscreen-fog approach:** see `memory/feedback_raymarch_for_nebulae.md` — fullscreen raymarch produced unfixable banding/jitter at far zoom. Don't go back to it for the all-sky volume; it's the right tool only for small per-nebula AABBs.\n","status":"deferred","priority":3,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T15:45:12Z","created_by":"Alex Marshall","updated_at":"2026-05-08T11:13:17Z","labels":["ism","phase-2","shelved"],"dependencies":[{"issue_id":"stellata-zq3","depends_on_id":"stellata-io3","type":"parent-child","created_at":"2026-05-02T18:39:19Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.202.10","title":"requirements-refresh.txt: pin Python 3.10+ requirement explicitly","description":"Scripts use `int | None` union syntax (3.10+) and rely on `from __future__ import annotations` for module-level type evaluation. If someone tries to run on 3.9 (still common in some venvs), `Bin.__init__(v_min: float | None, ...)` evaluation at module-import time will fail.\n\n`scripts/requirements-refresh.txt` doesn't say `python_requires`.\n\n## Acceptance\n\nEither (a) add a `# Python 3.10+` comment header to `requirements-refresh.txt` AND a runtime check in `refresh_lib.py` that raises a clear `SystemExit` if `sys.version_info \u003c (3, 10)`, OR (b) add the constraint to the venv-setup docs in CLAUDE.md / each script's docstring.\n\nP4 because it's documentation-shaped — the scripts already fail at import on 3.9, just with a confusing error message instead of an obvious one.","status":"open","priority":4,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-19T10:07:28Z","created_by":"Alex Marshall","updated_at":"2026-05-19T10:07:28Z","labels":["pr-88"],"dependencies":[{"issue_id":"stellata-9mm.202.10","depends_on_id":"stellata-9mm.202","type":"parent-child","created_at":"2026-05-19T11:07:28Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.162","title":"mag-bias 0.05 px/mag is a magic number; consider naming PICK_MAG_BIAS_PX_PER_MAG","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\n`pickScore` hard-codes the mag-bias coefficient as the literal `0.05` inside the formula `pxDist + appMag * 0.05`. The PR's design hinges on this number being 'small enough that any visible pxDist gap dominates' (third regression test) but 'large enough that two coincident catalog rows tiebreak by brightness' (Alula). It has a real semantic role: 0.05 px/mag means a 1-mag-fainter star is treated as 0.05 px farther from the cursor; a 4-mag-fainter star as 0.2 px farther. If anyone later tweaks the value, they'll be tweaking a literal in two places (`pickScore` body + the comment two lines above). Naming it as a top-of-file const `PICK_MAG_BIAS_PX_PER_MAG = 0.05` would document the unit, surface the tunability, and let the test file reference the same const for forward-compat.\n\n**Location**\nsrc/client/star-geometry.ts:91 (`pickScore` body)\nsrc/client/star-geometry.ts:81-89 (the comment that calls out '0.05 px / mag')\n\n**Suggested approach**\nHoist the literal to a named const (or pass it as an optional third arg with the default). Tests already use bare numbers (`pickScore(0, 4.59)`) so the test file doesn't need to change unless we want it to assert the const value itself.\n\n**Why**\nBookkeeping only — the value isn't expected to change. Filing in case Alex prefers the named-constant pattern other star-geometry.ts callers follow (e.g. `R_SUN_PC`, `DCAM_LOG_FLOOR_PC`). Skip if the inline literal is preferred for sub-formula readability; flag for explicit decision.\n\n**Smoke step**\n`npm run typecheck \u0026\u0026 npm test`. No behaviour change — pickScore returns the same values for the same inputs.\n","status":"closed","priority":4,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:16Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:59:10Z","started_at":"2026-05-10T20:09:18Z","closed_at":"2026-05-10T21:59:10Z","close_reason":"Shipped in PR #53 (merge commit 63da7a6, v1.6.2)","labels":["pr-53"],"dependencies":[{"issue_id":"stellata-9mm.162","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:12:15Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.160","title":"peakPhaseFactor name overpromises — only computes φ(0), not the polynomial maximum","description":"**Confidence: Medium**\n\n**Finding**\n`peakPhaseFactor` is named as if it returns the maximum of φ(α) over the polynomial's validity range, but it only evaluates φ(0) = `10^(−c0/2.5)`. For Saturn this happens to be the polynomial's maximum (c0=−0.55, c1=+0.026 — α growth darkens the body monotonically), so the cull-distance proxy is correct. But for Venus the polynomial peaks slightly above 1 near α≈3° (c1=−1.044e-3, dV crosses 0 just past α=2.83° before turning positive), so `peakPhaseFactor(VENUS_PHASE)` returns 1.0 while the true peak is ~1.001. Numerically negligible, but the name promises something stronger than the function delivers.\n\n**Location**\n`src/client/phase-function.ts` lines 96–104 (the `peakPhaseFactor` function and its docblock).\n\n**Suggested approach**\nTwo options worth Alex's call. Either (a) rename to `alphaZeroPhaseFactor` / `zeroPhasePhaseFactor` and update the call site comment in `planet-body-field.ts` accordingly, surfacing that the cull proxy uses α=0 not the true peak; or (b) keep `peakPhaseFactor` and audit each planet's polynomial for the true max within `alphaMaxDeg`, returning that (this is overkill given the Venus discrepancy is sub-millimagnitude). Option (a) is the cheaper truth.\n\n**Why**\nNaming is documentation. Future contributors reading `peakPhaseFactor` in a cull-distance computation will assume it covers the polynomial's full range; if exoplanet phase curves with a true peak above 1 ever land (`bk5`), a mis-named function buries that subtlety.\n\n**Smoke step**\n`npm test` — existing peakPhaseFactor tests pin the α=0 semantics, so the rename is a textual diff with no behavioural change.\n","status":"open","priority":4,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:11Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:50Z","labels":["pr-56"],"dependencies":[{"issue_id":"stellata-9mm.160","depends_on_id":"stellata-9mm.189","type":"parent-child","created_at":"2026-05-15T12:53:45Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.159","title":"swapObserveAnchor's idempotency split makes the function name misleading","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nPost-PR `swapObserveAnchor` does two distinct jobs depending on call site: when called from finishWarp AFTER a phase-3 recentre fired, the `if (this.focusedStar !== newIdx)` guard is true and only the observe-tail (`uHideFocusIdx` + `camera.position.set(0,0,0)` + `fireStateChange`) runs; when called from finishWarp on a skipped-or-pre-phase-3-skip warp, the body fully recentres + sets focus + runs the tail. The function name reads as 'do the swap' but its body is now 'finish the swap (the geometric half may or may not already be done)'. Idempotency is correct, the prose comment block flags it, but readers still have to mentally simulate two paths to know what runs.\n\n**Location**\nsrc/client/stellata.ts:2484-2494\n\n**Suggested approach**\nEither (a) split into two methods — `finishObserveAnchor(newIdx)` does the tail unconditionally, called from finishWarp; the phase-3 path calls `recenterFocusToStar` directly (which it already does); a skipped-pre-phase-3 path calls both — or (b) leave the idempotent shape but rename to something like `completeObserveSwap` to advertise that it's the finalizer. Tradeoff: (a) makes call sites explicit but adds a top-level method; (b) is a one-line rename. Worth Alex's call.\n\n**Why**\nIdempotent helpers are fine in isolation; this one is the one place focus-state mutates outside the explicit setFocus / setFocusedCloud APIs, so naming clarity costs more than usual.\n\n**Smoke step**\n1. Skip warp BEFORE phase 3 (during reorient or fly): observe→observe arrival lands cleanly via the full path, as the PR's test plan already exercises.\n2. Skip warp DURING phase 3: lands cleanly via the tail-only path.","status":"open","priority":4,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:12:10Z","created_by":"Alex Marshall","updated_at":"2026-05-10T18:24:51Z","labels":["pr-50"],"dependencies":[{"issue_id":"stellata-9mm.159","depends_on_id":"stellata-9mm.187","type":"parent-child","created_at":"2026-05-15T12:53:24Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.155","title":"pickScore parameter named 'pxDist_px' (Hungarian) inconsistent with codebase style","description":"**Confidence: Medium**\n\n**Finding**\n`pickScore`'s signature is `pickScore(pxDist_px: number, appMag: number)` — the first param has a unit suffix (`_px`) on top of an already-pixel-unit-implied name (`pxDist`). The rest of `star-geometry.ts` uses unit suffixes for *physics* quantities where the unit is ambiguous (`R_pc`, `dCam_pc`, `fovYRad`, `fovMinorRad`, `maxPhysSize` in px), but `pxDist` already encodes the pixel unit in the prefix. The double-marking is the only Hungarian suffix on a screen-space quantity in this file. The callsite uses `pxDist` (no suffix), so the dissonance shows up immediately at the call.\n\n**Location**\nsrc/client/star-geometry.ts:90 (`pickScore` signature)\nsrc/client/stellata.ts:2787, 2799 (`pxDist` callsite)\n\n**Suggested approach**\nRename to `pickScore(pxDist: number, appMag: number)`. Single-token, matches the variable name at the only callsite, and matches how the rest of the file treats unambiguous-unit quantities (e.g. `amplitudeMag`, `periodDays`, `baseSize`, `maxPhysSize` — none have suffixes despite being unit-bearing).\n\n**Why**\nTiny-DRY: the helper signature should read like the call, not introduce gratuitous notation. Lowest-priority finding in this PR review.\n\n**Smoke step**\n`npm run typecheck \u0026\u0026 npm test` — both pass with the rename.\n","status":"closed","priority":4,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:11:56Z","created_by":"Alex Marshall","updated_at":"2026-05-10T21:59:09Z","started_at":"2026-05-10T20:09:18Z","closed_at":"2026-05-10T21:59:09Z","close_reason":"Shipped in PR #53 (merge commit 63da7a6, v1.6.2)","labels":["pr-53"],"dependencies":[{"issue_id":"stellata-9mm.155","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:11:56Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.121","title":"[PR #38] 1.2.0 → 1.2.1 patch bump for a user-visible behaviour change","description":"**Confidence: Medium — needs Alex's input**\n\n**Finding**\nRELEASING.md states:\n\u003e **Patch** — bug fixes, copy tweaks, dependency bumps with no user-visible behaviour change.\n\u003e **Minor** — new user-visible features (rendering modes, overlays, data sources), backward-compatible.\n\nPR #38 ships a behaviour change to the no-URL bootstrap camera default (~0.11 AU → 4 AU from Sol) — a noticeably different first-load experience for any visitor without a shared `?v=` link. The PR also rewrites the About / Welcome / Credits modals around an \"empirical-model\" framing and adds a `docs/solar-system.md` topic doc + a SCIENCE.md \"Solar system\" section + four new Credits entries. The bump is `1.2.0 → 1.2.1` (patch).\n\n**Location**\npackage.json: `\"version\": \"1.2.1\"` (was `\"version\": \"1.2.0\"`)\n\n**Suggested approach**\nPer the project's own semver policy, this looks like a **minor** bump (1.3.0). The first-load camera default is the most user-visible change Stellata has shipped since 1.2.0, and the framing rewrite touches every intro surface. Worth a retrospective note (\"intended patch — copy + camera default within the same release window\") or, if a future similar PR comes up, recalibrate against the policy. No code change required for this bead — it's a process / policy-compliance flag.\n\n**Why**\nRELEASING.md is the single source of truth for what each bump means; deviating once trains the next session that the policy is loose. The `release-notes-guard` workflow only checks the section is non-empty — there's no automated semver-vs-diff check, so the discipline is purely review-time.\n\n**Smoke step**\nNone — already shipped as 1.2.1. Action item is to confirm the policy reading with Alex; if minor was intended, document the retrospective decision.","status":"closed","priority":4,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-10T14:10:34Z","created_by":"Alex Marshall","updated_at":"2026-05-10T14:49:20Z","closed_at":"2026-05-10T14:49:20Z","close_reason":"won't fix — 1.2.1 already shipped, version is immutable in deploy.yml-driven release tags. RELEASING.md classification (patch vs minor) was misapplied for PR #38's behaviour change but there's no retroactive remedy. Going forward, reference stellata-rename-and-stale-prose-sweep memory which mentions reading RELEASING.md before picking version on behaviour changes.","dependencies":[{"issue_id":"stellata-9mm.121","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T15:10:34Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.99","title":"catalog-pure.ts interpolate() comments out unreachable branch instead of asserting","description":"**Confidence: Low — needs Alex input**\n\n**Finding**\nPR #47's catalog-pure.ts:122-125 added a comment that the post-loop `return table[table.length - 1][1]` in `interpolate()` is \"unreachable for in-range subclass keys\" but kept the dead-code path as-is. The bead description (stellata-nqb) explicitly offered two options: \"comment as such or assert\" — comment was chosen. Since the input contract is enforced only by the parser's `^(\\d)` regex (which extracts a single digit 0-9) plus tables that span [0, 9] inclusive, a future change that widens the input range — say, supporting half-integer subclasses or a non-MS table that stops at key=8 — will silently fall through to the high-end clamp instead of crashing loud.\n\n**Location**\nscripts/catalog-pure.ts:113-126\n\n**Suggested approach**\nEither:\n1. Replace the post-loop return with `throw new Error('interpolate: key ' + key + ' outside table range');` — caller contract violations crash in tests rather than emit silent garbage.\n2. Or short-circuit the equal-to-last-key case at loop top (`if (key \u003e= table[table.length - 1][0]) return table[table.length - 1][1];`) — formally documents the \"out-of-range clamps to high end\" semantics and removes the unreachable-with-comment.\n\nOption 2 is cheaper (no test fallout) and matches the comment's \"clamps anyone who breaks the contract\" intent.\n\n**Why**\n\"Unreachable\" comments age badly when the upstream contract changes and nobody reads the comment. An assertion or explicit clamp would catch the contract violation at its actual source. Judgment call on whether crashing or clamping is the right behaviour, hence Low.\n\n**Smoke step**\nRun `npm test` — the existing catalog-pure.test.ts coverage of physicalRadius / tempKelvin / boloCorr exercises every classIdx 0-8 with subclass 2/5/9 and the `?? T_TABLE[8]` fallback. No new test required; existing suite proves option 2 doesn't change behaviour on the valid path.","status":"closed","priority":4,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:43Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:46Z","started_at":"2026-05-11T07:56:33Z","closed_at":"2026-05-11T08:46:46Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-47"],"dependencies":[{"issue_id":"stellata-9mm.99","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:42Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-9mm.91","title":"Histogram caption hard-codes 'last 60f' — was dynamic, regresses first-second accuracy","description":"**Confidence: Low — needs Alex's input**\n\n**Finding**\nThe pre-PR `renderHistogram` produced its caption with the live ring count: ``frame.total · last ${N}f · 16.7ms ref``. PR #41 moved caption creation into `ensurePanel()` (build-once, never updated), hard-coded as ``frame.total · last ${RING_SIZE}f · 16.7ms ref``. For the first ~1 s after install (and after a chart-mode toggle, etc.) the ring isn't full — the histogram shows N\u003c60 bars but the caption claims \"last 60f\". Minor accuracy regression.\n\n**Location**\nsrc/client/perf-hud.ts:223-227 (build-once caption with `RING_SIZE` literal). Compare to deleted `renderHistogram` at the bottom of the pre-PR file which produced ``last ${N}f``.\n\n**Suggested approach**\nTwo options: (a) keep the build-once caption but accept the inaccuracy (simplest, claim it's a documentation nit). (b) Add a third cached node `captionEl`, mutate `captionEl.textContent = `frame.total · last ${N}f · 16.7ms ref`` once per tick from renderPanel — N is already in scope from the histogram block. Cost: one branch + one textContent write per tick (~5 Hz), with an obvious dirty-track if it matters (`if (lastN !== N)`). Whichever option, also re-check whether the trailing 60-N bars should still render at zero (current behaviour) or be hidden — the caption regression is a hint that the original intent was \"show actual count, hide the rest implicitly\".\n\n**Why**\nJudgment call — at 5 Hz refresh the discrepancy lasts \u003c1 second after install. Worth a fix only if the intent is HUD accuracy across all reading windows. Filing because the regression was incidental to the DOM-cache rewrite, not deliberate.\n\n**Smoke step**\n`debug.panel()` immediately on page load, expand Perf section, observe the \"last Nf\" string ramps from 1 → 60 over the first second.","status":"closed","priority":4,"issue_type":"task","assignee":"Alex Marshall","owner":"git+claude@alxm.me","created_at":"2026-05-10T10:05:27Z","created_by":"Alex Marshall","updated_at":"2026-05-11T08:46:45Z","started_at":"2026-05-11T07:56:33Z","closed_at":"2026-05-11T08:46:45Z","close_reason":"Closed via PR #58 (9mm code-quality: low-hanging fruit) — merged 2026-05-11","labels":["pr-41"],"dependencies":[{"issue_id":"stellata-9mm.91","depends_on_id":"stellata-9mm","type":"parent-child","created_at":"2026-05-10T11:05:26Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"stellata-5nh","title":"Future: research higher-resolution per-cloud density datasets to supersede procedural substructure","description":"**Goal:** once the physical MC rendering epic (`stellata-c7u`) lands using procedural substructure, look for higher-resolution per-cloud data products that could replace or augment the procedural noise with real measured/derived density structure.\n\n**Why this is deferred:**\n- Procedural substructure is sufficient for the v1 physical MC implementation\n- Most public catalogues for the Zucker sample stop at bbox/sphere granularity\n- Per-cloud filament catalogues, dust column-density maps from Herschel Hi-GAL, ALMA observations of nearby SF regions exist but vary in coverage and format\n- Worth doing only after the procedural baseline is in place, so we know exactly what the procedural approach is missing\n\n**Candidate data sources to evaluate:**\n- Herschel Gould Belt Survey filament catalogues (Arzoumanian et al., André et al.)\n- Hi-GAL column-density maps\n- ALMA / NRO surveys of individual nearby clouds\n- 3D dust extinction maps at higher resolution than Edenhofer (if any post-2024 successors emerge)\n- Per-cloud studies cited in the Zucker 2020/2021 papers themselves\n\n**Output (if pursued):** a build-time data path that, for clouds with available high-res data, replaces the procedural substructure with real structure. Procedural fallback for the rest.\n\n**Priority:** low (P4) — pure enhancement, only attempted once the epic ships.","status":"open","priority":4,"issue_type":"task","owner":"git+claude@alxm.me","created_at":"2026-05-02T17:38:44Z","created_by":"Alex Marshall","updated_at":"2026-05-02T17:38:44Z","labels":["future","ism","molecular-clouds","research"],"dependencies":[{"issue_id":"stellata-5nh","depends_on_id":"stellata-c7u","type":"relates-to","created_at":"2026-05-02T18:39:17Z","created_by":"Alex Marshall","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
